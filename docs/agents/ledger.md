# AGENTS 分册 · 结论台账（已确证 / 已否证 / 未决）

> **来源**：原 `AGENTS.md` §4 / §5 / §6（2026-09-18 拆分，**逐字未改**）。
> **节号保持不变** ⇒ 历史文档里的「`AGENTS.md` §N.M」引用经索引一跳可达。
> 返回索引 → [`../../AGENTS.md`](../../AGENTS.md)

> 本册内容：C1–C28 / X1–X19 / O1–O13。

---

## 4. 已确证（可作为前提引用）

| # | 结论 | 关键数字 / 证据 |
|---|---|---|
| **C1** | **负 EV 的根因是 GRU 输入饱和**（不是奖励尺度、不是价值头结构） | `enc=relu(enc_fc(fused))` 范数 **533**（`grid_feat` 常分量 468、跨帧 std 1.06）→ GRU tanh 候选饱和 `n_abs=0.994` → h 跨帧 std **2.6e-5** → critic 恒常数，`EV_global −0.5817` 与恒等式 `−bias²/Var(R)=−0.584` 吻合。修复 = `enc_ln`（follower.py）。诊断 `../_archive/value_channel_saturation_diagnosis_2026-09-11.md`。 |
| **C2** | **饱和修复在 100k 尺度耐久**（v3 的正面确认） | 100k 全程 `h_std` 0.067→0.095（>0.05）、`n_abs` 0.667→0.826（<0.9）；100k 结构下无一个点越过门槛。证据 `../_archive/d1_long_100k_verdict_2026-09-13.md` §4。 |
| **C3** | **但 critic 仍不拟合，且加训练量救不了** | EV 仅前两点为正（+0.234/+0.385），其后 **9/11 点 ≈0 或负**；`vstd/rstd` 30k 后长期 **≈0.001**；run 自身 8 次 vitality 告警。⇒ **"加量救 critic"被 100k 直接否证**（【否证 X4】）。**⚠️ 2026-09-13 更正**：本条原来引用的 20k 基线"0.0035~0.0225"是**错的**——脚本复算 3 个 20k 基线末点为 **0.2473 / 0.0041 / 0.1648**（R4 类，禁止再手抄）；**方向不变**（100k 的 ≈0.001 仍低一个数量级）。另：`_r2` 的 critic **在 20k 之内就死了**（12500 起 0.0007~0.0084），而 r1/r3 到 20k 仍活 ⇒ 塌缩是**双稳态吸收态**，不是步数的确定性函数。 |
| **C4** | **自对弈 cycling 确凿** | RPS 三角：main 打冻结副本 0.85 / 打训练起点 0.13 / 打全新随机 0.505；锚点长期低位甚至整段 0.000；对照曲线相邻点可差 0.6+。取证 `scripts/_forensics_cycling.py`。 |
| **C5** | **D1（对手分布去镜像化 + 动态历史自身联赛 + PFSP 门禁）消除"整段输给随机策略"的相位** | 20k 三跑最差锚点 **0.125/0.200/0.250** vs 无干预 **0.000/0.000/0.025**（区间不重叠，Welch t≈4.9）；机制指纹：镜像自对弈 40%→**6.6%/13%/~13%**、本 run 快照 1→9 进池。`../_archive/d1_league_20k_verdict_2026-09-13.md`。 |
| **C6** | **D1 的防崩在 4× 训练量（100k，≈374 局）下成立** | P1 块 worst **0.388/0.475/0.275/0.237/0.525**（mean 0.380、min 0.237）vs 无变化组 [0.000,0.025]；与 D1 20k 区间 [0.125,0.250] 比 **min 0.237 落在其中 ⇒ 无统计差别**（不退化，也没证明改善）。`../_archive/d1_long_100k_verdict_2026-09-13.md`。 |
| **C7** | **评估节奏不能均匀放宽** | 锚点谷底只 1 个点宽；真实序列粗采样回放：5000 步即开始漏真谷底、10000 步把"最差点"从 0.192 抬到 **0.462**（病态组持续塌陷留得住、健康组周期性瞬态留不住）。⇒ 用"密锚点 + 稀全块"（C 方案）。`../_archive/eval_cadence_c_2026-09-13.md`。 |
| **C8** | **critic 的失败是"状态→回报"层面的**，不是表征没信息 | 按局分组留出后探针 R² 为负（MLP −0.36/−0.45）；监督微调同网络 test EV 可到 +0.22~+0.29 而 PPO 联合训练 ≤0 ⇒ 结构够用、主因在训练过程（这一条已被后续"critic 侧闭合"限定，见【否证 X6】）。 |
| **C9** | **行为链三层有真实爬坡**（winrate 平台期下） | 防守响应率 38.1%→**61.5%**（v2 通道）；接敌率 7.4%→**10.5%**、中段带部署占比 51%→59.7%、追尾 64%→31%（9k 拦截几何，提交 94ce339）；单边堆牌 45.8%→9.5%。 |
| **C10** | **词表/卡池/工具层的事实** | `ENTITY_NAMES` 13→**177**（旧 13 位序冻结，行拷贝兼容）；belief token 71→563；`PLAN_DIM` **58**（hint 8 维）；`build_card_pool` 塔类过滤唯一正确口径 = `n.startswith("King_")`；外置工具①②③ 已落地并有引擎对账 selftest，④ 未实现。 |
| **C11** | **价值通路的断崖是共享编码器的线性投影 `enc_fc`（2731→128）；两处 LayerNorm 都不是病因** | **真·同张量 + 整条 α 曲线**（`scripts/probe_v4_ln_pair.py`，离线重算对账 `max\|Δ\|=4.3e-06`）：`P-PROJ`（每 α 上 `EV(z_enc) ≤ 0.5·EV(fused)`）**26/27** 个 α 值成立（seed 7=9/9、11=9/9、13=8/9）；`P-LN` 只 **1/27**；价值支路 `post_ln/relu_ln` 在分母有意义处 **0.78~1.64** ⇒ 第二轮「`value_enc_ln` 不是掉点层」复现并升级 |
| **C12** | **训练出来的共享投影 ≈ 随机投影** | `z_enc/z_rand`：seed 7/13 ≈**0.9~1.0**；seed 11 上随机**更好**（+0.0443 vs +0.0209）⇒ 该投影对"回报线性可读性"无贡献，**与【R14】权重位移指纹互为独立证据**。⚠️ 不等于"权重随机"（`enc_fc` 在策略目标下确被训练） |
| **C13** | **中间张量可离线精确重算**（"没抓到"不再是障碍） | 用 ckpt 权重从 npz 的 `X_fused` 重算 `enc_fc→ReLU→enc_ln`，与网络实抓对账 `max\|Δ\|=4.3e-06`、`EV` 差 2.2e-10 ⇒ 正确性由对账保证。**前提：该层是已抓张量的确定函数** |
| **C14** | ★ **「放弃一张买得起的牌」在 10 万步自对弈里发生 0 次**（穷举，两臂一致）⇒ **攒费不可能被学到**（0 样本 ⇒ 0 梯度），且**与训练无关**（step 0 与 100k 同结构） | 新仪器 `scripts/pass_streak_audit.py`（只读录像；三类 A 主动不出牌 / B 整包被拒 / C 实际出牌；分类判据是**会计恒等式**「圣水只可能因回费上升」，不是启发式；仪器自检：A 类却花掉圣水的帧 = **0**）。A_et **75,891 帧**、B_ctrl 79,975 帧：**7,238 段「主动不出牌」里 0 段是在圣水 ≥4.0 时仍不出**（`max y = 4.000000`，25 段恰好到、**0 段超过**；卡组结构保证手牌最便宜那张 ≤4 费 ⇒ y≥4 必有可出）；出牌帧「圣水 − 所打最便宜卡费」中位 **+0.137**（< 1 帧回费 0.1786）、**0 段 >1.0**；A 段最长 **22 帧 < 34 帧**（攒到 Xbow 6 费所需；回费实测 **0.3571 圣水/s**）。三类占比在 14 个评估点**无趋势**（step 0 = 91.5%/0.0%/8.5%）。⚠️ 附带发现新病理 **O9**。`../_archive/elixir_saving_audit_2026-09-18.md` |
| **C15** | ★ **用 FirstLight 的 IL_Replay 做模仿学习（IL）在本仓可行**：管线三件套 + **47,715 条**人类标签 + 留出 8,877 | 数据 = HF `VanguardX101/IL_Replay` 分片 0（5000 局；FL `docs/training.md:11` 点名该集）。新仪器：`scripts/_fl_il_extract.py`（parquet→JSONL+普查，**必须 WSL python3**：Windows venv 无 pyarrow）、`scripts/fl_il_to_bc.py`（回放→`(obs,masks,bundle)`，两处**显式假设**：手牌强制 8.83% / 圣水强制 2.18%）、`scripts/il_eval_holdout.py`（留出判读，本仓**原先没有** BC 留出仪器）。**J1.1 卡牌经济对账（外部真值）= 99.72%**：双侧 3518/3528 逐圣水相等（我方侧 3528/3528、总额 **359,456 == 359,456**）；残差 10/3528 **全含 `Mirror`**、成因未定。★ **仪器口径修正**：`Mirror` 实际费 = **上一张牌费用 + 1**（`rl/action_mask.py:30-36`）且 `Mirror` 不覆盖 `last_card`（`player.py:47`）⇒ 第一版用卡面费时 **24/24 残差侧全含 Mirror、0/7044 无 Mirror 侧有残差**；**修正后 99.32%→99.72%，责任在仪器不在引擎**。另两条外部对账全对：payload 事件数 vs actions 行数 **4997/4997**、native 坐标 **6530/6530**。J2：`bundle_not_ok = 0/50,244`（引擎零拒绝）。★ 限制：**2200/2200 局**被我们引擎**提前 game over**（`end/dur` 中位 **0.822**）⇒ 标签偏早期（中位 104 s）。判读 [`../../docs/fl_il_2026-09-20.md`](fl_il_2026-09-20.md)、预注册 `fl_il_prereg_2026-09-20.md` |

---

## 5. 已否证 / 已关闭（**勿重走**，除非有新证据）

| # | 已否证的事 | 依据 |
|---|---|---|
| **X1** | F′ 首跑训练期 EV **+0.198** 是 in-sample 假象 | 修正口径后 **+0.031**（降 6.4×）；同权重离线 50 局 `EV_global` 为负。`../_archive/fprime_rerun_20k_verdict_2026-09-13.md` |
| **X2** | "表征含 24~42% 可预测信息、critic 吸收不了" | **逐帧随机留出 = 时间泄漏**（相邻帧 corr(R)≈0.99）；按局分组 R² 全负；换标签/打乱地板救不了。 |
| **X3** | 价值头架构四代（G'-fix 塔血通道 / B′ value 直连 enc / E′ 独立价值编码器+非线性头 / 重量级 bypass） | 训练期 EV 与离线 EV 全部 ≤0；探针复现显示 G'-fix 的 `enc→塔血差 +0.69` 是**通道接上了**，但不是 EV 改善。 |
| **X4** | "加训练量能救 critic" | 100k：EV 9/11 点 ≈0 或负、`vstd/rstd` ≈0.001（更差）。见 C3。 |
| **X5** | E2（把固定随机锚点按 10% 放进训练对手池）能破 cycling | 20k 末点 vs `baseline0` 0.075、vs `baseline_rand` 0.050，RPS 仍在转；10% 弱锚点压不住自对弈漂移。 |
| **X6** | **critic 侧程序已按预注册闭合**（用户预授权：无进展就转策略侧） | 三步（测量口径修复 → 单变量 A/B → 复跑对比）全部"无进展"⇒ 不再往 critic 加投入；转策略侧（对手分布 / cycling / 上限）。 |
| **X7** | 两条预注册判据因**基线手抄**失去区分力 | 20k 的 P1/P2（F′ 末4均值 0.400 vs 实际 0.588）与 C2（1/3 vs 实际 2/3）⇒ 立 **R4**。 |
| **X8** | early-stop 低置信裁定"降噪 28~40%" | 那是 eval 侧统计口径混入；训练侧实测早停率 **3%**。 |
| **X9** | `gates.json` 的绝对阈值判据 | 阈值 9.5 来自一次性脚本，对同一批权重实测 28.3~38.1（差 3×）⇒ 已改**相对本 run 首点**（engagement ≥50%×起点、ghost ≤2×起点）。 |
| **X10** | 「每评估点重打同样 40 局 ⇒ 锚点是权重的确定性函数」（原 `AGENTS.md` §3.1 / `eval_cadence_c` §5） | **实测 11 个评估点 11 种不同首局牌序**。`RLEnv.reset()` 是**原地链式洗牌**（`env_wrapper.py:349-357`：`deck0=list(self.deck0)` → `shuffle` → `self.deck0=deck0`），而并行评估 worker 的起点是 `env_kwargs["deck0"]=list(env.deck0)`（`train_solo.py:968`）=**父训练进程被反复覆盖后的当前牌序** ⇒ 固定种子只固定了 40 局之间的相对偏移，没固定起点。**后果**：`1σ=0.078` 有效（判据仍可用），但相邻点差分 SE=`√2σ=0.110`，故"|Δ|≈0.225≈3σ"应读作 **≈2.05σ**；C7 的"谷底 1 点宽"方向不变。见 `../_archive/d1_long_100k_cause_analysis_2026-09-13.md` §2.4。 |
| **X-17** | **「共享感知前端丢掉约 2/3 线性可读局内信号」**（v1/v2 两轮主结论，曾被标为"已复现"）——**推翻**。病理 ⑫「逐层阶梯用**各自选超参**的读数互相比较」：`fused` 前 2560 列**逐位就是** `grid_ln_out`（信息不可能因拼接而消失）；真因是**单个全局 α 服务不了两个条件数区**（`raw_obs` 需 α≈1e4、低 α 崩到 **−0.31**；网络特征需 α≈1~10）| 超集**逐位**验证（`np.array_equal`，`max\|Δ\|=0`，3/3 seed）+ **同 α** 对账：各自 α* 下 `fused − grid_ln_out` = **−0.059/−0.042/−0.032**，同 α 下塌到 **≤0.010**（seed 11 反号）⇒ 立 **【R17】** + **闸门 11/12**。`../_archive/value_ln_probe3_verdict_2026-09-14.md`、`docs/value_ln_probe3_mono_summary.json` ★ |
| **X-18** | **「`R0/E0 ≥ 2` 是可在多 seed 复现的比值判据」**（R16 曾把它当**正面**样板）——**推翻**。病理 ⑤ 的更深一层：那三条轨迹里**两条未播种**（不可复算，见 X-16）⇒ **"分布"本身是伪精度**（极差 0.060 是假的）| 用 **3 条已播种可复算**轨迹重测：ρ(fused) = **0.279 / 0.355 / 0.585** ⇒ 极差 **0.306**、**阈值 0.5 落在分布内部** ⇒ 按预注册 §5 判 **G-RATIO FAIL ⇒ `RATIO_WEAK`**（判据作废）。同一批数据顺带给出 X-17 的决定性证据。同上 ★ |
| **X-19** | ★ **「按局面结算的圣水交换信用分配」（用户的"给帧打分"最终形态）——已完整取证、判【不接线】** | 全程零训练成本取证，链见 `../_archive/s2_instrument_2026-09-18.md`、`../_archive/engagement_trade_online_2026-09-18.md`、`../_archive/online_measure_2026-09-18.md`。① **预注册首选 P3 被推翻**：`τ ≤ kill_credit` 恒成立、`τ==kill_credit` 占 75–89% ⇒ 它只是**已有 Φ 击杀项的重新加权**，不是新信息；② **P1 否决**（`τ` 均值 388–431 = `ΔΦ` 量级的 ~200 倍、ρ(胜) **5/5 批为负**、门禁兑现率只 80.7–88.6%）；**P2 否决**；③ 自构造的 **P4b** 是唯一候选：**离线口径**同批配对 Δρ(塔血)=+0.145、Δρ(胜)=+0.108（5/5 正，跨口径 10/10）——**但**④ 换成**奖励项真正会用的口径**（tick 级、K=30 tick，靠 measure-only 预设 `economy_etm` + 250 局实测）后掉到 **Δρ(塔血)=+0.008、Δρ(胜)=+0.014**（÷18 / ÷7.7），且**基准翻号**（`ρ(none,塔血)` 在线 +0.084 vs 离线 −0.198）⇒ **"配对 10/10 正"是帧采样切分口径的产物**；⑤ `τ` 太稀疏（`Στ/Σφ ≈ 1/12`、仅 ~38% 对局 `τ≠0`）⇒ 代理本身维度不足；⑥ 门禁（优势兑现率）在 run 模式 99.0–99.7%、`p1` 80.7–88.6% ⇒ **该判据在 run 模式无分辨力**，须换 `solo`。**⑦ 第五轮补上"尾部窗口"（局末/截断时仍开着的局面，占窗口总数 **8.0%**、个别局尾部 φ 达 −129）后重测**：Δρ(塔血) **+0.000**（3/5 正）、Δρ(胜) **+0.001**（**2/5 正**）⇒ **幅度归零、方向也不再稳定** ⇒ 判决由"证据不足"升级为**已否证**（奖励真正会用的口径下效果为零）。**⑧ solo 口径复核（预注册 §8 强制要求的口径，160 局，目标导向 hist 池）**：`τ` **自身** ρ(τ,塔血)/ρ(τ,胜) **4/4 批为正**（均值 +0.250/+0.257）且与 `φ` 大体不冗余（ρ(τ,φ)=+0.106）——**全调查唯一 4/4 一致的正信号**；但**按本方案的定义**把它加进 `Trade` 的增量 Δρ(λ=1) 只有 **+0.025/+0.022**，λ 放大到 10/30 均值升到 +0.08 却**出现批内反号**（−0.125/−0.162），配对 bootstrap 的 95% 区间**全部跨 0** ⇒ 精修判决为「**τ 有信号，但本方案的写法（加进 `Trade`）增量低于一切可用仪器的分辨率**」；按 §5.3 第 2 条**不启动 S3**（20k 单跑按【R5】只能看大效应）。**出路（都不在本方案范围内）**：O7 修机制，或把 `τ` 做成**独立**奖励项/价值头输入（新方案，须另行预注册）。**附带确证**：冰人白嫖推论**证实**（102/102 实例 `ΔΦ = −2.000000` 零散布，`edw=0.5` ⇒ −1.0，须补前提 t<120 s）。**工程产物（可复用，保留）**：`scripts/offline_engagement_trade.py`（离线仪器 + 8/8 不变量）、`rl/engagement.py` + `RLEnv` 接线（**默认关**、开/关逐位对账 PASS）、schema **4→5**（`root_cast`/`is_product`/`share` + 帧内 `v0/v1`）、`scripts/analyze_online_trade.py`、`scripts/s2_trade_probe.py`、`scripts/s2_neutrality_probe.py` | 取证 `../_archive/engagement_trade_prereg_2026-09-18.md` §11（预注册 + 六轮修订）| 

---

## 6. 未决与候选下一步

| # | 未决问题 | 现状 | 候选下一步（含判据设计要点） |
|---|---|---|---|
| **O1** | **上限 / 绝对强度**没动 | D1 20k 与 100k 在最差锚点上无差别；次判据（块中位数末−首）口径敏感（+0.125 或 +0.062）⇒ 按预注册**不判决** | ①**D2 = `rand_anchor` 0.1→0.3**（剂量-反应，单变量）；②若要判"上限"，先设计比"滚动均值"更稳的统计量（建议：**块内中位数 + 多跑聚合**，且基线脚本复算）；③注意**固定随机锚点目前是唯一能测绝对强度的仪器**——run 内自引用指标（打冻结副本/打上一评估点）全部失真（§3 判读禁则） |
| **O2** | **critic 拟合** | **机制已定位（2026-09-13）**：**价值头隐藏层对几乎全部帧输出恒零**（`value_enc_fc`/`mlp0`/`mlp2.weight` 在 35k→100k 的 22~24/27 个窗口**位移恰为 0**＝梯度精确为零，而 `mlp2.bias` 每窗都动）⇒ 价值函数 = 常数。第一性原因：价值编码器末端 `LayerNorm` 把近常数输入归一化 ⇒ 价值头可表达跨帧方差 ≈0.14 而目标 std≈12.11（差 60~600 倍）⇒ 自举失败 ⇒ ReLU 逐个死亡锁死（吸收态，**双稳态**：r2 在 20k 内就死、r1/r3 到 20k 仍活）。**已排除**「梯度小/被 `1/s²` 压制」（【R14】）| **惰性检验（Layer 1，2026-09-13）**：`grad_cos` 中位 **0.9973**、`resid_frac_norm` 中位 **0.0415**、`grad_norm_ratio` 0.994（145 个诊断更新）⇒ P1 成立，但 **⚠️ 同日复核否掉了「该 run 的 critic 没塌」**：**末点 critic 就是常数**——`scripts/_probe_value_path.py`（600 帧）实测 `value` **唯一值 1/600**（= `value_head_mlp[2].bias` **+0.1425485**，`std=0`）、`value_head_mlp[0]` 的 ReLU **逐帧存活率 0.000000**（64 单元全死，R14 吸收态完全体）⇒ **V≡常数时 `resid≈0`/`grad_cos≈1` 是恒等式**，不能读成「拟合良好的 critic 也只有 4%」⇒ **「关闭 critic 线」的依据作废**。另有两条口径更正：①`grad_cos≈1` 是**期望零假设**（任意状态函数基线都不改期望方向）⇒「修 critic 收益上界=几个百分点」**只在 `adv_norm="scale"` 下成立**；②逐批 `EVb` **145/145 为负**（中位 −3.149）而窗口池化 EV=+0.3003 ⇒**池化 EV 不是状态级价值质量指标**，判信用分配必须看**批内 EV / 优势形状残差**（该读数把只有 2 个评估点的 `ev_pairs` 整个窗口池化 ⇒ 活/死相位被平均）。判读 `../_archive/critic_inertia_verdict_2026-09-13.md` §0.1。**信息侧天花板（两轮已判）**：v1 **P0（不判决）**（三个估计器变体不一致）；v2 照预注册读出 **P3（H-LEARN）**：`EV_within` A **+0.182**/C **+0.186**/B **+0.197**（Ridge 同特征 **+0.30**），`B−C=+0.011<0.08` 且 `C≥0.15`；四道对照全过（阳性 +0.6707、时钟 +0.0144、状态打散 +0.0073、目标打散 −0.0026、重跑逐行一致）⇒**信息不是缺口：局内回报线性可预测而 critic 拿到 0**。**P3′**：`C−A=+0.0039<0.05` ⇒ **belief token 无增量**⇒「挂起 bf 再跑」期望收益封顶 ~1pp，该消融取消。`../_archive/pomdp_ceiling_v2_verdict_2026-09-13.md`。**价值阶梯三轮（2026-09-14）全部作废但换来更强读数**：v1 `L0_INVALID`（【R15】）、v2 `V2_INVALID`（【R16】）、v3 `V3_INVALID`+`RATIO_WEAK`；**主结论「前端丢 2/3 线性可读信号」被推翻【X-17】**（`fused` 前 2560 列**逐位就是** `grid_ln_out`，`max|Δ|=0`，3/3 seed；真因 = 单个全局 α 服务不了两个条件数区：`raw_obs` 需 α≈1e4、低 α 崩到 −0.31）⇒ 立【R17】+闸门 11/12；「三轨迹复现」是**伪分布**【X-18】（两条未播种；实测 ρ(fused)=0.279/0.355/0.585，阈值 0.5 落在分布内）。**修正后唯一三重受控读数**（同 α+字面父子+3/3 seed）：`enc=enc_ln(relu(enc_fc(fused)))` 每个 α/每条 seed 都读不出局内信号（≤0.019）而字面输入读 0.082~0.198 ⇒ **2731→128 共享编码器瓶颈是最强掉点处**；`grid_ln` 中性 ⇒ 该线关闭。v4 用真·同张量把断崖定位到 **`enc_fc`** ⇒【C11/C12/C13】、`../_archive/value_ln_probe4_verdict_2026-09-14.md` |
| **O3** | **行为病理未修完** | `elixir_avg` 仍低（不会攒费）；`setup_wait` 冷启动死锁已定位但**两阶段状态机未实现**；单边堆牌末点 100%。**✅ 2026-09-14 已量化（新仪器 `scripts/forensics_card_usage.py`，只读）**：一副 8 卡牌组的唯一赢牌手段 **Xbow 出手 3/9806（0.03%）**、5 张费 ≤3 的便宜卡占 **95.47%**、**部署前圣水 ≥6 只占 0.02%**、**全帧（含不出手帧）圣水 ≥6 只占 0.11%**（100860 帧里 111 帧）、同路部署间隔中位 **5.5 s**（单文件 8.0 s）。**同时当场推翻一条跨三手复用的旧口径**（`y≥20` 的"幽灵动作/引擎未执行"——实际那 452 次是被引擎执行了的**合法进攻法术落点**，见【否证 X-15】）。判读 `../_archive/replay_behavior_forensics_2026-09-14.md`。**✅ 同日的因果链合成（脚本复算 §7）**：Xbow（**6 费**）的**可达帧数上限 = 112 / 440 局**（全帧 `elixir≥6` 只占 **0.111%**；`≥4` 占 0.451%），实际出手 3 次 ⇒ **"从不打 Xbow"压倒性地是"永远没有 6 费"，不是"有 6 费不打"**；费 ≤3 的 5 张卡每张 **18.1%~20.5%**（近乎均匀）、费 4 各 2.25%、费 6 的 0.03% ⇒ 出手分布 = **"手里有什么便宜的就打什么"**。**"一攒到 3 费就立刻打掉"** 的**正确证据**是已精确对账的单子动作帧：部署**前**圣水（`post+卡费`，8804 帧）**中位 3.25 / p90 3.64**，而 `pre ≥6` 只占 **0.02%**（⚠️ 勿用"卡出手数 ÷ 圣水≥N 帧数"这类比率，分子分母不同样本集，R9⑤）。⚠️ **`BeliefPlanner` 不是手写策略**（`plan()` 只返回 PlanToken，动作仍由 RL 出）⇒ 要专家基线须另建 PlanToken 贪心执行器。**✅ 2026-09-18 穷举复核（新仪器 `scripts/pass_streak_audit.py`，A/B/C 三类）**：原方向成立且更精确——**「主动不出牌」时圣水从未达到 4.0**（**0/7,238 段**）⇒ **「放弃一张买得起的牌」在 10 万步里发生 0 次**（【确证 C14】）；有 6 费的帧确实存在（0.51%），但**几乎全是新病理 B 类（非法卡包被整包拒绝）的被动上涨**（见 **O9**）⇒ 「永远没有 6 费」应改读为「**从来没有为了攒而放弃任何一张牌**」 | 攒费链重构（不依赖血牛在手的窗口条件）+ 观测/plan 通道配合；判据用 gate 相对退化 + 接敌率。**新增可验收指标**：全帧圣水 ≥6 占比（起点 **0.11%**）、Xbow 出手率（起点 **0.03%**）。**下一步 = 用 `BeliefPlanner`（手写专家）跑同一副牌/同一批局做逐项对照**——那才是"是不是局部最优"的判据，零训练成本。**⚠️ 奖励侧候选已被排除（2026-09-14，W4）**：逐帧奖励精确拆成五项后**没有主导项**（crown 30.95% / edw 29.84% / tower 21.47% / terminal 10.11% / unit 7.63%）⇒ "奖励被资源账带偏"**不成立**；且双倍期切价被正面验证（edw 份额 32.5%→15.5%）。附带发现：塑形 `edw·[Φ(s')−Φ(s)]` 缺 `γ`，不满足 Ng et al. 1999 的 policy-invariance 条件，但偏差只有 **+0.0012/帧**（≈每帧奖励的 6%）⇒ 量级不足。判读 `../_archive/reward_composition_verdict_2026-09-14.md` |
| **O4** | 训练期 cycling 只"防崩"未"破除" | D1 让最差锚点不再归零，但 RPS 循环仍在（对照曲线大幅摆动） | D2 剂量；或提高 `_HIST_POOL_MAX`（12→24，长 run 的自身联赛更密）；或课程式提高 `defend` 权重 |
| **O5** | 同 seed 不可复现（机制未定） | 前 435 步逐位一致后 1e-4 漂移；`PYTHONHASHSEED=0` + 单线程 BLAS 无效（嫌疑 CUDA 非确定算子） | 属**方法学**问题：当前靠"多跑聚合 + 大效应判据"绕过；若要定位，需逐算子确定性差分（未做） |
| **O6** | **精确塔伤（触发式）已落地但默认关闭** | 旧近似闸门实测 BA **0.44~0.66**、4/6 组**劣于零信息基线**（`../_archive/threat_precise_probe_verdict_2026-09-14.md`）⇒ 改用引擎推演：`rl/threat_precise.py`（**上升沿触发 + 双向一次算完 + 保持寿命上限 20 s**），生产默认**关**（【R2】，关闭时逐位不变）。实测（镜像自对弈 3 seed）：触发率 2.1%~5.5%、摊销 **1.76~3.41 ms/帧**（预算 3.85）；**保持期内盘面变化一律不重算**（对方新放卡也不重算，347 帧事件 0 次推演）；保持时长中位 **10.5 s** 而推演视界只有 5 s ⇒ 加寿命上限 20 s（同轨迹配对 A/B：成本 **+0.26 ms/帧 / +6.8% 推演**、值龄 ≥20 s 占比 **5.0%→0.0%**、值龄 [20,40) 桶 BA **0.2500→0.5417**、值龄 <20 s 的四桶**逐位相同**、总 BA +0.0121（未检验显著性）；⚠️ 验收协议摊销跨 run 在 **2.90~6.23 ms/帧** 摆动 ⇒ **200 帧/seed 的成本判据分辨率不足**（【R5】））。翻转版 `my_pressure` 3/3 seed 提升（+0.008/+0.136/+0.191），**原方向 `threat` 1/3 seed 转差（seed 11 −0.240，未归因）** | ①**端到端训练 A/B（开关 on/off，20k）——改任何生产默认值之前的前置条件**；②seed 11 归因（嫌疑：退出时刻度跳变，134 次退出中 50.7% 保持值 ≈0）；③真实对手池下重测触发率/摊销（当前余量仅 1.1~1.75×） |
| **O7** | ★ **机制自锁：Xbow 卡组"永远没有 6 费"**（S1 门禁判"约束在探索/机制侧"的落点） | S1（`../_archive/s1_gate_2026-09-18.md`）：`token_strict` 全帧圣水≥6 = **0/8191**、Xbow 出手 **0/929**；而只多加一条**不在 PlanToken 语义里**的手写"攒费→Xbow"规则（`token_xbow`）就打出 **156×** 基线（47/1003，部署前圣水中位 6.25）⇒ **动作空间/掩码不是约束点**，约束在 **plan 通道 + 探索侧**。机制事实（脚本计数证实）：Xbow **不在** `ACE_CARDS` 也不在 `SINK_TANK_CARDS` ⇒ 唯一会置 `hold_mask` 的 `save_ace`/`setup_wait` 在 Xbow 卡组上 **0 帧触发**；`_pick_suggested_card` 只推荐**付得起的**牌，而 Xbow 要 6 费 ⇒ **自锁回路**。**★ 2026-09-18 第八轮（奖励侧干预 `economy_et`，100k 两臂）实测：约束没有被奖励侧改动打破** —— `A_et` 全帧圣水≥6 **0.52%**、Xbow **5/8769 = 0.057%**（阴性对照 `B_ctrl` = 0.05% / 1/8370），距手写专家的 **4.69%** 仍差 **~2 个数量级** ⇒ 与 X-19「奖励侧没有可学样本」一致；见 O8 与 `docs/et_solo100k_judgment_2026-09-18.md`。 | 候选下一步（**须单独预注册**，【R3】）：① 把 Xbow 一类"高费致胜卡"纳入 `ACE_CARDS`（或按卡费自动判定"攒费目标"），让 `hold_mask` 在**付不起时也触发**；② 修 `_pick_suggested_card` 的"只推付得起的牌"（应能推荐**目标卡**并抑制便宜卡）；③ 判据：同一副牌/同一批局，**全帧圣水≥6 占比**与**Xbow 出手率**相对起点（0.11% / 0.03%）的倍数，对照 = 同脚本 `token_strict` 与 `random` 两臂（【R4】禁手抄）；④ 注意这与奖励结算**无关**——在机制修好之前，任何结算侧改动都没有可学样本（X-19）。  **★ 2026-09-20（机制侧干预 intent-save，100k 两臂）追加**：约束的**位置**被挪动了一层 —— `J1 = count{≥34 帧保持 且 目标卡≥6 费 且 真打出}` = **0 ⇒ FAIL**，但**首次**让「为某张牌攒费」的帧大量出现（审计仪器 20 局：`set=2674`、**`held≈2,100–2,350` 帧 ≈ 37% 的帧**；对照臂 A 的同类量 = **0**）⇒ 失败从「帧从未出现」变成「**长保持从未完成**」（`fired_held` 最大 **4/6 帧**、`ready=17→fired=2`）。详见 **O11**。 **★ 2026-09-20 更正（同上勘误）**：前段那句的 `set=2674`、`held≈2,100–2,350 帧 ≈ 37%`、`fired_held 最大 4–6 帧`、`ready=17→fired=2` **作废**（判读仪器跑在错卡组，见 O11 勘误与 `docs/intent_target_attribution_2026-09-20.md`）；训练卡组上的正确读数 = `set=2853/2902`、`held=2932/2878（42.9%/42.2% 帧）`、**`fired=0`**（两 seed）⇒ 「**帧从未出现**」确实被打破（held 42%+）、但「**长保持从未完成**」更强（`fired=0`，**零转化**）。另 artifact 旁证：本 run 录像中 B 臂 **Xbow 部署 8 次**（A 臂 **0 次**）⇒ 「Xbow 永不出现」在 B 臂上**不再绝对成立**（但均非经 intent 路径）。 |
| **O8** | ★ **`economy_et` 100k 两臂干预：没有任何一项能用现有仪器分辨出疗效**（用户直接要求的第八轮长跑；**不构成判决**） | 判读 `docs/et_solo100k_judgment_2026-09-18.md`；读数 `docs/readout_et_solo100k.md`；原始仪器输出 `docs/_archive/readout_et_solo100k/`（含逐批兑现率）。① **机制层** 6 项里 **4 项不可分辨**（`level_shift`/`level_gap`/`gnorm_ratio` + `EVb`），另 3 项虽越界但绝对量极小（`grad_cos` 0.99730 vs 0.99800、`resid_norm` 0.12035 vs 0.08070、`corr` 0.99275 vs 0.99670）⇒ **不写"改善/恶化"**（§5.1 只写程序不写数字、**未定义方向**）；② **行为层** 全帧圣水≥6 **0.52% vs 0.05%**、Xbow **5/8769 vs 1/8370**、部署前圣水中位 **3.250 vs 3.286** ⇒ 差异停在**计数级**，两臂都**没有**把"攒费→打 Xbow"做出来；③ **门禁** 兑现率 **98.5% vs 98.2%**、逐批方向 **+6/−7/0** ⇒ 两臂都**贴上限、本层无分辨力**（但 A 的"打出塔伤"**低 4.1 pp**＝该病理的弱形式，留作候选）；④ **失败分支 1/2/4 全部"未触发"**（1 因前件不成立、2 因 A 不低于 B、4 因无降级）；⑤ 两臂各 **14/14** 评估点与回放、**0 降级**、配置逐字对称。**本轮不构成判决**：缺阳性对照（§5.2 无法判 `VALID`）+ **n=1 seed/臂** | ① **阳性对照** `C_pos = elixir_diff_weight 0.5→0`（§5.2 的**前置**）；② **2 seed/臂**（§8 原始要求）——这是把"差异"与"run 间散布"分开的**唯一**办法（§11.13.11 实测同配置单点可差 **0.60**）；③ **换门禁口径**：兑现率已饱和 ⇒ 无分辨力，候选 = "打出塔伤占比"（须**先标定阈值**，【R16】禁单观测）；④ 若要判"步数瓶颈"，先按 [`train_health_metrics_2026-09-18.md`](../_archive/train_health_metrics_2026-09-18.md) 预注册熵平台判据（并用 `ent_coef` 扫描去掉"正则平衡"混杂）；⑤ **不要在 n=1 下继续堆指标**——要加的是**判据的分辨力**，不是指标数量。 |
| **O9** | ★★ **新病理：模型会提交「非法卡包」被整包拒绝、白掉一帧**（B 类）——此前**所有**行为统计都看不见它 | A_et **497 帧 (0.7%)、涉及 39/280 局**，最长**连续 225 帧 ≈ 112.5 s**（单局共 180 s）；B_ctrl 84 帧 (0.2%)、最长 24 帧。**非法性 100% 可自证**（重复槽位 490 + 总费 > 决策圣水 7，**无法自证 0**）；B 段起始圣水**全 = 5.0**（**开局第一帧就卡住**，逐帧重复同一套包直到偶然采样到合法包）；每帧吃 `invalid_penalty = 0.05` ⇒ 225 帧累计 **≈ −11.25**（> 胜利奖励 +10），且**跨 9 个评估点仍在**（8000…100000）。⚠️ 与 **P2**（`P1-20` validate 通过但引擎拒绝）**不是同一条**（本类在 validate 阶段就必拒）；**成因未定**（【R10】）。**副作用（已污染既有读数）**：其圣水被动上涨被读成「攒费」——层 2「全帧圣水≥6 = 0.52%」**96% 是它**（375/390），「Xbow 5 次」**全部**发生在 B 类解锁帧（决策圣水 6.07~9.11） | ① **在 `act()` 采样点加不变量自检**：采样出的 bundle **必须过 `validate_bundle`**，否则大声失败；② 查录像记录器写的是**提交前**还是**提交后**的 bundle；③ 加**跨局边界**回归（【R8】）；④ **修好之前不要跑任何「攒费/探索」干预**（信号会被这 0.7% 噪声污染）。`../_archive/elixir_saving_audit_2026-09-18.md` §4。**✅ 2026-09-18 已定位并修复**：根因 = `RLEnv.get_action_mask_for` 里 `used` 存 **0-based** 下标却用 **1-based** `sa.slot` 做成员测试 ⇒ partial 出现「(s, s−1) 相邻降序对」时低槽位被整段跳过（掩码放行**已用槽位** ⇒ 提交重复槽位 ⇒ 整包拒绝）。**一行修复** + `act()` 两条**零成本掩码不变式**（已用槽位必须非法 / 被掩槽位不得被选中）+ 回归测试 `test_mask_partial_bundle_invariants`（判别力已证：换回修复前实现即失败）。同种子对照：掩码层 **44/256 → 0/256**、rollout 层拒绝帧 **474/1707（27.8%）→ 0/1609**；端到端（修复后产录像 → 同一台仪器审计）**B 类 = 0/3413 帧**。⚠️ 与「攒费」无关：修复后 **A ∩ 圣水≥6 仍 = 0 帧**（C14 不受影响）。全文 [`../_archive/mask_used_slot_offbyone_fix_2026-09-18.md`](../_archive/mask_used_slot_offbyone_fix_2026-09-18.md)。**★ 2026-09-18 追加：与之并列的 `P1-20`（validate 通过但引擎拒绝）首次被量化** —— 用户问「项目对非法动作的定义」触发四层取证 [`../_archive/illegal_action_layers_2026-09-18.md`](../_archive/illegal_action_layers_2026-09-18.md)：它与 B 类**不是同一条**（B 在 `validate_bundle` 阶段就必拒，`P1-20` 是 **L2 放行、L4 `deploy_card` 静默拒绝**）。真实读数（`docs/*.log` 复跑）：`P1-20` 共 **1,644 行、其中 BarbLog 1,644（100.0%）、其它卡 0**（`train_long1m` 561 / `train_run100k` 487 / `train_run_smoke` 274 / `train_et_measure` 165 / `train_et_measure2` 157）⇒ 与掩码缺口 **C2**（BarbLog 唯一「引擎有区域限制而掩码没有」的卡）**逐项一致**；`env_wrapper.py:686-692` 每次 `invalid_count += 1`（−0.05）并 `warnings.warn("P1-20: …")`。⚠️ 限定：1,644 是**日志行数不是独立事件数**（`train_et_measure*.log` 可能含 discarded 重跑）；**描述性、非判据**（【R3】）。 |

| **O11** | ★★ **intent-save（机制侧：`STOP` 尾部追加 `SAVE(slot)×4`+`CANCEL`，承诺期内把花费整块掩掉）A/B 各 100k** | 预注册 `../_archive/intent_save_prereg_2026-09-19.md`；判读 `docs/intent_ab_100k_judgment.md`、启动记录 `docs/intent_ab_100k_2026-09-19.md`。**唯一变量 = `--intent-save`**（入参层 `config.json` 与**产物层** `slot_head` 6→11 / `sub_emb` 8→13 / `enc_fc` 2731→2737 两处核实），两臂 `--fresh`、顺序跑、`eval-workers 8`（【R1】开跑前实测可用提交 10.51 GB < 12 GB 门，两臂同档）。 **① 主判据 J1 = FAIL（0 条）**：但**不是「轨迹从未被采样」**——机制被大量使用（`held` ≈ 37% 帧、`set=2674`），失败在**长保持从未完成**（`fired_held` ≤ 6 帧）与 **`ready=17→fired=2`**（攒够却不出）。 **② 三条预注册失败分支一个都没触发**（F1 前件 `fired=0` 不成立、F2 前件成立但兑现**确有**效果 `MiniPekka(4费) dmg_tower=206/kills=3`、F3 `set≠0`）⇒ 实际是**第四种情形**「机制被大量使用、长保持从未完成」= 预注册分支表的**设计缺口**（如实登记、不事后改定义）。 **③ ★ 绝对强度退化（同锚点配对，本 run 最重要负面）**：vs 固定随机锚点 A **0.500±0.112** vs B **0.100±0.067**（Δ≈0.40≈**5σ**），B 在 88k/96k/100k **三次**触发本仓 `<0.35` 警报；同时 B vs `baseline_prev` **1.000（20/0）** vs A 0.600 ⇒ 警报点名的「循环打转」指纹。 **④ 设计缺口（可复现）**：`ready` 一出现 `pending` 即被清、花费立刻恢复合法 ⇒ 目标卡在**转化那一刻不再被特权化**。 **⑤ J2**：A 自身 engagement 门终点 FAIL（0.0）、B 未 FAIL（42.7）——但该门禁是**相对自身 step-0** 的自标定、且本仓有**兑现率饱和**前科 ⇒ 不得当疗效判据。 **限制**：n=1/臂；审计仪器采样 RNG 未播种（两次运行 `held` 2118/2347、`fired_held` [2,4]/[6,0]，但 J1=0 与「最大保持 ≤6 帧」两次一致）。 | **候选下一步（须另行预注册）**：① **M2 时长 option**（`SAVE(slot, d)`：把「等多久」变成**一次**抽取，直接消掉 34 帧合取）——与 O10 第三问的「时长」结论同向；② 修 ④ 的设计缺口（`ready` 帧继续特权化目标卡 / `pending` 保留到真的打出）；③ 只在 ①② 之后才谈奖励侧（F2 的弱证据**反对**「奖励侧是瓶颈」）；④ 要分离「循环打转」与 intent 的因果，需**第二条对照**（同参数量但不放行 intent option）。 **★★ 2026-09-20 勘误（必读：推翻本条 ①②③ 的读数基础）**：本条 J1 读数**跑在错卡组上**——判读仪器 `scripts/audit_intent_save.py` 构造 `RLEnv` 时**不传 deck** ⇒ 落到 `DEFAULT_DECK_1`（原版 8 卡，**最高 5 费、无 Xbow**），而训练用 `DEFAULT_SOLO_DECK`（Xbow 6 费）⇒ ① 判据是**空判据**（`cost>=6` 按构造恒 0）② 被试 **OOD**。三层证据（源码 / 常量 / **录像产物**：录像里原版 8 卡 **0 次**出现，而旧 `fired_events` 报的是 Giant/MiniPekka）⇒ `docs/intent_target_attribution_2026-09-20.md`。**重测（同 ckpt、`--deck solo`、两 seed）**：`set=2853/2902`、`held=2932/2878（42.9%/42.2% 帧）`、`ready=11/6`、**`fired=0`** ⇒ **J1 仍 = FAIL（有效的 FAIL）**。⇒ **作废**：① 的 `held≈37%`/`ready=17→fired=2`/`fired_held≤6`、② 的「三条分支全未触发 ⇒ 第四种情形 = 预注册缺口」、F2 的「兑现有效（206 塔伤）」；**正确读数 = `fired=0` ⇒ 预注册 F1 触发**（处置跑前写死 = M2 时长 option）⇒ **分支表没有缺口**。★ 目标归因（新仪器 `scripts/probe_intent_target.py`，训练卡组 20 局）：`held` 帧 **61.1% 落在最贵的 Xbow（6 费）**、最便宜的 Skeletons（1 费）**0.2%**；**有真实选择**时选最便宜 **38.6%** / 最贵 **35.4%** ⇒ **不是「为最便宜的卡等待」**；真病灶 = **目标抖动**（74.8% 的段以「换目标」结束、段均 held **1.88 帧**、**攒够 457 段 / 打出 0 段**，其中 **448 段 ready 只维持 1 帧**）。**④ 的「ready 松手」设计缺口保留并加强**（451/451 转化丢失）。 |

| **C16** | ★★ **IL 的瓶颈不是「训练轮数 / 学习率」：0→3 epoch 的改善 92% 与选牌无关** | 预注册 `fl_il_scaling_prereg_2026-09-20.md`（判据**跑前写死**）；6 件仪器全新增（`scripts/il_bc_sweep.py` / `il_nll_decompose.py` / `il_paired_compare.py` / `il_ckpt_equal.py` / `il_curve_report.py` / `il_card_usage.py`），`human_play.py` / `follower.py` **一行未改**。**F0 强于阈值**：新跑 3-epoch ckpt 与已提交 `bc_fl.pt` **29/29 张量 `torch.equal`**、留出读数逐位相同（top1 `0.40419060493409936`、nll `5.596222589020527`）、配对 8877/8877 tie；**J-M6 跨进程确定性 PASS**（M2@3≡M0、M2@10≡M1 逐张量）。**NLL 三项分解**（不含 `follower.py`，复刻前向自带 1e-5 闸门，实测 9.54e-07）：0→3 ep 的 Δ3.0524 = **终止 1.5707（51.5%）** + 落点 1.2492（40.9%）+ **选牌 0.2325（7.6%）**；**3→10 ep 的 Δ0.1468 = 落点 63% + 选牌 37%**。曲线（留出 8,877，1/2/3/5/10 ep）：NLL 5.8399→5.6689→5.5962→5.4740→**5.4494**（单调）、top1 0.4013/0.4197/0.4042/0.4513/**0.4522**、**平衡口径 macro（对恒选免疫）0.3369/0.3365/0.3576/0.3576/0.4077**（随机初始化 0.3462）；**10 vs 3 ep 配对：NLL 均差 −0.1468（B 好 5245 / A 好 3632，p=5.6e-66）、top-1 McNemar 1005/579（p=6.3e-27）、四个槽位全净增 +80/+35/+192/+119**。★ **预注册 J-M2 合取判据 FAIL**（槽 1/2 recall 0.2791/0.3250 < 随机基线 0.4105/0.3521）——判据偏向「撒得开」的随机策略，**无偏 macro 才是实质读数（0.4077 > 0.3462）**，两条并列、不删。★ **用户提议的卡牌使用率口径**：训练后 TVD 0.1906→0.1035–0.1217、`ρ_cond` 0.4612→0.8751、`ρ_resid`（去槽位占用）CI 排除 0（10 ep 0.4497 [0.274,0.597]）⇒ **方向对**；但 **trivial「恒选槽 4」全面更强**（TVD 0.0617 / ρ_cond 0.9006 / U 0.6332），因**源数据的卡牌使用率几乎就是「第 4 槽占用」**（标签卡身份 vs 槽4 身份 TVD **0.0617** vs 槽1 **0.4208**）⇒ 该口径的 trivial 上界比训练后更好，**不能单独判「学到了」**；配套**位置受控命中率**（人类打非第 4 槽的 5,340 帧）：随机 0.3646 / 3 ep 0.2987 / **10 ep 0.3562** / 恒选槽4 **0.0000**。判读 `docs/fl_il_2026-09-20.md` §13–§15。★ **10→30 epoch 过拟合**：留出 NLL **5.4494(@10) → 5.9903(@20) → 6.6541(@30)**、训练-留出 gap **+0.43 → +2.84**、分解两侧同时退化（slot 1.1476→1.3694、cell 4.3018→5.2847）⇒ **早停点 ≈10 epoch**；而**边际使用率 TVD 反而一路变好到 0.0943、ρ_cond 0.8951 贴住 trivial 0.9006**，位置受控命中率却从 0.3562 掉到 0.3305 ⇒ **分布口径在过拟合段会给出「继续训练有效」的错判**。lr 轴完整：**1e-3 ≫ 3e-3 > 1e-2**（1e-2 卡在 −6.55 不下降、health=0、top1 0.3844 低于 trivial、macro 0.3253 低于随机初始化）。★ **§16 追问诊断（用户报 8702「打一轮就挂机」）**：**不是数据/采集管线问题，是训练/部署口径不匹配**——BC 训练走 `hidden=None`（逐帧）、部署 `FollowerOpponent` 携带 GRU 隐状态。**受控复现（不依赖 env / dashboard / runner）**：同一批训练输入下 `hidden=none` **702/702 帧出牌（0 STOP）** vs `hidden=carry` **31/702（STOP 95.6%）**、首 STOP 相对位置中位 **0.043**、`‖h‖` 有界 **5.1–8.4**（非数值爆炸）、`carry_r4` 部分恢复 26%；两个 ckpt（3/10 ep）同样塌。**独立复算** arm C 录像：15 局 / 8,761 帧 / p0 出牌 **24 次**（1.60 次/局）、最后一次出牌相对位置中位 **0.000**。⇒ **该 ckpt 只能逐帧口径使用**；循环部署/当作 PPO 初始化对手前必须先修 L6。仪器 `scripts/il_carry_diag.py`。★ **追问之二（用户）：「8702 后手方也只出 1-2 张」⇒ 我的 runner 口径不对称 bug**：`--hidden` 只作用于 p0，p1（`FollowerOpponent`）**恒 carry** ⇒ p1 落进同一 STOP 吸引子。**已修**：新增 `--opp-hidden {same,carry,none}`（默认 `same`）+ **子类**覆盖 `__call__` 清隐状态（★ 把 `__call__` 挂在**实例**上无效：特殊方法按**类型**查找——第一版修法实测无效）；`stats.json` 增加 `opp_hidden`。**更正 §12 读数**：旧三臂都带「后手方挂机」缺陷 ⇒ **胜率 100% 虚高**，修正后 **60%（solo）/ 70%（FL 卡组）**、局均帧 **152→351**、p0 出牌 **12.7→35.9/局**、p1 **13→360/局**（旧：p1 出 13/20/15/18 次）。另勘误：初版复算「15 局/8,761 帧」是**重复计数**（runner 按 block 落盘且后一块**累计**）⇒ 真值 **10 局/5,761 帧/17 次** |

| **O12** | ★★ **IL 学到了「落点」、没学到「出哪张牌」；且预注册闸门被 trivial 基线吃掉**（用户 2026-09-20：「用 FL 的数据进行一些 IL 看看效果」） | 同 **C15** 管线；BC 3 epoch（`rl/human_play.py --bc-train`，**既有通路未改一行**，`hidden=None` ⇒ 克隆的是**逐帧静态映射**）。留出 **8,877** 配对读数（同仪器同留出）：**NLL 8.65→5.60（−36%）**、**落点精确（给定选择正确）0.07%→12.10%（14×；576 格均匀 = 0.17%）**、top-1 option **32.91%→40.42%**、宏 recall 34.5%→35.8%。★ **闸门失效必须点名**：预注册「top-1 ≥ 40% ⇒ PASS」实测 **trivial「恒选槽 4」= 39.84%**、未训练策略 **32.91%** ⇒ 判定**照写 PASS 但无分辨力**（【R16】），实质结论 = 预注册 **F5 弱信号**；训后是靠**放大槽 3 偏置**换分（槽3 recall 0.266→0.563，槽1 0.412→0.220）。**根因候选**：标签的「槽位」是**我方分配**的（人类手牌不可观测 ⇒ 交换固定到槽 4，造成 **9.3 pp** 人为偏斜）；圣水是**上界**（不模拟 10 费泄漏；**13.6%** 标签帧正好在上限）。★ **J4 下游未跑**：`train_follower.py:225` 自初始提交 `b58db45` 起就是 **7 左值/6 右值**的解包 typo ⇒ 首个 episode 结束必崩、`--init-from` 通路被挡（另一通路 `run_league.py --mode solo --main-init` 存在）；本轮按不变量 §5.3 **未改**该行，决定交用户。 ★ **2026-09-20 追加（预算扫参，见 C16）**：「IL 不够 = 缺轮数/lr」在本数据上**基本不成立**——0→3 ep 的改善 92% 与选牌无关；3→10 ep 选牌项占比才升到 37.4%、macro 0.3576→0.4077，但预注册合取判据仍 FAIL，且 trivial「恒选槽 4」在用户提议的使用率口径上全面更强 ⇒ 缺口判在**数据/损失形态侧**（槽位人工分配、逐帧无历史、损失未加权）。 | ① 修 `train_follower.py:225`（补 1 个 `[]`，需【R2】【R18】）或直接走 `--main-init`；② 强制槽位**随机化** + 用 `elixir_leaked` 建泄漏模型（修 L1/L2）；③ 判据换成**配对 + 宏平均 + 相对 trivial 的余量**（【R16】）；④ 给 BC 侧接 `init_hidden`；⑤ 补 `void` 卡（`cards.json` 里有、引擎卡表没有）⇒ 可回收 **8.2%** 被拒局；⑥ 全集 52 分片 = **252,238 局**（本轮只用 1 片） |

| **C17** | ★★ **IL 数据集只取了 6.47% 的决策帧；丢掉的 93% 全是「不出牌/攒费」帧**（用户 2026-09-21 提问）| 旧口径（`scripts/fl_il_to_bc.py`，样本**只在**「本帧恰好 1 手人类出牌且过掩码」时产生）：**735,707 帧 → 47,715 标签（6.47%）**，其余帧 `ActionBundle.noop()` 推战局但**不留样本** ⇒ **STOP 标签 = 0 条**，BC 学的是**条件分布 `p(出哪张|人正在出牌)`** 而非策略。新普查（2200 局，`--stop-mode save`）：非出牌帧 **685,286（93.15%）**，其中 **638,278（93.14% = 掩码放行 ≥1 个有合法落点的槽）** ⇒ **人类在「有牌可出」的帧里 93.14% 选择不出牌**（save:play **13.4:1**）；这些帧重建圣水 p25/p50/p75/p90 = **4.86/6.43/7.89/9.29**（一半以上等待发生在圣水 ≥6.4 = Xbow 区间）。★ **朴素补上 = collapse**（单变量：只换训练集，3 ep/lr 1e-3/seed 0；168,587 条、save 占 77.1%）：留出出牌帧 top1 **0.4042→0.0174**、cell 0.0998→0.0041、NLL **5.5962→6.8697**、macro（对恒选免疫）0.3576→**0.0120**；部署（`--hidden none` 10 局）**局均出牌 35.9→0.3**（6,000 帧共 3 次）、空 bundle 100%、**10/10 局打到 600 帧上限、0 胜 0 负全平** ⇒ 判 **collapse**（不是「学会攒费」）。★ **两条度量事故（我的）**：① 「未加权 STOP 率 gap」被**常数预测器欺骗**（模型 0.7746 vs 人类 0.7758 gap 0.0011，而反加权到自然边际后 **0.9972 vs 0.9326 gap 0.0646 FAIL**）；② J6.3① 阈值**只写上界** ⇒ 退化策略即可满足 ⇒ **不计疗效**。★ **顺带查出管线不可复现**：同代码同 seed，**同 worker 数两次独立进程**样本差 **23/388（5.9%）**、固定 `PYTHONHASHSEED=0` 仍差 24/379，而**同进程内 0/42 差** ⇒ 跨进程不确定、**成因未定**（`src/` 无任何提交/未提交变更 ⇒ 非代码漂移）；⇒ 预注册 J6.2「与旧 pkl 逐位全等」**原理上不可达**。预注册 `fl_il_stopframe_prereg_2026-09-21.md`、判读 `fl_il_2026-09-21.md`；仪器 `il_determinism_probe.py`/`il_bc_equiv.py` |

| **C18** | ★★ **「对手信息不足」被否证（IL 二期 Stage 0）；瓶颈回到优化/混比**（用户 2026-09-21 假设），且**首次实测了规则对手模型的预判能力** | 预注册 `fl_il_il2_prereg_2026-09-22.md`（判据跑前写死）、判读 `fl_il_il2_stage0_2026-09-22.md`；新仪器 `scripts/il_act_upper_bound.py` / `il_opp_prediction.py` + `fl_il_to_bc.py --dump-frames`（逐决策帧导 F_obs/F_oracle）。★ **J7.1 信息增益上界**（200 局 / 66,458 决策帧 → 总体 62,461 帧、act 基率 7.54%；按局 5 折 + 按局 bootstrap 400）：只用「我圣水」AUC **0.5926**；`F_obs`（我圣水/时间/next_card/距下点帧数/信念 hand_probs+next_probs+elixir_mean+unc/最近 3 手对手事件）**0.6756 线性 / 0.6848 MLP**；`F_oracle`（+对手**真值**圣水/手牌 4/循环 8/下一张进手牌/距下点帧数）**0.6762 / 0.6746**；打乱对照 **0.6744 / 0.6796** ⇒ **ΔAUC = +0.00064 [−0.00094,+0.00227]（线性）/ −0.01022 [−0.01749,−0.00326]（MLP）**，**平均 −0.0048 < 0.03 ⇒ 预注册判 H-A（优化/混比优先）**。★ **J7.3 规则对手模型首次实测**：「下一张**进手**牌（cycle[4]）」top-1 **0.7226**（锁定率 **0.6915**，锁定即 one-hot 正确）；「下一次**打出**的牌」（手牌后验 argmax）top-1 **0.2491** vs 均匀 **0.125**、NLL 1.475 vs 2.079；手牌后验 Brier **0.0716**（常数 0.5 = 0.25）；对手 4/8/17 帧内是否出牌 AUC **0.639/0.654/0.681** ⇒ **学习化对手头的门槛 = top-1 > 0.2491 且 Brier < 0.0716**（J9.1）。★ **J7.4 人类 vs 模型 按圣水分箱出牌率**：人类 1–7 费平坦 **5–6%**、**8/9/10 费 0.107/0.186/0.094**（有钱才出手）；塌掉臂 **圣水中位 10.0、≥9 费帧占 96.1%、10 局仅出牌 3 次** ⇒ **H2「不出⇒圣水恒满⇒更不出」自强化陷阱有实测签名**。★ 局限（照实记）：`F_obs` **不含 grid/board** ⇒ 0.68 是**下界**；对手手牌**已通过锁定的信念**（69%）在 `F_obs` 里；人类圣水是重建值 |
| **C19** | ★★ **IL 混比剂量-反应 = 单调帕累托前沿；绑定约束是「出不出/出哪张」共用一个 6 类头**（IL 二期 Stage 1，O13 的直接结论） | 四臂（池内 save 占比 0 / 0.25 / 1.0 / 全量；3 ep / lr 1e-3 / seed 0，**唯一变量 = 池内 save 条数**）：池 **38,743 / 48,429 / 77,486 / 168,587**、logp@3 −5.633 / −4.763 / −3.293 / −1.786。★ **act-AUC 严格单调升 0.5074 / 0.5554 / 0.6480 / 0.7354**（基率 0.2242、n_play 8,916；复刻闸门 max\|Δ\| ≤ 2.4e-06）⇒ J8.1 过 3/4，且 **0.7354 高于 Stage 0 的任何探针上界**（F_obs 0.6756 线性 / 0.6848 MLP、+粗网格 0.6948）⇒ **act 不只可学、且已学到**（更正上一轮口头读法「act 学不会」——那只对 STOP 类几乎无支撑的 R00/R025 端成立）。★ **play top1 严格单调降 0.4188 / 0.3472 / 0.1874 / 0.0174**、NLL 5.5955 / 5.7159 / 6.1356 / 6.8697、cell 0.0929 / 0.0900 / 0.0508 / 0.0041 ⇒ **J8.2 只过 R00**（R025 top1 差 0.0028）⇒ **无内点最优：同臂不可兼得**。★ **部署（`--hidden none --opp-hidden same`、10 局/臂）四臂全出人类双侧带 [18,28.5]**：R00/R025/R10 **过度出牌 38.2 / 32.3 / 30.7**（`stop_when_playable` 0.000 / 0.009 / 0.223、帧/局 368.5 / 325.3 / 313.5），R337 **不出牌 0.3**（1.000、600 帧/局、10/10 全平、0 胜）。★ **口径级发现（本轮新）：「单帧校准差」与「闭环塌不塌」反相关** —— 自然边际 STOP 差**最小**的 R337（0.0646）**塌**，**最大**的 R10（0.2461）**不塌**（反向最大的 R00 = 0.9326 只是过度出牌）⇒ **J6.3① 那类单帧 STOP gap 不能当塌的判据；判塌必须看闭环分布**（出牌/局、帧/局、圣水量级）。★ C14 不变：**四臂 Xbow 全 0**。★ 归属：共享 `slot_head`（6 类 = 4 槽 + ABILITY + STOP）使二值门与 6 类条件分布互斥 ⇒ **Stage 2「独立 act 头」升为主因指向**，且有一条**零新参数、零重训**的实现（组合：R337 当 act 门 + R00 当动作头；留出侧**恒等**满足 J8.1+J8.2，真正要验的是闭环）。判读 `docs/fl_il_il2_mix_2026-09-22.md`、表 `docs/fl_il_2026-09-21/mix_report.json`（`scripts/il_mix_report.py` 复算，禁手抄）；预注册 `fl_il_il2_prereg_2026-09-22.md` §3；仪器 `scripts/il_eval_holdout.py`（`act_decision` 块）/`il_readout_games.py --opp-hidden` |
| **C20** | ★★ **「IL 策略开局火球砸敌方国王塔」的机械成因 = 掩码 EV 闸门漏了「只罩王塔」的落点（不是缺输入）**（用户 2026-09-22 提问，8702 第一局）| 现象（`runs/il_readout_mix*/replays/`，10 局/臂，p1 = **同 ckpt 冻结副本**；录像里的 `fl@0` 只是记录器标签）：`mixR00/R025/R10` 三臂共 **7 次 Fireball 命中 p1 王塔**，**每次恰好 206 血**（= lv11 `688×0.3`），**7/7 都在开局**（5 次即该局第一个动作，全部 t ≤ 2.5 s），其余 9 臂 **0/90**；三臂内「开局手牌含 Fireball」的 12 局占 **5 局（42%）**，而另外 7 个同样含 Fireball 的臂 **0/28**。★ 根因：`src/clasher_new/rl/action_mask.py:158-217 _spell_tower_ev_illegal` 的 **`if not hits_princess: return False`** —— 9h「反纯砸塔」闸门**只对罩到敌方公主塔的落点判定**，而两座公主塔之间的中路口（x≈9, y≈25–29）到公主塔 5.0–6.5（> 2.5+1.4）却到王塔 ≤ 3.9 ⇒ **只罩王塔 ⇒ 直接放行**（连「半径内有无非塔目标」与折费 EV 都没跑）；而 `_spell_has_enemy_target` 把**塔也算敌方目标** ⇒ 这类格子是**合法**的。★ 探针实证（`scripts/il_probe_kingtower_cast.py`，同 seed 零漂移旁挂 `FollowerPolicy.act`）：`mixR00` 局2 **第一次决策**（t=0.0、5 圣水、对方无单位）Fireball p=**0.4249**、合法格 **44 = 只罩王塔 40 + 公主塔 4 + 罩部队 0**，被选 **(9,25)**（`splash_unit=false, splash_king=true, ev_gate_illegal=false`）⇒ **纯砸王塔**；`mixR025` 局2 t=0.5（用户看到的那次）`cell_head` **原始 top-1 = 敌方公主塔格 logit +1.02 被闸门禁掉**，剩下最高分格 = **王塔格 (9,29)**（logit −4.24）⇒ **闸门禁公主塔 → 模型转向王塔**。两帧的 plan 软偏置影响均为 **0**（`cell_bias=0`，关掉偏置 argmax 相同）。★ 观测侧**不缺塔**：`grid` 里 `entity_id=KingTower` + `is_opponent` + HP%，`king_cell_legal=true`，且 `cell_head` 原始 top-1 常是塔格（含**自己**王塔）。★ 人类侧（子智能体取证，我复核口径）：砸王塔法术**存在但稀少**（≤半径 66/10,565 = **0.62%**；Fireball 引擎口径 11/361 = **3.05%**；原始回放 native 复核 18/727 = 2.48%；`Log`/`BarbLog` **0/1590、0/1909**；含一条**满血王塔+双公主塔存活**时的纯砸）⇒ 「人类不可能」**不成立**；但 **t ≤ 5 s 的法术 = 0/10,565、t ≤ 5 s 的任何出牌 = 1/47,715**（原始回放最早 5.40 s）⇒ **「开局第一帧砸王塔」人类 0 样本**。★ 修法（**未改代码**，须先预注册 + R13 位图对账）：`hits_princess` → `hits_any_tower`（王塔同用 `radius+collision_radius` 判定、残血加权含王塔），**不动** `_spell_has_enemy_target`（打躲在王塔后的部队仍合法）；判据 = 位图对账只允许「只罩王塔」格变化 + 探针不再出现 `splash_unit=false & splash_king=true` + `n_legal_cells ≥ 1` + 负对照复现那 7 次。判读 `docs/il_spell_kingtower_gap_2026-09-22.md`；仪器 `scripts/il_probe_kingtower_cast.py`。★ **2026-09-22 已修（同轮）**：**F1** `_spell_tower_ev_illegal` 的 `hits_princess` → `hits_princess or hits_king`（王塔按**名字**查找，不写死 id；只罩王塔时用 `tower_value_mult(king=True, princesses_alive=…)` 取倍数）；**F2** 新增 `_is_effect_body()`（`data.type ∈ {projectile, area_effect, bomb}`）并在 `_spell_has_enemy_target`/`_spell_covers_non_tower` 中**跳过效果载体** —— 先例：`mixR025` 局9 那次王塔空砸的唯一"理由"是场上有**对方 Log 的滚动弹**；**F3（不变式）** `env_wrapper.get_action_mask_for` 加「合法槽必须至少有一个合法落点」（否则 F1/F2 后 `act()` 会把 576 格全填 −1e9、argmax 落格 0 ⇒ 整包被 `validate_bundle` 拒收）。★ **门禁**：`_mask_diff_snapshot` 128 张快照 ×3 版（`kingtower_{before,after,after2}.npz`）、`--selftest` 5/5 PASS；`before→after` **24/128 张不同、逐格分类 960 格全落在唯一允许类别（只罩王塔 ∧ 罩不到公主塔 ∧ 无非塔目标）、方向全为只收紧、0 越界**（差异只在 Fireball/Arrows 两张）；⚠️ `after→after2` **128/128 逐位全等** ⇒ **R13 语料里没有「效果载体」状态，对 F2 是盲的**（覆盖缺口已记录）。★ **回归测试** `scripts/selftest_spell_kingtower.py` **16 断言全 PASS**（含「真实 `LogProjectileRolling` 在场 ⇒ 王塔格非法」的前提断言，防假绿）。★ **行为（同 seed 10 局/臂；事件口径 = 王塔恰好 −206 + 前 4 帧有 p0 `FireballSpell`）**：`mixR00` **2→0**、`mixR10` **2→0**、`mixR025` **3→1**（剩的 1 次是**对方 Knight 藏在王塔后**、Fireball 冲它去的 unit 类，9h 闸门特意放行）⇒ **纯砸王塔 6→0**；出牌/局 37.1/32.8/30.5、`Xbow` 仍 0（C14 不变，无塌陷）。★ **更正（我自己复核推翻）**：本文档初版写「本仓引擎只有 Fireball/Arrows 能扣王塔血」**是错的**；实测 Zap 57.6 / Snowball 58 / Poison 343.7 / Lightning 686.4 / Log 290 / Earthquake 171 / Vines 76.5 / Rage 53.7 / Freeze 34.5 / Tornado 0.6 **都扣王塔血**（`crown_tower_percent` 缺字段回落 1.0、滚动弹不走该乘子）。★ **新建开放项（未改代码）**：**F3** `_spell_deals_damage` 只覆盖 4 张卡 ⇒ 其余法术的 8h/9h 闸门**根本没开**；**F4** `_spell_tower_damage` 对 Log/Lightning 读 0（"无标定⇒放行"后门）；**Log/BarbLog 半径口径**是半宽、伤害区是 10.1/4.5 格走廊；**觉醒/进化**：引擎有（`evolutions.py` + `battle.py`）但 **`rl/` 引用数 0 ⇒ 对局里永不触发**，且 `observe()` 无任何觉醒字段（唯一被证实的"网络缺输入"候选，但**当前不构成任何已观测行为的成因**）；R13 语料缺"效果载体"状态；`_mask_diff_snapshot.build_states()` 的 ⑥ `late_low_tower` 是**死代码**（构造的 `s` 被丢弃，实际 append 满血公主塔）。|
| **C21** | ★★ **掩码法术谓词与引擎同源（已修）＋ 觉醒/进化：引擎有、RL 从未声明（预注册已落）**（用户 2026-09-22 两条指令）| **① F4/F5/F6 `_spell_deals_damage` 修复**：旧实现只读 `data["projectileData"]["damage"]|data["damage"]` ⇒ 漏判 Zap(row 75)/Poison(DOT 57)/Earthquake(39)/Tornado(106)/Vines(154)/Freeze(72)/RoyalDelivery/Log/BarbLog ⇒ **8h 空砸闸门与 9h 砸塔 EV 闸门对它们根本没开**；现改**四路取或**（`ProjectileData.damage`/`data.damage`/`spells[].damage`/`buff_data.damage_per_second`），并显式排除**友方语义**（`Heal/Rage/Clone/Mirror` 的 damage 是治疗/增益，否则回血法术被禁）与**滚动类**（`_ROLLING_SPELLS={Log,BarbLog}`：用户口径「落点=部署点、伤害在滚动走廊/释放的野蛮人」⇒ 落点几何闸门不适用，走 `_spell_requires_placement_target()`）。★ **门禁**：法术全卡位图 sweep（**24 张 × 8 状态 × 2 方 = 384 张**，`mask_snapshots/spell_{before,after}.npz`）⇒ **96/384 变化、全部只收紧（放松 0）**，涉 Zap/Poison/Earthquake/Tornado/Vines/RoyalDelivery（"只有塔"的场上 576 全合法 → 0）；**R13 128 张语料对本次是盲的**（手牌只有 Arrows/Fireball，逐位全等 —— 覆盖缺口第二次记录）；回归 `selftest_spell_kingtower.py` 扩到 **23 断言 PASS**。★ **数据管线影响**（同 200 局）：`mask_reject` 183→**219**、`labels` 4497→**4440（−1.27%）**、errors 0；**旧数据集不重跑**。★ **部署行为零变化**：三臂 10 局出牌/帧/胜率**逐值相同**（371/328/305；359.4/330.1/312.5；0.70/0.50/0.20）—— 受影响法术只有 Log 且被豁免。★ **引擎事实（与用户口径差异照实记）**：`Log` 部署**敌我半场都允许**（576/576，本仓当普通法术，与「落点和部队相同」不符 ⇒ 待拍板）、`BarbLog` **两侧 deploy 都被拒**（已知 O9/P1-20）、`Log` 塔伤 290 而 `_spell_tower_damage` 读 **0**、`Lightning` 塔伤 686.4 而四个字段都读不到（谓词仍未覆盖）。★ **② 觉醒/进化**：引擎**有**（`evolutions.py` 42 张周期表 + `battle.py:3068-3070` 触发 + `player.set_evolution_slots`），但 **`rl/` 对 `evo_slots/evo_plays/hero_slots/evolution_state` 引用数 = 0 ⇒ 本仓对局里觉醒永不触发**；`observe()` 五键**无觉醒字段**；人类回放 `-ev1` **11,293 槽 100% 可映射本仓觉醒表**（S1 判据① PASS）、`-hero` **4,550 槽本仓覆盖 0%**（Hero 是另一套未接线机制）；`fl_il_to_bc.py` 已解析 variant 但**从未 `set_evolution_slots`**⇒ 重建战局按基础卡跑（数据保真缺口）。预注册 `docs/il_evo_prereg_2026-09-22.md`（S1 数据保真 → S2 观测加字段（**R6：必须 `--fresh`**）→ S3 RL 声明 → S4 验收含性价比回滚）；判读 `docs/il_spell_kingtower_gap_2026-09-22.md` §8 |
| **C22** | ★★ **低费法术「空砸」豁免（W1，已实现）＋ 手牌打分/前期卡组信息分预注册（W2/W3）**（用户 2026-09-22 指令）| ★ **口径更正（用户）**：上一轮把 Poison 归入「便宜伤害法术」是**错的** —— **Poison = 4 费**。★ **本仓法术全枚举**（`card_data` 实测，非硬编码清单）：**27 张 `type=="spell"`** —— cost1 GlobalLightning/Heal/Mirror/WarmSpell；**cost2 BarbLog/GoblinCurse/Log/Rage/Snowball/Zap**；cost3 Arrows/Clone/DarkMagic/Earthquake/GlobalClone/GoblinBarrel/RoyalDelivery/Tornado/Vines；cost4 Fireball/Freeze/**Poison**；cost5 GoblinPartyRocket/Graveyard；cost6 Lightning/MergeMaiden/Rocket。**受落点几何闸门约束者**：cost2 {Snowball, Zap}、cost3 {Arrows, Earthquake, RoyalDelivery, Tornado, Vines}、cost4 {Fireball, Freeze, Poison}、cost6 {Rocket}。★ 「低费」= **`cost ≤ 2`**（阈值取 2 而非 3：cost≤2 恰好 = **过牌法术集合**；3 费里混着 Earthquake/Tornado 这类**不是过牌卡**的解场法术）。★ **实现**：新增单一来源常量 `SPELL_WHIFF_FREE_MAX_COST = 2.0`（R7）+ 谓词 `_spell_whiff_gate_applies()`（= 落点几何闸门适用 ∧ 费用 > 阈值），**只替换 8h 空砸闸门的两处调用点**（`_position_legal` 提交路径 + `legal_cells` 掩码路径）。★ **关键设计（必须记住）**：**只豁免空砸闸门，不豁免 9h 砸塔 EV 闸门** —— 因为 `_spell_tower_ev_illegal` 自带「落点没罩到任何存活塔 → 提前 `return False`」⇒ 低费豁免后**空地上的 Zap/Snowball 变合法**，而**「只罩敌方王塔」的 Zap/Snowball 仍被判非法**（**F1 修复不回退**）。把两个闸门合成一个开关是本批**明确避免**的做法。★ **门禁**：`selftest_spell_kingtower.py` **32/32 PASS**（23→32，含「≥3 费负对照仍全 0」与「Zap/Snowball 只罩王塔格仍全非法 n=44/44」）；**R13 主口径**标准 128 张语料对 `kingtower_after2.npz` **128/128 逐位全等**（该语料只含 Arrows/Fireball ⇒ 按构造对本改**盲**，**再次印证覆盖缺口**）；**新增可复算仪器** `scripts/_mask_diff_snapshot_spells.py`（**全 27 法术 × 8 状态 × 2 方 = 432 张**，同进程**单变量 A/B**：把阈值设为 −1.0 与当前值，比「文件 vs 文件」更强 —— 状态/代码/解释器全同源）⇒ **32 张变化（恰好 Zap+Snowball × 8 状态 × 2 方）、放松 14,656 格、收紧 0、越界 0**（两条断言：零收紧 + 变化卡 ⊆ 允许集）。⚠️ 该脚本取代了 C21 批那份**产出脚本没入库、无法复算**的 384 张 ad-hoc sweep（R13 反模式）。★ **数据管线影响（同 200 局，脚本复算）**：`mask_reject` **219 → 203（−16）**、`labels` **4440 → 4461（+21 / +0.47%）**、`frames` 66,064 → 66,455、`errors` 0 ⇒ **方向与 F4 批（−1.27%）反号，是「回血」**；机制 = 低费空砸落点不再被拒收 ⇒ 重建走样变少 ⇒ 更多人类帧保住。⚠️ 两次跑**不是同一批完全一致的局**（重建被拒即分叉）⇒ 只作**方向判据**（R17）。★ **W2/W3 预注册已落**（判据写死在 `docs/il_whiff_handscore_prereg_2026-09-22.md`）：**W2** 手牌打分（己方/对方/双方 × 圣水）——对方手牌是**精确队列推断**（`bayes_filter.py:9-14`：卡组已知时从第 4 张起 0/1，`entropy()==0`）故可**确定性计算**（R12），而对方圣水**只有有偏规则估计器**（硬编码 2.8 s/费 vs 引擎分阶段 `battle.py:2880-2881`）⇒ 特征必须**显式带不确定性**；注入走 **plan 尾追加**（`plan_mlp.0.weight` 有尾零兼容分支；scalar 通道在 fused **中段、无兼容分支会静默重置** ⇒ 必须 `--fresh`），⚠️ **必修坑**：`follower.py:424-427` 用 `v[PLAN_DIM-4+i]` 读 **hold** ⇒ 尾追加会**静默错位**，须引入显式 `PLAN_HOLD_OFFSET`；**W3** 前期卡组信息分（`known_opp/8` × 门控 `w(t)=clip(1−t/30,0,1)`），人类标定 = T=30 s 窗口内 median **4** 次出牌 / 4 张不同卡、首牌中位 **9.85 s**、≤2 费法术落己方侧占窗口法术 **49.71%**、1 费牌进敌方半场仅 **2/6,038**；**只进观测不改奖励**（R11） |
| **C23** | ★★ **解耦组合臂（R337 门 + R00 动作头）已跑完：先证伪了预注册里那条「恒等式」，再判 K1 FAIL；且该臂原理上无法隔离头耦合**（用户 2026-09-22「你继续吧」；O13/C19 的直接下一刀）| ★ **① 证伪「留出侧恒等式」**（`mix:102-103` 原文称「组合模型的 act-AUC ≡ 门的 0.7354、play top1/NLL ≡ R00 的 0.4188/5.5955 —— 即同时满足 J8.1 与 J8.2」）：留出集 `runs/_fl_il_bc_save/holdout` = **39,761 帧 = 8,916 play + 30,845 stop**（基率 **0.2242**），R337 连续分数 `1−p(STOP)` 的 act-AUC **0.7353786**（复现 0.7354 ✓）；但**τ 判决（0/1）后 AUC 上界仅 0.6713**（BA 最大点 τ=0.7407）**< J8.1 阈值 0.6948**，而基率匹配 τ=0.6610 下 play top1 仅 **0.1749 ≪ 0.35**。根因 = **R00 在全部 39,761 帧上 `argmax==STOP` 出现 0 次**（`holdout_mixR00.json` `model_stop_rate_argmax: 0.0`）⇒ 门的每次否决必然丢一帧人类出牌帧。⇒ 连续分数口径 = **同义反复**（= R337 自己，组合没被测）；τ 判决口径 = J8.1 FAIL ⇒ **本臂判决不许用 J8.1**（已写进 §6.1）。★ **② 实现**：`scripts/il_readout_games.py` 增 `--gate-ckpt` / `--gate-threshold`（默认 0.6610），门用 `_gate_p_stop()` 复刻 `act()` 第一步 `softmax[STOP]`（与 `il_eval_holdout.first_option_probs` 同款、**须同步**），门判 STOP 直接发空 `ActionBundle`；**`src/` 零改动、零新参数、零重训**（不触发 R6）。★ **F0 恒等回归 PASS**：`--gate-ckpt <同一 ckpt> --gate-threshold 1.0`（门恒不触发）vs 不给 gate，3 局同 seed ⇒ **13 项统计逐值全等**、`gate_stop_frames=0`、`p0_top_cards` 一致 ⇒ 补丁在门关闭时**行为中性**。★ **③ 主结果（10 局，与四臂同协议）**：**plays/game 34.70**（带 [18,28.5] ⇒ **K1 FAIL**）、frames/game **397.4**（K4 PASS，**余量仅 2.6 帧**）、p0 圣水中位 **4.36** / ≥9 费 **0.0554**（K3 PASS，未到 R337 的 10.0/0.96）、`gate_stop_rate` **0.9127**、`stop_when_playable` 0.9019、胜率 0.50。对照：R00 38.20/368.5/1.50/0.0000、R337 0.30/600.0/10.00/0.9606、人类 22.8/—/6.43/0.142。（`plays/game` 由 `solo_state.json` 复算，该复算法**逐值复现 mix_report 四臂** 38.20/32.30/30.70/0.30；K3 口径自校验复现出 R337 的 10.00/0.9606。）★ **④ 关键发现（写进 §6.8，不改判据文本）**：门的加入把 **`playable_rate` 从 0.1037 抬到 0.8900（8.6×）** ⇒ **状态分布巨变**；且**三条轴全部朝人类方向移动但都没到**（出牌 38.2→34.7 vs 22.8、圣水 1.50→4.36 vs 6.43、≥9 费 0→0.055 vs 0.142）⇒ 机制签名属于**「off-policy 状态分布不匹配」**（R00 的动作头是在**永不 STOP** 的分布下训出来的，`stop_when_playable=0.0000`，接到硬门下其 cell/slot 输入分布**训练时从未见过**）⇒ **§6.4 第三分支后半句「诊断转『不是头耦合』」被本次数据否定**：正确结论是「**零成本组合臂在原理上无法隔离「头耦合」这一个变量**（它同时改了状态分布）」，而不是「不是头耦合」。★ **⑤ 下一步**：**不做**更多组合臂，直接把「独立 act 头」放进 **Stage 2 联合训练**（唯一能在训练分布下隔离该变量的做法）。预注册全文（含 6.1 证伪 / 6.2 封口 6 条原「未定」口径 / 6.3 K1–K6 / 6.4 分支 / 6.5 不变量 / 6.7 结果 / 6.8 分支失效登记）=`docs/fl_il_il2_prereg_2026-09-22.md` §6；读数 `runs/il_readout_gateR337actR00/stats.json`（含 `gate_eval_frames=3974 / gate_stop_frames=3627 / gate_stop_rate=0.9127`）+ `solo_state.json` + `replays/` |
| **C24** | ★★ **独立 act 头（已实现 + 已判读）：离线双指标双向改善、闭环状态分布未被破坏；但 P4① FAIL 的归因是「配对池选错」而非「共享头诊断不成立」**（C23 的直接下游；预注册 `docs/fl_il_il2_prereg_2026-09-22.md` **§7**）| ★ **两条前置（取证挖出）**：① **预注册缺位** —— 全 249 行无任何一节写此事，§6 是给组合臂写的且第三分支已被 §6.8 推翻 ⇒ 按 R3 先补 **§7**；② **★ NLL 可比性陷阱** —— 拆头后 stop 帧 `-log p₂(STOP)` 与 play 帧多出的 `-log p₂(ACT) ≥ 0` 使 J8.2/J9.2 **不可直接照搬**（【R15】）；救法 = **嵌入 6 类口径** `p₆'(i)=p₂(ACT)·p₅(i)`、`p₆'(STOP)=p₂(STOP)`，此时 play 帧 `lp` 与解耦损失**逐值相等**（§7.3 给出证明 + **可执行断言**）。★ **R10 自我更正**：初稿写「新 stop NLL **恒 ≤** 旧（边缘）」**不是定理**（`p₂`/`p₆` 是两套独立参数），只是倾向；**必然**的只有 play 帧那一项。★ **命名更正**：本仓「Stage 2」已被 §3 的 J9（对手头/belief 回喂）占用 ⇒ 全节改称「独立 act 头」。★ **实现**（`src/` 唯一改动 = `rl/follower.py`）：`act_head=Linear(hidden,2)`（0=STOP/1=ACT）+ `slot_head` 出维 **6→5**（4 槽 + ABILITY，`ABILITY_IDX=K_MAX=4` **数值不变**）；新增 **`SUB_SPACE_DIM`** 把 GRU 的 `sub_emb` one-hot 空间**与头出维解耦** ⇒ `sub_emb (128,8)` 与 `enc_fc (128,2731)` **保持可加载**、形状破坏面收缩到只剩 `slot_head`+`act_head`；旧路径走**分离实现**（`_act_decoupled`/`_evaluate_decoupled` + 顶部 dispatch）⇒ 旧代码**一行未改**；PPO 的 `act_parallel`/`evaluate_batch` 在拆头下**显式 `NotImplementedError`**（不许静默用共享头口径 ⇒ ratio≠1）；`save/load_checkpoint` 加 `decoupled_act` 元数据 + **不一致告警**（照 `intent_options` 先例）。★ **门禁**：`scripts/selftest_decoupled_act.py` **15/15 PASS**（核心 **P3 ★ 嵌入口径 lp == 解耦 lp，`max|Δ|=6.0e-07`**；P2 默认架构与旧 ckpt **29/29 同形**；P1c `sub_emb`/`enc_fc` **未变**；P4 拆头下 6 类掩码函数**必须炸**而非静默；P6 PPO 路径显式拒绝；P7 无合法槽 ⇒ ACT 掩掉无 NaN；P8c 旧 ckpt 加载告警**实际打印**）；**F0 位图门 `29 张量 / 29 逐位相同`** ⇒ 旧路径 R2 成立；**仪器改造回归**：`il_eval_holdout.first_option_probs` 改为 `first_option_dist6(...)[STOP]` 后同 ckpt 读数**逐位不变**（`top1 0.40419060493409936` / `nll 5.596222589020527` / `macro 0.3576220752690757`）。★ **结果（臂 `sweep_mixR00da`，池=R00 同池 `--mix-ratio 0.0` 38,743 play-only、seed 0）**：训练 `mean_logprob` −5.976→−5.763→**−5.603**（R00 **−5.633**）；**P5 PASS**（top1 **0.4152** ≥0.35、NLL **5.5785** ≤5.75）、**P6 PASS**（**Δact-AUC +0.0224**、**ΔNLL −0.0170**）、**P4③ PASS**（帧/局 **331.6** ≤400）、**P4① FAIL**（出牌/局 **33.90** > 带上界 28.5）。★ **P4① 归因（关键）**：`stop_when_playable` 两臂**都是 0.0000**、`playable_rate` **0.1022 vs 0.1037**（状态分布几乎重合，**无** C23 的 8.6× 漂移）⇒ `--mix-ratio 0.0` 的池**无 save 帧** ⇒ `act_head` 只在 **play 帧终止步**见过 STOP、**从未把「不出牌」当标签**，R00 处境相同 ⇒ **两臂 act 行为必然一致** ⇒ **33.9 是「池的性质」不是「头的性质」**。⚠️ **这是本预注册的一处设计错误**（§7.4 P6 配对池写成 play-only；「头耦合」只有在池**同时含 play 与 save** 时才可检验）⇒ 按 §7.5 分支：**P4① 失败 ≠ 共享头诊断不成立**，而是**该臂配对池选错、检验无效**；**不改判据文本**（同 §6.8 纪律），登记为**实施错误**。★ **已确立且与池无关的正向结果**：同池同 seed 下拆头使 **act-AUC +0.0224 / NLL −0.0170**（top1 基本持平），且部署侧 `playable_rate`/`stop_when_playable` 与 R00 **几乎重合** ⇒ **拆头没有破坏闭环状态分布**（C23 里组合臂失败的原因在此**不成立**）。★ **修正配对已启动**：`sweep_mixR10da`（与 **R10 同池** `--mix-ratio 1.0`，77,486 = play+save 各 38,743），对照臂换成 R10（act-AUC 0.6480 / top1 0.1874 / NLL 6.1356 / 出牌 30.7 / `stop_when_playable` 0.2228）。★ 预注册全文 §7（7.0 命名 / 7.1 根因 / 7.2 架构 / 7.3 可比性 + 证明 / 7.4 P1–P6 / 7.5 分支 / 7.6 成本 / 7.7 ckpt 守卫 / 7.8 必改评估路径 / 7.9 与 intent 互斥 / **7.10 结果 / 7.11 归因 / 7.12 修正配对**）；读数 `docs/fl_il_2026-09-21/holdout_mixR00da.json`、`readout_mixR00da_stats.json`；回归 `scripts/selftest_decoupled_act.py` |
| **C25** | ★★ **拆头路线关闭：「共享 6 类头耦合」作为当前绑定约束的假设【不成立】（负结果，且是在正确配对上得到的）**（C24 的修正配对判读；预注册 `docs/fl_il_il2_prereg_2026-09-22.md` **§7.13**）| C24 的 P4① 失败被归因为**配对池选错**（`--mix-ratio 0.0` 的池 play-only ⇒ `act_head` 只在 **play 帧终止步**见过 STOP、**从未把「不出牌」当标签** ⇒ 两臂 act 行为必然一致）。本次跑**修正配对**：`sweep_mixR10da` = 与 **R10 同池**（`--mix-ratio 1.0`，77,486 = play/save 各 38,743）+ 同 seed，**唯一变量 = 架构**。★ **结果（负）**：act-AUC 0.6480→**0.6526**（+0.0045）、play top1 0.1874→0.1766、play NLL（嵌入口径）6.1356→**6.2053**（**+0.0697**）、**出牌/局 30.70→33.20（+2.50）**、帧/局 313.5→335.0、**`stop_when_playable` 0.2228→0.0879（−0.1349）**、胜率 0.40→0.40。★ **判据**：**P6 FAIL**（ΔNLL +0.0697 > 0）、**P4① FAIL**（33.20 > 28.5）、P4③ PASS（335.0 ≤ 400）；⚠️ **P5 的绝对阈值对本配对无区分力**（R10 **自身** top1 0.1874 / NLL 6.1356 也不达标 —— 0.35/5.75 是 **R00 档**标定，【R15】）。★ **判决（按 §7.5）**：P4① 在**正确配对**下仍失败，且 **P1/P3 均 PASS**（实现无误，`max\|Δ\|=6e-07`）⇒ **共享头耦合作为绑定约束的假设不成立**。★ **决定性的是方向不是幅度**：`stop_when_playable` **下降**（0.2228→0.0879）—— 给**专用** act 头后模型**更少**在「买得起」时选择不出；而耦合假设预言**相反**方向（专用头应更能区分「出不出」与「出哪张」⇒ 更合理地停）。⇒ 拆头**没有解开任何东西**，且在四轴中的三轴上轻微恶化。★ **据此关闭 C19/O13 的「下一刀 = 解耦」**：R00↔R337 那条**单调帕累托前沿不是头架构的性质，而是池构成的性质**（混入多少 save 帧）⇒ 下一刀**不再动「头」**，转 ① **数据/标签分布**（save 帧到底在教什么；§7.11 已证 play-only 池里 `act_head` 从未见过「不出牌」标签）② **机制侧**（**O7** 让攒费值得做 / **O11** 承诺锁定修目标抖动）。`decoupled_act` 开关**保留**（默认关、零成本、旧路径逐位不变、**是目前唯一被证明「不引入闭环状态漂移」**的架构改动：`playable_rate` 0.1022 vs 0.1037），但**不再投入**。读数 `docs/fl_il_2026-09-21/holdout_mixR10da.json`、`readout_mixR10da_stats.json` |
| **C26** | ★★ **W2 手牌打分（己方/对方/双方 × 圣水）+ W3 前期卡组信息分：实现 + 门禁已交付；疗效判读见本节续**（用户 2026-09-22 指令「加入手牌打分机制…增加一个前期特有的卡组信息分，让模型前期进行一些试探」）| ★ **口径更正（用户）**：**Poison = 4 费**（上一轮我把它算进「便宜」是错的）；本仓 **27 张 `type=="spell"`**，「低费」可执行定义 = **`cost ≤ 2`** = {Zap, Snowball, Log, BarbLog}（3 费里混着 Earthquake/Tornado 这类解场牌，不是过牌卡）。★ **新增 `src/clasher_new/rl/hand_score.py`**（**纯函数**、无副作用、可单测；【R12】EBK「能算的不许让网络猜」）：plan **尾部追加 17 维** = own(4: `n_playable/4`、`cheapest/10`、`total_cost/40`、`mean_cost/10`) + opp(4: **后验期望版** `Σp_i·1[cost_i≤ê]/4`、支持集最便宜、`Σp_i·cost_i/10`、`P(cost≥5)`) + diff(1: own−opp 的可用牌数差) + elixir(3: `elixir/10`、`ê_opp/10`、差/10) + t_afford(1: `max(0,max_cost−elixir)/2.8/10`) + info(4: `1−k/8`、`w(t)=clip(1−t/30,0,1)`、`probe=(n_playable/4)·unknown`、`unknown·w`)。⚠️ **诚实标注**：`S_opp` 的输入 `hand_probs` **本来就是 belief token 的 [0:8]**（`rl/belief.py:348`）⇒ 那 4 维是**确定性再编码**（免去网络自己学阈值）而**不是**新信息源；真正不在任何既有输入里的是「**cost × elixir 的合取**」。★ **两处跑前修正**（草稿自身的**线性重复**，写死在预注册 §6.1）：① `S_own` 第 4 维 `afford_frac = n_playable/4` 与第 1 维**逐值恒等** ⇒ 改 `mean_cost/10`；② 信息分第 4 维 `known_opp/8` 与第 1 维 `1−known_opp/8` **仿射等价** ⇒ 改 `info_unknown·w(t)`。★ **★★ 方法学关键修正（本批最重要，预注册 §6.2）**：**对照组不能是 58 维网络** —— `il_bc_sweep.train_one` 先 `torch.manual_seed(seed)` **再**建 `FollowerPolicy` ⇒ `plan_dim` 58 vs 75 让 `plan_mlp[0]=Linear(·,64)` 消耗**不同数量**的 RNG 抽样 ⇒ **其后每一个参数（cnn/gru/slot_head/cell_head/value_head）都拿到不同初值** ⇒ 「加了信息」与「换了初值」**混淆**（这正是 C24 那类配对错误的同型风险）。改为**同形状 + 前向抹末 K 列**（`FollowerPolicy(plan_extras_zero=K)`）⇒ 三臂**形状、参数量、初始化逐值、permutation 顺序完全一致**，唯一变量 = **那 17 列有没有携带信息**（置零列梯度恒 0 = 17 个死输入）⇒ 顺带把「加了信息」与「纯加容量」彻底分开。★ **不变量修复**（§6.2 W2-b）：新增 `PLAN_BASE_DIM(58)` / `PLAN_HOLD_OFFSET(54)`，`_plan_biases` 的 hold 下标不再写 `PLAN_DIM-4`。⚠️ **坑的精确形状被我自我更正**：预注册初稿说「尾追加会让 `PLAN_DIM−4` 静默错位」——**不准确**：`PLAN_DIM` 是常量 58，尾追加后 `v[54+i]` **仍然正确**；真正会错的是「按**向量长度**取尾 4 维」（`v.shape[0]−4=71`）⇒ 本修复的作用是**把正确读法写成显式常量、杜绝将来改成 `len(v)−4`**，并在回归 T2.4 里**构造性演示**那条错误路径读到 extras（实测错值 `slot 3 = −1.7` vs 正确 `slot 2/4 = −2.5`）。★ **门禁（全部脚本复算）**：`scripts/selftest_hand_score.py` **44/44 PASS**（T2 不变量 / T6 消融粒度 / T7 ckpt 兼容 J-W2.2「58 维 ckpt 载入 75 维：新增 17 列**严格 0**、前 58 列逐位拷贝」/ T8 布局冻结 / T10 推理侧 `plan_dim` 守卫）；`scripts/probe_plan_extras_prefix.py` **全 PASS**（**同进程双跑**：obs/belief/masks **逐位**、**`plan[:58]` 逐位**、追加维非零、关闭时恒 58 维 —— 因为跨进程不可复现【C17：同参差 5.9%】⇒ 「新旧语料逐位全等」原理上不可达，故必须换这个口径）；8 局管线 smoke：三臂 ckpt 元数据 `plan_dim=75` 且**只有该动的列在动**（HS17 vs HS04：W2 段 `max|Δ|=9.3e-2`、**W3 段 `==0`**；HS04 vs HS00：W3 段 `9.3e-2`）；推理侧新增 `build_plan_vec`（`plan_dim` 不符**显式报错**，不静默产短向量）+ `info_window` 读数（J-W3.2 的仪器）。★ **语料必须重生成一次**（plan 是**生产期烤进样本**的）：`runs/_fl_il_bc_hs`，2200 局 / **1711 s** / `plan_dim=[75]` / play **47,270** + save **160,668**（`save/off=0.9306`、`frames=736,277`、`mask_reject=0.0577`、errors **0**）。★ **三条环境事故（新登记，都会让跑静默死掉）**：① **并发 torch CPU 训练撞 CUDA OOM** —— 三条泳道在**第一处 `opt.step()`** 全部 `torch.AcceleratorError: CUDA error: out of memory`：`Optimizer.step` 的 `_accelerator_graph_capture_health_check()` 会调 `torch.accelerator.current_stream()`，**即使模型在 CPU 也会去建 CUDA 上下文** ⇒ 3 进程 × 各建一个上下文 ⇒ 小显存卡 OOM ⇒ 并发跑**必须** `export CUDA_VISIBLE_DEVICES=""`（本训练本来就是 CPU，`FollowerPolicy.device=="cpu"`、`il_bc_sweep` 从不 `to_device` ⇒ **不改变任何数值**）；② **本机 torch 进程并发上限 = 3** —— 第 4 个 torch 进程直接 `OSError [WinError 1455] 页面文件太小`（连 `import torch` 都过不去）⇒ **读数链必须串行、且排在训练泳道之后**（已写进 `scripts/_run_hs_readouts.sh` 的注释）；③ **多进程 torch 会饿死纯 Python 的引擎进程** —— 30 个逻辑线程抢 16 核时，单跑 159 样本的 sweep 6 分钟不出结果（正常 5 s），语料生成从 86 局/分掉到 5 局/分 ⇒ 判「卡死」前必须先看是不是被抢核。★ **三臂设计**（预注册 §6.4，跑前冻结）：`HS17`（尾 17 列抹零 = **对照组**）/ `HS04`（只有 W2 活着）/ `HS00`（W2+W3 全活），× seed {0,1,2}，`--configs 3:1e-3 --mix-ratio 1.0 --threads 4`；**判据**：**J-W2.3** = 配对 `HS04−HS17` 的 `mean_k ΔNLL ≤ 0 ∧ mean_k Δact-AUC ≥ 0` ∧ ≥2/3 seed 方向一致（`mean ΔNLL>0 ∧ mean Δact-AUC<0` ⇒ **无收益 ⇒ 回滚特征**）；**J-W3.2** = 配对 `HS00−HS04` 的 `mean_k Δ(known_opp_median_in_window) ≥ 0`（**方向判据**，R15/R16）；**J-W2.4** = `plays/game ∈ [18,28.5]` **只登记不判定**（带值来自另一批语料，【R15】不可照抄）。★ 另设**描述性归因仪器** `scripts/il_hs_feature_use.py`（§6.8，跑前写死）：对训练好的网把 17 列**整块跨帧打乱**（保边际、破「帧↔特征」对应）看 ΔNLL ⇒ 把「特征无增量信息」与「有信息但没学进去」**分开**（前者该弃特征、后者该加轮数）。★ **结果（9 跑 = 3 臂 × 3 seed，池 76,926，单跑 37.7–40.0 min，0 报错）** → ★★ **W2：J-W2.3 FAIL，效应与种子噪声同量级；但归因证明「信息进得去、网络也学得会用」**。逐 seed（`HS04−HS17`）：**ΔNLL = −0.0755 / +0.0521 / +0.1302（均 +0.0356）**、**Δact-AUC = +0.0022 / +0.0103 / +0.0028（均 +0.0051，3/3 同号）**、Δtop1 均 +0.0032 ⇒ 判据要求 `mean ΔNLL ≤ 0` **不满足 ⇒ FAIL**；⚠️ 实测落进 **§6.4 未注册的第三分支**（`ΔNLL>0 ∧ Δact-AUC>0`，既非 PASS 也非回滚分支）⇒ **登记为预注册缺口**，处置（保守）= **不采用为已验证改进 / 不回滚代码 / 默认关**。★ 【R10】**不许写「W2 有害」**：ΔNLL **逐 seed 极差 0.206 ≫ 均值 0.036** ⇒ 端到端效应**与种子噪声同量级**（1/3 seed 反而更好）；唯一方向一致的是 act-AUC（3/3 正，符号检验 p=0.125，**不足以当结论**）。★ **★★ 归因（本批最有信息量的读数，`scripts/il_hs_feature_use.py`，HS00 s0 / 4000 帧 / n_perm=4）**：`ΔNLL(zero) = +0.0132`、**`ΔNLL(perm) = +0.0811`**（对 17 列**整块跨帧打乱**：保边际分布、只破「帧↔特征」对应），perm 抖动尺度（逐帧 std 0.1824/√4000）≈ **0.0029** ⇒ **≈28×** ⇒ **模型确实在用这 17 列**，且 **play 帧 +0.1902** 远大于 **stop 帧 +0.0478**（依赖集中在「出不出/出哪张」）；**负对照** = 同仪器跑抹零臂 HS17 s0 ⇒ `ΔNLL(perm) = ΔNLL(zero) = 0.0` **恰好为 0**（`hs_feature_use_HS17_s0_NEGCTRL.json`）⇒ +0.081 不是「任意扰动都会变差」的假象。⇒ **瓶颈不在「特征未被利用」**，而在该训练强度/该池构成下**端到端可分辨的收益本身就不存在或极小**（与 C19/C25 同向）。★ **W3：判据饱和失效 ⇒ 「PASS」是空的**：`known_opp_median_in_window` 在**全部 9 个 run** 上**都等于 4.00**（`frames_in_window` 恒 610）⇒ 统计量**无分辨力**；更细同源读数（均值 3.398–3.648、max 恒 5、`plays_in_window` 5.6–5.9、`distinct` 恒 5.0）**臂间差 < 臂内 seed 散布** ⇒ **不可分辨** ⇒ **不得写成「W3 有效」**（同 J6.3① 「只写上界」那次的病）；未预注册的描述性配对 ΔNLL 均 −0.0487（两负一正）、Δ出牌/局 均 +2.0（+3.6/−2.2/+4.6）⇒ 方向偏正但**散布 > 均值**，不构成结论。★ **行为带（J-W2.4，只登记不判定）**：`plays/game` 全 9 跑 **31.4–37.0** ⇒ **0/9 在 [18,28.5] 内、全部高于上界**（与混比池过度出牌同一现象；带值来自另一批语料，【R15】）；`winrate` 0.30–0.70（10 局/跑 SE≈0.15）不可分辨。★ **设计有效性脚本自检 3/3 PASS**：`HS17 vs HS04` 的 W2 段 `max|Δ| = 3.93/3.49/3.16`、**W3 段 `== 0.0`**；`HS04 vs HS00` 的 W3 段 `2.13/1.74/1.50`（**只有该动的列在动** ⇒ `plan_extras_zero` 真接上了，不是三臂逐位相同）。★ **顺带修好的既有仪器缺陷**：`il_card_usage.py` 假设每帧都有人类出牌（`bundle.sub_actions[0]`）⇒ 在 **save 口径语料**上 `IndexError`（本批实测），已加「人类不出牌 ⇒ 记『未出牌』桶」分支；**【R2】实证**：对**旧 play-only 语料**重跑逐位复现历史读数（`tvd_human_policy` **0.128083812098682**、`pref_capture_policy` 0.154399383244035、`spearman_cond` 0.723272497070453，全 EQ）。★ **我自己脚本里的两处静默 bug 也被抓出并修**：① `il_hs_report.py` 文件名拼成 `holdout_hshs17_s0.json`（格式串已有 `hs` 前缀又传 `arm.lower()`）⇒ 全文读数静默 `None`、判据一律假 FAIL ⇒ 加 `_tag()` + **缺失数据守卫**（缺数据 = 「不可判」而非 FAIL）；② `il_hs_feature_use.py` 在 `os.chdir(SRC)` **之后**才取 cwd ⇒ 相对路径被拼到 `src/clasher_new/runs/...` ⇒ `FileNotFoundError` ⇒ 改为 chdir 前记 `ORIG_CWD`；③ `il_hs_report.py` 打印 `⚠️` 撞 **GBK** ⇒ 整步失败 ⇒ 内联 `_utf8_stdout()`。★ **净结论/下一刀**：基础设施全部交付且门禁 PASS；**W2 未过判据但不是「没用」（归因强阳性）⇒ 下一刀不是再加特征，而是训练强度/损失/池构成**；**W3 需换有分辨力的仪器并重新预注册**。｜ 读数 `docs/fl_il_2026-09-21/hs_report.json`、`hs_feature_use_HS00_s0.json`、`hs_feature_use_HS17_s0_NEGCTRL.json`；全文 → 分册 **§30** |
| **C27** | ★★ **觉醒 S1（数据保真）已实现并跑通，判据②在字面上 FAIL；但它**第一次**把引擎的觉醒代码路径跑起来，并**立刻暴露两个真崩溃** ⇒ 按预注册判据④**停手**（不做 S2/S3）**（用户 2026-09-22「对于觉醒，我认为是时候去做了」；C21 的直接下一刀）| **背景**：引擎**有**觉醒（`evolutions.py` 42 张周期表 + `battle.py:3068-3070` 触发 + `player.set_evolution_slots`），但 **`rl/` 对 `evo_slots/evo_plays/hero_slots/evolution_state` 引用数 = 0** ⇒ 本仓对局里**觉醒永不触发**，而人类回放牌组**带着** `-ev1` 后缀（11,293 槽）⇒ 重建战局按**基础卡**跑（数据保真缺口）。**实现**（`src/` **一行未改**）：`fl_il_to_bc.py` 新增 `--evo-slots`（缺省关 ⇒ 旧路径逐位不变，由产物层面 E1 检查）、`evo_slot_cards()`（**只接 `-ev*`**；`-hero` 是另一套机制、本仓覆盖 0% ⇒ **显式不接线**；**可声明须两处齐备**：`name ∈ EVOLUTION_CYCLES` ∧ `Card(name).evo_raw` —— 缺一即死代码 ⇒ 计入「未映射」不静默声明）、`install_evo_counters()`（**两个只读计数器**：`finish` 包 `_finish_deploy`，在调用前按 `battle.py:3068-3070` 同组输入读数 ⇒ 口径=**觉醒出牌次数**、覆盖部队/建筑/法术；`wrap` 包 `_wrap` 数 `len(entity_data)==6 and entity_data[5]` = 引擎从出生队列带的**觉醒标记位** ⇒ 口径=**觉醒实体个数**、只覆盖部队/建筑；⚠️ 两口径**量纲不同** ⇒ 预注册初稿写的「`finish ≥ wrap`」**不准确**，已改正）；另修 `run_samples` 只带 `names`（剥掉后缀）⇒ 后缀丢失（就是 §1#8 的成因）、新增 `r["_evo"]`。**结果（200 局；`--stop-mode save --stop-stride 4 --plan-extras --workers 10`；唯一变量 = `--evo-slots`）**：★ **③ 生效性 PASS**：**445 次触发**（我方 248 / 对手 197）、`wrap` **1239**、声明 **191/200 局**（= **100% 的可成功转换局**）—— 旧路径触发**必然 0** ⇒ 这是实打实的阳性。★ **① 能声明率 0.9204 FAIL**（659/716 槽，闸门 0.95）：差集**恰 1 张卡** `elite-barbarians-ev1`→`AngryBarbarians`（`in_cycles=True ∧ has_evo_raw=False`：**本仓没有这张卡的觉醒数值快照** ⇒ 声明也是死代码 ⇒ 判据④失败分支猜的「补映射表」**无效**）。★ **② labels 4469→4182（−6.42%）、`errors` 0→9 ⇒ FAIL**，但**归因明确**：9 局（4.5%）在 on 臂抛异常（`_worker` 吞掉 ⇒ 这些局计数全 0），**逐 tag 剔除后（191 局可比）labels −1.65%（≤5%）**、frames −3.06%、`team_single` −1.68%、`mask_reject` −2.46%、`stop_save_cand` −3.38% ⇒ **剔除崩溃后 ② 会 PASS**；其余负移是「觉醒真的改变了战局」的预期结果。★ **崩溃根因（只读取证，一行未改）**：`evolutions.py:70-86 collect_evo_mechanics()` = **全树扫描 + 同名键首见优先**，而 `evolvedSpellsData.statsTags` 是「**字段名 → 标签串**」的**元数据**表，在 JSON 键序里排**第 4 位**（真数值所在的 `baseData` 排第 30 位）⇒ 标签串**遮蔽真值**：`Witch_EV1.spawnPauseTime='interval_of_spawns'`（真值 7000）⇒ `card_mechanics.py:87 TypeError: str / float`；`GoblinDrill_EV1.deathSpawnCount='spawn_count'`（真值 4）⇒ `battle.py:1383 ValueError: int('spawn_count')`。全量枚举 34 张 evo：mechanics 含字符串值的 6 张，**只有这 2 张是数值字段**被污染（其余 4 张的字符串是合法名字）⇒ 觉醒**必崩**只在 2 张卡上。**为什么从没人踩到**：`rl/` 从没声明过觉醒位 ⇒ 该路径**从未被执行过**。**门禁**：`scripts/selftest_evo_slots.py` **20/20 PASS**（T1 映射与拒收、T2 引擎真跑 Knight 连出 6 次 `finish==2` 且单兵卡 `wrap==finish`、T4 节律 `[0,0,1,1,1,2]`、T7 与 `evolution_state` 解析式一致、**T3 负对照（不声明）两计数器都 0**、**T5 Hero 互斥 ⇒ 两计数器都 0**（证明我的谓词与引擎 `not in hero_slots` **同口径**）、T6 法术只进 `finish`）；`scripts/probe_evo_pair.py`（**同进程配对**，因为【C17】已证跨进程不可复现）⇒ P1 **8 局构造对照逐值相等**、P2 13 局声明/18 触发 PASS、P3 **1/14 崩溃**；⚠️ **第一版 P1 是空洞 PASS**（真实回放 95%+ 的局都声明 ⇒ 自然样本零覆盖）⇒ 改为**显式构造对照**（`--p1-control`），否则 P1 不许报 PASS（与 C26 的 J-W3.2「判据饱和」同型的病）。**附带读数**：E1 schema 不变量 PASS（off 侧**无** `S1_evo`/逐局 `evo_*`）；E2 `wrap/finish = 2.784`；E3 **跨进程噪声地板**（**非判据**）= 与 2200 局旧语料同一批 200 tag 比 `labels` **4468 vs 4469 = +0.022%** ⇒ 5% 阈值对聚合 labels **充分宽**；同进程配对里**逐局语义差双向**（labels 13→13/18→21/38→22/25→29）⇒ 单局可达 **±40%**、聚合仅 −1.65%。**处置**：判据④原文只设想了「映射缺表」与「语义变了」两种成因，实测是**第三种（引擎崩溃）** ⇒ 登记为**预注册缺口**（连同判据①「能映射 100%」vs「能声明 92.04%」的口径歧义）、**不改判据文本**、**停手：不启动 S2/S3**、代码**不回滚**（默认关、`src/` 零改动 ⇒ 不影响任何既有读数），**在崩溃修好并重新预注册之前不用于生成训练语料**。**新开放项**：**O-E1** 缺 `AngryBarbarians` 觉醒快照（57/716 槽落空）；**O-E2** `statsTags` 遮蔽（觉醒必崩）；**O-E3** `[J6.1]` 打印在 `save_frame_share_of_off is None`（全崩时）抛 `TypeError` **把 `errors=N` 的真失败盖掉**（**本批已修**：None 安全 + 新增 `[errors]` 行打印首个 worker 异常与 tb）；**O-E4** `arena.can_deploy_at(..., is_spell=True)` 是死路（`arena.py:181` 调 `TileGrid._is_rolling_projectile_spell()`，**该方法不存在**）本轮不修。**下一刀（预注册 §10，尚未执行）**：`collect_evo_mechanics` 扫描跳过 `statsTags` 子树（一行），判据跑前写死 = 全 34 张 evo 新旧**逐键对比**变化集合**必须恰** = {Witch.spawnPauseTime, GoblinDrill.deathSpawnCount} 且新值 = 7000/4、其余 **32 张逐键逐值相等**；回归 20/20 + 新增 T8（Witch 连出 3 次不抛异常，**跨局边界**）；S1 200 局重跑 ⇒ `errors=0` ∧ labels 漂移 ≤5%；失败分支 = 出现其它卡变化或 errors 仍 >0 ⇒ **停手登记**（不许为「让 errors=0」吞异常）。预注册 §2/§5/§6–§10 = `docs/il_evo_prereg_2026-09-22.md`；读数 `docs/fl_il_2026-09-21/{evo200_off,evo200_on,evo200_verdict,evo_pair_probe}.json`；全文 → 分册 **§31** |
| **C28** | ★★ **O-E2 已修（`collect_evo_mechanics` 跳过 `statsTags`）⇒ 觉醒 S1 的 ②③ 兑现；① 仍 FAIL 且原因单一（一张卡缺数据）**（C27 的直接续作；预注册 `docs/il_evo_prereg_2026-09-22.md` **§10 → §11**）| **做法**（单变量最小改动）：`evolutions.py::collect_evo_mechanics()` 的 `_walk` 加一行 `if k == 'statsTags': continue`（`statsTags` = 「字段名→标签串」的**元数据表**，不是机制参数）。**不动**触发/数值逻辑、**不动** `battle.py`/`action_mask.py`/`player.py`。**判据①（数据层 A/B）**：仪器 `scripts/probe_evo_mechanics_ab.py` —— **修复前逻辑在本文件内逐字复制**（不 import、不靠 git 回滚）⇒ 同进程、同数据、真单变量。全 **34** 张 `evolvedSpellsData`：**其余 32 张逐键逐值 `==` 相等**；变化**恰好 2 处** —— `Witch_EV1.spawnPauseTime` `'interval_of_spawns'`→**7000**、`GoblinCage_EV1_TEMPNAME.deathSpawnCount` `'spawn_count'`→**1**（两处新值都 == 同一张卡 evo 树里的真值）。⚠️ **两处判据在预注册里写错了，已登记为缺口（不改判据文本）**：（3）把第二张卡写成 `GoblinDrill_EV1`，**实际是 `GoblinCage_EV1_TEMPNAME`**（`GoblinDrill_EV1` 的字符串值 `spawnCharacterOnHide='Goblin'` 是**合法的名字字段**）⇒ A1 字面 FAIL / A1' 修正 PASS；（4）A3「新输出不得有任何字符串」**过严**（4 个残留全是合法的名字/动作引用：`onKilledDoneAction`、`nextAction.spawnData`×2、`spawnCharacterOnHide`）⇒ 改为数据驱动的 A3'（残留字符串须能在同卡 evo 树里找到同键的**字符串**真值）⇒ 字面 FAIL / 修正 PASS。**两套口径都保留在读数 JSON 里**。**判据②（回归）**：`selftest_evo_slots.py` **28/28 PASS**（原 20 全未变 + **T8** 6 条 + **T9** 3 条）；**T8 = O-E2 的跨 tick 回归**（Witch 出 3 次 ⇒ 场上存在觉醒 Witch、`evo['spawnPauseTime']==7000`、**推进 8 tick 不抛异常**，并**逐 tick 记录**钉住出兵那一 tick 的 `next_spawn_remaining` **恰为 7.0**；实测轨迹 `[1.0,1.0,0.5,0.0,7.0,6.5,6.0,5.5]`）；**T9 = 量纲构造性演示**（Skeletons `spawn_number=3`：`finish==1`、`wrap==3==n×finish`，把「两口径量纲不同」变成可执行断言）。**判据③（端到端 200 局）**：① 覆盖率 0.9204→**0.9239（692/749）仍 FAIL**；② labels **−6.42%→−1.30%（4465→4407）PASS**、`errors` **9→0 PASS**；③ 触发 445→**482**（我方 267/对手 215）、`wrap` 1311、声明局 **191→200/200**。★ **决定性证据**：修后逐卡触发表**首次出现 `Witch: 5` 与 `GoblinCage: 5`** —— 正是修前**必崩**的两张卡⇒ 修复把「崩掉 4.5% 的局」变成「正常触发 10 次」。**同进程配对探针（同 14 局 tag）**：P3 **CRASHES 1/14 → CLEAN 0/14**。**E1/E2/E3 同修前**（schema 不变量 PASS、`wrap/finish=2.72`、跨进程噪声地板 −0.067%）。**S1 总判（更新）= ① FAIL / ② PASS / ③ PASS**：**① 的失败原因单一且明确** —— `elite-barbarians-ev1`（→`AngryBarbarians`）**本仓没有觉醒数值快照** ⇒ 57/749 槽不可声明。这**不是**「映射表缺一条」⇒ 判据④第一条失败分支（补映射表）**不适用**。**处置**：`--evo-slots` **仍缺省关**（E1 已在产物层验证旧路径不受影响）；**仍不启动 S2/S3**（① 未达标）；**O-E1 留作下一刀**（补游戏数据 = `evo_2025_data.py` 自建 7 张的先例 + 一张**外部来源表**，须另行预注册、判据跑前写死、并给出**来源 URL 与取数时间**；【R10】不猜数值）。预注册 §11 = `docs/il_evo_prereg_2026-09-22.md`；读数 `docs/fl_il_2026-09-21/{evo_mechanics_ab,evo200_off,evo200_on,evo200_verdict,evo_pair_probe}.json`；全文 → 分册 **§32** |

| **O13** | ★★ **IL 的下一刀 = 混比（H-A 胜出）**（IL 二期；C18 的直接下游）| 预注册 `fl_il_il2_prereg_2026-09-22.md` §5：**Stage 4 混比剂量-反应**（play:save = 1:0 / 1:0.25 / 1:1 / 1:3.37[= 塌点]）⇒ 每臂报 **act-AUC** + play 组 top1/NLL + **双侧行为带 [18,28.5]** + 分箱出牌率对靶。**已开工**：`il_bc_sweep.py --mix-ratio`（默认关 = 旧行为逐位不变；F0 回归门 = 旧目录 3 ep 与 `bc_fl.pt` 29/29 `torch.equal`）。**关键实现纪律**：Adam+bs=1 下**逐样本 loss 加权是 no-op**（更新≈`lr·sign(g)`）⇒ 只能改**样本出现频率**。**排在其后**：① Stage 0 尾项（grid 探针补「act 天花板」+ 未锁定帧 ΔAUC 单列）；② Stage 1 输入分派（EBK 派生标量先行；「进 conv vs 进 MLP」须单变量消融）；③ Stage 2 多任务输出 + 回喂（**真值只作标签；自回喂必须与时序同轮**）；④ Stage 3 时序（帧堆叠+1D conv vs 截断 BPTT）。⑤ 管线跨进程 5.9% 漂移未定位（疑与 P4 同源）。★ **2026-09-22 结果（C19）：混比已跑完并判读 ⇒ 得到单调帕累托前沿**（act-AUC 0.5074→**0.7354** / play top1 0.4188→**0.0174**），**无一臂同时过 J8.1+J8.2**，部署侧**四臂全出人类带**（三臂过度出牌 30.7–38.2、塌点 0.3/局）⇒ 旧序里「① 排在混比之后」的其它项**整体后移**；**下一刀 = 解耦**：先做**零成本组合臂**（R337 当 act 门 + R00 当动作头，τ 单档由**留出侧预测出牌率**定，不许拟合部署数据），随后按 §6 分支做真正的 **Stage 2 独立 act 头**；预注册须先加 §6，判据 = 带 [18,28.5] + 圣水分箱对靶 0.107/0.186/0.094 + 圣水中位 6.43 / ≥9 费 14.2% **不被闭环推爆**。|

---
| **O10** | ★★ **探索侧「随机×随机 + 位置随机 + 正弦/衰减」（用户 2026-09-18 提问）——字面方案被定量否证；有效轴是「时长」** | **取证（只读；仪器 `scripts/probe_explore_randomization.py`，留证 `docs/probe_explore_randomization/`）**：① **合取结构**——攒到 Xbow 需**连续 34 帧不花钱**（回费 0.1786/帧），其中**圣水 ≥3 的帧必有合法出牌项**（本卡组任意 4 张手牌最便宜那张 ≤3，70 种手牌穷举），这些帧必须**逐帧**抽中「不出」⇒ 成功率 = `p_pass^k`。② **解析上界（两臂 155,866 帧录像，9,956 次攒费尝试/100k 帧）**：真正的「从低圣水攒到 6」（`k≥17` 桶，7,582 次/100k 帧）事件数 = **4.5×10⁻¹⁰ /100k 帧**（`p=1/6`，字面均匀）；`p=1/3` → 5.9×10⁻⁵；**放宽到荒谬的 `p=1/2`** 也只有 **5.8×10⁻²**；现状策略 `p=0.07` → 1.8×10⁻¹⁶（与 C14 实测的 0 一致）。合计值 5.85 中 **98% 来自 `k=5–9`（普通波动，非攒费）**。③ **引擎蒙卡（同 ckpt / 同 8 局 / 只换行为臂）**：把随机开到**最满**（`uniform`：每 decoder 步在合法项均匀抽 + 合法格均匀抽）**2,084 帧里圣水一次没到 6（峰值 5.36）**，`≥25 帧`/`≥34 帧`的不花钱段**均为 0**（该臂两遍逐字相同）；而**带时长**的 `hold d~U{1..40}` 臂 ⇒ **≥6 帧占 28.96%/38.81%（两遍）**、`≥34 帧`段 **15 次/1,533 帧 ≈ 978 次/100k 帧** ⇒ 与字面方案差 **~12 个数量级**。④ **位置那一半**：落点**本来就在采样**（`Categorical` over 合法格，`follower.py:500-504`）⇒ 实际是**均匀化**；实测该策略落点**高度集中**（两臂 18,040 次部署只用到 **84/90 格**、经验熵 **4.49/4.24 bits ≈ 20 个有效格**、top1 格 12.3%/15.0%、top10 格 **73.3%/78.4%**）而每卡合法区 `mean log` ≈ **5.5~5.65 nat（≈250 格）** ⇒ 剧烈改动，且会给「打哪张牌」**注入结果噪声**（随机落点的 Fireball/Log/Xbow 大多白打）⇒ 与「让 Xbow 值得打」**反向**。⑤ **正弦/衰减**是**训练步**上的调度 ⇒ 对「局内连续 N 帧」的合取结构**零作用**；且行为策略 ≠ 被训策略时 `ratio` 初值 ≠1，被 `clamp(ratio,1±clip)` 截断（`ppo.py:379/381-382`）⇒ **周期性把探索样本的梯度裁掉**；「概率本身随机」比「恒取最大」还差 ~15×（`E[q¹⁷]≈4.9×10⁻⁷` vs `0.5¹⁷=7.6×10⁻⁶`）。⑥ **实现面（子智能体逐行审计）**：仓内**没有任何**探索开关/调度（`ent_coef` 固定 0.01 且从未被覆盖）；`evaluate_batch` **只重算存档 bundle**（不重采样）；**RNG 状态不落盘**（种子在恢复前重播）⇒ 任何额外采样在 resume 后**不可逐位复现**。 | **候选下一步（须单独预注册，【R3】）**：① **不要**做字面方案（本文已给出否证级读数）；② 若走探索侧，**唯一**有效形态是**带时长的 option**（`hold d`，且必须**作为策略自身的动作**带 logprob，否则只加噪声不加梯度；本仪器只证「状态可被访问」，**不证「能学会」**）；③ 位置随机**必须单变量**、且先做「只换落点来源」的同种子配对 A/B（当前 8 局分辨率**判不出来**，§11.13.11）；④ 零新代码的对照 = 抬 `ent_coef`（效果≈压平分布，**一样到不了 6 费**，且它改的是目标函数）；⑤ **最贴病根的是机制侧 O7**（S1 已证：手写「攒费→Xbow」规则打出 **156×** 基线）⇒ 优先做 O7 而非探索噪声。全文 [`docs/exploration_randomization_analysis_2026-09-18.md`](../exploration_randomization_analysis_2026-09-18.md) |**★ 2026-09-18 第二问（偏置随机）追加**：「抬高不出牌概率」**有效**——`q` 的每一点都被放大 `k` 次方：解析 `q=0.9` 在 `k≥17` 桶 = **1,264 事件/100k 帧**（等概率 `4.5×10⁻¹⁰`）；实测 `uniform` **0.000%** 帧圣水≥6 → `bias_0.9` **18.9%**；**on-policy 形态（STOP logit 偏置 β）同样成立**且 `w≡1`：β=5/6/7 ⇒ **12.70%/51.27%/82.98%** 帧 ≥6；**「抬高费用牌概率」反向**（α=1.5 使 ≥6 帧占比 **−36%**）；**★ 但覆盖率 ≠ 会学**：这些轨迹**每局回报从 −4.46 掉到 −41.5/−54.1**（β=6/7）⇒ PPO 会学到「别这么干」，与 S1/O7「约束在机制侧」独立同向。全文 [`../_archive/exploration_bias_pass_2026-09-18.md`](../_archive/exploration_bias_pass_2026-09-18.md) |
**★ 2026-09-18 第三问（状态条件随机）追加**：**「低压时才随机」方向对，且是三轮里第一个「覆盖率与回报同时成立」的结果**——前提成立（门 `d`＝最近 15 帧没掉塔血 ⇒ 开 **60.9%** 帧、最长 **544** 帧、够攒到 6 费的窗口 **838/100k 帧**）；引擎实测（同 ckpt／同 8 局）：基线 `0.000%`／`+1.63` → 未门控 `48.209%`／**`−56.13`** → **`gate@6:d` `6.765%`／`+1.68`**（`gate@6:h` `0.385%`／`−13.21`）⇒ **门控扭回回报、覆盖率 ÷7**（比离线预测 ÷1.6 更贵：持牌推高压力 ⇒ 离线是乐观上界）；**用码内 `PRESSURE_THRESHOLD=2.0` 当低压判据则只开 4.7% ⇒ 门控后 5.0/100k（等于没有）**；**`gate@6:d` 攒到 6 费 133 帧但 Xbow 出手 0** ⇒ 「省下的费没花出去」仍未解决（仍要 O7）；回报差异**不可当判据**（基线跨 run 摆动 +1.63／−4.46，O5）。全文 [`../_archive/exploration_pressure_gate_2026-09-18.md`](../_archive/exploration_pressure_gate_2026-09-18.md) |
**★ 2026-09-18 简单测试（初始模型 + 全过程随机，100 局）追加**：p0 = **全过程均匀随机**、p1 = 初始模型 `solo_main_0.pt` 的确定性副本，**100 局 / 31,491 帧**，录像 schema 5、可在 dashboard（**127.0.0.1:8701**）逐帧回放。**从录像独立复算**：A/B/C = **90.4% / 0.0% / 9.6%**（**B 整包被拒 = 0** ⇒ 掩码修复端到端复核）、**A ∩ 圣水≥6 = 0**、**圣水峰值 5.357 = 开局 5.0 + 2 帧回费**（只在每局第 3 帧）⇒ **100 局里圣水从未超过开局水平**、A 段最长 24 帧、**Xbow 0 次**；胜率 **58.0%±4.9**（对手与 p0 同源 ⇒ **不含绝对强度**）、空 bundle 90.4% ⇒ **全过程随机同样到不了 6 费**（与前几轮解析/probe 逐项吻合）。全文 [`../_archive/rand100_eval_2026-09-18.md`](../_archive/rand100_eval_2026-09-18.md) |

