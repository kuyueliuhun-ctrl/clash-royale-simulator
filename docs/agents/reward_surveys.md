# 分册：外部调研类台账（奖惩机制 / RL 奖惩设计 / 录像格式）

> **本册的由来**：2026-09-19，`AGENTS.md` 再次逼近注入上限（**64,190 / 65,244 B，余量 1,054 B**），
> 而这三条长条目合计约 7 KB。按 §「本文件的由来」既定做法**拆到分册**，主文件只留**一行指针**。
> **⚠️ 只增不改**：以下三行是**从 `AGENTS.md` §B 逐字搬来的原文**（未改写、未删减）。

| 我要找什么 | 去哪 |
|---|---|
| **★ 其他 IL（模仿学习）项目的奖惩机制 → 我们的参考**（2026-09-19；含对**根目录**《三轴对照 我们 vs FirstLight》的独立复核） | 主报告 [`docs/il_reward_reference_analysis_2026-09-19.md`](docs/il_reward_reference_analysis_2026-09-19.md)。**复核出三处需修正**：① **硬错误**（`_DEFAULT_REWARD` 16 键 vs `config.DEFAULT_REWARD` 22 键的**差集精确为 6**，其中 5 个 `engagement_trade*` 是 config-only、**只有 `draw_penalty` 是真实缺口**；而 `rl/reward.py:25` 注释却写「与 config 保持一致；勿单独改一处」⇒ **声称单一常量源而实际不是，关系【R7】**）；② **引用错位**（FL 六项精细奖罚的默认值实测在 `train_ppo_self_play_cluster.py:937-975`，**不是** doc 写的 `launch_ppo_multinode_training.sh:61-77`，后者 77 行全是 exec 参数）；③ **强度不一致**（「掩码结构上不可漂移」应限定为**主路径** `arena.py:705→:752→:616`，doc 自己的 F-2 已列一条**独立复刻**残余路径）。**已执行 doc 的 H5**：`scripts/_mask_diff_snapshot.py` 加 `--compare`/`--selftest`（**5/5 判别力断言**；真实树 **128/128 逐位全等 rc=0**、翻 1 位 **rc=1**），并堵住 doc 未提的洞（`except` 把异常吞成**全零位图** ⇒ 假绿）。核心结论：**IL 阶段结构性没有奖惩**（FL 的 IL 六项损失全掩码 CE/Huber，`penalty` 在 7 个 IL 文件 **0 命中**）；**FL 的 IL→PPO 锚定项默认关闭**（`--expert-bc-manifest` 默认 `None`），真正有 KL 锚定的一手先例是 **VPT**（`L_klpt=ρ·KL(π_pt,π_θ)`，ρ=0.2×0.9995，**替代熵正则**，有「无 KL ⇒ 10 万局停滞」消融）；**「奖励塑形退火」是对 OpenAI Five 的误读**（`anneal` 7 处**无一**指奖励权重，奖励函数「constructed once」）。**判定**：**可做** = I2 时序化 BC（最高性价比，须预注册）/ I3 掩码契约 / I1 仪器 + **H5（已做）**；**待预注册** = I5 优势加权 / I6 return-conditioning / I7 DAgger（效力全依赖 O7）+ **CR 人类数据可行性最小验证**；**不做** = I8 KL/BC 锚定（**方向与病根相反**，且我们无 RM）/ 照搬绝悟 `No-op` 惩罚 / 外部权重当基线。调研分册（各 `_2026-09-19.md`，**未验证项原样保留、来源全标 URL+取数时间**）：`il_reward_mechanism_synthesis`、`alphastar_reward_il_survey`、`juewu_research`(+`/`)、`rlhf_kl_penalty_survey`、`vpt_minerl_dd`、`il_human_data_survey`、`openai_five_reward_anneal_audit`。★ **新发现**：**公开人类 CR 回放数据是存在的**（`VanguardX101/IL_Replay`：**252,238 局 / 17,836,160 动作**、公开非 gated、**许可未声明** `license=None`），但**只有动作侧**（`card/native_x/native_y/tick@20Hz`；塔血只有终局、圣水只有全场聚合、`"hp"` 0 次）⇒ 状态必须**靠引擎重放反推**（FL 的 `cache_builder` 正是「起真引擎重建对局」）⇒ 把「我们无人类数据」从**无解**改成「**有输入、缺管道**」（前置：许可 + 引擎可重放性，两项**未验证**） |
| **★ 跨项目 RL 奖惩设计横向分析 + 人类回放可行性最小验证**（2026-09-19） | ① [`docs/rl_reward_design_crossproject_2026-09-19.md`](docs/rl_reward_design_crossproject_2026-09-19.md) —— **六条设计律**，最硬三条：**惩罚项放在哪里决定可靠性**（环境奖励内 < 损失内 < 结构内 < **契约内**）；**「把奖励塑形权重退火到 0」在整个调查里没有一手实例**（Five 的 `anneal` 是超参/环境特性；VPT 退火的是 KL 系数 ρ）；**密集手工奖励可能与胜率脱钩**（Five 置零 128 个参数使胜率 **+55%** 而塑形奖励几乎不变 ⇒ 与 C14 同型）。另含零和性三处境（Five 方法层零和 / FL 引擎层零和被训练侧破坏 / **我们根本不零和**）与归一化三问全表。② [`docs/il_replay_feasibility_2026-09-19.md`](docs/il_replay_feasibility_2026-09-19.md) —— **公开人类回放能否驱动我们的引擎**：卡键 **100% 可映射**（`card_aliases.resolve_card` 现成；⚠️ 未知键**返回 None 不抛异常**）、卡组 **8/8** 可重建（`-ev1`/`-hero` 走我们已有的 `set_evolution_slots`/`set_hero_slots`）、**手牌循环费用 6/6 精确相等**、**坐标无需变换**（A/B：`raw` 40.5/44.9/61.5% vs `flip_y` 2.3/2.3/0% ⇒ **否证了"需要 32−y"这条我自己先写错的假设**）；但**直接重放只走通 35–62% 的动作**，主缺口 = **逐事件圣水不可观测**（`unaffordable` 62–86）+ 回放 **300.55–311.55 s** 超我们 **300 s** 上限、我们引擎在 **180–223.5 s** 就按皇冠规则提前终局 ⇒ **可用于 IL，但必须先"状态重建"**（正是 FL `cache_builder` 在做的事）。工具 `scripts/_il_replay_extract.py`（WSL python3+pyarrow）+ `scripts/replay_feasibility_probe.py`（venv）；留证 `docs/il_replay_probe_2026-09-19/`。⚠️ 该数据集 **`license=None`** ⇒ 你拍板前**不得用于训练** |
| **★ 两个录像格式逐字段对照（人类回放 vs 我们 schema 5）**（2026-09-19） | [`docs/replay_format_comparison_2026-09-19.md`](docs/replay_format_comparison_2026-09-19.md)；新仪器 `scripts/_il_replay_format_scan.py`（`--schema`/`--stats`/`--check-cycle-elixir`）。★ **一手确认这套人类回放 = FirstLight 的数据集**（README 自述名 `Firstlight anonymized battle replays` + `CR_REPLAY_DATASET_ROOT`）；manifest 明列**脱敏 8 删 / 7 留**。**格式严格固定**：1000 局里 **136 条结构路径恒存在、非 `card_counts` 的可选路径 0 条**；但 `form_at_play` / `ability_source_authoritative` / `deck_card_key_candidates` **三个字段是常量**（后两个连 FL 也不读）。**四套坐标实测**：`native = (display.x, 32000−display.y)` **4001/4001**；`arena_column=chr('A'+grid_x)`、`row=grid_y+1` **0 例外**。**语义验证**：`four_card_cycle_elixir` == 最便宜 4 张费用和 **1941/1941**（⚠️ 但 FL **不读**它）。**卡覆盖 177/178**，唯一缺 **`void`**（引擎 212 名里没有；`cards.json` 123 条里**有**）⇒ 影响 **≈3% 卡组**。**FL 侧消费清单（一手）**：IL 主路径**只读 `payload_json`**（`actions/` 表**无任何 reader**）、落点用 `grid_cell_floor`+`subcell_offset` 并**自己按 `data_i` 翻转**（`native_world_units`/`inversion_applied` 不读）；tick = `+1` 再 `90+⌊(t−90)/5⌋×5`（首帧 4.5 s、250 ms/帧）；**逐事件圣水不是读来的**，是「编造相容牌序 → 与真引擎发牌校准 → 调度动作进引擎重演」；`require_accepted_timeline` 是真门禁，**预检/`validation`/`warning_count` 都不是**（失败**丢标签保帧**）。**我方 schema 5**：帧 **16 键**（+可选 `cards`/`et_tail`）、实体 **15 元组**（schema 3/4/5 = **10/12/15**）、`meta` **无 seed**、`load_league_replays` **丢 schema 号**；★ **`logprob`/`value`/`masks` 在 `run_league.py:190` 被 `_` 丢弃**（三者都**现成算好**）⇒ **我方缺的是「存盘决定」，人类回放缺的是「状态记录」** —— 两格式**互补**、各自都要重建 |

---

## 附：本册三行各自指向的文档与工具

| 主题 | 主报告 | 分册 / 工具 |
|---|---|---|
| IL 项目奖惩机制对我们的参考（含对根目录《三轴对照》的独立复核） | `docs/il_reward_reference_analysis_2026-09-19.md` | 分册：`il_reward_mechanism_synthesis` / `alphastar_reward_il_survey` / `juewu_research`(+`/`) / `rlhf_kl_penalty_survey` / `vpt_minerl_dd` / `il_human_data_survey` / `openai_five_reward_anneal_audit`（均 `_2026-09-19.md`） |
| 跨项目 RL 阶段奖惩设计横向分析 | `docs/rl_reward_design_crossproject_2026-09-19.md` | 5 份统一 schema 对象报告：`rl_reward_crossproject_tables` / `juewu_reward_table_unified` / `vpt_minerl_rl_reward_tables` / `rlhf_offline_il_reward_audit` / `ours_vs_firstlight_reward_tables` |
| 人类回放可行性最小验证 | `docs/il_replay_feasibility_2026-09-19.md` | `scripts/_il_replay_extract.py`（WSL python3 + pyarrow）/ `scripts/replay_feasibility_probe.py`（venv）；留证 `docs/il_replay_probe_2026-09-19/` |
| 两个录像格式逐字段对照 | `docs/replay_format_comparison_2026-09-19.md` | `scripts/_il_replay_format_scan.py`（`--schema` / `--stats` / `--check-cycle-elixir`） |
| 本轮两条"可执行断言"化（H1 / 【R7】） | `docs/agents/structure.md` 的 H1 节、`docs/agents/redlines.md` R7 | `scripts/probe_run_trainer_params.py` / `scripts/selftest_reward_tables.py`；`rl/run_league.py::_report_trainer_wiring`（只报告） |
