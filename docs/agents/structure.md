# AGENTS 分册 · 全项目结构优化（盘点 + Tier 0/1/2/3 执行记录）

> **来源**：从 `AGENTS.md` §B 决策索引迁出的**逐字原文**（2026-09-19）。
> **为什么迁出**：这三行合计 ~12 KB，而 `AGENTS.md` 的**注入上限实测 65,244 B**
> （2026-09-13 曾因超限导致**末尾对会话不可见**）⇒ 按本仓纪律「索引留 AGENTS.md、细节进分册」。
> 返回索引 → [`../../AGENTS.md`](../../AGENTS.md)

| 主题 | 内容 |
|---|---|
| **★★ 全项目结构盘点 + 分级优化方案（用户要求「做一次全项目的代码结构优化」，2026-09-19）**：**现状** = **199 个 `.py` / 62,402 行**（`src/clasher_new/` 91/35,565；`scripts/` 102/26,494）；**9 个 >700 行 God file**（`rl/selftest.py` 5,909 / `battle.py` 3,457 / `rl/dashboard.py` 3,173 / `card_mechanics.py` 1,895 / `rl/run_league.py` 1,787 / `rl/train_solo.py` 1,748 / `belief_planner` 891 / `env_wrapper` 811 / `follower` 806），分两族：**A 多职责混杂**（dashboard/run_league/train_solo，簇边界≈职责边界）与 **B 单一巨型类**。**分层事实**：**引擎 → `rl/` 反向边 = 0**（语法层干净），但 **14 个 rl 模块直插引擎深层内部**（`battle`/`spell_module`/`threat_calc`/`simulate_exchange` 无门面）；**rl 内部 429/572 条 import 写在函数体内（75%）**（部分是显式防循环依赖的惰性 import）。**★ 最贵的真问题 = cwd 隐性契约**：`card_utils.py:4-17` 在**模块顶层**用**裸相对路径** `open('gamedata.json')` 读 5 个数据文件，而它是**全仓被依赖最多的模块（20 importer / 44 条 import 语句，其中 12 个是 rl 模块）** ⇒ **import 任何 rl/引擎模块都隐含要求 `cwd = src/clasher_new`**，仓内靠 wrapper 的 `os.chdir(_SRC)` 把这个约束藏起来了。**★ 测试布局**：`rl/selftest.py` **100 个 `test_*`** 全靠 `main()` **手工逐条登记**（实测 定义=调用=100，无遗漏）+ **4 条硬约束**（`scripts/run_selftests.py:36/42/60` 的 `dir()` 反射按名调用 / `scripts/rl/selftest.py:5-7` 的 `os.chdir`+`runpy` / `main()` 100 行清单 / **外部硬编码 4 个测试名** `scripts/_apply_s2_channel_when_idle.sh:70-71`）；测试归属**三处并存**（`rl/selftest.py` 100 / `scripts/selftest_*.py` 11 / `scripts/test_m*.py` **98**）；`scripts/` 105 个文件里**一次性取证占 64%**；`docs/agents/env.md §2.4` **69 个脚本未登记**。**★ 死件/残留（11 条，全部 0 importer 实测）**：`pathfinding.py`（0 importer，但删前须 `git log --follow` 确认不是对照实现）、`re_lib.py`（0 B）、`tmp_*.py`×5 + `runs/_tmp_*`×18、`gamedata.json.bak_ronin_vines`（341 KB）、`hero_ability_extract.json`（**保留**，可能是待接入）。**★ 方案分四级**（§6，21 项，每项五要素）：**Tier 0 零行为变化可独立验证**（T0-1 死件清理 / T0-2 新增只读结构核对器 `scripts/_structure_check.py` / T0-3 文档补 69 个未登记脚本 / T0-4 `card_utils` 数据缺失时报**期望 cwd**而非自动找路径）；**Tier 1 低风险等价重构**（收敛 `_force_utf8_stdout` 单一实现 / 修 **5 处硬编码绝对路径** `E:/clash-royale-simulator-main/...` / selftest 加**只加不删**的发现与计数 / `runs/_tmp_drawtest` 改 `tempfile` / 显式化 2 个假绿 SKIP）；**Tier 2 结构性拆分**（抽 `_HTML` 1879 行 / 抽 dashboard 数据面 / 拆 run_league 判定与评估并行 / train_solo 抽函数 / `card_mechanics` 拆包 / `scripts/` 分目录 / 抽奖励簇 / 拆 selftest）；**Tier 3 触红线需拍板**（拆 `battle.py`、数据文件归拢、迁 `tests/`+pytest、统一 `sys.path`）。**★★ 两条硬约束**：① **碰不得** = 【R2】`ppo.py` 默认参数 + `DEFAULT_REWARD`、【R7】`config.py:61,63`+`STALL_*`+`PRESSURE_THRESHOLD`、【R13】`action_mask.py`+`get_action_mask_for`、冻结 schema `LEAGUE_REPLAY_SCHEMA=5`、**运行中的 8700/8701 面板**；② **并发会话正在改** `rl/config.py`/`engagement.py`/`battle.py`(root_cast)/`selftest.py`/`docs/agents/*`（未跟踪新文档 15 份 = `docs/cmp_*`(8) + `reward_mechanism_*`(4) + …）⇒ **T2-7 / T2-8 / T3-1 标注「并发期间禁止」**。含 §7 拒绝做的 10 条重构（逐条给理由）与 §8 **7 组冲突并列表**（含「selftest 一个函数到底是 1937 行还是 50 行」的**粗算法 vs AST 精算**真矛盾） | [`docs/structure_optimization_plan_2026-09-19.md`](docs/structure_optimization_plan_2026-09-19.md)（§1 规模 / §2 God file+簇 / §3 重复死码 / §4 分层耦合 / §5 测试布局 / **§6 Tier 0–3 21 项** / §7 碰不得+拒绝做 / §8 冲突） |
| **★★ Tier 0 已执行完毕（2026-09-19，用户拍板「全做 + 死件直接删除」）**：**T0-1 死件清理 31 个**（**3 个 tracked** = `re_lib.py`(0 B) / `pathfinding.py`(0 importer，唯一命中是 profiler 的 `FAMILY` 字典) / `scripts/stop_solo_training_2200.py`(一次性脚本+硬编码绝对路径)；**28 个 untracked** = 根目录 `1`/`.p6.png`/`0631.pdf`/`libg_strings.txt`(789 KB)/`:memory:.ses` + `src/clasher_new/tmp_*.py`×5 + `gamedata.json.bak_ronin_vines`(341 KB) + `scripts/stop_solo_2200.log` + `runs/_tmp_*`×18）—— ⚠️ **未跟踪的删了不可恢复** ⇒ 删**前**逐条记 **sha256+字节+判据**（`docs/structure_cleanup_2026-09-19.md` §2），**删前每一条都由本人重跑 0-importer 复核**（扫描限定 `src scripts ideas`，不扫 6.7 G 的 `runs/`）。**验证**：`run_selftests.py --list` = **100 不变**；子集 **5/5 PASS**；引擎仍可导入；引擎顶层 **34→28** 文件 / **9,997→9,581** 行；**死件复活 = 0**。**★ T0-2 新增只读核对器 `scripts/_structure_check.py`**（7 项检查：①分区规模 ②`_force_utf8_stdout` 三处实现**互异**（`dashboard:3033`/`run_league:1538`/`probe_value_ln:116`）⇒ T1-1 目标已量化 ③**引擎→rl 反向边 = 0** ④死件复活 ⑤硬编码绝对路径 **9 处/7 文件**（盘点稿的 5 处只扫 `sys.path.insert`+正斜杠 ⇒ **两套口径并列**）⑥`rl/` 函数体内 import **两套口径**：含 selftest **566/917=61.7%**、排除 **95/439=21.6%**（盘点稿写 75% 与「58 条」⇒ **三套并列**；交叉验证：本工具对 `selftest.py` 单文件给出 **7 top/471 lazy**，与盘点稿**逐字一致**）⑦selftest **100/100**）；**`--selftest` 9/9 PASS**，含一条**反向验证**（修掉合成树的非法反向 import 后 ③ 必须变 0）⇒ 判别力已证；**纯 `ast` 不 import 产品代码** ⇒ 不受 cwd 契约影响。**★ T0-3 新增 `docs/agents/scripts_inventory.md`**：`env.md §2.4` 只登记 **34** 个 `.py` 名而顶层实有 **91** 个 ⇒ **57 个未登记**（盘点稿按 105 文件算写 69 ⇒ 两套并列），全量表**脚本生成不手抄**，`env.md` 只加**一行指针**。**★ T0-4 `card_utils` 加 `_resolve_data()`**：数据文件缺失时打出**期望 cwd**（原先只有裸 `FileNotFoundError: gamedata.json`）—— **故意不改成 `os.path.dirname(__file__)`**（那会改掉 cwd 契约，属 T3-2）；**A/B 用 `pickle` 逐字节指纹对账 7/7 SAME**（不能 `json.dumps`：`card_data` 有**循环引用**）。**踩到并留档的 4 个坑**：f-string 嵌套引号（3.13 也 SyntaxError）/ Windows python 把 `/tmp/x` 当 `E:\tmp\x` / `json.dumps` 循环引用 / 全树 grep 因 6.7 G `runs/` 超时 | [`docs/structure_cleanup_2026-09-19.md`](docs/structure_cleanup_2026-09-19.md)（T0-1 明细 §1–§5 + 验证 §6 + T0-2/T0-3/T0-4 §7–§9 + 工程坑 §10）、`docs/agents/scripts_inventory.md`、留证 `docs/structure_after_2026-09-19/baseline.json` |
| **★★ Tier 1 已执行完毕（2026-09-19）**：**T1-1 UTF-8 兜底收敛到单一实现** —— 先**纠盘点稿一处不准确**：「3 处函数体已逐字相同」**实测不是**：`rl/run_league.py` 与 `rl/dashboard.py` 的**强版函数体逐字相同**（仅 docstring 异），而 `scripts/probe_value_ln.py` 是**弱版**（只碰 stdout、**无 `errors="replace"`**、不管 stderr）。新增 [`src/clasher_new/rl/io_bootstrap.py`](src/clasher_new/rl/io_bootstrap.py) `force_utf8_stdout()`（两路 + replace、幂等、吞异常、**零依赖**）；3 处 definer 改**别名**（assign / import-as）⇒ **调用点一行未动**；`probe_value_ln` 由弱版升强版 ⇒ **行为只增不减**（已声明）。核对器 ② 升级为**三阶段可读**（否则收敛后会变成**空检查**）：收敛后实测 **定义 1 处 / 别名 3 处 ⇒ 已收敛**；`--selftest` 现 **13/13**，含反向验证「未收敛时 `single_impl` 必须为 False」。**★ 我自己的仪器 bug 留档**：② 最初把 **docstring 也算进指纹** ⇒ 给出误导性的「3 种互异」，已拆成 `body_digest`（判据）与 `full_digest`。新增回归 [`scripts/selftest_io_bootstrap.py`](scripts/selftest_io_bootstrap.py) **12/12 PASS**（行为 9 + 单一来源 3，复用核对器**不造第二套口径**）。**★ T1-1b 部分收敛 + 明确不做**：全仓 **61** 个内联 `stdout.reconfigure` 调用点，只收敛**该块之前已有 `sys.path.insert`** 的 **29** 个（codemod [`scripts/_converge_utf8_bootstrap.py`](scripts/_converge_utf8_bootstrap.py) 留档）；**17 个无 guard + 15 个形态不匹配不做** —— 对它们收敛必须**新增 `sys.path.insert`**，会**改变 import 解析顺序**，而收益只是 DRY（这些是**调用点**不是第二份**实现**）⇒ 风险不对称。**★ 顺带量到的真实洞**：**32 个文件仍只处理 stdout** ⇒ **stderr 乱码**（实测证据：`duel_search.py` 的 traceback 在 GBK 下就是乱码）。**T1-2 9 处硬编码绝对路径 → 0**（核对器 ⑤ 复核；7/7 编译；`duel_search --help` rc=0）—— ⚠️ **一条重要限定**：该脚本在**仓库根 cwd** 下**仍失败**，但那是 `card_utils` 的 **cwd 契约**（T3-2），**不是回归**（取证：`git show HEAD:` 的修改前副本在同一 cwd **报同一个错**；且这次能看懂原因正是 T0-4 的收益）。**T1-3 selftest 加 x/y 计数 + 逐测试耗时**：做法是**入口 `_instrument_tests()` 把 `globals()` 里的 `test_*` 换成计时包装** ⇒ `main()` 那 **100 行手工调用逐字未动**（AST 对比 HEAD：**100/100 且序列 True**）；`--order-check` 报**定义序 / `main()` 手工序 / `dir()` 字母序**三者的差集（**集合相同、顺序两两不同**）；⚠️ **偏离方案处**：方案写「`--list` 增加差集报告」，我改成**独立开关 `--order-check`**（`--list` 有外部消费者，改它的输出契约风险不对称）。**T1-4** `runs/_tmp_drawtest` → `tempfile.mkdtemp()` + `finally`（原写法断言中途失败就**在仓库留残留**且依赖 cwd）⇒ 跑后实测**无该目录**。**T1-5** 两处「假绿」SKIP → `_mark_skip()` + `[SKIP-NO-CKPT]` + 汇总**重复打印**；用**打桩空 ckpt** 真跑跳过分支 ⇒ 2 条都被登记（⚠️ **不改绿/红语义**：缺 ckpt 仍**不算 FAIL**，只把"没测"与"测过且通过"分开）。**Tier 1 合并回归**：selftest 子集 **10/10**；`check_dashboard_js.py` **FAILURES=0**；核对器 `--selftest` **13/13**。**未决**：32 个 stdout-only 调用点、15 个形态不匹配、**全量 `main()` 下的包装机制未端到端跑过**（【R19】只跑子集） | [`docs/structure_tier1_2026-09-19.md`](docs/structure_tier1_2026-09-19.md)（§1 T1-1 含 §1.4/§1.5、§2 T1-2、§3 T1-3、§4 T1-4、§5 T1-5、§6 未决、§7 工程坑） |
