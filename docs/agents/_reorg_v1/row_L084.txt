| **★★ 全项目结构优化（盘点 + Tier 0/1/2/3 执行记录；2026-09-19）** | **拆到分册**（原三行 ~12 KB 会挤爆 65,244 B 注入上限）→ [`docs/agents/structure.md`](docs/agents/structure.md)：**盘点**（199 `.py` / 62,402 行、9 个 God file、**引擎→`rl/` 反向边 = 0**、**`card_utils` cwd 隐性契约**、selftest 100 个手工登记）→ **Tier 0 完成**（死件清理 **31**、只读核对器 `scripts/_structure_check.py`（自检 13/13）、`docs/agents/scripts_inventory.md`、`card_utils` 报错带**期望 cwd**）→ **Tier 1 完成**（UTF-8 兜底收敛为**单一实现**、硬编码绝对路径 **9→0**、selftest **x/y 计数 + 逐测试耗时**、tempfile 化、**假绿 SKIP 显式化**）→ **Tier 2 部分完成**（T2-1/T2-2：`rl/dashboard.py` **3,171 → 566 行** ⇒ 抽出 `rl/dashboard_html.py`（1,879 行内联页面，值 sha256 **逐字相同**）+ `rl/dashboard_data.py`（常量/4 缓存/24 函数，A/B **4/4 SAME**）；⚠️ 抽 `_HTML` 打破的 `check_dashboard_js.py` 已被它自己抓到并修好）。**Tier 2 第二批完成**（**T2-3 判定语义**半簇 → `rl/league_rules.py`（**8/8 逐字相同**；**评估并行半簇未搬** —— 依赖 `_ET_MEASURE` 会成环，且 `selftest.py:5298` 断言该 worker 必须留在 `rl.run_league`）；**T2-5**
`card_mechanics.py` **1,896 → 861 行**（M8 段 1,040 行拆出，**公开命名空间 65 名逐名相同**；刻意**不**改成包以免破坏 `battle.py:5` 的 `import *` 语义）；**★ 意外收获**：`test_m3_evo.py` 的 **12 个失败里 10 个是 GBK 编码假失败**（测试名含 ⑪⑫…）⇒ 给 **13 个入口脚本**补 UTF-8 兜底后 **39/12 → 60/2**（余 2 个是真实行为失败，先 A/B 证明其既有））。**Tier 3 · T3-2 Stage A 完成**（**取消 cwd 契约**：`card_utils._resolve_data` 改为 `__file__` 优先，与本来就正确的 `card_aliases.py:432` 看齐；A/B 派生数据 **7/7 逐字节 SAME**；**从仓库根**跑通 `import battle` / `duel_search --help` / `assassin_left_bridge_test`（改前**全部失败**）；**Stage B 搬文件明确不做** —— 实测 `.pt` 被 **34 个文件**、`gamedata.json` 被 **~20 份文档**引用，而契约修好后搬文件**零功能收益**）。**T1-1b 收尾**（手写 `sys.stdout.reconfigure` 块 **0 残留**：本轮再收敛 **29+2** 个 ⇒ 全仓 **77** 个脚本走单一实现；核对器新增 **⑧**（门禁，含判别力断言）与 **⑨**（只报告）；⚠️ 收敛引入的新失败模式 **`import` 早于 path 引导** —— 真 bug 只有一个 `_schema5_probe.py`（已修并端到端跑通），`selftest_engagement_trade_online.py` 经查**不是回归**（按文档 cwd **3/3 PASS**））。**T2-8 完成**（`rl/selftest.py`
**6,023 → 172 行**：100 个测试按定义序切成 `rl/selftests/part{1..5}.py`（各 20）、共用底座 `rl/selftest_common.py`、聚合文件只留 `main()`；**四条硬约束一条未破**（`main()` 调用序列 vs HEAD **100/100 逐字相同**、`--list` **100**）；**全量套件真跑过 = 100 通过 / 0 失败 / 0 跳过、`ALL SELFTESTS PASSED`、累计 218.5 s**；配套 6 处必改（其中「聚合文件顶部 path 引导」不做则全量套件**根本起不来** —— 与 T1-1b 同类失败模式的**第二次**，故核对器 ⑨ 覆盖面已扩到 `rl/` 与引擎顶层）；★ 我这次漏了 3 次，**全部由仪器抓到**（状态变量 `AnnAssign` 漏搬 / 核对器 ⑦ 假失败 / path 引导缺失））。**T2-6 判定「不做」**（`scripts/` 分目录：实测引用面 **1,293 次提及 / 113 份文档**，其中 **~1,167 落在历史留证文档**，而本仓纪律是「只增不改历史结论」；连最小的家族 `_survey_*`（9 个）也要碰 8 份历史文档 ⇒ 要么漏改、要么改写历史。**功能面反而很小**：`.sh` 仅 5 处、`.bat` 只引用本就要保持原位的 `scripts/rl/*`。⇒ 改用**替代物**：`scripts/README.md`（**自动生成**的分类索引，4 类 + 判据 + 清单，`--write-scripts-readme` 重写）+ 核对器新增 **⑩**（README ↔ 脚本集一致性，**进硬门禁**，判别力已证：加一个脚本 ⇒ **FAIL/DRIFT** rc=1））。**余 6
项全部判定「不做」并逐条留证**（[`docs/structure_remaining_2026-09-19.md`](docs/structure_remaining_2026-09-19.md)）：**T2-3 评估并行半簇** —— 被**既有 test 契约**挡住（`selftests/part5.py:1123` 断言 `_eval_pair_worker_main.__module__ == "rl.run_league"`，spawn 的 pickle 路径）；**T2-4** `train_solo` 抽函数 —— §7.1【R2】碰不得 + **无逐位对账**（CUDA 同配置单点差 0.60）；**T2-7 完成（用户拍板「做」）**：奖励簇 **L38-264（227 行 / 14 定义）** → `rl/reward.py`，`env_wrapper.py` **812 → 598 行**；**纯搬运逐字节相同**（`git show HEAD:` 对账 True）、**刻意不**把 `_DEFAULT_REWARD` 改成从 `config.DEFAULT_REWARD` 派生；14 个名字（含下划线）显式重导出 ⇒ 调用方一行未改。★ **验证 = 数值逐位 A/B**（纯函数才能这么做）：`compute_reward` **4,000 例 0 不一致**、helper **5,021 例全同**、且 `env_wrapper.compute_reward is rl.reward.compute_reward` = True（非包装器）；全量套件 **100 通过 / 0 失败**。⇒ 与 T2-4 的分界**不是胆子，是有没有逐位对账手段**。**T3-1** 拆 `battle.py` —— §7.2 **#3 明确「拒绝做」**（【R13】128 张位图证不了全局等价）；**T3-3** 迁 `tests/`+pytest —— §7.2 **#2 拒绝做**（【R19】+ 2 个硬编码调用方），且 **T2-8
已把它的动机解决**（6,023→172 行）；**T3-4 完成（用户拍板「做」）—— 做的是「顺序保证」而非「参数统一」**：先量骨架 ⇒ **135 条引导语句的参数表达式有 ~10 种**（各算各需要的路径）⇒ **参数层面无可统一**；且 guarded 72 / unguarded 63，加 guard 会改 `insert(0,…)` 的**顺序语义**（方案 §7 警告的正是这个）⇒ **不动**。⇒ 改为交付**真正可统一且有价值的那一半**：核对器 **⑪「入口文件的 path 引导必须先于产品 import」**（**门禁**，判别力已断言）—— 这个 bug 类已两次真实咬人（`_schema5_probe.py` 与 T2-8 拆分后的 `rl/selftest.py`）；⑨ 覆盖面同时收紧以去掉假阳性。**我自己在这项里踩的两个坑已留档**（判据把引擎顶层当违规 ⇒ 10 个假阳性；行级替换撞上**两处同形语句**+把注释插进**续行中间** ⇒ 两次 SyntaxError）。**⇒ 方案 §6 的 21 项现在全部有结论：完全完成 15 / 部分完成 3（T2-3、T2-6、T3-2 各有一半判定不做）/ 判定不做 3（T2-4、T3-1、T3-3）**（每条不做都带证据）。**全部读数、口径冲突（并列不调和）与未决项**见分册与各 Tier 文档 |
