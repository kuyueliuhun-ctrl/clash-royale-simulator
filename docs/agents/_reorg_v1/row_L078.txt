| **★★ 修复：掩码 `used` 集合 0/1-based off-by-one（B 类「非法卡包」根因，2026-09-18）**：`RLEnv.get_action_mask_for` 存 **0-based** 下标却用 **1-based** `sa.slot` 做成员测试 ⇒ partial 出现「(s, s−1) 相邻降序对」时低槽位被整段跳过 ⇒ **掩码放行已用槽位** ⇒ 采样器照掩码行事 ⇒ 提交**重复槽位**的包 ⇒ `validate_bundle` **整包拒绝**（白掉一帧 + `invalid_penalty`）；真实 100k 录像里造成 **497 帧（0.7%）/ 39 of 280 局 / 最长连续 225 帧 ≈112 s**。**一行修复 + `act()`/`act_parallel` 两条零成本掩码不变式 + 回归测试 `test_mask_partial_bundle_invariants`**（判别力已证：换回修复前实现即失败）。同种子对照：掩码层 **44/256 → 0/256**、rollout 拒绝帧 **474/1707 → 0/1609**；端到端（修复后产录像 → **同一台仪器**审计）**B 类 = 0/3413 帧**。⚠️ 与「攒费」无关（修复后 `A ∩ 圣水≥6` 仍 = 0，C14 不变） | [`docs/mask_used_slot_offbyone_fix_2026-09-18.md`](docs/mask_used_slot_offbyone_fix_2026-09-18.md)、留证 `docs/mask_used_slot_offbyone_fix_2026-09-18/`、台账 **O9** |
