# AGENTS.md 整理草稿 v2（三层形态）—— 报告

> **状态**：**✅ 已于 2026-09-19 上线**（`AGENTS.md` 就是本形态）。
> 产物：`AGENTS.md`（已上线）、`docs/agents/agents_long_entries.md`（下沉正文逐字件）、
> `docs/agents/_reorg_v1/`（上线前原文、v1 草稿、抽取件、12 份取证记录、生成脚本、两份报告）。
> v1 报告见同目录 `REPORT.md`。

## 0. 上线记录（2026-09-19）

| 项 | 内容 |
|---|---|
| 上线命令 | `python3 docs/agents/_reorg_v1/build_v2.py --apply` |
| 结果 | `AGENTS.md` **61,545 → 22,291 B（−63.8%）**；距注入上限余量 **3,699 → 42,953 B** |
| 仓库校验器 | `python3 scripts/_agents_split.py --check` ⇒ **PASS**（尺寸 / **不丢内容 0 行** / **坏链 0** / 分册节号 1..9） |
| 独立复核 | 6/6：A 区 19 条红线逐字保留、原引言逐字、**B 区 10 行 + C 区 6 条 0 行丢失**、最大行 795 B（≤800 闸门）、坏链 0、分册 §1–§16 齐全 |
| **幂等性** | 从 `_reorg_v1/AGENTS.pre-v2.md` 重跑 `--apply` ⇒ `AGENTS.md` **sha256 逐字节相同**（`585fe6db…b2353e`） |
| 【R18】文档同步 | `docs/README.md` 两处（第 21 行「AGENTS 分册索引」→ 记 v2 三层形态与 `agents_long_entries.md`；第 97 行 `agents/` 目录 → 补两册），并新增一行 `agents/_reorg_v1/` 留证目录；**`docs/agents/plans_runs_docs.md` 未动** —— 其页眉声明「逐字未改」，不为加一行而破坏该声明 |
| 产物处置 | `AGENTS.reorg-draft.md` **已删除**（上线后与 `AGENTS.md` 重复，留两份必漂移）；上线前原文 = `_reorg_v1/AGENTS.pre-v2.md`（61,545 B）+ `git HEAD:AGENTS.md` |
| 回滚 | `cp docs/agents/_reorg_v1/AGENTS.pre-v2.md AGENTS.md`（或 `git checkout -- AGENTS.md`） |
| 仍未处置 | §6 的 **8 处缺口/不一致**（B 区 3 + C 区 5）—— 本次按 **(b) 只加备注**，**台账与既有文档一字未改** |

## （以下为上线前的草稿说明，保留原文）

## 1. v2 的形态（与 v1 的区别）

| 段 | v1 | **v2** | 依据 |
|---|---|---|---|
| 头部 | 原样 | 原样 **+ 新增「文件形态纪律」6 行**（在场必要性四类 + 闸门 ≤40 KB / 单行 ≤800 B + 只增不改 + 逐字搬迁） | 把「重要且不长的保留」固化成可核对判据 |
| **A 红线 19 条** | 原样 | **原样不动**（3,271 B） | 必须遵守 ⇒ **不可拆**：想不起来的红线等于没有红线 |
| **B 决策索引** | 一行式（812–1,470 B/行） | **一行式压紧**（632–795 B/行） | 索引必须在场；把指针也拆走 = 一跳变两跳 |
| **C 当前活跃工作** | 原样 25,337 B | **状态速览表 6 行 + 未决指针**（4,623 B） | 完成态不需要每次会话都在场；但**状态必须留** |

## 2. 测量

| 项 | 原文 | v1 | **v2** |
|---|---|---|---|
| **AGENTS.md 等价体** | **61,545 B** | 46,137 B | **22,657 B**（**−38,888 B / −63.2%**） |
| 距注入上限 65,244 B 余量 | 3,699 B | 19,107 B | **42,587 B（65.3%）** |
| 单行最大 | **6,918 B** | 1,470 B | **795 B**（全 115 行无一超闸门） |
| A 区 | 3,271 B | 3,271 B | 3,271 B（不动） |
| B 区 10 行 | 26,822 B | 11,070 B | **7,511 B** |
| C 区正文 | 25,337 B | 25,337 B | **4,623 B** |

下沉正文（逐字，不注入）：`docs/agents/agents_long_entries.md` **58,958 B** = B 区 26,822 B + C 区 25,337 B + 出处/取证头尾。

## 3. 校验（独立脚本复算，7/7 通过）

| # | 断言 | 结果 |
|---|---|---|
| ① | 去掉「草稿标识行」与 6 行纪律块后，草稿头部 = 源文件第 1–76 行**逐字**（含 A 区红线 19 条） | ✅ |
| ② | `R1`–`R19` 19 条全部在场 | ✅ |
| ③ | B 区新行 10 行、**格数均为 3**（两列，与表头一致）；原 10 行**逐字**进分册 | ✅ |
| ④ | C 区 6 条**逐条逐字**进分册；草稿中不再残留源 C 正文 | ✅ |
| ⑤ | 分册锚点 16 个（§1–§16） | ✅ |
| ⑥ | 单行最大 795 B ≤ 闸门 800 B | ✅ |
| ⑦ | 生成脚本内置断言：B 区**行号+字节数**双断言、C 区**条目边界**断言、A 区逐字断言 —— 任一不满足即 **ABORT**（不产出错位草稿） | ✅ |

## 4. ★ C 区取证：查出 **5 处缺口**（本次最重要的发现）

C 区 6 条做了与 B 区同样的「承重事实 → 目标文档 `file:line`」取证：

| 条 | 结果 | 缺口 |
|---|---|---|
| **C0** `run100k` | **5 MISSING** | ★ **`run100k`「已跑完」这个状态、`runs/run100k/` 路径、14 个录像名、`+ 14 ckpt`、`runs/run_schema5/` —— 在三个目标文档里一个都不存在**（`docs/run100k_2026-09-18.md` 仍停在启动记录、`plans_runs_docs.md §8` 无 run100k 行、ledger 无；实证是 `runs/run100k/run_state.json` step=100000）。⇒ **这些事实此前只存在于 `AGENTS.md`** ⇒ 简单压掉就是真丢信息 |
| **C1** `long1m` | 1 PARTIAL | **状态相反**：`docs/long1m_stopped_2026-09-18.md:50` 写 verdict **不会产出**，`docs/agents/plans_runs_docs.md:51`（§8 台账）仍写「**待出**」 |
| **C2** 新方案 | **0 缺口** | 35 条承重事实全部在预注册/判读/台账里 ⇒ 可安全压缩 |
| **C3** `et_solo100k` | 17 PARTIAL / 0 MISSING | 细节分散在 run 记录 §10/§11/§12 与预注册 §11.13.6–13；判读文档只有总述 ⇒ 必须保留「不构成判决 + n=1 分辨率标定」在场 |
| **C4** 训练健康 | 1 PARTIAL | 一条不变式只在代码里（`train_health.py:283,296-297`、`dashboard_html.py:1031`），主文档无明述 |
| **C5** 五份报告 | 1 PARTIAL | 「五份各自独立、不合并」只有 ④⑤ 明写，①②③ 只有编排者声明 |

**对设计的影响**：C 区**不能**压成纯指针 —— 所以 v2 的速览表专门用「必须在场的要点」这一列把 C0 的 5 个缺失事实、C1 的终止事实、C3 的分辨率标定**留在场**；同时把 5 处缺口逐条记进分册 §11–§16 的备注与 `_reorg_v1/verify_C*.md`。

## 5. B 区取证（回顾，详见 `REPORT.md` §3）

10/10 行取证：7 行 0 MISSING；3 行各 1 处不符 —— L83 行号口径（`2996-3090` vs 分册 `2996-3077`）、L84 行内自相矛盾（「余 6 项全不做」vs 结算 15/3/3）、L85 措辞（「三条逐字搬入」实测 2/3）。**原文一律未改**，只加备注。

## 6. 需要你拍板的 3 件事

1. **是否采纳 v2 覆盖 `AGENTS.md`**（本版可直接用；`AGENTS.md` 目前仍原样）。
2. **8 处缺口/不一致怎么处置**（B 区 3 处 + C 区 5 处）：
   - 方案 (a) **补写进台账/文档**（例如给 `docs/run100k_2026-09-18.md` 追加终局状态、给 `plans_runs_docs.md §8` 补 run100k 行、修正 long1m 的「待出」）——这是长期正确的做法，但属于**改写既有文档**；
   - 方案 (b) **只在 `AGENTS.md` / 分册里加备注**（v2 现状，零风险，但台账仍然是错的）。
3. **是否追求"终局形态"**（去掉 `agents_long_entries.md` 这一层，指针直连一手文档）：
   现在这一层是 58,958 B 的「第二手摘要」，而本次取证已证明**摘要层正是漂移来源**（B 区 3/10 行、C 区 6/6 条都有偏差）。
   但要删它，**必须先做第 2 项的 (a)** —— 否则会把只存在于摘要里的信息（尤其 C0 那 5 个）一起删掉。

## 7. 复现 / 回滚

```
python docs/agents/_reorg_v1/build_v2.py     # 幂等；行号+字节+边界三重断言，AGENTS.md 变动即 ABORT
```

- v1 草稿留存：`docs/agents/_reorg_v1/AGENTS.reorg-draft.v1.md`（46,137 B）。
- v1 的分册 `docs/agents/index_notes.md` **已删除** —— 它的 B 区 §1–§10 与新分册是同一批正文，留两份必然漂移；新分册是它的超集。
- 回滚 = 删除 `AGENTS.reorg-draft.md`、`docs/agents/agents_long_entries.md`、`docs/agents/_reorg_v1/`；`AGENTS.md` 从未被改动。
