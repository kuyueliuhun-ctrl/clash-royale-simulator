| **★ 状态条件随机（第三问）：低压时才随机（用户追问，**只分析不实现**）**：**方向对，且实测买回了回报**——前两轮的致命伤是「攒费轨迹回报差」（未门控 β=6 ⇒ 每局 **−56.13**）。**① 前提成立**：现在**确实有足够长的低压时间**（门 `d`＝最近 15 帧没掉塔血 ⇒ 开 **60.9%** 帧、最长 **544** 帧、够攒到 6 费的窗口 **838/100k 帧**；门 `h`＝半场无敌 ⇒ 44.7%／195 帧／752）。**② 实测（同 ckpt／同 8 局）**：`sample@0` 基线 **0.000%** 帧圣水 ≥6、回报 **+1.63**；`sample@6`（未门控）**48.209%**、**−56.13**；**`gate@6:d`（低压才偏置）6.765%、回报 +1.68**；`gate@6:h` 0.385%、−13.21；`holde@40:d`（可中止 option）16.131%、−38.04。⇒ **门控把 −56 的崩坏扭回基线水平、同时把 ≥6 帧从 0 抬到 6.8%**（三轮追问里**第一个同时成立**的结果）。**③ 代价＝覆盖率 ÷7**（48.2%→6.8%），比离线预测的 ÷1.6 更贵，因为**持牌本身推高压力**（引擎门开启率 24.2%／42.8% ＜ 离线 44.7%／60.9%）⇒ 离线数是**乐观上界**（非外生）。**④ 门的选择决定成败**：若用**码内 `PRESSURE_THRESHOLD=2.0`**（`belief_planner.py:51`）当低压判据，它只开 **4.7%** 帧 ⇒ 门控后覆盖率 **5.0/100k**（≈ 等于没有）——因为 `_crude_enemy_pressure` 对**全场**敌方单位求和、中位 `threat=4.99`。**⑤ 两个限定**：n=8 且**基线回报跨 run 摆动**（+1.63 vs −4.46，【O5】）⇒ 回报差异**不可当判据**，只能读「没有出现 −56
那种崩坏」；**「省下的费没花出去」仍未解决**（`gate@6:d` 攒到 6 费 **133 帧、Xbow 出手 0**）⇒ **仍要 O7**。**★⑥ 我自己的推理错误留档**：最初用「平均场」估门控代价（把 17 帧当 17 次独立抽签 ⇒ 掉 10 个数量级）——**错了**：门状态**强自相关**（开 12 帧／关 15 帧交替）而必须拒绝的 17 帧**连续** ⇒ 代价由**窗口长度分布**决定，实测只掉 **÷1.6~÷4.6**（离线逐帧乘积）／**÷7~÷125**（引擎）。含建议形态（**`slot_logits[STOP] += β(t)·1[低压]`，on-policy、`p=0` 逐位回旧**）与两段判据／失败分支 | [`docs/exploration_pressure_gate_2026-09-18.md`](docs/exploration_pressure_gate_2026-09-18.md)、仪器同前（新增 `--mode pressure`／`--gate-mode`／臂 `gate@β:门` 与 `holde@D:门`）、留证 `docs/probe_explore_randomization/{pressure,gated_*,pressure_gate}.*`、台账 **O10** |
