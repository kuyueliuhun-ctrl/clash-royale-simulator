| **★ 上游引擎增量核查（原作者 Jason-XII 最新版 `f616f19` vs 我们）**（2026-09-19） | 全文 → [`docs/upstream_engine_delta_2026-09-19.md`](docs/upstream_engine_delta_2026-09-19.md)（逐字原稿合并件 → [`upstream_four_sections_merge_2026-09-19.md`](docs/upstream_four_sections_merge_2026-09-19.md)，四份源稿的 4 处截断已在其 §0.2 标注）。**分叉点 = `f20fa4d`**（两仓 `merge-base --is-ancestor f20fa4d HEAD` 都 **rc=0**；我方分叉后 **242** 提交 / 上游 **24** 提交）。★ **退出码陷阱**：`merge-base --is-ancestor f616f19 8d2552a` 是 **`rc=128` + `fatal: Not a valid object name`**（上游独占对象不在本机），**不是**「不是祖先」（那才是 `rc=1`）⇒ 不得用它断言「两仓无共同祖先 / 独立历史线」（与 `docs/upstream_claim_audit_2026-09-19.md` 并列不调和，两说 + 原始命令见正文 §1.2）。**上游 24 提交里 6 个动引擎**（仅 `battle.py`/`arena.py`/`pathfinding_heap.py`/`card_utils.py`，净 **+52/−33**），**引擎数据与基座零改动**。逐条：① **建筑掉血 `372ed0e` = 我们已在分叉点后独立修好**（`card_utils.py:304-306` `lifeTime/1000` + `battle.py:1357-1360` 去重复 `take_damage`）② **碰撞 `*0.5`（`d1e0a16`）未跟进** ⇒
建议**先测**（我方 `battle.py:3299-3303` 无 `*0.5`，另有上游没有的除零保护 + 塔矩形推出）③ **20 Hz（`fcb1d24`，自称 BREAKING）未跟**（我方 `rl/env_wrapper.py:88-89` = 30×1/60 = 0.5 s/决策，与上游 10×1/20 **等价**）④ **河面 A\*（`2daab60`+`574e946`）建议不照搬** —— 上游最终版把 jump 触发整段注释掉、**再无 `jumping_across_river = True` = 死代码**，我方那段是**活代码**（`battle.py:1187-1190 / 1233-1239`）⑤ `1e9716b` 仅注释。**非引擎侧零影响**（`grep -rn environment src/clasher_new/rl/ --include=*.py` = **0**）。★ **顺带查出我们自己的四处**（非上游引入；**本轮一行代码未改，待拍板**）：**P1** `battle.py:3292` `if abs(direction_vector) == 0: return` 应为 `continue`（二重循环内 ⇒ **同心重叠一次性跳过本 tick 剩余所有碰撞对**；**上游同一行逐字相同、作者未修**）；**P2** 桥面最外侧 **16** 个半格（`x=2.25/4.75/13.25/15.75`，各 4 行）在 tilemap 里是 `W` ⇒ A\* 计价 **800** 而 `arena.py:113-116` 判其**可走**⇒ **名义 3 格宽的桥，对行军实际只有 2 格宽**（上游用「`W` 降到 50」绕过，根因两边都没修）；**P3（高）过河 jump 是**无界闩锁** —— 复位只认 `y>17`、位移分支带 `or self.jumping_across_river` ⇒ 目标停在河带 `[15,17]`（**桥上**）或回到自己一侧时**永久**停在 jump 态：**贴脸 18
s、承伤 0**、且被误判成空军（实测原始输出在正文 §5-P3）；**上游把整个 jump 实现注释掉（死代码）⇒ 这条只我们暴露**；**P4** `arena.walkable_cache` 是模块级全局 + `int(x)` 向零截断 ⇒ **一次越界查询就把 `(0,y)` / `(6..11,0)` 永久写成不可走**（跨局生效；**上游逐字同构、同样未修**）。 |
