| **★ 分析：探索侧「随机×随机 + 位置随机 + 正弦衰减」（用户提问，**只分析不实现**）**：**字面方案（逐帧 i.i.d. 随机）在目的上做不到任何事**——攒到 Xbow 是**合取**事件（连续 34 帧不花钱，其中 ~17 帧「有牌可出」必须**逐帧**抽中「不出」）⇒ 成功率是**乘积**（指数在帧数上）。**实测**：把随机开到**最满**（`uniform` 臂：每 decoder 步在合法项均匀抽 + 合法格均匀抽）**2,084 帧里圣水一次没到 6（峰值 5.36，≥25 帧与 ≥34 帧的不花钱段均为 0）**；解析上界（只读录像、9,956 次攒费尝试/100k 帧）**真正的「从低圣水攒到 6」事件 = 4.5×10⁻¹⁰ /100k 帧**（`p=1/6`；即便放宽到**荒谬**的 `p=1/2` 也只有 5.8×10⁻²）。**决定成败的是「时长」不是「随机」**：同一 ckpt / 同 8 局，换成**带时长**的 `hold d~U{1..40}` ⇒ **≥6 帧占 28.96%/38.81%（两遍）**、≥34 帧的段 **978 次/100k 帧**（与字面方案差 **~12 个数量级**）⇒ 与骰子分析同构：**只有 option（带时长的动作）有时域能力**。**位置那一半**：落点**本来就在采样**（`Categorical` over 合法格）⇒ 用户说的其实是**均匀化**；实测该策略落点**高度集中**（两臂 18,040 次部署只用到 **84/90 格**、经验熵 4.5/4.2 bits ≈ **20 个有效格**、top10 格占 **73%/78%**）而每卡合法区 ≈**250 格** ⇒ **剧烈改动**，且会给「打哪张牌」注入**结果噪声**（随机落点的 Fireball/Log/Xbow 大多白打）⇒ 与「让 Xbow 显得值得打」**反向**。**正弦+衰减**是**训练步**上的调度 ⇒ 对「局内连续 N
帧」的合取结构**零作用**，且会周期性把探索样本的梯度**裁掉**（行为策略 ≠ 被训策略 ⇒ `ratio` 初值 ≠1，被 `clamp(ratio,1±clip)` 截断，「概率也随机」比「恒取最大」还差 ~15×）；三个新超参在 n=1 分辨率下**不可判**。含 **§7 三种实现写法对照**（唯一推荐：**混合即策略**、记 `log π_train`、`p=0` 逐位回旧行为、【R2】）、**RNG 状态不落盘 ⇒ resume 后不可逐位复现**的坑（子智能体逐行审计）、以及替代手段对比（抬 `ent_coef` = 零新代码但**一样无效** / `hold` option / **机制侧 O7 最贴病根**） | [`docs/exploration_randomization_analysis_2026-09-18.md`](docs/exploration_randomization_analysis_2026-09-18.md)、仪器 `scripts/probe_explore_randomization.py`（`--mode analytic/engine/placement`）、留证 `docs/probe_explore_randomization/`、台账 **O10** |
