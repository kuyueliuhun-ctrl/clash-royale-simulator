| **★ 偏置随机（第二问）：抬高「不出牌」/「大费牌」概率（用户追问，**只分析不实现**）**：**「提高不出牌概率」是唯一正确的杠杆且真的有效**——要的是 `k≈17` 帧的**合取** ⇒ 逐帧概率 `1/6→q` 把成功率抬成 `q^k`（**每帧 5 倍 → 覆盖率 12 个数量级**）。解析（只读录像）：`q=0.9` 在「真正从低圣水攒到 6」桶 = **1,264 事件/100k 帧**（等概率 `4.5×10⁻¹⁰`）；实测同 ckpt/同 8 局：`uniform` **0.000%** 帧圣水 ≥6（峰值 5.36）→ `bias_0.9` **18.9%**（峰值 10.00）、最长不花钱 21→75 帧。**✦ 且可零 off-policy 实现**：直接给 **STOP logit 加正偏置 β**（`π_θ` 自己变成「爱不出牌」）⇒ `ratio≡1`、无重要性权重、无 clip 错配（写法 (C)）；剂量-反应 β=0/4/5/6/7 ⇒ ≥6 帧占比 **0.000%→0.178%→12.70%→51.27%→82.98%**；仓内**已有**静态旋钮 `stop_logit_bias=-1.0`（`follower.py:156/253-255`，**仅初始化生效**）⇒ 改成「正值 + 退火」是最小改动。**「提高大费牌概率」方向是反的**：实测 `α=1.5`（权重 ∝ 费^1.5）把 ≥6 帧占比 **18.9%→12.1%（−36%）**、最长不花钱段 75→48；漂移式 `0.1786−(1−q)·E[卡费]` ⇒ 盈亏平衡 `c*=1.786` ⇒ 偏向便宜卡才有正漂移。它只对「已攒到 6 费后把费花在 Xbow 上」有用，而那需要**状态条件**（`elixir<6` 偏不出 / `≥6` 偏目标卡）= 手写规则 = **O7**。**★★ 但覆盖率 ≠ 会学（本文最重要的限定）**：这些攒费轨迹的**每局回报从 −4.46 掉到 −41.5（β=6）/ −54.1（β=7）** ⇒ **采样到了，PPO
会学到「别这么干」**（攒费期挨打 + 省下的费没兑现：292 帧圣水 ≥6 只打 5 次 Xbow）；γ 折扣已排除为瓶颈（`0.99¹⁷=0.843`、`0.99³⁴=0.71`）⇒ 与 S1/O7「约束在机制侧」**独立同向**。含三种写法的梯度权重 `w=(1−p)π/((1−p)π+pq)`（混合写法 `p=1` 时 `w=0`；**on-policy `w≡1` 严格最优**）与「覆盖率×`w`」表、以及⚠️ **我自己的仪器 bug 留档**（`xbow_plays` 累加从未执行、静默恒 0，**修前 JSON 的该字段不可引用**） | [`docs/exploration_bias_pass_2026-09-18.md`](docs/exploration_bias_pass_2026-09-18.md)、仪器同前（新增 `bias_<q>_<α>` 与 `名@β` 臂）、留证 `docs/probe_explore_randomization/bias_{proposal,onpolicy,cards}.*`、台账 **O10** |
