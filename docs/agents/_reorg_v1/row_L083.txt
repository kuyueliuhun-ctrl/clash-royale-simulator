| **★ 「非法动作」是怎么定义的 —— 四层取证（用户提问；**只读取证，不改代码**）**：**四层并列且互不推导**：**L1 掩码层**（`rl/action_mask.py` 全部规则 + `env_wrapper.py:473-536` + `follower.py` `masked_fill(-1e9)`，只产 `bool`、**不产 reason**）→ **L2 整包校验**（`validate_bundle` `action_mask.py:521-573`，**9 条中文 reason**，**任一非法则整包拒收**）→ **L3 惩罚/统计**（`invalid_penalty=0.05`（`env_wrapper.py:50`/`config.py:58`；预设 `defensive` = 0.1）× `invalid_count`，`env_wrapper.py:262-263`；**两个来源**：整包拒 =1、**引擎拒逐卡 +1**）→ **L4 引擎物理**（`battle.py:2702→2996-3090`，**失败一律静默 `return False`、无理由串**）。**★ 10 条冲突并列不调和**：掩码**过严**（C1 MergeMaiden 静态 6 vs 引擎动态 3/6；C6 **Miner 全区豁免** —— `grep Miner rl/action_mask.py` = **0 命中**）、掩码**缺口**（C2 BarbLog 限部队区，掩码对 `type=="spell"` **提前 `return True`**）、**作用域空隙**（C3 8h 不裸下：掩码只管**空 bundle 首卡** vs L2 只管**整包恰好 1 张** ⇒ 2 卡包首卡裸下**两层都不拦**）、**RL 层专有约束**（C4 法术三道闸门 7h/8h/9h 引擎**完全不校验**；C10 坐标越界是 RL 专有）、**统计错位**（C5 `evaluate.bundle_illegal`
**看不见引擎级拒绝**）、**同名两种基**（C9 `used` L1 0-based vs L2 1-based）。**★ 编排者自证新增 F1**：**MergeMaiden 掩码层同时「费用过严 + 位置过松」（方向相反）** —— 掩码查 `Card("MergeMaiden")`.type = **`spell`** ⇒ `_position_legal:394-405` **提前放行**（占位/王塔身后/六条区域/后排几何**全跳过**），而引擎实际部署的 `MergeMaiden_Mounted/_Normal` 是 **`character`** ⇒ 六条区域规则**全部适用**；根因 = `_effective_card`（`:39-43`）**只处理 Mirror**。**★ F2 暴露面切分**：三冲突卡**都不在** `DEFAULT_SOLO_DECK`；BarbLog **在** `FOUR_DECK_SET`（`opponents.py:44`）⇒ **solo 实时暴露**；Miner/MergeMaiden 只在 `build_card_pool()`（139 卡、`run_league.py:795-812` 的「全随机」agent）⇒ **solo 下潜伏**；而 `run` 模式 p1 侧非法动作**不被 L3 计数**（`flow_league.py:323` 硬编码 `invalid_count=0`）⇒ **静默**。**★ 真实日志读数（我自证复跑）**：`P1-20`（validate 通过但引擎拒绝）在 `docs/*.log` 共 **1,644 行，其中 BarbLog 1,644（100.0%）、其它卡 0** —— 与 C2 预测**逐项一致**。⚠️ 限定：1,644 是**日志行数不是独立事件数**（`train_et_measure*.log` 可能含 discarded 重跑）、累计扣分 **1644×0.05=82.2 是跨 5 份日志之和**、**描述性非判据**（【R3】）；**未定
U8–U12**（越界静默夹取 / `use_ability` 全文 / 对账脚本明细 / F1 可达性 / `spell` 是否数据缺陷）均**未补全** | [`docs/illegal_action_layers_2026-09-18.md`](docs/illegal_action_layers_2026-09-18.md)（§1 分层总览 / §2–§5 逐层 / §6 冲突 C1–C10 / §7 已修漏洞 / §8 未定 / **§10 编排者独立复核 + F1/F2 + 日志读数**）、台账 **O9**（P2 已量化） |
