# AGENTS.md 整理草稿 v1（2026-09-19）—— B 区巨型行下沉

> **⚠️ 本文件已被同日 v2 取代** ⇒ 见 [`REPORT_v2.md`](REPORT_v2.md)。v1 的产物处置：
> 草稿本体留存为 `AGENTS.reorg-draft.v1.md`；分册 `docs/agents/index_notes.md` **已删除**
> （其 §1–§10 与 v2 分册 `docs/agents/agents_long_entries.md` 是同一批逐字正文，留两份必然漂移）。
> 本文件里的 B 区取证结论仍有效（v2 完整继承）。

> **状态**：**草稿，未生效**。`AGENTS.md` 本身**一个字未改**（回滚 = 删本目录产物即可）。
> 产物：`AGENTS.reorg-draft.md`（草稿本体）、`docs/agents/index_notes.md`（长条目逐字搬迁件）、
> `docs/agents/_reorg_v1/`（10 行原文抽取 + 6 份取证记录 + 生成脚本）。

## 1. 这一版做了什么（只做一件事）

把 `AGENTS.md` **B 区「决策索引」**里 **10 行巨型行**（占 B 区 **87%**）换成
**一行式摘要 + 分册锚点**，正文**逐字**搬进新分册 `docs/agents/index_notes.md`。

这正是本仓既有的做法（`docs/agents/structure.md`、`docs/agents/reward_surveys.md`
历史上都是这么拆出来的），因此**不引入新范式**，也**不触碰** A 区（红线 19 条）与 C 区（当前活跃工作）。

| 项 | 前 | 后 | 变化 |
|---|---|---|---|
| **AGENTS.md** | **61,545 B** | **46,137 B** | **−15,408 B（−25.0%）** |
| 距注入上限 65,244 B 的余量 | 3,699 B（5.7%） | **19,107 B（29.3%）** | **×5.2** |
| B 区 | 31,807 B | 16,399 B | −15,408 B |
| 10 行巨型行合计 | 26,822 B | 11,070 B | 每行 812–1,470 B |
| 单行最大 | **6,918 B**（L84） | **1,470 B**（§10） | 对 `read` 的 2000 字符截断不再命中 |

分册 `docs/agents/index_notes.md` = **30,584 B**（正文 26,822 B 逐字 + 出处/取证状态头尾）。
**净增 15,176 B**（仓库总字节），换 `AGENTS.md` 的 25% 与「单行可读」。

## 2. 完整性校验（脚本复算，全部通过）

| # | 断言 | 结果 |
|---|---|---|
| ① | 草稿与 `AGENTS.md` 的差异**只**在第 77–86 行（外加头部 1 行草稿标识） | ✅ `diff == [77..86]` |
| ② | 新 10 行均为**两格**表格行（ASCII `\|` 各 3 个，与 B 表头列数一致） | ✅ 曾捕获一次三格错位并修掉 |
| ③ | 10 行原文在 `docs/agents/index_notes.md` 中**逐字命中** | ✅ 10/10 |
| ④ | 生成脚本对搬迁前 10 行做**行号 + 字节数双断言**（`AGENTS.md` 一变动即 ABORT，不产出错位草稿） | ✅ |

复现/重生成（幂等）：`python docs/agents/_reorg_v1/build_draft.py`

## 3. 事实取证（10/10 行）—— 以及顺带查出的 3 处不一致

压缩前按本仓纪律做了一次**存在性取证**：把每行拆成「承重事实」（数字/口径/结论/`file:line`），
逐条到该行自己指向的目标分册里查 `file:line` 证据，判定 PRESENT / PARTIAL / MISSING。

| 行 | 主题 | 承重事实 | 结果 |
|---|---|---|---|
| §1（L77） | 攒费穷举实测 + B 类病理 | 17 | **0 MISSING / 0 PARTIAL** |
| §2（L78） | 掩码 `used` off-by-one 修复 | 14 | **0 / 0** |
| §3（L79） | 探索随机第一问 | 45+ | **0 / 0** |
| §4（L80） | 偏置随机第二问 | 19 | **0 / 0** |
| §5（L81） | 状态条件随机第三问 | 31 | **0 / 0** |
| §6（L82） | 100 局随机评估 + dashboard | 41 | **0 / 0** |
| §7（L83） | 非法动作四层取证 | 24 | ⚠️ **1 PARTIAL** |
| §8（L84） | 全项目结构优化 | 34 | ⚠️ **1 PARTIAL** |
| §9（L85） | 外部调研台账 | 15 | ⚠️ **1 PARTIAL** |
| §10（L86） | 上游引擎增量核查 | 18 | **0 / 0** |

★ **3 处不一致是本次整理的真实收获**（`AGENTS.md` 现行本自身的缺陷；**原文一律未改**，
按「只增不改历史结论」纪律冻结在 `index_notes.md` 的对应 § 备注里）：

1. **§7 / L83 —— 行号口径不一致**：该行写 L4 引擎物理在 `battle.py:2702→2996-3090`，
   而 `docs/illegal_action_layers_2026-09-18.md:41` 写作 `2996-3077`（分册全篇无 3090）；
   且两者都未覆盖 `_deploy_card_impl` 的真实终点（源码下一个 `def` 在 3221）。**成因未定**（【R10】）。
2. **§8 / L84 —— 行内自相矛盾**：该行中段写「余 6 项全部判定『不做』」，但同一行后文又写
   T2-7 / T3-4「完成」，且 `docs/structure_remaining_2026-09-19.md:18-20` 的最终结算是
   **完全完成 15 / 部分完成 3 / 判定不做 3**。⇒ 以分册为准。
3. **§9 / L85 —— 措辞不精确**：该行写「三条长条目逐字搬入」，实测只有 **2/3**：
   第 85–86 行（提交 `2e8e0e9^`）与 `docs/agents/reward_surveys.md:9-10` 逐字节相同（仅 CRLF 差），
   第 3 条（录像格式）是随其源文档 `docs/replay_format_comparison_2026-09-19.md` **新建**的，
   没有「搬入」前身。

## 4. 已知取舍与可调旋钮

- **保守口径**：本版把 10 行**摘要**压到 0.8–1.5 KB/行；**没有**进一步删到「只有指针」。
  若要把每行压到 ≤700 B（预计 `AGENTS.md` ≈41 KB、余量 ≈24 KB），只需改
  `build_draft.py` 里的 `COMPACT_ROWS` 文案后重跑——**其余机械部分不动**。
- **`read` 工具陷阱（本次实测）**：单行 >2000 字符会被 `read` **静默截断**（L84 命中）。
  抽行核对请用 `docs/agents/_reorg_v1/row_L0*.txt`（已按 900 字符硬折行）。
- **未做**：C 区「当前活跃工作」25,387 B（其中 C2 新方案 10.6 KB、C3 economy_et 9.3 KB 占 79%）
  一行未动；C 区多数条目已完结（C0/C1/C3/C4/C5），若要同样下沉需**另一轮**取证。
- **未采纳前 `AGENTS.md` 保持原样**；草稿不会被注入机制读取（注入只认 `AGENTS.md`）。
