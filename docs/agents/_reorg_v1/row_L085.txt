| **★ 外部调研类台账（IL 奖惩参考 / 跨项目 RL 奖惩设计 / 人类回放可行性 / 录像格式对照）**（2026-09-19） | **拆到分册** → [`docs/agents/reward_surveys.md`](docs/agents/reward_surveys.md)（三条长条目**逐字搬入**，本文件只留本行指针；起因 = 本文件再次逼近上限 **64,190 B / 65,244 B**）。四条一句话结论：① **IL 阶段结构性没有奖惩**（FL 的 IL 六项损失全掩码 CE/Huber），**FL 的 IL→PPO 锚定项默认关闭**，真正有 KL 锚定的一手先例是 **VPT**；② **「奖励塑形退火」没有一手实例**（Five 的 `anneal` 是超参/环境特性，VPT 退的是 KL 系数 ρ）；③ **人类回放可行性**：卡键 **100% 可映射**、卡组 **8/8**、手牌循环费用 **6/6 精确相等**、**坐标无需翻转**（A/B 否证了我先写的假设），但**直接重放只走通 35–62%**（主缺口 = 逐事件圣水不可观测）⇒ 可用于 IL 但**必须先状态重建**（FL 的做法 = 编造相容牌序 → 与真引擎发牌校准 → 调度动作进引擎重演）；④ **两个录像格式互补**：人类回放 = 动作 + 卡组元数据（无状态），我方 schema 5 = 逐帧状态 + 奖励/价值（无卡组元数据、无 seed、`logprob`/`masks` 在 `run_league.py:190` 被丢弃）；★ 附带：**卡覆盖 177/178，唯一缺 `void`**（影响 ≈3% 卡组）。 |
