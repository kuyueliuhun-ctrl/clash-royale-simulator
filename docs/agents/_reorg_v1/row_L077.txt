| **★★ 穷举实测：「模型靠自己学不会攒费」这个判断成立，且更狠**（用户命题）：**「放弃一张买得起的牌」在 75,891 帧里发生 0 次**——7,238 段「主动不出牌」**0 段**是在圣水 ≥4.0 时仍不出（卡组结构保证手牌最便宜那张 ≤4 ⇒ y≥4 必有可出）；出牌帧「圣水−最便宜卡费」中位 **+0.137**（<1 帧回费），A 段最长 **22 帧 < 34 帧**（攒到 Xbow 所需）⇒ **不是"学得慢"，是这条轨迹从未被采样**（0 样本 ⇒ 0 梯度），且 **step 0 与 100k 同结构**。★★ 附带查出**新病理 B 类**（模型提交非法卡包被整包拒绝、白掉一帧，A_et **497 帧 / 最长连续 225 帧 ≈112 s**、每帧罚 0.05、跨 9 个评估点仍在、**非法性 100% 可自证**）——它把既有读数污染了：层 2「圣水≥6 = 0.52%」**96% 是它**，5 次 Xbow **全在 B 解锁帧** | 审计 [`docs/elixir_saving_audit_2026-09-18.md`](docs/elixir_saving_audit_2026-09-18.md)、仪器 `scripts/pass_streak_audit.py`、台账 **C14 / O9**（O3 已补记） |
