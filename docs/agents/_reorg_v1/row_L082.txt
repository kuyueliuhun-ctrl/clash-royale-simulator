| **★ 「初始模型 + 全过程随机」100 局评估 + dashboard 观测（用户直接要求的简单测试）**：**p0 = 全过程均匀随机**（每个 decoder 步在掩码给的合法项里均匀抽、落点在合法格里均匀抽）、**p1 = 初始模型 `solo_main_0.pt` 的确定性副本**（argmax，与生产 `--only-vs-main` 同约定），**100 局 / 31,491 帧 / 1,367.9 s**，录像走生产同一写入路径（**schema 5**）。**读数**：58W/42L/0D = **58.0%±4.9**、局均 **314.9 帧**、空 bundle **90.4%**、每帧出牌 0.097。★ **从产出的录像独立复算**（既有只读仪器）：**A 主动不出牌 90.4% / B 整包被拒 0.0%（掩码修复的更大样本复核，31,491 帧）/ C 出牌 9.6%**；**A ∩ 圣水≥6 = 0 帧**；**圣水峰值 5.357 = 开局 5.0 + 2 帧回费**（只出现在每局第 3 帧）⇒ **100 局里圣水从未超过开局水平**；A 段最长 24 帧 < 34；**Xbow 0 次**（3,062 次部署）⇒ **全过程随机同样到不了 6 费**（与前几轮解析/probe 逐项吻合）。**位置**：随机臂用到 **484/576 格、熵 8.47 bits ≈356 有效格**（对比训练策略 84/90 格、4.5 bits ≈20 格）。**dashboard**：**http://127.0.0.1:8701**（专用；8700 上另有指向 `et_ctrl100k` 的常驻面板未动）——solo 卡片出胜率曲线与逐点表、「对局回放」列出 `league_31491.pkl`（100 局）可逐帧播放、「卡牌使用统计」出双侧出牌分布。⚠️ 纪律：p0 是**随机 actor 不是策略**（无 logprob ⇒ 该录像**不能用于训练/BC**）；对手与 p0 **同源** ⇒ 胜率**不含绝对强度**；`step`
= 累计决策帧（【R9】）；面板行为指标列**留空显示 `–`**（不伪造）；中间 9 个分块录像已删（否则按 `league_*.pkl` 通配会**重复计数**） | [`docs/rand100_eval_2026-09-18.md`](docs/rand100_eval_2026-09-18.md)、runner `scripts/random_eval_100.py`、独立复算 `docs/rand100_eval_2026-09-18/`、run 目录 `src/clasher_new/runs/rand100_eval/`（gitignored）、台账 **O10** |
