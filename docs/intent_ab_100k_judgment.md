# intent-save A/B（各 100k）—— **判读文档**（读数已填；判据与分支**跑前**锁定）

> 两臂已完成：**A `EXIT_A=0` / B `EXIT_B=0` / `ALLDONE 2026-09-20 04:00:02`**（B 训练循环 7223.8s）。
> 判据、失败分支、允许写法在任何读数之前就已写定（【R3】），依据 = [`intent_save_prereg_2026-09-19.md`](intent_save_prereg_2026-09-19.md) §4/§8.5/§8.6 + [`intent_ab_100k_2026-09-19.md`](intent_ab_100k_2026-09-19.md)（启动记录）。
> **本文件未在读数出来之后修改任何判据或分支定义**；新增内容一律以「★ 事后补记（不改判据）」标注。

## 0. 结论

**J1 = FAIL（0 条）。**

但**必须连着说清它是什么失败**：**不是「轨迹从未被采样」**（那是改动前的状态：臂 A `A ∩ 圣水≥6` = **0 帧**、
「放弃一张买得起的牌」75,891 帧 0 次）。本次实测：机制被**大量使用** —— 20 局里 `set=2,674`、
`held=2,347 帧`（约占 **37%** 的帧）、`ready=17` 次；**失败点在"长保持从未完成"**：两次独立运行里
`fired_held` 的最大值分别只有 **4** 与 **6** 帧，**远离 34**；`ready=17 → fired=2`（**15/17 次攒够之后没有把牌打出去**）。

按 §6 的三种允许写法 ⇒ 本条属于 **「观察到」**：观察到「机制被显著使用，但未产生 ≥34 帧保持**并**兑现」。

★ **同时必须报告的负面观测（不是 J1 的一部分，但同等重要）**：**B 臂的绝对强度显著低于 A 臂**——
对**同一个固定随机锚点**，A `0.500±0.112` vs B **`0.100±0.067`**（Δ≈0.40 ≈ **5σ**，远超已标定噪声地板 1σ≈0.078），
且 B 在 88k/96k/100k **三次触发本仓的 `<0.35` 绝对强度警报**；同时 B `vs baseline_prev = 1.000 (20W/0L)`
而 A 为 `0.600`——**这正是该警报点名的「自对弈可能在循环里打转」指纹**（赢过自己的过去、打不过独立对手）。
⇒ **即便机制层有正面迹象，本次处理在"会打游戏"这一维度上是退化的**（n=1/臂，但同锚点配对、Δ≈5σ）。

## 1. 协议与唯一变量（已核实）

| 项 | 值 |
|---|---|
| A 臂 | `intent_off_100k`：`--config economy`，**无** `--intent-save`（旧架构 6 option / `scalar_dim=3`） |
| B 臂 | `intent_on_100k`：同上 **+ `--intent-save`**（11 option / `scalar_dim=9`） |
| **唯一变量** | `--intent-save`（【R3】） |
| 两臂共同 | `--fresh`、100k 步、`steps-per-eval 8000`、`big-eval-every 100000`、`n-eval-games 20/40`、`eval-workers 8`、`seed 0`、`device cuda`、`value-norm running`、`adv-norm scale`、`hist-seed-dir ×2`、`ppo-epochs 4 / minibatch 32 / shuffle`；各 **14 个评估点回放**（0…100000） |
| 偏离（已留证） | `eval-workers 8`（非文档的 12）：【R1】开跑前实测可用提交 **10.51 GB < 12 GB 门** ⇒ 按 `check_commit.py` 自身建议降档；**两臂同档** ⇒ 单变量不受影响 |
| 跑中修正（**只影响 B**） | `_intent_age` 同目标重述不再归零（启动记录 §9.1）：A 臂走不到该代码路径 ⇒ A 逐位不变 |
| **产物级单变量证明** | A `solo_main_0.pt`：`intent_options=False` / `slot_head=(6,128)`；B：`intent_options=True` / `slot_head=(11,128)` / `sub_emb=(128,13)` / `enc_fc=(128,2737)`（= 2560+40+**9**+64+64 ⇒ 观测标量 3→9）。**入参层与产物层两处成立**（启动记录 §11） |

## 2. J1（**主判据**，二值计数，不受 n=1 胜率噪声影响）

**判据（跑前写死）**：`J1 = count{ 逐帧事件 : 保持帧数 ≥ 34 且 目标卡费用 ≥ 6 }`，**≥ 1 即 PASS**。

| 项 | 值 |
|---|---|
| **B 臂判定** | **J1 = 0 ⇒ FAIL**（仪器 `scripts/audit_intent_save.py --ckpt runs/intent_on_100k/solo_main.pt --games 20 --max-frames 400`） |
| 两次独立运行（同 ckpt / 同 N；采样 RNG 未播种 ⇒ 跨跑有差异，【R9】） | ① `set=2615 held=2118 ready=18 fired=2 cancelled=298 dropped=272`，`fired_held=[2,4]`；② `set=2674 held=2347 ready=17 fired=2 cancelled=261 dropped=274`，`fired_held=[6,0]` ⇒ **两次都 J1=0；最大保持 4 / 6 帧** |
| `fired_held` 直方图 | `0-5: 2` ｜ `6-11: 0` ｜ `12-23: 0` ｜ `24-33: 0` ｜ **`≥34: 0`** |
| 达标事件明细 `(card, cost, held)` | **空**（无任何 ≥34 帧且 ≥6 费的兑现） |
| **基线列①**（臂 A，**脚本复算，禁手抄**）★ 已出（14 点 / **56,677 帧**） | `A ∩ 圣水≥6` = **0 帧**｜A 主动不出牌 91.2% / 整包被拒 **0.0%（0 帧）** / 实际出牌 8.8%｜**A 段最长 33 帧**（段内圣水峰值 **5.99**）｜段峰值分档 `≥3:460 / ≥4:15 / ≥5:14 / **≥6:0** / ≥8:0`｜仪器自检 **0** |
| **基线列②**（历史基线，**与①并列，不得互相顶替**） | `et_solo100k` 已训练池：**0 条**、主动不出牌最长段 **22 帧**（预注册 §4/§7） |
| 阈值 34 的来源 | **解析换算**（6 费 ÷ 0.3571 圣水/s ÷ 0.5 s/帧 ≈ 33.6），并在本 run 上**独立复现三次**：① step-0 录像；② **臂 A 全量 56,677 帧**（回费中位 **0.1786 圣水/帧 = 0.3571/s**）；③ A 段最长 33 帧与峰值 5.99 互相印证（【R4】【R16】） |
| **判定** | **J1 FAIL** |

### 2.1 ★ 事后补记（不改判据）：这个 FAIL 的**形状**比 FAIL 本身更重要

1. **机制确实被用起来了**：`held` ≈ **2,100–2,350 帧**（20 局 ≈ 6,300 帧 ⇒ **~37% 的帧在"为某张牌攒费"**）。
   对照臂 A 的同类量是 **0**（`A ∩ 圣水≥6` = 0 帧）⇒ **行为分布被显著改变**。
2. **失败点是"长保持从未完成"**：`fired_held` ∈ {0,2,4,6}，而门槛是 34 ⇒ 差距仍在，但机制**不再**是"0 样本"。
3. **★ 未预期的一环：`ready=17 → fired=2`**（15/17 次"攒够了"却没把牌打出去）。
   机制上可解释：**`ready` 一出现 `pending` 就被清、花费立即恢复合法** ⇒ 目标卡在"攒够那一帧"**不再被特权化**，
   策略必须在那一帧凭记忆主动选它 ⇒ **机制恰好在"转化"那一刻松手**。这是我实现里的一处**设计缺口**（不是判据问题）。

## 3. J2（不拖累；**只判大效应**，【R5】n=1/臂）

| 指标（`gates.json` @ step 100000） | A（`intent_off`） | B（`intent_on`） |
|---|---|---|
| `ok` | **False** | **True** |
| 自身 step-0 基线 | eng **7.9** / ghost **17.6** | eng **3.8** / ghost **18.7** |
| `engagement_rate` | **0.0**（门 `≥ 3.95`）✗ | **42.7**（门 `≥ 1.9`）✓ |
| `ghost_rate` | **0.5**（门 `≤ 35.2`）✓ | **6.7**（门 `≤ 37.4`）✓ |

★ **J2 的实际机制（读数之前已核实并写死）**：门禁是**相对该 run 自己第一个评估点**判的
（`baseline` 由 step-0 自标定，`basis` 形如 `50%衰减(3.8)` / `200%放宽`），**不是**与历史地板比。
⇒ J2 只能回答「B 相对**自身**基线有没有退化」，**不能**回答「B 比 A 强/弱」。
本处实测只能**描述**为：**A 自身的 engagement 门在终点被自己判 FAIL（0.0），B 未 FAIL（42.7）**；
**不得**据此写成"B 的防守更好"（跨臂不可比、n=1；且 §4 给出反向结论）。
⚠️ 本仓已记录该门禁**兑现率饱和**（`et_solo100k` 两臂 98.5% vs 98.2%）⇒ `ok` **不得**当疗效判据。

## 4. ★ 绝对强度（**跨臂同锚点配对**，本 run 最重要的负面读数）

| 对照（终点 20 局） | A（`intent_off`） | B（`intent_on`） | 备注 |
|---|---|---|---|
| **vs 固定随机锚点 `baseline_rand`** | **0.500±0.112**（10W/10L） | **0.100±0.067**（2W/18L） | Δ≈**0.40 ≈ 5σ**（地板 1σ≈0.078）；**同一锚点** ⇒ 跨臂可比 |
| vs `baseline_prev`（上一评估点） | 0.600±0.110（12W/8L） | **1.000±0.000**（20W/0L） | B 赢过"自己的过去"，却打不过独立对手 |
| vs `baseline0`（训练起点） | 0.150±0.080 | 0.150±0.080 | 两臂相同 |
| 绝对强度警报（`<0.35`） | **未触发** | **@88000 / @96000 / @100000 三次触发**（0.100 / 0.000 / 0.100） | 警报原文点名「自对弈可能在循环里打转，v3 §3.8.4 取证」 |

⇒ **描述性结论（不构成疗效判据，但与 §3 反向且更强）**：本次处理**没有**让智能体更会打游戏，
**反而在绝对强度上明显退化**，并呈现该警报点名的"循环打转"指纹。
n=1/臂 ⇒ 不能宣称"intent-save 一定会退化"，但**本 run 方向明确、幅度 5σ**。

## 5. 失败分支判定表（**前件/结论跑前写死**）

| 分支 | 前件 | 处置（跑前写死） | 实测前件 | 触发？ |
|---|---|---|---|---|
| **F1** | `set > 0 但 fired = 0` | 打断过强 ⇒ 下一刀 = **M2 时长 option**，**不是**调奖励 | `set=2674`、**`fired=2`（≠0）** | **未触发**（前件不成立） |
| **F2** | `fired > 0 但 fired 的目标卡从未造成塔伤/解场` | 奖励侧 ⇒ 转 **O7** | `fired=2`；已结算观察窗 1 条：**MiniPekka(4 费) held=0 ⇒ `dmg_tower=206.0`、`kills_units=3`** | **未触发**（兑现**确有**塔伤/解场） |
| **F3** | `set = 0` | 初始概率被压死 ⇒ 调初始 logit | `set=2674`（≠0） | **未触发** |

★ **F2 的仪器缺口已按 §5 要求补齐**（读数之前就写明"若 F2 前件成立须先补"）：`scripts/audit_intent_save.py`
新增兑现后 **`F2_WATCH_FRAMES=40` 帧**观察窗，逐条记录 `dmg_tower`（对手三塔总血下降）与
`kills_units`（兑现时在场的敌方部队/建筑死亡数），落 JSON 的 `fired_events`。
⚠️ 已结算样本仅 **1 条**（另 1 条观察窗在局末未结算）⇒ F2"未触发"是**弱证据**，如实标注。
★ **副作用（正面）**：它**削弱**了"奖励侧是瓶颈"的解释——这 1 条兑现的 4 费 MiniPekka 打出了 **206 塔伤 + 3 杀**。

### 5.1 ★ 事后补记（不改分支定义）：**三条预注册分支一个都没覆盖实际情形**

实际出现的是**第四种**：**「机制被大量使用、但长保持从未完成」**——
`set`/`held` 都很高（不是 F3）、`fired>0`（不是 F1）、兑现有效（不是 F2）。
⇒ 这是我**预注册的一处设计缺口**（分支表按"要么没 fired、要么 fired 但没用"写的，
没考虑"fired 有、但都在 0~6 帧内完成"）。**如实登记**，不**事后**改分支定义。
★ 与该情形对应的下一刀（**候选，须另行预注册**，不是本 run 的判据）：
① **M2 时长 option**（`SAVE(slot, d)`：把"等多久"变成**一次**抽取，直接消掉 34 帧的合取）；
② **修 §2.1-3 的设计缺口**：`ready` 那一帧**继续特权化目标卡**（例如再压制其它花费 1 帧，或把 pending 保留到真的打出为止）；
③ 只在 ①② 之后才谈奖励侧（F2 弱证据**反对**"奖励侧是瓶颈"）。

## 6. 允许的三种写法（照 `et_solo100k` 判读纪律）

1. **观察到** ← **本 run 采用**：给出具体计数/读数 + 可得性边界（见 §2.1/§4/§7）；
2. **不可分辨**：用于 J2 门禁类差异（§3）——**不得**写成"确认有效/无效"；
3. **门禁饱和无分辨力**：J2 的 `ok` 属此列（有前科），**不得**当疗效判据。

**禁止**：写"修好了 / 确认有效 / 确认无效"（n=1/臂）；跨 run 直接比数字（【R5】，唯一例外是**同锚点配对**的 §4）；把描述性读数当判据（【R3】）。

## 7. 限制与未定（【R10】）

1. **n = 1/臂** ⇒ 唯一有判别力的是 J1（已 FAIL）；§3/§4 均为**描述**。§4 的 5σ 是**同锚点配对**，比一般跨 run 比数字强，但仍不能外推成"intent-save 必然退化"。
2. **审计仪器采样 RNG 未播种** ⇒ 两次运行的同类字段有差异（`held` 2118/2347、`fired_held` [2,4]/[6,0]）；但 **J1=0 与"最大保持 ≤6 帧"在两次运行中一致** ⇒ 结论稳健，**单次具体数字不得当固定值引用**。
3. **J3（描述性）**：A 臂末段熵坍缩到 `~0.002~0.10`（伴 `GRU 活力不达标`），B 臂到 32k 仍 `1.61–1.87`（启动记录 §10/§12）。
   ⚠️ **raw entropy 跨动作空间不可比**（11 vs 6 option）；`EVin` 两臂都差且是本仓已记录的 critic 问题 ⇒ **都不是疗效信号**。
4. **未定**：「循环打转」是**算法/自对弈结构**问题还是 intent 机制直接导致 ⇒ 本 run **分不开**（要分开需第二条对照，例如"同参数量但不放行 intent option"的臂）。
5. **未定**：F2 只有 1 条结算样本 ⇒ 「兑现有效」的强度未知。

## 8. 台账与索引

- [x] `ledger.md` §6 **O7** 补本 run 直接证据 + intent-save 条目
- [x] `plans_runs_docs.md` §8 新增本 run 行
- [x] `AGENTS.md` B 区 intent-save 行改为"已跑完 + 判读（J1 FAIL + 绝对强度退化）"
- [x] `docs/README.md` 登记本判读文档 + 读数
- [x] `python scripts/_agents_split.py --check` 与 `_structure_check.py` **PASS**

## 9. ★★ 勘误（2026-09-20 追加；**不改上面任何原文**，只登记更正与重判）

> 触发 = 用户的复核问题「你所谓的攒费帧，是不是依旧在为一张最便宜的卡等待？」
> 追查该问题时查出：**本判读的 J1 读数跑在了一个策略从未训练过的卡组上** ⇒ §2 / §2.1 / §5 / §5.1
> 的读数与由它推出的结论**作废重判**。完整证据、修法与新的目标归因 ⇒
> [`intent_target_attribution_2026-09-20.md`](intent_target_attribution_2026-09-20.md)。

### 9.1 事实：判读仪器的卡组 ≠ 训练卡组（三层独立证据）

| 层 | 证据 |
|---|---|
| **源码** | `scripts/audit_intent_save.py` 的 `_run()` 原先构造 `RLEnv(opponent=None, seed=..., intent_save=True)`——**不传 `deck0`/`deck1`** ⇒ 按 `rl/env_wrapper.py` 的缺省落到 `DEFAULT_DECK` / `DEFAULT_DECK_1` |
| **常量** | 训练用 `rl/train_solo.DEFAULT_SOLO_DECK`（`resolve_deck_set("default")`）＝ Xbow 2.9：`Xbow:6, Tesla:4, Skeletons:1, IceWizard:3, Archer:3, Knight:3, Log:2, Fireball:4`；审计用的 `DEFAULT_DECK_1` ＝ `Minions:3, Archer:3, MiniPekka:4, Musketeer:4, Giant:5, Fireball:4, Arrows:3, Knight:3` ⇒ **两套卡组逐张不同，且后者最高只有 5 费、没有 `cost ≥ 6` 的卡** |
| **产物** | 本 run 的 14 份录像（`runs/intent_on_100k/replays/`）里出现的卡只有 Xbow/Tesla/Skeletons/IceWizard/Archer/Knight/Log/Fireball，原版 8 卡里的 Giant/MiniPekka/Musketeer/Minions **出现 0 次**；而本判读的 `fired_events` 报出的却是 **`Giant`(5 费) 与 `MiniPekka`(4 费)** ⇒ 两层卡组确实不同 |

⇒ 两个后果，**都不是"读数偏低"，而是判据本身失效**：

- **A. 空判据**：J1 的「同一张 **≥6 费**卡」过滤在 `DEFAULT_DECK_1` 上**按构造恒为 0** ⇒ 无论机制多好，
  §2 都只会打印 FAIL；连"34 帧阈值"所对应的那张牌（Xbow）都**不在场上**。
- **B. OOD**：被测策略（B 臂）是在 Xbow 卡组上训练的，`DEFAULT_DECK_1` 它**从未见过** ⇒
  `set/held/ready/fired` 描述的不是训练分布下的行为。

### 9.2 重测（仪器修好卡组后：同一 ckpt、同一协议、`--deck solo`、两 seed）

`./.venv/Scripts/python.exe scripts/audit_intent_save.py --ckpt src/clasher_new/runs/intent_on_100k/solo_main.pt --deck solo --games 20 --max-frames 400 [--seed 1]`
（读数留证：`docs/audit_intent_save_B_solodeck.txt` / `.json`、`docs/audit_intent_save_B_solodeck_seed1.txt` / `.json`）

| 项 | seed 0 | seed 1 | （已作废的旧读数，原版卡组） |
|---|---|---|---|
| 帧数 | 6,842 | 6,825 | ≈6,300 |
| `set` | 2,853 | 2,902 | 2,615 / 2,674 |
| `held` | **2,932（42.9% 的帧）** | **2,878（42.2%）** | 2,118 / 2,347（≈37%） |
| `ready` | 11 | 6 | 18 / 17 |
| **`fired`** | **0** | **0** | **2** |
| `cancelled` / `dropped` | 444 / 280 | 448 / 272 | 298/272、261/274 |
| `fired_held` | 空 | 空 | `[2,4]`、`[6,0]` |
| **J1** | **0 ⇒ FAIL** | **0 ⇒ FAIL** | 0（空判据，无效） |
| 卡组对账 | `matches_training=True`，`has_ge_6=['Xbow']` | 同 | —（跑的是 `DEFAULT_DECK_1`） |

⇒ **J1 = FAIL 的结论不变，但这次是"有效的 FAIL"**（判据在评估卡组上可满足：存在 Xbow=6 费）。
**变化的不是结论而是形状**：旧读数说"机制被大量使用、`ready=17→fired=2`（部分转化）"，
正确读数说 **`fired = 0`（零转化）**——20 局 × 2 seed 里，**被攒的那张牌一次都没有被真的打出去**。

### 9.3 被推翻 / 撤回的具体条目

1. **§5.1「三条预注册分支一个都没覆盖实际情形 ⇒ 预注册有设计缺口」——撤回。**
   在正确卡组上 `set > 0` 而 **`fired = 0`** ⇒ 预注册 **F1 的前件成立、F1 触发**，其处置在跑前就写死了
   （**「打断过强 ⇒ 下一刀 = M2 时长 option，不是调奖励」**）⇒ **分支表没有缺口**；
   之前的"缺口"是**空判据卡组**上的 `fired=2` 造出来的假象。§5 表里 F1 的"未触发"与 §5.1 整节按此更正。
2. **§2.1-1 的「held ≈ 37% 的帧」数字作废**（它在错卡组上）；正确卡组上的同类量 = **42.2%–42.9%**（§9.2）。
3. **F2「兑现确有塔伤/解场 ⇒ 削弱『奖励侧是瓶颈』」——撤回该证据。**
   那 1 条兑现是错卡组上的 `MiniPekka`(4 费) ⇒ 与本 run 的目标卡族无关。
   ⇒ 「奖励侧是不是瓶颈」**回到未定**（并非被推翻为"是"，而是**证据被撤**）。
   已作废的 `fired_held ∈ {2,4,6}` 同理。
4. **§2 的"基线列"与阈值复现不受影响**（它们是**臂 A** 的 56,677 帧复算，臂 A 不开 intent、不涉及审计卡组）。
5. **§3（J2）、§4（绝对强度）不受影响**：二者读的是训练侧 `gates.json` 与评估回放，走的是**训练卡组**。
   故本 run 最重要的负面读数（同锚点 A 0.500 vs B 0.100，Δ≈5σ + 三次 `<0.35`）**依然成立**。

### 9.4 仍然成立的两条机制性观察（在正确卡组上更强）

- **`ready > 0` 但 `fired = 0`**：17/17（11+6）次"攒够"**全部没有转化**（旧读数说 15/17 没转化）；
  归因仪器把这条读得更细：457 段"攒够"里 **448 段的 `ready` 只维持了 1 帧**就结束了。
  ⇒ §2.1-3 指出的**代码级设计缺口**（`ready` 一出现 `pending` 即被清、花费立刻恢复合法 ⇒
  目标卡在"攒够那一帧"不再被特权化）**仍然是我实现里的真实缺口**，且在新读数下**更严重**。
- **长保持从未完成**：两次运行 `fired_held` 直方图 `≥34: 0`；目标归因显示**目标主要是 Xbow**
  （**不是**"最便宜的卡"：正确卡组上 `held` 帧的 **61.1%** 落在最贵的 Xbow(6 费)、最便宜的 Skeletons(1 费) **0.2%**，见新文档 §3）⇒ 失败点是**保持时长/承诺不稳**，不是"攒错了牌"。

### 9.5 同步更正

- 仪器：`scripts/audit_intent_save.py` 新增 `--deck {solo,vanilla}`（缺省 **solo = 训练卡组**）、
  打印卡组与"≥6 费卡"清单；卡组**不可满足 J1** 时**拒绝给判决并退 2**（`--allow-inapplicable` 才放行）。
- 回归测试：`rl/selftests/part5.py::test_intent_audit_deck_invariant`（① 仪器缺省卡组 == 训练卡组；
  ② 判据阈值在评估卡组上**可达**；③ 负对照 `vanilla` 不满足 ② ⇒ 有判别力；④ 端到端：门禁退 2）。
- 归因仪器：`scripts/probe_intent_target.py`（新增）。
- 台账：`docs/agents/ledger.md` **O11 / O7** 追加本节；`AGENTS.md` B 区该行与 `docs/README.md` 同步。
