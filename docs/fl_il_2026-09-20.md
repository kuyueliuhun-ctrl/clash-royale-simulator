# 用 FirstLight 的 IL 数据集做模仿学习（IL）——**判读**

> 日期：2026-09-20 ｜ 预注册：[`fl_il_prereg_2026-09-20.md`](fl_il_prereg_2026-09-20.md)（判据在实现前落盘，§8 为
> 实现期补录，**J1–J4 阈值一字未改**）
> 数据：[`VanguardX101/IL_Replay`](https://huggingface.co/datasets/VanguardX101/IL_Replay)（FirstLight_CR 声明的 IL 训练集）
> 证据目录：`docs/fl_il_2026-09-20/` ｜ 数据与 ckpt：`/mnt/e/fl_il_data/`、`runs/_fl_il_bc/`（`runs/` 被 .gitignore）

---

## 0. 一句话结论

**管线成立、IL 确实学到东西，但学到的是「落点」而不是「出哪张牌」。**

我们第一次把**外部人类回放**（FL 的 IL_Replay 分片 0）灌进我们自己的引擎、产出 **47,715 条**
`(obs, masks, bundle)` 训练对、训了 3 个 epoch 的行为克隆，并在**按局切分**的 8,877 条留出上读数：

| 读数（留出 8,877） | 未训练（随机初始化，`torch.manual_seed(0)`） | **IL 训后** | trivial 基线 |
|---|---|---|---|
| **NLL**（`evaluate` 口径） | 8.649 | **5.596**（−36%） | — |
| **落点精确率**（给定出牌选择正确） | 0.07% | **12.10%**（14×） | 1/576 = 0.17% |
| 落点精确率（不分选择对错） | 0.62% | **9.98%**（16×） | 0.17% |
| **top-1 option**（出哪张牌） | 32.91% | **40.42%** | **常数「恒选槽 4」= 39.84%** |
| 宏平均 recall（对常数预测免疫） | 34.62% | 35.76% | 常数预测器 = 25% |

⇒ **预注册 J3 闸门（top-1 ≥ 40%）= PASS（40.42%）**，但**这个 PASS 没有分辨力**：
本留出集上「恒选槽 4」就能拿 **39.84%**（见 §5.3）。有分辨力的读数是
**NLL 与落点**（都大幅改善）与**出牌选择**（只比 trivial 高 0.58 pp、宏 recall +1.3 pp）。
**因此本轮实质性结论 = 预注册 F5「弱信号」**：落点侧确认学到，出牌选择侧**未获证实**。

---

## 1. 做了什么（三件新仪器 + 两次作业）

| 步骤 | 工具 | 口径 | 产物 |
|---|---|---|---|
| ① parquet → JSONL + 普查 | `scripts/_fl_il_extract.py`（WSL `/usr/bin/python3` + pyarrow） | 分片 0：5000 局 / 384,275 事件 | `/mnt/e/fl_il_data/replays_part000000.jsonl`(21.8 MB)、`census_part000000.json` |
| ② 回放 → IL 训练对 | `scripts/fl_il_to_bc.py --mode samples` | 2200 局、10 进程、`card_level=11`、0.5 s 决策帧 | `runs/_fl_il_bc/{train,holdout}/bc_*.pkl`（2.0 GB / 47,715 条） |
| ③ BC 训练 | `rl/human_play.py --bc-train`（**既有通路，未改一行**） | 38,838 训练样本、3 epoch、loss = `-logprob` | `runs/_fl_il_bc/bc_fl.pt` |
| ④ 留出判读 | **新** `scripts/il_eval_holdout.py` | 8,877 留出、`hidden=None`、三基线 + 宏 recall | `holdout_bc_fl.json`、`holdout_random_init.json` |

**机器成本**：转换 2200 局 **1084 s**（0.49 s/局，10 进程）；BC 3 epoch ≈ 21 min（CPU、batch=1）；
留出判读 40 s。数据源**只下了 1 个分片**（17.4 MB，与官方声明字节数逐字节相同）。

---

## 2. J1 · 管线可行性（两条判据全 PASS）

### 2.1 过滤漏斗（5000 局 → 3528 局）

| 拒绝原因 | 局数 |
|---|---|
| 局时长 > 我们引擎的 300 s 硬上限 | 1008 |
| 卡组含无法解析的卡键（`void` 等，普查里 `void` 出现 501 次） | 409 |
| `game_mode` 不在白名单（`1v1 Showdown` / `Normal Battle`） | 49 |
| 我方出牌 < 4 次 | 6 |
| **保留** | **3528（70.6%）** |

### 2.2 J1.1 卡牌经济对账（**外部真值**）＝ **PASS：99.72%**

逐局逐侧核 `Σ_{play_card} 实际费用` vs 回放自带的 `aggregate_stats[side].total[1]`：

- 双侧同时一致：**3518 / 3528 = 99.72%**（闸门 ≥90%）
- 我方侧：**3528 / 3528 全对**，总花费 **359,456 == 359,456**（逐圣水相等）
- 张数对账：双侧 **3528 / 3528**
- 残差 **10 / 3528 = 0.28%**：**全部含 `Mirror`**（10/10），方向混合，成因**未定**【R10】

★ **本仪器第一版口径错，修正后 99.32% → 99.72%**：`Mirror` 的实际费用 = **上一张牌费用 + 1**
（`rl/action_mask.py:30-36`），且 `Mirror` **不覆盖** `last_card`（`player.py:47`）。
第一版用**卡面费** `Card('Mirror').elixir`（=1）⇒ 当时 **24 个残差侧全部含 Mirror、而 0/7044 无 Mirror 的侧有残差**
（2×2 列联表由脚本复算）——**责任在仪器，引擎实现是对的**（这一点最初写成了「引擎缺口」，此处更正）。

### 2.3 另外两条外部对账（零成本、全对）

- `payload.events` 的条数 == `actions/part-*.parquet` 的逐局行数：**4997 / 4997**
- `payload.events[].coordinates.native_world_units` == actions 表的 `native_x/native_y`：**6530 / 6530**

### 2.4 J1.2 标签量 ＝ **PASS：47,715 ≥ 20,000**

2200 局 → **737,207 帧**、50,244 个「我方恰好 1 手」的帧 → **47,715 条标签**（0 错误）。
另有 244 个「同帧 ≥2 手」的帧按预注册丢弃（仍打进战局）。

---

## 3. J2 · 重建保真度（结构性读数）

| 读数 | 值 | 说明 |
|---|---|---|
| 引擎拒绝率 `bundle_not_ok` | **0 / 50,244 = 0.0%** | **J2.1 PASS**（闸门 ≤5%）：强制手牌+圣水后，我们引擎**没有**拒绝任何人类落点 |
| 无坐标事件 | **0** | 见 §6.1 的仪器修正 |
| 手牌强制率 | 4,437 / 50,244 = **8.83%** | 人类手牌不可观测 ⇒ 把要出的牌换进手牌位（§8.5） |
| 圣水强制率 | 1,093 / 50,244 = **2.18%** | 我们的圣水模型 ≥ 费用时无需抬高 |
| **掩码拒绝率** | 2,529 / 50,244 = **5.03%** | 人类动作**引擎合法、掩码非法**（法术空砸/砸塔 EV/后排几何/不裸下闸门）⇒ 丢弃该标签 |

**★ 提前终局（本轮最大的保真度限制）**：**2200 / 2200 局**都是被我们引擎的 game over 终止的
（不是帧上限），`end_time / 真实时长` 的中位数 = **0.822**（min 0.185 / max 5.709）
⇒ 重建局**普遍早于真实局结束**，本批标签**时间分布偏早期**：留出标签时刻中位 **104.0 s**
（p90 168.5 s、max 283 s），而真实局时长中位 ~199 s。

---

## 4. 数据画像（47,715 条标签 / 留出 8,877 条）

- **覆盖 114 张不同卡牌**（我们的训练卡组只有 8 张）——卡池覆盖是本数据集最大的信息增量。
- 最高频：`Skeletons` 561、`BarbLog` 360、`ElectroSpirit` 325、`Log` 293、`Princess` 272、
  `IceSpirits` 272、`Berserker` 271、`Cannon` 247
- 手牌槽位分布：**{槽1: 1347, 槽2: 1809, 槽3: 2184, 槽4: 3537}**——高度偏斜（槽 4 占 39.8%）。
  其中 **9.3 pp 是我方 `_force_hand` 固定换到槽 4 造成的仪器痕迹**（见 §6.2 的整改建议）。
- 出牌时刻圣水：中位 **7.79**、均值 7.29、**13.6% 的标签帧正好在 10 费上限**
  ⇒ 印证 §8.4：**我们的圣水估计是上界**（不模拟人类在 10 费处的泄漏），
  `elixir_force = 2.18%` 是「模型宽裕」而非「人类宽裕」。
- 落点用到 428 / 576 格。

---

## 5. J3 · 模仿质量（留出 8,877；同一仪器、同一留出、配对比较）

### 5.1 读数表

> 未训练对照 = `FollowerPolicy` **随机初始化且播种**（`torch.manual_seed(0)`）：不播种时每次运行抽到不同权重，
> 实测两次 `top1` **0.3287 vs 0.3261**、`nll` **8.72 vs 8.80** ⇒ 对照读数不可复现（本轮已修）。

| 指标 | 随机初始化 | IL 训后 | trivial 基线 |
|---|---|---|---|
| NLL（`evaluate`，与 BC 训练同口径） | 8.6492 | **5.5962** | — |
| 落点精确（给定选择正确） | 0.07% | **12.10%** | 0.17%（576 格均匀） |
| 落点精确（不分选择对错） | 0.62% | **9.98%** | — |
| top-1 option | 32.91% | **40.42%** | uniform-6 **16.67%** / uniform-4 **25%** / **常数槽 4 = 39.84%** / 常数最高频卡 = 6.32% |
| 宏平均 recall（4 槽） | 34.62% | 35.76% | 常数预测器 **25%** |
| 逐槽 recall | 0.411 / 0.352 / 0.347 / 0.275 | 0.220 / 0.306 / 0.342 / **0.563** | — |

### 5.2 判读（**两句话，都要写**）

1. **落点侧确认学到**：给定「出哪张牌」猜对的前提下，落点逐格命中 **12.10%**（随机初始化 0.07%，
   均匀 0.17%）；整条动作分布的 NLL **8.65 → 5.60（−35%）**。这是**明确**的学习证据。
2. **出牌选择侧未获证实**：top-1 **40.42%** 只比「恒选槽 4」的 **39.84%** 高 **0.58 pp**；
   宏平均 recall 只从 34.62% → 35.76%（+1.3 pp），且训后是靠**放大槽 3 的偏置**换来的
   （槽 3 recall 0.266→0.563，代价是槽 1 recall 0.412→0.220）。
   ⇒ 换句话：模型学会了「**在哪儿放**」和「**动作分布长什么样**」，但**没有**学会
   「**人类此刻为什么出这张牌**」。

### 5.3 ★ 预注册闸门失效（必须点名的仪器缺陷，【R16】）

预注册写死「top-1 option ≥ 40% ⇒ PASS」。实测本留出集的**常数预测基线 = 39.84%**
⇒ **闸门恰好压在 trivial 基线上，PASS/FAIL 无分辨力**；随机初始化策略也能拿 **32.91%**
（因为它对槽 4 有初始偏置，而标签分布本就偏斜）。
**处置**：预注册判定**照写为 PASS**（不篡改），但**实质结论以上表第 2 行为准**（= F5 弱信号）；
下一轮预注册必须**改用「配对 + 宏平均 + 相对 trivial 的余量」判据**（阈值余量 ≫ 散布）。

### 5.4 健康度

留出 8,877 条**全部** `logprob > -1e8`（0 条被掩码静默掩掉）⇒ 数据集与掩码**自洽**
（这是 BC 侧最容易静默出错的地方）。

---

## 6. 与既有结论的关系 / 勘误

1. **不推翻任何既有结论**。`docs/agents/reward_surveys.md` 的「我方缺的是『回放 → (obs, action)』这一段」
   本轮被**补上**（新增两个脚本）；「直接重放只走通 35–62%」仍然成立——那是**不做手牌/圣水强制、
   且不区分「引擎已终局」**时的读数（本轮实测 `bundle_not_ok = 0`，因为强制后引擎不再拒绝）。
2. **更正一处我自己写错的归因**：§2.2 的 `Mirror` 残差**不是引擎缺口**，是我方对账仪器的口径错。
3. **§8.3 提前终局**是本轮新发现的限制，之前任何文档都没有记过。

---

## 7. 不确定与限制（**不许事后补，写在这里**）

| # | 限制 | 影响 |
|---|---|---|
| L1 | 人类**手牌顺序**不可观测 ⇒ `_force_hand` 是**假设**（交换会改 `next_card`，且该状态在正常出牌下不可达） | 标签的「手牌槽位」是**我方分配**的，不完全是人类的真实手牌位；下一轮应把强制槽位**随机化**（本轮固定槽 4 造成 9.3 pp 的人为偏斜） |
| L2 | 逐事件**圣水**不可观测 ⇒ 我们的圣水是**上界**（不泄漏）；13.6% 的标签帧在 10 费上限 | 观测的圣水通道整体偏高；`elixir_force=2.18%` 不能读成「人类不缺费」 |
| L3 | 人类局多数 **lv16**，我方统一 **lv11**（与 `rl/config.py:181` 的训练口径一致） | 与部署分布一致、与人类局不一致 |
| L4 | 我们引擎 ≠ 真实引擎；**2200/2200 局提前终局**（`end/dur` 中位 0.822） | 标签偏早期；残局行为**没有**样本 |
| L5 | `activate_ability` 事件（本分片 18,913 条）本轮**未转标签**；同帧 ≥2 手 244 帧丢弃 | 少了两类动作 |
| L6 | 只训 **`team` 侧**；BC 把每条样本当**首帧**（`hidden=None`，`human_play.py:224`） | 样本量约半；克隆的是**逐帧静态映射**，不是多帧序列策略 |
| L7 | 留出与训练**同源同分布**（同一分片、同一重建管线） | 测的是「能否克隆人类」而不是「能否打赢」 |

---

## 8. J4 · 下游效应 → **本轮未执行**（并给出原因，不粉饰）

预注册已把 J4 定为「**n=1/臂 ⇒ 不下结论，只作描述性**」。本轮连描述性读数也**没跑**，原因是
**一条既有 bug**：

- `src/clasher_new/rl/train_follower.py:225`
  `ep_obs, ep_belief, ep_plan, ep_bundle, ep_lp, ep_val, ep_rew = [], [], [], [], [], []`
  —— **7 个左值 / 6 个右值**（对照 `:162` 的正确写法是 7 个）；触发点 = 第一个 episode 结束时的重置块
  ⇒ **任何跑到首局结束的 `train_follower.py` 必崩**（`ValueError: not enough values to unpack (expected 7, got 6)`）。
  `git blame` = 初始提交 `b58db45`（2026-09-03），**从未被改过**。
- 后果：**`--init-from` 这条下游通路被它挡住**。另一条通路 `run_league.py --mode solo --main-init <ckpt>`
  （`rl/config.py:253 main_init`）**存在**且未被本 bug 影响。
- 本轮**没有改这一行**（本实验的不变量 §5.3「新增代码只加不改」），把决定**交回用户**：
  修 = 补 1 个 `[]`（属既有训练入口的行为变更，需按【R2】【R18】同步文档）。

**已完成的替代验证**：BC ckpt 的架构与维度被**独立复核**——`load_checkpoint` 成功、
`plan_dim=58` / `belief_dim=563` / 29 张量 / 634,735 参数，且 `--init-from` 的断言路径在
**同架构 ckpt** 上 exit 0（见 `runs/_probe_il/probe_ppo_initfrom_smoke.py`）。

---

## 9. 复现命令

```bash
# ① 取数据（1 分片 = 17.4 MB + 22.7 MB）
curl -L -o /mnt/e/fl_il_data/replays/part-000000.parquet \
  https://hf-mirror.com/datasets/VanguardX101/IL_Replay/resolve/main/replays/part-000000.parquet
# ② parquet → JSONL + 普查（必须 WSL python3：Windows venv 无 pyarrow）
/usr/bin/python3 scripts/_fl_il_extract.py --replays /mnt/e/fl_il_data/replays/part-000000.parquet \
  --actions /mnt/e/fl_il_data/actions/part-000000.parquet \
  --out-jsonl /mnt/e/fl_il_data/replays_part000000.jsonl \
  --out-census docs/fl_il_2026-09-20/census_part000000.json
# ③ 卡牌经济对账（J1.1）
./.venv/Scripts/python.exe scripts/fl_il_to_bc.py --mode reconcile \
  --jsonl /mnt/e/fl_il_data/replays_part000000.jsonl \
  --out docs/fl_il_2026-09-20/reconcile_part000000.json
# ④ 回放 → IL 训练对（J1.2 / J2）
./.venv/Scripts/python.exe -u scripts/fl_il_to_bc.py --mode samples \
  --jsonl /mnt/e/fl_il_data/replays_part000000.jsonl --out-dir runs/_fl_il_bc \
  --games 2200 --workers 10 --out docs/fl_il_2026-09-20/samples_part000000.json
# ⑤ BC 训练（既有通路）
cd src/clasher_new && ../../.venv/Scripts/python.exe rl/human_play.py --bc-train \
  --data-dir ../../runs/_fl_il_bc/train --bc-out ../../runs/_fl_il_bc/bc_fl.pt --bc-epochs 3
# ⑥ 留出判读（J3）+ 未训练对照
./.venv/Scripts/python.exe scripts/il_eval_holdout.py --ckpt runs/_fl_il_bc/bc_fl.pt \
  --data-dir runs/_fl_il_bc/holdout --train-dir runs/_fl_il_bc/train \
  --out docs/fl_il_2026-09-20/holdout_bc_fl.json
./.venv/Scripts/python.exe scripts/il_eval_holdout.py --random-init \
  --data-dir runs/_fl_il_bc/holdout --train-dir runs/_fl_il_bc/train \
  --out docs/fl_il_2026-09-20/holdout_random_init.json
```

## 10. 证据文件

| 文件 | 内容 |
|---|---|
| `docs/fl_il_2026-09-20/census_part000000.json` | 分片普查（模式/时长/卡等/卡键/void/坐标对账） |
| `docs/fl_il_2026-09-20/reconcile_part000000.json` | J1.1 对账 + 漏斗 + 逐局读数（3.9 MB） |
| `docs/fl_il_2026-09-20/samples_part000000.json` | J1.2/J2 + 逐局 `end_time`/强制计数（880 KB） |
| `docs/fl_il_2026-09-20/samples_run.log` | 转换作业真实输出（含进度与警告） |
| `docs/fl_il_2026-09-20/bc_train.log` | BC 训练真实输出（3 epoch） |
| `docs/fl_il_2026-09-20/holdout_bc_fl.json` | J3 训后（含混淆矩阵、宏 recall、三基线） |
| `docs/fl_il_2026-09-20/holdout_random_init.json` | J3 未训练对照（同仪器同留出） |
| `docs/fl_il_2026-09-20/samples_smoke.json` | 3 局 smoke（含逐标签 `elix_before` 留证） |

## 11. 下一刀（按性价比排序，均需**各自**的预注册）

1. **修 `train_follower.py:225`**（1 行）→ 才能跑 J4 下游；或直接用 `run_league.py --mode solo --main-init`。
2. **强制槽位随机化**（消除 §4 的 9.3 pp 人为偏斜）+ **圣水泄漏模型**（用 `elixir_leaked` 摊还）⇒ 修 L1/L2。
3. **换更强的判据**：配对 + 宏平均 + 「相对 trivial 的余量」（§5.3）。
4. **多帧序列克隆**（给 BC 侧接 `init_hidden`，`human_play.py:224` 现在是 `hidden=None`）⇒ 修 L6。
5. **扩数据**：分片 0 只用了 3528 局里的一部分；52 个分片 = 252,238 局（另有 20% 因时长/`void` 被拒）。
6. **`void` 卡**：409 局因此被拒，`src/clasher_new/cards.json` 里有 `Void`（id 28000023, 3 费），
   但引擎卡表不含 ⇒ 补表可再回收 409/5000 = 8.2% 的局。

---

## 12. 对局观测（用户 2026-09-20 追加：「给我 dashboard，我看几场 IL 后的对局」）

新 runner **`scripts/il_readout_games.py`**：p0 = IL 策略（`FollowerPolicy.act`，默认 argmax），
p1 = **同一 ckpt 的独立冻结副本**（自对弈镜像），信念/plan 链路与 `rl/human_play.py:142-154` 逐句同序，
逐块落**生产同格式（schema 5）录像** + `solo_state.json`，供 `rl/dashboard.py --solo` 边看指标边播回放。

三个臂（各 **10 局**、`seed=0`、argmax）：

| 臂 | 卡组 | `--hidden` | 局均帧 | 买得起帧 | **其中选择不出** | p0 出牌 | 出牌 top |
|---|---|---|---|---|---|---|---|
| **A** `runs/il_readout_solo` | 我方标准 solo 卡组 | `none` | **152.0** | 127/1520（8.4%） | **0.0%** | 127 | Skeletons 27 / IceWizard 26 / Log 25（**Xbow 0**） |
| **B** `runs/il_readout_fl` | FL 真实人类卡组（2765 套里前 10 套） | `none` | **298.8** | 285/2988（9.5%） | **0.0%** | 278 | ElectroSpirit 40 / Skeletons 39 / Log 39 / Firecracker 31 |
| **C** `runs/il_readout_carry` | 我方标准 solo 卡组 | **`carry`** | **576.1** | 5745/5761（99.7%） | **99.7%** | **17** | — |

### 12.1 ★★ 主读数：**逐帧 BC 不能当循环策略用**（L6 的活体演示）

**同一个 ckpt、同一副牌**，只改「GRU 隐状态是否跨帧携带」：

* `--hidden none`（**BC 训练时的口径**，`human_play.py:224` `hidden=None`）：**买得起时 0% 选择不出**——
  该出的牌都出，局在 152 / 299 帧内正常打完；
* `--hidden carry`（**生产约定**）：**买得起时 99.7% 选择不出**——10 局一共只出 **17 手**，
  局局拖到 600 帧上限（5761 帧 / 10 局）。

⇒ 结论：**IL ckpt 只在「每帧重置隐状态」的口径下可用**；按生产的 carry 约定部署会掉进 STOP 吸引子。
⚠️ C 臂的「买得起 99.7%」是**内生**的（不出牌 ⇒ 圣水堆到 10 ⇒ 永远买得起），**不是**局面宽裕。

### 12.2 其余读数（含禁则）

* **胜率不可读**：A/B 的 10 胜 0 负是「p0 每帧先手 + 双方同策略同卡组」的产物，**不是强度**
  （【R5】n=1、且本仓既有「同源对手 ⇒ 不含绝对强度」禁则）。C 臂 3 胜 6 负 1 平同样是镜像产物。
* **「能出牌」窗口占比** A **8.4%** / B **9.5%**——与 C14「全帧圣水≥6 ≈ 0.5%」同量级结构 ⇒
  **IL 没有改变「大部分帧买不起牌」这一结构**；且两个 none 臂都是 **0% 选择不出**，
  与 C14「A 段最长 22 < 34 帧」一致（policy 一旦买得起就出）。
* A 臂 **Xbow（6 费）0 次**、Tesla 2 次：IL 策略与人类一样**偏便宜牌**
  （人类数据 top1 也是 Skeletons 561）；B 臂前 10 套人类卡组下出的也是那些卡组的低费牌
  （ElectroSpirit / Skeletons / Log / Firecracker）。

### 12.3 dashboard（已实测 HTTP 200）

```bash
cd src/clasher_new
../../.venv/Scripts/python.exe rl/dashboard.py --solo ../../runs/il_readout_solo  --host 0.0.0.0 --port 8700
../../.venv/Scripts/python.exe rl/dashboard.py --solo ../../runs/il_readout_fl    --host 0.0.0.0 --port 8701
../../.venv/Scripts/python.exe rl/dashboard.py --solo ../../runs/il_readout_carry --host 0.0.0.0 --port 8702
```
⇒ `http://127.0.0.1:8700`（A）/ `:8701`（B）/ `:8702`（C）。
作业日志留证 `docs/fl_il_2026-09-20/readout_games.txt`（`.log` 被 `.gitignore` 覆盖，故副本用 `.txt`）；`runs/` 被 `.gitignore` 覆盖 ⇒ 录像不入库。

---

## 13. 预算剂量-反应：IL「不够」是不是只是算力/步数问题（用户 2026-09-20 追加）

> 预注册 [`docs/fl_il_scaling_prereg_2026-09-20.md`](fl_il_scaling_prereg_2026-09-20.md)（判据**跑前写死**，含失败分支）；
> 本节是**判读**。承接 §1–§12 的纪律：**只增不改**。20/30-epoch 点与 lr 轴的回填见 §15。

### 13.0 一句话

用户判断「**IL 还不够，要更多轮的训练或者调高学习率**」。本实验把它做成**单变量剂量-反应**：
固定数据 / 初值 / 样本序 / 损失 / 逐帧口径，只动 `epochs` × `lr`。

★ **结论**：**方向对，但要到 10 epoch 才真正越过分辨线，而且 0→3 epoch 买到的几乎不是「选牌」。**
1. **0→3 epoch 的 3.05 nats 改善里，选牌只占 7.6%**（51.5% 是「出完一张就 STOP」这个数据集自带的平凡模式、40.9% 是落点）；
2. **3→10 epoch 变了**：选牌项占比升到 **37.4%**，平衡口径（4 个槽位 recall 的宏平均）**0.3576 → 0.4077**，
   配对检验相对 3 epoch **净修好 426 条**（McNemar `1005 / 579`，p=6.3e-27），且**四个槽位全部净增**；
   ⇒ **「更多轮」在中段确实买到了真东西**（不是只买落点，也不是只靠塌向主流位）；
3. 但**预注册的 J-M2 合取判据仍然 FAIL**（槽 1/2 的 recall 没超过随机初始化参考）——该判据对「撒得开」的
   未训练策略有利（见 13.6 L-b），**无偏的平衡口径 macro 才是实质读数**，它 PASS。

> **★ 临时判读更正（诚实记录）**：我在 1–5 epoch 的中间读数上曾给出「训练越久越靠占位、真选择反而变差」
> （依据：位置受控命中率 0.2612 → 0.2316 → 0.2987 → 0.2348、macro 平在 0.34–0.358）。
> **10-epoch 点推翻了这个判读**：macro 跳到 0.4077、位置受控命中率跳到 0.3562（本轮最高）、
> 配对 per-slot 四个位全净增。⇒ 1–5 epoch 的摆动是**训练动力学的非单调段**，不是趋势。
> 这条更正按「只增不改」留在这里，原中间读数不删。

### 13.1 仪器与不变量（**全部新增；`human_play.py` / `follower.py` / 掩码一行未改**）

| 仪器 | 作用 |
|---|---|
| `scripts/il_bc_sweep.py` | 逐行复刻 `human_play.py:205-233` 的训练循环；样本只加载一次；逐 epoch 落 `mean_logprob`+秒数；`--save-every` 落快照 |
| `scripts/il_eval_holdout.py` | 留出读数（沿用 §5 的仪器，未改） |
| `scripts/il_nll_decompose.py` | NLL 三项分解（选牌/落点/终止）+ 参考值；**自带 1e-5 复刻闸门** |
| `scripts/il_paired_compare.py` | 配对：逐样本 NLL 差 + 符号检验 + top-1 McNemar + 逐槽配对（口径与上者同源） |
| `scripts/il_ckpt_equal.py` | 跨进程确定性闸门：逐张量 `torch.equal` |
| `scripts/il_curve_report.py` | 把上面几件产物汇成表（**禁手抄**，【R4】） |
| `scripts/il_card_usage.py` | **卡牌使用率口径**（用户提议，见 §14） |

* 4 个并行进程、全部 `--threads 1`。**跑前标定**：`batch=1` 是**开销瓶颈** —— `threads=1` 与 `threads=8`
  每步同为 ~11.5 ms（`scripts/il_bc_bench.py`）⇒ 加线程无用，正确姿势是**多进程单线程**（每进程 RSS 2.49 GB）。
* 实测一 epoch ≈ **400–590 s**（后期因并行的读数进程争 CPU 变慢）；**M1（10 ep）总墙钟 4,984 s ≈ 83 min**。
* 各格同 seed（`torch.manual_seed(0)` + `np.random.seed(0)`，且在构造 policy 之前）⇒ 同初值 + 同 permutation 序列。

### 13.2 F0 / J-M6：复刻等价与跨进程确定性 —— 全部 PASS

| 检验 | 结果 |
|---|---|
| ckpt 逐张量（`il_ckpt_equal.py`） | **29/29 `torch.equal`**：M0(3 ep) ≡ 已提交 `bc_fl.pt`；**M2@3 ≡ M0**；**M2@10 ≡ M1** |
| 留出聚合（`il_eval_holdout.py`） | top1 `0.40419060493409936`、cell `0.09980849386053847`、nll `5.596222589020527` —— **与 §5 逐位相同** |
| 配对（`il_paired_compare.py`） | 8,877/8,877 **tie**，ΔNLL `+0.0000`，McNemar `0/0` |
| 分解总和 vs 评测 NLL | `il_nll_decompose` 的 `nll_total` `5.5962` == 评测 `5.596222589…`（**跨仪器一致**） |

⇒ 扫参管线与既有 BC 通路**逐位等价**，且「长跑格的第 k 个 epoch 快照」**就是**独立 k-epoch 跑的同一组参数
（`threads=1` vs 默认线程数也没造成数值差异 ⇒ batch=1 路径的归约本就是单线程）。**J-M6 PASS**，单变量成立。

### 13.3 学习曲线（同一留出 8,877；`il_curve_report.py` 汇总，全部脚本复算）

| 格 | epoch | lr | 训练末 mean_lp | 留出 NLL | 分解 选牌/落点/终止 | top1 | 落点(条件) | 槽1 | 槽2 | 槽3 | 槽4 | **macro** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 随机初始化 | 0 | — | — | 8.6486 | 1.4350/5.6429/1.5707 | 0.3291 | 0.0007 | 0.4105 | 0.3521 | 0.3466 | 0.2754 | 0.3462 |
| M2@1 | 1 | 1e-3 | −5.973 | 5.8399 | 1.2196/4.6203/0.0000 | 0.4013 | 0.0839 | 0.1210 | 0.2902 | 0.3237 | 0.6127 | 0.3369 |
| M2@2 | 2 | 1e-3 | −5.725 | 5.6689 | 1.2094/4.4595/0.0000 | 0.4197 | 0.1068 | 0.0973 | 0.2261 | 0.3191 | 0.7037 | 0.3365 |
| M0 | 3 | 1e-3 | −5.612 | 5.5962 | 1.2025/4.3937/0.0000 | 0.4042 | 0.1210 | 0.2197 | 0.3057 | 0.3416 | 0.5635 | 0.3576 |
| M2@5 | 5 | 1e-3 | −5.395 | 5.4740 | 1.1819/4.2921/0.0000 | 0.4513 | 0.1321 | 0.1024 | 0.2261 | 0.3237 | 0.7781 | 0.3576 |
| **M1** | **10** | 1e-3 | **−5.022** | **5.4494** | **1.1476**/4.3018/0.0000 | **0.4522** | 0.1482 | 0.2791 | 0.3250 | 0.4295 | 0.5971 | **0.4077** |
| *trivial* | — | — | — | — | — | 0.3984 | — | 0 | 0 | 0 | 1.0 | 0.25 |

* 训练集 `mean_logprob` **单调下降**（−5.973 → −5.022，10 个 epoch **无平台**）——**但只说训练集**。
* **留出 NLL 也单调下降**（5.8399 → 5.4494）。
* **top1 越过预注册的 trivial+5pp 闸门（0.4484）出现在 5 epoch（0.4513）与 10 epoch（0.4522）**。
* **macro（对「恒选某位」免疫）在 10 epoch 才明显抬起来**：0.3369 → 0.3365 → 0.3576 → 0.3576 → **0.4077**。

**配对（M1 vs M0，10 ep vs 3 ep，同 8,877 帧）**：
* NLL：均差 **−0.1468**、中位 −0.2151、**B 更好 5,245 / A 更好 3,632**（符号检验 p=5.6e-66）；
* top-1 McNemar：**A 错→B 对 1,005 / A 对→B 错 579**（p=6.3e-27）⇒ 净 +426；
* **逐槽净增：槽1 +80（171 vs 91）、槽2 +35、槽3 +192（266 vs 74）、槽4 +119** —— **四个位全部净增**，
  即「改善不是只靠塌向主流位」。

### 13.4 NLL 三项分解：**算力买到的是哪一项**

`FollowerPolicy.evaluate` 的 logprob 是三项之和（本仓每帧恰 1 子动作 ⇒ 可分离）；
`il_nll_decompose.py` **不改 `follower.py`**，而是复刻该前向并自带闸门：**各点 `max|Δ| = 9.54e-07`**（阈值 1e-5）。

| 阶段 | Δ选牌(slot) | Δ落点(cell) | Δ终止(stop) | Δ合计 | 选牌占比 |
|---|---|---|---|---|---|
| 随机 → 3 ep | 0.2325 | 1.2492 | 1.5707 | 3.0524 | **7.6%** |
| 3 ep → 10 ep | 0.0549 | **0.0919** | 0 | 0.1468 | **37.4%** |

* 终止项 **1.5707 → 0 全在第 1 个 epoch 完成**：即「出完一张牌就 STOP」——本仓每条标签恰好 1 个子动作，
  是**数据集结构**而不是策略智慧（§5 J2 已记）。
* 选牌项 `nll_slot`：1.4350（随机）→ 1.2196（1 ep）→ 1.2025（3 ep）→ 1.1819（5 ep）→ **1.1476（10 ep）**；
  参考「不看特征的先验下界」`H(槽) = 1.3219` ⇒ 条件信息 `H − nll_slot`：3 ep **0.119**、10 ep **0.174 nats**（**+46%**）。
* 落点项 `nll_cell` 5.6429 → 4.3018（≈ 均匀撒合法格 5.6279 ⇒ 未训练落点头 ≈ 均匀；10 ep 时仍离解很远：落点条件命中 14.8%）。

### 13.5 判据（预注册的，逐条给结论）

| 判据 | 阈值（跑前写死） | 实测 | 结论 |
|---|---|---|---|
| **F0** 复刻等价 | 与已提交 ckpt 逐位（容差 5e-4） | 逐张量 `torch.equal` + 读数逐位相同 | **PASS**（且强于阈值：连浮点都一样） |
| **J-M6** 跨进程确定性 | M2@3≡M0、M2@10≡M1 逐张量 | 29/29 + 29/29 逐位相同 | **PASS** |
| **J-M1** NLL 单调 + 降 ≥0.28（3→30） | 1<2<3<5<10<20<30 且 3→30 ≥0.28 | 单调 ✓；3→10 = **0.1468** | **部分**：单调成立；幅度**要等 30 ep**（§15） |
| **J-M2** 合取（top1 ≥0.4484 且 槽1/2/3 > 随机基线 且 槽1 ≥0.45） | 三条全中 | 10 ep：top1 **0.4522** ✓；槽1 0.2791 ✗（<0.4105）、槽2 0.3250 ✗（<0.3521）、槽3 0.4295 ✓；槽1 ≥0.45 ✗ | **FAIL**（合取）——但见下 |
| ↳ 无偏替代（平衡口径 macro > 随机初始化） | 未预注册（判据偏差所致） | 10 ep **0.4077 > 0.3462** | **PASS**（实质读数） |
| **J-M3** lr 方向 | 描述性 + 健康度 | M3/M4 未跑完 | 待 §15 |
| **J-M4** 行为面 | 描述性 | 未跑 | 待 §15 |
| **F5′**（NLL 掉而选择不动） | — | **未触发**：3→10 ep 选牌占比 37.4%、net +426、macro +5.0 pp | 不成立 |

★ **J-M2 为什么 FAIL 而仍判「学到了」**：判据要求「槽 1/2/3 的 recall 都超过**随机初始化**」。
随机初始化不是无信息策略——它**撒得开**（大量预测槽 1，故槽 1 recall 0.4105 比人类的槽 1 占比 15.2% 高得多）。
以它为门槛，等于要求收敛型策略先复现「撒网式的错误分布」。**无偏的对等读数是 macro**（各槽 recall 的宏平均，
对恒选免疫）：随机 0.3462 → 10 ep **0.4077**。两条都如实列出，**不删预注册判据**。

### 13.6 口径反思与限制（本实验自己的）

* **L-a 非单调**：1–5 epoch 段有摆动（macro 0.3369/0.3365/0.3576/0.3576；TVD 0.138/0.112/0.128/0.104）
  ⇒ **单点不能当曲线**，本实验的「10 epoch 跳变」需要 20/30 ep 复现（§15）。这也意味着
  **J-M1「单调」在 NLL 上成立、在别的一律别当成单调**。
* **L-b 参考点不是「无信息」**：见 13.5 的 ★。凡以 random-init 的 per-slot recall 作阈值的判据都偏保守。
* **L-c 全是逐帧 off-policy 读数**（`hidden=None` + argmax），**不是对局行为**；对局行为见 §12，且 §12 已证
  **carry 隐状态会掉进 STOP 吸引子**（逐帧 BC 不能直接当循环策略）。
* **L-d 「槽位」是我们重建出来的**：`_force_hand` 只在 `cycle` 内对换（`fl_il_to_bc.py:294-306`），
  91.2% 的帧人类出的牌本来就在重建出的 4 窗内（≫ 随机 1/2）⇒ 窗口大体是对的；
  但**窗口内的次序约定**未被独立验证 ⇒「人类 39.8% 出第 4 槽」**是行为还是重建约定的产物，成因未定**（§14 直接受影响）。
* **L-e 未跑完**：M3（lr 3e-3）/ M4（lr 1e-2）/ ep20 / ep30 —— 见 §15 回填。


---

## 14. 卡牌使用率口径：源数据 vs 训练后（用户 2026-09-20 提议）

> 判据（J-U1/J-U2/J-U3）与参考点写在预注册 [`fl_il_scaling_prereg_2026-09-20.md`](fl_il_scaling_prereg_2026-09-20.md) **§4bis**
> —— **在本口径任何读数之前**写完（诚实标记：该口径由用户在看过 1/2/3-epoch 读数后提出）。
> 仪器 `scripts/il_card_usage.py`；同一批 8,877 留出帧逐帧配对。

### 14.0 读数

**主表**（`il_curve_report.py` 汇总；`U` = 1 − mad(cond_h,cond_p)/mad(cond_h,0.25)；`位置受控命中率` 见 14.2）

| 策略 | TVD(人类,策略) | U 偏好复现 | ρ_cond | ρ_resid [95%CI] | **位置受控命中率** |
|---|---|---|---|---|---|
| 随机初始化 | 0.1906 | −0.1992 | 0.4612 | 0.3852 [0.202, 0.548] | 0.3646 |
| 1 ep | 0.1382 | 0.0848 | 0.6819 | 0.1654 [−0.030, 0.358] | 0.2612 |
| 2 ep | 0.1119 | 0.2467 | 0.7245 | 0.1683 [−0.030, 0.360] | 0.2316 |
| 3 ep | 0.1281 | 0.1544 | 0.7233 | 0.3120 [0.131, 0.488] | 0.2987 |
| 5 ep | **0.1035** | 0.4109 | 0.8924 | 0.4349 [0.275, 0.583] | 0.2348 |
| **10 ep** | 0.1217 | 0.2766 | 0.8751 | **0.4497 [0.274, 0.597]** | **0.3562** |
| *trivial 恒选槽 4* | **0.0617** | **0.6332** | **0.9006** | 无定义（残差 ≡ 0） | **0.0000** |
| *手牌均分* | 0.2077 | — | — | — | — |

**逐卡（源数据 vs 3 ep vs 10 ep；同一留出帧，脚本生成）**：

| 卡 | 人类 n | 3ep 策略 n | 10ep 策略 n | 在手 n | cond 人类 | cond 3ep | cond 10ep |
|---|---|---|---|---|---|---|---|
| Cannon | 247 | 240 | 174 | 1547 | 0.1597 | 0.1551 | 0.1125 |
| BarbLog | 360 | 349 | 310 | 1291 | 0.2789 | 0.2703 | 0.2401 |
| Skeletons | 561 | 631 | 741 | 1289 | 0.4352 | 0.4895 | 0.5749 |
| Log | 293 | 247 | 188 | 1268 | 0.2311 | 0.1948 | 0.1483 |
| Rocket | 56 | 77 | 28 | 1248 | 0.0449 | 0.0617 | 0.0224 |
| Tornado | 146 | **418** | 163 | 1012 | 0.1443 | **0.4130** | 0.1611 |
| MightyMiner | 208 | 142 | 154 | 957 | 0.2173 | 0.1484 | 0.1609 |
| Earthquake | 64 | 96 | 20 | 934 | 0.0685 | 0.1028 | 0.0214 |
| GoblinBarrel | 191 | 185 | 213 | 880 | 0.2170 | 0.2102 | 0.2420 |
| Fireball | 73 | 100 | 42 | 822 | 0.0888 | 0.1217 | 0.0511 |
| Berserker | 271 | 282 | 321 | 791 | 0.3426 | 0.3565 | 0.4058 |
| ElectroSpirit | 325 | 306 | 355 | 694 | 0.4683 | 0.4409 | 0.5115 |
| Princess | 272 | 302 | 334 | 690 | 0.3942 | 0.4377 | 0.4841 |
| IceSpirits | 272 | 255 | 324 | 677 | 0.4018 | 0.3767 | 0.4786 |
| Lightning | 43 | 21 | **2** | 632 | 0.0680 | 0.0332 | 0.0032 |

读法（**逐卡不是「定型」，是在换偏差画像**）：
* 3 ep 的**病态**（Tornado 处方 418 vs 人类 146，cond 0.4130 vs 0.1443）在 **10 ep 被修好**（163 / 0.1611）；
* 但 10 ep 出现**新的系统性偏差：法术被压得很低** —— Lightning `0.0032`（人类 0.0680，比例 ~1/21）、
  Earthquake 0.0214 / 0.0685、Fireball 0.0511 / 0.0888、Rocket 0.0224 / 0.0449；同时 Skeletons 被抬高
  （cond 0.5749 vs 0.4352）。⇒ 与 TVD 在 5→10 ep 回升（0.1035 → 0.1217）一致。
* 全表在 `docs/fl_il_2026-09-20/sweep/usage_*.json`（`per_card`）。

### 14.1 判据逐条

| 判据 | 阈值（§4bis 跑前写死） | 实测 | 结论 |
|---|---|---|---|
| **J-U1** 边际分布逼近（合取） | ①TVD 在 {1,2,3,5,10,20,30} **单调不增**；②min TVD ≤ 0.1906−0.02；③min TVD ≤ 0.5×0.0617=0.0308 | ①**不成立**（0.1382→0.1119→0.1281→0.1035→0.1217，两次回升）；②✓（0.1035）；③**不可能成立**（trivial 自己就 0.0617） | **FAIL**（③被 trivial 参考打穿，见 14.2） |
| **J-U2** 条件偏好排序 | ρ_cond ≥ 0.5 且 ≥ ρ_cond(随机)+0.2 = 0.6612 | 5 ep 0.8924 ✓／10 ep 0.8751 ✓（随机 0.4612） | **PASS**（5/10 ep 均通过） |
| **J-U3①** 去占用后仍非噪声 | ρ_resid > 0 且卡牌级自助 95% CI 下界 > 0 | 10 ep **0.4497 [0.274, 0.597]**；且 3→5→10 单调上升（0.312→0.435→0.450） | **PASS** |
| **J-U3②** 超过随机初始化 +0.15 | ≥ 0.3852+0.15 = 0.5352 | 0.4497（>随机 0.3852，但未达 0.5352） | **FAIL** |
| **F-U**（NLL 降而分布不逼近） | — | TVD 从 0.1906 → 0.1035–0.1217、ρ_cond 0.4612 → 0.8751、ρ_resid CI 排除 0 | **不成立** |

### 14.2 关键结构：这个口径有多大成分是在量「槽位占用」

* **源数据的人类「选牌」几乎就是「第 4 槽占用」**：标签卡身份分布 vs **槽 4** 身份分布 TVD = **0.0617**，
  而 vs 槽 1 = **0.4208**、槽 2 = **0.2853**、槽 3 = **0.1764**；标签槽位分布 = 槽1 15.2% / 槽2 20.4% / 槽3 24.6% / **槽4 39.8%**。
* 四个槽位的**身份边缘分布彼此差得很大**（槽1↔槽4 TVD = **0.4160**）⇒ 身份**不是**位置不变的。
  两者合起来的意思是：**人类出的牌倾向于恰好落在我们重建手牌的第 4 位**，于是「恒选第 4 槽」天然拿到
  TVD 0.0617 / ρ_cond 0.9006 / U 0.6332 —— 全口径**最强的参考是一个 trivial 策略**。
* ⇒ **J-U1 的边际 TVD 只能判「像不像源分布」，不能判「学到没学到偏好」**（它的 trivial 上界比训练后更好）。
  真读数在 **`U` / `ρ_cond` / `ρ_resid`** 与下面的位置受控命中率。
* **这 39.8% 是行为还是重建约定的产物：成因未定**（§13.6 L-d）。
  `_force_hand` 的对换只作用于 `cycle` 内部（`fl_il_to_bc.py:304-305`，目标位默认 `i=3` ⇒ 默认就摆到第 4 槽），
  而「人类出的牌本来就在重建窗口内」占 91.2%（≫ 随机的 50%）⇒ 窗口大体可信，但**窗口内的次序**无从独立核对。

### 14.3 位置受控的「真选择」读数（本实验为避免上面这个坑而加）

**定义**：只在**人类打了非第 4 槽**的 5,340 帧（60.2%）上量 top-1 命中率。
此时机会线 = 1/4 = 0.25，而**恒选第 4 槽的策略恒为 0.0000** ⇒ 这个子集上的命中率**不能**靠占位先验刷出来。

| 策略 | 非第4槽子集命中率 | （对照）第4槽子集 |
|---|---|---|
| 随机初始化 | **0.3646** | 0.2754 |
| 1 ep | 0.2612 | 0.6127 |
| 2 ep | 0.2316 | 0.7037 |
| 3 ep | 0.2987 | 0.5635 |
| 5 ep | 0.2348 | 0.7781 |
| **10 ep** | **0.3562** | 0.5971 |
| 恒选槽 4 | **0.0000** | **1.0000** |

* 10 epoch 是曲线上的**最好点**（0.3562，1 ep 时的 0.2612 → 10 ep 的 0.3562），**但仍略低于未训练策略的 0.3646**。
* 该子集命中率必须**同时**看第 4 槽子集：5 ep 是「独占第 4 槽」的极端（0.7781 / 0.2348），
  10 ep 是「两边都还行」的解（0.5971 / 0.3562）⇒ **10-epoch 的策略不是被占位主导的**。

### 14.4 对用户这条指标的总判读

1. **它确实捕捉到了「训练在向源数据靠拢」**，且这个靠拢**不只是落点**：TVD 0.1906 → 0.1035–0.1217、
   `ρ_cond` 0.4612 → 0.8751、`ρ_resid` 的 CI 排除 0 且 3→10 ep 单调上升（0.312→0.450）。
2. **但它把「跟着槽位占用走」也算成分数**：trivial 恒选槽 4 在 TVD / U / ρ_cond 上**全面强于训练后策略**，
   因为源数据的卡牌使用率本身主要由「第 4 槽占用」解释（§14.2）。
3. ⇒ **用它做「IL 好不好」的单一指标会被 trivial 参考压住**；建议的读法是
   **`ρ_resid`（去占用后的偏好排序）+ 位置受控命中率** 这一对，再加上 §13 的分解口径。
   两个口径互相独立，且**在 10 epoch 上一致指向「学到了、但只到弱-中水平」**。

---

## 15. 回填与待跑（lr 轴 / 20-30-epoch 终点 / 行为面）

> 本节按「只增不改」在 §13–§14 之后追加；每格落盘即回填，**不改**上面已写的读数。

**待回填清单**（守夜作业自动跑：留出读数 + NLL 分解 + 卡牌使用率 + 配对）：

| 格 | 目的 | 状态 |
|---|---|---|
| M3 = 10 ep / lr 3e-3 | J-M3 学习率方向（同 epoch 数、换 lr） | ✅ 已回填 §15.1 |
| M4 = 10 ep / lr 1e-2 | J-M3 发散探针 | ✅ 已回填 §15.3（非发散、卡坏区） |
| M2@20 / M2@30 | J-M1 的 1<2<3<5<10<20<30 单调 + 3→30 ≥0.28 | ✅ 已回填 §15.4/§15.5（**破单调 + 过拟合**，J-M1 FAIL） |
| J-M4 对局观测 | 最优格（10 ep）跑 `il_readout_games.py --decks solo --hidden none --games 10` | ✅ 已回填 §15.2 |

### 15.1 回填：lr 轴（M3 = 10 ep / lr 3e-3，已落盘）

同一 epoch 数、只换 lr ⇒ **调高学习率更差**：

| 格 | 留出 NLL | top1 | macro | 位置受控命中率 | TVD | ρ_cond | ρ_resid |
|---|---|---|---|---|---|---|---|
| M1 = 10 ep / **1e-3** | **5.4494** | **0.4522** | **0.4077** | **0.3562** | 0.1217 | 0.8751 | 0.4497 |
| M3 = 10 ep / **3e-3** | 5.5902 | 0.4274 | 0.3742 | 0.2972 | 0.1206 | 0.7850 | 0.4051 |

* **配对（M3 为 B、M1 为 A）**：NLL 均差 **+0.1409**（A 更好 4,878 / B 更好 3,999，符号检验 p=1.1e-20）；
  top-1 McNemar **A 对→B 错 847 / A 错→B 对 627**（p=1.1e-8）⇒ 3e-3 **净弄坏 220 条**。
* ★ **J-M3 判读**：**「调高学习率」不是通路**（同 epoch 数下全面更差，不是发散、是收敛更慢/更粗）。
  注意 **边际 TVD 分辨不出两者**（0.1206 vs 0.1217）——**只有选择类口径能分辨**，又一次印证 §14.2 的结构。
* 训练集 `mean_logprob` 也印证：M3 第 9 个 epoch = **−5.340** vs M1 同期 **−5.080**。
  ⇒ 单位墙钟的「有效学习」在 1e-3 更好；若目标是「更远」，正确轴是 **epochs（§13.3）**，不是 lr。

### 15.2 回填：J-M4 对局观测（10-epoch ckpt，`--decks solo --hidden none --games 10`）

作业 `bash-31`；留证 `docs/fl_il_2026-09-20/readout_10ep.txt`（录像在 `runs/il_readout_10ep/`，被 `.gitignore` 覆盖）。

| 读数 | 10-epoch ckpt | （对照）§12 臂 A = 3-epoch ckpt |
|---|---|---|
| 帧数 / 局均 | 1,423 / **142.3** | 1,520 / 152.0 |
| 空 bundle（买不起） | 91.4% | 91.6% |
| 买得起却选择不出 | **0.0%** | **0.0%** |
| p0 出牌次数 | 122 | 127 |
| 出牌 top | Skeletons 27 / Knight 24 / Log 24 / IceWizard 24 / Archer 21 / Fireball 1 | Skeletons 27 / IceWizard 26 / Log 25（**Xbow 0**） |

⇒ **离线读数的改善（macro +5.0 pp）在行为面上没有可见变化**：出牌谱、每帧出牌率、`买得起却不出` 仍是同一个样子，
**10 局里 Xbow 依旧 0 次**（与 **C14「从不攒费」**一致：逐帧策略只在买得起时立刻出便宜牌）。
⚠️ n=10、同源对手 ⇒ 这是**描述性观测，不构成判决**（【R5】+ §12 的禁则）。

### 15.3 回填（续）：lr 轴的**完整**剂量-反应（M4 = 10 ep / lr 1e-2 已落盘）

| lr（均 10 epoch） | 训练末 mean_lp | 留出 NLL | top1 | macro | 位置受控命中率 | TVD | ρ_resid [95%CI] |
|---|---|---|---|---|---|---|---|
| **1e-3** | **−5.022** | **5.4494** | **0.4522** | **0.4077** | **0.3562** | 0.1217 | 0.4497 [0.274, 0.597] |
| 3e-3 | −5.305 | 5.5902 | 0.4274 | 0.3742 | 0.2972 | 0.1206 | 0.4051 [0.222, 0.571] |
| 1e-2 | **−6.547**（10 个 epoch 全在 **−6.55 ± 0.01**，**根本不下降**） | 6.5000 | 0.3844 | 0.3253 | — | — | — |

* **配对（B vs A）**：M3 vs M1 → NLL 均差 **+0.1409**（A 好 4,878 / B 好 3,999，p=1.1e-20）、McNemar **847 / 627**（p=1.1e-8）；
  M4 vs M1 → NLL 均差 **+1.0506**（A 好 **6,765** / B 好 2,112）、McNemar **1,302 / 700**（p=9.6e-42）。
* M4 的 top1 **0.3844 低于 trivial 0.3984**、macro **0.3253 低于随机初始化 0.3462**、槽 1 recall **0.0252**（几乎不再预测槽 1）。
* **J-M3 判读**：**「调高学习率」= 单调更差**（1e-3 ≫ 3e-3 > 1e-2）。且 1e-2 **不是数值发散**——
  `logprob < -1e8` 计数 = **0**、无 NaN，是**卡在坏区域不下降**（逐 epoch 变化 < 0.02）。
* ★ 边际 TVD 在 M1/M3 之间**分辨不出**（0.1206 vs 0.1217），**只有选择类口径能分辨**（macro 0.3742 vs 0.4077）——第三次印证 §14.2。

### 15.4 回填（关键）：10 → 20 epoch 出现**过拟合拐点**，并**推翻 §13.5 的 J-M1「单调成立」**

| epoch | 训练 mean_lp | 留出 NLL | 差（留出 − 训练） | top1 | macro | 位置受控 | TVD | ρ_resid |
|---|---|---|---|---|---|---|---|---|
| 1 | −5.973 | 5.8399 | −0.133 | 0.4013 | 0.3369 | 0.2612 | 0.1382 | 0.165 |
| 3 | −5.612 | 5.5962 | −0.016 | 0.4042 | 0.3576 | 0.2987 | 0.1281 | 0.312 |
| 5 | −5.395 | 5.4740 | +0.079 | 0.4513 | 0.3576 | 0.2348 | 0.1035 | 0.435 |
| **10** | −5.022 | **5.4494（最优）** | +0.427 | **0.4522** | **0.4077** | 0.3562 | 0.1217 | 0.4497 |
| 20 | −4.438 | **5.9903（反弹 +0.54）** | **+1.605** | 0.4383 | 0.4060 | **0.3639** | **0.1075** | **0.5649** |

* ★ **推翻（按「只增不改」记录）**：§13.5 当时写「J-M1 单调成立（1<2<3<5<10）」——**第 20 个 epoch 破单调**：
  `nll(20) = 5.9903 > nll(10) = 5.4494`，且 `nll(3) → nll(20)` 是 **+0.394（变差）** ⇒ **J-M1 判 FAIL**。
* ★ **两种口径必须分开写，不许一句话调和**：
  * **似然口径过拟合确凿**：训练侧仍单调下降（−5.973 → −4.438），留出侧在 10 epoch 触底后反弹 **+0.54 nats**，
    训练-留出 gap 从 **+0.43 拉到 +1.61**；分解两侧同时变差（slot 1.1476→**1.2562**、cell 4.3018→**4.7341**）。
  * **确定性策略口径未退化**：macro 0.4060 ≈ 0.4077、位置受控 **0.3639 ≥ 0.3562**、TVD **0.1075 < 0.1217**、ρ_resid **0.5649 > 0.4497**。
  ⇒ 正确写法 = **「留出似然的最优点在 ~10 epoch，其后是过拟合；argmax/分布口径未退化」**，不得写成「20 epoch 更差」。
* 规模侧背景：训练样本 **38,838** / 参数 **634,735** / **无正则** / batch=1（≈38.8k 次更新每 epoch）
  ⇒ 与「数据量是瓶颈」的假设一致（**O12 ⑥：全集 52 片 = 252,238 局**）。
* ep30 待回填（作业 `bash-16`）。

### 15.5 回填（终局）：30 epoch —— 过拟合继续，而**用户那条分布口径仍在「变好」**

全部扫参格已落盘（`bash-16` 于 19:11 结束，30 epoch 总墙钟 **14,852 s ≈ 4.1 h**）。

| epoch | 训练 mean_lp | 留出 NLL | gap | top1 | macro | 落点(条件) | TVD | ρ_cond | ρ_resid | 位置受控 |
|---|---|---|---|---|---|---|---|---|---|---|
| **10** | −5.022 | **5.4494** | +0.43 | **0.4522** | **0.4077** | 0.1482 | 0.1217 | 0.8751 | 0.4497 | 0.3562 |
| 20 | −4.438 | 5.9903 | +1.61 | 0.4383 | 0.4060 | 0.1306 | 0.1075 | 0.8642 | 0.5649 | **0.3639** |
| 30 | −3.816 | **6.6541** | **+2.84** | 0.4422 | 0.3923 | 0.1159 | **0.0943** | **0.8951** | 0.5545 | 0.3305 |
| *trivial 恒选槽 4* | — | — | — | 0.3984 | 0.25 | 0 | **0.0617** | **0.9006** | 无定义 | **0.0000** |
| *随机初始化* | — | 8.6486 | — | 0.3291 | 0.3462 | 0.0007 | 0.1906 | 0.4612 | 0.3852 | 0.3646 |

* **配对（30 vs 10）**：ΔNLL **+1.2047**（A 好 **5,924** / B 好 2,953，p=2.6e-222）；
  top-1 McNemar **1,060 / 1,149（p=0.061）** ⇒ 30 epoch 的 top-1 与 10 epoch **统计上分不开**，而**似然明显更差**。
* **分解**：slot **1.3694**（10 ep 1.1476）、cell **5.2847**（10 ep 4.3018）⇒ 两侧同时退化。
* ★★ **本实验最重要的一条口径证据**：**TVD 一路「变好」直到 30 epoch**（0.1382→…→0.1217→0.1075→**0.0943**）、
  `ρ_cond` **0.8951 已贴到 trivial 的 0.9006**；**而同一批 ckpt 的留出似然从 5.45 涨到 6.65、位置受控命中率从 0.3562 掉到 0.3305**。
  ⇒ 边际使用率在**过拟合段**仍继续改善，因为改善方向就是**向「第 4 槽占用」这个 trivial 解收敛**（§14.2）。
  **单看这一条口径会判「继续训练有效」，而它是错的**——这正是本实验把「分布口径」和「位置受控 / 似然口径」并列的原因。

### 15.6 本实验的最终结论（三句话）

1. **「IL 还不够」成立；「更多轮 / 更高 lr」都不成立**——3→10 epoch 确实买到了选择类改善（macro +5.0 pp、McNemar 1005/579、四槽全净增），
   但 **10 epoch 是留出似然最优点**（其后 20/30 epoch 单调恶化到 6.65，gap +2.84），**lr 调高单调更差**（3e-3 净坏 220 条，1e-2 卡在 −6.55）。
2. **瓶颈在数据量与损失/标签形态**，不在训练强度：0→3 epoch 的改善 92% 与选牌无关；用户的分布口径在过拟合段还继续「变好」；
   位置受控的真选择命中率（10 ep 0.3562）**始终没超过未训练策略**（0.3646）。
3. 下一刀按证据优先级：**加数据（52 片 = 252,238 局）/ 正则 + 留出 NLL 早停 / 槽位随机化（消人工第 4 槽）/ 损失侧重 / 时序结构**（O12）。

**复现命令**（全部从仓库根、Windows venv）：

```bash
# 扫参（4 进程并行、单线程，见 §13.1）
./.venv/Scripts/python.exe -u scripts/il_bc_sweep.py --data-dir runs/_fl_il_bc/train     --out-dir runs/_fl_il_bc/sweep --configs 10:3e-3 --threads 1 --manifest runs/_fl_il_bc/sweep/sweep_m34.json
# 留出读数 / 分解 / 配对 / 使用率
./.venv/Scripts/python.exe scripts/il_eval_holdout.py --ckpt <ckpt> --data-dir runs/_fl_il_bc/holdout     --train-dir runs/_fl_il_bc/train --out docs/fl_il_2026-09-20/sweep/curve_<tag>.json
./.venv/Scripts/python.exe scripts/il_nll_decompose.py --ckpt <ckpt> --data-dir runs/_fl_il_bc/holdout     --out docs/fl_il_2026-09-20/sweep/nll_decomp_<tag>.json
./.venv/Scripts/python.exe scripts/il_paired_compare.py --ckpt-a <A> --ckpt-b <B>     --data-dir runs/_fl_il_bc/holdout --out docs/fl_il_2026-09-20/sweep/paired_<tag>.json
./.venv/Scripts/python.exe scripts/il_card_usage.py --ckpt <ckpt> --data-dir runs/_fl_il_bc/holdout     --top 15 --out docs/fl_il_2026-09-20/sweep/usage_<tag>.json
# 汇总表（禁手抄）
python3 scripts/il_curve_report.py --dir docs/fl_il_2026-09-20/sweep     --rows "ep01:1:1e-3,m0_3ep_1e-3:3:1e-3,ep05:5:1e-3,m1_10ep_1e-3:10:1e-3,ep20:20:1e-3,m2_30ep_1e-3:30:1e-3"
```
