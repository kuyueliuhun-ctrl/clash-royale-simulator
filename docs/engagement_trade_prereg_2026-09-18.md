# 局面圣水交换信用分配 · 预注册（2026-09-18）

> **触发**：用户方案（三轮澄清后的最终形态）——
> ①以**局面**为基准结算，对每个决策帧打分；②评价含「放的牌之后造成的伤害 / 打死的敌方圣水价值 /
> **被索敌时其他己方卡牌的战果**」；③「差分」不是"有价值即加分"而是"**有足够价值才加分**"；
> ④划分条件修正为「仅有建筑与其最近召唤物」；⑤**召唤物本身不用圣水计量，但它的战斗价值（吸引的仇恨 + 造成的伤害）
> 算给召唤它的单位**，再用**召唤者的圣水费用去对标它防守的卡**。
>
> **设计依据与全部论证**：[`frame_credit_proposal_review_2026-09-18.md`](frame_credit_proposal_review_2026-09-18.md)
> §7（缺口确证与改判）、§8（局面划分规格 v1）、§9（溯源路由 / 货币泵否决）。
> **本文件性质**：**预注册**。判据、失败分支、不变量**在写代码之前写死**（【R3】）；跑完照单读。
> 凡**未经实测**的数字一律标 **待标定**，并登记偏离（沿用 `reward_settlement_prereg_2026-09-14.md` §3 的先例）。

---

## §0 摘要（三句话）

1. **为什么做**：现行奖励里**「被阻止的伤害 / 拉扯」这一格是 0（甚至为负）**——塔血没掉不产生任何正奖励，
   而冰人/骷髅被吃掉还让 `Φ` 掉 2 费 ⇒ **正解被记为负**。本仓当年把这一格交给 `critic + 示范`
   （`rl_reward_plan_v2.md:169`），而 **critic 是常数、示范也没有** ⇒ **缺口确证无人管**。
2. **做什么**：把**已有的 `Φ` 账**从"逐帧"改成"**按局面窗口结算 + 阈值**"，再补一项**真新信息**（塔血保全 / 仇恨），
   并把免费产物的**战斗价值溯源路由给召唤者**（**绝不给产物体估值** —— 估值是货币泵，见 §4）。
3. **判据**：主判据**不用胜率**（【R5】）；用机制层 + 行为层 + **优势兑现率门禁**三层，全部同批配对复算（【R4】）。

---

## §1 定义（全部必须可脚本复算）

| 概念 | 定义 | 数据来源 |
|---|---|---|
| **索敌关系图** `G_t` | 每 tick 在场上实体上建无向图：`(a,b)` 有边 ⟺ `a.current_target is b` 或 `b.current_target is a` | `battle.py:1174` `update_current_target()` 每 tick 重算 |
| **局面** | `G_t` 的**连通分量**（并查集，用实体 `uid` 为键；**不是**"建筑/召唤物"分类） | 新增 `uid`（§6） |
| **交战对** | 分量内存在 `(a,b)`：`a` 侧 vs `b` 侧**分属两边**，且 **`a`/`b` 至少一侧是"非产物体"**（**规则①**，用户已确认） | `uid` + 溯源标记 `is_product` |
| **局面结算** | 该分量**连续 `K` 个 tick 无交战对**（去抖），或分量**分裂/清空** | `K = 30` tick = **1 个决策帧**（粒度对齐，非标定阈值） |
| **产物体** | 由**建筑/单位/法术**生成的实体（Tombstone 骷髅、FirespiritHut 火精灵、GoblinDrill、Graveyard 骷髅、Witch/SuperWitch 召唤物、亡语产物…） | 溯源标记，非卡类别表 |
| **`root_cast`** | 该产物体**溯源链的根 = 那次"出牌事件"**（见 §3） | 新增字段，**写在产物体自己身上** |
| **残值 `V`** | 既有 Φ 账里的 `Σ存活部署实体 × 部署费`（**产物体计 0**，沿用 `rl_reward_plan_v2.md:19`） | 既有 `my_v_*` / `opp_v_*` |

---

## §2 指标规格：**净交换 = 窗口内 ΔΦ + 塔血保全项**

### 2.1 主项（**不是新信息**，只是把已有 Φ 改成窗口结算）

对局面窗口 `[t0,t1]`，`Trade_me` 恒等于（推导见评审 §7.2）：

```
Trade_me = ΔΦ_window + tower_term
ΔΦ_window = [Δ(手牌圣水+残值)_me − Δ(手牌圣水+残值)_op]  在窗口内
```

**等价的自明形式**（便于对账与 selftest）：

```
净支出_x = Σ_{x 在窗口内 deploy 的花费} − (V_x(t1) − V_x(t0))
Trade_me = 净支出_op − 净支出_me + tower_term
```

- **自检算例 A（纯进攻，用户例）**：我 3+3=6 费全下、对方 6 费来解且**全部死光**、我全活 ⇒
  `净支出_me = 6 − 6 = 0`、`净支出_op = 6 − 0 = 6` ⇒ `Trade_me = +6` ✅ 与用户口径一致。
- **自检算例 B（拉扯，用户例）**：1 费骷髅解 4 费小皮卡（骷髅死、皮卡死）⇒
  `净支出_me = 1`、`净支出_op = 4` ⇒ `Trade_me = +3` ✅「1 费解 4 费」。
- **结算时刻的残值必须计入**：对面 5 费小屋仍立在场上 ⇒ 算它 **5 费返场**（否则虚增我方交易）。
- **圣水自然回复不计**（双方对称，且不是任何动作引起的）。

### 2.2 塔血保全 / 仇恨项 `tower_term`（**唯一真新信息**）

**这是全文件唯一不能凭设计写死的量**（真反事实要 43.3 ms/次推演，见评审 §3 P5 ⇒ 只能代理）。
三个候选代理，**按此顺序尝试，前一个无信号才用后一个**：

| 代理 | 公式 | 需要新常量 | 可刷性 |
|---|---|---|---|
| **P3（首选）** | `Σ_{敌方单位 E} cost(E) × [E 在"被我方实体钉住"期间死亡]` | **不需要**（复用既有 `V` 注销 + 索敌关系） | **不可刷**（延长钉住不加分，只有真打死才结算） |
| P1 | `Σ_E dps(E) × 钉住秒数`（`dps` 由引擎 `data.damage/hit_speed` 算） | 不需要（引擎已有），但**会高估**（把"本来就在走路"算成被阻止） | 可刷（延长钉住） |
| P2 | `Σ_E cost(E) × min(1, 钉住秒数 / T_ref)` | **需要 `T_ref`（待标定）** | 可刷（延长钉住） |

- **首选 P3 的理由**：**零新常量**（满足【R7】"单常量源"）、**零和**（信用只在既有 `V` 注销时发生）、
  且**不可刷**（延长钉住不增加信用）。P1/P2 必须**先过 P3 无信号**才启用，且启用时 `T_ref` 的标定程序见 §5。
- **P3 的已知代价（如实记）**：它**只承认"钉住→打死"的路径**，对"钉住→拖住但没打死"（纯节奏收益）
  **仍然为 0** ⇒ 若 S2 显示这类局面占比很低，P3 可能就够了；占比高则必须上 P1/P2。

### 2.3 分数化（"有足够价值才加分"）

```
score = max(0, Trade_me − θ)          # 平滑 hinge（不用硬阈值 ⇒ 不造悬崖）
```
`θ = 1.0` 圣水，**且必须登记为"有原则的选择"而非实测标定**（圣水是游戏自己的单位，"差 1 费才算一次有效交换"）。
⇒ 配 **敏感性臂 `θ ∈ {0, 1, 2}`**（§8），若结论在 θ 上翻转 ⇒ 判"阈值是刀口"、**不判决**。

---

## §3 溯源路由规格（**用户第四问的答案**）

1. **绝不估值**：`V_product ≡ 0`（沿用既有约定）。**理由见 §4。**
2. **产物体造成的每一项"已有"账目变动**——塔伤、我方单位**死亡注销**、我方单位**掉血**、
   `tower_term` 的 P3 部分——**记到它的 `root_cast` 桶**上。
3. **`root_cast` 必须是"出牌事件"，不是实体**：Graveyard 是**法术、没有召唤者实体**（实测池中 10 局），
   `SuperWitch`/`NightWitch` 是**单位生单位** ⇒ 溯源要**链式取根**（子→父→…→那次出牌）。
4. ★ **溯源指针写在产物体自己身上**（`uid` 存活于实体上），**不能靠"查召唤者"**（召唤者可能已死）。
   ⇒ **账的归属与局面边界解耦** ⇒ 「最近一次/最近两次」的批次计数**不再必要**（评审 §9.5）。
5. **桶独立结算**：每个 `root_cast` 一个桶；局面结算时把窗口内的路由项累加到桶；
   **不回头改写已关闭的局面的账**（避免回溯改写）。
6. **路由层级 = Tier-1（按局面成员 + 溯源）**，**不做 Tier-2 精确归因**
   （Tier-2 需在那 35 处无 `source` 的伤害调用点补 `source` ⇒ 撞上 **Ronin 格挡会开始吞法术**的语义陷阱）。

---

## §4 ⛔ 免费产物**不得估值**（两条否决证明，写死备查）

| 做法 | 后果 | 判决 |
|---|---|---|
| **给产物体估值** `w>0` | Tombstone `~2.9 s/个`、不被打是整局 ⇒ **60+ 个** ⇒ 一个 **3 费建筑凭空产生 60 费价值** ⇒ **Φ 无界**；`value_norm="running"` + `adv_norm="scale"` 会把整条奖励**尺度**绑到该项 ⇒ 学"下小屋别防守" | **货币泵 ⇒ 否决** |
| 死亡时**零和转移**（杀死者 `+w`、召唤者 `−w`） | 不是货币泵，但杀死者**零成本**拿 `+w` ⇒ 清 60 个免费骷髅 = `+60w`（**一个圣水没花**）⇒ **可无限刷分** | **否决** |

⇒ **唯一允许的形式 = §3 的溯源路由**（战斗价值路由给召唤者，产物体本身始终 0 费）。

---

## §5 判据（**跑前写死**，含失败分支）

### 5.1 主判据：机制层（不用胜率，【R5】）

| 层 | 指标 | 仪器 | 对照（脚本复算，禁手抄【R4】） |
|---|---|---|---|
| **机制（主）** | 批内 EV（**更新前池化**）、`resid_norm`、`grad_cos`、`EVb` 形状 | `diagnose_every` 日志 + `scripts/judge_critic_inertia.py` | **本实验 run 自己前 100 次诊断更新**的中位 ± 3×MAD |
| **行为（可复算）** | 全帧圣水 ≥6 占比、Xbow 出手率、部署前圣水中位、同路间隔 | `scripts/forensics_card_usage.py`（只读） | `long1m` 起点 / 本实验阴性对照 |
| **★ 门禁（新增）** | **优势兑现率**：`Trade ≥ θ` 后的 **N=20 s** 内，我方累计部署花费 ≥ `Trade` **或** 塔伤 > 0 的比例 | 新仪器（回放 + 部署事件） | 本实验**同批阴性对照**；`N=20 s` 登记为**有原则的选择** |

**阈值标定程序（写死的是程序，不是数字）**：阈值取**本实验同一 run** 的前 100 次诊断更新作对照池，
脚本复算中位 ± 3×MAD；**禁止**沿用其它实验阈值（【R15】）、**禁止**单次观测标定（【R16】）、
分子分母必须同超参（【R17】）。
> **有意偏差登记**：本项目**没有**"结算口径改动"的历史散布读数可作余量依据 ⇒ 此刻写任何数字都是单次观测。
> 与 `reward_settlement_prereg_2026-09-14.md` §3 同样处理：**只写程序，不写数字**。

### 5.2 对照臂（缺一不可）

- **阳性对照**：`elixir_diff_weight: 0.5 → 0.0`（已知会改写结构层与部分行为读数）。
  对照**不过** ⇒ 判 **`INVALID`（不判决）**，先修仪器/流程，**禁止**解释主判据。
- **阴性对照**：**同配置重跑**（不同 seed）——量出"无改动时的散布"，是判"有变化"的前提（【R5】）。

### 5.3 失败分支（写死）

1. 阳性对照不过 ⇒ `INVALID`，不解释主判据。
2. 机制层改善但**行为层三项全无改善** ⇒ 判「**未达行为可见阈值**」，**不得**写成"修好了"（【R11】）。
3. **门禁不过**（兑现率显著低于阴性对照）⇒ 判「**买到只守不推**」⇒ **不接受该改动**（圣水上限 10 ⇒ 只守不推可攒交易打平）。
4. 结论在 **θ 敏感性臂**（§8）上翻转 ⇒ **不判决**。

---

## §6 引擎侧改动清单（**纯记录，默认关时逐位回旧**）

| # | 改动 | 位置 | 行为中立性 |
|---|---|---|---|
| 1 | 实体新增 `uid`（唯一、单调、跨局重置） | `battle.py` 实体基类（已有 `self.id` 但**未进快照**） | 纯新增字段 |
| 2 | 新增 `root_cast`（出牌事件 id）+ `is_product`；spawn 时链式继承 | 建筑/单位/法术生成产物的全部路径 | 纯新增字段 |
| 3 | 新增**独立记录通道**（**绝不**给 `take_damage` 补 `source`） | 见评审 §3 P3：`source` 会喂 `on_take_damage` ⇒ **Ronin 会开始吞法术** | 必须独立通道 |
| 4 | 快照带上 `uid`/`root_cast`（`replay.py` schema 升版 3→4） | `rl/replay.py:85-90` 的 10 元组 | 只对新 run 生效 |
| 5 | 奖励项 `engagement_trade`（**默认关**，`TrainConfig` 显式开） | `rl/env_wrapper.py` + `rl/config.py` | 【R2】默认关时逐位回旧 |

**★ 开/关对账 battery（提交前置条件）**：同一 seed，`engagement_trade` 开/关两跑，
**奖励序列、胜负、掩码位图逐位相同**（【R13】精神 + 【R8】跨局边界）。

---

## §7 不变量与回归测试（先写测试名，再写实现）

| # | 不变量 | 测试名（`rl/selftest.py`） |
|---|---|---|
| 1 | **反对称/零和**：同一局同一局面 `trade_p0 == −trade_p1` | `test_engagement_trade_antisymmetry` |
| 2 | **桶守恒**：`Σ(各 root_cast 桶) == 该局全部路由项之和`（不漏不重） | `test_root_cast_bucket_conservation` |
| 3 | **产物体恒 0 费**：任何产物体出现/死亡都不改变 `V` | `test_product_zero_value` |
| 4 | **窗口等式**：`Trade_me == ΔΦ_window + tower_term`（同轨配对复算） | `test_trade_equals_phi_window` |
| 5 | **局面切分**：手工构造 6 个场景（孤零零小屋 / 小屋+最近批 / 该批 vs 我方弓手 / 单只免费骷髅打塔 / 双路同时开战 / 目标瞬态死掉）逐一断言分段与结算时刻 | `test_engagement_segmentation` |
| 6 | **跨局边界**：账本/桶在局边界完全重置（9j 事故类） | `test_trade_reset_across_episodes` |
| 7 | **默认关逐位回旧** | `test_engagement_trade_default_off` |

---

## §8 实验设计（【R3】单变量）

- **模式 = `solo`**（**必须**）：`run` 模式的 5 个对手是 **mask-随机脚本**，
  既不"应对"我们的推进（对方花费那一半是噪声），也**不组织一次皮卡推进** ⇒ **拉扯没有可学样本**（评审 §7.4 实测）。
- **唯一变量**：`engagement_trade.enable`（on/off）；`θ` 敏感性臂另跑 `{0,1,2}`（每臂 1 跑，只作敏感性）。
- **协议**：标准 20k（§2.2 命令原文照抄），`--eval-workers 12`，`--fresh`。
- **臂**：`A_ctrl`（阴性对照，同配重跑）×2 seed、`B_trade`（干预）×2 seed、`C_pos`（阳性对照：`elixir_diff_weight→0`）×1。
- **成本**：5 跑 × ~47 min ≈ **4 h**（20k 协议评估占 73% 墙钟）。

---

## §9 实施顺序（三步，前两步零训练成本）

| 步 | 做什么 | 成本 | 出口判据 |
|---|---|---|---|
| **S1** | **零成本门禁**（`reward_settlement_prereg` §5，**至今未跑**）：`BeliefPlanner` + PlanToken 贪心执行器，同牌同批局量行为三项 | **0** | 手写专家能做到"攒费→打 Xbow"（全帧圣水≥6 ≫ **0.11%**、Xbow ≫ **0.03%**）⇒ 约束在**结算侧**，继续；做不到 ⇒ 约束在**探索/机制侧**，**S3 不做** |
| **S2** | **仪器化**（§6 的 1–4 + §7 的 5/6 测试 + 离线归因脚本）：在 `runs/long1m` 的 **18 个现成录像**上先算 | **0**（默认关） | ①量出 `Trade` 的**方差地板**与分布式；②**选 `tower_term` 代理**（P3 → P1 → P2，按 §2.2 顺序）；③量"钉住但没打死"的局面占比；④验证 `Trade` 与**非自引用**结果（皇冠/局终塔血）的相关性 |
| **S3** | **仅当 S1 判"在结算侧"且 S2 显示相关性**：§8 的 A/B | ≈4 h | §5 三层判据 + 门禁 + θ 敏感性 |

**不许跳过 S2 直接做 S3**（【R11】：不在取证之前改奖励）；**S2 未出相关性别开 S3**。

---

## §10 局限与未确认（预先声明）

1. **`tower_term` 没有精确实现**：P1/P2/P3 全是代理，**真反事实未实现**（43.3 ms/次推演 ⇒ 训练不可用）。
2. **`K=30 tick` 与 `θ=1.0`、`N=20 s` 三个数字是"有原则的选择"，不是实测标定** —— 已在 §1/§2.3/§5.1 分别登记；
   按【R15】【R16】，它们**不得**被当作已标定阈值引用。
3. **`Trade ≈ ΔΦ` 的等价性由 §2.1 的代数推导给出，未做数值对账** ⇒ S2 必须补 `test_trade_equals_phi_window`。
4. **缺口规模未知**：本文件据"Φ 对冰人白嫖记 −1.0"（读码推论，未实测）立论 ⇒ S2 应顺便在回放上**逐帧算 Φ**验证。
5. **对手侧**：`solo` 对手是冻结副本/hist（**目标导向**），但**自引用失真**（§3 判读禁则）⇒
   **不得**用 `main vs 冻结副本` 的胜率当判据；判据只用机制层 + 行为层 + 门禁。
6. **`runs/long1m` 的 18 个录像**：来自 `run` 模式（对手是脚本）⇒ 可用于**仪器标定**，
   **不可**用于判"拉扯有没有可学样本"（那要靠 solo）。

---

## §11 S2 实施记录 · 第一步（2026-09-18，已落地）

> **范围声明（【R3】/【R18】）**：本节只记录**已完成并验证**的那一步。
> **未做**：`root_cast` 溯源（需给 `_spawn_entity` 的约 20 个调用点串宿主）、奖励项本身、离线仪器脚本。
> **未跑任何训练**（本步对训练行为**零影响**）。

### 11.1 改了什么

`src/clasher_new/rl/replay.py`：

| 项 | 改前 | 改后 |
|---|---|---|
| `LEAGUE_REPLAY_SCHEMA` | `3` | **`4`** |
| 实体条目 | 10 元组 `[name,x,y,hp,player,kind,max_hp,shield,shield_max,radius]` | **12 元组**（**末尾追加** `id`、`target_id`） |

**为什么不需要新造 `uid`**：`battle_state.next_entity_id` **本就是每局单调唯一**的（实体一直有 `.id`，只是**没进快照**）。
所以本步是"把已有的身份写出去"，不是新增身份系统 —— 比预注册 §6 第 1 项乐观（该项可删）。

**`target_id` 是意外收获**：它让**索敌关系图可以直接从回放重建** ⇒ **离线局面划分不需要重推演**。

**向后兼容**：新字段追加在**末尾**，所有按下标读 `0..9` 的消费者不受影响
（`dashboard.py` 前端、`scripts/forensics_card_usage.py`、`rl/train_solo.py::behavioral_metrics`）；
`rl/prophet.py` 用的是另一套 dict 格式（`e["player"]`/`e["pos"]`），与本元组无关。

### 11.2 验证

**（a）回归（【R19】子集）** —— 6/6 PASS：

```
cd src/clasher_new && PYTHONIOENCODING=utf-8 ../../.venv/Scripts/python.exe \
  ../../scripts/run_selftests.py test_league_replays test_dashboard_replays \
  test_dashboard_league_payload test_dashboard_card_stats test_behavioral_metrics test_replay_roundtrip
```

**（b）实跑产出 schema-4 录像**（短 smoke，命令见下），在其上复算：

| 检查 | 结果 |
|---|---|
| `schema` | **4** ✅ |
| 实体元组长度 | **12** ✅ |
| **帧内 `id` 不唯一** | **0**（= 身份唯一）✅ |
| 有 `target_id` 的条目 | **76.3%**（177,562 / 232,607）✅ 仇恨图可用 |
| 每帧重复实体是否可分辨 | ✅ 例：同帧两个 `King_PrincessTowers(player=1)` 的 `id` = **1 / 2** |

```
cd src/clasher_new && PYTHONIOENCODING=utf-8 ../../.venv/Scripts/python.exe -u rl/run_league.py \
  --mode run --config-name run_schema4 --fresh --total-steps 600 --steps-per-eval 300 \
  --n-eval-games 4 --eval-workers 2 --device cuda
# 注：本次只跑到 eval@0 落盘（60 局 / 18,705 帧）就被 10 min 超时掐断，未跑满 600 步；
#     产物 runs/run_schema4/replays/league_0.pkl（schema 4）保留作参照。
```

### 11.3 ⚠️ 口径细化（**结论不变，数字要记准**）

评审 §3 P4 / §6.5 引用的"**8584/9079 帧（94.6%）存在重复 `(name,player)`**"**数字正确**，
但**载重的口径是单位级**，不是全实体级 —— 因为**每个公主塔恒有 2 个同名实体**（`King_PrincessTowers` ×2/方），
把所有实体都算进去会**高估**歧义率：

| 口径 | `long1m/88000`（schema 3，9,079 帧） | `run_schema4/0`（schema 4，18,705 帧） |
|---|---|---|
| 含重复 name（**含塔**） | 8,584 帧（94.5%） | 18,259 帧（97.6%） |
| **含重复单位（`kind=='troop'`）** | **5,596 帧（61.6%）**，多余单位 **11,517** | 13,396 帧（71.6%），多余单位 35,968 |

⇒ **评审的结论不变**：schema ≤3 下"两只 Minions / 三只 Bats / 一堆骷髅不可区分"⇒ **精确归因不可能**；
**但引用该论据时应使用 61.6%（单位级）**，不要用 94.5%。本步（schema 4）同时消除两种歧义。

### 11.4 下一步（S2 剩余）

1. **`root_cast` 溯源**：给 `BattleState._spawn_entity` 加可选 `spawner`，在**约 20 个调用点**传入宿主
   （多数调用点就在宿主自身方法里 ⇒ `spawner=self`），并**链式取根到出牌事件**；
   法术产物（Graveyard）的根 = 施放事件而非实体。**必须配 §7 的测试 2/3/6**。
2. **离线局面/交换仪器**：用 `id` + `target_id` 在回放上做并查集分段（§1 定义）、
   算 `Trade = ΔΦ_window + tower_term`、跑 §7 的测试 1/4/5。
3. 之后才谈奖励项接线（§6 第 5 项，**默认关**）与 §8 的 A/B。

---

## §11.5 S2 实施记录 · 第二步 + ★ 门禁判读（2026-09-18，已落地）

> **范围**：本节只记录**已完成并验证**的部分。**未做**：`root_cast` 纯记录通道、schema 5、
> §7-7 默认关逐位对账、优势兑现率门禁（N=20 s）、奖励项接线。**未跑任何训练。**

**全文（读数 / 5 批扫描 / 5 个口径缺陷 / 门禁判定）**：
[`s2_instrument_2026-09-18.md`](s2_instrument_2026-09-18.md)

### 11.5.1 交付物
- `scripts/offline_engagement_trade.py` —— 离线仪器（只读回放，支持 schema 4 **与** 5，
  9 种 `tower_term` 代理、`component`/`global` 两种 ΔΦ 口径、`core`/`members` 两种身份判据）
- `scripts/selftest_offline_engagement_trade.py` —— **§7 的 1–6 条，6/6 PASS**（合成帧，零引擎依赖）
- `docs/s2_instrument_2026-09-18.md` —— 读数与判读

### 11.5.2 ★ 对被本文件 §2.2 写死的"P3 首选"的推翻
| 代理 | 实测判决 | 依据（5 个批次，非自引用结果） |
|---|---|---|
| **P3a/P3b（本文件首选）** | **推翻** | `τ≠0` 仅 7%–11%；`τ ≤ kill_credit` **恒成立**、`τ == kill_credit` 占 77%–89% ⇒ **已有 Φ 击杀项的重新加权**，不是新信息；ρ(塔血) 5 批**不同向** |
| P1 | **否决** | `τ` 均值 388–430（`ΔΦ` 量级 ±5 的 ~80 倍）；ρ(塔血)/ρ(胜) **5/5 批为负** |
| P2 | **否决** | 同量纲缩放后 ρ 符号批次间乱跳 |
| **P4b（新构造）** | **唯一候选** | 塔血零掉血门控 × `min(1, 钉住秒/T_ref)`；ρ(塔血) 与 ρ(胜) **5/5 批同向**；但 5 批均值差（0.14）**< 批间 sd（0.16）** ⇒ **未超散布（R16）** |

### 11.5.3 缺口实测（本文件 §0.1 的"下半格"）
`league_0` 100 局：**20.6%** 的窗口是「被非塔实体钉住但**没打死**」（2235 个），
其 `Trade` **中位 0 / 均值 −0.43 / p25 −1.00** ⇒ **"正解被记为负"成立**（量级小于 §0.1 举的单个例子）。

### 11.5.4 `K` 不可辨识（对本文件 §1 的补充）
`K ∈ {1,2,3,5,10}` 的读数**逐位相同**（窗口/局 108.6→108.5，σ(Trade) 2.09→2.10，ρ 完全一致）
⇒ **"连续 K tick 无交战对"这条结算规则在本回放分辨率下不可辨识**；
窗口实际是因为**交战方全死/分量清空**而结束（79% `split_or_vanish`）。
`K=30 tick` 仍按"有原则的选择"保留，但**任何"去抖有效"的论证在本数据上是空的**。

### 11.5.5 本文件需要更正的条目（只增不改）
| 本文件原文 | 更正 |
|---|---|
| §6 第 1 项"实体新增 `uid`" | `next_entity_id` 本就唯一；该项可删（§11.1 已记） |
| §6 第 4 项"schema **3→4**" | schema 4 已被 `id`+`target_id` 占用；`root_cast`/`is_product`/`share`/`v0`/`v1` 需 **4→5**（清单见判读文档 §9） |
| §10.6 / §11.4 "在 `long1m` 的 18 个录像上先算" | ❌ **不可执行**：那 18 个录像**全是 schema 3** ⇒ 改用 `run100k` 的 schema-4 录像 |
| §2.2 "P3 首选" | **被 §11.5.2 推翻**（本节为准，原条目保留） |
| §5.1 "优势兑现率门禁" | **尚未实现**（未做完项） |

### 11.5.6 门禁判定（本文件 §9："S2 未出相关性别开 S3"）
> **判：有候选（P4b）、无判决。** S3 **不得按原样开跑**（其 `tower_term` 用的是已被推翻的 P3）；
> 若开，唯一允许的干预臂是 `p4b`，且必须**同批**带阳性/阴性对照与 θ 敏感性臂，
> 阈值按 §5.1 的程序在本实验自己的量纲上标定（【R15】【R16】），且**必须挂 `solo`**（【R11】）。

---

## §11.6 S1 零成本门禁 · 实测与对 §9 的收缩（2026-09-18，已落地）

**全文**：[`s1_gate_2026-09-18.md`](s1_gate_2026-09-18.md)；仪器 `scripts/s1_plan_gate.py` +
`scripts/phi_offline_check.py`（均**新建**，未改任何既有文件）。

按本文件 §9 的 S1 行（"手写专家能做到『攒费→打 Xbow』⇒ 约束在结算侧；做不到 ⇒ 约束在探索/机制侧，S3 不做"）实测：

| 臂 | 全帧圣水≥6 | Xbow 出手率 | 部署前圣水中位 |
|---|---|---|---|
| `random`（主基线，同脚本同批复算） | 0/9256 = 0 | 0/1104 = 0 | 3.2857 |
| **`token_strict`**（§9 指定的 PlanToken 仪器） | **0/8191 = 0** | **0/929 = 0**（95% 上界 0.32%） | 3.2857 |
| `token_xbow`（能力上限探针） | 48/9110 = 0.53%（4.79×） | **47/1003 = 4.686%（156×）** | 3.4226 |

**判决：未通过（不显著 ≫ 基线）⇒ 按 §9 第二行：约束主要在探索/机制侧 ⇒ S3 不做、结算改动暂停。**

**能力上限已交代**（不得把失败等同于"手写专家做不到"）：执行器无多卡协同、无 `target_kind`、无 Xbow 战术；
机制自锁已被脚本计数证实——Xbow 不在 `ACE_CARDS`/`SINK_TANK_CARDS` ⇒ 唯一会置 `hold_mask` 的
`save_ace`/`setup_wait` 在 Xbow 卡组上 **0 帧触发**，而 `_pick_suggested_card` 只推荐付得起的牌
⇒ Xbow（6 费）永远不被推荐。`token_xbow` 只多加一条**不在 PlanToken 语义里**的手写规则即打出 156× 基线
⇒ **动作空间/掩码被证伪为约束点**。

### 11.6.1 冰人白嫖推论：**证实**（带相位限定）
本文件 §10.4 登记的"读码推论，未实测"现已数值验证：三个数据集共 **102/102** 个真实
`IceGolemite` 实例端到端 `ΔΦ = (-cost + 部署份额) + (-死亡注销) = −2.000000`（min = max，零散布）。
`edw` 项：死亡在 **t < 120 s（`edw=0.5`）⇒ 恰好 −1.0**；双倍期（`edw=0.1`）⇒ **−0.2**。
⇒ 原推论**成立**，但**必须补前提 `t < 120 s`**。（"冰人打不出伤害"这一前提本步未验证，与本文件 §2 的 Φ 记账无关。）
reward 对账：schema 4 非末帧严格相等 70.4%、`<0.01` 92.8%；**"无塔伤/无单位伤"帧严格相等 98.9%**
⇒ 严格相等的缺口主因是**回放把 hp `round(…,1)`**。

### 11.6.2 ★ 两条门禁的合并结论（对 §9 的最终收缩）
| 门禁 | 判定 | 后果 |
|---|---|---|
| **S1**（§9 第 1 行） | **未通过** ⇒ 约束在**探索/机制侧** | 先修机制；**结算改动暂停** |
| **S2**（§9 第 2 行 + §11.5） | **有候选、无判决** | **S3 不得按原样开跑**（其 `tower_term` 用已被推翻的 P3） |

⇒ **本预注册的 S3（§8 的 solo A/B）整体不开**。要重启这条路，前置条件是：
① 先修 §11.6 指出的**机制自锁**（`ACE_CARDS`/`SINK_TANK_CARDS` 名单 + "只推荐付得起的牌"），
② 再做 `p4b` 单变量 A/B（同批带阳性/阴性对照与 θ 敏感性臂，阈值按 §5.1 程序标定），
③ 实验**必须挂 `solo`**（§8）。

---

## §11.7 S3 修订预注册（**跑前写死**，替换 §8 的干预臂定义；2026-09-18）

> **状态：未开跑，也不应在机制自锁修好之前开跑**（§11.6.2）。本节的作用是：
> 一旦人类拍板重启，**判据在跑之前就已经写死**（【R3】），不需要临场决定任何阈值或臂。

### 11.7.1 唯一允许的干预臂（替换 §8 的 `B_trade`）
§8 原本写的是"`tower_term` = P3"。**P3 已被 §11.5.2 推翻**。修订：

| 臂 | 配置 | 作用 |
|---|---|---|
| **`A_ctrl`**（阴性对照）×2 seed | `engagement_trade.enable = false`（**默认关，逐位回旧**） | 量"无改动时的散布"（【R5】） |
| **`B_p4b`**（干预）×2 seed | `engagement_trade.enable = true`、`tower_mode = "p4b"`、`theta = 1.0`、`t_ref = 2.0` | 唯一候选 |
| **`C_pos`**（阳性对照）×1 | `elixir_diff_weight: 0.5 → 0.0`（**必须改写结构层读数**） | 对照不过 ⇒ **`INVALID`，不判决**（§5.2） |
| `D_theta`（敏感性）×3 | `theta ∈ {0, 1, 2}`（各 1 跑） | 结论在 θ 上翻转 ⇒ **不判决**（§2.3/§5.3） |

**P4b 的精确定义（写死，实现必须逐字对齐）**：
```
τ = Σ_{E: 敌方实体，且 E 在窗口内至少一帧的跨边对手中存在非塔实体(id>6)} min(1, 钉住秒数(E)/T_ref)
    × [窗口内我方三塔总掉血 == 0]        # 门控；否则 τ = 0
tower_term = [+(τ_me − τ_opp)/2, +(τ_opp − τ_me)/2]   # 零和且两侧对称（§11.5 缺陷 7.3）
score = max(0, Trade_me − θ)
```

### 11.7.2 判据（不变，见 §5.1；此处只补"用 P4b 后各格如何读"）
- **主判据（机制层）**：批内 EV（更新前池化）、`resid_norm`、`grad_cos`，对照池 = **本实验同一 run 前 100 次诊断更新**的中位 ± 3×MAD（§5.1 的程序，**不写数字**）。
- **行为层**：全帧圣水 ≥6 占比 / Xbow 出手率 / 部署前圣水中位 —— 脚本 `scripts/forensics_card_usage.py` 或 `scripts/s1_plan_gate.py --replay-dir`，**对照 = 本实验的 `A_ctrl`**（【R4】禁止手抄）。
- **门禁**：优势兑现率（`Trade ≥ θ` 后 20 s 内我方累计部署花费 ≥ `Trade` **或** 塔伤 > 0 的比例）**必须新实现**（§11.4 未做完项）。
- **新增通过条件（来自 §11.5.4）**：`K` **不得**被当作已标定参数引用；若任何结论只依赖 `K` 的取值 ⇒ **不判决**。

### 11.7.3 失败分支（在 §5.3 之上再补两条）
5. **机制前置未修**（§11.6 的 `ACE_CARDS`/`SINK_TANK_CARDS` 自锁仍在）⇒ **不开跑**，先修机制。
6. **对照臂 `A_ctrl` 的两次 seed 之间**在行为层三项上的差 **不小于** `B_p4b` 与 `A_ctrl` 的差 ⇒ **分辨率不足，不判决**（【R16】的配对版）。

### 11.7.4 成本（照 §8，未变）
标准 20k 协议（`env.md` §2.2 命令原文），`--eval-workers 12`，`--fresh`；**模式必须 `solo`**；
5 跑 × ~47 min ≈ **4 h**（20k 协议评估占 73% 墙钟）。
