# 经验台账（冻结版）：结论 / 推翻 / 病理分类 / 检测协议

> **用途**：把本项目到 2026-09-13 为止的**全部结论性经验**压成一份可引用的索引，作为论文的
> Table 1 + 附录底稿，也作为跨会话的"别再重走"清单。
> **生成规则（R4）**：本文件**不引入任何新数字**。所有数字引用自各自证据文件，写明路径；
> 带 **★** 的数字有脚本可复算（脚本名在"证据"列）。本文件是**索引与分类**，不是二次测量。
> **上游**：[`AGENTS.md`](../AGENTS.md)（红线 R / 确证 C / 否证 X / 未决 O 的正式条目）、
> [`docs/plan_master.md`](plan_master.md)（计划整合视图）。
> **状态**：冻结于 2026-09-13 夜；此后新增经验**追加**，已被推翻的条目**保留并标注**（不改写历史）。

---

## 1. 被推翻 / 被挡住的结论（14 条）—— 项目最核心的一类产出

每行的读法：**当时我们相信什么 → 用什么读数相信的 → 读数为什么说谎 → 哪个测试抓住了它**。

| # | 当时的结论 | 当时的读数 | 病理类型 | 抓住它的测试 | 证据 |
|---|---|---|---|---|---|
| **X1** | F′ 架构让 critic 训练期 EV 到 **+0.198**（认为修好了） | 训练期 EV（末轮、in-sample） | ① in-sample 口径 | 换成**更新前**口径 → **+0.031**（降 **6.4×**）；同权重离线 50 局 `EV_global` 为负 | `fprime_rerun_20k_verdict_2026-09-13.md` ★ |
| **X2** | 表征含 24~42% 可预测信息、critic 吸收不了 | 探针 R² | ② **时间泄漏**（逐帧随机留出） | 改**按局分组留出** → R² 全负（MLP −0.36/−0.45）；相邻帧 `corr(R)≈0.99` | `critic_probe_experiment_2026-09-12.md` ★ |
| **X3** | 价值头架构四代（G'-fix / B′ / E′ / 重量级 bypass）能救 critic | 训练期 EV、离线 EV | 病症在训练过程，不在架构 | 四代 EV 全 ≤0；G'-fix 的 `enc→塔血差 +0.69` 只证明**通道接上了** | `train_{byp_cprime,eind,fprime,fprime_ev}_20k.log` ★ |
| **X4** | 加训练量能救 critic | 短 run 的上升趋势 | ③ 单 run 曲线外推 | 100k：EV 9/11 点 ≈0 或负、`vstd/rstd` 30k 后 ≈0.001 | `d1_long_100k_verdict_2026-09-13.md` ★ |
| **X5** | 训练池放 10% 固定随机锚点能破 cycling | 与 `baseline_rand` 的对照 | 剂量不足 | 20k 末点 vs `baseline0` **0.075**、vs `baseline_rand` **0.050**，RPS 仍在转 | `train_e2_rand_anchor_20k.log` |
| **X6** | critic 侧程序"无进展"⇒ 关闭该线（用户预授权） | 三步程序全"无进展" | ④ **自引用/退化锚点**（见 X-13） | **2026-09-13 复核**：该程序的锚点 run 末点 critic **是常数** ⇒ 依据需重审 | `critic_inertia_verdict_2026-09-13.md` §0.1 ④ ★ |
| **X7** | 两条 20k 预注册判据有区分力 | **手抄**的基线列（0.400 / 1/3） | ⑤ 基线不可信 | 脚本复算：实际 **0.588** / **2/3** ⇒ 判据失去区分力，立 **R4** | `docs/rl_training_fix_plan_v3.md` ★ `judge_anchor_blocks.py --groups` |
| **X8** | early-stop 能降噪 28~40% | 混入了 eval 侧统计 | ⑥ 口径混装 | 训练侧实测早停率 **3%** | `training_audit_2026-09-11.md` |
| **X9** | `gates.json` 的绝对阈值判据可用 | 一次性脚本给的阈值 **9.5** | ⑤ 阈值无标定 | 同批权重实测 **28.3~38.1**（差 3×）⇒ 改**相对本 run 首点** | `rl_training_fix_plan_v2.md` |
| **X10** | 每个评估点重打同样 40 局 ⇒ 锚点是权重的确定性函数 | `RAND_ANCHOR_EVAL_SEED=90000` | ⑦ **随机链状态**（`reset` 原地链式洗牌） | 11 个评估点 **11 种**不同首局牌序 ⇒ 相邻点差分 SE 应为 **√2σ=0.110**，原"3σ"读作 **≈2.05σ** | `d1_long_100k_cause_analysis_2026-09-13.md` §2.4 ★ |
| **X-11** | 「修 critic 的收益上界 = 让更新方向偏移几个百分点」 | `grad_cos` 中位 **0.9973**、`grad_norm_ratio` 0.9936 | ⑧ **判据是恒等式**：任意状态函数基线都是同一条策略梯度的**无偏**估计 ⇒ cos≈1 是**期望零假设** | 无偏性证明 + 本 run `adv_norm="scale"` 把模长通道归一化掉 | `critic_inertia_verdict_2026-09-13.md` §0.1 ① |
| **X-12** | 「该 run 的 critic 并没有塌（EV +0.30）」 | 窗口**池化** EV **+0.3003** | ⑨ **跨相位池化**（该 run 只有 2 个评估点，`ev_pairs` 覆盖整段；早期活相位与晚期死相位被平均） | **逐批** `EVb` **145/145 为负**（中位 −3.149、最大 −0.05）；批内近似常数预测器**数学上必然** `EVb ≤ 0` | `critic_inertia_verdict_2026-09-13.md` §0.1 ②③ ★ |
| **X-13** | 「惰性检验 P1 成立 ⇒ critic 惰性 ⇒ 关闭 critic 线」 | `resid_norm` 中位 **0.0415**、`grad_cos` 0.9973 | ⑧ **恒等式**：**V ≡ 常数**时 `adv_real` 与 `adv_const` 只差一个常数 ⇒ `resid≈0`/`cos≈1` | 独立复核：`value` **唯一值 1/600**（= `value_head_mlp[2].bias` = **+0.1425485**，`std=0`）、`MLP0` ReLU **逐帧存活率 0.000000** | `critic_inertia_verdict_2026-09-13.md` §0.1 ④；`_probe_value_path.py` ★ |
| **X-14** | 信息侧天花板首跑的读数：`EV_within` = **−62.8 / −94.1 / −56.5 / −57.4**（"状态完全预测不了局内回报"） | 在**整局留出**上的监督拟合估值 | ⑩ **估计器可控范围不足**：岭回归 alpha 网格上限只有 10（对 d=8648、n≈2e4 ≈ 0.05% 正则）、无输出裁剪、未做保信息降维 | **冒烟自查**（未采信）：放宽网格到 1e-1…1e8 + 输出裁剪 + 丢弃训练集零方差列（8648→5005）⇒ **alpha 最优点从网格边界 10 移到内部 1e4~1e5、`val_EV` 转正**；并新增**闸门 G4**：所有模型 val_EV ≤ 0 ⇒ `verdict = INVALID_ESTIMATOR`（**否则这批数字会照预注册触发 P2"信息不是瓶颈"——一个由发散模型制造的假结论**） | `pomdp_ceiling_prereg_2026-09-13.md` §7（跑前修订 3）；`pomdp_ceiling_probe_stoch_INVALID_v1.log` |

**读这张表的正确方式**：**14 条里 11 条是"读数 / 仪器 / 估计器"制造的**
（X1 口径、X2 泄漏、X6 自引用、X7 手抄基线、X8 口径混装、X9 阈值无标定、X10 随机链、
X-11/X-13 恒等式判据、X-12 跨相位池化、X-14 估计器范围），
**只有 3 条**（X3 架构四代、X4 加量、X5 弱锚点）是对**训练问题本身**的实质结论。
⇒ **在这个项目里，最贵的成本不是算力，是"相信了一个不该信的读数"。**

**并注意 X-14 的性质与前 13 条不同**：它是**唯一一条"被纪律当场挡住"而不是"骗过我们"**的——
发散拟合会照预注册触发 P2（"信息不是瓶颈"），是自查 + **新增闸门 G4** 把它拦下来的。
⇒ 这说明**闸门是可加的**：每被咬一次，就补一道闸门，而闸门本身可以复用到别的实验
（G4 现在已写进 `pomdp_ceiling_prereg` §3）。

---

## 2. 病理分类学（10 类）与最小检测手段

| 类 | 病理 | 我们的实例 | 最小检测手段（可抄） |
|---|---|---|---|
| ① | **in-sample 口径**（读过拟合，不是读能力） | X1 | 一切"预测质量"必须在**参数更新前**、且在**独立 rollout**上读 |
| ② | **时间泄漏**（逐帧随机留出） | X2 | 留出必须**按局分组**；先报 `corr(R_t, R_{t+1})` |
| ③ | **单 run 曲线外推** | X4 | 结论必须写进**预注册判据**；跨 run 数字默认不可比（R5） |
| ④ | **自引用仪器**（打自己/打刚同步的副本） | X6、X-13 | 只用**外生绝对仪器**（固定随机锚点）；自引用指标一律不作进步证据 |
| ⑤ | **基线不可信 / 阈值无标定** | X7、X9 | 基线**脚本复算**；阈值改为**相对本 run 首点** |
| ⑥ | **口径混装**（eval 侧统计当训练侧结论） | X8 | 声明每个数字的**样本集**；比值分子分母同集（R9⑤） |
| ⑦ | **随机链状态**（看似固定，实则比上次多走了一步） | X10 | 固定种子前后**实测首局牌序**是否相同，不靠推理 |
| ⑧ | **判据在退化情形下成为恒等式** | X-11、X-13 | 问"若被测量退化为 X，这个判据会给出什么？"——若答案与"正常情形"相同，判据无效 |
| ⑨ | **跨相位池化**（把活相位与死相位平均成一个漂亮数字） | X-12 | 报告**分布而非均值/池化值**；批内与池化**两个口径都报**，并声明差异来源 |
| ⑩ | **估计器可控范围不足**（超参网格太窄 / 未做保信息降维 / 无输出约束） | X-14 | ① 检查最优超参**是否落在网格边界**——落在边界就是网格不够宽；② 给估计器一个**平凡解下界**（"预测训练均值" = EV 0），保证任何取值都不会比它更差；③ 先丢**训练集内零方差列**（保信息、降 d） |

---

## 3. 已确证（可作为前提引用）

| # | 结论 | 关键读数（引用源） |
|---|---|---|
| **C1** | 负 EV 的根因是 **GRU 输入饱和**（不是奖励尺度、不是价值头结构） | `enc` 范数 **533**（`grid_feat` 常分量 468、跨帧 std 1.06）→ tanh 候选饱和 `n_abs=0.994` → h 跨帧 std **2.6e-5**；`EV −0.5817` 与恒等式 `−bias²/Var(R) = −0.584` 吻合；修复 = `enc_ln`。`value_channel_saturation_diagnosis_2026-09-11.md` ★ |
| **C2** | 饱和修复在 **100k（4×）** 尺度耐久 | 全程 `h_std` 0.067→0.095（>0.05）、`n_abs` 0.667→0.826（<0.9）。`d1_long_100k_verdict` §4 ★ |
| **C3** | 但 **critic 仍不拟合，且加量救不了** | EV 仅前两点为正（+0.234/+0.385）、其后 9/11 ≈0 或负、`vstd/rstd` ≈0.001；20k 基线末点 0.2473/0.0041/0.1648（**注意：塌缩是双稳态**，`_r2` 在 20k 内就死）。同上 ★ |
| **C4** | 自对弈 **cycling 确凿**（RPS 三角） | main 打冻结副本 **0.85** / 打训练起点 **0.13** / 打全新随机 **0.505**。`_forensics_cycling.py` ★ |
| **C5** | **D1**（对手分布去镜像化 + 动态历史自身联赛 + PFSP 门禁）消除"整段输给随机策略"的相位 | 20k 三跑最差锚点 **0.125/0.200/0.250** vs 无干预 **0.000/0.000/0.025**（区间不重叠）；镜像自对弈 40%→6.6%/13%/~13% | 
| **C6** | D1 的防崩在 **4× 训练量**下成立 | 块 worst 0.388/0.475/0.275/0.237/0.525（mean 0.380、min 0.237）；与 20k 区间 [0.125,0.250] **无统计差别**（不退化，也没证明改善） |
| **C7** | 评估节奏不能均匀放宽 | 5000 步即开始漏真谷底；10000 步把"最差点"从 0.192 抬到 **0.462** ⇒ 用"密锚点 + 稀全块" |
| **C8** | critic 的失败是"**状态→回报**"层面的，不是表征没信息 | 按局留出 R² 全负；监督微调同网络 test EV 可达 **+0.22~+0.29** 而 PPO 联合训练 ≤0 ⇒ 结构够用、主因在训练过程（**已被 X6/X-13 限定**） |
| **C9** | 行为链三层有真实爬坡（winrate 平台期下） | 防守响应 38.1%→**61.5%**；接敌 7.4%→**10.5%**；中段带部署 51%→**59.7%**；追尾 64%→**31%**；单边堆牌 45.8%→**9.5%** |
| **C10** | 词表 / 卡池 / 工具层事实 | `ENTITY_NAMES` 13→**177**、belief token 71→**563**、`PLAN_DIM` **58**、塔类过滤唯一正确口径 `n.startswith("King_")` |

---

## 4. 红线（R1–R14）：违反 = 结论作废或事故

> 全文见 [`AGENTS.md`](../AGENTS.md) §1。此处只留"一句话 + 触发它的历史代价"。

| # | 一句话 | 代价 |
|---|---|---|
| R1 | 训练期"性能异常"先归因外部，暂停留证等人类；不为绕开它改代码/降配置 | 曾把未定的性能异常写成"页面文件不够" |
| R2 | `ppo.py` 被四处共用 ⇒ **函数默认参数必须恒等于旧行为** | — |
| R3 | **单变量 + 预注册**（判据跑前写死，含失败分支） | — |
| R4 | 阈值判据的基线列**必须脚本复算** | 同一会话连错三次 |
| R5 | n=1/臂 分辨率不足；同 seed 同代码也**不可复现**（前 435 步逐位一致后 1e-4 漂移） | 一批"单跑看曲线"的小效应结论全部降级 |
| R6 | 架构变更必须 `--fresh`；旧 ckpt 缺 `enc_ln`/`grid_ln` 时**静默**保持新初始化 | — |
| R7 | 尺度改动必须**单常量源** + 对账 selftest | 两次量纲失配（塔伤 ×1000 使空砸王塔被误判正 EV） |
| R8 | 每个修复配回归测试；冒烟必须**跨局边界** | 9j 事故：漏 `nonlocal` ⇒ 首局后缓冲泄漏，30 步冒烟没拦住 |
| R9 | 测量口径纪律五条（决策帧 / 结构性恒等 / 更新前池化 / 按局留出 / 同样本集） | — |
| R10 | **不确定就写不确定**（成因未定 / 样本不足 / 不显著） | 曾把未定异常写成自信的错答案并被后续会话照抄 |
| R11 | 明确不做清单（不扩参 / 不上训练时 MCTS / LLM 不进梯度 / 不重构动作语义…） | — |
| R12 | **EBK：能算的不许让网络猜**（私有信息 + 确定性机制做特征注入） | — |
| R13 | 改判定逻辑（掩码/合法性）必须 **128 张位图逐位对账** | — |
| **R14** | **判 critic 好坏不许只看 EV，必须看「参数有没有在动」** | 见 §5 |

---

## 5. 技术核心：**参数位移指纹**（本文档最可引用的单条产出）

**命题**：在 Adam 下，**"某张量在一个训练窗口内位移恰为 0" ⟺ "该张量梯度精确为 0"**。
理由：Adam 的更新对 loss 的**常数缩放不变**（`m` 与 `√v` 同比缩放）⇒ 任何非零梯度都会给出 ≈`lr` 量级的步长。

**为什么它比 EV/loss 强**：EV 与 loss 对两种完全不同的状态给出**同一个读数 ≈0**：
(a) **梯度小**（加量/调系数可能救），(b) **梯度精确为零**（吸收态，加量永远救不了）。
位移指纹能分开这两者，**且零成本**（直接从 ckpt 序列算 `‖ΔW‖/‖W‖`）。

**实测（100k，41 个 ckpt、27 个窗口）**：
- 策略侧：**27/27** 窗口全在动；
- 价值侧：`value_enc_fc` / `mlp0` / `mlp2.weight` **22~24/27** 窗口**位移恰为 0**，而 `mlp2.bias` **每窗都动** ⇒ **价值函数 = 常数**。

**两条独立的闭合证据（2026-09-13）**：

| 证据 | 读数 |
|---|---|
| 训练批逐批 EV | `EVb` **145/145 为负**（中位 −3.149、**最大 −0.05**）；批内常数预测器**数学上必然** `EVb ≤ 0` |
| 权重直读（独立实现 `_probe_value_path.py`，600 帧） | `value` **唯一值 1/600** = `value_head_mlp[2].bias` = **+0.1425485**（`std=0`）；`MLP0` ReLU **逐帧存活率 0.000000 / 全零帧 1.000** |

**机制（上游诊断）**：价值编码器末端 `LayerNorm` 把近常数输入归一化 ⇒ 价值头可表达的跨帧方差 ≈ **0.14**，
而目标 std ≈ **12.11** ⇒ 差 **60~600 倍** ⇒ 自举失败 ⇒ ReLU **逐个死亡锁死**（**吸收态、双稳态**：
同一协议三次 20k 里 `_r2` 在 20k 之内就死，r1/r3 到 20k 仍活）。

**已排除的替代解释**：不是"梯度小"、不是被 `1/s²` 压制、不是奖励尺度、不是价值头结构（四代架构 EV 全 ≤0）。

### 5.1 跨 run 指纹扫描（2026-09-13 新增，脚本 `scripts/value_displacement_scan.py`）

**on-path** 值头张量 = `value_enc_fc.weight` / `value_enc_ln.weight` / `value_head_mlp.0.weight` /
`value_head_mlp.2.weight`；窗口 = 相邻 `solo_main_<step>.pt`（2500 步），共 8 窗。

| run | 冻结窗口（on-path） | 读数 |
|---|---|---|
| `critic_inert_probe_20k` | 前 5 窗在动；**12500→15000 / 15000→17500 / 17500→20000 全冻结** | 死于 ~12500（62% 训练量处） |
| `d1_league_20k`、`_r3`、`eind_20k`、`fprime_20k` | **8/8 窗口全在动** | 从未冻结 |
| `d1_league_20k_r2` | 12500→15000、15000→17500 冻结，**17500→20000 又动了** | **准吸收态可逃逸** |
| 6 个 `value_independent=True` run 的 `value_head.weight/bias` | **8/8 全冻结** | **不在前向路径**，不是死亡 |
| `byp_cprime_20k`（`bypass=True, independent=False`） | `value_head.*` **8/8 都在动**（0.024 / 0.035） | 反例，印证"先搞清前向路径" |

**由此得到四条新结论（都进论文）**：

1. **两种失败模式必须分开**：
   - **M1 吸收态**：on-path 张量**位移恰为 0** ⇒ 梯度精确为零（`critic_inert_probe_20k` 后 3 窗、100k run 22~24/27 窗）；
   - **M2 活着但拟合不上**：on-path 张量**在动**、EV 仍 ≈0（`d1_league_20k` 8/8 在动、pooled EV **+0.0183**；
     四代架构变体 `eind_20k`/`fprime_20k` 同样全程在动）。
   ⇒ **EV 把 M1 与 M2 读成同一个 ≈0；位移指纹能把它们分开。** 这是检测器的真正价值。
2. **准吸收态可逃逸**：`d1_league_20k_r2` 在两个窗口完全冻结后又恢复非零梯度
   ⇒ 机制叙述中的 "absorbing" 应改为 **"quasi-absorbing / escapable"**（对"死就永远救不回"的说法是反例）。
3. **`value_head.*` 在 `value_independent=True` 下全窗冻结是"不在前向路径"，不是"死亡"**
   （6 个 run、8/8 窗口），而 `byp_cprime_20k` 里同一批张量 8/8 都在动
   ⇒ R14 的"诊断陷阱"现在有了跨 run 的量化证据。
4. **Layer 1 的惰性不是同义反复**：把该 run 按 12500 切成"活头期（89 点）/ 死头期（56 点）"后，
   `resid_norm` 中位 **0.0473 / 0.0414**（活头期尾部更重：max 1.688 vs 0.290）
   ⇒ 一个**梯度非零、参数在动**的 critic 对优势形状的贡献仍只有 ~5%；
   但**"在动"≠"拟合"**（本 run 逐批 `EVb` 全程 ≤0）⇒ **位移与 EV 是两个正交读数**。
   详见 `critic_inertia_verdict_2026-09-13.md` §0.1 ⑤。

**对论文的影响**：E6（用指纹重判四代架构）**已做**，结论是**分类而非推翻**——
四代变体的值头全程在动，它们不是死于吸收态；因此 §3 的 K4 从
"用指纹提前否掉一个会被接受的错误结论"改写为 **"用指纹把 EV≈0 分成 M1/M2 两类"**。

---

## 6. 正面结果（论文里必须有一节"什么 work"）

1. **C1+C2**：GRU 输入饱和的**诊断—修复—4× 尺度耐久**闭环（`enc_ln`）。
2. **C5+C6**：对手分布去镜像化 + 动态自身联赛 + PFSP 门禁，**消除"整段输给随机策略"的相位**，且在 4× 训练量下不退化的证据链。
3. **C9**：胜率平台期下，**行为链三层指标**（防守响应 / 接敌 / 单边堆牌）有真实爬坡 ⇒ "曲线不动 ≠ 什么都没学到"。
4. **工具/协议产出**：按局留出的探针、128 帧批内 vs 池化双口径、锚点绝对仪器、脚本复算基线、
   位图对账、预注册判据模板 —— 这些是**可抄的检测纪律**。
5. **信息侧天花板检验（2026-09-13 第二轮，P3/H-LEARN）**：**局内回报的变化在现有输入里就是可预测的。**
   `EV_within`：A（原始观测）**+0.182**、C（+belief token）**+0.186**、B（+oracle 隐藏量）**+0.197**；
   线性对照 Ridge 在同样特征上到 **+0.30**。⇒ **oracle 隐藏量只再加 1.1pp，belief token 只加 0.4pp。**
   **含义**：惰性**不是 POMDP 的信息缺口**——"挂起 bf 通道再跑"这个消融的期望收益
   **已被量化封顶在 ~1 个百分点**，可以取消。
   证据：`docs/pomdp_ceiling_v2_prereg/verdict_2026-09-13.md`、日志 `docs/pomdp_ceiling_v2_*.log`。

---

## 7. 未决（O1–O5）

| # | 未决 | 现状 |
|---|---|---|
| O1 | 上限/绝对强度没动 | D1 20k 与 100k 在最差锚点上无差别；次判据口径敏感 ⇒ 按预注册不判决。候选：`rand_anchor 0.1→0.3`（D2 剂量） |
| O2 | critic 拟合 | 机制已定位（§5）；**信息侧上限**待 `pomdp_ceiling_prereg_2026-09-13.md` 判读 |
| O3 | 行为病理未修完 | `elixir_avg` 低（不攒费）；`setup_wait` 两阶段状态机未实现；单边堆牌末点 100% |
| O4 | cycling 只"防崩"未"破除" | RPS 循环仍在（**这一条是博弈结构性质，不是实现 bug**，见 §9） |
| O5 | 同 seed 不可复现（机制未定） | 属方法学问题；当前靠"多跑聚合 + 大效应判据"绕过 |

---

## 8. 检测协议（读者可直接抄的 8 道闸门）

> 任何"指标变好了/模型学会了"的结论，按顺序过这 8 道；任一道不过就不下结论。

1. **口径闸**：这个数字读的是**参数更新前**还是更新后？样本集是哪个？（R9①②）
2. **留出闸**：留出是**按局分组**的吗？先报 `corr(R_t, R_{t+1})`。（R9④）
3. **基线闸**：基线是**脚本复算**出来的吗？（R4）
4. **退化闸**：**若被测量的东西退化为常数/平凡解，这个判据会给出什么？** 若与"正常情形"给出同样的读数，判据无效。（X-11/X-13）
5. **相位闸**：报的是**分布**还是**池化值/均值**？批内与整体两个口径都报了吗？（X-12）
6. **参数闸**：**参数在动吗？** 逐张量 `‖ΔW‖/‖W‖`；出现"位移恰为 0"的窗口 ⇒ 该参数**梯度精确为零**。（R14）
7. **仪器闸**：这个读数是**外生绝对仪器**（固定随机锚点）还是 run 内自引用？自引用读数不得当进步证据。（X6/C4）
8. **估计器闸**（2026-09-13 补）：这个读数是**一个合格估计器**给出的吗？两个子闸——
   **（a）阳性对照（上界）**：构造一个"按构造必然含信号"的靶子，**同一条流水线**必须能打中它；
   **（b）证伪对照**：把信号源打散/置换（保留其余结构），读数必须**塌回去**。
   并且：**在自己的留出集上都劣于平凡解的估计器，其读数不得用来断言"信息不存在"。**
   （用例：① X-14 的发散拟合；② `pomdp_ceiling_prereg` §9 的 V5 ——
   "打散状态、保留时钟 ⇒ `EV_within` 从 +0.20 塌到 +0.005"；
   ③ `pomdp_ceiling2_prereg` 的**四道对照电池**（阳性对照 ≥0.15 / 时钟基线 ≤0.05 /
   状态打散 ≤0.05 / **目标打散 ≤0.05**）——四条同时通过才允许读主判据；
   **合格性由对照决定，不由结论决定**，这是本轮能把"P0 灰区"推进到"P3"的唯一理由）

---

## 9. 两条"非架构/非程序"的瓶颈（论文的第二个支柱）

用户 2026-09-13 提出的写作条件是"等到项目遇到一个不是来自架构与程序的瓶颈"。**这个条件其实已经（部分）满足**：

1. **自对弈的非传递性 / cycling（C4）**：RPS 三角是**博弈结构的性质**——自对弈必然转圈，
   任何实现都会遇到；D1 只做到"防崩"（C5/C6），没有也不该期望"破除"。
   ⇒ 它是**问题本身的属性**，不是我们的 bug。
2. **（待判）advantage 窗口内回报的状态可预测性**：由
   [`pomdp_ceiling_prereg_2026-09-13.md`](pomdp_ceiling_prereg_2026-09-13.md) 判定。
   若落在 **P2**（连 oracle 隐藏量都解释不了局内回报变化）⇒ 瓶颈是**环境/奖励结构本身**
   （GAE 窗口内的回报不可由状态预测），与我们一行代码无关。

---

## 10. 附录：证据文件索引（run → 日志 → 判读 → 脚本）

| run / 实验 | 日志 | 判读 | 关键脚本 |
|---|---|---|---|
| `d1_long_100k` | `train_d1_long_100k.log`、`_dashboard_d1_long_100k.log` | `d1_long_100k_verdict_2026-09-13.md`、`d1_long_100k_cause_analysis_2026-09-13.md`、`diag_d1_long_100k.md` | `diag_value_head.py`、`_probe_value_path.py`、`_probe_value_collapse.py` |
| `d1_league_20k{,r2,r3}` | `train_d1_league_20k*.log` | `d1_league_20k_verdict_2026-09-13.md` | `judge_anchor_blocks.py --groups` |
| `critic_inert_probe_20k` | `train_critic_inert_probe_20k.log` | `critic_inertia_prereg_2026-09-13.md`、`critic_inertia_verdict_2026-09-13.md`（§0.1 为更正） | `judge_critic_inertia.py`、`_probe_value_path.py` |
| `critic_inert_probe_20k` 信息侧 | `pomdp_ceiling_probe_stoch.log` | `pomdp_ceiling_prereg_2026-09-13.md`（判读待写） | `pomdp_ceiling_probe.py` |
| 架构四代 | `train_{byp_cprime,eind,fprime,fprime_ev,gfix}_20k.log` | `fprime_rerun_20k_verdict_2026-09-13.md` | `diag_critic_ev.py`（5 种 EV 口径） |
| 饱和诊断 | `train_fix_gru_ln_*.log`、`train_prod_200k.log` | `value_channel_saturation_diagnosis_2026-09-11.md` | `diag_encoder_scale.py`、`diag_gru_ablation.py` |
| cycling 取证 | — | `cycling_league_plan_2026-09-13.md` | `_forensics_cycling.py`、`forensics_response.py` |
| 口径/闸门 | `train_ab_valnorm_20k.log` | `ab_valnorm_20k_verdict_2026-09-11.md` | — |
| 评估节奏 | — | `eval_cadence_c_2026-09-13.md` | `summarize_solo_run.py` |
| R1 事件 | — | `r1_incident_2026-09-13_layer1_cuda_unknown.md` | — |
| 可复现性 | `_repro_{a,b,c,d}.log` | `AGENTS.md` R5 | — |
