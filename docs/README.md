# 文档索引

> 本文件是 `docs/` 与全仓库文档 ↔ 源码文件的导航地图。改文档前先看这里，避免重复/失联。
> 维护约定：新增 curated 文档必须在本文登记；原始采集数据（`_` 前缀文件）只被引用、不手改。

## 0. 快速入口

| 想做什么 | 去哪 |
|---|---|
| 理解模拟器/跑起来 | 根目录 `README.md`（中文）/ `readme_en.md` |
| 启动训练 | `start_rl.bat`（solo/run/flow 三模式 + 仪表盘）、`start_training.bat` |
| 训练算法代码导读 | `src/clasher_new/rl/README.md`（模块表 + 训练入口命令） |
| **所有计划的整合视图**（哪条路走通了/被否证/未决 + 优先级与判据设计） | `plan_master.md` |
| **当前问题的归因**（critic 为何塌成常数 / 上限为何不动 / 判别性实验与预注册要素） | `d1_long_100k_cause_analysis_2026-09-13.md` |
| **critic 惰性检验**（预注册 + 判读） | `critic_inertia_prereg_2026-09-13.md`、`critic_inertia_verdict_2026-09-13.md`（判读脚本 `../scripts/judge_critic_inertia.py`） |
| R1 事件留证（`cudaErrorUnknown` / 宿主提交压力 / 孤儿 worker） | `r1_incident_2026-09-13_layer1_cuda_unknown.md` |
| **逐文件函数全解**（每个代码文件的每个函数：作用/参数/实现/行号；含覆盖对账与编造体检） | `full_code_reference.md`（170 个代码文件 / 1554 条索引） |
| **训练方法文档**（训练过程、被调函数、函数参数、函数索引；docx 同名） | `training_method.md` / `training_method.docx` |
| **游戏引擎文档**（地图 / 寻路 / 索敌 / 战斗主循环；docx 同名） | `game_engine.md` / `game_engine.docx` |
| 跨会话红线与结论索引（先读这个再动方案） | 根目录 `AGENTS.md`（编号可引用：R/C/X/O） |
| **AGENTS 分册索引**（2026-09-18 拆分 + **2026-09-19 v2 三层形态**：`AGENTS.md` = **A 红线全量 / B 索引一行式 / C 状态速览**，**节号不变**） | `agents/redlines.md`（R1–R19 全文）、`agents/env.md`（§2）、`agents/metrics.md`（§3）、`agents/ledger.md`（§4–6）、`agents/plans_runs_docs.md`（§7–9）、`agents/structure.md`、`agents/reward_surveys.md`、**`agents/agents_long_entries.md`**（v2 起从 `AGENTS.md` B/C 两区下沉的**逐字长条目全文**：B 区 10 行 + C 区 6 条 = 52,159 B 正文）；校验 `scripts/_agents_split.py --check`（v2 上线后 **PASS**） |
| 历史决策全文（过程细节与推理链） | `agents_archive_2026-09.md` |
| 自检/回归 | `src/clasher_new/rl/selftest.py`、`scripts/test_m1..m6*.py`、`scripts/batch_smoke.py` |

## 1. Curated 文档（策划/规格/报告，已定稿可引用）

| 文档 | 内容 | 关联源文件/数据 |
|---|---|---|
| `full_code_reference.md` | **项目内容全解（2026-09-14）**：对 `src/` `scripts/` 及仓库根目录共 **170 个代码文件**做逐文件函数级摸底——每个类/函数/方法给出类型、签名（含默认值）、作用、参数、返回、实现步骤、调用关系、置信度，全部带源码行号；含代码库总览、非代码文件清单、**覆盖对账**（147 个 `.py` / 1479 个符号 100% 覆盖）、**签名·行号·参数抽检**（对账 1479 符号：0 类型不符、0 参数缺失；共 170 文件 / 46917 行 / 1554 条索引）与**全局符号索引**（1528 条） | 全仓库代码；生成链 `scripts/_survey_{inventory,groups,brief,verify,audit,merge}.py`（完整保留）；素材 `docs/_survey/`（⚠️ 2026-09-14 已清理 `parts/` 51 份稿件碎片 —— 内容已全文并入本文件；`inventory/groups/coverage/audit/reverse_check` 报告保留） |
| `training_method.md` / `.docx` | **训练方法文档（2026-09-14）**：三种训练模式、一次训练的数据流（观测→前向→动作→奖励→GAE→PPO）、网络逐层结构、**TrainConfig 全 47 字段**、`run_league.py` 全 54 个 CLI 参数、检查点与恢复、评估诊断口径、辅助训练脚本、**rl 全包函数索引**（含字母序快速索引） | `src/clasher_new/rl/*.py`、`battle.py`；素材 `docs/_survey/drafts/training_*.md`（⚠️ 2026-09-14 已清理，内容已并入本文件） |
| `game_engine.md` / `.docx` | **游戏引擎文档（2026-09-14）**：地图与坐标系统、实体体系、`step(dt)` 逐阶段战斗主循环、伤害与命中、皇家塔与胜负判定、**寻路**（两套实现与 A* 代价）、**索敌与目标选择**、卡牌机制分类、法术落点与命中公式、部署合法性 | `battle.py` `arena.py` `core.py` `player.py` `pathfinding*.py` `card_mechanics.py` `spell_module.py` `threat_calc.py` `rl/action_mask.py` |
| `../AGENTS.md` | **红线与结论索引**（2026-09-13 重构）：12 条红线（R）+ 10 条已确证（C）+ 9 条已否证（X）+ 5 条未决（O）+ 计划台账 + run 台账；条目编号可引用 | 全部 `rl/` 与 `scripts/`；过程细节见 `agents_archive_2026-09.md` |
| `plan_master.md` | **计划总纲（所有计划的整合视图）**：决策地图（8 个方向的通/否/未决）、阶段结论、13 份计划的谱系台账、**当前优先级 P0-P4（含判据设计要点与成本）**、纪律清单 | `rl_training_fix_plan_v1/v2/v3.md`、`rl_review_fix_plan.md`、`rl_reward_plan_v2.md`、`rl_plan_design_v1.md`、`ai_training_plan.md`、`P0-mechanics-plan.md`、`mcts_design.md` |
| `d1_long_100k_cause_analysis_2026-09-13.md` | **当前问题归因（2026-09-13）**：价值分支参数级指纹（35k→100k 的 22~24/27 窗口位移**恰为 0** ⇒ 价值函数=常数）、第一性原因（末端 LayerNorm ⇒ 可表达方差 0.14 vs 目标 std 12.11 ⇒ 自举失败 ⇒ ReLU 死亡锁死）、上限不动的策略侧成因（相对目标 + RPS）、**新否证 X10**（锚点每点重抽 40 局）、**新红线 R14**、3 处对上游报告的口径更正、4 条判别性实验 | `runs/d1_long_100k/`（41 ckpt + replays）、`rl/follower.py`、`rl/ppo.py`、`rl/env_wrapper.py`、`rl/train_solo.py` |
| `report_d1_long_100k_structured_2026-09-13.md` | 上述归因的**结构化取证全文**（6 路并行子任务 + 评审归并，41 KB）：训练信号预算基数、critic 目标与归一化、对手漂移时间尺度、行为病理、奖励分解、仪器偏差；含 4 处子任务截断披露 | 只读证据（原始行号与数字） |
| `agents_archive_2026-09.md` | **历史决策全文存档**（旧 `AGENTS.md` 逐字冻结，100,068 B，sha1 见文头）：含全部过程叙述、证据链、逐点判读表与已过期中间结论 | 只读；新结论写 `../AGENTS.md` |
| `P0-mechanics-plan.md` | P0 游戏机制全面补全计划（族1-8）与进度 | `battle.py` `card_mechanics.py`；回归 `scripts/test_m2.py` `scripts/batch_smoke.py` |
| `card_coverage.md` | 卡牌覆盖矩阵（148 条快照、75 张 implemented 口径） | `card_utils.py` `gamedata.json` `cards_stats_*.json`；`scripts/coverage.py`；`card_registry.json` `batch_smoke_report.json` |
| `review_needed.md` | 人工评审队列（L1 结论与剩余未知） | 同上 |
| `数值规则查证汇总.md` | 觉醒周期表/英雄能力/法术锚点三线查证（gamedata=1级基准 vs wiki=11级基准，换算 ≈1.1^10） | `gamedata.json` `cards_stats_spell.json` `evolutions.py` |
| `errata_batch2_13_implementation.md` | 勘误批 2-13 实施汇总（用户三原则 + 回归红线） | `battle.py` `card_utils.py` `core.py` 等（批量）；回归 `test_m2..m6` |
| `data_integration_notes.md` | M7 三卡（Ronin/Vines/Spirit Empress）引擎钩子决策 | `card_mechanics.py` `battle.py`；证据 `docs/_fp_*.txt` |
| `evo_2025_new7.md` | 2025 新觉醒 7 张实现规格（快照无 evolvedSpellsData → 独立数据层） | `evo_2025_data.py` `evolutions.py`；`scripts/test_m3_evo.py` `test_m4_evo7.py`；证据 `docs/_evo_*.txt` `_*_evo.txt` |
| `evo_hook_gap_matrix.md` | 觉醒机制钩子缺口矩阵（42 张闭环审计） | `evolutions.py` `battle.py`（evolved 分支） |
| `elite17_spec.md` | Elite17 精英/Hero 卡规格（内存=官方 15.535 权威，Fandom 双记冲突） | `elite17_data.py` `battle.py`（Hero overlay）；`scripts/test_m6_elite.py`；证据 `docs/_elite_*.txt`；附录 `re/official/extracted/elite17/`（不入库） |
| `_elite_fandom_report.md` | Elite 抓取过程报告（工具/落盘约定） | `scripts/cdp_extract.js`；原始落盘 `docs/_elite_*.txt` |
| `ai_training_plan.md` | RL 训练算法闭环规划（先知/信念/跟随者/联赛） | `rl/` 全包（导读见 `../src/clasher_new/rl/README.md`） |
| `rl_review_fix_plan.md` | RL 链路修复手册（6 条 Critical 已修） | `rl/selftest.py`（回归） |
| `rl_reward_plan_v2.md` | 奖励与计划通道重构 v2（奖励经济预设、法术空砸/裸下/坦克几何闸门） | `rl/config.py` `rl/env_wrapper.py` `rl/action_mask.py` |
| `rl_plan_design_v1.md` | PlanToken 战术意图扩展设计 v1（57 维/21 意图） | `rl/plan_space.py` `rl/belief_planner.py` `rl/prophet.py` `rl/follower.py` |
| `mcts_design.md` | 推理时浅 MCTS 设计（UCT+引擎叶推演，预算/对手口径/评估接入） | `rl/mcts.py` `rl/selftest.py`（test_mcts_*） |
| `question_bank_feasibility.md` | 题库预训练可行性评估（POC 实测：引擎判卷 3 题+checkpoint 答题 EV gap；判卷口径两缺口；三步走路线） | `../scripts/question_bank_poc.py` `simulate_exchange.py` `threat_calc.py` `rl/follower.py` |
| `training_audit_2026-09-11.md` | **训练体系审计（四问：训练问题/评判指标/步数预算/参数量）**：评论家损失主导更新（gnorm 中位 6.8k vs 阈值 0.5、clip_frac 恒 0%）、指标分辨率与锚点缺失、三档步数建议、629,359 参数实测 | `rl/ppo.py` `rl/config.py` `rl/train_solo.py` `rl/evaluate.py` `rl/follower.py`；证据 `docs/train_economy_*.log` `runs/*/solo_state.json` |
| `rl_training_fix_plan_v1.md` | **训练整改计划 v1**（P0 价值通道/指标可信化 → P1 结构性机制 → P2 工程；含 G1/G2/G3 门禁、单变量 A/B 协议、回归测试清单、风险登记册、明确不做项） | `rl/ppo.py` `rl/config.py` `rl/train_solo.py` `rl/follower.py`；依据 `training_audit_2026-09-11.md` |
| `ab_valnorm_20k_verdict_2026-09-11.md` | **`ab_valnorm_20k` 20k 步验证跑 G1 判读**：`step`=决策帧（20k 步≈55~80 局，非"训练很多"）、`v/p` 2.43→0.95 达成、`ratio≡1.000` 判据作废、**critic 解释方差≈0（新发现）**、三曲线净变化为零、门禁阈值口径错位（9.5 vs 实测 28~38） | `rl/ppo.py` `rl/config.py` `rl/train_solo.py`；证据 `../docs/train_ab_valnorm_20k.log` `runs/ab_valnorm_20k/` |
| `rl_training_fix_plan_v2.md` | **训练整改计划 v2**（v1 的修订版）：G1 判据 `ratio`→**EV**、门禁改**相对首个评估点**、预算改**局数口径**、对手池 `0.7/0.2/0.1`→`0.5/0.3/0.2`、main 曲线**先评估后同步**；含 200k 长跑协议与 G1/G2/G3 分支 | `rl/ppo.py` `rl/config.py` `rl/train_solo.py` `rl/dashboard.py`；依据 `ab_valnorm_20k_verdict_2026-09-11.md` |
| `value_channel_saturation_diagnosis_2026-09-11.md` | **价值通道失效根因诊断（GRU 输入饱和）**：`enc` 量级 533（CNN 输出 468）→ GRU tanh 候选饱和(abs 0.994) → 隐状态 h 冻成常数(跨帧 std 2.6e-5) → critic 恒为常数，EV 精确等于 −bias²/Var(R)；另发现"128 连续帧批"把 EV 放大约 3 倍；病理跨全部历史 run | `rl/follower.py` `rl/ppo.py` `rl/train_solo.py`；证据 `scripts/diag_critic_ev.py` `diag_value_head.py` `diag_encoder_scale.py` `diag_gru_ablation.py`；数据 `runs/prod_200k_valnorm_ev/` |
| `rl_training_fix_plan_v3.md` | **训练整改计划 v3**：纠正 v2 §4 病因误判（非奖励/价值头，而是 GRU 饱和）；P0-A enc 后加 LayerNorm、P0-B EV 改池化口径 + 加 GRU 活力指标、P0-C 诊断常态化 + **启动前架构护栏**；§3.6 = **5k 验证跑判读**（n_abs 0.994→0.46/0.55、EV −0.58→−0.06 单调上升未过零、**不执行 v2 §4 高侵入项的理由**、补齐"值/R std"缺口、20k 协议与判读门槛/分支）；§3.7 = **enc_ln 20k 判读**（饱和修复成立、critic 仍常数、查出融合层尺度失衡 grid 占 fused 98%）；§3.8 = **`grid_ln`（P0-A 备选）落地 + 20k 判读**（尺度修复达成：grid 101→5.87；EV_global −0.134→−0.005 只修对中未修拟合；`n_abs` 回升 0.745；**`vs baseline0` 崩塌 0.125 = cycling 红旗**）；§3.8.4 = **A′ 取证**（RPS 三角确凿、绝对强度≈随机、早停裁定混淆）；§3.8.5 = **E1 固定随机锚点落地**（每评估点 `vs baseline_rand` 对照 + 报警线；短验证 0.15/0.15 确定）；§3.9 = **E2 训练侧锚点对手改造**（`rand_anchor` 进训练对手池第 4 槽 frozen 0.4/hist 0.3/defend 0.2/rand 0.1，锚点与 E1 同权重、附带修无 hist 归一化缺陷）；§3.9.3 = **E2 20k 判读**（干预无效：vs baseline_rand 末点 0.050、vs baseline0 0.075，绝对强度崩溃依旧、RPS 循环仍在转、critic 依旧常数；10% 弱锚点压不住自对弈漂移） | `rl/follower.py` `rl/train_solo.py` `rl/ppo.py` `rl/diagnostics.py` `rl/dashboard.py`；依据 `value_channel_saturation_diagnosis_2026-09-11.md`；数据 `runs/fix_gru_ln_5k/` `runs/fix_gru_ln_20k/` `runs/fix_gru_ln_norm_20k/` `runs/e2_rand_anchor_20k/` |
| `fprime_rerun_20k_verdict_2026-09-13.md` | **F′ 修正口径复跑判读（critic 侧收尾）**：与 `fprime_20k` 训练语义等价、同 seed/协议，唯一差异 = 无塔血通道 ⇒ G'-fix 的单变量对照；EV(更新前池化) 均值 **+0.031**（首跑 +0.198 是 in-sample 假象）；同窗口 `vstd/rstd` 8.5× 于 legacy 但 EV 不涨（"活起来的方差不可预测"）；离线 `EV_global −0.159` 且查出 EV 为负的**第二个来源 = 量纲失配**（回归斜率 `R~v` 10~525）；无塔血通道架构上 `enc→塔血差` 分组 R² −0.17（反证 G'-fix 的 +0.69 是通道真接上）；末点崩塌 = cycling 相位（非 G'-fix 特有）；**方法学：同 seed 同代码两次 run 前 435 步逐位一致、之后 1e-4 漂移、`eval@800` 已不同（`PYTHONHASHSEED=0`+单线程 BLAS 无效，机制未定）⇒ 20k 单跑 A/B(n=1/臂) 分辨率不足**；判定 = critic 停、转策略侧 | `rl/train_solo.py`（分支 C 仅度量修复）；证据 `../docs/train_fprime_ev_20k.log` `diag_predict_fprime_ev.log` `diag_vh_fprime_ev_20k.log` `_repro_{a,b,c,d}.log`；数据 `runs/fprime_ev_20k/` |
| `cycling_league_plan_2026-09-13.md` | **D1 预注册**（判据先钉死再改代码）：对手分布去镜像化（frozen 0.4→0.1 / hist 0.3→0.6）+ 动态历史自身联赛（`refresh_hist`）+ PFSP 门禁（alpha 0.05→0.20、易胜对手 ×0.2）；含 P1/P2 判据、判定分支、协议与可观测指纹 | `rl/train_solo.py` `rl/pfsp.py` `rl/config.py`；产物 `runs/d1_league_20k/` |
| `d1_long_100k_verdict_2026-09-13.md` | **D1 100k 长跑判读（已完成）**：**P1 通过**（块 worst 0.388/0.475/0.275/0.237/0.525 → mean 0.380、min 0.237，与无变化组 [0.000,0.025] 不重叠）⇒ **防崩在 4× 训练量（≈374 局）下成立**；min 0.237 落在 D1 20k 区间 [0.125,0.250] 内 ⇒ 该判据上与 20k 无统计差别；**上限仍未解决**（次判据口径敏感 +0.125/+0.062、cycling 未消除）；**加量不能救 critic**（EV 仅前两点为正，`vstd/rstd` 30k 后 ≈0.001，8 次 vitality 告警），但 GRU 活力 100k 全程健康（v3 修复耐久）；含**第三次同类口径自纠**（预注册写 9,8,8,8,8，脚本按序号实切 9,9,9,9,5，两口径都算且均 PASS） | 工具 `scripts/judge_anchor_blocks.py` `scripts/summarize_solo_run.py`；证据 `../docs/train_d1_long_100k.log` `diag_d1_long_100k.md`；数据 `runs/d1_long_100k/` |
| `eval_cadence_c_2026-09-13.md` | **评估节奏 C 方案（密锚点 + 稀全块）**：实测 20k 协议里评估占 **73% 墙钟**（纯训练 26 步/s、全点 210s、轻点 53s）⇒ 100k 从 3.46h 降到 **2.15h**；**为什么不能均匀放宽**（真实序列粗采样回放：D1 三跑锚点谷底只有 1 个点宽、5000 即开始漏、10000 把最差点 0.192→0.462；病态组持续塌陷留得住、健康组周期性瞬态留不住）；实现 `--anchor-every`（默认 0=旧行为）+ 轻点同密度落快照/Adam/run_state；**C ⊇ A 主判据**（锚点读数逐点相同，确定性论证）；两跑冒烟验收 | `rl/train_solo.py` `rl/config.py` `rl/run_league.py` `rl/selftest.py`；证据 `../docs/smoke_anchor_c{,2}.log` |
| `d1_long_100k_prereg_2026-09-13.md` | **D1 100k 长跑预注册**（跑前写死）：唯一变量=训练量；主判据 **C1-块口径**（41 锚点分 5 块、每块 9 点 worst，与 D1 20k 基线同构）；次判据仅描述（cycling 未消除 ⇒ 天花板不设判决线）；跑前披露 5 条偏差（补种 ckpt 挤出、n=1/臂、锚点数 41 vs 9、resume 粒度、运行期冻代码）；失败分支事先写死 | 代码 = `main`（D1）；协议命令与预算见文内 |
| `d1_league_20k_verdict_2026-09-13.md` | **D1 判读（三跑）**：C1「单跑最差锚点」D1 0.125/0.200/0.250 vs 无干预 0.000/0.000/0.025 **区间不相交** ⇒ **消除"整段输给固定随机策略"的相位（防崩有效）**；用户指定的"末 4 点滚动均值"两组重叠（相位主导）⇒ **未证明提高上限**；含**两处预注册标定错误的自披露**（基线列手抄算弱）与新纪律 | `rl/train_solo.py` `rl/pfsp.py`；证据 `../docs/train_d1_league_20k{,_r2,_r3}.log`；数据 `runs/d1_league_20k{,_r2,_r3}/` |
| `reward_settlement_prereg_2026-09-14.md` | **奖励结算方式改造预注册**：结算链路 5 个改动点（含源码行号）+ 零代码开关表；**先证明两类改动是恒等操作**（整体缩放奖励在 `adv_norm=scale`+`value_norm=running` 下 k 精确约掉；调权重无机制依据，因 W4 无主导项）；判据三层（机制/行为/结构）+ 阳性对照 `elixir_diff_weight 0.5→0`；§8 同日更新：**E-A/E-B 降级为近乎恒等**（ΔA=+0.0330 vs std(A)=14.68 ⇒ 0.22%，上界 1.5%）、**在线状态基线全部 ≤0**（最好 +0.0344）、段级/尾部收缩无机制依据 | `rl/env_wrapper.py:238-263` `rl/ppo.py:168-248` `rl/config.py`；仪器 `scripts/probe_gae_kernel.py` `scripts/probe_credit_baseline.py`；⚠️ 该支线此前**未被 AGENTS.md / 本文件索引** |
| `reward_settlement_v3_design_2026-09-14.md` | **奖励结算 v3 方案（设计稿，同日深夜追加）**：L0 可审计结算台账 / L1 结算字段化（开）、L2 基线层（**关**：四协议实测全 ≤0）、L3 段级结算（**不做**：帧间自相关 0.987、窗口内方差仅 7.7%）、L4 势函数补齐 γ（唯一可证明正确，+0.0012/帧）；**§9/§10 = 对用户"训练打分器给每帧打分"与"搜索+推演训打分器"两提案的直接判决**（前者判**不行**：输入无增量 + potential 不改最优解 + 直接打分数无合法来源 + 在线口径 MLP `EV_within=−0.2225` 而阳性对照 +0.2710；后者落【R11】须用户拍板，且实测 v1 搜索结构**在数学上表示不了"攒费 8.4s 打 Xbow"**） | `rl/env_wrapper.py` `rl/ppo.py`；仪器 `scripts/probe_credit_baseline.py` `scripts/probe_scorer_offline.py` `scripts/probe_search_cost.py`；成本实测 43.3 ms/单次推演 |
| `frame_credit_proposal_review_2026-09-18.md` | **逐帧信用分配提案评估（2026-09-18，用户新方案）**：**性能栏**——在线账本 ≪1% 可接受，但**伤害归因当前仅 17/52 调用点带 `source`**、`take_damage` 无返回、回放**无实体 id**（实测 8584/9079 帧存在重复 `(name,player)`）、**关键陷阱 `source=` 是语义参数**（`battle.py:565-571` M7 钩子 + `card_mechanics.py:695-713` Ronin 格挡 ⇒ 补 source 会让 Ronin 开始吞法术）、反事实读法 2~5× 墙钟；**效果栏**——击杀圣水价值**已在 `edw` Φ 兑现（29.84%）**、「差分」可动空间已被实测压到 ≤+0.0344、助攻项 = 引擎决定（**假归因**，落 `rl_reward_plan_v2` P3 不做清单）、瓶颈不在结算侧（全帧圣水≥6=0.11%）；结论 **作为奖励不接受 / 作为仪器-教师值得做**，并给 S1 门禁→S2 仪器化→S3 守恒式 A/B 的三步路径与失败分支 | 只读评审（未改代码）；复算命令见文内 §6：`battle.py:550/564/565-571`、`card_mechanics.py:695-713`、`replay.py:85-90`、`rl/env_wrapper.py:181-263` |
| `s1_gate_2026-09-18.md` | **S1 零成本门禁（2026-09-18，手写专家）**：`scripts/s1_plan_gate.py`（PlanToken 贪心执行器，三臂 random / token_strict / token_xbow，同副牌 `DEFAULT_SOLO_DECK`、同批 seed、同对手 `SelfDefenderPolicy`，各 20 局）+ `scripts/phi_offline_check.py`（离线逐帧复算 Φ 并与回放 reward 对账）。实测：`token_strict` 全帧圣水≥6 = **0/8191**、Xbow = **0/929**（判据是 ≫ 基线 0.11%/0.03%）⇒ **未通过 ⇒ 约束主要在探索/机制侧**（Xbow 不在 `ACE_CARDS`/`SINK_TANK_CARDS` ⇒ 攒费 token 0 帧触发；`_pick_suggested_card` 只推荐付得起的牌 ⇒ 自锁）；`token_xbow` 只多加一条手写规则即 **156×**（47/1003，圣水中位 6.25）⇒ 动作空间/掩码被证伪为约束点。冰人白嫖推论 **证实**（102/102 实例 ΔΦ = −2.000000，`edw=0.5` ⇒ −1.0，双倍期 −0.2）。非法动作阴性对照 80/80 + 20/20 + 2/2 全被拒 | 数据 `runs/d1_long_100k/replays`（仪器自检逐位复现预注册 §3）|\n| `online_measure_2026-09-18.md` | **在线口径实测：`p4b` 的优势不成立（2026-09-18 第四轮）**：用 **measure-only**（新预设 `economy_etm`：跑在线监视器、把逐帧结算明细写进录像帧 `et` 键、**一分奖励都不加**、行为逐位不变）+ **250 局**（5 评估点 × 50 局）做**同批配对** ρ：**Δρ(终局塔血) = +0.008**、**Δρ(胜负) = +0.014**（离线口径是 +0.145/+0.108 ⇒ **÷18 / ÷7.7**）；基准也翻号（`ρ(none, 塔血)` 在线 **+0.084** vs 离线 −0.198；`ρ(none, 胜)` 在线 +0.240）⇒ **S2 相关性门禁在正确口径下不通过，S3 不开**；`τ` 太稀疏（Στ/Σφ ≈ 1/12、仅 ~38% 对局 τ≠0）⇒ 换代理要换口径而非调权重。附 4 个接线坑（评估 env 不收 reward_weights / WSL→Windows 不传环境变量 /spawn worker 全局重置 / 在线 τ 曾是 p0 视角）+ R1 孤儿检查救了 4 次 | `scripts/analyze_online_trade.py`、`runs/et_measure/`、预注册 §11.10 |
| `engagement_trade_online_2026-09-18.md` | **在线奖励项 + 落地验证 + 两处实质不确定（2026-09-18 第三轮）**：`rl/engagement.py::EngagementTradeMonitor`（tick 级并查集 / 交战核心身份判据 / K=30 tick / ΔΦ 用 `phi_mode=global` / τ=P4b）+ `RLEnv` 接线 + `DEFAULT_REWARD` 四开关（**默认 0.0 = 关**）；**开/关逐位对账 PASS**（逐帧奖励 + 全实体状态 SHA-256 两棵树逐字相同）。**记录通道 + schema 5 已落真树**：中立性 DIGEST 一致 / 【R19】selftest 4/4 / smoke 产出 schema 5（元组全 15 长、`v0` 100%、`is_product` 9.7%、非塔 `root_cast` 97.9%）。★ 两处实质不确定 ⇒ **S3 仍不放行**：① 在线 vs 离线局面切分不可比（同一局 67 vs 2 窗口）；② 重建 V 误差首次量出（严格相等 44.9% / 中位 0.23 / p90 3.0 / max 7.13）。⚠️ CRLF 假阴性事故（回滚了一次成功的补丁）| 工具 `scripts/s2_trade_probe.py`、`scripts/_apply_s2_channel_when_idle.sh` |
| `root_cast_channel_2026-09-18.md` | **引擎侧「出牌溯源」纯记录通道（2026-09-18）**：规格预注册 §3/§6。`root_cast`（出牌**事件** id，链式继承到根，指针写在产物体身上）+ `is_product`；`deploy_card` 薄包装 + 26 个出生点经 AST 插入宿主 + `schedule` 3 元组 + 绕过点补记。**状态：sandbox 已实现 + 行为中立性逐位对账 PASS（1200 tick 全实体 SHA-256 两棵树逐字相同），尚未落真树**（`run100k` 在跑）。逐字 diff `root_cast_channel_2026-09-18.diff`（+134 −40）| 工具 `scripts/_patch_battle_root_cast.py`、`scripts/s2_neutrality_probe.py` |\n| `s2_instrument_2026-09-18.md` | **S2 离线仪器·实测与门禁判读（2026-09-18）**：仪器 `scripts/offline_engagement_trade.py`（只读回放，支持 schema 4/5、9 种 `tower_term` 代理、`component`/`global` 两种 ΔΦ 口径、`core`/`members` 两种身份判据）+ 不变量 **6/6 PASS** （`scripts/selftest_offline_engagement_trade.py`，合成帧零引擎依赖）；读数：`σ(Trade)`=2.06/窗口、108.6 窗口/局、**零膨胀**（p25=p75=0）、全局口径圣水对账残差 max **0.71**；**P3 首选被推翻 / P1·P2 否决 / P4b 唯一候选（未超批间散布）**；缺口实测 **20.6%**；**5 个口径缺陷**（身份判据、`K` 不可辨识、零和对称化、`crown` 方向、重复计数）；**门禁判定 = 有候选、无判决**；附 schema 5 扩展名清单与全部复算命令 | 预注册 `engagement_trade_prereg_2026-09-18.md` §11.5；数据 `runs/run100k/replays/league_{0,8000,16000}.pkl`（schema 4） |
| `engagement_trade_prereg_2026-09-18.md` | **局面圣水交换信用分配·预注册（2026-09-18）**：缺口 = 现行奖励里「被阻止的伤害/拉扯」恒为 0（塔血没掉无正奖励，拉扯者被吃掉反而让 Φ 掉费）⇒ 正解被记为负；指标 `Trade_me = ΔΦ_window + tower_term`（附两个自检算例：6 费全下 +6、「1 费解 4 费」+3）；`tower_term` 三代理 **P3 首选**（复用既有 V 注销、零新常量、不可刷）；**免费产物绝不估值**（货币泵 + 零成本刷分两条否决证明）；溯源路由 `root_cast` 链式取根且**写在产物体身上**；判据三层 + **优势兑现率门禁** + 阳性/阴性对照 + 4 条失败分支；不变量 7 条（含 `trade_p0 == −trade_p1`、桶守恒、默认关逐位回旧）；引擎改动 5 项（纯记录通道，**不给 `take_damage` 补 `source`**）；实验**必须挂 `solo`**（`run` 对手是 mask-随机 ⇒ 无拉扯样本） | 设计论证 `frame_credit_proposal_review_2026-09-18.md` §7–§9；仪器 `scripts/_agents_split.py --check`；数据 `runs/long1m/replays/`（18 录像） |
| `run100k_2026-09-18.md` | **`run100k` 启动记录（100k 步 run 模式，沿用 long1m 两级评估）**：启动前【R1】卫生（**14 个孤儿 worker 占 7.7 GB** ⇒ 清掉后提交 7.51→23.24 GB、档位判回 12 OK）；评估计划脚本复算 **14 点（大 2 / 小 12）/ 800 局**；命令原文；成本 ≈1.65 h；仪表盘实测 run_meta 与 schedule 逐点一致 | 命令见文内 §3；数据 `runs/run100k/` |
| `long1m_stopped_2026-09-18.md` | **`long1m` 提前终止记录（部分 run）**：按用户指令停在 **128,000/1,000,000（12.8%）**——18 个评估点（含插入大点 100k）、**0 次降级**、无孤儿 worker；**J1 按定义不成立**，其余判据只能描述性地读这 12.8%；含终止时 Elo / 逐对手胜率读数、50 局环境画像（**0 平局、49/50 三冠碾压、中位 168 帧**）、以及「明确未做」三节 | `runs/long1m/`（18 ckpt + 18 录像，393 MB）；复算命令见文内 §1 |
| `et_solo100k_judgment_2026-09-18.md` | ★★ **`et_solo100k` 判读（`A_et` vs `B_ctrl`，2026-09-18 已跑完）**：按预注册 §11.13.8 结构（不构成判决声明 / 两臂 + **六处测量侧更正** / 四层读数 / **失败分支逐条判定** / 明确未做 / 台账落点）。**结论 = 没有任何一项能用现有仪器分辨出疗效**：机制层 **4/6 不可分辨**（余 3 项越界但 `grad_cos` 0.99730 vs 0.99800 这种量级 ⇒ 不写"改善/恶化"）；行为层差异停在**计数级**（全帧圣水≥6 **0.52% vs 0.05%**、Xbow **5/8769 vs 1/8370**、部署前圣水中位 3.250 vs 3.286）⇒ 两臂都**没**把"攒费→打 Xbow"做出来；门禁兑现率 **98.5% vs 98.2%**、逐批方向 **+6/−7/0** ⇒ **饱和、无分辨力**（但 A 的"打出塔伤"低 **4.1 pp** 留作候选）。**分支 1/2/4 全部"未触发"**（含 §3.1 人工判读依据）。缺阳性对照 + n=1/臂 ⇒ **不构成判决** | 读数 `readout_et_solo100k.md`、证据 `readout_et_solo100k/`、预注册 §11.13、run 记录 `et_solo100k_2026-09-18.md`、台账 **O8** |
| `readout_et_solo100k.md` | `et_solo100k` **§11.13.2 四层读数**（脚本复算、非手抄）：层 1 机制（within-run 前 100 点中位±3×MAD + 跨臂「不可分辨」判定）+ 层 1b EVb/池化 EV + 层 2 行为 + **层 3a 兑现率**（离线口径）+ 层 3b **配对 Δρ**（在线口径，含逐批 14 批）+ 层 4 项体检（`et` 覆盖 100%、`τ≠0` 仅 3.6%）。**只出读数与程序性谓词，不做判决** | 生成器 `scripts/et_solo100k_readout.py`（`--selftest` 7/7）；原始仪器输出同目录 `readout_et_solo100k/` |
| `et_solo100k_2026-09-18.md` | **`et_solo100k` — 用户要求的 100k 局面圣水交换干预长跑（两臂）记录（2026-09-18，★ 已跑完）**：`A_et = economy_et`（`engagement_trade=0.5` 与 `edw` 同汇率）+ `B_ctrl = economy_etm`（仅 `measure_only` 差），均带 `--adv-inert-probe` 与 `--diagnose-every 1`；**三次发射**（第 1 次漏探针 ⇒ step 7547 作废；第 2 次 `de=10` ⇒ step 15895 作废留证）；`eval@0` 三层读数（结果 `0.650±0.107` / 行为层「全帧圣水≥6 = **0.00%**、Xbow 出手 **0**、部署前圣水中位 **3.250**」/ 门禁 `Δρ(塔血) +0.024`、`ρ(τ,φ) +0.443`）；★ 另含 **§8 同 seed `eval@8000` 差 0.60 的分辨率标定**、§9 评估空转归因、§10 自动收尾、**§14 收尾竞态修正（阳性门禁）**、**§15 换臂到 `B_ctrl` + 健康面板实测核验**；【R1】预检与「内联 `$_.CommandLine` 假阴性」教训 | 预注册 §11.13 / **§11.13.6/7/9/10/12/13**（六处测量侧更正）；数据 `runs/et_solo100k/`、`runs/et_ctrl100k/`、`runs/_discarded_et_solo100k_{noprobe,de10}/` |
| `pass_prob_2026-09-18.md` | **读数：模型选择「不出牌」的概率（用户直接提问，2026-09-18）**：口径 = **bundle 第一个 decoder 步选 `STOP_IDX`**（掩码后、含 plan 软偏置），**必须把「被迫」与「选择」分层**。① **开局帧**（双方 5 圣水空场，n=30 局）：A_et **0.052** / B_ctrl **0.034** / **未训练** 0.081，**argmax 从不是 STOP（0/30）**；② **有合法选项时（真·选择概率）**：A_et 0.06~0.09、B_ctrl 0.058、未训练 0.15~0.20，**去 plan 软偏置后翻倍**（偏置在推着出牌）；③ **全程 ≈0.90 是"没得选"**：**88.3%~90.6% 的帧一个合法项都没有**，**全部因为买不起**（圣水中位 **1.50**），「不裸下」门禁触发 **0** 帧 ⇒ 纠正历史 `P(STOP)=0.945/0.929` 的读法（独立复现 2026-09-14 判读 §3.1 的 87%~93%） | 仪器 `scripts/probe_pass_prob.py`（只读，~2 min 40 s，`--json` 落逐帧数组）；对照 `channel_gradient_verdict_2026-09-14.md` §3.1、`tool_usage_audit_2026-09-14.md` §2 |
| `elixir_saving_audit_2026-09-18.md` | **穷举实测：「模型靠自己学不会攒费」（用户命题）——判断成立且更狠**：新仪器把每帧分成 **A 主动不出牌 / B 整包被拒 / C 实际出牌**（分类判据 = **会计恒等式**「圣水只可能因回费上升」，不依赖录像没记的 `invalid_count`；决策圣水**精确重建** `pre[i]=elixir0[i-1]`）。**核心读数**：**「放弃一张买得起的牌」在 75,891 帧里发生 0 次**——7,238 段「主动不出牌」**0 段**是在圣水 ≥4.0 时仍不出（卡组结构保证手牌最便宜那张 ≤4 费 ⇒ y≥4 必有可出；25 段恰好到 4.000000、0 段超过）；出牌帧「圣水−所打最便宜卡费」中位 **+0.137**（<1 帧回费 0.1786）、**0 段 >1.0**；A 段最长 **22 帧 < 34 帧**（攒到 Xbow 6 费所需；回费**实测 0.3571 圣水/s**）；三类占比 14 个点**无趋势**（step 0 = 91.5%/0.0%/8.5%）⇒ **不是"学得慢"，是这条轨迹从未被采样（0 样本 ⇒ 0 梯度）**。★★ **附带查出新病理 B 类**：模型**提交非法卡包被整包拒绝、白掉一帧**（A_et **497 帧 / 0.7% / 39 of 280 局**、最长**连续 225 帧 ≈112.5 s**（单局 180 s）、起始圣水全 = 5.0（**开局就卡住**）、每帧吃 `invalid_penalty=0.05`（225 帧 ≈ −11.25 > 胜利奖励 10）、**跨 9 个评估点仍在**、**非法性 100% 可自证**：重复槽位 490 + 总费>圣水 7、无法自证 0）——**它污染了既有读数**：层 2「全帧圣水≥6 = 0.52%」**96% 是它**（375/390），**5 次 Xbow 全在 B 解锁帧**（决策圣水 6.07~9.11）⇒ Xbow 是「卡死解除时顺手打出去的」，不是攒出来的。⚠️ 与 **P2**（`P1-20` validate 通过但引擎拒绝）**不是同一条**；成因**未定**（【R10】） | 仪器 `scripts/pass_streak_audit.py`（只读，~20 s 跑两臂 155,866 帧；v1 已作废）、原始输出 `pass_streak_audit_2026-09-18/`、台账 **C14 / O9**（O3 已补记）；追溯更正 `et_solo100k_judgment_2026-09-18.md` 补记 |
| `mask_used_slot_offbyone_fix_2026-09-18.md` | **修复：掩码 `used` 的 0/1-based off-by-one（B 类「非法卡包」根因）**：`get_action_mask_for` 里 `used` 存 0-based 下标、成员测试却用 1-based `sa.slot` ⇒ partial 出现「(s, s−1)」降序对时低槽位被整段跳过（既漏记 `used` ⇒ **掩码放行已用槽位** ⇒ 提交重复槽位 ⇒ `validate_bundle` 整包拒绝；又漏扣卡费 ⇒ 圣水模拟与校验口径不一致）。**一行修复** + `act()`/`act_parallel` **两条零成本掩码不变式**（已用槽位必须非法 / 被掩槽位不得被选中）+ 回归测试 `test_mask_partial_bundle_invariants`（四条不变式、跨局边界、**判别力已证**）。**同种子对照**：掩码层 44/256 → **0/256**；rollout 拒绝帧 474/1707（27.8%）→ **0/1609**；**端到端**（修复后产录像 → 同一台仪器 `pass_streak_audit.py` 审计）**B 类 = 0/3413 帧**。真实 100k 录像里的病例：**497 帧 / 39 of 280 局 / 最长连续 225 帧 ≈112.5 s**（单局 180 s）、每帧吃 `invalid_penalty=0.05`（225 帧 ≈ −11.25 > 胜利奖励 10）。⚠️ **与「攒费」无关**（修复后 `A ∩ 圣水≥6` 仍 = 0 帧，C14 不变）；遗留观察项：`slots` 合法但 `cells` 整行为空时 `argmax` 会落到 cell 0（本次 4293 帧未出现） | 源码 `rl/env_wrapper.py`、`rl/follower.py`、`rl/selftest.py`；留证 `docs/mask_used_slot_offbyone_fix_2026-09-18/`（含**修复前源码副本** `env_wrapper_prefix_792736a.py.txt` + A/B 日志 + 审计日志）；台账 **O9** |
| `dice_pass_option_analysis_2026-09-18.md` | **分析：「让模型掷骰子决定出不出牌」+「让模型可选是否用骰子」（用户提问，只分析不实现）**：① 骰子**作用域只有 ~10% 的决策帧**（其余 88.3%~90.6% 被掩码强制，被迫帧全因买不起），覆盖率 **0.54%~1.79% → ~5%**；② **i.i.d. 骰子给不了「攒费」需要的时域**（需连续 10~25 帧"有牌可出却不出"，i.i.d. 只有 `0.5^K`：K=10 → 1e-3、K=25 → 3e-8）⇒ 只有**带时长的 option** 形态有时域能力；③ 「可选是否用骰子」若做成可学元门**大概率退化解**，且**骰子帧对 slot_head 的 play/pass 梯度恒为 0**（与掩码-损失恒等式同型）⇒ 骰子只能当**数据生成器**喂 critic，**教不会**策略攒费；④ **「初始」的动机不成立**（未训练网 P(不出牌) 0.081 开局/0.153 可动，与已训练同量级；`argmax=STOP` 0/30 ⇒ 冷启动自锁已被 `stop_logit_bias=-1.0` 解决，100k 后该偏置仅 −1.000→−0.971）；⑤ **本卡组攒费通道是死代码**（`hold_mask ≡ 0`：`_setup_wait` 需 `SINK_TANK_CARDS`、`_save_ace` 需 `ACE_CARDS`，solo 卡组都不沾；独立吻合于"Δlogit 恒 −0.8000 零散布"⇒ 唯一在起作用的 plan 槽位偏置是 `PLAN_CARD_BIAS=+0.8`，方向是**推你出牌**）；⑥ **最大工程坑 = PPO 重放一致性**（骰子结果必须写进 rollout 且 `evaluate*` 按记录复现，否则静默破坏 ratio）；含 11 条红线的逐条合规做法、V1–V4 变体的可falsify预测、预注册判据/不变量/失败分支、**零成本前置探针**与替代手段对比表（结论指向机制侧 O7） | 引用 `pass_prob_2026-09-18.md`、`s1_gate_2026-09-18.md`、`readout_et_solo100k.md`；核实源码 `rl/follower.py`、`rl/belief_planner.py`、`rl/plan_space.py`；编排三路只跑出一路（已如实标注，其余由主体补齐并回源码核实） |
| `exploration_randomization_analysis_2026-09-18.md` | **探索侧「随机×随机 + 位置随机 + 正弦衰减」可行性取证（2026-09-18，用户提问；只分析不实现）**：**字面方案（逐帧 i.i.d. 随机）被定量否证**——攒到 Xbow 是**合取**事件（连续 34 帧不花钱，回费 0.1786/帧；其中圣水 ≥3 的帧必有合法出牌项，因为本卡组任意 4 张手牌最便宜那张 ≤3）⇒ 成功率 `p_pass^k`。**解析上界**（只读两臂 155,866 帧录像，9,956 次攒费尝试/100k 帧）：真正的「从低圣水攒到 6」（`k≥17`，7,582 次/100k 帧）= **4.5×10⁻¹⁰ 事件/100k 帧**（`p=1/6`）、`p=1/3` → 5.9×10⁻⁵、**放宽到荒谬的 `p=1/2`** 也只有 5.8×10⁻²、现状 `p=0.07` → 1.8×10⁻¹⁶（与 C14 的 0 一致）；合计 5.85 中 98% 来自 `k=5–9`（普通波动，非攒费）。**引擎蒙卡**（同 ckpt / 同 8 局）：把随机开到**最满**（`uniform` 臂）**2,084 帧里圣水一次没到 6（峰值 5.36，`≥25`/`≥34` 帧的不花钱段均为 0）**；换成**带时长**的 `hold d~U{1..40}` ⇒ **≥6 帧 28.96%/38.81%（两遍）**、`≥34` 帧段 **978 次/100k 帧**（差 ~12 个数量级）⇒ 与骰子分析同构：**只有 option 有时域能力**。**位置**：落点本来就在采样 ⇒ 实际是**均匀化**；实测策略落点**高度集中**（18,040 次部署只用 84/90 格、熵 4.5/4.2 bits ≈20 有效格、top10 占 73%/78%）而合法区 ≈250 格 ⇒ 剧烈改动 + 给「打哪张牌」注入**结果噪声**（与目标反向）。**正弦/衰减**是训练步上的调度 ⇒ 对局内合取**零作用**，且 `ratio≠1` 被 clip **裁掉梯度**；「概率也随机」比「恒取最大」还差 ~15×。含 **PPO 实现三写法对照**（唯一推荐「混合即策略 + 记 `log π_train` + `p=0` 逐位回旧」）、**RNG 不落盘 ⇒ resume 不可复现**、以及替代手段对比（抬 `ent_coef` 零新代码但一样无效 / `hold` option / **机制侧 O7 最贴病根**） | 仪器 `scripts/probe_explore_randomization.py`（`--mode analytic/engine/placement`）；留证 `docs/probe_explore_randomization/`（`analytic` / `engine` / `placement` 三份 txt+json）；台账 **O10** |
| `exploration_bias_pass_2026-09-18.md` | **偏置随机（第二问）：抬高「不出牌」/「大费牌」概率（2026-09-18，用户追问；只分析不实现）**：**① 「提高不出牌概率」是唯一正确的杠杆，且真的有效**——要的是 `k≈17` 帧的合取 ⇒ 逐帧概率 `1/6→q` 把成功率抬成 `q^k`（**每帧 5 倍 → 覆盖率 12 个数量级**）。解析（只读录像）：`q=0.9` 在「真正从低圣水攒到 6」桶 = **1,264 事件/100k 帧**（等概率 4.5×10⁻¹⁰）；实测同 ckpt/同 8 局：`uniform` **0.000%** 帧 ≥6 圣水（峰值 5.36）→ 偏置 `q=0.9` **18.9%**（峰值 10.00）。**② 且能零 off-policy 地实现**：直接给 STOP logit 加正偏置 β（`π_θ` 自己变「爱不出牌」）⇒ `ratio≡1`、无重要性权重、无 clip 错配；剂量-反应 β=0/4/5/6/7 ⇒ ≥6 帧占比 **0.000%→0.178%→12.70%→51.27%→82.98%**；仓内已有静态旋钮 `stop_logit_bias=-1.0`（`follower.py:156/253-255`，仅初始化生效）。**③ 「提高大费牌概率」方向是反的**：实测 α=1.5 把 ≥6 帧占比 **18.9%→12.1%（−36%）**、最长不花钱段 75→48；漂移公式 `0.1786−(1−q)·E[卡费]` ⇒ 盈亏平衡 `c*=1.786`。它只对「已攒到 6 费后把费花在 Xbow 上」有用，而那需要**状态条件** = 手写规则 = O7。**★④ 但覆盖率 ≠ 会学（本文最重要的限定）**：这些攒费轨迹的**每局回报从 −4.46 掉到 −41.5（β=6）/ −54.1（β=7）** ⇒ 采样到了，PPO 会学到「别这么干」（攒费期挨打 + 省下的费没用出去：292 帧圣水≥6 只打 5 次 Xbow）；γ 折扣已排除为瓶颈（`0.99¹⁷=0.843`、`0.99³⁴=0.71`）。含三种写法的梯度权重 `w` 与「覆盖率×w」表（on-policy `w≡1` 严格最优）、以及⚠️ **我自己的仪器 bug 留档**（`xbow_plays` 计数曾静默恒 0，修前 JSON 的该字段不可引用） | 仪器 `scripts/probe_explore_randomization.py`（新增 `bias_<q>_<α>` 与 `名@β` 臂）；留证 `bias_proposal` / `bias_onpolicy` / `bias_cards` 三组 txt+json；台账 **O10** |
| `exploration_pressure_gate_2026-09-18.md` | **状态条件随机（第三问）：低压时才随机（2026-09-18，用户追问；只分析不实现）**：**方向对且实测买回了回报**——前两轮的致命伤是攒费轨迹回报差（未门控 β=6 ⇒ 每局 **−56.13**）。**① 前提成立**：门 `d`＝最近 15 帧没掉塔血 ⇒ 开 **60.9%** 帧、最长 **544** 帧、**838 个窗口/100k 帧**够攒到 6 费；门 `h`＝半场无敌 ⇒ 44.7%／195／752；而**码内 `PRESSURE_THRESHOLD=2.0` 只开 4.7%**、280 局仅 1 个 ≥34 帧窗口 ⇒ 用它等于没有。**② 引擎实测（同 ckpt／同 8 局）**：基线 `0.000%`／`+1.63`；未门控 `48.209%`／`−56.13`；**`gate@6:d` `6.765%`／`+1.68`**；`gate@6:h` `0.385%`／`−13.21`；`holde@40:d` `16.131%`／`−38.04` ⇒ **门控把崩坏扭回基线、同时把 ≥6 帧从 0 抬到 6.8%**。**③ 代价**：覆盖率 ÷7，比离线预测 ÷1.6 更贵（**持牌推高压力** ⇒ 引擎门开启率 24.2%／42.8% ＜ 离线 44.7%／60.9% ⇒ 离线是**乐观上界**、非外生）。**④ 两个限定**：基线回报跨 run 摆动（+1.63 vs −4.46，O5）⇒ 回报差异不可当判据；`gate@6:d` 攒到 6 费 133 帧却 **Xbow 出手 0** ⇒ 「省下的费没花出去」仍未解决（仍要 O7）。**★⑤ 我自己的推理错误留档**：平均场估计（17 帧当独立抽签 ⇒ 掉 10 个数量级）**错了**——门状态强自相关、必须拒绝的帧连续 ⇒ 代价由**窗口长度分布**决定（实测 ÷1.6~÷4.6 离线／÷7~÷125 引擎）。含建议形态（`slot_logits[STOP] += β(t)·1[低压]`，on-policy、`p=0` 逐位回旧）与两段判据／失败分支 | 仪器 `scripts/probe_explore_randomization.py`（`--mode pressure`／`--gate-mode`／臂 `gate@β:门`、`holde@D:门`）；留证 `pressure`／`gated_{h,d,t}`／`pressure_gate`；台账 **O10** |
| `rand100_eval_2026-09-18.md` | **「初始模型 + 全过程随机」100 局评估 + dashboard 观测（2026-09-18，用户直接要求的简单测试）**：p0 = **全过程均匀随机**（合法项 × 合法格）、p1 = **初始模型 `solo_main_0.pt` 的确定性副本**（argmax）；**100 局 / 31,491 帧 / 1,368 s**；录像走**生产同一写入路径（schema 5）**。读数：**58W/42L/0D = 58.0%±4.9**、局均 314.9 帧、空 bundle **90.4%**、每帧出牌 0.097。**★ 从录像独立复算**（既有只读仪器）：A/B/C = **90.4% / 0.0% / 9.6%**（**B 整包被拒 = 0**，掩码修复在 31,491 帧上的端到端复核）、**A ∩ 圣水≥6 = 0**、**圣水峰值 5.357 = 开局 5.0 + 2 帧回费**（只在每局第 3 帧）⇒ **100 局里圣水从未超过开局水平**、A 段最长 24 帧、**Xbow 0 次**；落点 **484/576 格、熵 8.47 bits ≈356 有效格**（对比训练策略 84/90 格 / 4.5 bits / ≈20 格）。**dashboard → http://127.0.0.1:8701**（solo 曲线 + 对局回放播放 + 卡牌使用统计）。⚠️ 局限：p0 是随机 actor 无 logprob（录像不能用于训练/BC）、对手与 p0 同源（胜率不含绝对强度）、面板行为列留空 `–` | 仪器 `scripts/random_eval_100.py`；证据 `docs/rand100_eval_2026-09-18/`、`docs/random_eval_100.log`、`docs/dashboard_rand100_eval.log`；run 目录 `src/clasher_new/runs/rand100_eval/`（`runs/` 已 gitignore）；台账 **O10** |
| `illegal_action_layers_2026-09-18.md` | **「非法动作」是怎么定义的 —— 四层取证（2026-09-18，用户提问；只读取证、不改代码）**：**四层并列且互不推导** = **L1 掩码层**（`rl/action_mask.py` 全部规则 + `env_wrapper.py:473-536` + `follower.py` `masked_fill(-1e9)`；只产 `bool`、**不产 reason**）→ **L2 整包校验**（`validate_bundle` `action_mask.py:521-573`，**9 条中文 reason**，**任一非法则整包拒收**）→ **L3 惩罚/统计**（`invalid_penalty=0.05`（`env_wrapper.py:50`/`config.py:58`；预设 `defensive` = 0.1）× `invalid_count`，`env_wrapper.py:262-263`；**两个来源** = 整包拒 **恒 1**、**引擎拒逐卡 +1**）→ **L4 引擎物理**（`battle.py:2702 → 2996-3090`，**失败一律静默 `return False`、无理由串**）。**★ 10 条冲突并列不调和**（§6）：掩码**过严**（C1 MergeMaiden 静态 6 vs 引擎动态 3/6；C6 **Miner 全区豁免** —— 掩码层 `grep Miner` = **0 命中**）、掩码**缺口**（C2 BarbLog 限部队区，掩码对 `type=="spell"` **提前 `return True`**）、**作用域空隙**（C3 8h 不裸下：掩码只管**空 bundle 首卡** vs L2 只管**整包恰好 1 张** ⇒ 2 卡包首卡裸下**两层都不拦**）、**RL 层专有约束**（C4 法术三道闸门 7h/8h/9h 引擎**完全不校验**；C10 坐标越界是 RL 专有）、**统计错位**（C5 `evaluate.bundle_illegal` **看不见引擎级拒绝**）、**同名两种基**（C9 `used` L1 0-based vs L2 1-based）。**★ 编排者自证新增 F1**：**MergeMaiden 掩码层同时「费用过严 + 位置过松」（方向相反）** —— 掩码查 `Card("MergeMaiden")`.type = **`spell`** ⇒ `_position_legal:394-405` **提前放行**（占位/王塔身后/六条区域/后排几何**全跳过**），而引擎实际部署的 `MergeMaiden_Mounted/_Normal` 是 **`character`** ⇒ 六条区域规则**全部适用**；根因 = `_effective_card`（`:39-43`）**只处理 Mirror**。**★ F2 暴露面切分**：三冲突卡都**不在** `DEFAULT_SOLO_DECK`；BarbLog **在** `FOUR_DECK_SET`（`opponents.py:44`）⇒ **solo 实时暴露**；Miner/MergeMaiden 只在 `build_card_pool()`（139 卡、「全随机」agent，`run_league.py:795-812`）⇒ **solo 下潜伏**；`run` 模式 p1 侧非法动作**不被 L3 计数**（`flow_league.py:323` 硬编码 `invalid_count=0`）⇒ **静默**。**★ 真实日志读数（自证复跑）**：`P1-20`（validate 通过但引擎拒绝）在 `docs/*.log` 共 **1,644 行，其中 BarbLog 1,644（100.0%）、其它卡 0**，与 C2 预测**逐项一致**。⚠️ 限定：1,644 是**日志行数不是独立事件数**、累计 **1644×0.05=82.2** 是**跨 5 份日志之和**、**描述性非判据**（【R3】）；**未定 U8–U12** 未补全 | §10 = **编排者独立复核 + F1/F2 + 日志读数**；台账 **O9**（P2 已量化） |
| `structure_optimization_plan_2026-09-19.md` | **全项目结构盘点 + 分级优化方案（2026-09-19，用户要求「做一次全项目的代码结构优化」；盘点全程只读）**：**规模** = **199 个 `.py` / 62,402 行**（`src/clasher_new/` 91/35,565、`scripts/` 102/26,494）；**9 个 >700 行 God file**（`rl/selftest.py` 5,909 / `battle.py` 3,457 / `rl/dashboard.py` 3,173（其中 `_HTML` 内联 **1,879 行**）/ `card_mechanics.py` 1,895（**58 个类**）/ `rl/run_league.py` 1,787 / `rl/train_solo.py` 1,748 / `belief_planner` 891 / `env_wrapper` 811 / `follower` 806）；**分层** = **引擎→`rl/` 反向边 0**（语法层干净）但 **14 个 rl 模块直插引擎内部无门面**、**rl 内 429/572 条 import 在函数体内（75%）**；**★ 最贵真问题 = cwd 隐性契约**（`card_utils.py:4-17` 模块顶层裸相对路径 `open('gamedata.json')`，而它是全仓 importer 最多的模块 20 个 ⇒ import 任何 rl 模块都隐含要求 `cwd=src/clasher_new`）；**测试布局** = `rl/selftest.py` **100 个测试靠 `main()` 手工登记** + **4 条硬约束**（含 `_apply_s2_channel_when_idle.sh:70-71` 外部硬编码 4 个测试名）、测试**三处并存**（100 / 11 / **98**）、`scripts/` **一次性取证占 64%**、`env.md §2.4` **69 个脚本未登记**；**死件 11 条**（全部 0 importer 实测）。**方案分 Tier 0–3 共 21 项**（每项：做什么/涉及文件/行为中性理由或风险/验证脚本/并发冲突等级）；**Tier 0 四项零行为变化**、**Tier 2/3 明确标注「并发期间禁止」**（撞 `config.py`/`engagement.py`/`battle.py`/`selftest.py`）。含 §7 **碰不得 6 类 + 拒绝做 10 条**、§8 **7 组冲突并列表**（含「一个函数到底 1937 行还是 50 行」的粗算法 vs AST 真矛盾） | 分级方案 §6；规模 §1；God file 簇 §2；重复死码 §3；耦合 §4；测试布局 §5 |
| `structure_cleanup_2026-09-19.md` | **Tier 0 执行记录（2026-09-19，用户拍板「全做 + 死件直接删除」）**：**T0-1 死件清理 31 个**（3 tracked = `re_lib.py`(0 B) / `pathfinding.py`(0 importer) / `scripts/stop_solo_training_2200.py`；28 untracked = 根目录 `1`/`.p6.png`/`0631.pdf`/`libg_strings.txt`(789 KB)/`:memory:.ses` + `tmp_*.py`×5 + `gamedata.json.bak_ronin_vines`(341 KB) + `stop_solo_2200.log` + `runs/_tmp_*`×18）—— ⚠️ 未跟踪的**删了不可恢复** ⇒ 逐条留 **sha256+字节+判据**（§2）；**删前每条都由本人重跑 0-importer 复核**。验证：`--list` **100 不变** / 子集 **5/5 PASS** / 引擎顶层 **34→28** 文件 / **死件复活 0**。**T0-2** 新增只读核对器（7 项，含**引擎→rl 反向边=0**、硬编码绝对路径 **9 处/7 文件**、`_force_utf8_stdout` **三处互异**）；`--selftest` **9/9 PASS** 含**反向验证**。**T0-3** 新增 `agents/scripts_inventory.md`（`env.md §2.4` 只登记 34 / 顶层实有 91 ⇒ **57 未登记**）。**T0-4** `card_utils` 数据缺失时报**期望 cwd**（**故意不改 cwd 契约**，留给 T3-2），**A/B 用 `pickle` 逐字节指纹 7/7 SAME**。含 §10 **本次踩到的 4 个工程坑**（f-string 嵌套引号 / `/tmp`→`E:\tmp` / `json.dumps` 循环引用 / 全树 grep 超时） | T0-1 明细 §1–§5；验证 §6；T0-2/T0-3/T0-4 §7–§9；留证 `docs/structure_after_2026-09-19/baseline.json` |
| `agents/scripts_inventory.md` | **`scripts/` 全量登记表（T0-3，脚本生成不手抄）**：顶层 `scripts/*.py` **91** 个，其中 `env.md §2.4` 已登记 **34**、**未登记 57**。每个脚本给出 docstring 首行 / 是否含 `--selftest` / `assert` 数 / 是否已登记，分 4 组（内部工具 `_` 21 / 测试 6 / 测试·带自检 3 / 一次性取证·仪器 61）。⚠️ **两套口径并列**：本表按 91 个 `.py` 算；盘点稿的「69 个未登记」按含 `.sh/.ps1/.js` 与 `scripts/rl/*` 的 **105** 个文件算 | 生成依据 `docs/agents/env.md` §2.4 + 各脚本 docstring |
| `structure_tier1_2026-09-19.md` | **Tier 1 执行记录（2026-09-19）**：**T1-1 UTF-8 兜底收敛** —— 先**纠盘点稿一处不准确**：「3 处函数体已逐字相同」**实测不是**（`run_league`/`dashboard` 强版**体逐字相同**、`probe_value_ln` 是**弱版**：只碰 stdout、**无 `errors="replace"`**、不管 stderr）。新增 `rl/io_bootstrap.py::force_utf8_stdout()`（两路 + replace、幂等、吞异常、**零依赖**）；3 处 definer 改**别名** ⇒ **调用点一行未动**；核对器 ② 升级为**三阶段可读**（否则收敛后成**空检查**）⇒ 实测 **定义 1 处 / 别名 3 处**；回归 `scripts/selftest_io_bootstrap.py` **12/12**。⚠️ **我自己的仪器 bug**：② 原先把 **docstring 算进指纹** ⇒ 误导性的「3 种互异」，已拆 `body_digest`/`full_digest`。**★ T1-1b 部分收敛 + 明确不做**：**61** 个内联 `stdout.reconfigure` 调用点只收敛 **29** 个（codemod 留档），**17 无 guard + 15 形态不匹配不做**（收敛需**新增 `sys.path`** ⇒ 改 import 解析顺序，收益只 DRY）⇒ **32 个文件仍有 stderr 乱码洞**（实测：`duel_search` 的 traceback 就是乱码）。**T1-2** 硬编码绝对路径 **9→0**；⚠️ **限定**：`duel_search --help` 在仓库根 cwd **仍失败**，但那是 **cwd 契约**（T3-2）**不是回归**（`git show HEAD:` 副本报**同一个错**；能看懂原因正是 T0-4 的收益）。**T1-3** selftest 加 x/y 计数与耗时 —— 做法是**入口替换 `globals()` 里的 `test_*` 为计时包装** ⇒ `main()` 那 **100 行逐字未动**（AST 对比 HEAD 序列 `True`）；`--order-check` 报三种顺序差集（集合相同、顺序两两不同）；⚠️ **偏离方案**：改用**独立开关**而非改 `--list`（后者有外部消费者）。**T1-4** `tempfile` + `finally`（跑后实测无 `runs/_tmp_drawtest`）。**T1-5** 假绿 SKIP → `[SKIP-NO-CKPT]` + 计数（用**打桩空 ckpt** 真跑分支验）；⚠️ 不改绿/红语义。合并回归：子集 **10/10**、`check_dashboard_js.py` **FAILURES=0**、核对器 `--selftest` **13/13** | §1 含 §1.4/§1.5；§2–§5；§6 未决；§7 工程坑 |
| `structure_tier2_2026-09-19.md` | **Tier 2 执行记录（本轮完成 T2-1 / T2-2）**：**T2-1** `dashboard.py` 的 `_HTML`（**1879 行 / 80,974 字符**内联页面）→ `rl/dashboard_html.py`，用 `ast.get_source_segment` 取**精确源片段**生成 ⇒ 值 **sha256 逐字相同**（`7dcbde46…`，且**同一对象**），`dashboard.py` **3,171 → 1,296 行**。⚠️ **抽走后 `scripts/check_dashboard_js.py` 立刻失败**（它按**源码文本**找 `<script>`）⇒ 被它自己抓到，改为**三层回退**（`dashboard_html.py` → `dashboard.py` → `import rl.dashboard._HTML`）后 **FAILURES=0**。**T2-2** 数据面（4 常量 + 4 缓存 + 24 函数）→ `rl/dashboard_data.py`，`dashboard.py` **1,296 → 566 行**；搬前先实测**三条可行性**（`Handler`/`main` 不直接读缓存、唯一 `global _DECK_INDEX` 与函数同模块、外部按 `dash.<名>` 用 ⇒ 显式 re-export）。**★ A/B 差点被误判**：直接比 payload sha256 得 **4 个 DIFF** ⇒ 先做**易变性取证**（同码跑两遍，唯一差异键 = `updated_at` 时间戳）⇒ 再做**真 A/B**（`git show HEAD:` 的旧模块 vs 新模块，两边都剥掉 `updated_at`）⇒ **4/4 SAME**。回归：selftest 子集 **4/4**、前端 **FAILURES=0**。**未完成项如实列出**（T2-3…T2-8 与 Tier 3；T2-7/T2-8/T3-1 标注**并发期禁止**）⚠️ 口径留档：`build_payload`/`build_solo_payload` 因**运行中面板占文件**报 `PermissionError` ⇒ **本轮无 A/B 覆盖** | §1 T2-1；§2 T2-2（§2.3 A/B）；§3 规模；§4 未完成/未决 |
| 同上（续） §6–§7 | **T2-3** 判定语义半簇 → `rl/league_rules.py`（**8/8 逐字相同**；**评估并行半簇不搬**：依赖 `_ET_MEASURE` 会成环，且 `selftest.py:5298` 断言 worker 必须留在 `rl.run_league`）；**T2-5** `card_mechanics.py` **1,896 → 861 行**（M8 Elite17 段 1,040 行 → `card_mechanics_elite17.py`，**公开命名空间 65 名逐名相同 / 泄漏名同对象**；**刻意不改成包** —— `battle.py:5` 是 `from card_mechanics import *`，改成包未必能复现原来的泄漏语义；加了「不能独立导入」守卫）；**★ §7 意外收获**：`test_m3_evo.py` **12 个失败里 10 个是 GBK 编码假失败**（测试名含 ⑪⑫…，`print` 抛 `UnicodeEncodeError` 被记成「异常」）⇒ 给 **13 个入口脚本**补 UTF-8 兜底后 **39/12 → 60/2**（余 2 个真实行为失败，**已用 `git stash` A/B 证明是既有的**）；含**我自己插错代码 8 个文件**的踩坑留档（跨行 `sys.path.insert` + 属性级数假设） |
| `structure_tier3_2026-09-19.md` | **Tier 3 执行记录（本轮完成 T3-2 Stage A）**：**取消 cwd 契约** —— `card_utils._resolve_data` 改为 **`__file__` 优先**（与一直正确的 `card_aliases.py:432`、`pathfinding_heap.py` 看齐；`card_utils` 本是全仓**异类**，却因 **20 个 importer** 把 cwd 契约定格成全仓隐性约束）。验证：派生数据 **pickle 逐字节 7/7 SAME**；**从仓库根**跑通 `import battle`（`Card('Xbow').elixir=6`）、`duel_search.py --help` **rc=0**、`assassin_left_bridge_test.py` 真跑引擎 —— 三条改前**全部失败**（且 traceback 是 GBK 乱码）。**Stage B（搬 `*.json` → `assets/`、16 个 `.pt` → `runs/`）明确不做**：实测引用面 `.pt` **34 个文件**、`gamedata.json` **~20 份文档**，而契约修好后搬文件**零功能收益** ⇒ 代价/收益不对称（方案把 T3-2 标为极高风险是对的，**但风险全在不必要的那一半**）。含 T3-1/T3-3/T3-4 未做的逐条理由 |
| 同上（续） §9 | **T2-8 拆 `rl/selftest.py`（6,023 → 157 行）**：100 个测试按**定义序**切成 `rl/selftests/part{1..5}.py`（各 20）+ 共用底座 `rl/selftest_common.py`，聚合文件只留 `main()`。**拆前先做可行性审计**：`__file__` 仅 3 处、测试只依赖 6 个模块级名字、0 个 test→test 调用。**四条硬约束未破**（`main()` 序列 vs HEAD **100/100 逐字相同** / `--list` **100** / `--order-check` ① OK / 4 个外部硬编码名可达）。**验证：全量套件 100 通过 0 失败 0 跳过、`ALL SELFTESTS PASSED`、218.5 s**；跨 5 片子集 10/10；核对器 ⑦ **100/100**。含**6 处必改**（`_instrument_tests(namespace)` / 导入期注册命名空间 / 定义序排序键改 `(模块,行号)` / 两个 `__file__` 测试改 `_PARENT` / `functools.wraps` / **聚合文件顶部 path 引导** —— 缺最后一个**全量套件根本起不来**）；★ **我这次漏了 3 次，全由仪器抓到** |
| `structure_remaining_2026-09-19.md` | **结构优化 · 剩余 6 项逐条判定（2026-09-19）**：Tier 0/1/2（除下列项）与 T3-2 Stage A 已落地；**剩余 6 项全部被判「不做」并逐条留证** —— **T2-3 评估并行半簇**（被**既有 test 契约**挡住：`selftests/part5.py:1123` 断言 `_eval_pair_worker_main.__module__ == "rl.run_league"`；spawn pickle 路径）；**T2-4**（§7.1【R2】碰不得 + **无逐位对账**：CUDA 同配置重跑单点差 **0.60**）；**T2-7**（§7.1【R2】【R7】+ §7.2 **#8 并发期禁止**；依赖面 **8 文件**）；**T3-1 拆 `battle.py`**（§7.2 **#3 拒绝做**：【R13】128 张位图证不了全局等价）；**T3-3 迁 `tests/`+pytest**（§7.2 **#2 拒绝做**：【R19】+ 2 个硬编码调用方；且 **T2-8 已解决其动机**）；**T3-4 统一 `sys.path`**（124 文件；「重复」实为**两个不同用途** —— 包导入 vs **脚本入口**：`python rl/x.py` 时 `rl/__init__.py` 不执行 ⇒ 统一会打破脚本入口） |
| `agents/structure.md` | **AGENTS 分册 · 全项目结构优化（盘点 + Tier 0/1/2/3 执行记录）**：从 `AGENTS.md` §B 迁出的**逐字原文**（三行合计 ~12 KB）—— 因为 AGENTS.md 的**注入上限实测 65,244 B**（2026-09-13 曾因超限导致**末尾对会话不可见**）⇒ 按本仓纪律「索引留 AGENTS.md、细节进分册」。含**盘点**（199 `.py`/62,402 行、9 个 God file、**引擎→`rl/` 反向边 = 0**、**`card_utils` cwd 隐性契约**、selftest 100 手工登记）、**Tier 0 完成**、**Tier 1 完成**的完整读数与口径冲突 | `_agents_split.py --check` 的比对池已改为扫 **`docs/agents/*.md`**（原先只认固定 5 册 ⇒ **新增分册会被误判丢内容**） |
| `train_health_metrics_2026-09-18.md` | **训练健康指标：价值损失与策略熵（用户直接要求，2026-09-18）**：结论 = 这两个量**本来就在算、逐 update 打日志**（100k ≈ 781 点），是 `solo_state.json` 的 32 键里**没有**才在 UI 上看不见；**口径红线** = 熵是「掩码后、一次决策内各 decoder 步熵之**和**」、价值损失**必须用 `vraw=`（原始 MSE）** 而非 ÷`v_scale²` 的 `value=`（A_et 实测 `1.75→24.87`，只看它会读到"损失在降"的假象）；**A_et 实测**熵 **0.596→0.322 全部发生在头 ~20–30k 步**、其后 70k 步平台（`diff=-0.0568` vs `3×MAD=0.1988`，`snr=0.86`）；⚠️ **熵平台 ≠ 性能平台**（`ent_coef` 固定本就趋于平衡点 + n=1 胜率层无分辨力）⇒ 只能写"观察到熵已平台"；含判据预注册须写死的 6 项、禁止事项、三条工程坑（**GBK 新证据：`PYTHONIOENCODING` 实测不生效**；仓库根推错一层被新回归抓出） | 工具 `scripts/health_curve.py`（`--selftest` 5/5）、`src/clasher_new/rl/train_health.py`、dashboard `/api/health` |
| `agents/`（目录） | **AGENTS 分册（2026-09-18 拆分，逐字未改 + 节号不变）**：`redlines.md`（19 条红线全文与事故证据）、`env.md`（§2 环境与命令）、`metrics.md`（§3 训练口径与常量）、`ledger.md`（§4 已确证 / §5 已否证 / §6 未决）、`plans_runs_docs.md`（§7 计划 / §8 run / §9 文档地图）、`structure.md`、`reward_surveys.md`、**`agents_long_entries.md`**（**2026-09-19 v2 上线**：`AGENTS.md` B 区 10 行 + C 区 6 条正文的**逐字**下沉件，§1–§16；含 8 处「原文与目标文档不一致/缺口」的备注） | 根 `AGENTS.md`（索引）；迁移与校验 `scripts/_agents_split.py` |
| `agents/_reorg_v1/`（目录） | **2026-09-19 AGENTS.md 整理留证**：上线前原文 `AGENTS.pre-v2.md`（61,545 B）、v1 草稿、B/C 两区抽取件（`row_L0*.txt` / `citem_*.txt`）、12 份事实取证记录（`verify_L0*.md` / `verify_C*.md`）、生成与复核脚本（`build_draft.py` / `build_v2.py`，`--apply` 直接上线）、两份报告（`REPORT.md` / `REPORT_v2.md`） | `REPORT_v2.md` §3 校验 7/7、§4 C 区 5 处缺口、§6 待拍板 3 项 |
| `../scripts/forensics_response.py` | 9j 响应率取证脚本（防守响应占比/延迟/落点距离，v2 修正坐标口径） | `AGENTS.md` 9j 节（结论存档） |

## 2. 外置工具（引擎侧确定性服务，2026-09 起步；设计与优先级见 `../AGENTS.md`）

| 工具 | 文件 | 语义 | 对账 selftest |
|---|---|---|---|
| ① 塔伤威胁计算器 | `../src/clasher_new/threat_calc.py` | "双方不再部署"下敌方现存部队 20s 内对我方各塔伤害 | `test_tower_threat_calc` |
| ② 交换模拟器 | `../src/clasher_new/simulate_exchange.py` | "我方现在打出这张牌"的反事实推演（none/script/fn 三档对手） | `test_simulate_exchange` |
| ③ 法术知识模块 | `../src/clasher_new/spell_module.py` | 法术引擎标定档案 + 落点估值 + 引擎对账口径 | `test_spell_module` |
| ④ 循环规划器 | 未实现 | 牌序 depth/过牌 ETA 特征（私有信息+确定性队列） | — |

## 3. 原始采集数据（`_` 前缀 / `.cdp*` / `.wikitext`，只读证据）

> **⚠️ 2026-09-14 已清理（用户指令「清除编写文档的中间文件」）**：本节列出的原始抓取文件
> （`_page_*` / `_elite_*` / `_fp_*` / `_evo_*` / `_fandom_*` / `_hero_*` / `_spell_*` / `_*_evo.txt` / 页面级 `.cdp*.js`，
> 共 **190 个 / 6.88 MB**）**已删除**。下表保留**命名约定与引用方**，供将来重新抓取。
> 重新抓取：`node scripts/cdp_extract.js`（通用页）、`node scripts/cdp_evo.js`（觉醒页）。
> 清单与保留项：`docs/docs_cleanup_2026-09-14.md`。

由 CDP 脚本从 Fandom Wiki 抓取，被上表 curated 文档按 `[Fandom]` 标注引用。**禁止手改**；
数值疑义以 `gamedata.json`（游戏快照）为权威，Fandom 只作机制参考（用户三原则）。

| 命名 | 内容 | 引用方 |
|---|---|---|
| `_page_*.txt`（+`_page_*_wiki.txt`/`_clean`） | Fandom 卡牌/机制页整页 | `evo_2025_new7.md`、勘误批报告 |
| `_fp_<卡名>.txt` | 卡牌策略页（"From Players"） | `data_integration_notes.md`、法术病理取证 |
| `_elite_<卡名>.txt` / `_elite_*.txt` | 精英卡逐卡页 + Hero 总览 | `elite17_spec.md`、`_elite_fandom_report.md` |
| `_evo_*.txt` / `_<卡名>_evo.txt` | 觉醒卡页 | `evo_2025_new7.md`、`evo_hook_gap_matrix.md` |
| `_fandom_b06_*` | 批次6 原始页 + wikitext | 勘误批报告 |
| `.cdp*.js` | CDP 抓取/解析脚本（页面级临时件） | `_elite_fandom_report.md` |
| `_digest_v3_for_master_plan.md`、`_digest_three_plans.md`、`_digest_five_plans_2026-09-13.md`、`_five_reports_merged_2026-09-13.md` | 2026-09-13 计划整合时**并行子智能体产出的原始 digest**（各自读一组计划文档后的结构化压缩），只读证据 | `plan_master.md` §6（出处） |
| `re/fandom_check/`、`re/fandom_stats/`（在 `.gitignore` 的 `re/` 内，不入库） | 批次校验报告与结构化 JSON | 勘误批/觉醒批 |

## 4. 数据文件（docs/ 下，curated 可引用）

| 文件 | 内容 | 消费方 |
|---|---|---|
| `card_registry.json` | 卡牌注册/覆盖状态 | `card_coverage.md`、`scripts/coverage.py` |
| `batch_smoke_report.json` | 全卡冒烟结果 | `scripts/batch_smoke.py` |
| `evolution_cycles.json` | 觉醒周期表 | `evolutions.py` |
| `leaderboard_decks.json` / `leaderboard_decks_classified.json` | 天梯 200 副卡组（推进 60/防反 120/自闭 20） | `rl/decks.py`、`rl/run_league.py`、`rl/opponents.py` |
