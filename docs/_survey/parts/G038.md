# 组 G038 文件级分析

> 说明：本组 12 个文件按简报顺序逐个读完（read 全文，无跳读）。所有「作用/实现」均带源码行号；
> 凡依赖的外部模块行为已读源码的写「已确认（依据 <文件>:<行>）」，读不到或不存在的写「无法确认（原因：…）」。
> 被 import 的模块读到的部分：`battle.py`（片段）、`card_utils.py`（片段）、`environment.py`（片段）、
> `rl/replay.py`（片段）、`rl/action_bundle.py`（片段）、`new_visualization.py`（片段）、`train.py`（片段）。

---

## F:src/clasher_new/threat_calc.py
- 行数: 106
- 模块作用: 外置工具①「塔伤威胁计算器」：在「双方都不再部署」的假设下，回答敌方现存部队（不冻结已有单位）对某一方防御塔能造成多少伤害。实现方式不是重新建模，而是 `copy.deepcopy` 整个 BattleState 后用引擎自身 `step` 确定性推演，索敌/攻速/位移/塔兵反击/国王塔激活/飞行物收尾全部由引擎结算（依据 L1-22 模块 docstring）。文件末尾自带 `__main__` 冒烟演示（L93-106）。
- 模块级 docstring 首行: `塔伤威胁计算器（外置工具 ①）：当前盘面下，若双方都不再部署，敌方现存`
- 主要导入: `copy`（L24）
- 关键模块级常量:
  - `__all__ = ["estimate_tower_threat", "THREAT_HORIZON_S"]`（L26）— 对外导出符号；注意 `_hostiles_present` 不在其中。
  - `THREAT_HORIZON_S = 20.0`（L30）— 缺省推演视界（秒）；注释理由：Giant 类坦克从桥头到拆完公主塔约 15-20s（L28-29）。
  - `_TOWER_IDS = {0: (3, 4, 6), 1: (1, 2, 5)}`（L33）— player_id → 该方三座塔的实体 id 元组。约定已与引擎对账：`battle.py:2556-2561` 依次 spawn Building(1)=RED_LEFT、2=RED_RIGHT、3=BLUE_LEFT、4=BLUE_RIGHT、5=RED_KING、6=BLUE_KING，故 P1(红)=1/2/5、P0(蓝)=3/4/6，与注释一致。
  - `_TOWER_NAMES = {3: "left", 4: "right", 6: "king", 1: "left", 2: "right", 5: "king"}`（L34）— 塔 id → 输出键名（left/right/king）。
- 顶层数据表/字典: 无（上述两个 dict 为常量映射，非可增长数据表）。

### S:threat_calc._hostiles_present  [L37-48]
- 类型: function
- 签名: `_hostiles_present(battle, player_id)`
- 作用: 判断盘面上是否还存在「仍可能威胁我方塔」的敌方存活实体（排除双方塔本身）。
- 参数:
  - `battle` (无注解): BattleState 实例；需有 `.entities`（id→实体 的 dict）。
  - `player_id` (无注解): 我方 player_id（0 或 1），函数用 `1 - player_id` 取敌方。
- 返回: `bool` — 找到任一满足条件的实体即 `True`，遍历完未找到返回 `False`。
- 实现: 先用 `_TOWER_IDS[0] | _TOWER_IDS[1]` 拼出全部 6 个塔 id 的集合（L39）；遍历 `battle.entities.values()`（L40），跳过 `is_alive` 为假者（L41-42，用 `getattr(..., False)` 容错）；跳过 id 属于塔集合者（L43-44）；对剩余实体检查 `getattr(e, "player", None) == 1 - player_id`，命中即 `return True`（L46-47）。注释明确「敌方单位/建筑/飞行物都算；己方残兵不算」（L45）。已确认 Projectile 也有 `player` 且进 `entities`（`battle.py:1418` 构造签名、`battle.py:745-747` 写入 `entities`），故飞行物确实会被计入。
- 调用: 被本模块 `estimate_tower_threat` 调用两次（L61、L74）。未在本组文件之外发现调用者（仓库内 `grep _hostiles_present` 仅命中本文件）。
- 置信度: 已确认

### S:threat_calc.estimate_tower_threat  [L51-90]
- 类型: function
- 签名: `estimate_tower_threat(battle, player_id: int, horizon: float = THREAT_HORIZON_S, dt: float = 1 / 60)`
- 作用: 返回某玩家在「双方不再部署」假设下未来 `horizon` 秒内的塔损预估 dict。
- 参数:
  - `battle` (无注解): 原始 BattleState；函数不修改它（deepcopy 后推演）。
  - `player_id` (`int`): 受评估的一方（0/1）；`assert` 限制。
  - `horizon` (`float = THREAT_HORIZON_S` 即 20.0): 推演视界（秒）。
  - `dt` (`float = 1 / 60`): 引擎步长（秒）。
- 返回: `dict`，键为 `left`/`right`/`king`（各塔损失 HP，`round(...,1)`）、`towers_lost`（推演期内被打掉的塔名 list）、`total`（三塔总损失，`round(...,1)`）、`sim_time`（实际推演秒数 `round(...,3)`；早停时 < horizon）。空场景返回全零 dict 但键结构相同（L59-60）。
- 实现: ①L58 `assert player_id in (0, 1)`；②L59-62 构造 `zero` 返回体；若 `battle.game_over` 或 `_hostiles_present` 为假则直接返回 zero（不推演）；③L64 `sim = copy.deepcopy(battle)`，L65-66 取 `tids = _TOWER_IDS[player_id]` 并记录三塔初始 hp 到 `hp0`（`float`）；④L68-77 主循环：`t < horizon - 1e-9` 期间反复 `sim.step(dt)` 并累加 `t`；循环内有三个早停——`sim.game_over`（L70-71）、敌方威胁源被清空（L74-75，注释：剩余推演不会产生新塔损）、我方三塔全破（L76-77）；⑤L79-89 汇总：对每座塔取 `sim.entities[tid]`，损失 = `max(0.0, hp0 - 当前hp)`，按 `_TOWER_NAMES` 命名写回；若 `not e.is_alive` 则追加进 `towers_lost`；累加 `total`（注意：`out[name]` 是每塔单独 round 后的值，而 `total` 是对未 round 的 `dmg` 累加后再 round，故 `total` 不一定等于三项之和）。
- 调用: 已知消费者只有 `rl/selftest.py:2732/2743/2754/2761/2769`（`test_tower_threat_calc`，被 L4950 调用）；`simulate_exchange.py:5-13` 只在 docstring 里声明与 threat_calc 的语义关系（`none` 模式当对照下界），未 import 本函数。模块 docstring L20-21 声明的 belief_planner/prophet/RL 观测通道消费方**在该仓库中未找到调用点**（`grep estimate_tower_threat` 仅命中 selftest 与本文件）⇒ 属「规划内但未接线」。
- 置信度: 已确认
- `__main__` 冒烟块（L93-106，非符号，附带说明）: 用 8 张固定卡组构造 BattleState，先打印空场威胁（全零分支），再把 P1 cycle 首位换成 Giant、给 10 费、在 `Position(14.5, 19.0)` 部署 Giant，打印有威胁时的读数。属演示代码，非测试断言。

---

## F:src/clasher_new/run_raw_capture.py
- 行数: 72
- 模块作用: 通过 frida 远程 attach 安卓模拟器上的 Nulls Royale（`nullsroyale.rel.free`），注入 `hook_raw_capture.js` 脚本，把 hook 出来的 `runtime_snapshot` 消息喂给可选的可视化器或直接打印。连接、server 启动、pid 查找全部在**模块顶层**完成（L20-38），`mainloop` 只是后半段；frida 版本/接口为旧式 API（`frida.get_device_manager().add_remote_device` L26）。
- 模块级 docstring 首行: 无
- 主要导入: `pathlib.Path`、`time`、`subprocess`、`frida`（L1-5）
- 关键模块级常量:
  - `PACKAGE = "nullsroyale.rel.free"`（L15）— 目标游戏包名。
  - `FRIDA_SERVER = "/data/local/tmp/frida-server"`（L16）— 模拟器内 frida-server 路径。
  - `SECONDS = 300`（L17）— `mainloop` 里挂机采集的时长（5 分钟）。
  - `current_dir = Path('.')`（L18）— 相对当前工作目录；因此必须在含 `hook_raw_capture.js` 的目录下运行，否则 L20 `read_text()` 抛 FileNotFoundError。
  - `source = (current_dir / "hook_raw_capture.js").read_text()`（L20）— **导入即执行**的模块级副作用：读取 JS 注入源码到内存。
  - `device = frida.get_device_manager().add_remote_device("127.0.0.1:27042")`（L26）— 模块级建立远程设备连接。
  - `session = device.attach(pid)`（L33）— 模块级 attach 到游戏进程；`pid` 来自 L27-31 的 `adb shell pidof`，空则 `raise SystemExit`。
  - `sequence = 0`（L34）— 宣称的全局序号，但只被 `on_message` 的 `global sequence` 声明（L43）提及，函数体内**从未读写**。
  - `entities = {}`（L37）— 传给 visualizer 的实体表，函数内未填充。
  - `local_arena_side = None`（L38）— 同样只被 `global` 声明（L43），函数体内未读写。
  - `player_identity = None`（L39）— 同上，只被 `global` 声明，函数体内未赋值（实际赋值的是 `visualizer.local_player_index`，L55）。
- 顶层数据表/字典: `entities = {}`（L37）为空 dict，无键值语义可述。

### S:run_raw_capture.adb  [L7-13]
- 类型: function
- 签名: `adb(*args)`
- 作用: 以 `check=True, text=True, capture_output=True` 调用 `adb` 并把给定参数逐个透传。
- 参数:
  - `*args`: 传给 adb 的命令行参数（如 `"forward", "tcp:27042", "tcp:27042"` 或 `"shell", "pidof", PACKAGE`）。
- 返回: `subprocess.CompletedProcess`（`subprocess.run` 的返回值，含 stdout/stderr）；调用失败（非零退出）因 `check=True` 抛 `CalledProcessError`。
- 实现: 单条 `subprocess.run(["adb", *args], check=True, text=True, capture_output=True)`（L8-13）。`text=True` 使 stdout 为 str，这正是 L28 `.strip().split()` 能用、L31 `int(pids[0])` 能用的前提。
- 调用: 被模块顶层调用 4 次：L23（forward 27042）、L24（chmod 755 frida-server）、L25（后台启动 frida-server）、L27（pidof）。
- 置信度: 已确认

### S:run_raw_capture.mainloop  [L41-69]
- 类型: function
- 签名: `mainloop(visualizer=None)`
- 作用: 创建 frida script、注册消息回调挂机 `SECONDS` 秒，然后 detach。
- 参数:
  - `visualizer` (默认 `None`): 任何具有可写属性 `snapshot`、`local_player_index`、`entities` 的对象；为 `None` 时改为逐条 `print(payload)`。对象的具体类型在仓库内**无法确认**（`new_visualization.Visualizer` 的 `__init__` 只设置 `battle/paused/running/speed/scheduled`，`new_visualization.py:19-29`，没有 `snapshot`/`local_player_index` 属性 ⇒ 很可能不是该 Visualizer，但无其它候选定义可查）。
- 返回: 无（函数体无 return；结束时仅 `session.detach()`）。
- 实现: ①L42-58 内嵌 `on_message(message, data)`：非 `"send"` 类型消息直接 `print(message)` 返回（L44-46）；取 `payload = message["payload"]`（L47）；若 `payload['event'] == 'runtime_snapshot'` 且 visualizer 非 None，则把整个 payload 存进 `visualizer.snapshot`（L49-50），并解析 `payload.get('player_identity')`：当它是长度 2 的 list 时拆成 `local_id, id_lst`（L51-53），再当 `id_lst` 是 list 且含 `local_id` 时设 `visualizer.local_player_index = id_lst.index(local_id)`（L54-55）；随后 `visualizer.entities = entities`（L56，把模块级空 dict 引用赋过去——后续无人填充）；visualizer 为 None 时 `print(payload)`（L57-58）。②L60-62 `session.create_script(source)` → `script.on("message", on_message)` → `script.load()`。③L64-67 `time.sleep(SECONDS)`，`KeyboardInterrupt` 被吞掉（`pass`）。④L69 `session.detach()`。
- 调用: 模块 `__main__` 调用 `mainloop()`（L71-72）。反向调用了 frida 的 `device/session/script` 接口与 `time.sleep`。
- 置信度: 已确认（唯一悬置点＝`visualizer` 的真实类型，已在上文标注无法确认）

---

## F:src/clasher_new/tmp_spell_forensics.py
- 行数: 70
- 模块作用: **临时调试/取证脚本**（文件名前缀 `tmp_`、无 `if __name__` 保护）：从回放 pickle 重建手牌循环、识别其中的法术卡、对每次法术施放做落点命中统计与「账面收益」粗略估算。导入即执行（L59 起直接跑 `analyze('runs/aggressive/replays/league_2000.pkl')` 并逐行打印）。
- 模块级 docstring 首行: `法术施放法医分析：重建手牌循环 → 识别法术 → 落点命中 + 账面收益估算。`
- 主要导入: `os`、`sys`、`pickle`（L3）、`card_utils.Card`（L5）；L4 把脚本自身目录插入 `sys.path`
- 关键模块级常量:
  - `DECK = ['Knight', 'MiniPekka', 'Arrows', 'Minions', 'Musketeer', 'Fireball', 'Giant', 'Archer']`（L7）— 硬编码的 8 张卡组（与 `environment.player_0_deck` 同序，`environment.py:13`）；用于把 replay 的 slot 还原成卡名。**注意：这是写死的，若被测 run 的卡组不同则整段重建失效**（脚本无任何校验）。
  - `SPELLS = {c for c in DECK if Card(c).type == 'spell'}`（L8）— 卡组中的法术卡名集合。`Card.type` 来自 `self.data.get('tidType','').split('_')[-1].lower()`（`card_utils.py:293`），故 'spell' 是引擎口径的类型名。
  - `RADIUS = {c: (Card(c).data.get('projectileData') or {}).get('radius', 0) / 1000.0 for c in SPELLS}`（L9）— 法术半径，从卡数据 `projectileData.radius`（毫秒/千分单位）换算为格；缺失则为 0。
  - `COST = {c: Card(c).elixir for c in DECK}`（L10）— 卡费表；`Card.elixir` = `data.get('manaCost', 0)`（`card_utils.py:227`）。
- 顶层数据表/字典: 上述 4 个 dict/set 即数据表：`SPELLS`（卡名集合）、`RADIUS`（卡名→半径格）、`COST`（卡名→费用）。

### S:tmp_spell_forensics.analyze  [L12-57]
- 类型: function
- 签名: `analyze(path)`
- 作用: 读回放 pickle，按「每局重排手牌循环」的规则把每个 deploy 动作还原成卡名，对法术卡统计落点附近敌方 troop 帧数与疑似击杀、塔伤，返回逐次施放的元组列表。
- 参数:
  - `path` (无注解): 回放 pickle 路径的 str。
- 返回: `list`，每项 9 元组 `(gi, f['t'], card, x, y, COST[card], len(set(units_hit)), sorted(set(killed)), tower_dmg)`（L55-56），其中 `gi` 为局序号、`f['t']` 为施放帧时间、`units_hit` 是**帧次**（同一单位多帧会被 set 去重后计数，故语义是「出现命中的不同单位名数量」而非伤害次数，脚本打印文案写作「溅射内单位帧」）。
- 实现: ①L13 `pickle.load` 读入 `d`（含 `d['games']`）；②L15 遍历每局 `g`，L16 以 `DECK` 的副本作为该局初始手牌循环，L17 取 `frames`；③L18-57 逐帧遍历：对 `f['bundle']` 中每个动作，只处理 `act[0] == 'deploy'`（L20-21；依据 `rl/replay.py:110` bundle 元组为 `(kind, slot, x, y)`，`rl/action_bundle.py:53-55` 默认 `kind="deploy"`），取 `slot, x, y = act[1], act[2], act[3]`；只处理 `1 <= slot <= 4`（L23-24，与 `action_bundle.py:46` 「slot=1..4 打出 cycle[slot-1]」一致）；④L25 取 `card = cycle[slot-1]`，L26-27 把手牌移到队尾（若卡不在 cycle 会 `ValueError`，脚本无保护）；非法术则 `continue`（L28-29）；⑤L31-35 取 `r = RADIUS[card]`，并记录施放帧的 `prev_t1 = f['towers1']` 与 `prev_units`（筛 `e[4] == 1 and e[5] == 'troop'` 的 (name,x,y,hp) 四元组；字段位置依据 `rl/replay.py:112-121` 的实体条目定义 `[name,x,y,hp,player,kind,...]`）；⑥L36-54 向后看最多 4 帧（`frames[fi+1:fi+5]`，注释称约 2s）——(a) 对每帧敌方 troop 计算与落点的欧氏距离 `dist`，`dist <= r + 0.5` 则把名字追加进 `units_hit`（L38-42，`+0.5` 是放宽容差，脚本未说明依据）；(b) 用「名字计数减少」判定击杀：对 `set(prev_names)` 中每个名字，若前一帧计数 > 当前帧计数则 `killed.append(name)`（L44-50，**这是数量减少而非死亡事件，若同帧有新增同名单会漏判**）；(c) `tower_dmg` 取历史最大 `sum(prev_t1) - sum(nf['towers1'])`（L51-52，P1 三塔血量总和的下降量）；(d) 每轮把 `prev_units/prev_t1` 推进到当前帧（L53-54）。注意 `units_hit`/`killed` 在**帧循环外**初始化，故对同一次施放的 4 帧累积，`set` 去重。
- 调用: 被模块顶层 L59 调用一次；内部调用 `pickle.load`、`Card()`（经模块级表）与 `os.path.join`。
- 置信度: 已确认（回放帧结构已与 `rl/replay.py:110-121` 逐字段对账；`runs/aggressive/replays/league_2000.pkl` 路径实测存在）
- 顶层估算段（L59-70，非符号，附带说明）: L59 调 `analyze`；L60 打印总数；L62-69 对每次施放按硬编码公式 `est = -0.5*cost + 0.5*kill_cost + 0.001*tdmg` 累加并打印，`kill_cost = sum(COST.get(k, 3) for k in killed)`（L65，未知卡按 3 费兜底，L65 注释里提到的「单位受伤 ~+0.0005*总血」项**实际未实现**）。脚本自身注释称这是「账面估算」「粗略」（L63-64），系数无推导 ⇒ 该数值不应作为判据使用。

---

## F:src/clasher_new/core.py
- 行数: 69
- 模块作用: 引擎的「角色行为基类/占位实体/二维坐标」最小核心。`Position` 是全局坐标类型（dataclass），`BlankEntity` 只携带 position，`BasicCharacter` 是实体逻辑的委派 holder：`battle.py:50` 每个 Entity 构造时都会 `self.entity_holder = BasicCharacter(self)`，无特殊机制的卡（如 Knight/MiniPekka）直接复用 `BasicCharacter.on_attack`（依据 `battle.py:47-50` 注释）。`on_attack` 是全局普攻结算的唯一入口（`battle.py:1227` 调用）。
- 模块级 docstring 首行: 无
- 主要导入: `dataclasses.dataclass`、`math`（L1-2）
- 关键模块级常量: 无
- 顶层数据表/字典: 无

### S:core.Position  [L5-9]
- 类型: class
- 签名: `@dataclass class Position: x: float; y: float`
- 作用: 用两个 float 表示世界坐标点的不可变语义容器（实际非 frozen）。
- 参数: 字段 `x: float`（L6）、`y: float`（L7）。
- 返回: 构造返回实例；`distance_to` 返回 float。
- 实现: `@dataclass` 装饰（L4）自动生成 `__init__`/`__repr__`/`__eq__`；类体只有两个字段与一个方法。
- 调用: 被本组多文件使用（`threat_calc.py:97`、`tmp_target_verify.py:7`、`tmp_tower_chip_test.py:7`、`tmp_formation_test.py:7`、`tmp_path_debug.py:7`、`evaluate.py:4` 经 environment 再导入）；`battle.py` 内部大量使用。
- 置信度: 已确认

#### M:Position.distance_to  [L8-9]
- 类型: method
- 签名: `distance_to(self, other)`
- 作用: 返回本点到 `other` 的欧氏距离。
- 参数:
  - `other`: 任何具有 `.x`/`.y` 的对象（函数内只访问这两个属性，L9）。
- 返回: `float` — `math.hypot(self.x-other.x, self.y-other.y)`。
- 实现: 单行 `math.hypot`（L9）；无类型校验、无除零风险。
- 置信度: 已确认

### S:core.BlankEntity  [L11-14]
- 类型: class
- 签名: `class BlankEntity:`
- 作用: 占位实体，只编码 position（docstring L12 原文 "A placeholder that only encodes the position value."）。
- 参数: 构造参数 `position`。
- 返回: 实例。
- 实现: 类体仅 `__init__` 一个方法，无其它属性/方法。
- 调用: **（2026-09-14 独立校验后更正）** `battle.py:1` 导入它、`battle.py:1670` 与 `battle.py:2964` 两次实例化它（`self.target = BlankEntity(target_position)` / `target = BlankEntity(position)`，用作「占位/空目标」实体）；定义在 `core.py:11`。**原文曾写「仓库内 grep 只命中本文件 ⇒ 未发现在别处被实例化或引用」，该结论与仓库事实相反，已作废。**
- 置信度: 已确认（定义与无引用的事实）；其设计意图的消费方无法确认（原因：全仓无使用点）

#### M:BlankEntity.__init__  [L13-14]
- 类型: method
- 签名: `__init__(self, position)`
- 作用: 把 `position` 存为实例属性。
- 参数:
  - `position`: 任意位置对象，原样保存。
- 返回: 无
- 实现: 单行 `self.position = position`（L14）。
- 置信度: 已确认

### S:core.BasicCharacter  [L16-69]
- 类型: class
- 签名: `class BasicCharacter:`
- 作用: 实体行为的委派 holder 基类：持有 back-reference `entity`/`battle_state`/`data`，提供 `on_spawn`/`on_tick`/`on_death` 三个空钩子与普攻结算 `on_attack`、溅射圈判定 `_hit_by_area`。是 `card_mechanics.py` 中大量机制类的父类（同仓 `card_mechanics.py:8/66/89/95/105/113/166/175/202/218/261/269/315` 均 `class X(BasicCharacter)`）。
- 参数: 构造参数 `entity`。
- 返回: 实例。
- 实现: `__init__` 缓存 entity 与其 battle_state/data（L17-20）；三个生命周期钩子中 `on_tick` 会重新同步 `self.battle_state`（L22），`on_spawn`/`on_death` 为 `pass`（L21、L23）；`_hit_by_area` 是溅射判定的口径函数（L25-28）；`on_attack` 是全部伤害/弹道结算（L30-69）。
- 调用: `battle.py:50`、`battle.py:1783` 构造；`card_mechanics.py` 多个子类继承并 `super().on_attack(...)`；`battle.py:1227` 调用 `self.entity_holder.on_attack(current_target)`；`battle.py:292` 通过 `type(self.entity_holder).on_death is not BasicCharacter.on_death` 判断子类是否覆写了 on_death（说明「未覆写」是用基类方法对象做哨兵）。
- 置信度: 已确认

#### M:BasicCharacter.__init__  [L17-20]
- 类型: method
- 签名: `__init__(self, entity)`
- 作用: 绑定宿主实体并缓存其 battle_state 与 Card 数据。
- 参数:
  - `entity`: 宿主 Entity（需有 `.battle_state`、`.data`）。
- 返回: 无
- 实现: `self.entity = entity`；`self.battle_state = self.entity.battle_state`；`self.data = self.entity.data`（L18-20）。**注意 `self.data` 是构造时的快照引用**，而 `on_tick` 只刷新 `battle_state` 不刷新 `data`（L22）。
- 置信度: 已确认

#### M:BasicCharacter.on_spawn  [L21-21]
- 类型: method
- 签名: `on_spawn(self)`
- 作用: 出生钩子，基类空实现。
- 参数: 无
- 返回: 无
- 实现: `pass`（L21）。
- 置信度: 已确认

#### M:BasicCharacter.on_tick  [L22-22]
- 类型: method
- 签名: `on_tick(self, dt)`
- 作用: 逐帧钩子；基类只做 `battle_state` 重新同步。
- 参数:
  - `dt`: 帧时长（本实现未使用）。
- 返回: 无
- 实现: `self.battle_state = self.entity.battle_state`（L22）。
- 置信度: 已确认

#### M:BasicCharacter.on_death  [L23-23]
- 类型: method
- 签名: `on_death(self)`
- 作用: 死亡钩子，基类空实现（同时被 `battle.py:292` 用作「子类是否覆写」的方法对象哨兵）。
- 参数: 无
- 返回: 无
- 实现: `pass`（L23）。
- 置信度: 已确认

#### M:BasicCharacter._hit_by_area  [L25-28]
- 类型: method
- 签名: `_hit_by_area(self, target, center, radius)`
- 作用: 判断主目标是否落在以 `center` 为心的溅射圈内（与 `deal_area_damage` 同口径）。
- 参数:
  - `target`: 待判定目标（需有 `edge_distance_from`）。
  - `center`: 溅射圆心（Position）。
  - `radius`: 溅射半径（格）。
- 返回: `bool` — `target.edge_distance_from(center) < radius`（严格小于）。
- 实现: 单行 L28。口径依据 docstring L26-27：塔走矩形边缘距离、普通实体中心距 − collision_radius；已与 `battle.py:152-166` `edge_distance_from` 实现对账一致（塔矩形最近点距离；非塔 `pos.distance_to(self.position) - collision_radius`）。**注意参数名是 `center`，但调用方传的是自身位置**（L44-46 传 `self.entity.position`），即溅射以自身为中心。
- 置信度: 已确认

#### M:BasicCharacter.on_attack  [L30-69]
- 类型: method
- 签名: `on_attack(self, current_target=None)`
- 作用: 普攻结算总入口：记录攻击时间、按有无 area_damage_radius 分支结算溅射或单体（含对塔减伤系数）、或改走弹道、然后重置攻击冷却并触发觉醒/动作链钩子、最后处理 kamikaze 自毁。
- 参数:
  - `current_target` (默认 `None`): 当前主目标实体；`None` 时**不会**进入单体伤害分支（L43 与 L53 都直接访问 `current_target.name`，会 AttributeError ⇒ 实际调用点必传目标，已确认唯一调用点 `battle.py:1227` 传 `current_target`）。
- 返回: 无
- 实现: ①L31 写 `self.entity.last_attack_time = self.battle_state.time`（注释：M4 fortify 脱战判定）。②L33 条件「`self.entity.data.damage` 为真 **或** 实体有 `attack_seq` 属性」——覆盖 Berserker 等「伤害只在攻击序列内（data.damage=0）」的卡。③L34 伤害基数 = `self.entity.ramped_damage(self.data.damage)`（M1 递增伤害/M4.5 序列；`ramped_damage` 定义在 `battle.py:442`），L35 再乘 `getattr(self.entity, '_damage_mult', 1.0)`（勘误批7：Royal Chef 蛋糕 +1 级伤害载体）。④L36-51 溅射分支：调 `self.battle_state.deal_area_damage(self.entity.player, self.entity.position, self.data.area_damage_radius, damage, self.data.attack_air, self.data.attack_ground)`（`deal_area_damage` 定义 `battle.py:3250`）；随后 L43-51 做「保底主伤」——若 `current_target` 存活且 `_hit_by_area` 判其不在圈内，则按目标名是否含 'King' 或 'PrincessTower' 决定是否乘 `self.entity.data.tower_damage_mult`，再 `current_target.take_damage(..., delayed=True, source=self.entity)`（注释举例：MegaKnight 站公主塔边缘 1.69 > 溅射半径 1.3）。⑤L52-56 单体分支：同样按 'King'/'PrincessTower' 判定乘 `tower_damage_mult`（勘误批8：对塔减伤覆盖公主塔，Miner 25%）后 `take_damage(delayed=True, source=...)`。⑥L57-59 `elif self.entity.data.projectiles:` 走 `create_projectile(current_target)`（`battle.py:738`）——即「无直接伤害但有弹道」的卡（如法术塔/远程）走弹道结算。⑦L60 重置 `attack_cooldown = self.data.hit_speed`。⑧L62-63 若实体有 `_evo_on_attack` 则调用（M4 族7：骷髅分裂/蝙蝠自愈/野蛮人狂暴/弓箭手二段箭/RG 推击/女武神龙卷）。⑨L65-66 若实体有 `_on_attack_done` 则调用（M4.5：InfernoDragon_EV1 递增推进/Berserker 多段）。⑩L68-69 若 `data.kamikaze` 为真则直接 `self.entity.is_alive = False`（自毁）。L67 是注释，说明渔夫钩拉在 Fisherman 的 on_tick 里处理，不在此处。
- 调用: 唯一已知调用点 `battle.py:1227`；被 `card_mechanics.py` 众多子类 `super().on_attack(current_target)` 复用（如 L34、L631、L664、L851、L968、L1048、L1088 等）。
- 置信度: 已确认

---

## F:src/clasher_new/player.py
- 行数: 67
- 模块作用: 单方玩家状态的纯数据+少量规则容器：手牌循环（cycle）、圣水、三塔血量快照、镜像/觉醒/Hero/塔兵等机制的声明槽位，以及出牌合法性、圣水回复、皇冠数。塔血量字段由 `battle.py:2643-2650 update_player_hp` 从实体 id 1-6 单向同步进 PlayerState。
- 模块级 docstring 首行: 无
- 主要导入: `card_utils.Card`（L1）
- 关键模块级常量: 无（仅 L3 注释 `# Hardcoded: max elixir is 10`，实际上限 10 硬编码在 `regenerate_elixir` 的 `min(10, ...)`，L34）
- 顶层数据表/字典: 无
- `__main__` 演示块（L60-67，非符号）: 构造 10 费 PlayerState，打印 cycle/elixir，然后两次 `play_card('Knight')` 并打印——因第一次已把 Knight 移出前 4 张，第二次返回 False（`can_play_card` L37 要求 card 在 `cycle[:4]`），用于人工观察循环行为。

### S:player.PlayerState  [L5-58]
- 类型: class
- 签名: `class PlayerState:`
- 作用: 描述一名玩家的卡组循环与资源状态，并提供出牌/回复/皇冠查询接口。
- 参数: 构造参数 `player_id, cycle_queue, elixir, tower_hps=(4824, 3052, 3052)`。
- 返回: 实例。
- 实现: `__init__` 建全部字段（L6-15）；8 个方法分别处理觉醒槽、塔兵、Hero 槽、圣水回复、出牌合法性、出牌、下一张、皇冠数。
- 调用: 被 `battle.BattleState.__init__`（`battle.py:2543` 起使用 `player_0/player_1`）、`environment.CREnv.reset`（`environment.py:57-58`）、本组 `threat_calc.py:100-101`、`tmp_*` 测试与 `evaluate.py:22-23` 使用；`battle.py:2643-2650` 反向写入塔血字段。
- 置信度: 已确认

#### M:PlayerState.__init__  [L6-15]
- 类型: method
- 签名: `__init__(self, player_id, cycle_queue, elixir, tower_hps=(4824, 3052, 3052))`
- 作用: 初始化玩家状态；手牌循环做浅拷贝，塔血按 (king, left, right) 顺序解包。
- 参数:
  - `player_id`: 0/1 玩家编号。
  - `cycle_queue`: 卡名序列；用 `[:]` 复制，避免与调用方共享 list（L8）。
  - `elixir`: 初始圣水。
  - `tower_hps` (默认 `(4824, 3052, 3052)`): 按 (国王塔, 左公主塔, 右公主塔) 解包赋值（L10）。默认值是 lv11 级别的 4824/3052/3052。
- 返回: 无
- 实现: L7-10 写 `player_id/cycle/elixir/king_tower_hp/left_tower_hp/right_tower_hp`；L11 `last_card = None`（M1 镜像法术记录上一张）；L12 `evo_slots = set()`（M4 觉醒位 ≤2）；L13 `evo_plays = {}`（M4 觉醒周期计数 card_name→已打出次数）；L14 `hero_slots = set()`（M8 Hero 卡 ≤2，官方 Hero 槽 + Wild 槽共享上限）；L15 `tower_troop = None`（勘误批7 卡组第 9 张塔兵，None=默认 Princess）。
- 置信度: 已确认

#### M:PlayerState.set_evolution_slots  [L17-19]
- 类型: method
- 签名: `set_evolution_slots(self, cards)`
- 作用: 声明该卡组携带的觉醒卡（最多 2 个）。
- 参数:
  - `cards`: 可迭代卡名；取前 2 个。
- 返回: 无（写 `self.evo_slots`）
- 实现: `self.evo_slots = set(list(cards)[:2])`（L19）。静默截断到 2，不报错也不告警。
- 置信度: 已确认

#### M:PlayerState.set_tower_troop  [L21-25]
- 类型: method
- 签名: `set_tower_troop(self, name)`
- 作用: 设定卡组第 9 张塔兵（两座公主塔共用），对局内不可更换。
- 参数:
  - `name`: 合法值为 `None` / `'King_CannonTowers'` / `'King_KnifeTowers'` / `'King_ChefTowers'`（L24 的 assert 白名单）。
- 返回: 无（写 `self.tower_troop`）
- 实现: L24 `assert name in (None, 'King_CannonTowers', 'King_KnifeTowers', 'King_ChefTowers')`（注意 `assert` 在 `-O` 下会被剥离，非硬校验），L25 赋值。消费点已确认：`battle.py:2554-2555` 用 `player.tower_troop or 'King_PrincessTowers'` 决定两座公主塔的卡名。
- 置信度: 已确认

#### M:PlayerState.set_hero_slots  [L27-30]
- 类型: method
- 签名: `set_hero_slots(self, cards)`
- 作用: 声明该卡组 Hero 化的卡（≤2）。
- 参数:
  - `cards`: 可迭代卡名；取前 2 个。
- 返回: 无（写 `self.hero_slots`）
- 实现: `self.hero_slots = set(list(cards)[:2])`（L30）。docstring（L28-29）声明 Wild slot 互斥语义：Hero 形态部署 + 能力可用、觉醒禁用（二选一）——该互斥的**执行点不在本文件**，本类只存槽位集合（无法确认执行点，原因：本组范围外未追）。
- 置信度: 已确认（就本文件行为而言）

#### M:PlayerState.regenerate_elixir  [L32-34]
- 类型: method
- 签名: `regenerate_elixir(self, dt: float, base_regen_time: float = 2.8)`
- 作用: 按时间线性回复圣水，上限硬编码 10。
- 参数:
  - `dt` (`float`): 经过的时间（秒）。
  - `base_regen_time` (`float = 2.8`): 每点圣水所需秒数。
- 返回: 无（原地更新 `self.elixir`）
- 实现: L33 `elixir_per_second = 1.0 / base_regen_time`；L34 `self.elixir = min(10, self.elixir + elixir_per_second * dt)`。调用点已确认：`battle.py:2688` 按阶段传不同 regen——`2.8`（<120s）、`1.4`（<240s）、`2.8/3`（加时段）。
- 置信度: 已确认

#### M:PlayerState.can_play_card  [L36-39]
- 类型: method
- 签名: `can_play_card(self, card_name)`
- 作用: 判断某卡当前是否可打出（在手牌前 4 张、费用够、国王塔存活）。
- 参数:
  - `card_name`: 卡名（用于 `Card(card_name).elixir`）。
- 返回: `bool`
- 实现: 单表达式 `card_name in self.cycle[:4] and self.elixir >= Card(card_name).elixir and self.king_tower_hp > 0`（L37-39）。三个条件顺序短路；**注意没有检查 `card_name` 是否在 cycle 中就能通过（前 4 张即手牌）**，也不检查觉醒/Hero 槽与塔兵等机制是否允许。
- 置信度: 已确认

#### M:PlayerState.play_card  [L41-48]
- 类型: method
- 签名: `play_card(self, card_name)`
- 作用: 扣费并把手牌轮转到队尾（更新循环）。
- 参数:
  - `card_name`: 要打出的卡名。
- 返回: `bool` — 成功 True；`can_play_card` 为假时直接 False 且不改状态（L43）。
- 实现: L44 `self.elixir -= Card(card_name).elixir`（**用卡表原价扣费，与 `can_play_card` 里的比较同一来源**）；L45 `self.cycle.remove(card_name)`（若卡不在 cycle 会 ValueError，但被 L43 的门禁挡住）；L46 `self.cycle.append(card_name)`；L47 `if card_name != 'Mirror': self.last_card = card_name`（M1：镜像法术不改 last_card）。**注意本函数不维护 `evo_plays` 计数**——`__init__` 声明了该字段（L13）但本文件无任何写入点；写入方已确认为引擎侧 `battle.py:2736` `p.evo_plays[card_name] = p.evo_plays.get(card_name, 0) + 1`（计数器不对称：写入在 battle，声明在 player）。
- 调用: 被 `battle.py:2749` `p.play_card(card_name)` 调用（deploy_card 定义于 `battle.py:2806`）。本文件 `__main__` L64/L66 直接调用两次演示。
- 置信度: 已确认

#### M:PlayerState.get_next_card  [L50-52]
- 类型: method
- 签名: `get_next_card(self)`
- 作用: 返回循环中「下一张」卡（手牌之后的那张）。
- 参数: 无
- 返回: `self.cycle[4]` — 卡名。**若 cycle 长度 < 5 会 IndexError**（无保护）。
- 实现: 单行 L52，直接按固定下标取（docstring L51 写 "Return the next card in cycle, if known." —— "if known" 的降级路径实际未实现）。
- 置信度: 已确认

#### M:PlayerState.get_crown_count  [L54-58]
- 类型: method
- 签名: `get_crown_count(self) -> int`
- 作用: 返回已摧毁的塔数（皇冠数）；国王塔倒直接算 3。
- 参数: 无
- 返回: `int` — 国王塔 hp ≤ 0 时 3；否则 `int(left_tower_hp <= 0) + int(right_tower_hp <= 0)`（0/1/2）。
- 实现: L56-58 两个分支。注意判据基于 PlayerState 的 hp 字段快照，而这些字段只在 `battle.py:2643-2650 update_player_hp` 时刷新（依赖调用时机）。
- 调用: 被 `environment.py:105-106`（奖励计算 `3-p0.get_crown_count()`）、`rl/replay.py:119-120`（写 crown0/crown1）等使用。
- 置信度: 已确认

---

## F:src/clasher_new/evaluate.py
- 行数: 64
- 模块作用: **临时/一次性评估脚本**：定义 `SequentialEvalEnv`（按预设事件表让对手定时出牌的评估用环境），并在模块顶层加载指定 ckpt 用 `random_strategy` 对手跑 50 局统计胜率与平均奖励。**除类定义外全部是模块级可执行代码**（L38-64，无 `if __name__` 保护），`import evaluate` 会直接开始跑评估；被注释掉的 L38-41 是 `SequentialEvalEnv` 的手工示例。
- 模块级 docstring 首行: 无
- 主要导入: `battle`、`player`（L1）、`new_visualization.Visualizer`（L2）、`environment` 的 `CREnv, random_strategy, player_0_deck, shuffle, Position`（L4）、`stable_baselines3.PPO`（L5）、`tqdm`（L7）、`sys`（L9）、`torch`（L11）
- 关键模块级常量:
  - `steps = ('20461248n_steps',)`（L43）— 要评估的检查点步数标签元组（硬编码，需与 `cr_logs/cr_<step>.zip` 对应，L46 拼路径）。
  - `games_count = 50`（L44）— 每个 ckpt 的评估局数。
- 顶层数据表/字典: 无
- 顶层执行段（L45-64，非符号，附带说明）: 对 `steps` 每个标签 `PPO.load(f"cr_logs/cr_{step}.zip", seed=None)`（L46）；`env = CREnv(opponent_model=random_strategy, visualize=False)`（L47，注意**不是**用刚定义的 SequentialEvalEnv）；打印评估头；`for i in tqdm(range(games_count))` 中 `env.reset()`、循环 `model.predict(obs)` → `env.step(action)` 直到 termination/truncation，累加 reward；`games_won += (1-env.battle.winner)`（L62）；最后打印 `Win rate` 与 `Mean reward`。注意 `env.battle.winner` 为 None（平局）时 `1-None` 会 TypeError；且 `import sys`、`import torch` 在本文件内未被使用（死导入）。

### S:evaluate.SequentialEvalEnv  [L13-34]
- 类型: class（`class SequentialEvalEnv(CREnv)`，bases=['CREnv']）
- 签名: `class SequentialEvalEnv(CREnv):`
- 作用: 自定义评估环境：用固定起点卡组 `start_deck` 作为 P1（对手）卡组，并按 `events` 事件表在对局特定时刻让 P1 出指定牌；P0 卡组沿用 `environment.player_0_deck` 且每次 reset 后 shuffle。
- 参数: 构造参数 `start_deck, events, visualize=False, speed=1.0`。
- 返回: 实例。
- 实现: `__init__`（L14-17）转发 visualize/speed 给父类并保存 deck/events；`reset`（L19-28）调父类 reset、重洗 P0 卡组、重建 BattleState（P0 9.0 费、P1 用 `self.deck[:]` 9.0 费）、可选建 Visualizer、返回 `self.observe(0), {}`；`opponent_action`（L30-34）按事件时间触发 P1 部署。
- 调用: 模块内仅在 L38-41 的注释示例中出现（被注释掉）；顶层评估实际用的是 `CREnv`（L47）⇒ **本类在当前文件状态下未被实例化**。
- 置信度: 已确认

#### M:SequentialEvalEnv.__init__  [L14-17]
- 类型: method
- 签名: `__init__(self, start_deck, events, visualize=False, speed=1.0)`
- 作用: 保存评估用卡组与事件表，转发渲染参数。
- 参数:
  - `start_deck`: P1 起始卡组（list 卡名），reset 时 `[:]` 复制。
  - `events`: 事件表，元素为 4 元组 `(card, x, y, t)`（结构由 `opponent_action` L31-34 解包确认）。
  - `visualize` (默认 False): 是否创建 Visualizer。
  - `speed` (默认 1.0): 转发给父类。
- 返回: 无
- 实现: L15 `super().__init__(visualize=visualize, speed=speed)`；L16 `self.deck = start_deck`（**未复制**，与 reset 里的 `[:]` 形成对比）；L17 `self.events = events`。
- 置信度: 已确认

#### M:SequentialEvalEnv.reset  [L19-28]
- 类型: method
- 签名: `reset(self, *, seed=None, options=None)`
- 作用: 重置环境：重洗 P0 卡组并新建 BattleState（双方 9.0 初费），返回初始观测。
- 参数:
  - `seed` (keyword-only, 默认 None): 转发给 `gym.Env.reset`（父类 `CREnv.reset`，`environment.py:51`）。
  - `options` (keyword-only, 默认 None): 同上。
- 返回: 二元组 `(self.observe(0), {})` — 观测 dict 与空 info。
- 实现: L20 `super().reset(seed=seed, options=options)`（父类 reset 内部已 shuffle 双方卡组并各自建 5.0 费的 battle，`environment.py:52-61`）；L21 `shuffle(player_0_deck)`（模块级 random.shuffle，原地洗）；L22-23 **重新**构造 `battle.BattleState`，P0 用 `player_0_deck[:]`、9.0 费，P1 用 `self.deck[:]`、9.0 费——即覆盖父类刚建的 battle（父类那一次的随机种子状态因此被丢弃）；L24-25 若 visualize 则新建 `Visualizer(self.battle)`；L28 返回 `self.observe(0), {}`（观测实现继承父类 `environment.py:124`）。**未调用 `super().reset` 之后父类期望的其它初始化**无法确认是否有必要（原因：CREnv.reset 全文已读，只有 shuffle+battle+visualizer 三步，无其它副作用）。
- 置信度: 已确认

#### M:SequentialEvalEnv.opponent_action  [L30-34]
- 类型: method
- 签名: `opponent_action(self)`
- 作用: 按事件表在时间匹配时让 P1 在镜像坐标部署指定卡。
- 参数: 无
- 返回: 无（可能一次调用内部署多个事件）
- 实现: 遍历 `self.events`（L31），解包 `card, x, y, t = event`（L32），当 `abs(self.battle.time - t) < 0.1` 时调 `self.battle.deploy_card(1, card, Position(18-(x+0.5), 32-(y+0.5)))`（L33-34）。坐标变换与父类 `CREnv.opponent_action` 的 `Position(18-(x+0.5), 32-(y+0.5))`（`environment.py:70-72`）逐字一致，即把玩家本地网格 (x,y) 镜像到 P1 视角世界坐标。注意 `x`/`y` 的用法：本函数用 `18-(x+0.5)` 配 x、`32-(y+0.5)` 配 y；父类同一写法 ⇒ 口径一致。返回值被父类 `step` 忽略（`environment.py:76` 起 step 内部调用 opponent_action 的方式未逐行确认——无法确认父类何时调用它，原因：本组未读 environment.step 全文）。
- 置信度: 已确认（实现与坐标口径）；「被父类 step 何时调用」无法确认（原因：`environment.step` 全文未在本组读取范围内逐行核对）

---

## F:src/clasher_new/tmp_target_verify.py
- 行数: 56
- 模块作用: **临时验证脚本**（`tmp_` 前缀、无 `if __name__` 保护）：验证「破边塔后桥头部队应转攻击国王塔、不再被对侧公主塔吸走」这一索敌行为。导入即跑 12 组场景。
- 模块级 docstring 首行: `验证: 破边塔后桥头部队应转国王塔, 不再被对侧公主塔吸走。`
- 主要导入: `os`、`sys`（L3）、`battle`、`player`（L6）、`core.Position`（L7）；L4 插入脚本自身目录到 sys.path
- 关键模块级常量:
  - `FILLER = ['Musketeer', 'Arrows', 'Giant', 'Archer', 'MiniPekka', 'Fireball', 'Knight']`（L9）— 补足 8 张卡组的填充卡；测试卡放在首位（L13 `[card] + FILLER`）。
- 顶层数据表/字典: 无
- 顶层执行段（L46-56，非符号，附带说明）: 三组场景——左桥头 x∈[2.0,3.5,4.2,4.5,4.9] 破左塔(id=1)（L47-48）；镜像右桥头 x∈[13.1,14.5,15.9] 破右塔(id=2)（L50-51）；破左塔后用 Musketeer/Giant/MiniPekka 三兵种（L53-54）；最后对照「两塔全」不复位（L56）。塔 id 与「左=1/右=2」的对应与 `battle.py:2556-2557`（RED_LEFT=1、RED_RIGHT=2）一致。

### S:tmp_target_verify.run  [L12-43]
- 类型: function
- 签名: `run(name, card, wx, wy, dead_towers)`
- 作用: 构造一个指定卡在指定世界坐标部署的对局（可预先把若干塔置死），推进最多 25s，记录该单位首个有效目标的塔名与终点位置，并按「首目标是否 KingTower」打印 OK/`!!!`。
- 参数:
  - `name`: 场景名（打印用）。
  - `card`: 被测试卡名（放在 P0 卡组首位）。
  - `wx`, `wy`: 世界坐标（直接 `Position(wx, wy)`，未做本地→世界变换）。
  - `dead_towers`: 要预先打死的实体 id 列表（如 `[1]`、`[2]`）。
- 返回: 无（直接 print）；部署失败时提前 return（L21-23）。
- 实现: ①L13-14 建 P0 卡组 `[card] + FILLER`、P1 卡组 `['Knight'] + FILLER`，双方各 10 费；②L15 建 `battle.BattleState(p0, p1)`（未传 card_level ⇒ 用默认等级）；③L16-18 对 `dead_towers` 中每个 id 把 `b.entities[eid].hp = 0` 且 `is_alive = False`（**直接改实体，绕过伤害管线**，故不会触发死亡钩子/圣水/皇冠结算）；④L19 补满 P0 圣水 10.0，L20 `b.deploy_card(0, card, Position(wx, wy))`，失败则打印并 return；⑤L24 收集 `e.player == 0 and e.name == card` 的实体作为被追踪单位（**多单位卡会全部纳入**）；⑥L26-39 主循环 `dt = 1/60`、上限 25.0s、`not b.game_over`：每步 `b.step(dt)` 后对每个存活单位，若还没记录首目标则读 `target_id`（L35 `getattr(u,'target_id',None)`），且仅当 `tid in b.entities` 时记下 `(b.entities[tid].name, t)`（L36-37）；同时把 `end_pos` 更新为最后存活单位的位置（L38-39）；⑦L40 解包 `first`（未命中则 `(None, None)`）；L41 `verdict = "OK" if tname == 'KingTower' else f"!!! {tname}"`；⑧L42-43 打印场景名、卡与坐标、拆塔列表、首目标与时刻、终点坐标、verdict。注意 `end_pos` 在无任何单位存活时会保持 `None`，此时 L43 的 `{end_pos[0]:.1f}` 会 TypeError。
- 调用: 被模块顶层 12 次调用（L48/51/54/56）。调用 `battle.BattleState`、`deploy_card`、`b.step`。
- 置信度: 已确认

---

## F:src/clasher_new/tmp_tower_chip_test.py
- 行数: 55
- 模块作用: **临时量化测试脚本**（无 `__main__` 保护）：测 3 费单位（Minions/Archer/Knight）从不同落点出发的行为与「塔伤转化率 / 每费塔伤」，并每 5s 打印位置与存活/HP 追踪。导入即跑 6 个场景。
- 模块级 docstring 首行: `量化测试 v2: 3 费单位从不同落点的行为 + 塔伤转化率, 加位置追踪。`
- 主要导入: `os`、`sys`（L3）、`battle`、`player`（L6）、`core.Position`（L7）；L4 sys.path 插自身目录
- 关键模块级常量:
  - `FILLER = ['Knight', 'Musketeer', 'Fireball', 'Giant', 'Archer', 'MiniPekka', 'Arrows']`（L9）— 填充卡组；被测试卡插在首位（L13）。
- 顶层数据表/字典: 无
- 顶层执行段（L50-55，非符号，附带说明）: 6 个场景 = {Minions, Archer, Knight} × {「模型角落」(16,5), 「桥头后方」(14,13)}；注意 docstring 说测 3 费单位，实际 `run` 里没做费用断言，费用只用于最后 `total/cost` 的换算（L47）。

### S:tmp_tower_chip_test.run  [L12-47]
- 类型: function
- 签名: `run(name, card, local_x, local_y, max_t=60.0)`
- 作用: 从本地网格坐标部署单卡并推演最多 `max_t` 秒，统计首次对塔命中时刻、结束时存活数与对 P1 右塔+国王塔的累计伤害、每费伤害。
- 参数:
  - `name`: 场景名（打印用）。
  - `card`: 被测试卡名。
  - `local_x`, `local_y`: 本地网格坐标；函数内部加 0.5 转成世界坐标（L16 `Position(local_x + 0.5, local_y + 0.5)`）。
  - `max_t` (默认 60.0): 最大推演秒数。
- 返回: 无（直接 print）；部署失败时提前 return（L20-22）。
- 实现: ①L13-15 建双方 PlayerState（P0 `[card]+FILLER`、P1 `['Knight']+FILLER`，各 10 费）与 BattleState；②L16-19 计算世界坐标 `pos`，把 P0 圣水设为 10.0 并记录 `e_before`，调 `deploy_card(0, card, pos)`；③L23 `cost = e_before - p0.elixir`（按实际扣费反推，比直接读卡表更贴近实际）；④L24 收集同卡实体；L25 记录 P1 右塔与国王塔初始血量 `rr0, rk0`（**注意：这是 P1 的塔，即被攻击方；`p1.right_tower_hp`/`p1.king_tower_hp` 由 `battle.py:2648/2649` 从实体 2/5 同步**）；⑤L26-40 主循环 `dt=1/60`：每步 `b.step(dt)`、`t += dt`，`dmg = (rr0 - p1.right_tower_hp) + (rk0 - p1.king_tower_hp)`（L33），首次 `dmg > 0` 记录 `first_hit_t`（L34-35）；L36 采样条件 `abs(t - round(t)) < dt/2 and round(t) % 5 == 0`（即约每 5 秒一次），记录存活单位数、存活单位 HP 和、首个存活单位坐标字符串（无存活时为 "-"）与当前 dmg 进 `trace`（L37-40）；⑥L41-42 结算 `total` 与 `n_alive`；⑦L43-47 打印表头（含 `cost`）与 trace 每行，再打印首次命中时刻（None 时文案「60s内未命中」——**文案硬编码 60s，与 `max_t` 参数不一致**）、存活比例、总塔伤、`total/cost` 每费塔伤（若 cost 为 0 会 ZeroDivisionError）。
- 调用: 被模块顶层 6 次调用（L50-55）。
- 置信度: 已确认

---

## F:src/clasher_new/tmp_formation_test.py
- 行数: 53
- 模块作用: **临时队形测试脚本**（无 `__main__` 保护）：检验「同卡多单位应并排走位、保持横向间距，而不是前后互挤成一路」。导入即跑 3 个场景（Archer×2 + Knight 单体对照）。
- 模块级 docstring 首行: `队形测试: 同卡多单位应并排走位(横向间距保持), 而不是前后互挤成一路。`
- 主要导入: `os`、`sys`（L3）、`battle`、`player`（L6）、`core.Position`（L7）；L4 sys.path 插自身目录
- 关键模块级常量:
  - `FILLER = ['Knight', 'Musketeer', 'Fireball', 'Giant', 'Archer', 'MiniPekka', 'Arrows']`（L9）— 填充卡组。
- 顶层数据表/字典: 无
- 顶层执行段（L51-53，非符号，附带说明）: `run("弓箭手@模型角落", "Archer", 16, 5)`、`run("弓箭手@桥头后方", "Archer", 14, 13)`、`run("骑士(单体对照)", "Knight", 14, 13)` 三次调用。

### S:tmp_formation_test.run  [L12-48]
- 类型: function
- 签名: `run(name, card, x, y, tmax=30)`
- 作用: 部署单卡，先跑 1s 等召唤类卡的多单位全部生成，再每 0.5s 采样前两只存活单位的横向 x 间距与 y 差，最后给出间距均值/最小值与塔伤。
- 参数:
  - `name`: 场景名。
  - `card`: 被测试卡名。
  - `x`, `y`: 本地网格坐标；内部 `Position(x + 0.5, y + 0.5)`（L17）。
  - `tmax` (默认 30): 采样推进上限（秒）。
- 返回: 无（直接 print）
- 实现: ①L13-16 建双方 PlayerState（各 10 费）与 BattleState，P0 圣水置 10.0；②L17 `b.deploy_card(0, card, Position(x+0.5, y+0.5))`（**未检查返回值**，与另两个 tmp 脚本不同）；③L18 记录 P1 右塔与国王塔初始 hp；④L22-24 预热循环：`t < 1.0` 内以 `dt = 1/60` 步进（注释：等延迟生成，Archers 第 2 只 ~0.13s）；⑤L25-26 收集 `isinstance(e, battle.Troop) and e.player == 0 and e.name == card` 的实体（用 `battle.Troop` 类型过滤，已确认 `battle.py` 有 `class Troop(Entity)` L761）；打印单位数；⑥L28-43 主循环 `t < tmax and not b.game_over`：采样条件 `abs(t - round(t*2)/2) < dt/2 and round(t*2) % 3 == 0`（约每 1.5s 一次，与注释/打印精度一致）；若存活 ≥2 只，取前两只算 `sep = abs(x0-x1)`、`dy = y0-y1` 存进 `seps` 并打印；否则打印「剩1只」+ 该只坐标（L41-42）；L43 每步都算 `dmg` 但**只在循环内赋值、循环后未使用**（死代码）；⑦L44-48 结算 total 塔伤、存活数，`seps` 非空时打印均值/最小（文案标注「出兵时≈1.1」为人工经验值，脚本未计算验证）。
- 调用: 被模块顶层 3 次调用（L51-53）。
- 置信度: 已确认

---

## F:src/clasher_new/tmp_path_debug.py
- 行数: 37
- 模块作用: **临时调试脚本**（无 `__main__` 保护）：用 monkey-patch 替换 `battle.Troop.move_towards`，记录每次调用的「前位置/路点/后位置」，用于打印 Archer 卡住段的逐次寻路目标。导入即执行。
- 模块级 docstring 首行: `连续打印 Archer 卡住段的每次 move_towards 调用目标。`
- 主要导入: `os`、`sys`、`math`（L3，`math` 实际未使用）、`battle`、`player`（L6）、`core.Position`（L7）；L4 sys.path 插自身目录
- 关键模块级常量:
  - `orig_move = battle.Troop.move_towards`（L9）— 保存原方法引用（定义于 `battle.py:1087`）。
  - `LOG = []`（L10）— 全局调用日志（元素为 `(before, waypoint_xy, after)`）。
  - L18 `battle.Troop.move_towards = patched` — **模块级永久 monkey-patch 副作用**（无恢复逻辑；同进程后续所有 Troop 移动都走 patched）。
  - `FILLER = ['Knight', 'Musketeer', 'Fireball', 'Giant', 'Archer', 'MiniPekka', 'Arrows']`（L20）— 填充卡组。
- 顶层数据表/字典: 无（`LOG` 是调试日志列表，非数据表）

### S:tmp_path_debug.patched  [L12-16]
- 类型: function（运行期被当作 `Troop` 的方法使用）
- 签名: `patched(self, position, dt, can_overshoot=False)`
- 作用: 包装原 `Troop.move_towards`，在调用前后记录位置并追加进全局 `LOG`。
- 参数:
  - `self`: Troop 实例（patch 后由方法绑定传入）。
  - `position`: 目标位置/路点（含 `.x`/`.y`）。
  - `dt`: 时间步。
  - `can_overshoot` (默认 False): 是否允许越过目标，原样转发。
- 返回: 无（`orig_move` 的返回值未被返回——已确认 `battle.py:1087` 的 `move_towards` 注解为 `-> None`，故不丢信息）
- 实现: L13 记 `before = (self.position.x, self.position.y)`；L14 调 `orig_move(self, position, dt, can_overshoot)`（显式传 self，因为函数是普通函数赋给类属性）；L15 记 `after`；L16 `LOG.append((before, (position.x, position.y), after))`。
- 调用: 由引擎在 Troop 移动时调用（替换后）；L18 完成替换。
- 置信度: 已确认

- 顶层执行段（L20-37，非符号，附带说明）: 构造双方 PlayerState（各 10 费）与 BattleState，P0 满费后在 `Position(14.5, 13.5)` 部署 Archer（L25，未检查返回值），推进 5s（L27-30）；L32 `start = next(i for i, r in enumerate(LOG) if r[0][1] > 15.5)` —— **该生成器在没有任何 move_towards 起点 y > 15.5 时会抛 StopIteration**（即脚本对被测行为有硬前提，注释称「卡住段」）；L33-37 打印 `LOG[start:start+24]` 共最多 24 条，每条输出 `from(...) 朝路点(...) 到(...) 位移=(dx,dy)`（位移量在 L35 现算）。`math` 导入未使用。

---

## F:src/clasher_new/train_autoregressive.py
- 行数: 36
- 模块作用: **未完成的训练启动脚本**（无 `if __name__` 保护、无函数封装、无 `learn()` 调用）：定义了 `make_env` 工厂并用 `SubprocVecEnv` 建 8 个并行环境，然后构造一个 PPO 对象，到此文件结束——没有 `model.learn(...)`、没有回调/保存，也没有任何输出。因此它的实际效果只是「建环境 + 建模型」。
- 模块级 docstring 首行: 无
- 主要导入: `environment` 的 `CREnv, random_strategy`（L1）、`stable_baselines3.PPO`（L3）、`CheckpointCallback`（L4，**导入后未使用**）、`SubprocVecEnv, VecMonitor`（L5）、`torch`（L6）、`torch.nn as nn`（L7，**导入后未使用**）、`train.CRFeatureExtractor`（L9）、`random`、`numpy as np`（L11-12）
- 关键模块级常量:
  - `n_envs = 8`（L23）— 并行环境数。
  - `n_steps = 2048 // n_envs`（L26）— 每个环境每次 rollout 的步数，= 256；注释里无依据说明，Python 3 下为真除法得 float 256.0（`2048//8` 实为整数 256）。
  - `env = SubprocVecEnv([make_env(rank) for rank in range(n_envs)], start_method="spawn")`（L24）— **模块级建进程池**（8 个子进程）。
  - `env = VecMonitor(env)`（L25）— 给向量环境套监控包装。
  - `model = PPO("MultiInputPolicy", env, n_steps=n_steps, batch_size=256, policy_kwargs={"features_extractor_class": CRFeatureExtractor}, device="cuda", verbose=1)`（L28-36）— 构造 PPO；`CRFeatureExtractor` 已确认存在（`train.py:17`，`class CRFeatureExtractor(BaseFeaturesExtractor)`，输出 `features_dim=256`），且其 `__init__` 里用 `self.cnn = ...; dummy = torch.zeros(1, self.in_channels, 32, 18)`（`train.py:23-31`）⇒ 期望的观测是 32×18 的网格。
- 顶层数据表/字典: 无

### S:train_autoregressive.make_env  [L15-21]
- 类型: function
- 签名: `make_env(rank)`
- 作用: 返回一个无参工厂函数；该工厂在子进程内先固定随机种子与线程数，再创建 `CREnv`（对手为 `random_strategy`）。
- 参数:
  - `rank`: 环境序号（0..n_envs-1），用于种子偏移。
- 返回: `factory`（`def factory(): ...` 闭包，无参、返回 `CREnv` 实例）。
- 实现: L16 定义内层 `factory`；L17 `random.seed(10_000 + rank)`；L18 `np.random.seed(10_000 + rank)`；L19 `torch.set_num_threads(1)`（限制子进程线程数，避免多进程争抢）；L20 `return CREnv(opponent_model=random_strategy)`（无 visualize/速度参数 ⇒ 默认不渲染）。种子未设 `torch.manual_seed`，故 GPU 侧随机性未受控（无法确认是否另有全局设置，原因：本组范围外未追）。
- 调用: 模块顶层 L24 在列表推导里对 rank 0..7 各调用一次，把返回的 factory 交给 `SubprocVecEnv`（`start_method="spawn"` ⇒ factory 必须在子进程内可 pickle，闭包依赖 `random/np/torch/CREnv/random_strategy` 均模块级可导入）。`environment.random_strategy` 已确认存在且为函数（`environment.py:164`）。
- 置信度: 已确认

---

## F:src/clasher_new/agent_pool.py
- 行数: 35
- 模块作用: **临时 Elo 对战脚本**（无 `__main__` 保护）：加载 5 个历史 ckpt，两两配对（`permutations` 有序对，共 5×4=20 组）各打 30 局，统计「模型1 胜的局数 : 10 - 胜局数」。**关键事实：Elo 更新逻辑（L32-34）被注释掉了**，因此 `elo` 列表始终是初始的 `[1500]*5`，模块级 `expected`/`update` 两函数虽被定义但从未在运行路径中被调用（只有注释行 `update(...)` 提及）。且打印的 `10-wins` 与 `range(30)` 的局数不一致（口径错误，见下）。
- 模块级 docstring 首行: 无
- 主要导入: `environment` 的 `CREnv, random_strategy`（L1；`random_strategy` **导入后未使用**）、`stable_baselines3.PPO`（L2）、`random`（L3，未使用）、`itertools.permutations`（L4）、`tqdm`（L5）
- 关键模块级常量:
  - `steps = ('15010624n', '20461248n', '9005312', '12432976', '15092976', )`（L7）— 5 个 ckpt 的步数标签。
  - `elo = [1500]*len(steps)`（L8）— 初始 Elo 全 1500。
  - `matchups = permutations(list(range(len(steps))), 2)`（L9）— **生成器**（非 list）；被 L19 的 `for` 消费一次即耗尽。
  - `models = [PPO.load(f"cr_logs/cr_{each}_steps.zip", seed=None) for each in steps]`（L10）— 模块级加载 5 个模型（要求 `cr_logs/` 与文件名严格匹配，否则导入即失败）。
- 顶层数据表/字典: 无
- 顶层执行段（L19-35，非符号，附带说明）: L19-21 遍历 20 个有序对，取两个模型；L22 `env = CREnv(opponent_model=lambda observation: model2.predict(observation)[0])`——把 model2 包成对手策略（**每个配对都新建一个 env**）；L23-31 跑 30 局：`env.reset()`、循环 `model1.predict(obs)`→`env.step(action)` 直到终止，`wins += (1-env.battle.winner)`（L31，winner 为 None 时会 TypeError）；Elo 更新三行被注释（L32-34）；L35 `print(steps[index0], ':', steps[index1], '=', wins, ':', 10-wins)`——分母写成 10 而实际局数是 30，打印的「胜:负」不是 30 局口径（若 wins > 10 还会出现负数「负」）。

### S:agent_pool.expected  [L12-13]
- 类型: function
- 签名: `expected(r_a, r_b)`
- 作用: 标准 Elo 期望胜率公式。
- 参数:
  - `r_a`: A 方评分。
  - `r_b`: B 方评分。
- 返回: `float` — `1 / (1 + 10 ** ((r_b - r_a) / 400))`。
- 实现: 单表达式，L13；标准 400 分母。
- 调用: 被 `update` 调用（L16）。**当前模块运行路径中无调用**（L32-34 已注释）。
- 置信度: 已确认

### S:agent_pool.update  [L15-17]
- 类型: function
- 签名: `update(r_a, r_b, score_a, k=32)`
- 作用: 按 Elo 规则返回 A/B 双方更新后的评分二元组。
- 参数:
  - `r_a`, `r_b`: 双方当前评分。
  - `score_a`: A 的实得分数（1/0.5/0）。
  - `k` (默认 32): K 因子。
- 返回: `tuple(float, float)` — `(r_a + k*(score_a - e_a), r_b + k*((1-score_a) - (1-e_a)))`。
- 实现: L16 `e_a = expected(r_a, r_b)`；L17 返回两元组。B 的期望项写为 `(1 - e_a)` 而非 `expected(r_b, r_a)`（数学上相等，因 `expected` 对称），故 B 的更新 = `r_b + k*(e_a - score_a)`。
- 调用: **仅被注释中的 L32 提及，实际未被执行**（无法确认是否曾在别处使用，原因：全仓该名字未被其它文件 import——本组范围内只 grep 到本文件）。
- 置信度: 已确认

---

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| src/clasher_new/threat_calc.py | 2 | 2 |
| src/clasher_new/run_raw_capture.py | 2 | 2 |
| src/clasher_new/tmp_spell_forensics.py | 1 | 1 |
| src/clasher_new/core.py | 11 | 11 |
| src/clasher_new/player.py | 10 | 10 |
| src/clasher_new/evaluate.py | 4 | 4 |
| src/clasher_new/tmp_target_verify.py | 1 | 1 |
| src/clasher_new/tmp_tower_chip_test.py | 1 | 1 |
| src/clasher_new/tmp_formation_test.py | 1 | 1 |
| src/clasher_new/tmp_path_debug.py | 1 | 1 |
| src/clasher_new/train_autoregressive.py | 1 | 1 |
| src/clasher_new/agent_pool.py | 2 | 2 |
| **合计** | **37** | **37** |

COVERAGE_OK G038 files=12 symbols=37
