# 组 G016 文件级分析

## F:src/clasher_new/rl/evaluate.py
- 行数: 557
- 模块作用: 评测模块（文件头 docstring L1 自述「评测（规划文档 11）：对局胜率 + 同刻多卡统计 + 信念校准 + 联赛 Elo」）。核心是对已训练 checkpoint 跑对局并统计多维指标：胜负/平、平均回报、bundle 大小与合法率、圣水效率、Plan 意图分布、逐意图采纳率（region/card/hold/budget）、皇冠与塔血差、信念预测的 Next-Card 命中/Brier/ECE/手牌 Top4 重叠（L178-291）。另提供 belief/plan 输入消融对比（`run_ablation`，含 JSON+CSV 落盘）与独立信念协议 `evaluate_belief`（N=500）。`if __name__ == "__main__"` 提供 CLI 三种模式（信念-only / 完整消融 / 普通评测）。
- 模块级 docstring 首行: `评测（规划文档 11）：对局胜率 + 同刻多卡统计 + 信念校准 + 联赛 Elo。`
- 主要导入: os, sys, json, math, random, argparse, numpy(as np), rl.env_wrapper(RLEnv), rl.belief(BeliefInference), rl.belief_planner(BeliefPlanner), rl.follower(load_checkpoint), rl.plan_space(PlanToken), rl.train_follower(heuristic_opponent, FollowerOpponent), rl.observation(ENTITY_NAMES), rl.action_bundle(K_MAX), card_utils(Card)
- 关键模块级常量:
  - `_PARENT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` — L18；本文件所在目录的上层（即 `src/clasher_new`），L19-20 把它插入 `sys.path` 以便 `from rl.* / from card_utils` 可导入。
  - `ABLATION_VARIANTS = [(...), ...]` — L308-313；4 元组列表，每项 `(变体名, ablation 参数值, 中文描述)`：`("full", None, "基线（belief + plan 都注入）")`、`("plan-off", "plan", "plan token 置零（belief 保留）")`、`("belief-off", "belief", "belief token 置零（plan 保留）")`、`("both-off", "both", "两者都置零")`。仅被 `run_ablation` 遍历使用（L330）。
  - 其它模块级赋值：无（`_overtime_open`/`_ece`/`_plan_region_hit` 等均为函数；L59 的 `9.0`、L67 的 `15.5`、L69 的 `16.5`、L71 的 `13.0`/`19.0` 是 `_plan_region_hit` 内的字面量，不是模块级常量）。
- CLI 参数（`if __name__ == "__main__"` 的 `argparse`，L526-557，逐条列出）:
  - `--policy` (`type=str, required=True`) — L528；被评测策略 ckpt 路径，三个分支都用。
  - `--n-games` (`type=int, default=50`) — L529；透传给 `run_eval`/`evaluate_belief`/`run_ablation`（注意 `run_ablation` 自身默认 200，但 CLI 会给 50）。
  - `--opponent` (`type=str, default="random"`, `choices=["random","heuristic","checkpoint"]`) — L530-531；透传 `run_eval`/`run_ablation`。
  - `--opponent-policy` (`type=str, default=None`) — L532；checkpoint 对手路径。
  - `--seed` (`type=int, default=0`) — L533。
  - `--hidden-dim` (`type=int, default=None`) — L534。
  - `--max-steps` (`type=int, default=300`) — L535。
  - `--belief-only` (`action="store_true"`, help=「只跑信念协议」) — L536；为真时走 `evaluate_belief`（L544-546）。
  - `--ablation` (`type=str, default=None`, `choices=["plan","belief","both","all"]`, help「消融：plan=禁用 plan token，belief=禁用 belief token，both=两者都禁用，all=跑完整对比(full/plan/belief/both)并落盘」) — L537-540；`"all"` 走 `run_ablation`（L547-553），其余非 None 值作为 `ablation` 传给 `run_eval`（L555-557）。
  - `--ablation-out` (`type=str, default=None`, help「--ablation all 的结果落盘路径（缺省 ablation_result.json）」) — L541-542；L548-549 缺省取 `dirname(abspath(policy))` 下的 `ablation_result.json`。
  - 分支顺序：L544 先判 `--belief-only`，否则 L547 判 `--ablation == "all"`，否则 L554-557 走单次 `run_eval`。注意 `--ablation` 的三个单值选项只在 `elif` 之外的最后分支生效（L555-557 会把 `args.ablation` 传下去）。
- 顶层数据表/字典: 无模块级数据表/字典。字典均在各函数体内构造：`run_eval` 的 `stats`（L178-186：胜/负/平、累计 reward、bundle_sizes、bundle_illegal/bundle_total、next_hits/next_total/next_brier、hand_hits/hand_total、elixir_spent、plan_intents、adoption、crown_diff、tower_diff、belief_conf、belief_correct）、`_adoption_seed()` 的计数种子字典（L83-86）、`run_ablation` 的 `results`/`deltas`/`adoption_vs_off`/`adoption_aggregate`（L327/L351/L358/L427）。

### S:_overtime_open  [L35-38]
- 类型: function
- 签名: `def _overtime_open(battle):`
- 作用: 惰性转发到 `rl.run_league.overtime_open`，判断该局是否处在加时窗口内（从而允许越过 `max_steps` 继续跑）。
- 参数:
  - `battle`: 当前引擎 Battle 实例；原样透传给 `overtime_open`（该函数签名见 `rl/overtime.py` 的 `overtime_open` 导出，`run_league.py:66` 亦从 `rl.overtime` 导入）。
- 返回: `overtime_open(battle)` 的返回值（布尔语义的「加时窗口是否开启」）；本函数不做加工。
- 实现: docstring L36 明说是「惰性 import，避免 evaluate ↔ run_league 循环 import」；L37 在每次调用时才 `from rl.run_league import overtime_open`，L38 立即 `return overtime_open(battle)`。无分支、无缓存。
- 调用: 被同文件 `run_eval`（L195 的 while 条件）使用；`run_league.py:66` 显示 `overtime_open` 实际定义/导出于 `rl.overtime`（`run_league` 只是再导出），故此处是经 run_league 的二跳转发。
- 置信度: 已确认

### S:_ece  [L41-49]
- 类型: function
- 签名: `def _ece(conf, acc, n_bins=10):`
- 作用: 计算期望校准误差（Expected Calibration Error）：把置信度分箱后，按各箱样本占比加权「箱内平均置信度 − 箱内准确率」的绝对值之和。
- 参数:
  - `conf`: 模型预测置信度序列（可被 `np` 索引，如 `np.ndarray` 或 list）；调用点传入 `stats["belief_conf"]` 转成的数组（L280）。
  - `acc`: 同长度、取值 0/1 的正确性标签序列；调用点传入 `stats["belief_correct"]` 数组（L280）。
  - `n_bins` (int, 默认 10): 分箱数；等距划分 [0,1]。
- 返回: `float(ece)`（L49）。
- 实现: L42 初始化 `ece=0.0`；L43 用 `np.linspace(0.0, 1.0, n_bins+1)` 生成箱边界；L44-48 逐箱：L45 用 `(conf >= edges[i]) & (conf < edges[i+1])` 构造布尔掩码，L46 空箱 `continue` 跳过，L47-48 累加 `(箱内样本数 / 总样本数) * |箱内 acc 均值 − 箱内 conf 均值|`。注意边界：最后一箱右端 1.0 取不到（`conf == 1.0` 的样本不落入任何箱），未做特殊处理。
- 调用: 被 `run_eval`（L284）与 `evaluate_belief`（L519）调用。它自身只调用了 numpy。
- 置信度: 已确认

### S:_plan_region_hit  [L52-79]
- 类型: function
- 签名: `def _plan_region_hit(region: str, x: float, y: float):`
- 作用: 判断「首个部署格 (x, y)」（player0 本地坐标）是否落在 plan 指定的 focus_region 的粗粒度区域里；region 无法识别时返回 None（表示不计入统计）。
- 参数:
  - `region` (str): plan 的焦点区域名，只识别 `own_left/enemy_left/bridge_left`、`own_right/enemy_right/bridge_right`（L61-64）以及以 `own`/`enemy`/`bridge` 开头的名字（L66-71）。
  - `x` (float): 部署格横坐标（本地坐标）。
  - `y` (float): 部署格纵坐标（本地坐标）。
- 返回: `None`（当既没匹配到左右分边、也没匹配到半场/桥带规则，L72-73）或 `bool(ok)`（L79），表示是否吻合。
- 实现: L59 强制 `x = float(x); y = float(y)`；L60 初始化 `side_ok=None`，L61-64 按 `*_left` 判 `x < 9.0`、`*_right` 判 `x >= 9.0`（LANE=9 分边，见 docstring L56）；L65 初始化 `half_ok=None`，L66-71 按前缀判半场：`own` ⇒ `y < 15.5`、`enemy` ⇒ `y > 16.5`、`bridge` ⇒ `13.0 <= y <= 19.0`，三种前缀都不匹配则 `half_ok` 保持 None（注释 L56 说 center 类只查半场，但源码中 `center` 前缀不匹配任何分支）。L72-73 两者皆 None 则返回 None；L74-78 把存在的条件做逻辑与（`ok = ok and side_ok` / `ok = ok and half_ok`）后返回布尔。
- 调用: 仅被同文件 `_record_adoption` 调用（L127），用于 region_rate 探针；docstring L57 声明「不参与训练/判分」。未在全仓检索到其它调用点（本次仅在 evaluate.py 内 grep 确认）。
- 置信度: 已确认

### S:_adoption_seed  [L82-86]
- 类型: function
- 签名: `def _adoption_seed():`
- 作用: 返回单个 macro_intent 的采纳率计数器初始字典（全 0）。
- 参数: 无。
- 返回: dict，键固定为 10 个：`frames`、`deploys`、`reg_frames`、`reg_hit`（L83）、`hold_frames`、`hold_ok`（L84）、`card_frames`、`card_hit`（L85，注释标明「7h：首部署槽 == plan.suggested_card」）、`budget_frames`、`budget_ok`（L86，注释标明「7h：bundle 花费 ≤ plan.elixir_budget×圣水」）；值均为 0。
- 实现: L83-86 直接 return 一个字面量字典，无循环无分支。
- 调用: 被 `_record_adoption`（L120，`setdefault(plan.macro_intent, _adoption_seed())`）调用。
- 置信度: 已确认

### S:_adoption_summary  [L89-108]
- 类型: function
- 签名: `def _adoption_summary(adoption):`
- 作用: 把逐意图的原始计数字典转成可落盘的摘要字典：把 hit/frames 折算成 4 个 rate（样本为 0 时该 rate 为 None），并保留原始帧数。
- 参数:
  - `adoption`: `{macro_intent: 计数字典}`，计数字典结构同 `_adoption_seed()` 返回值。
- 返回: `{macro_intent: {...}}`，每项含 `frames`、`deploys`、`region_frames`、`region_rate`、`hold_frames`、`hold_rate`、`card_frames`、`card_rate`、`budget_frames`、`budget_ok_rate`（L93-107）。
- 实现: L91 注释说明「rate 为空样本返回 None」；L92 逐意图遍历；L93-94 直取 frames/deploys；L95-97 `region_rate = round(reg_hit/reg_frames, 4) if reg_frames else None`；L98-100 同法算 `hold_rate = hold_ok/hold_frames`；L101-103 算 `card_rate = card_hit/card_frames`；L104-106 算 `budget_ok_rate = budget_ok/budget_frames`；L108 返回。
- 调用: 被 `run_eval` 调用（L292），结果放进返回值的 `adoption` 字段（L297），进而被 `run_ablation` 的 `results[...]["adoption"]` 与 CSV 落盘使用（L423、L452-459）。
- 置信度: 已确认

### S:_record_adoption  [L111-154]
- 类型: function
- 签名: `def _record_adoption(stats, plan, bundle, obs=None):`
- 作用: 逐意图采纳探针：在当前帧的 plan 意图标签下，累计「模型本帧动作与 plan 建议是否吻合」的 4 类计数（region 几何吻合、save_ace 的 hold 服从、7h 对牌采纳、7h 预算服从）。docstring L118 说明 full vs plan-off 的 Δ 即 plan 注入的因果采纳。
- 参数:
  - `stats`: 全局统计字典（`run_eval` 的 `stats`），必须含 `"adoption"` 子字典（L120 用 `setdefault`）。
  - `plan`: `PlanToken` 实例，读取 `macro_intent`、`focus_region`、`suggested_card`、`elixir_budget`、`hold_slots()`。
  - `bundle`: `ActionBundle`，遍历其 `sub_actions` 取 `kind/slot/x/y`（L122-123），并用 `bundle.sub_actions` 直接计数（未用 `bundle.size`）。
  - `obs` (默认 None): 观测字典；用于取 `hand` 与 `elixir`（L137-145）。为 None 时预算项不统计。
- 返回: 无返回值（None）；副作用是原地更新 `stats["adoption"]`。
- 实现: L120 取/建 `plan.macro_intent` 对应的计数块并 `frames += 1`；L122-123 收集本帧所有合法部署 `dep = [(slot, x, y)]`，条件是 `sa.kind == "deploy"` 且 `1 <= sa.slot <= K_MAX`；L124-125 无部署则直接 return（因此 deploys/后续指标只在有部署帧累计）；L127 用 `_plan_region_hit(plan.focus_region, dep[0][1], dep[0][2])` 只看**首个**部署格，命中非 None 时 L129-130 累加 `reg_frames`/`reg_hit`；L132-135 若 `suggested_card` 在 `1..K_MAX` 内，则 `card_frames += 1` 且首部署槽 `dep[0][0]` 等于它时 `card_hit += 1`；L137-148 预算项：仅当 `plan.elixir_budget < 1 - 1e-6`（即 plan 真的限了预算）且 obs 含 `hand`、`elixir` 时，L140 把 `obs["elixir"]`（shape-(1,) 数组，注释说明 numpy 新版本禁止隐式标量转换）经 `np.asarray(...).item()` 取标量乘以 `elixir_budget` 得 `allowed`，L142-145 遍历 dep 用 `obs["hand"][slot-1]` 取卡 id 并查 `ENTITY_NAMES[cid]` 构造 `Card(...)` 累加其 `.elixir` 得 `spent`（cid 越界则不计），L146-148 累加 `budget_frames`，并在 `spent <= allowed + 0.01` 时 `budget_ok += 1`；L149-154 若 `plan.macro_intent == "save_ace"`，取 `plan.hold_slots()`，非空时 `hold_frames += 1`，且本帧没有任何部署落在 hold 槽里时 `hold_ok += 1`。
- 调用: 被 `run_eval` 在每帧策略出牌后调用（L209，注释标明「plan 意图（注入前/置零前同标签）vs 模型本帧动作」）；其内部调用 `_adoption_seed`（L120）、`_plan_region_hit`（L127）、`Card`（L145）。
- 置信度: 已确认

### S:_make_opponent  [L157-165]
- 类型: function
- 签名: `def _make_opponent(env, opponent, opponent_policy=None, rng=None):`
- 作用: 按对手类型字符串构造对手对象：`heuristic` ⇒ 脚本对手；`checkpoint` ⇒ 用 `FollowerOpponent` 包装加载的策略；其它（含 `random`）⇒ 返回 None（由环境内置随机对手接管）。
- 参数:
  - `env`: RLEnv 实例，传给 `heuristic_opponent` 与 `FollowerOpponent` 的构造。
  - `opponent`: 字符串，源码只显式判 `"heuristic"`（L158）与 `"checkpoint"`（L160），其余走 L165 的 `return None  # random`。
  - `opponent_policy` (默认 None): checkpoint 路径；`opponent == "checkpoint"` 且该值为假时 L162 抛 `ValueError("--opponent checkpoint 需要 --opponent-policy")`。
  - `rng` (默认 None): 传给 `heuristic_opponent(env, rng)`（L159）的随机源。
- 返回: `heuristic_opponent(env, rng)` 的返回值（L159） / `FollowerOpponent(pol, env)` 实例（L164） / `None`（L165）。
- 实现: L158-159 `heuristic` 分支直接返回 `heuristic_opponent(env, rng)`；L160-164 `checkpoint` 分支先做参数校验（L161-162），再 `pol = load_checkpoint(opponent_policy)`（L163）后返回 `FollowerOpponent(pol, env)`；L165 兜底返回 None。注意 `load_checkpoint` 此处未传 `hidden_dim`。
- 调用: 被 `run_eval` 调用（L172，赋值给 `env.opponent`）。它调用了 `heuristic_opponent`、`load_checkpoint`、`FollowerOpponent`（三者分别定义于 `rl/train_follower.py:33`、`rl/follower.py:67`、`rl/train_follower.py:52`）。
- 置信度: 已确认

### S:run_eval  [L168-301]
- 类型: function
- 签名: `def run_eval(policy_path, n_games=50, opponent="random", seed=0, hidden_dim=None, max_steps=300, opponent_policy=None, ablation=None):`
- 作用: 主评测入口：跑 `n_games` 局对局，累计胜负/回报/bundle 合法率/圣水效率/Plan 意图分布/逐意图采纳率/皇冠与塔血差/信念指标，打印人类可读报告并返回汇总字典（含 winrate 与二项 SE）。
- 参数:
  - `policy_path`: 被评测策略 ckpt 路径，传给 `load_policy`（L174）。
  - `n_games` (int, 默认 50): 对局数（L189 的 for 上界）。
  - `opponent` (str, 默认 "random"): 对手类型，传给 `_make_opponent`（L172）。
  - `seed` (int, 默认 0): 同时用于 `RLEnv(seed=...)`（L170）、`random.Random(seed)`（L171）、`BeliefInference(seed=...)`（L175）。
  - `hidden_dim` (默认 None): 转交 `load_policy`（L174）。
  - `max_steps` (int, 默认 300): 每局步数上限；与 `_overtime_open` 组成 while 条件（L195）。
  - `opponent_policy` (默认 None): checkpoint 对手路径，转交 `_make_opponent`（L172）。
  - `ablation` (默认 None): 取 `"plan"`/`"belief"`/`"both"` 时分别把 plan_vec 或 belief token 置零（L200-203）。
- 返回: dict（L293-301）：`winrate`、`mean_reward`、`wins`、`losses`、`draws`、`n_games`、`adoption`（`_adoption_summary` 结果）、`winrate_se`（二项标准误 `sqrt(p(1-p)/N)`，L298-301）。
- 实现: L170 建 `RLEnv(opponent=None, seed=seed)`，L171 建 `random.Random(seed)`，L172 用 `_make_opponent` 的返回值赋给 `env.opponent`；L174-176 依次加载策略、`BeliefInference(opp_deck=env.deck1, n_particles=128, seed=seed)`、`BeliefPlanner()`；L178-186 建统计字典；L187 用 `ENTITY_NAMES.index(c) for c in env.deck1` 得到 `deck_ids`（对手牌组的全局 id 列表）。主循环 L189-265：每局 `env.reset()`（L190）、`belief.reset(env.deck1)`（L191）、`hidden=None`、`steps=0`；L195 的 while 条件为 `not done and (steps < max_steps or _overtime_open(env.battle))`（即到步数上限后若在加时窗口则继续）；循环体依次：L196 `bp.plan(env.battle, belief.state(), obs)` 得 plan，L197 累加 `plan_intents`，L198 `plan.to_vector()`，L199 `belief.encode(obs, None)`，L200-203 按 ablation 置零 plan_vec / tok，L204-205 `policy.act(obs, tok, plan_vec, env.get_action_mask, hidden=hidden, deterministic=True)`；L206-207 记 `bundle_sizes`、`bundle_total`；L209 调 `_record_adoption`；L211-215 遍历 sub_actions 把合法 deploy 的卡费累加进 `elixir_spent`（用决策时刻 `obs["hand"]`）；L216 `env.step(bundle)`；L217-218 `info["bundle_ok"]` 为假时 `bundle_illegal += 1`；L219 `belief.update(obs, info.get("opp_played"))`；L220-241 若 `info["hidden"]` 存在则统计信念：真实下一张 `hid["opp_next"]`、预测 `deck_ids[argmax(st.next_probs)]`、`next_total/next_hits`、Brier（对 one-hot 求均方，仅当 real_next 在 deck_ids 中才置 1，L230-233）、置信度与正确性（L235-236）、手牌 Top4 重叠（L238-241，`stats["hand_total"] += 4`）；L242-244 `done = term or trunc`、累计 `rew`、`steps += 1`。循环后 L245 取 `env.battle.winner`；L246-254 若 winner 为 None 且引擎尚未 game_over，则惰性 import `timeout_winner`（L248）做皇冠差补判，并按 `env.reward_weights` 的 `win_bonus`/`lose_penalty` 手动补 rewards（L250-254，注意 `lose_penalty` 是 `-=`）；L255-260 按 w 归入 wins/losses/draws；L261-265 追加本局皇冠差与三维塔血差（`king+left+right` 双方相减）。L267-291 打印报告（含 `_ece(conf, acc)` L284、Plan 意图按计数降序 L288）；L292 生成 adoption 摘要；L293-301 返回汇总 dict。
- 调用: 被 `run_ablation` 逐变体调用（L331）；CLI 的 else 分支调用（L555）。它调用了 `_overtime_open`、`_record_adoption`、`_adoption_summary`、`_ece`、`_make_opponent`、`load_policy`、`RLEnv`、`BeliefInference`、`BeliefPlanner`、`Card`、`K_MAX`、`ENTITY_NAMES`、`rl.run_league.timeout_winner`。仓内其它调用点未逐一检索（未确认是否还有别的模块直接调用 run_eval）。
- 置信度: 已确认

### S:run_ablation  [L316-470]
- 类型: function
- 签名: `def run_ablation(policy_path, n_games=200, opponent="random", seed=0, hidden_dim=None, max_steps=300, opponent_policy=None, out_path=None):`
- 作用: 跑 full / plan-off / belief-off / both-off 四种 belief/plan 注入消融变体，计算各变体 winrate±SE、相对 full 的 delta/delta SE/z、以及逐意图与合计的采纳率对比，打印并可选落盘 JSON + CSV。
- 参数:
  - `policy_path`: 被评测 ckpt 路径，逐变体透传给 `run_eval`（L331）。
  - `n_games` (int, 默认 200): **每个变体**的局数（docstring L329 打印 `N={n_games}/变体`）。
  - `opponent` (str, 默认 "random"): 对手类型，透传 `run_eval`。
  - `seed` (int, 默认 0): 所有变体共用同一 seed（透传 `run_eval`）。
  - `hidden_dim` (默认 None): 透传 `run_eval`。
  - `max_steps` (int, 默认 300): 透传 `run_eval`。
  - `opponent_policy` (默认 None): 透传 `run_eval`。
  - `out_path` (默认 None): JSON 落盘路径；给出时同时写同名 `.csv`（L439）。
- 返回: `out` dict（L417-434）：`policy`（`os.path.abspath`）、`opponent`、`n_games`、`seed`、`max_steps`、`variants`（每变体 winrate/winrate_se/mean_reward/wins/losses/draws/n_games/adoption）、`deltas_vs_full`、`adoption_vs_plan_off`、`adoption_aggregate`、`note`。
- 实现: docstring L318-326 给定判定规则与注意事项（token 置零保守、RNN hidden 仍携带历史信息、|z|≥2 视为真实贡献）。L327 建 `results`；L328-329 打印表头；L330-337 遍历 `ABLATION_VARIANTS`，每项调 `run_eval(...)` 存入 `results[name]` 并按行格式化打印（`flush=True`）。L339-349 定义内部函数 `delta_vs(name)`：取 full 与该变体的 winrate 差 `d`、`se = math.hypot(两个 SE)`（L342）、`z = d/se if se > 0 else None`（L343），返回四舍五入后的 `full_winrate`/`abl_winrate`/`delta`/`delta_se`/`z` 与 `verdict`（`abs(z) >= 2` ⇒ `"贡献/损害显著"`，否则 `"无显著差异（噪声内）"`，L348-349）；L351 对 `plan-off`/`belief-off`/`both-off` 三个键构造 `deltas`；L352-355 打印。L357-390 采纳率对比：取 `full`/`plan-off` 的 adoption（L359-360），打印表头 L361-364，L365 按 full 帧数降序遍历意图并集，L367-372 取两侧 rate 并算差值（任一为 None 则该项为 None），L373-381 组装 `adoption_vs_off[intent]`，L382-390 格式化打印。L393-401 定义内部 `_adopt_agg(a)`：对某变体的全部意图求和 `card_frames`/`card_hit`/`budget_frames`/`budget_ok`，返回 `card_frames`、`card_rate`、`budget_frames`、`budget_ok_rate`（分母为 0 则 rate 为 None）；L403 对 full 与 plan-off 各求一次；L404-415 打印合计与 `Δcard`/`Δbudget`。L417-434 组装 `out`（`variants` 用字典推导只保留 `adoption` 等字段，L420-424；`note` L430-433 复述口径）。L435-469 落盘：若给 `out_path` 则 L436 取绝对路径、L437-438 `json.dump(..., ensure_ascii=False, indent=2)`；L439 用 `os.path.splitext(out_path)[0] + ".csv"` 建 CSV 路径，L440-441 写表头 `variant,winrate,...`，L442-444 写各变体行，L445-447 写 `# delta vs full` 注释行，L448-459 写按意图的采纳明细（只写 `full` 与 `plan-off` 两个变体，空 rate 写成空串），L460-467 写 `# adoption delta (full - plan-off)` 注释行；L468-469 打印两个落盘路径；L470 返回 `out`。
- 调用: CLI `--ablation all` 分支调用（L550-553）；仓内 `rl/selftest.py:1458` 有 `from rl.evaluate import run_ablation`（已确认的另一个调用方）。它调用了 `run_eval` 与 `math.hypot`。
- 置信度: 已确认

### S:load_policy  [L473-474]
- 类型: function
- 签名: `def load_policy(path, hidden_dim=None):`
- 作用: 策略加载薄封装，直接转发到 `rl.follower.load_checkpoint`（docstring L4 说明这是 P0-5 修复：从 checkpoint 元数据读维度，不再硬编码 belief_dim）。
- 参数:
  - `path`: ckpt 路径。
  - `hidden_dim` (默认 None): 转交 `load_checkpoint(path, hidden_dim=hidden_dim)`（`rl/follower.py:67` 的签名含该参数）。
- 返回: `load_checkpoint` 的返回值（策略对象）。
- 实现: L474 单行 `return load_checkpoint(path, hidden_dim=hidden_dim)`，无其它逻辑。
- 调用: 被 `run_eval`（L174）与 `evaluate_belief`（L480）调用。注意 `_make_opponent` 里的 checkpoint 对手**不**经此函数（L163 直接调 `load_checkpoint`）。
- 置信度: 已确认

### S:evaluate_belief  [L477-523]
- 类型: function
- 签名: `def evaluate_belief(policy_path, n_games=500, seed=0, hidden_dim=None, max_steps=300):`
- 作用: 独立信念评测协议（docstring L478 自称「只统计信念预测 vs 特权标签（N=500，规格 §11.2）」）：跑对局只累计 Next-Card 命中/Brier/ECE 三项，并打印 uniform 先验 Brier 基线。
- 参数:
  - `policy_path`: ckpt 路径（L480）。
  - `n_games` (int, 默认 500): 对局数（L487）。
  - `seed` (int, 默认 0): 用于 `RLEnv(seed=...)`（L479）与 `BeliefInference(seed=...)`（L481）。
  - `hidden_dim` (默认 None): 转交 `load_policy`（L480）。
  - `max_steps` (int, 默认 300): while 条件为 `steps < max_steps`（L493），**不**调用 `_overtime_open`（与 `run_eval` 的 L195 不同）。
- 返回: `{"next_acc": hits / max(1, total), "next_brier": brier / max(1, total)}`（L523）。
- 实现: L479 建环境（`opponent=None`，即随机构造的对手）、L480 加载策略、L481 建 `BeliefInference(opp_deck=env.deck1, n_particles=128, seed=seed)`、L482 建 `BeliefPlanner()`；L483 得 `deck_ids`；L484-486 初始化 `hits`/`total`/`brier` 与两个列表 `confs`/`accs`。L487-515 主循环与 `run_eval` 同构但**丢弃** reward/bundle 统计：L488 reset、L489 belief.reset、L494 plan、L495 belief token、L496-497 `policy.act(..., deterministic=True)`、L498 `env.step`、L499 `belief.update`；L500-513 若 `info["hidden"]` 存在则累加 total、命中数、Brier（one-hot 同 L508-511 逻辑）、置信度 `st.next_probs.max()` 与正确性。循环后 L516 把 confs/accs 转 np 数组；L517-522 打印 `Next-Card Acc`、`Next-Card Brier` + `ECE`（L519 调 `_ece`）、以及 `vs uniform 先验 Brier` 基线（用 `np.full(len(deck_ids), uniform)` 与 `np.eye(len(deck_ids))[0]` 的均方差，L520-522）；L523 返回两项汇总（注意返回值**不含** acc 的 N 或 ECE）。
- 调用: CLI `--belief-only` 分支调用（L544-546）。它调用了 `RLEnv`、`load_policy`、`BeliefInference`、`BeliefPlanner`、`_ece`、`np`。仓内其它模块是否调用未检索确认。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| src/clasher_new/rl/evaluate.py | 11 | 11 |

COVERAGE_OK G016 files=1 symbols=11
