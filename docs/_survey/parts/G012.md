# 组 G012 文件级分析

## F:scripts/pomdp_ceiling_probe.py
- 行数: 726
- 模块作用: 一个**只读**离线探针脚本（不参与训练路径）：用已训练 ckpt 跑 rollout 收集逐帧特征/回报/价值，再在局分组留出的数据上训练 Ridge / MLP 回归器，检验「当前逐帧观测能否解释**局内**回报变化」。定义三套特征集 A（跟随者实际观测 obs 展平）、C（A + belief token）、B（A + oracle 隐藏量：对手循环/手牌/进手/圣水），并给出 P1/P2/P3/P0 形式判决（L648-715）。依据：文件头 docstring L2-14 + main 的整体流程。
- 模块级 docstring 首行: `只读探针：POMDP 信息天花板检验。`
- 主要导入: argparse, json, os, sys, time, numpy(as np), torch, rl.config(TrainConfig), rl.belief(BeliefInference), rl.belief_planner(BeliefPlanner), rl.plan_space(PLAN_DIM), rl.follower(FollowerPolicy, load_checkpoint), rl.train_follower(FollowerOpponent), rl.train_solo(solo_env, resolve_deck_set), rl.ppo(PPOTrainer), rl.observation(ENTITY_NAMES)
- 关键模块级常量:
  - `_HERE = os.path.dirname(os.path.abspath(__file__))` — 本脚本所在目录（scripts/），L28
  - `_ROOT = os.path.dirname(_HERE)` — 仓库根，L29；用于默认输出路径 L718 与 sys.path 注入 L30
  - `_SRC = os.path.join(_ROOT, "src", "clasher_new")` — 待插入 sys.path 的源码目录，L30-32
- 顶层数据表/字典: 无（无引擎数值表 / 卡牌表；仅有 main 内构造的 `res` 结果字典 L569-584）

### S:feat_obs  [L51-58]
- 类型: function
- 签名: `def feat_obs(obs) -> np.ndarray:`
- 作用: 构造特征集 A —— 把跟随者本帧真实观测的 5 个字段各自展平后拼接成一个一维向量。
- 参数:
  - `obs` (无类型注解): 单帧观测字典，须含键 `grid`/`hand`/`elixir`/`next_card`/`time`（由 L139 处的 `env` 产出）。
- 返回: `np.float32` 一维 `np.ndarray`，= 上述 5 个字段 ravel 后的 `np.concatenate`（L58）。
- 实现: L53-57 对五个键分别 `np.asarray(..., dtype=np.float32).ravel()` 后放进列表 `parts`；L58 用 `np.concatenate(parts)` 拼成一维。没有任何归一化 / 特征选择 / 掩码处理，纯粹是"obs 原样展平"（docstring L52 即此意）。
- 调用: 被 `rollout` 在 L145-147 调用三次（A 集、C 集的前半、B 集的前半）；本身只调用 numpy 函数。
- 置信度: 已确认

### S:make_hidden_mapper  [L61-67]
- 类型: function
- 签名: `def make_hidden_mapper(deck):`
- 作用: 建立「引擎实体 id（`ENTITY_NAMES` 下标）→ 本副牌内的槽位序号」映射，并返回牌组长度。
- 参数:
  - `deck` (无类型注解): 卡名序列（L121 传入 `env.deck0`），元素为字符串卡名。
- 返回: 二元组 `(eid2slot, ndeck)`：`eid2slot` 是 `dict[int, int]`（entity id → slot），`ndeck = len(deck)`（L67）。
- 实现: L63 建空字典；L64-66 用 `enumerate(deck)` 遍历槽位，仅当卡名出现在 `ENTITY_NAMES` 中时，以 `ENTITY_NAMES.index(name)` 为键、`slot` 为值写入；不在词表内的卡名被静默跳过（⇒ 该槽位在后续 one-hot 中永远为 0）。
- 调用: 被 `rollout` 在 L121 调用；它调用 `ENTITY_NAMES.index`。下游 `feat_hidden` 消费其结果。
- 置信度: 已确认

### S:feat_hidden  [L70-90]
- 类型: function
- 签名: `def feat_hidden(hid, eid2slot, ndeck) -> np.ndarray:`
- 作用: 构造特征集 B 的**增量**部分（oracle 隐藏量）：把对手循环序编码成 slot 上的位置编码、手牌与进手编码成 one-hot，再接对手圣水标量。
- 参数:
  - `hid` (无类型注解): 引擎真值标签字典，须含 `opp_cycle`/`opp_hand`/`opp_next`/`opp_elixir`（由 `env.get_hidden_state()` 给出，L144；对应 `rl/observation.py:147-172` 的 `hidden_labels`）。
  - `eid2slot` (无类型注解): `make_hidden_mapper` 产出的 entity id → slot 字典。
  - `ndeck` (无类型注解): 牌组长度，决定三个向量的维度。
- 返回: `np.ndarray`，长度 `3*ndeck + len(opp_elixir)`（L90 的 concatenate：`pos`、`hp`、`np_` 各 `ndeck` 维，再接 `elx`）。
- 实现: L72-74 取 `opp_cycle`/`opp_hand` 并 ravel，取 `opp_next` 的第 0 个元素转 int；L75-79 建 `pos`（长度 ndeck 的 0 向量），对循环中第 k 张牌若能在映射中查到 slot，则写入归一化位置 `k / max(1, ndeck-1)`；L80-84 建 `hp`，对手手牌中的牌对应 slot 置 1.0；L85-88 建 `np_`，`opp_next` 对应 slot 置 1.0；L89 取 `opp_elixir` 为 float32；L90 拼接四项返回。查不到 slot 的卡（不在 `ENTITY_NAMES` 内）被静默丢弃。
- 调用: 被 `rollout` 在 L147 调用（拼在 A 集之后构成 B 集）；只调用 numpy。
- 置信度: 已确认（`hid` 的键名与形状依据 `src/clasher_new/rl/observation.py:147-172`）

### S:rollout  [L96-190]
- 类型: function
- 签名: `def rollout(a, cfg, device):`
- 作用: 用 ckpt 权重在镜像环境中逐局跑 rollout，收集三套逐帧特征（A/C/B）、GAE 回报 `y`、critic 价值 `v` 与局编号 `ep`。
- 参数:
  - `a` (无类型注解): argparse 命名空间，使用到的字段：`seed_rng`/`seed`/`ckpt`/`mode`/`frames`/`verbose`（L100-101、L108、L116、L138、L177）。
  - `cfg` (`TrainConfig`): 由 main L474 解析；用 `cfg.hidden_dim`、`cfg.gamma`、`cfg.gae_lambda`、`cfg.deck_set`。
  - `device` (str): 由 main L485 决定 `"cpu"` 或 `"cuda"`。
- 返回: 四元组 `(X, y, v, ep)`：`X` 是 `{"A","C","B"}` → `np.float32` 二维数组（L181-183）；`y` 为 float64 GAE 回报（L184）；`v` 为 float64 critic 估计（L185）；`ep` 为 int64 局编号（L186）。
- 实现: L100-104 若 `a.seed_rng` 为真（默认）则 `torch.manual_seed(a.seed)` 并按需 `cuda.manual_seed_all`，目的是让 `pol.act` 内的全局 RNG 采样可复现；L105 建镜像环境 `solo_env(cfg, a.seed)`；L106-109 用 `BeliefInference(opp_deck=env.deck1, n_particles=128, seed=a.seed).encode(None, None)` 的长度决定 `belief_dim`（`encode` 支持 obs/opp_played 双 None，见 `rl/belief.py:344`），再 `load_checkpoint` 载入策略并 `to_device(device).eval()`；L111-115 另建一个 `FollowerPolicy` 作为对手并 `load_state_dict(pol.state_dict())`；L117-119 用 `FollowerOpponent` 包装对手，`deterministic=(a.mode=="det")`，信念种子为 `a.seed+1`；L120 调用 `resolve_deck_set` 但**丢弃返回值**（疑为遗留调用）；L121 建隐藏量映射。L123-127 建 `BeliefPlanner` 与 player0 侧 `BeliefInference`，`env.reset(seed=a.seed)`、`belief.reset(env.deck1)`、`hidden=None`。L129-134 初始化各逐帧/逐局缓冲与计数。L136 起 `torch.no_grad()` 内循环：L139 `bp.plan(...)` 得计划、L140 `belief.encode(obs, None)` 得 token、L142-143 `pol.act(...)` 得 bundle/value/hidden，L144 取引擎隐藏真值，L145-147 分别构造 A（`feat_obs`）、C（A+token）、B（A+`feat_hidden`）三条样本，L148 记 value，L149 `env.step(bundle)`，L150-152 记奖励/终止/截断，L153 用 `info["opp_played"]` 更新信念。L156-179 局结束分支：若为截断（非终止）则再前向算 `last_val`（L158-162），用 `PPOTrainer.compute_gae(..., truncated=ep_trunc, last_value=last_val)` 得逐帧回报（L163-165），把缓冲 extend 进全量列表并记局长/局号（L166-171），清空缓冲后 `env.reset(seed=a.seed+step)` 与信念/hidden 复位（L174-176）；L177-179 每 10 局打印进度。L180-189 记录墙钟、堆成数组并打印帧率。注意 `n_frame` 是累计帧数、循环条件为 `n_frame < a.frames`（L138），因此**最后一步可能超过 frames 上限**；局号 `ep_idx` 从 0 递增。
- 调用: 被 `main` 在 L489 调用（`--npz` 未给时）；它调用 `solo_env`、`load_checkpoint`、`FollowerPolicy`、`FollowerOpponent`、`resolve_deck_set`、`make_hidden_mapper`、`BeliefPlanner`、`BeliefInference`、`feat_obs`、`feat_hidden`、`PPOTrainer.compute_gae`、`env.reset/step/get_action_mask/get_hidden_state`。是否被仓库其它脚本调用：待确认（本组文件外未检索）。
- 置信度: 已确认（其中 `hidden_labels` 键名由 `src/clasher_new/rl/observation.py:147-172` 确认）

### S:ev  [L196-199]
- 类型: function
- 签名: `def ev(pred, y):`
- 作用: 池化（全局）解释方差 `1 - MSE/Var(y)`。
- 参数:
  - `pred` (无类型注解): 预测值序列。
  - `y` (无类型注解): 目标序列。
- 返回: `float`；当 `np.var(y) <= 1e-12` 时返回 `None`（L199）。
- 实现: L197 把两者转 float64 算 MSE；L198 算目标方差；L199 用 `None if var <= 1e-12 else 1.0 - mse/var` 返回。无裁剪、无分组。
- 调用: 被 `fit_ridge` L367、`fit_mlp` L407、`main` L562/L580/L604/L606/L617/L621 调用。
- 置信度: 已确认

### S:ev_within  [L202-213]
- 类型: function
- 签名: `def ev_within(pred, y, ep):`
- 作用: 局内中心化 EV：**预测与目标都在各自局内减去局均值**后再算 `1 - MSE/Var`，只衡量局内变化。
- 参数:
  - `pred` (无类型注解): 预测值。
  - `y` (无类型注解): 目标值。
  - `ep` (无类型注解): 与样本等长的局编号数组。
- 返回: `float`；`np.var(yy) <= 1e-12` 时返回 `None`（L211-212）。
- 实现: L204-205 各做一次 `copy()` 避免改原数组；L206-209 遍历 `np.unique(ep)`，对每局的布尔掩码 `m` 分别减去该局均值（预测和目标都减）；L210 用中心化目标算方差；L211-212 方差过小返回 `None`；L213 返回 `1 - mean((p-yy)^2)/var`。
- 调用: 被 `fit_ridge` L367、`fit_mlp` L407、`main` L562-563/L580-581/L605/L607/L618/L622 调用。
- 置信度: 已确认

### S:ev_within_raw  [L216-224]
- 类型: function
- 签名: `def ev_within_raw(pred, y, ep):`
- 作用: 用「局内中心化目标的方差」作分母、但**分子用未中心化的原始预测**对原始目标的 MSE 的 EV 变体。
- 参数:
  - `pred` (无类型注解): 预测值。
  - `y` (无类型注解): 目标值。
  - `ep` (无类型注解): 局编号。
- 返回: `float`；`np.var(yc) <= 1e-12` 时返回 `None`（L222-223）。
- 实现: L217-220 只把 `y` 复制并逐局减去局均值得到 `yc`；L221 用 `yc` 算方差；L222-223 方差过小返回 `None`；L224 **分子是 `mean((pred - y)^2)`（未中心化）**除以 `var(yc)`。注意该函数**没有 docstring**；`pred` 未中心化而分母用了中心化目标的方差 ⇒ 与 `ev_within` 的口径不同（预测中残留的局均值水平差会直接进入分子）。
- 调用: 被 `main` L563/L581/L608/L619 调用（打印与结果字典）；`fit_ridge`/`fit_mlp` 不调用它。
- 置信度: 已确认（口径差异依源码 L217-224 逐行确认）

### S:ev_win128_med  [L227-237]
- 类型: function
- 签名: `def ev_win128_med(pred, y, ep, win=128):`
- 作用: 把每局按 128 帧等长不重叠窗口切开，算每个窗口内的 EV，返回这些 EV 的**中位数**与窗口计数。
- 参数:
  - `pred` (无类型注解): 预测值。
  - `y` (无类型注解): 目标值。
  - `ep` (无类型注解): 局编号。
  - `win` (int, 默认 128): 窗口长度。
- 返回: 二元组 `(中位数或 None, 有效窗口数)`（L237）；无有效窗口时中位数为 `None`。
- 实现: L228 建 `vals`；L229 转 float64；L230-236 遍历每局，取该局样本下标 `idx`，按 `range(0, len(idx)-win+1, win)` 切窗口；窗口内 `Var(y)<=1e-12` 则 `continue`（不计数）；否则 append `1 - MSE/Var`；L237 返回 `np.median(vals)`（空则为 None）与 `len(vals)`。**不足一个窗口的尾部被丢弃**。
- 调用: 被 `main` L565（对 critic 价值 `v` 求该指标）与 L609/L620（对模型预测）调用。
- 置信度: 已确认

### S:var_decomp  [L240-249]
- 类型: function
- 签名: `def var_decomp(y, ep):`
- 作用: 把目标方差分解成「局间（局均值之间的方差）」与「局内（各局方差的样本量加权平均）」两部分。
- 参数:
  - `y` (无类型注解): 目标值数组（须支持布尔掩码索引与 `.mean()`/`.var()`）。
  - `ep` (无类型注解): 局编号。
- 返回: 三元组 `(tot, between, within)`（L249）：`tot = np.var(y)`（总体方差）、`between = np.var(局均值数组)`（**未按局样本量加权**）、`within = Σ(n_e * Var(y_e)) / Σ n_e`（样本量加权平均的局内方差）。
- 实现: L241 算总体方差；L242 初始化 `means`/`wsum`/`within`；L243-247 逐局累加该局均值、样本数与 `n_e * Var(y_e)`；L248 对局均值数组（长度 = 局数）取方差；L249 用 `max(1.0, wsum)` 防除零并返回。
- 调用: 被 `main` L501（对真实回报算 `tot_real/bet_real/wit_real`，用于阳性对照的方差配平）与 L558 调用。
- 置信度: 已确认

### S:clock_perm  [L252-266]
- 类型: function
- 签名: `def clock_perm(n, ep, seed=0):`
- 作用: 构造一个行置换下标数组：只在「同一条局内位置 k」的样本之间跨局互换，从而打散"状态↔回报"配对而保留时钟位置结构。
- 参数:
  - `n` (int): 样本总数。
  - `ep` (无类型注解): 长度 `n` 的局编号数组。
  - `seed` (int, 默认 0): 随机种子基础值；实际用 `seed + 707` 建 RNG（L254）。
- 返回: `np.int64` 数组 `perm`，长度 `n`，语义为 `X_new = X[perm]`。
- 实现: L254 建 `default_rng(seed+707)`；L255-258 建 `k`：每局内按出现顺序赋 0,1,2,… 的局内位置；L259 `perm = np.arange(n)`；L260-265 对每个 k 值取对应下标集合 `idx`，若不足 2 个元素则跳过（该位置保持自映射）；否则 `tgt = idx[rng.permutation(len(idx))]` 后在 L265 写 `perm[idx] = np.roll(tgt, 1)`，即把置换结果整体错开一位后写回（代码注释 L265 称此 `roll` 保证不落在"自己那一行"）。
- 调用: 被 `scramble_rows_by_clock` L276 与 `scramble_target_by_clock` L288 调用。
- 置信度: 已确认（各行号行为依源码；关于 roll 是否对所有置换都严格无不动点，源码只给了注释断言，未做检查）

### S:scramble_rows_by_clock  [L269-276]
- 类型: function
- 签名: `def scramble_rows_by_clock(X, ep, seed=0):`
- 作用: 按「局内位置相同、来自不同局」的行置换打散特征矩阵（证伪对照用）：时钟信息保留、状态信息被打散。
- 参数:
  - `X` (无类型注解): 特征矩阵（可为任意支持 `np.asarray` 与花式索引的对象）。
  - `ep` (无类型注解): 局编号。
  - `seed` (int, 默认 0): 传给 `clock_perm`。
- 返回: `np.asarray(X)` 经 `perm` 花式索引后的新数组（L276），形状与 `X` 相同。
- 实现: 单行实现：`np.asarray(X)[clock_perm(len(ep), ep, seed)]`（L276）。docstring L270-275 说明其判读含义：若真实目标上的 `EV_within` 在此输入下不塌，则说明模型用的是与局无关的公共结构而非状态。
- 调用: 被 `main` L535 对 `X` 的每个特征集调用（`--scramble-x` 时）。
- 置信度: 已确认

### S:scramble_target_by_clock  [L279-288]
- 类型: function
- 签名: `def scramble_target_by_clock(y, ep, seed=0):`
- 作用: 用同一类时钟置换打散**目标**（阴性对照）：保留目标边缘分布与局内位置结构，只破坏"哪一帧特征对应哪个回报"的配对。
- 参数:
  - `y` (无类型注解): 目标数组。
  - `ep` (无类型注解): 局编号（用于 `len(y)` 与位置分组）。
  - `seed` (int, 默认 0): 传入 `clock_perm` 时被加 909（L288）。
- 返回: `np.asarray(y)[clock_perm(len(y), ep, seed + 909)]`（L288）。
- 实现: 单行实现（L288），用与特征侧不同的种子偏移（+909）以免与 `scramble_rows_by_clock` 使用同一置换。docstring L280-287 说明合格估计器在该靶子上必须给 ≈0。
- 调用: 被 `main` L519 调用（`--scramble-y` 时，在中心化之后执行，见 L516-521 注释）。
- 置信度: 已确认

### S:load_npz  [L292-300]
- 类型: function
- 签名: `def load_npz(path):`
- 作用: 从 `--save-npz` 落盘的 npz 文件离线重建 `(X, y, v, ep)`，免于重跑 rollout。
- 参数:
  - `path` (无类型注解): npz 文件路径。
- 返回: 四元组 `(X, y, v, ep)`，与 `rollout` 的返回同构（L300）；`X` 的键来自 npz 中 `X_` 前缀的文件名去掉前缀后的部分（L295-296）。
- 实现: L294 `np.load(path)`；L295-296 字典推导：对 `z.files` 中以 `"X_"` 开头的键，按 `k.split("_", 1)[1]` 取集名并 astype float32；L297-299 分别取 `z["y"]`→float64、`z["v"]`→float64、`z["ep"]` 原样。
- 调用: 被 `main` L489 调用（`--npz` 给定时，走三元表达式的 else 分支）。
- 置信度: 已确认（依赖 main L495-496 的落盘键名约定）

### S:make_positive_control  [L303-331]
- 类型: function
- 签名: `def make_positive_control(Xa, ep, seed=0, within_var=58.0, between_var=166.0):`
- 作用: 构造闸门 G5 的阳性对照目标：用少量真实特征列合成一个**按构造必然存在**的局内信号，再叠加局间水平偏移，方差按真实回报的局内/局间量配平。
- 参数:
  - `Xa` (无类型注解): 真实特征矩阵（main 传 `X["A"]`，L504），只用来选列。
  - `ep` (无类型注解): 局编号。
  - `seed` (int, 默认 0): 实际用 `seed + 101` 建 RNG（L314）。
  - `within_var` (float, 默认 58.0): 目标局内方差；main 调用时显式传入真实回报的 `wit_real`（L505），故默认值仅在直接调用时生效。
  - `between_var` (float, 默认 166.0): 目标局间方差；main 调用时传 `bet_real`（L505）。
- 返回: 二元组 `(y, meta)`：`y` 为合成目标数组；`meta` 字典含 `cols`（被选中的列下标列表）、`within_var`、`between_var`、`realized_var`（L330-331）。
- 实现: L313 转 float64；L314 建 RNG；L315-317 按列标准差降序取前 64 列作为候选，再从候选中**不重复**随机抽 8 列；L318 把这 8 列各自零均值单位方差化；L319 用固定权重合成非线性信号 `1.0*z0 + 0.7*tanh(1.5*z1) + 0.5*z2*z3 + 0.3*z4`（只用 5 列，其余 3 列被抽出但未参与合成本式）；L320-322 逐局减去局均值；L323 把信号缩放到 std = sqrt(within_var)；L324-328 建 `off`，每局填一个 `N(0, sqrt(between_var))` 的常数偏移；L329-331 返回 `s + off` 与元信息。注意局间偏移按**局**独立抽（未保证与真实局间分布的其它性质一致）。
- 调用: 被 `main` L504 调用（`--positive-control` 时）。
- 置信度: 已确认（方差配平语义由 docstring L304-312 与 L323-328 共同确认）

### S:standardize  [L337-341]
- 类型: function
- 签名: `def standardize(Xtr, Xs):`
- 作用: 用训练集的均值/标准差对若干矩阵做标准化（只在训练集上统计，避免泄漏）。
- 参数:
  - `Xtr` (无类型注解): 训练集特征矩阵，用于统计 `mu`/`sd`。
  - `Xs` (无类型注解): 待变换矩阵的**列表**（L341 用列表推导逐项变换）。
- 返回: 变换后的矩阵列表（与 `Xs` 等长、同序）。
- 实现: L338 算训练集列均值；L339 算训练集列标准差；L340 把 `sd < 1e-8` 的列置 1.0 防除零；L341 返回 `[(x - mu) / sd for x in Xs]`。
- 调用: 被 `main` L597 调用，一次传入 `[Xk[m_tr], Xk[m_va], Xk[m_te]]`（三者共用训练集统计量）。
- 置信度: 已确认

### S:fit_ridge  [L344-375]
- 类型: function
- 签名: `def fit_ridge(Xtr, ytr, Xva, yva, Xte, alphas=None, ep_va=None, select="pooled"):`
- 作用: 在给定 alpha 网格上做岭回归（闭式解），按 `select` 口径在验证集上选最优 alpha，并把预测裁剪到训练目标区间内。
- 参数:
  - `Xtr`/`ytr` (无类型注解): 训练特征与目标。
  - `Xva`/`yva` (无类型注解): 验证特征与目标（用于选 alpha）。
  - `Xte` (无类型注解): 测试特征（用于输出预测）。
  - `alphas` (None, 默认 None): 若为 None 则用 L357 的网格 `(1e-1 … 1e8)` 共 10 个值。
  - `ep_va` (None, 默认 None): 验证集的局编号；当 `select="within"` 时必需（否则 `ev_within` 会拿 None 做分组）。
  - `select` (str, 默认 "pooled"): 模型选择口径；`"pooled"` 用 `ev`，其它值走 `ev_within`（L367）。
- 返回: 四元组 `(预测, alpha, 验证得分, 验证集预测)`（L375）；若所有 alpha 都不可评（`best_a is None`）则返回 `(None, None, None, None)`（L373）。
- 实现: L356-357 设默认网格；L358 取训练目标的最小/最大用于裁剪；L359 取训练目标均值 `mu`；L360-361 预计算 `XtX = Xtr.T @ Xtr` 与 `Xty = Xtr.T @ (ytr - mu)`；L362 建单位阵；L363 初始化 `best`/`best_a`；L364-371 对每个 alpha 解 `(XtX + al*I) w = Xty`，验证预测 `np.clip(Xva @ w + mu, lo, hi)`，按 `select` 算得分，跳过 `None` 得分，取最高分对应的 `(al, w, pv)`；L372-373 全不可评则返回四个 None；L374-375 解包并在**测试集**上输出 `np.clip(Xte @ w + mu, lo, hi)`，同时回传验证集预测。docstring L345-354 记录两处修订缘由（alpha 上限从 10 放宽到 1e8 + 输出裁剪；`select` 口径与主判据对齐）。
- 调用: 被 `main` L600 调用；它调用 `ev`/`ev_within`；测试/验证指标由 main 事后计算。
- 置信度: 已确认

### S:fit_mlp  [L378-422]
- 类型: function
- 签名: `def fit_mlp(Xtr, ytr, Xva, yva, Xte, seed=0, epochs=400, hidden=128, lr=0.001, device="cpu", ep_va=None, select="pooled"):`
- 作用: 训练一个 3 层全连接 MLP 回归器（带早停），同样按 `select` 口径在验证集上选最优 epoch，并裁剪预测到训练目标区间。
- 参数:
  - `Xtr`/`ytr`、`Xva`/`yva`、`Xte` (无类型注解): 训练/验证/测试数据（numpy 数组）。
  - `seed` (int, 默认 0): 传给 `torch.manual_seed`（L381）。
  - `epochs` (int, 默认 400): 最大训练轮数。
  - `hidden` (int, 默认 128): 第一隐藏层宽度；第二隐藏层为 `hidden // 2`（L384）。
  - `lr` (float, 默认 0.001): Adam 学习率。
  - `device` (str, 默认 "cpu"): 网络与张量所在设备（main 传 L485 解析出的 `device`）。
  - `ep_va` (None, 默认 None): 验证集局编号，`select="within"` 时需要。
  - `select` (str, 默认 "pooled"): 选择口径，同 `fit_ridge`（L407）。
- 返回: 三元组 `(测试集预测, 最优验证得分, 验证集预测)`（L422）；若一轮都没产生有效得分（`best` 保持 None）则 `best` 为 None，返回的第一/三项仍是从最终权重算出的预测。
- 实现: L381 播种；L382 取输入维度；L383-385 建 `Linear(d,hidden) → ReLU → Linear(hidden, hidden//2) → ReLU → Linear(hidden//2, 1)` 并 `to(device)`；L386 建 Adam（`weight_decay=1e-4`）；L387 取训练目标 min/max；L388-391 把三份数据转成 float32 tensor；L392 取样本数；L393 初始化 `best`/`best_state`/`bad`；L394-415 逐 epoch：L396 `randperm` 打乱后按 1024 一批前向、`mse_loss`、反向、`opt.step()`（L397-402）；eval 模式下在验证集上前向并裁剪（L403-406）；按 `select` 算得分（L407），`None` 则跳过（L408-409），得分更优则保存 `state_dict` 的**深拷贝**并清零 `bad`（L410-411），否则 `bad += 1` 且 `bad >= 40` 时 break（L412-415）——这是早停。L416-417 若存在最优权重则回载；L418-421 在 eval + no_grad 下输出测试与验证预测（同样 `np.clip(..., lo, hi)`）。
- 调用: 被 `main` L615 调用（按 `--repeats` 重复多次，每次 `seed=a.seed+r`）；它调用 `ev`/`ev_within`。
- 置信度: 已确认

### S:main  [L426-722]
- 类型: function
- 签名: `def main():`
- 作用: 脚本入口：解析命令行 → 加载配置与 ckpt 元数据 → 跑 rollout 或读 npz → 应用各种对照/修订开关 → 做局分组留出 → 拟合 Ridge/MLP 并打印指标 → 按预注册判据给出 `verdict` 并落盘 JSON。
- 参数: 无（命令行参数经 L427-472 的 `argparse` 解析为 `a`）。
- 返回: 无返回值（正常结束时隐式返回 None）；副作用是把结果 JSON 写到 `--out` 或默认路径（L717-722）。
- 实现: L427-472 定义全部 CLI 参数（`--ckpt` 必填；`--mode stoch/det` 默认 stoch；`--frames` 默认 30000；`--seed` 默认 7；`--test-frac` 0.25；`--val-frac` 0.15；`--models` 默认 `"ridge,mlp"`；`--repeats` 默认 1；`--sets` 默认 `"A,C,B"`；以及 `--seed-rng/--no-seed-rng` 默认开、`--npz`、`--select` 默认 pooled、`--center-target`、`--positive-control`、`--tag`、`--scramble-x`、`--scramble-y`、`--clock-baseline`）。L474 以 `TrainConfig.resolve("economy")` 为基底；L475-484 若 `run_dir/config.json` 存在，就把其中**与 cfg 同名属性**的键逐个 `setattr` 覆盖（失败的静默忽略）。L485 解析设备（非 cpu 且 CUDA 不可用则退回 cpu）；L486-487 打印表头。L489 决定数据来源：`--npz` 给定则 `load_npz`，否则 `rollout`。L491-498 若给了 `--save-npz` 且**不是**从 npz 读入，则建目录并用 `np.savez_compressed` 落盘（`y`/`v` 为 float32、`ep` 原样、`X_<集名>` 为 **float16**）。L501 先用真实回报算方差三分量 `tot_real/bet_real/wit_real`；L503-508 `--positive-control` 时用这些真实方差作参数替换 `y`；L509-515 `--center-target` 时把 `y` 逐局中心化；L516-521 `--scramble-y` 时按注释要求的顺序（先中心化再打散）替换 `y`。L524-531 `--clock-baseline` 时把 `X` 整个替换成单列「局内第几帧」并把 `sets` 强制为 `"A"`。L534-536 `--scramble-x` 时对每个集做行置换。L539-542 计算闸门 G1（`len(y) >= 30000 and n_ep >= 60`，其中 `n_ep = ep.max()+1`）与 G3（`y`/`v`/全部 `X` 均有限）。L544-556 用 `default_rng(a.seed)` **打乱局列表**后按 `test_frac`/`val_frac` 切出测试局与验证局，构造 `m_te`/`m_va`/`m_tr` 掩码并打印规模——这是按局分组留出（避免逐帧随机留出的时间泄漏）。L558-567 打印方差分解、critic 的四种 EV 口径与 `std(V)/std(R)`、`EV_win128_med`。L569-584 初始化结果字典 `res`（含 ckpt/mode/frames/episodes/seed/开关状态/真实方差/对照规格/两个闸门/方差分解/critic 指标/空的 `sets`）。L586-588 切出训练/验证/测试的 `y` 与局编号。L590-647 对 `--sets` 中每个集名：L594-596 丢掉**训练集内零方差**的列（docstring 说明是"保信息的列过滤"）；L597 用训练集统计量标准化训练/验证/测试；L599-609 若启用 ridge 则拟合并把 alpha、验证选择分、验证/测试的四种 EV 指标写进 `row["ridge"]`；L610-626 若启用 mlp 则按 `--repeats` 多次拟合，把 7 个指标的 **mean 与 all 列表**写进 `row["mlp"]`；L627-629 存入 `res` 并打印维度信息（含丢弃列数）；L630-647 打印 ridge/mlp 的指标行。L649-654 定义内部取数函数 `g(setname, model, key)`（对 dict 取 `"mean"`，异常返回 None）。L656-657 取 `eB/eC/eA`（均为 mlp 的 `EV_within`）与 critic 的 `EV_within`。L661-670 汇总三集上 ridge/mlp 的**验证集 `val_EV`**，`estimator_ok = max(vvals) > 0`（否则估计器发散、判据不可读）。L673-677 `--positive-control` 时取三集最好 `EV_within` 作 `ctrl_best`，`ctrl_pass = ctrl_best >= 0.15`。L678-679 合成 `gate_ok`（G1、G3、estimator_ok、`ctrl_pass is not False`、critic `EV_within <= 0.05` 全真）。L680-693 判决：估计器不合格或阳性对照失败 → `INVALID_ESTIMATOR`；否则先置 `GATE_FAIL`，再在 `gate_ok` 且 `eB/eC` 均非 None 时按预注册读：`eB>=0.15 且 ΔB−C>=0.08` → `P1_H_POMDP_SUPPORT`；`eB<=0.05` → `P2_H_STRUCT_FALSIFIED_POMDP`；`eC>=0.15 且 ΔB−C<0.08` → `P3_H_LEARN`；否则 `P0_GRAY`。L694-703 把判据与所有闸门写入 `res["criteria"]`。L704-715 打印判据段与 `VERDICT`。L717-722 组装输出路径（`--out` 或 `<repo>/docs/pomdp_ceiling_probe_<mode><tag>.json`）并以 UTF-8、`ensure_ascii=False`、缩进 2 写 JSON，最后打印 `[out]`。
- 调用: 由 L725-726 的 `if __name__ == "__main__": main()` 调用；它调用 `TrainConfig.resolve`、`load_npz`/`rollout`、`var_decomp`、`make_positive_control`、`scramble_target_by_clock`、`scramble_rows_by_clock`、`standardize`、`fit_ridge`、`fit_mlp`、`ev`、`ev_within`、`ev_within_raw`、`ev_win128_med`。
- 置信度: 已确认（`TrainConfig.resolve` / `config.json` 覆盖语义依 L474-484 源码；未逐字核对 `TrainConfig` 内部实现，不影响本条目）

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/pomdp_ceiling_probe.py | 18 | 18 |

COVERAGE_OK G012 files=1 symbols=18
