# 组 G017 文件级分析

## F:src/clasher_new/rl/ppo.py
- 行数: 547
- 模块作用: 实现一个轻量 PPO 更新器，供 follower 的自回归 bundle head 使用：对 rollout 记录的 transition 做批量重放（`policy.evaluate_batch`）重算 logprob/value/entropy，用 GAE 计算优势与回报，做 clip 代理目标 + 价值损失 + 熵正则的一步（或多轮小步）梯度更新；同时内置价值损失量纲缩放（`ReturnScaler`）、优势归一化、EV 诊断、梯度成分分解诊断与"替代优势"惰性检验。文件的顶层结构 = `ReturnScaler` 类（回报运行统计）+ `PPOTrainer` 类（全部训练逻辑）+ 模块 docstring 中的算法约定说明。
- 模块级 docstring 首行: `轻量 PPO 更新器（规划文档 8.5 跟随者 RL 微调）。`
- 主要导入: math, os, random, sys, numpy, torch, torch.nn.functional
- 关键模块级常量:
  - `_PARENT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` (L32) — 本文件所在目录 `rl/` 的父目录（即 `src/clasher_new`）；L33-34 在 `_PARENT` 不在 `sys.path` 时把它插到 `sys.path[0]`，使 `import rl.xxx` 可用。这是本模块唯一的模块级赋值常量。
- 顶层数据表/字典: 无（模块级只有两个类定义与上述路径常量）

### S:ReturnScaler  [L41-83]
- 类型: class
- 签名: `class ReturnScaler:`
- 作用: 维护回报（return）标量的在线运行均值/方差（Welford 算法），供价值损失做量纲对齐；只做统计、不改变任何返回值域。
- 参数: 无（类构造见 `__init__`）
- 返回: 不适用
- 实现: 类 docstring（L42-46）声明三点：只做标量统计、`scale()` 在样本不足或方差退化时返回 0、调用方据此回退"不缩放"（= 旧行为）。实例状态只有 4 个标量字段（`eps`/`count`/`mean`/`m2`，L48-52 初始化）。方法集合 = `update`/`var`/`std`/`scale`/`to_dict`/`from_dict`。
- 调用: 由 `PPOTrainer.__init__`（L123）默认新建，或在断点续训时由外部传入已恢复实例；`PPOTrainer.update` L242-243 调用 `update()`+`scale()`。仓库内直接使用它的位置（grep 确认）：`src/clasher_new/rl/train_solo.py:37` import、`train_solo.py:1152-1156` 用 `from_dict` 恢复并打印、`train_solo.py:1452` 用 `to_dict` 落盘。
- 置信度: 已确认

#### M:ReturnScaler.__init__  [L48-52]
- 类型: method
- 签名: `def __init__(self, eps=1e-6):`
- 作用: 初始化 Welford 统计状态与退化阈值。
- 参数:
  - `eps` (默认 `1e-06`): 标准差退化阈值；`scale()` 只有在 `std() > eps` 时才返回非零缩放因子。
- 返回: 无
- 实现: `self.eps = float(eps)`（L49）；`self.count = 0`（L50，样本计数）；`self.mean = 0.0`（L51）；`self.m2 = 0.0`（L52，平方差累积量，方差 = m2/(count-1)）。
- 置信度: 已确认

#### M:ReturnScaler.update  [L54-60]
- 类型: method
- 签名: `def update(self, values):`
- 作用: 把一批回报逐个喂入 Welford 在线更新（list/np.ndarray 均可）。
- 参数:
  - `values`: 一批回报值；内部 `np.asarray(values, dtype=np.float64).ravel()` 展平后逐元素迭代，因此多维输入也被拉直成一维。
- 返回: 无（原地更新 `count/mean/m2`）
- 实现: L56 把输入转成 float64 数组并 ravel 后逐元素循环；每个元素执行标准 Welford 三步：`count += 1`（L57）、`d = float(v) - self.mean`、`self.mean += d / self.count`（L58-59）、`self.m2 += d * (float(v) - self.mean)`（L60，其中第二次的 `self.mean` 已是更新后的均值）。docstring 首行（L55）：`批量喂入一批回报（list/np.ndarray）。`
- 调用: 被 `PPOTrainer.update` 在 `value_norm == "running"` 时调用一次（L242，整份 rollout 调用一次，不按 minibatch）。
- 置信度: 已确认

#### M:ReturnScaler.var  [L62-63]
- 类型: method
- 签名: `def var(self):`
- 作用: 返回当前样本方差（无偏，除以 count-1）。
- 参数: 无
- 返回: `self.m2 / (self.count - 1)`（当 `count > 1`），否则 `0.0`。
- 实现: L63 单行三元表达式：`return self.m2 / (self.count - 1) if self.count > 1 else 0.0`。count ≤ 1 时返回 0.0（避免除零）。
- 置信度: 已确认

#### M:ReturnScaler.std  [L65-66]
- 类型: method
- 签名: `def std(self):`
- 作用: 返回标准差（对方差做非负截断后开方）。
- 参数: 无
- 返回: `float`，`math.sqrt(max(0.0, self.var()))`。
- 实现: L66 一行：先 `max(0.0, self.var())` 防浮点负值，再开方并转 float。
- 调用: 被 `scale()`（L70）与外部日志（`train_solo.py:1156` 打印 `ppo.ret_scaler.std()`）使用。
- 置信度: 已确认

#### M:ReturnScaler.scale  [L68-71]
- 类型: method
- 签名: `def scale(self):`
- 作用: 返回值损失的除数 s；统计未建立或方差过小时返回 0.0 表示"不缩放"。
- 参数: 无
- 返回: `float`；`s = self.std()`，`s > self.eps` 时返回 `s`，否则返回 `0.0`。
- 实现: docstring（L69）明确"价值损失的除数 s（v_loss /= s²）。统计未建立/方差过小 → 0.0 表示不缩放"。L70-71 两行实现。注意返回 0.0 是一个哨兵值，调用方（`PPOTrainer.update` L243-244）据此把 `v_scale` 设为 1.0。
- 置信度: 已确认

#### M:ReturnScaler.to_dict  [L73-74]
- 类型: method
- 签名: `def to_dict(self):`
- 作用: 导出统计状态用于 checkpoint 落盘。
- 参数: 无
- 返回: `dict`，仅三个键：`{"count": self.count, "mean": self.mean, "m2": self.m2}`。注意 `eps` 不落盘。
- 实现: L74 单行 return 构造三键字典。
- 调用: `train_solo.py:1452` 把 `ppo.ret_scaler.to_dict()` 写进 run_state。
- 置信度: 已确认

#### M:ReturnScaler.from_dict  [L77-83]
- 类型: classmethod
- 签名: `@classmethod` / `def from_dict(cls, d):`
- 作用: 从字典恢复统计状态（断点续训时不重置回报尺度统计）。
- 参数:
  - `d`: 字典或 None；`d = d or {}`（L79），因此 None/空字典都退化为"全零新实例"。
- 返回: `ReturnScaler` 实例。
- 实现: L78 `s = cls()` 新建（eps 用类默认 1e-6，不从字典恢复）；L79 空值兜底；L80-82 分别以 `int(d.get("count", 0))`、`float(d.get("mean", 0.0))`、`float(d.get("m2", 0.0))` 赋回三个字段；L83 返回 s。
- 调用: `train_solo.py:1154` 恢复 `ppo.ret_scaler`；测试 `src/clasher_new/rl/selftest.py:3847`（往返一致）与 `:3850`（`from_dict(None).count == 0`）。
- 置信度: 已确认

### S:PPOTrainer  [L86-547]
- 类型: class
- 签名: `class PPOTrainer:`
- 作用: PPO 训练器：持有 policy 与 Adam 优化器，提供 GAE 计算、EV 计算、以及 `update()` 主入口（优势归一化 → 价值尺度 → 单 pass 旧分支 或 多轮×小批新分支 → 反传/裁剪/`opt.step`）。
- 参数: 无（类构造见 `__init__`）
- 返回: 不适用
- 实现: 类无类级 docstring（L86 直接 `class PPOTrainer:`，紧随 `__init__` 的 docstring）。全部逻辑集中在 9 个方法中；实例可变状态包括 `opt`/`updates`/`grad_steps`/`ret_scaler`/`last_ev_pairs`/`last_ev_pre`/`_adv_inert_stats`/`last_adv_inert_grad`/`rng`。方法分两组：静态工具（`explained_variance`、`compute_gae`）+ 实例方法（`update`、`_loss_pass`、`_apply_grad`、`_plan_batches`、`_update_epochs`）。
- 调用: 被 `rl/run_league.py:585`、`rl/flow_league.py:139`/`:189`、`rl/train_follower.py:149`、`rl/train_solo.py:1138` 构造（grep 确认），并被 `scripts/` 下多个只读诊断脚本 import（`diag_critic_ev.py:51`、`diag_gru_ablation.py:35`、`pomdp_ceiling_probe.py:44`、`probe_reward_composition.py:44`、`probe_value_ln.py:54`、`_probe_value_collapse.py:38`、`_probe_value_path.py:38`）以及 `rl/selftest.py` 多处测试使用。
- 置信度: 已确认

#### M:PPOTrainer.__init__  [L87-142]
- 类型: method
- 签名: `def __init__(self, policy, lr=3e-4, gamma=0.99, gae_lambda=0.95, clip=0.2,` / `             vf_coef=0.5, ent_coef=0.01, max_grad_norm=0.5, adv_norm="batch",` / `             value_norm="none", diagnose_every=0, ret_scaler=None,` / `             n_epochs=1, minibatch_size=0, shuffle=False, seed=12345):`（源码分 4 行书写，见 L87-90）
- 作用: 保存全部 PPO 超参、建立 Adam 优化器与统计/诊断状态；默认值即"旧行为"，新能力必须显式开启。
- 参数（**全部默认超参逐一列出**）:
  - `policy`: 策略网络对象；要求具备 `parameters()`、`device`、`train()`、`evaluate_batch(...)`（本文件 L113/L368/L370/L212/L327 使用）。
  - `lr=3e-4`: Adam 学习率（L113 `torch.optim.Adam(policy.parameters(), lr=lr)`）。
  - `gamma=0.99`: 折扣因子（存 `self.gamma`，L114；被外部 GAE 调用复用）。
  - `gae_lambda=0.95`: GAE 的 λ（`self.gae_lambda`，L115）。
  - `clip=0.2`: PPO ratio 裁剪半径（`self.clip`，L116；`_loss_pass` L382 使用）。
  - `vf_coef=0.5`: 价值损失系数（`self.vf_coef`，L117；`_loss_pass` L393 与 `_apply_grad` L426 使用）。
  - `ent_coef=0.01`: 熵正则系数默认值（`self.ent_coef`，L118；`update` 中若 `ent_coef` 实参为 None 则用它，L234）。
  - `max_grad_norm=0.5`: 梯度全局裁剪阈值（`self.max_grad_norm`，L119；`_apply_grad` L462-463 传给 `clip_grad_norm_`）。
  - `adv_norm='batch'`: 优势归一化模式，注释 L120 写明 `batch=整批中心化(旧) / scale=只除std / none=原始`。
  - `value_norm='none'`: 价值损失缩放模式，默认 `none` = 旧行为（L121）；docstring L91 写明 `none=旧行为（价值损失不缩放）/ running=按回报运行 std 缩放`。
  - `diagnose_every=0`: >0 时每 N 次 update 额外做一次 p_gnorm/v_gnorm 梯度分解（多两次 backward），0 = 关闭、旧行为（docstring L92-93；L122 `self.diagnose_every = int(diagnose_every or 0)`）。
  - `ret_scaler=None`: 断点续训时传入已恢复的 `ReturnScaler`；None = 新建（docstring L94；L123）。
  - `n_epochs=1`, `minibatch_size=0`, `shuffle=False`: F′ 更新预算三件套，默认 = 原单一 pass 分支（docstring L96-110；L126-128）。注意 `n_epochs` 被 `max(1, int(n_epochs or 1))` 夹到 ≥1（L126），`minibatch_size` 被 `max(0, int(minibatch_size or 0))` 夹到 ≥0（L127），`shuffle` 被 `bool()` 强制转换（L128）。
  - `seed=12345`: 小批打乱用的 `random.Random` 种子（L129 `self.rng = random.Random(int(seed))`）。
- 返回: 无
- 实现: docstring（L91-111）分三段：`value_norm`/`diagnose_every`/`ret_scaler` 语义（L91-94）；F′ 更新预算的动机（L96-104，旧实现 = 1 forward/1 backward/1 step，而喂入的是同一局连续 128 帧、相邻帧 corr≈0.99，导致 20k 步只有 156 次梯度步）；兼容性红线（L105-110：`n_epochs=1, minibatch_size=0, shuffle=False` 时走原单一 pass 分支、loss 仍为 `sum` 口径、逐位等于旧实现；只有显式开启才切到"多轮×打乱×小批 + `mean` 口径"；并声明开启后 `ratio` 会离开 1.000、`clip_frac` 会 >0 是正常的）。赋值顺序：policy/opt（L112-113）→ 六个标量超参（L114-119）→ `adv_norm`/`value_norm`/`diagnose_every`/`ret_scaler`（L120-123）→ `self.updates = 0`（L124，累计 update 次数）→ F′ 三参数 + `rng`（L126-129）→ `self.grad_steps = 0`（L132，累计 `opt.step()` 次数，注释 L130-131 说明旧实现恒等于 `updates`、是判读"梯度步够不够"的硬指标）→ `self.last_ev_pairs = None`（L137，最近一次 update 的逐帧 `(value_pred, return)`，供训练环按评估窗口池化算 EV）→ `self.last_ev_pre = None`（L139，**更新前**整批预测算出的 EV，对外报出口径）→ `self._adv_inert_stats = {}`（L141）与 `self.last_adv_inert_grad = None`（L142，惰性检验用）。
- 置信度: 已确认

#### M:PPOTrainer.explained_variance  [L145-165]
- 类型: staticmethod
- 签名: `@staticmethod` / `def explained_variance(values, returns):`
- 作用: 计算 critic 对回报的解释方差 `EV = 1 − MSE(v, R) / Var(R)`；无量纲，是判读 critic 是否在学的口径。
- 参数:
  - `values`: 预测值序列（任意形状，内部 ravel 成 float64 一维）。
  - `returns`: 回报序列（同样 ravel 成 float64 一维）；必须与 `values` 等长。
- 返回: `float`；`1`=完美预测、`0`=等价于恒定预测批均值、`<0`=比常数预测还差。两个退化分支都返回 `0.0`：① `r.size < 2 or v.size != r.size`（L159-160）；② `var <= 1e-12`（L162-163）。docstring（L147-155）给出上述解释，并说明弃用 `value_loss` 判读的原因（`value_loss` 被 `value_norm` 除以 `s²`、量纲漂移、跨版本不可比；实测 `value_loss=1.59` 看似达标但 EV≈0）。
- 实现: L157-158 转 float64 一维数组；L159 长度/规模守卫；L161 `var = float(r.var())`（总体方差，ddof=0）；L162-163 方差退化守卫；L164 `mse = float(((v - r) ** 2).mean())`；L165 `return float(1.0 - mse / var)`。
- 调用: 内部被 `update`（L335-336 更新前口径、L402 旧分支）、`_update_epochs`（L534）调用；外部被 `train_solo.py:1294`（全体帧池化）与 `:1298`（分块）调用，并被 `selftest.py:4635` 断言与 `last_ev_pre` 一致。
- 置信度: 已确认

#### M:PPOTrainer.compute_gae  [L168-192]
- 类型: staticmethod
- 签名: `@staticmethod` / `def compute_gae(rewards, values, dones, gamma=0.99, lam=0.95, truncated=None, last_value=0.0):`
- 作用: 反向递推计算 GAE 优势与回报（returns = advantages + values）。
- 参数:
  - `rewards`: 每步奖励序列（`len()` 决定 T）。
  - `values`: 每步 value（与 rewards 等长）。
  - `dones`: 每步是否真实终止（bool 序列）。
  - `gamma=0.99`: 折扣因子。
  - `lam=0.95`: GAE λ。
  - `truncated=None`: 每步是否因 `max_ep_steps` 被截断；None 时内部补 `[False]*T`（L176-177）。
  - `last_value=0.0`: 截断步用于 bootstrap 的末值。
- 返回: `(adv, returns)` 两个 `np.float32` 一维数组；`returns = adv + np.asarray(values, dtype=np.float32)`（L191）。
- 实现: docstring L170-174 声明两条规则：`dones[t]=True`（真实终止）→ `next_val=0`；`truncated[t]=True` 且非终止 → `next_val=last_value`（bootstrap，属 P1-7 修复）。L175 取 T；L176-177 默认 truncated；L178-179 初始化 `adv`（float32 零数组）与 `gae = 0.0`；L180 从 `T-1` 反向循环：最后一步（L181-185）在"truncated 且非 done"时 `next_val=last_value`，否则 0.0；非最后一步（L186-187）`next_val = 0.0 if dones[t] else values[t+1]`；L188 `delta = rewards[t] + gamma * next_val - values[t]`；L189 `gae = delta + gamma * lam * (0.0 if (t == T - 1 or dones[t]) else gae)`（末步或终止步切断递推）；L190 写入 `adv[t]`；L191 由优势加 value 得到 returns。
- 调用（grep 确认）: `rl/flow_league.py:232`、`rl/run_league.py:767`/`:898`/`:1091`、`rl/train_follower.py:211`、`rl/train_solo.py:1228`（真实价值）与 `:1234`（用常数 `c = float(ppo.ret_scaler.mean)` 作 values 的 V≡常数对照）、`scripts/diag_critic_ev.py:200`、`scripts/diag_gru_ablation.py:147`、`scripts/pomdp_ceiling_probe.py:163`、`scripts/probe_value_ln.py:288`、`scripts/_probe_value_collapse.py:152`、`scripts/_probe_value_path.py:168`、`selftest.py:4876`/`:4879`。
- 置信度: 已确认

#### M:PPOTrainer.update  [L194-356]
- 类型: method
- 签名: `def update(self, transitions, ent_coef=None, adv_norm=None, adv_alt=None):`
- 作用: 一次 PPO 更新主入口：对一批 transition 做优势归一化、价值尺度计算、旧分支或多轮分支的梯度更新，返回统计字典。
- 参数:
  - `transitions`: dict 列表，每个 dict 需含 `{obs, belief, plan, bundle, old_logprob, adv, returns, masks, init_hidden(可选)}`（docstring L195-196）。
  - `ent_coef=None`: 单次更新覆盖熵系数；None = 用构造参数 `self.ent_coef`（L234）。
  - `adv_norm=None`: 单次更新覆盖优势归一化模式；None = 用 `self.adv_norm`（L222）。
  - `adv_alt=None`: 与 transitions 等长的"替代优势"（同口径、由调用方用 V≡常数重算的 GAE）；docstring L206-210 声明：**默认 None = 逐位旧行为**，给定后**不参与任何被写入梯度的量**，只在诊断命中的更新上多算 1 次前向 + 1 次反传，返回 `adv_inert_*` 统计与 `last_adv_inert_grad`。
- 返回: `dict` stats。空输入（L213-218）返回固定字典：`policy_loss/value_loss/value_loss_raw/entropy = 0.0`、`ratio_mean=1.0`、`clip_frac=0.0`、`adv_mean/adv_std/grad_norm=0.0`、`value_scale=1.0`、`explained_variance=0.0`、`n=0`、`ppo_epochs=self.n_epochs`、`grad_steps=0`（注意此时 `self.updates` 不递增）。正常路径末尾（L343-346）追加 `adv_mean`/`adv_std`（**用未归一化的 `adv_raw` 算**）、`value_scale`、`n`；若 `_adv_inert_stats` 非空则合并它（L348-350）；仅当梯度分解真跑过（`p_gnorm is not None`）才追加 `p_gnorm`/`v_gnorm`（L353-355）。
- 实现（按源码顺序）:
  1. L212 `self.policy.train()`；L213-218 空 batch 提前返回（不递增 updates）。
  2. L219 `self.updates += 1`；L220 `adv_raw = np.array([t["adv"] ...], dtype=np.float32)`；L221 `advs = adv_raw.copy()`。
  3. **优势归一化**（L222-233）：`mode` 取覆盖值或构造值；L223 `std = float(advs.std())`（**先算 raw std，再按模式处理**）。`"scale"`：`std > 1e-6` 时 `advs = advs / (std + 1e-8)`（只除 std、不中心化，注释 L202-203 说明是为避免"躺平零优势帧"被批均值抬成伪正优势）；`"none"`：不改；`"batch"`：`std > 1e-6` 时 `advs = (advs - advs.mean()) / (std + 1e-8)`（整批中心化 + 除 std，旧默认）；其他字符串 `raise ValueError(f"未知 adv_norm='{mode}'（batch/scale/none）")`。
  4. L234 `coef = self.ent_coef if ent_coef is None else ent_coef`；L235 `n = len(transitions)`；L236 `rets_np = np.array([t["returns"] ...], dtype=np.float32)`。
  5. **价值通道尺度**（L237-248，P0-1b）：`self.value_norm == "running"` 时先 `self.ret_scaler.update(rets_np)`（本批即参与统计），再 `s = self.ret_scaler.scale()`，`v_scale = s if s > 0 else 1.0`（L241-244）；`value_norm in ("none", "", None)` 时 `v_scale = 1.0`（L245-246）；否则 `raise ValueError(f"未知 value_norm='{self.value_norm}'（none/running）")`（L247-248）。注释 L239-240 强调统计**永远按整份 rollout 更新一次**、不按 minibatch，否则多轮小批会让 s 抖动、跨 epoch 目标尺度不一致。
  6. **分支判定**（L250-253）：`legacy = (self.n_epochs <= 1 and not self.shuffle and (self.minibatch_size <= 0 or self.minibatch_size >= n))`；`diag_on = bool(self.diagnose_every) and (self.updates % self.diagnose_every == 0)`。
  7. **替代优势的同一尺度化口径**（L254-295，adv_alt 给定才走）：`alt_raw = np.asarray(adv_alt, dtype=np.float32)`，形状不符则 `raise ValueError(f"adv_alt 形状 {alt_raw.shape} 与批 {adv_raw.shape} 不一致")`（L257-259）；`advs_alt = alt_raw.copy()` 后按与 `advs` **相同的 mode 分支**做 scale/batch 归一化（none 原样）（L261-268）；计算 `_cc = np.corrcoef(adv_raw, alt_raw)[0,1]`（任一 std ≤1e-12 时取 1.0）（L269-273）；注释 L274-277 说明关键定义：真正进损失的是**归一化后**的优势（`adv_norm="scale"` ⇒ A/σ），优势整体缩放对梯度是恒等变换，所以 critic 的贡献必须量在归一化向量上；`resid_frac(raw)` 只作描述（同时含形状差与尺度差）。L278-285 用归一化后的 `advs`/`advs_alt` 算 `_resid_norm = std(advs−advs_alt)/std(advs_alt)` 与 `_level_shift = |mean(advs−advs_alt)|/std(advs_alt)`（`_san ≤ 1e-12` 时二者置 None）。L286-295 写入 `self._adv_inert_stats` 共 8 个键：`adv_inert_corr`、`adv_inert_resid_frac`（raw 口径）、`adv_inert_resid_frac_norm`、`adv_inert_level_shift_norm`、`adv_inert_std_ratio`（`_sr/_sa`）、`adv_inert_norm_std_ratio`（归一化 std 比）、`adv_inert_mean_real`、`adv_inert_mean_alt`；否则 L296-297 置空 dict。L298 `self.last_adv_inert_grad = None`（**重置职责在此**，见 `_apply_grad` 的注释 L417-420）。
  8. **legacy 分支**（L299-316）：`pack = self._loss_pass(transitions, np.arange(n), advs, rets_np, coef, v_scale, reduction="sum")`（整批、sum 口径）；若 `diag_on and advs_alt is not None` 再对替代优势做一次同参数 `_loss_pass`（`pack_alt`，L303-305）；`p_gnorm, v_gnorm = self._apply_grad(pack, diag_on, pack_alt=pack_alt)`（L306）；`self.grad_steps += 1`（L307）；`out = pack["stats"]`（L308）后补 `ratio_mean`/`clip_frac`/`grad_norm`/`explained_variance`（= `pack["ev"]`，即该次 forward 后、backward 前的口径）/`grad_steps=1`/`ppo_epochs=1`/`ppo_minibatch=n`（L309-315）；`self.last_ev_pairs = pack["ev_pairs"]`（L316）。
  9. **新分支**（L317-342）：注释 L318-325 记录 EV 口径陷阱——必须用**更新前**预测算 EV，若沿用末轮预测读到的是刚在这批帧上训过多步的 in-sample 值（实测 fprime_20k 末点训练 EV=+0.41，而同权重在 50 局独立 rollout 上 EV_global=−0.0255；受控实验同批 EV：更新前 +0.011 / 更新后 +0.042 / 更新后换独立 rollout −0.384）；代价是每次 update 多一次整批 no_grad 前向（≈1/16 更新开销）。实现：L326-333 `with torch.no_grad(): self.policy.evaluate_batch([obs...], [belief...], [plan...], [bundle...], [masks...], [t.get("init_hidden")...])` 取 `(v_pre, _, _)`；L334 `self.last_ev_pairs = (v_pre.squeeze(-1).cpu().numpy(), rets_np)`；L335-336 `self.last_ev_pre = self.explained_variance(...)`。L337-339 `p_gnorm, v_gnorm, out = self._update_epochs(transitions, advs, rets_np, coef, v_scale, diag_on, advs_alt=advs_alt)`。L341-342 把末轮 in-sample EV 移到 `out["explained_variance_insample"]`，`out["explained_variance"] = self.last_ev_pre`（对外只报更新前口径、与全部历史 run 可比）。
  10. 收尾 L343-355 同"返回"段所述。
- 调用: 由 `rl/train_solo.py:1653`（`ppo.update(batch, adv_alt=_adv_alt)`）、`rl/run_league.py`/`flow_league.py`/`train_follower.py`、以及 `selftest.py` 多处测试调用（grep 确认 PPOTrainer 构造点见类条目）。
- 置信度: 已确认

#### M:PPOTrainer._loss_pass  [L361-407]
- 类型: method
- 签名: `def _loss_pass(self, transitions, idx, advs, rets_np, coef, v_scale, reduction='mean'):`
- 作用: 在 `idx` 指定的样本子集上做一次前向并算出 PPO loss 与各项诊断量（旧单 pass 分支与新多轮分支共用）。
- 参数:
  - `transitions`: 全体 transition 列表（本方法只取 `idx` 指定的子集）。
  - `idx`: 样本下标（np 数组或序列）。
  - `advs`: 已归一化（或未归一化）的优势数组，按 `advs[idx]` 取子集。
  - `rets_np`: 全量回报数组，按 `rets_np[idx]` 取子集。
  - `coef`: 熵系数（`update` 已把 `ent_coef` 覆盖逻辑解析完）。
  - `v_scale`: 价值损失除数（`value_norm="running"` 时为回报 std，否则 1.0）。
  - `reduction='mean'`: `"sum"` = 旧实现量纲（整批求和，梯度与逐条累加等价）；`"mean"` = 标准 PPO 小批口径（docstring L365-366）。
- 返回: `dict`，键为 `loss`（标量 tensor，待 backward）、`p_loss`、`v_loss`、`v_mse`、`ratio_mean`、`clip_frac`、`ev`、`ev_pairs`（`(v_np, rets_np[idx])`）、`stats`（`policy_loss`/`value_loss`/`value_loss_raw`/`entropy` 四个 float，**均已除以 `m = max(1, len(idx))`**）。
- 实现: L368 `dev = self.policy.device`；L369 `sub = [transitions[i] for i in idx]`；L370-376 `self.policy.evaluate_batch(...)` 传入 sub 的 obs/belief/plan/bundle/masks 与 `t.get("init_hidden")`，返回 `(lp_new, value, ent)`；L377-378 由 `t["old_logprob"]` 建 float32 tensor（放 dev）；L379 `ratio = torch.exp(lp_new - old)`；L380 `adv_t = torch.tensor(advs[idx], ...)`；L381 `surr1 = ratio * adv_t`；L382 `surr2 = torch.clamp(ratio, 1 - self.clip, 1 + self.clip) * adv_t`（PPO clip 目标）；L383 `rets` tensor。损失三件套（L384-393）：`sum` 口径为 `p_loss = -torch.min(surr1, surr2).sum()`、`v_mse = F.mse_loss(value.squeeze(-1), rets, reduction="sum")`、`ent_term = ent.sum()`；`mean` 口径为三者各自 `.mean()`。L392 **`v_loss = v_mse / (v_scale * v_scale)`**（价值损失量纲缩放，v_scale=1 时等于原值）；L393 **`loss = p_loss + self.vf_coef * v_loss - coef * ent_term`**（熵项取负号 = 最大化熵）。L394 `m = max(1, len(idx))`；L395-399 在 `torch.no_grad()` 下算 `ratio_mean = ratio.mean()`、`clip_frac = ((ratio < 1-clip) | (ratio > 1+clip)).float().mean()`、`v_np = value.squeeze(-1).detach().cpu().numpy()`；L400-407 组装返回 dict，其中 `ev = self.explained_variance(v_np, rets_np[idx])`（**这次前向的同一批预测**），`stats` 四项各除以 m。
- 调用: 被 `update`（L300-305，`reduction="sum"`）与 `_update_epochs`（L505-506 与 L512-513，`reduction="mean"`）调用。
- 置信度: 已确认

#### M:PPOTrainer._apply_grad  [L409-465]
- 类型: method
- 签名: `def _apply_grad(self, pack, diag_on=False, pack_alt=None):`
- 作用: 对一次 loss pack 做 backward（可选先做梯度成分分解与替代优势梯度余弦诊断）→ 全局梯度裁剪 → `opt.step()`，即"一次梯度步"。
- 参数:
  - `pack`: `_loss_pass` 的返回 dict。
  - `diag_on=False`: 是否做梯度成分分解（`p_loss` 与 `vf_coef*v_loss` 分别求梯度范数；会多两次反传）。
  - `pack_alt=None`: 替代优势在同一 minibatch 上的 loss pack；给定且 `diag_on` 时额外算策略损失对它的梯度，写入 `self.last_adv_inert_grad`（`grad_cos` / `grad_norm_ratio`），**不改变被写入梯度的任何量**（docstring L412-414）。
- 返回: `(p_gnorm, v_gnorm)`；未做诊断或诊断抛 RuntimeError 时为 `(None, None)`。
- 实现: L416 先置 `p_gnorm = v_gnorm = None`。注释 L417-420 明确**不在此重置** `last_adv_inert_grad`（诊断只命中 (ep==0, bi==0) 那一次，而同一 update 后续还有更多 `_apply_grad` 调用；早期版本每次清一次会把刚算出的 grad_cos 擦掉，端到端 smoke 抓到过 `n_grad_cos=0`），重置职责归 `update()` 开头（L298）。诊断分支（L424-458）：L425 取 `requires_grad` 的参数列表；L426 `gv_loss = self.vf_coef * pack["v_loss"]`；L428-431 用 `torch.autograd.grad(..., retain_graph=True, allow_unused=True)` 分别对 `pack["p_loss"]` 与 `gv_loss` 求梯度；L433-438 内嵌 `_norm(gs)` 把各张量梯度平方和累加后开方；L439 `p_gnorm, v_gnorm = _norm(gp), _norm(gv)`；若 `pack_alt is not None`（L440-453）再对 `pack_alt["p_loss"]` 求梯度 `ga`，逐参数累加内积 `num` 与 `||ga||²`，得到 `na`，当 `p_gnorm > 0 and na > 0` 时写 `self.last_adv_inert_grad = {"grad_cos": num/(p_gnorm*na), "grad_norm_ratio": p_gnorm/na}`；L454-455 `except RuntimeError` 时把 `p_gnorm = v_gnorm = None`（诊断失败不影响训练）；`finally`（L456-458）里 `self.opt.zero_grad()` + `pack["loss"].backward()`。非诊断分支（L459-461）：`zero_grad()` + `backward()`。L462-463 `pack["grad_norm"] = float(torch.nn.utils.clip_grad_norm_(self.policy.parameters(), self.max_grad_norm))`（该函数返回裁剪**前**的总范数）；L464 `self.opt.step()`；L465 返回 `(p_gnorm, v_gnorm)`。
- 调用: 被 `update`（L306，legacy 分支）与 `_update_epochs`（L514）调用。
- 置信度: 已确认

#### M:PPOTrainer._plan_batches  [L467-475]
- 类型: method
- 签名: `def _plan_batches(self, n):`
- 作用: 规划一轮内的样本顺序切分（可选打乱；小批不足则退化为整批）。
- 参数:
  - `n`: 本轮样本总数。
- 返回: `np.int64` 数组的列表；不分批时是单元素列表 `[np.array(order)]`，分批时是每个小批一个数组。
- 实现: L469 `order = list(range(n))`；L470-471 `if self.shuffle: self.rng.shuffle(order)`（用 `__init__` 的 `random.Random(seed)`，因此同 seed 可复现切分顺序）；L472-473 若 `minibatch_size <= 0` 或 `>= n` 直接返回整批 `[np.array(order, dtype=np.int64)]`；L474-475 否则按 `range(0, n, minibatch_size)` 切片成顺序小批（**注意：最后一批可能不足 minibatch_size，未做丢弃或补齐**）。
- 调用: 被 `_update_epochs` 每轮调用一次（L499）。
- 置信度: 已确认

#### M:PPOTrainer._update_epochs  [L477-547]
- 类型: method
- 签名: `def _update_epochs(self, transitions, advs, rets_np, coef, v_scale, diag_on, advs_alt=None):`
- 作用: F′ 主循环——`n_epochs` 轮 × 小批 ×（可选）打乱，每小批一次 `opt.step()`，并聚合统计。
- 参数:
  - `transitions`/`advs`/`rets_np`/`coef`/`v_scale`/`diag_on`: 透传给 `_loss_pass`/`_apply_grad`（含义同上）。
  - `advs_alt=None`: 替代优势数组；仅在第一轮第一个小批上与 `diag_on` 同时生效。
- 返回: `(p_gnorm, v_gnorm, out)`；`out` 是聚合统计字典。
- 实现: docstring L481-486 写明统计口径：`policy_loss`/`value_loss`/`entropy` 按小批样本数加权平均（跨全部轮次）；`ratio_mean`/`clip_frac`/`grad_norm` **只取最后一轮**（标准 PPO 报告口径）；`explained_variance` 与 `last_ev_pairs` 取**最后一轮**的逐帧 `(v,R)` 池化。实现：L488 `n = len(transitions)`；L489-497 初始化累加器——`agg` 四键（policy_loss/value_loss/value_loss_raw/entropy）、`w_tot=0`、`p_gnorm = v_gnorm = None`、`n_batches=1`、`ratio_last=1.0`、`clip_last=0.0`、`gnorm_sum/gnorm_cnt`、`ev_last=0.0`。外层 `for ep in range(self.n_epochs)`（L498）：L499 `batches = self._plan_batches(n)`（**每轮重新切分/打乱**，L500 记 `n_batches`）；L501-503 本轮局部 `ev_v/ev_r` 列表、`r_acc/c_acc`、`w_ep=0`（注释 L503：ratio/clip 必须除本轮样本数，不能除全部轮次）。内层 `for bi, idx in enumerate(batches)`（L504）：L505-506 `pack = self._loss_pass(..., reduction="mean")`；L508 `_diag_here = diag_on and ep == 0 and bi == 0`（梯度分解只在第一轮第一个小批做，否则每步 3 次反传）；L509-513 命中且 `advs_alt is not None` 时在**同一 minibatch**、**同一尺度化口径**下再算 `pack_alt`；L514 `pg, vg = self._apply_grad(pack, _diag_here, pack_alt=pack_alt)`；L515-516 非 None 时更新 `p_gnorm/v_gnorm`；L517 `self.grad_steps += 1`（**每小批一次**，因此 `grad_steps = n_epochs × n_batches`）；L518-524 把 `pack["stats"][k] * m` 累进 `agg`、`w_tot += m`、`r_acc/c_acc` 累加、`w_ep += m`；仅在最后一轮（L525-529）累加 `grad_norm` 与收集 `ev_pairs` 的 `(pred, ret)`。轮末（L530-536）：仅最后一轮做 `np.concatenate` 成 `ev_v/ev_r`、写 `self.last_ev_pairs`、`ev_last = self.explained_variance(ev_v, ev_r)`、`ratio_last = r_acc / max(1, w_ep)`、`clip_last = c_acc / max(1, w_ep)`。轮后（L537-546）：`w = max(1, w_tot)`；`out = {k: v / w for k, v in agg.items()}`；追加 `ratio_mean=ratio_last`、`clip_frac=clip_last`、`grad_norm = gnorm_sum / max(1, gnorm_cnt)`、`explained_variance=ev_last`、`ppo_epochs=self.n_epochs`、`ppo_minibatch = self.minibatch_size if > 0 else n`、`grad_steps = self.n_epochs * n_batches`；L547 返回三元组。
- 调用: 仅被 `update` 的新分支调用（L337）。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| src/clasher_new/rl/ppo.py | 17 | 17 |

COVERAGE_OK G017 files=1 symbols=17
