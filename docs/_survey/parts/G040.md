# 组 G040 文件级分析

> 阅读范围：本组 5 个 `.py` 文件全文（共 581 行，与简报一致）。调用关系核对依据：对全仓 `*.py` 的符号级检索（`from card_utils import ...` / `from player import ...` / `PlayerState(` / `client_side`），以及 `ls` 确认 `client_side/` 内的数据文件存在。除本组文件外未阅读任何模块正文。
>
> 前置事实（贯穿本组）：仓库内存在**两套同名模块** —— 引擎版 `src/clasher_new/card_utils.py` 与 `src/clasher_new/player.py`，以及本组的 `src/clasher_new/client_side/card_utils.py` 与 `src/clasher_new/client_side/player.py`。本组两文件只被**同目录**的 `client.py`/`download_images.py`/`assure_names.py` 以同级模块名导入（依据：`client.py:7-8`、`download_images.py:1`、`assure_names.py:1`、`client_side/player.py:1`）；全仓检索中出现的 `from card_utils import _value_at_level` / `character_to_card` / `spells` 等符号在本组 `client_side/card_utils.py` 全文中**不存在**（已逐行读完确认），故那些导入不可能解析到本组副本。⇒ 本组 `client_side/*` 是**客户端侧独立副本**，不被仓库其他代码复用。

---

## F:src/clasher_new/client_side/client.py
- 行数: 317
- 模块作用: pygame 图形客户端：卡组选择/IP 输入两屏（默认 `DEBUG=True` 下被短路），随后以 TCP 长连接连到服务器，起后台线程接收 JSON 状态，并把竞技场、实体、UI、手牌、圣水条画到窗口；同时把手牌拖拽手势转成 `deploy` 消息发回服务端。文件本身既是函数库又是可执行脚本：**没有 `if __name__ == '__main__'` 保护**，导入即会连 socket。依据：文件顶部 import 区（L1-10）、`# --- Main ---` 注释块（L111-132）、`# --- Main game loop ---` 注释块（L265-315）。
- 模块级 docstring 首行: 无
- 主要导入: random, pygame, socket, json, threading, os, card_utils(card_data, Card), player(PlayerState)
- 关键模块级常量:
  - `font_path = os.path.join(os.path.dirname(pygame.__file__), "freesansbold.ttf")` (L10) — 直接取 pygame 包内的 DejaVu 兜底字体路径。
  - `DEBUG = True` (L12) — 开关；为真时跳过两个交互屏并用硬编码 IP。
  - `CARDS` (L15-26) — 47 个卡牌 id 的字符串列表（逐个数：L15=6、L16=5、L17=5、L18=4、L19=4、L20=4、L21=5、L22=4、L23=5、L24=5，合计 47），是本客户端可用的卡池。
  - `english_names = [card_data[each]['englishName'] for each in CARDS]` (L28) — 由卡牌 id 查 `client_side/card_utils.py` 的 `card_data` 取显示名。
  - `resolved = dict(zip(english_names, CARDS))` (L30) — 显示名 → 卡牌 id 的反查表（只在非 DEBUG 路径 L114 使用）。
  - `CARD_SIZE = 80` (L32) — 定义后全文未被使用（选择屏实际用 `CARD_W/CARD_H`）。
  - `CARD_W, CARD_H = 60, 90` (L33) — 选择屏缩略图尺寸。
  - `COLS = 6` (L34) — 选择屏列数。
  - `PORT = 9999` (L35) — 服务器端口。
  - `TILE = 22` (L147)、`AX, AY = 50, 50` (L148)、`AW, AH = 18 * TILE, 32 * TILE` (L149)、`W, H = AW + 120, AH + 100` (L150) — 世界坐标 → 屏幕的几何：一格 22 px、竞技场左上角 (50,50)、18×32 格。
  - 颜色常量解包 (L151-154)：`BLUE, RED, GREEN, CYAN, DKGRAY, BLACK, WHITE` 七个 RGB 三元组。
  - `CARD_GAP = 8` (L208)、`HAND_CARD_W = (AW - CARD_GAP * 3) // 4` (L209)、`HAND_CARD_H = int(HAND_CARD_W * 420 / 285)` (L210)、`HAND_Y = AY + AH + 30` (L211) — 4 张手牌铺满竞技场宽度的布局参数。
  - `H_NEW = HAND_Y + HAND_CARD_H + 20` (L262) — 二次 `set_mode` 用的最终窗口高度。
- 顶层数据表/字典:
  - `images` (L45-53)：键 = `english_names` 里的英文显示名，值 = 从 `images/<name>.png` 载入并缩放到 (60,90) 的 `Surface`；文件缺失/加载失败时（裸 `except`，L50）用 (80,80,80) 灰色填充的占位 `Surface`（L51-53）。
  - `state` (L135)：`{'entities': [], 'elixir': [5, 5], 'game_over': False, 'winner': None}`，由 `receiver` 线程用服务端 `state` 消息 `update` 覆盖；注意**初始键集合里没有 `hands`**，而 L299 直接读 `state['hands']`。
  - `hand_images` / `hand_images_bw` (L214-224)：键 = 卡牌 id，值分别是按 (HAND_CARD_W, HAND_CARD_H) 缩放后的彩色图与**灰度版**（L219-223 用 `pygame.surfarray.pixels3d` 取三维视图后把三通道替换为通道均值实现灰化）。
  - `player_state` (L202)：`PlayerState(player_id, list(deck), 5)` 客户端侧本地状态镜像。
- 其他模块级语句（无函数包装，导入即执行）：
  - `pygame.init()`、`screen = pygame.display.set_mode((700, 700))`、`set_caption("Card Selection")`、`font`(18)/`small_font`(12)/`clock` (L37-42)。
  - 主流程 L112-132：DEBUG 为真 ⇒ `deck = random.sample(CARDS, 8)`（L116）、`server_ip = "192.168.0.103"`（L120）；否则调 `card_selection_screen()`+`resolved`（L113-114）与 `ip_input_screen()`（L118）。随后建 TCP socket、`connect((server_ip, PORT))`（L122-123）、读一行 hello、取 `player_id`、`sendall` 发 `{'type':'deck','cards':deck}`（L126-129）、再阻塞读一行 start（L131）。
  - L161-163 第二次 `set_mode((W, H))` 并重建 `clock`、`render_font`（字体 `None`, 18）。
  - L267-315 主循环（下文各符号的调用点）。

### S:card_selection_screen  [L56-91]
- 类型: function
- 签名: `card_selection_screen()`
- 作用: 卡牌多选屏：滚动网格里点选恰好 8 张卡并返回英文显示名列表。
- 参数: 无
- 返回: `list[str]`——8 个英文显示名（在点中 Confirm 按钮时 `return selected`，L91）；其余情况不返回（`while True` 内继续循环）。
- 实现: L57-58 初始化 `selected=[]`、`scroll_y=0`；循环内先按 `i % COLS`/`i // COLS` 算 6 列网格坐标，Y 减去 `scroll_y` 后 blit 缩略图（L61-64）；已选卡描 3 px 绿框（L65-66），并在图下用 `small_font` 画前 8 个字符的标签（L67-68）；底部显示 `Select 8 cards (n/8)`（L70-71）；选满 8 张时在 (500,650,160,45) 画 Confirm 按钮（L72-74）。事件处理（L77-90）：`QUIT` 直接 `pygame.quit(); exit()`（L78）；`MOUSEWHEEL` 把 `scroll_y` 限制在 `max(0, scroll_y - event.y*30)`（L79-81，即只允许向下滚动、不允许出现负偏移）；`MOUSEBUTTONDOWN` 时把鼠标 Y 加回 `scroll_y` 做命中判定（L83），命中则已选则移除、未选且不足 8 张则追加（L88-89）；Confirm 的命中用**未加 scroll_y** 的原始坐标矩形 (500-660, 650-695) 判定（L90）。
- 调用: 定义于 L56；被 L113 调用，但 L112 的 `if not DEBUG` 在默认 `DEBUG=True` 下为假 ⇒ 默认运行路径不执行。内部调用 pygame 绘图/事件 API，无本仓函数调用。
- 置信度: 已确认

### S:ip_input_screen  [L95-109]
- 类型: function
- 签名: `ip_input_screen()`
- 作用: 文本输入屏：收集服务器 IP 字符串，按回车返回。
- 参数: 无
- 返回: `str`——输入框内累积的 IP 文本（L107 回车且非空时返回）。
- 实现: L96 初始 `ip=""`；循环里填背景、画提示语、输入框矩形 (150,310,400,40) 与当前文本（L98-101）、底部提示（L102）；事件处理中 `QUIT` 退出（L105），`KEYDOWN` 时：回车且 `ip` 非空 ⇒ 返回（L107）；退格 ⇒ 去掉最后一字符（L108）；`event.unicode.isprintable()` ⇒ 追加该字符（L109）。注意 L109 未过滤回车/退格之外的不可打印字符以外的功能键（`isprintable()` 对多数控制字符为假，因此该分支基本安全，但源码未做白名单限制）。
- 调用: 定义于 L95；被 L118 调用，同样受 `if not DEBUG` 门控 ⇒ 默认路径不执行。
- 置信度: 已确认

### S:receiver  [L136-143]
- 类型: function
- 签名: `receiver()`
- 作用: 后台线程：循环读取 socket 行并 JSON 解析，把 `type == 'state'` 的消息合入全局 `state`。
- 参数: 无
- 返回: 无（`while True` 中退出靠异常；函数末尾无返回值）
- 实现: L137 用 `sock.makefile()` 取独立文件对象（与主线程共用同一 socket，但读流独立）；死循环 L140 `f.readline()` 后 `json.loads`；若消息 `type` 为 `'state'`，用 `state.update(msg)` 覆盖全局字典（L141-142）；任何异常（含服务端关闭导致的空行 `json.loads` 报错）被裸 `except` 捕获并 `break` 结束线程（L143）——即**断线不报错、不重连**，主循环会继续用陈旧状态渲染。
- 调用: 定义于 L136；被 L145 `threading.Thread(target=receiver, daemon=True).start()` 启动（daemon 线程随主进程退出）。它调用 `sock.makefile`、`json.loads` 与全局 `state.update`。
- 置信度: 已确认

### S:w2s  [L156-159]
- 类型: function
- 签名: `w2s(x, y)`
- 作用: 世界（格）坐标 → 屏幕像素坐标，并按本方视角做 Y 轴翻转。
- 参数:
  - `x` (数值): 世界 X（格）。
  - `y` (数值): 世界 Y（格）。
- 返回: `tuple[int, int]`（`int(AX + x*TILE), int(AY + y*TILE)`）。
- 实现: L157-158 当 `player_id == 0` 时把 `y` 变为 `32 - y`（以 32 格高度做镜像），L159 统一乘 `TILE` 加偏移 `AX/AY` 并取整。注意 `player_id` 是 L127 从服务端 hello 得到的模块级变量（隐式全局依赖）。
- 调用: 定义于 L156；唯一调用点是 `draw_entities` L178。`player_id`、`AX`、`AY`、`TILE` 均为模块级全局。
- 置信度: 已确认

### S:draw_arena  [L165-174]
- 类型: function
- 签名: `draw_arena()`
- 作用: 画竞技场底板：绿地、中央河道、两座桥、18×32 格网格线。
- 参数: 无
- 返回: 无
- 实现: L166 先画整块绿色矩形 (AX,AY,AW,AH)；L167-168 把河道画在 `AY + 15*TILE` 起算的 2 格高青色条；L169-170 在 x 偏移 [2, 13] 两处画 3 格宽深灰桥；L171-172 画 19 条竖网格线（x=0..18，深绿 1 px），L173-174 画 33 条横网格线（y=0..32）。
- 调用: 定义于 L165；被主循环 L303 每帧调用。只调 pygame.draw。
- 置信度: 已确认

### S:draw_entities  [L176-191]
- 类型: function
- 签名: `draw_entities()`
- 作用: 把服务端实体列表画成带名字、血条（建筑还带血量数字）的圆。
- 参数: 无
- 返回: 无
- 实现: L177 遍历 `state.get('entities', [])`（用 `get` 容错，键缺失时画空）；L178 用 `w2s` 转屏幕坐标；L179 按 `e['player'] == player_id` 选蓝/红；L180 半径 = `collision_radius * TILE`；L181-182 画实心圆＋黑描边；L183-184 用 `render_font` 在圆下方 10 px 处居中画 `card_name`；L185-188 若 `hp>0 且 max_hp>0` 则画黑底血槽（宽 `max(r*2,16)`）与绿色血量比例条；L189-191 仅当 `e.get('type') == 'building'` 时在圆心叠白色整型血量文本。逐字段读取 `x/y/player/collision_radius/card_name/hp/max_hp/type`，全部假定服务端消息含这些键（无容错）。
- 调用: 定义于 L176；被主循环 L304 调用，内部调用 `w2s`(L178)。
- 置信度: 已确认

### S:draw_ui  [L193-200]
- 类型: function
- 签名: `draw_ui()`
- 作用: 在竞技场下方画一行状态文本（玩家号/时间/圣水，结束时追加胜负）。
- 参数: 无
- 返回: 无
- 实现: L194 取 `state.get('time', 0)`；L195 组串 `Player {player_id}  t={t:.1f}s  Elixir={state['elixir'][player_id]:.1f}`；L196-198 若 `state['game_over']` 为真，追加以 `winner == player_id` 判断的 `YOU WIN!`/`YOU LOSE`；L199-200 渲染并 blit 到 (AX, AY+AH+10)。
- 调用: 定义于 L193；被主循环 L307 调用。
- 置信度: 已确认

### S:get_hand_rects  [L226-233]
- 类型: function
- 签名: `get_hand_rects()`
- 作用: 返回手牌区前 4 张卡的 `(卡牌 id, Rect)` 列表。
- 参数: 无
- 返回: `list[tuple[str, pygame.Rect]]`——顺序即 `player_state.cycle[:4]`；X 步长 `HAND_CARD_W + CARD_GAP`，Y 固定 `HAND_Y`（L232）。
- 实现: L228 取 `player_state.cycle[:4]`（只展示前 4 张，即当前可打出的手牌）；L230-232 逐张算矩形并 append。docstring 为 `"""Return list of (card_name, rect) for the 4 cards in hand."""`（L227）。
- 调用: 定义于 L226；被 `draw_hand` L236 与主循环事件处理 L274 调用。
- 置信度: 已确认

### S:draw_hand  [L235-248]
- 类型: function
- 签名: `draw_hand()`
- 作用: 画 4 张手牌图（圣水不足时用灰度版），并逐张叠紫色费用徽标。
- 参数: 无
- 返回: 无
- 实现: L236 遍历 `get_hand_rects()`；L237 每张卡**新建一个 `Card` 对象**取 `elixir`，与 `player_state.elixir` 比较决定 `can_play`；L238 据此选彩色或灰度 `Surface`；L239-243 若正在拖拽且该牌是被选中的牌，则在原位画灰度图（表示正在被拖走），否则画 `img`；L244-248 无条件画 12 px 半径的紫色圆与白色费用数字，圆心在卡片左上角内 12 px 处。
- 调用: 定义于 L235；被主循环 L306 调用；内部调用 `get_hand_rects`(L236) 与 `Card(...)`（L237、L245 共两次构造/张/帧）。
- 置信度: 已确认

### S:draw_elixir_bar  [L250-259]
- 类型: function
- 签名: `draw_elixir_bar()`
- 作用: 画一条满宽圣水进度条并在条中央显示 `x.x/10`。
- 参数: 无
- 返回: 无
- 实现: L251-253 条宽 = `AW`、高 14、位置 `(AX, HAND_Y-20)`；L254 取 `player_state.elixir`；L255 画深灰底色；L256 填充宽 = `int((elixir/10.0)*bar_w)`（上限 10 硬编码，与 `player.py` L3 注释的 “max elixir is 10” 一致；若 elixir 超过 10 会画出界外）；L257 用紫色 (160,32,240) 画填充；L258-259 居中画白字 `{elixir:.1f}/10`。
- 调用: 定义于 L250；被主循环 L305 调用。
- 置信度: 已确认

---

## F:src/clasher_new/client_side/card_utils.py
- 行数: 180
- 模块作用: 客户端侧的卡牌数据层：**导入时**从当前工作目录读取 5 个 JSON（`gamedata.json`、`cards_stats_characters.json`、`cards_stats_spell.json`、`cards_stats_building.json`、`cards_stats_projectile.json`）建立 `card_data`（卡牌字典）与 characters/spells/buildings/projectiles 四张分表，并手工补齐 `golemite`/`LavaPups`/`Barbarian`/`KingTower` 等派生条目；其上定义 `Card`、`Projectile`、`TimedExplosiveData`、`AreaEffectData` 四个把原始字段换算成秒/格量纲的数据包装类。依据：L1-53 的加载与补丁段、L55 起的类定义。
- 模块级 docstring 首行: 无
- 主要导入: json, fastcore.all(nested_idx)
- 关键模块级常量:
  - `data` (L4-5, L20) — 先被整体赋为 `gamedata.json` 的解析结果，L20 重新绑定为 `data['items']['spells']`（在 `gamedata.json` 里所有卡牌条目都挂在该键下）。
  - `characters_data` / `air_units` / `characters` (L8-10) — 角色原始行列表；`air_units` = `flying_height != 0` 的名字列表（判定空中单位的来源）；`characters` = 名字 → 行 的字典。
  - `spells_data` / `spells` (L12-13)、`buildings_data` / `buildings` (L15-16) — 法术表、建筑表（名字 → 行）。
  - `projectiles` (L18) — 弹道表（名字 → 行），由 `cards_stats_projectile.json` 直接构建。
  - `card_data` (L21) — 卡牌 id → 卡牌条目字典；这是本模块最核心的导出。
  - `king_tower_stats` (L31-51) — 手写的国王塔条目（源码注释 L30 说明“`gamedata.json` 里没有国王塔”）：包含 `hitpoints=2100`、`hitSpeed=1000`、`damage=109`、`range/sightRange=7000`、`collisionRadius=2000`、`deployTime=3300`、`loadTime=700` 及内嵌 `KingProjectile`（speed 600 / damage 109）。
- 顶层数据表/字典:
  - `card_data`：键 = 卡牌 id（如 `'Knight'`、`'KingTower'`），值 = 原始 JSON 条目（含 `hitpoints`/`manaCost`/`tidType`/`tidTarget`/`summonCharacterData` 等字段）。
  - 动态补入的键（L23-28、L52-53）：`'Golemite'`（取 `Golem.summonCharacterData.deathSpawnCharacterData`，L23）；`'LavaPups'`（`{'name':..., 'summonCharacterData': lava_pups} | lava_pups`，L27，用 dict 合并把死亡分裂数据摊平到条目顶层）；`'Barbarian'`（同法取 `BattleRam` 的死亡分裂，L28，`lava_pups`/`barbarian` 中间变量见 L25-26）；`'KingTower'` = `king_tower_stats`（L52）；`'King_PrincessTowers'` 的 `summonCharacterData` 被就地改指为自身的 `statCharacterData`（L53）。
  - 注意 L58 `self.data.setdefault('summonCharacterData', self.data)` 会就地修改**共享的** `card_data` 条目：`Card.__init__` 不是拷贝，`self.data` 与 `card_data[name]` 是同一对象（L57）。
  - 未在本文件出现的符号（对照引擎版副本的差异证据）：`_value_at_level`、`level_scale`、`_rarity_level_index`、`character_to_card`、`projectile_from_row` 均**不存在**（全文读完后确认）。

### S:Card  [L55-135]
- 类型: class
- 签名: `class Card`（无基类、无类级 docstring）
- 作用: 把一条 `card_data` 原始条目扁平化成带物理量纲的卡牌对象（血量、费用、伤害、半径、速度、攻速、弹道、死亡分裂、跳跃、守卫等），并在构造末尾按稀有度调用 `set_level(11)` 折算成 11 级数值。
- 参数: 无（`__init__` 见下）
- 返回: 构造出的实例；实例上共设置约 40 个属性（逐行见 `__init__`）。
- 实现: 见 `__init__` 与 `set_level`；类内无其他方法与类变量。
- 调用: 本组内被 `client.py:237,245,275`（判断/显示费用）、`client_side/player.py:18,24`（费用校验与扣除）、本文件 `AreaEffectData.__init__:168`、`__main__:180` 构造。跨模块不被仓库其他文件使用（见文首前置事实）。
- 置信度: 已确认

#### M:Card.__init__  [L56-110]
- 类型: method
- 签名: `__init__(self, card_name)`
- 作用: 用 `card_data[card_name]` 填充卡牌的全部数值属性，并把毫秒/千分格之类的原始单位换算成秒/格。
- 参数:
  - `card_name` (str): `card_data` 的键，即卡牌 id（缺失会 `KeyError`，无容错）。
- 返回: 无（构造函数）。
- 实现: L57 取共享条目；L58 若无 `summonCharacterData` 则让它指向自身（保证后续 `self.data['summonCharacterData']` 恒可取到字典）。L59-80 依次取：`hp`（`hitpoints`，默认 0）、`elixir`（`manaCost`，默认 0，注释说明公主塔没有费用，L60）、`name`、`damage`、`spawn_number`（`summonNumber` 默认 1）、`spawn_delay`（`summonDeployDelay`/1000）、`spawn_radius`（`summonRadius` 默认 550，/1000）、`area_damage_radius`（/1000）、`projectile_damage_radius`（用 `nested_idx` 取 `projectileData.spawnProjectileData.radius`，L68）、`collision_radius`（默认 1000，/1000）、`hit_speed`（`hitSpeed`/1000）、`load_time`（`loadTime`/1000）、`speed`（`speed`/60）、`target_only_buildings`（`tidTarget == 'TID_TARGETS_BUILDINGS'`，L73）、`is_air_unit`（自身名或 `summonCharacterData.name` 落在 `air_units` 里，L74）、`attack_air`（`'AIR' in tidTarget`）、`attack_ground`（`'GROUND' in tidTarget` **或** `target_only_buildings`，L76）、`range`/`sight_range`/`deploy_time`/`charge_range`（各自 /1000，L77-80）。L82-88：`projectiles` 布尔（条目里是否有 `projectileData`）、`projectile_data` = `Projectile(...)`（无弹道时传空 dict 也得一个全默认对象，L83）、`projectile_waves`（`projectileWaves` 默认 1）、`wave_interval`（/1000）、`charge_damage`（`damageSpecial`）、`shield_health`（`shieldHitpoints`）。L90-100：`lifetime`（`lifeTime` 默认 `inf`）、`death_spawn_data`、`death_area_effect`、`death_damage`、`jump_height`、`jump_speed`（/60）、`spawn_data`（`spawnAreaObjectData`）、`kamikaze`（默认 False）。L102-105：`tower_damage_mult = 1 + crownTowerDamagePercent/100`；`type` = `tidType.split('_')[-1].lower()`（如 `TID_TYPE_TOWER_TROOP` → `'troop'`，缺字段则为空串）；`rarity` 默认 `'Common'`。L107-108 特例：`name == 'King_PrincessTowers'` 时强制 `collision_radius = 1.5`（覆盖 L69 的推导值）。L110 最后调用 `self.set_level(11)` 用 11 级数据覆盖 hp/damage。
- 调用: 由 `Card(card_name)` 触发（见类的“调用”行）。
- 置信度: 已确认

#### M:Card.set_level  [L112-135]
- 类型: method
- 签名: `set_level(self, level)`
- 作用: 按稀有度把等级换算成 `*_per_level` 数组下标，并用该下标覆盖弹道伤害、血量与（若原伤害非零）伤害。
- 参数:
  - `level` (int): 卡牌等级（构造时固定传 11）。
- 返回: `level`（原样返回，L135）。
- 实现: L113-117 按 `self.rarity` 分支算下标：Common 为 `level-1`、Rare 为 `level-3`、Epic 为 `level-6`、Legendary 为 `level-9`、Champion 为 `level-11`；**没有 else 分支** ⇒ 若 rarity 不在五类内，`level_index` 未绑定，后续任一次使用都会 `UnboundLocalError`。L119-121 若有弹道，用 `summonCharacterData.projectileData.name` 查 `projectiles[...]['damage_per_level'][level_index]` 覆盖 `self.projectile_data.damage`。L123-125 若 `self.type == 'troop'`，用 `summonCharacterData.name` 查 **`buildings`** 表的 `hitpoints_per_level[level_index]` 覆盖 `self.hp`（源码如此：troop 类型走建筑表）。L127-131 若 `self.type == 'character'`，先查 `characters` 表覆盖 `hp`，再在 `self.damage` 为真时覆盖 `damage`。L132-134 `elif self.type == 'spell'` 为空实现，注释说明“法术按弹道处理即可”。
- 调用: 仅被 `Card.__init__` L110 调用（本组文件内未发现其他调用点）。
- 置信度: 已确认

### S:Projectile  [L137-153]
- 类型: class
- 签名: `class Projectile`（无基类、无 docstring）
- 作用: 弹道数据包装：伤害、速度、命中半径、buff、击退、滚动射程、王塔伤害比例与空地命中判定。
- 参数: 无（`__init__` 见下）
- 返回: 实例。
- 实现: 见 `__init__`；类内无其他方法。
- 调用: 由 `Card.__init__` L83 构造；`client.py` 不直接使用该类。
- 置信度: 已确认

#### M:Projectile.__init__  [L138-153]
- 类型: method
- 签名: `__init__(self, projectile_data)`
- 作用: 从一条原始 `projectileData` 字典导出弹道属性。
- 参数:
  - `projectile_data` (dict): 原始弹道条目；`Card.__init__` 传 `{}` 时全部取默认值。
- 返回: 无。
- 实现: L139 保存原始字典；L140-150 依次：`damage`（默认 0）、`speed`（/60）、`radius`（优先 `spawnProjectileData.radius`，否则 `radius`，用 `or` 把 0 视为缺省，最后 /1000，L142）、`target_buff`（`targetBuffData`）、`buff_time`（`buffTime`/1000）、`hits_air`（`'AIR' in tidTarget`）、`hits_ground`（`'GROUND' in tidTarget` 或 `'BUILDING' in tidTarget`）、`pushback`（/1000）、`name`（默认 `'Unknown'`）、`roll_range`（`projectileRange`/1000）、`crown_tower_percent`（`(crownTowerDamagePercent + 100)/100`）。L151-153 特例：`name == 'TowerPrincessProjectile'` 时强制 `hits_air = hits_ground = True`。
- 调用: 仅由 `Card.__init__` L83 调用。
- 置信度: 已确认

### S:TimedExplosiveData  [L155-163]
- 类型: class
- 签名: `class TimedExplosiveData`（无基类、无 docstring）
- 作用: 定时爆炸（死亡分裂）数据包装：名字、死亡伤害、部署时间、碰撞半径、固定 3.0 射程与王塔伤害比例。
- 参数: 无（`__init__` 见下）
- 返回: 实例。
- 实现: 见 `__init__`；类内无其他方法。
- 调用: 在本组 5 个文件内**未发现任何构造点**（`client.py` 只从 `card_utils` 导入 `card_data, Card`，`player.py` 只导入 `Card`，`download_images.py`/`assure_names.py` 亦然）。
- 置信度: 已确认

#### M:TimedExplosiveData.__init__  [L156-163]
- 类型: method
- 签名: `__init__(self, death_spawn_data)`
- 作用: 从 `deathSpawnCharacterData` 条目导出死亡爆炸参数。
- 参数:
  - `death_spawn_data` (dict): 死亡分裂角色条目（直接下标取键，缺键即 `KeyError`）。
- 返回: 无。
- 实现: L157 保存原始字典；L158 `name`；L159 `damage = deathDamage`；L160 `deploy_time = deployTime/1000`；L161 `collision_radius = collisionRadius/1000`；L162 `range` 硬编码为 `3.0`（不来自数据）；L163 `crown_tower_damage_percent = crownTowerDamagePercent/100`，默认 100（即 ×1.0）。
- 调用: 本组文件内无调用点（见类的“调用”行）。
- 置信度: 已确认

### S:AreaEffectData  [L165-175]
- 类型: class
- 签名: `class AreaEffectData`（无基类、无 docstring）
- 作用: 区域效果（源码注释 L167 说明“只对 lumberjack 有效，以后改”）数据包装：持续时间、半径、buff 时间/数据、速度倍率、伤害与王塔伤害比例。
- 参数: 无（`__init__` 见下）
- 返回: 实例。
- 实现: 见 `__init__`；类内无其他方法。
- 调用: 在本组 5 个文件内**未发现任何构造点**。
- 置信度: 已确认

#### M:AreaEffectData.__init__  [L166-175]
- 类型: method
- 签名: `__init__(self, source_card_name)`
- 作用: 以“源卡牌名”为入口，取该卡的死亡区域效果子字典并导出参数。
- 参数:
  - `source_card_name` (str): 卡牌 id/名（用于构造 `Card`）。
- 返回: 无。
- 实现: L168 单个表达式：`Card(source_card_name)['summonCharacterData'].get('deathSpawnCharacterData', {}).get('deathAreaEffectData', {})`。**按源码，`Card` 是本文件自定义类、未定义 `__getitem__`（本文件全文无该方法）**，因此 `Card(...)[...]` 在运行时会抛 `TypeError`（对象不可下标）——除非另有运行时补丁（未在本组文件内发现）。L169-175 依次导出：`duration`（`lifeDuration`/1000）、`radius`（/1000）、`buff_time`（`buffTime`，注意**未除 1000**）、`buff_data`（`buffData`）、`speed_multiplier`（`speedMultiplier`，可能为 None）、`damage`（`spawnAreaEffectObjectData.damage`，默认 0）、`crown_tower_damage_percent`（`buffData.crown` 或 `crownTowerDamagePercent`，用 `or` 把 0 当缺省）。
- 调用: 本组文件内无调用点。
- 置信度: 已确认（“未定义 `__getitem__`”为读全文所得；运行时是否被外部动态修补无法确认，原因：越出本组允许阅读范围）

### S:__main__ 块（模块级语句，非符号）  [L177-180]
- 类型: 模块级 `if __name__ == '__main__':` 块（简报名单未列，照实说明）
- 作用: 临时调试脚本：对 8 张牌名的固定卡组逐个 `print(Card(each).type)`，用于查看 `tidType` 推导结果。
- 实现: L178 `deck = ['Knight','MiniPekka','Arrows','Minions','Musketeer','Fireball','Giant','Archer']`；L179-180 循环打印 `type`。
- 置信度: 已确认

---

## F:src/clasher_new/client_side/player.py
- 行数: 37
- 模块作用: 客户端侧的玩家状态容器：持有一副牌的循环队列、当前圣水与三座塔的血量，提供回费、出牌合法性/扣费/轮转、下一张牌与皇冠数计算。依据：L3 注释 “Hardcoded: max elixir is 10” 与 L5-37 的类定义。
- 模块级 docstring 首行: 无
- 主要导入: card_utils(Card)
- 关键模块级常量: 无（只有 L3 一行注释声明最大圣水 10，该数值以字面量 10 出现在 L14）
- 顶层数据表/字典: 无

### S:PlayerState  [L5-37]
- 类型: class
- 签名: `class PlayerState`（无基类、无 docstring）
- 作用: 单个玩家的可变状态：`cycle` 8 张牌的循环队列（前 4 张为手牌）、`elixir` 当前圣水、三塔血量；并给出出牌与皇冠判定。
- 参数: 无（`__init__` 见下）
- 返回: 实例。
- 实现: 见下 5 个方法；类内无类变量、无 property。
- 调用: 本组内由 `client.py:202` 构造（`PlayerState(player_id, list(deck), 5)`）；`regenerate_elixir`/`get_next_card`/`get_crown_count` 在 `client.py` 中**无调用点**（`client.py` 每帧用服务端值直接覆盖 `elixir` 与 `cycle`，见 `client.py:298-299`）。跨模块不被仓库其他文件使用（见文首前置事实）。
- 置信度: 已确认

#### M:PlayerState.__init__  [L6-10]
- 类型: method
- 签名: `__init__(self, player_id, cycle_queue, elixir, tower_hps=(4824, 3052, 3052))`
- 作用: 保存玩家 id、牌序队列、圣水，并把三元组塔血量拆成王塔/左塔/右塔三个属性。
- 参数:
  - `player_id` (int): 玩家编号（`client.py` 用 0/1，与服务端 hello 一致）。
  - `cycle_queue` (list): 牌序循环队列（`client.py:202` 传 `list(deck)`，8 张）。
  - `elixir` (数值): 初始圣水（`client.py` 传 5）。
  - `tower_hps` (tuple，默认 `(4824, 3052, 3052)`): 依次为 (王塔, 左塔, 右塔) 血量。
- 返回: 无。
- 实现: L7-10 四个赋值，其中 L10 用元组解包把 `tower_hps` 拆为 `self.king_tower_hp, self.left_tower_hp, self.right_tower_hp`。注意 `self.cycle = cycle_queue` 是**引用赋值**，与调用方传入的 list 共享同一对象（`client.py:299` 每帧把它整体换成服务端列表）。
- 调用: 由 `PlayerState(...)` 构造（本组内 `client.py:202`）。
- 置信度: 已确认

#### M:PlayerState.regenerate_elixir  [L12-14]
- 类型: method
- 签名: `regenerate_elixir(self, dt: float, base_regen_time: float = 2.8)`
- 作用: 按时间步长线性回费，上限 10。
- 参数:
  - `dt` (float): 经过的时间（秒）。
  - `base_regen_time` (float，默认 2.8): 每 1 点圣水所需秒数。
- 返回: 无（原地改 `self.elixir`）。
- 实现: L13 `elixir_per_second = 1.0 / base_regen_time`；L14 `self.elixir = min(10, self.elixir + elixir_per_second * dt)`（上限字面量 10，与 L3 注释一致）。本组内无调用点：`client.py` 每帧用服务端圣水覆盖本地值（`client.py:298`），故该函数在客户端主循环路径上不生效。
- 调用: 本组 5 个文件中无调用点（已逐文件确认）。
- 置信度: 已确认

#### M:PlayerState.can_play_card  [L16-19]
- 类型: method
- 签名: `can_play_card(self, card_name)`
- 作用: 判定某张牌此刻是否可以打出：在手牌前 4 张内、圣水够、且王塔未倒。
- 参数:
  - `card_name` (str): 卡牌 id。
- 返回: `bool`——三个条件同时成立（L17-19）。
- 实现: L17 `card_name in self.cycle[:4]`（只看手牌区）；L18 `self.elixir >= Card(card_name).elixir`（每次调用新建 `Card` 对象取费用）；L19 `self.king_tower_hp > 0`。三者用 `and` 短路连接，任一不成立即返回 False。
- 调用: 被 `play_card` L23 调用；被 `client.py:287`（鼠标抬起时的服务端发送门控）调用。
- 置信度: 已确认

#### M:PlayerState.play_card  [L21-27]
- 类型: method
- 签名: `play_card(self, card_name)`
- 作用: 扣费并把该牌从队列前部移到队尾（完成循环轮转）；不可出时返回 False 且不改状态。
- 参数:
  - `card_name` (str): 卡牌 id。
- 返回: `bool`——成功 True / 被 `can_play_card` 否决时 False。
- 实现: docstring L22 `"""Update the player's deck when playing a card."""`。L23 若 `can_play_card` 为假立即 `return False`；L24 扣 `Card(card_name).elixir`；L25-26 `self.cycle.remove(card_name)` 后 `self.cycle.append(card_name)`——用 `remove` 删第一个匹配项，把牌推到队尾。注意 L25 依赖 `card_name` 在队列中唯一（同一副牌 8 张不同卡时成立；重复卡会删错位置）。
- 调用: 被 `client.py:293` 调用（发送 deploy 消息之后本地同步扣费与轮转）。
- 置信度: 已确认

#### M:PlayerState.get_next_card  [L29-31]
- 类型: method
- 签名: `get_next_card(self)`
- 作用: 取循环队列中的第 5 张（即将补进手牌的那张）。
- 参数: 无
- 返回: `cycle[4]`（卡牌 id）；队列长度 < 5 时抛 `IndexError`（无边界检查）。
- 实现: docstring L30 `"""Return the next card in cycle, if known."""`；L31 单行 `return self.cycle[4]`。
- 调用: 本组 5 个文件中无调用点。
- 置信度: 已确认

#### M:PlayerState.get_crown_count  [L33-37]
- 类型: method
- 签名: `get_crown_count(self) -> int`
- 作用: 按塔血量算已获得的皇冠数：王塔倒=3，否则两座侧塔各计 1。
- 参数: 无
- 返回: `int`（0~3）。
- 实现: docstring L34 `"""Get number of crowns (destroyed towers)"""`；L35-36 若 `king_tower_hp <= 0` 直接 `return 3`（短路，不再数侧塔）；L37 否则 `int(left_tower_hp <= 0) + int(right_tower_hp <= 0)`，利用布尔转 int 累加。
- 调用: 本组 5 个文件中无调用点。
- 置信度: 已确认

---

## F:src/clasher_new/client_side/download_images.py
- 行数: 28
- 模块作用: 一次性数据采集脚本（非库）：从 `cards.json` 取每张卡的 `iconUrls.medium` URL，用 httpx 下载并写进 `images/<英文名>.png`。依据：L21-27 的主流程。
- 模块级 docstring 首行: 无
- 主要导入: card_utils(card_data, Card), json, httpx, pathlib(Path)
- 关键模块级常量:
  - `CARDS` (L6-17) — 47 个卡牌 id 的列表，内容与 `client.py` L15-26 的 `CARDS` 逐项相同（逐行数同为 47 项）。
  - `english_names = [card_data[each]['englishName'] for each in CARDS]` (L19) — id → 英文显示名（依赖 `client_side/card_utils.py` 导入时把 JSON 读完）。
  - `resolved = dict(zip(english_names, CARDS))` (L20) — 显示名 → id 反查表；**定义后未被使用**。
  - `names` (L24) — `cards.json['items']` 里 `name -> iconUrls['medium']` 的下载地址表。
- 顶层数据表/字典: `cards` (L21-22)：`json.load(open("cards.json"))["items"]`，卡牌官方 API 条目列表；`names` (L24) 见上。**注意 `names` 的键是官方 API 的 `name` 字段（如 `Knight`），而 L25-26 用 `english_names` 去索引 `names`**：两者必须是同一套英文名，否则 `KeyError`（是否一致无法确认，原因：需要读 `cards.json`/`gamedata.json` 数据文件内容，超出本组允许阅读范围）。
- 其他模块级语句（无函数/类，简报名单为 0 符号）：L21 打开并解析 `cards.json`；L25-27 `for each in english_names:` —— `httpx.get(names[each]).content` 取字节流（L26）、`Path(f"images/{each}.png").write_bytes(img)` 直接落盘（L27）、并 `print(f"Downloaded {each}")`（L28）。**没有 `if __name__ == '__main__'` 保护，且没有 `mkdir`**（`images/` 目录必须预先存在，仓库中已存在）；导入即联网下载。L1 导入的 `Card` 在全文未被使用。
- 置信度: 已确认

---

## F:src/clasher_new/client_side/assure_names.py
- 行数: 19
- 模块作用: 极简校验/探针脚本：照抄一份 47 卡 id 列表，然后在导入 `card_utils` 之后只打印列表长度。依据：全文只有 L6-17 的数据字面量与 L19 一条 `print`。
- 模块级 docstring 首行: 无
- 主要导入: card_utils(card_data, Card), json, httpx, pathlib(Path)
- 关键模块级常量:
  - `CARDS` (L6-17) — 47 个卡牌 id，与 `client.py`/`download_images.py` 的同一列表逐项相同（逐行数 47）。
- 顶层数据表/字典: 无
- 其他模块级语句（无函数/类，简报名单为 0 符号）：L19 `print(len(CARDS))`（输出 `47`）。L1-4 导入的 `card_data`、`Card`、`json`、`httpx`、`Path` **在全文均未被使用**；其中 `from card_utils import ...` 的副作用是执行 `card_utils` 的模块级 JSON 加载（L4-18），因此该脚本实际起“能否成功加载 5 个数据 JSON”的冒烟作用，但源码本身未对这个语义做任何断言或说明。无 `if __name__ == '__main__'` 保护。
- 置信度: 已确认

---

## 覆盖清单
| 文件 | 符号总数 | 已文档化 | 
|---|---|---|
| src/clasher_new/client_side/client.py | 10 | 10 |
| src/clasher_new/client_side/card_utils.py | 9 | 9 |
| src/clasher_new/client_side/player.py | 7 | 7 |
| src/clasher_new/client_side/download_images.py | 0 | 0 |
| src/clasher_new/client_side/assure_names.py | 0 | 0 |

COVERAGE_OK G040 files=5 symbols=26
