# 组 G043 文件级分析

## F:src/clasher_new/rl/opponents.py
- 行数: 208（`wc -l` 实测）
- 模块作用: 定义 RL 对手池用的**脚本对手**（非神经网络基线）。`ScriptedPolicy` 是统一的掩码随机出牌策略，可作 player-0 / player-1；`SelfDefenderPolicy` 把 `simulate_exchange.script_defender` 的确定性反制逻辑包成 env 对手，会真把部队放到过河敌军行进路线上。卡池由引擎数据构建（`build_card_pool`），四套固定卡组由常量 `FOUR_DECK_SET` 给出。依据：模块 docstring（L1-9）与顶层结构。
- 模块级 docstring 首行: `脚本对手 / 基线模型（random / heuristic / 卡组完全随机）。`
- 主要导入: os, sys, random, numpy(np), card_utils, rl.action_bundle
- 关键模块级常量:
  - `DECK_SIZE = 8` — 一副卡组 8 张（L24）。
  - `FOUR_DECK_SET = [...]`（L30-46）— 四种卡组字面量（2026-09-08 用户指定换血），依次为：速猪（HogRider/IceGolemite/IceSpirits/Musketeer/Cannon/Skeletons/Fireball/Log）、石头人（Golem/Witch/BabyDragon/MegaMinion/Bats/Tombstone/Zap/Snowball）、X 弩（Xbow/Tesla/Skeletons/IceWizard/Archer/Knight/Log/Fireball）、巨骷髅攻城槌（BarbLog/BattleRam/GiantSkeleton/WitchMother/Ghost/Vines/Wizard/MiniSparkys）。
  - `_PARENT`（L15-17，局部模块级变量）— 把 `src/clasher_new` 目录插入 `sys.path`，使 `card_utils` / `rl.*` 可导入。
- 顶层数据表/字典: 无（`FOUR_DECK_SET` 是列表常量，不是字典）。

### S:build_card_pool  [L49-68]
- 类型: function
- 签名: `def build_card_pool() -> list:`
- 作用: 遍历引擎卡表 `card_data.keys()`，返回**可部署**的卡名排序列表。
- 参数: 无
- 返回: `sorted(pool)` — 通过全部过滤条件的卡名字符串列表（docstring 称实测 139 张）。
- 实现: L52 遍历 `card_data.keys()`；L53-56 用 `Card(n)` 构造失败则跳过；L57-61 用 `n.startswith("King_")` 过滤王塔/塔形态卡（注释说明不能用 `"Tower" in n` / `endswith("Tower")`，否则误伤 BombTower / InfernoTower）；L62-64 读 `c.elixir`，为 None 或 ≤0 则跳过；L65-66 要求 `c.type` ∈ ("character","spell","building")，否则跳过；L67 收录；L68 排序后返回。
- 调用: 本组文件内无调用点。被 `observation.py` 的注释（L68）声明为手牌/卡名词表的集合来源之一；`rl/selftest.py` 中 `pol.deck()` 类测试间接涉及卡池（未在本组文件中确认直接调用点）⇒ 具体调用方待确认（原因：仅读了本组文件与被 import 的 `rl/pfsp.py`、`rl/elo.py`、`simulate_exchange.py` 片段，未全仓检索 `build_card_pool(`）。
- 置信度: 已确认（实现与返回值）；调用方"待确认"

### S:sample_deck  [L71-73]
- 类型: function
- 签名: `def sample_deck(rng, pool) -> list:`
- 作用: 从卡池里无放回随机抽 8 张互不相同的卡。
- 参数:
  - `rng` (random.Random 实例): 提供 `sample` 的随机源。
  - `pool` (list): 候选卡名列表。
- 返回: `list` — 长度为 `min(8, len(pool))` 的卡名列表。
- 实现: 单行 `return list(rng.sample(pool, min(DECK_SIZE, len(pool))))`（L73）；`min` 保证卡池不足 8 张时不抛异常。
- 调用: 被 `ScriptedPolicy.deck()` 在 `self.pool is not None` 时调用（L102）。
- 置信度: 已确认

### S:ScriptedPolicy  [L76-122]
- 类型: class
- 签名: `class ScriptedPolicy:`
- 作用: 掩码采样合法动作的脚本策略，可作任意一侧对手。docstring（L77-84）声明 `pool` 语义（None→固定默认卡组；list→每局从卡池重采样 8 张）与 `deck_pool` 语义（list[deck]→每局随机抽一副完整卡组）。
- 参数: 无（构造见 `__init__`）
- 返回: 无
- 实现: 类体只含 4 个方法 `__init__` / `deck` / `play` / `__call__`（L86-122），无类级常量、无类级属性声明。
- 调用: 被 `rl/selftest.py`（L617 附近，`[pol.deck() for _ in range(5)]`）使用；本组文件内不被调用。
- 置信度: 已确认

#### M:ScriptedPolicy.__init__  [L86-94]
- 类型: method
- 签名: `def __init__(self, mode="random", pool=None, deck_pool=None, seed=0, env=None):`
- 作用: 校验 mode 并保存随机源/卡池/env。
- 参数:
  - `mode` ("random"): 只接受 "random"/"heuristic"，其他值抛 `ValueError(f"未知 mode: {mode}")`（L87-88）。
  - `pool` (None): 若真值则 `list(pool)`，否则 None（L90）；**注意空列表会被转成 None**。
  - `deck_pool` (None): 同上，真值才 `list(...)`（L91）。
  - `seed` (0): 传入 `random.Random(seed)`（L92-93）。
  - `env` (None): 由 `play_pair`/训练循环注入（docstring L83），供 `__call__` 用（L94）。
- 返回: 无
- 实现: L87-88 mode 白名单校验；L89-94 依次赋 `mode`/`pool`/`deck_pool`/`seed`/`rng`/`env`。docstring 明确 mode="heuristic" 当前与 "random" 等价（都是掩码采样，L5）。
- 置信度: 已确认

#### M:ScriptedPolicy.deck  [L96-103]
- 类型: method
- 签名: `def deck(self):`
- 作用: 返回本局使用的卡组（三类来源的优先级选择）。
- 参数: 无（读 `self.deck_pool` / `self.pool`）
- 返回: `list` 卡名（从 deck_pool 抽或从卡池重采样 8 张）；两者都为假时返回 `None`（L103，表示用引擎默认卡组）。
- 实现: L98-100 若 `self.deck_pool` 非空，`self.rng.choice` 抽一副；若元素是 dict 取 `pick["cards"]`，否则直接 `list(pick)`。L101-102 否则若 `self.pool is not None`，调 `sample_deck(self.rng, self.pool)`。L103 否则返回 None。
- 调用: `rl/selftest.py` L617 调用（每次调用都会消耗 rng 并重采样）。**本组范围内的训练对手池 `_OpponentPool` 未在 `sample()` 中调用它**（`rl/train_solo.py` L436-477 只选择对手对象）⇒ 运行期实际谁调用 `deck()` 待确认（原因：未全仓检索）。
- 置信度: 已确认

#### M:ScriptedPolicy.play  [L105-116]
- 类型: method
- 签名: `def play(self, env, player_id: int) -> ActionBundle:`
- 作用: 从 `player_id` 的合法动作掩码里随机选一个「卡槽 + 落点」子动作。
- 参数:
  - `env`: 提供 `get_action_mask_for(player_id)`，返回含 `slots` 与 `cells` 的 dict。
  - `player_id` (int): 动作方（0/1）。
- 返回: `ActionBundle`；无合法卡槽或无合法格点时返回 `ActionBundle.noop()`（L110、L114）。
- 实现: L107 取掩码；L108 `np.flatnonzero(mask["slots"])` 得合法槽位下标；L109-110 空则 noop；L111 `rng.choice` 选槽；L112 取该槽的合法格点；L113-114 空则 noop；L115 随机选一格；L116 用 `ActionBundle.from_single(slot + 1, cell % 18, cell // 18)` 还原（槽位 +1 因为 bundle 槽位 1..4；18 = 网格宽，故 x=列、y=行）。
- 调用: 被 `__call__`（L122）调用；调用 `env.get_action_mask_for` 与 `ActionBundle.noop/from_single`。
- 置信度: 已确认

#### M:ScriptedPolicy.__call__  [L118-122]
- 类型: method
- 签名: `def __call__(self, obs):`
- 作用: player-1 对手的调用接口（忽略 obs，直接用注入的 env）。
- 参数:
  - `obs`: 未使用（docstring L119 说明为对手接口签名）。
- 返回: `ActionBundle`（来自 `self.play(self.env, 1)`）。
- 实现: L120-121 `self.env is None` 抛 `RuntimeError("ScriptedPolicy 需要先注入 env（.env = ...）")`；L122 返回 `self.play(self.env, 1)`——**硬编码 player_id=1**。
- 置信度: 已确认

### S:SelfDefenderPolicy  [L125-208]
- 类型: class
- 签名: `class SelfDefenderPolicy:`
- 作用: 真防守脚本对手（9j，A 层对手池组件）：威胁出现时用 `script_defender` 的确定性反制（挑 (DPS+HP/15)/费 最优反制部队 + 塔前迎击落点），无威胁时按 `passive_prob` 低频缓出。docstring（L126-136）强调与 `ScriptedPolicy(mode="heuristic")`（= mask 随机）的本质区别是会把部队放在过河敌军的行进路线上。
- 参数: 无（构造见 `__init__`）
- 返回: 无
- 实现: 类体含 `__init__` / `deck` / `_defend_action` / `_random_action` / `play` / `__call__`（L138-208）。反制用的 `script_defender(sim, defender_id)` 是**纯函数**（`simulate_exchange.py` L80-89，docstring 声明无副作用、被 `test_simulate_exchange` 对账）。
- 调用: 被 `rl/train_solo.py` `_OpponentPool.__init__` 导入并实例化（L338、L346-347：`SelfDefenderPolicy(seed=cfg.seed + 7, env=env, deck_pool=defender_deck_pool)`），作为对手池的 "defend" 槽。
- 置信度: 已确认

#### M:SelfDefenderPolicy.__init__  [L138-146]
- 类型: method
- 签名: `def __init__(self, seed=0, env=None, passive_prob: float = 0.6, deck_pool=None):`
- 作用: 保存随机源、env、停手概率与卡组集合。
- 参数:
  - `seed` (0): `random.Random(seed)`（L140-141）。
  - `env` (None): 外部注入；`_defend_action` / `_random_action` 都依赖它。
  - `passive_prob` (0.6): 无威胁帧的停手概率（L143 注释：高=更省费更防守；1.0=纯防守零进攻），强制 `float()`。
  - `deck_pool` (None): 真值才 `list(...)`（L146）；None=固定默认卡组。
- 返回: 无
- 实现: L140-146 六次赋值，无校验（`passive_prob` 未做 [0,1] 断言）。
- 置信度: 已确认

#### M:SelfDefenderPolicy.deck  [L148-153]
- 类型: method
- 签名: `def deck(self):`
- 作用: 本局卡组：有 deck_pool 抽一副完整卡组，否则返回 None（固定默认）。
- 参数: 无
- 返回: `list` 卡名 或 `None`（L153，注释说明与 `ScriptedPolicy` 无 pool 时一致）。
- 实现: L150-152 与 `ScriptedPolicy.deck` 的后半段同构：`rng.choice(deck_pool)`，dict 取 `["cards"]`。
- 置信度: 已确认

#### M:SelfDefenderPolicy._defend_action  [L155-179]
- 类型: method
- 签名: `def _defend_action(self, player_id: int):`
- 作用: 调 `script_defender` 拿反制候选，返回第一个「手牌里且前 4 槽内」候选的**本地网格坐标**三元组。
- 参数:
  - `player_id` (int): 防守方（`play` 恒传 1）。
- 返回: `(slot, gx, gy)` 或 `None`（无候选 / 全部候选都不可出，L159、L179）。
- 实现: L157 函数内 `from simulate_exchange import script_defender`；L158 取候选 `acts`（元素为 `(card, pos)`）；L159-160 空则 None；L161 取 `env.battle.players[player_id]`；L162-178 逐个候选：L163 `p.can_play_card(card)` 不通过则 `continue`；L166-167 卡必须在 `p.cycle[:4]`（部署要求在手牌前 4），否则跳过；L168 `slot = p.cycle.index(card) + 1`（1..4）；L171-177 **世界坐标→本地网格逆变换**：player_id==1 时 `gx = round(17.0 - wx)`、`gy = round(31.0 - wy)`（P1 有镜像），否则 `gx = round(wx - 0.5)`、`gy = round(wy - 0.5)`；L178 返回首个通过者。注释（L169-170）说明 `script_defender` 返回世界坐标（喂 `sim.deploy_card` 口径），而 `ActionBundle` 要本地网格坐标。
- 置信度: 已确认（镜像公式与 `observation.py` L121-123 的 `x=17-x, y=31-y` 一致）

#### M:SelfDefenderPolicy._random_action  [L181-191]
- 类型: method
- 签名: `def _random_action(self, player_id: int):`
- 作用: 掩码随机选一个 (slot, x, y)，与 `ScriptedPolicy.play` 的采样同构（但**不改槽位 +1**，返回原始 slot，由 `play` 统一转换）。
- 参数:
  - `player_id` (int): 动作方。
- 返回: `(slot, x, y)` 三点元组 或 `None`（无合法槽/格，L185、L189）。
- 实现: L182 `env.get_action_mask_for`；L183-185 空槽→None；L186 选槽；L187-189 空格→None；L190 选格；L191 返回 `(slot, cell % 18, cell // 18)`。
- 置信度: 已确认

#### M:SelfDefenderPolicy.play  [L193-203]
- 类型: method
- 签名: `def play(self, env, player_id: int = 1) -> ActionBundle:`
- 作用: 单帧决策：先按 `passive_prob` 决定是否尝试低缓出，否则/失败时走真防守反制。
- 参数:
  - `env`: 注入并保存到 `self.env`（L194）。
  - `player_id` (1): 默认 1（对手侧）。
- 返回: `ActionBundle`（`from_single(slot, x, y)`），全无动作时 `ActionBundle.noop()`（L200-201）。
- 实现: L194 `self.env = env`；L196-197 `rng.random() >= passive_prob` 时才调用 `_random_action`（即 **passive_prob 概率停手**）；L198-199 若未拿到动作则 `_defend_action`；L200-201 仍为 None → noop；L202-203 解包三元组并 `ActionBundle.from_single(slot, int(x), int(y))`。注意此处 slot **不 +1**（与 `ScriptedPolicy.play` 的 `slot + 1` 不同）——`_defend_action` 已返回 1..4，而 `_random_action` 返回的 0 基槽位在此处直接使用。
- 置信度: 已确认（slot 基数差异照实记录，未推测意图）

#### M:SelfDefenderPolicy.__call__  [L205-208]
- 类型: method
- 签名: `def __call__(self, obs):`
- 作用: player-1 对手接口。
- 参数:
  - `obs`: 未使用。
- 返回: `ActionBundle`。
- 实现: L206-207 `self.env is None` 抛 `RuntimeError("SelfDefenderPolicy 需要先注入 env（.env = ...）")`；L208 返回 `self.play(self.env, 1)`。
- 置信度: 已确认

## F:src/clasher_new/rl/plan_space.py
- 行数: 188（`wc -l` 实测）
- 模块作用: 定义 Prophet / BeliefPlanner 输出的 **plan token**（计划空间）及其到固定长度向量的离散化。布局分两段：前 21 维为旧布局（intent 8 + region 8 + 旧标量 5），其后为 v1 尾部追加字段；`PLAN_DIM` 由 `len(PlanToken().to_vector())` 动态算出，作为唯一维度常量源（docstring L3-5）。含旧 21 维向量的反解析入口 `from_old_layout`（兼容旧 ckpt/replay）。
- 模块级 docstring 首行: `计划空间：Prophet / BeliefPlanner 输出的 plan token（规划文档 5.2 / 5.5）。`
- 主要导入: os, sys, dataclasses(dataclass, field), typing(Optional, List), numpy(np)
- 关键模块级常量:
  - `MACRO_INTENTS = [...]`（L31-50）— 21 个宏观意图字符串。前 8 个为旧布局（索引 0-7）：`defend_left, defend_right, defend_king, push_left, push_right, counterpush, spell_value, cycle_and_wait`；后 13 个 v1 追加（索引 8-20）：`soft_control, spell_trade, punish, setup_wait, push_commit, pre_defend, spell_finish, king_activate, cycle_small, pull, protect_backline, anti_spell, save_ace`（各带中文注释）。
  - `_OLD_INTENT_COUNT = 8`（L51）— 旧语义意图数（兼容锚）。
  - `FOCUS_REGIONS = [...]`（L53-57）— 8 个关注区域：`own_left, own_center, own_right, bridge_left, bridge_right, enemy_left, enemy_center, enemy_right`。
  - `TARGET_KINDS = ["none","unit","building","tower","my_backline"]`（L60-62）— v1 行动目标类型。
  - `PLACEMENT_HINTS`（L65-74）— 8 个放位策略：`none, pull_across, pull_aggro, support_zone, anti_spell_zone, bridge_front, king_front, intercept_mid`。
  - `OPP_SPELL_THREATS = ["none","fireball","poison","lightning","freeze","big_unknown"]`（L77-79）— 对手法术威胁类别。
  - `ACE_CARDS = ["Lightning","Vines","Freeze","Rocket"]`（L83）— save_ace 固定终结卡名单。
  - `COMBO_NONE, COMBO_TANK_SUPPORT, COMBO_SPELL_UNIT, COMBO_SPLIT_PUSH = 0, 1, 2, 3`（L86）— bundle 组合语义。
  - `PLAN_DIM = int(len(PlanToken().to_vector()))`（L187）— 唯一维度常量源（docstring 称 57；L12 正文亦称 PLAN_DIM = 57）。
  - `_OLD_PLAN_DIM = 21`（L188）— 旧布局维度。
- 顶层数据表/字典: 无（全部为 list 常量）。`dataclass` 是数据容器，不是顶层字典。

### S:_one_hot  [L89-95]
- 类型: function
- 签名: `def _one_hot(name, choices, default_idx=0, dtype=np.float32) -> np.ndarray:`
- 作用: 把类别名转成 one-hot 向量；不在候选中则置 `default_idx` 位为 1。
- 参数:
  - `name`: 类别名（字符串）。
  - `choices` (list): 候选名列表，决定向量长度与索引。
  - `default_idx` (0): 未知名的回退索引。
  - `dtype` (np.float32): 输出 dtype。
- 返回: `np.ndarray`，长度 `len(choices)`。
- 实现: L90 建零向量；L91-92 `name in choices` 则按 `choices.index(name)` 置 1.0；L93-94 否则 `vec[default_idx] = 1.0`；L95 返回。**注意**：`default_idx` 不做范围校验（越界会 IndexError）。
- 调用: 被 `PlanToken.to_vector` 三次调用（L150-152：target/hint/threat）以及 region 一次（L141-142，`default_idx=FOCUS_REGIONS.index("own_center")`）。
- 置信度: 已确认

### S:PlanToken  [L99-183]
- 类型: class（`@dataclass` 装饰，L98）
- 签名: `class PlanToken:`
- 作用: 战术意图 token 的数据容器 + 到固定长度向量的编解码。
- 参数: 见各字段
- 返回: 无
- 实现: 字段分两组——旧布局字段（`macro_intent="cycle_and_wait"`、`focus_region="own_center"`、`suggested_card: Optional[int]=None`（槽位 1..4）、`bundle_size_hint=1`、`combo_hint=0`、`risk_profile=0.5`、`value_estimate=0.0`，L100-106）与 v1 追加字段（`target_kind="none"`、`placement_hint="none"`、`opp_spell_threat="none"`、`elixir_budget=1.0`、`hold_mask=0`（4 bit），L108-112）。类方法：`intent`（classmethod 构造）、`hold_slots`、`to_vector`、`from_old_layout`（classmethod）、`zeros`（classmethod）。
- 调用: 被 `rl/follower.py` 相关链路与 `rl/selftest.py` 使用（本组未确认具体行号）⇒ 待确认（原因：未读 `rl/follower.py`）。
- 置信度: 已确认（字段与默认值已逐条核对源码）

#### M:PlanToken.intent  [L117-118]
- 类型: classmethod（`@classmethod`，L116）
- 签名: `def intent(cls, name, region="own_center", **kw) -> "PlanToken":`
- 作用: 便捷构造：用意图名 + 区域 + 任意额外字段建 PlanToken。
- 参数:
  - `name`: 赋给 `macro_intent`。
  - `region` ("own_center"): 赋给 `focus_region`。
  - `**kw`: 其余 dataclass 字段透传。
- 返回: `PlanToken`（`cls(macro_intent=name, focus_region=region, **kw)`）。
- 实现: 单行返回（L118）；无校验（未知 kwargs 由 dataclass `__init__` 抛 TypeError）。
- 置信度: 已确认

#### M:PlanToken.hold_slots  [L120-122]
- 类型: method
- 签名: `def hold_slots(self) -> List[int]:`
- 作用: 解出 hold_mask 命中的槽位列表（1 基）。
- 参数: 无
- 返回: `List[int]`，元素为 1..4 中 `(hold_mask >> (i)) & 1` 为 1 的槽位。
- 实现: 列表推导 `[i + 1 for i in range(4) if (self.hold_mask >> i) & 1]`（L122），即 bit0→槽 1、bit1→槽 2、bit2→槽 3、bit3→槽 4。
- 置信度: 已确认

#### M:PlanToken.to_vector  [L124-160]
- 类型: method
- 签名: `def to_vector(self) -> np.ndarray:`
- 作用: 把 token 离散化为定长向量，前 21 维严格等于旧布局。
- 参数: 无
- 返回: `np.ndarray`（float32），长度 = 8 + 8 + 5 + 13 + 5 + 8 + 6 + 1 + 4 = 58（**按代码实算**；模块 docstring/注释写 57 ⇒ 见"实现"末的口径说明）。
- 实现: L133-140 构造 intent：`intent_old` 8 维 + `intent_new`（`len(MACRO_INTENTS)-8` = 13 维）；`macro_intent` 属旧 8 → 旧组 one-hot 且新组全 0；属新意图 → 旧组全 0、新组 `index-8` 位；否则 fallback 到 `cycle_and_wait` 的旧位（L140）。L141-142 region 用 `_one_hot`（默认 `own_center`）。L143-149 `old_scalars` 5 维：`suggested_card or 0)/4.0`、`bundle_size_hint`、`combo_hint`、`clip(risk_profile,0,1)`、`clip(tanh(value_estimate),-1,1)`。L150-154 `target`(5)/`hint`(8)/`threat`(6) one-hot 与 `hold`(4) 位展开。L155-160 按序 `concatenate([intent_old, region, old_scalars, intent_new, target, hint, threat, [clip(elixir_budget,0,1)], hold])`。
- 调用: 被模块尾 `PLAN_DIM = int(len(PlanToken().to_vector()))`（L187）调用；调用 `_one_hot`（L141、L150-152）。
- 置信度: 已确认（代码实算长度 58；源码注释 L12 与 L186 写 57，属源码内注释与实现的**数字不一致**，照实记录，不代为解释）

#### M:PlanToken.from_old_layout  [L163-179]
- 类型: classmethod（`@classmethod`，L162）
- 签名: `def from_old_layout(cls, old_vec: np.ndarray) -> "PlanToken":`
- 作用: 把旧 21 维向量反解析回 token（v1 新字段取默认）。
- 参数:
  - `old_vec` (np.ndarray): 期望长度 ≥21 的旧布局向量；不足时逐段降级。
- 返回: `PlanToken`。
- 实现: L168 `intent_idx = argmax(old_vec[0:8])`（size<8 时取 0）；L169 `region_idx = argmax(old_vec[8:16])`（size<16 时取 0）；L170 `scal = old_vec[16:21]`（size<21 时取 `np.zeros(5)`）；L171-179 构造：`macro_intent`/`focus_region` 均用 `min(idx, len-1)` 夹紧；`suggested_card = round(scal[0]*4)` 且仅当 `scal[0] > 0` 否则 None；`bundle_size_hint = int(scal[1])`、`combo_hint = int(scal[2])`、`risk_profile = float(scal[3])`、`value_estimate = arctanh(clip(scal[4], -0.999, 0.999))`（与 `to_vector` 的 tanh 互逆）。
- 调用: 源码注释（L13）称旧 ckpt 经 `rl.follower.load_checkpoint` 前 21 列拷贝加载；本组文件内无调用点 ⇒ 调用方待确认（原因：未读 `rl/follower.py`）。
- 置信度: 已确认（实现）；调用方"待确认"

#### M:PlanToken.zeros  [L182-183]
- 类型: classmethod（`@classmethod`，L181）
- 签名: `def zeros(cls) -> "PlanToken":`
- 作用: 返回全默认字段的 token。
- 参数: 无
- 返回: `PlanToken`（即 `cls()`）。
- 实现: 单行（L183）。**注意**：名为 zeros 但字段默认值并非全零——`macro_intent="cycle_and_wait"`、`focus_region="own_center"`、`bundle_size_hint=1`、`risk_profile=0.5`、`elixir_budget=1.0`（L100-111）。
- 置信度: 已确认

## F:src/clasher_new/rl/bayes_filter.py
- 行数: 184（`wc -l` 实测）
- 模块作用: 对手 8 卡循环牌序的信念滤波器（规划文档 3.6 规则信念层）。核心是 v2 的 **O(1) 队列锁定流**：由数学事实「第 4 张起当前手牌集合 = 卡组 − 最近 4 张（互异），下一张进手 = 第 k−3 张打出的牌，与开局洗牌无关」替代 v1 的 40320 全量排列粒子滤波；粒子相只用于前 3 张与异常观测。docstring（L1-29）声称该数学事实经暴力验证 3000 随机对局 × 59 步零反例。不引入 hash/系统随机 ⇒ 同 seed 跨进程一致（L28）。
- 模块级 docstring 首行: `对手 8 卡循环牌序信念（规划文档 3.6 规则信念层）。`
- 主要导入: os, sys, random, numpy(np)
- 关键模块级常量: 无
- 顶层数据表/字典: 无

### S:CycleBayesFilter  [L42-184]
- 类型: class
- 签名: `class CycleBayesFilter:`
- 作用: 维护对手 8 卡循环队列的信念，提供「手牌概率 / 下一张概率 / 熵 / 是否锁定」四个查询。
- 参数: 无（构造见 `__init__`）
- 返回: 无
- 实现: 状态字段见 `__init__`；方法含 `locked`（property）、`reset`、静态 `_play`、`_lock_from_tail`、`_resample_uniform`、`_degrade`、`_consistent`、`update`、`hand_probs`、`next_probs`、`entropy`。状态机语义：`_cycle is not None` 为锁定流，否则粒子相。
- 置信度: 已确认

#### M:CycleBayesFilter.__init__  [L45-54]
- 类型: method
- 签名: `def __init__(self, deck, n_particles: int = 128, seed: int = 0):`
- 作用: 记录卡组与粒子数，初始化观测序列/粒子/权重/锁定状态，并做一次均匀先验重采样。
- 参数:
  - `deck`: 对手 8 卡卡组，`list()` 拷贝（L46）。
  - `n_particles` (128): 粒子数（L47）。
  - `seed` (0): `random.Random(seed)`（L48）。
- 返回: 无
- 实现: L49 `self.observed = []`（观测出牌序列，含异常观测，供调试/重锁窗口）；L50 `self.particles = []`；L51 `self.weights = np.ones(n_particles, dtype=np.float32) / n_particles`；L52 `self._cycle = None`（None=粒子相）；L53 `self._run = 0`（粒子相连续一致步数）；L54 调 `_resample_uniform()`。
- 置信度: 已确认

#### M:CycleBayesFilter.locked  [L57-59]
- 类型: property（`@property`，L56）
- 签名: `def locked(self) -> bool:`
- 作用: 是否处于 O(1) 精确锁定流（hand/next 概率 0/1、熵 0）。
- 参数: 无
- 返回: `bool`（`self._cycle is not None`）。
- 实现: 单行返回（L59）。
- 置信度: 已确认

#### M:CycleBayesFilter.reset  [L61-67]
- 类型: method
- 签名: `def reset(self, deck=None):`
- 作用: 复位滤波器（可换卡组），清观测、回粒子相。
- 参数:
  - `deck` (None): 非 None 才替换 `self.deck`（L62-63）。
- 返回: 无
- 实现: L62-63 可选换 deck；L64 `observed=[]`；L65 `_cycle=None`；L66 `_run=0`；L67 `_resample_uniform()`。**未重置 `self._rng`** ⇒ 同 seed 但在 reset 前后采样流不同（照实记录）。
- 置信度: 已确认

#### M:CycleBayesFilter._play  [L72-74]
- 类型: staticmethod（`@staticmethod`，L71）
- 签名: `def _play(perm, card):`
- 作用: 队列规则：打出 `card` 后把它移到队尾。
- 参数:
  - `perm`: 当前 cycle 排列（须让 `card` 落在 `perm[:4]`，否则等价于"把该卡移到队尾"而卡组不变）。
  - `card`: 打出的卡。
- 返回: 新排列 `list`：`[c for c in perm if c != card] + [card]`。
- 实现: 单行（L74），与 `player.play_card` 的队列更新一致（docstring L3-5）。
- 置信度: 已确认

#### M:CycleBayesFilter._lock_from_tail  [L76-87]
- 类型: method
- 签名: `def _lock_from_tail(self, last4):`
- 作用: 用最近 4 张合法出牌重建规范 cycle，进入锁定流。
- 参数:
  - `last4`: 最近 4 张打出的牌（调用方保证互异且都在 deck 内）。
- 返回: 无
- 实现: L82 `queue = list(last4)`；L83 `qset = set(queue)`；L84 规范 cycle = `[c for c in self.deck if c not in qset] + queue`（手牌取卡组序，队尾 = 真实进手序）；L85 清空粒子；L86 权重置空数组；L87 `_run = 0`。
- 置信度: 已确认

#### M:CycleBayesFilter._resample_uniform  [L89-100]
- 类型: method
- 签名: `def _resample_uniform(self):`
- 作用: 均匀先验重采样粒子（去重、等权）。
- 参数: 无
- 返回: 无
- 实现: L91-99 `while len(self.particles) < self.n_particles`：每次 `shuffle(list(self.deck))`，用 tuple 作 key 去重，未见过才 append；L100 权重置 `1/n_particles`。**边界**：若 `len(deck)!` 的全排列数 < `n_particles`，该循环永不退出（代码无上限保护）——照实记录为潜在死循环风险。
- 置信度: 已确认

#### M:CycleBayesFilter._degrade  [L102-106]
- 类型: method
- 签名: `def _degrade(self):`
- 作用: 异常/分歧时退回粒子相（保留 observed，后续合法 4 张自动重锁）。
- 参数: 无
- 返回: 无
- 实现: L104 `_cycle=None`；L105 `_run=0`；L106 `_resample_uniform()`。
- 置信度: 已确认

#### M:CycleBayesFilter._consistent  [L108-114]
- 类型: method
- 签名: `def _consistent(self, card):`
- 作用: 粒子一致性筛选：只保留能打出 `card` 的候选，并推进到打出后状态，结果去重。
- 参数:
  - `card`: 观测到的出牌。
- 返回: `list[list]` — 演化后的候选 cycle 列表（去重）。
- 实现: L110 `out = {}`（用 dict 去重）；L111-113 对每个 `perm`，若 `card in perm[:4]` 则 `out[tuple(self._play(perm, card))] = True`；L114 返回 `[list(k) for k in out]`。**不含权重**（等权由调用方在 `update` 里重设）。
- 置信度: 已确认

#### M:CycleBayesFilter.update  [L116-147]
- 类型: method
- 签名: `def update(self, played_card):`
- 作用: 观测到对手打出某张牌 → 推进信念（锁定流 O(1)，或粒子相筛选/重采样/重锁）。
- 参数:
  - `played_card`: 打出的卡；`None` 直接返回（L118-119，视为无观测）。
- 返回: 无
- 实现: L120 追加进 `self.observed`（**含异常观测**）；L123-129 锁定流分支：若 `played_card in self._cycle[:4]` 则 `_play` 推进并 return，否则（手牌外出牌/状态分歧）`_degrade()` 并 return（**不推进**）；L132 粒子相 `perms = self._consistent(played_card)`；L133-136 无一致解 → `_degrade()` 并 return；L138-139 全量保留 `perms` 并置等权（注释 L137：一致解数 ≤ 输入粒子数，故无抽样灭真解）；L140 `_run += 1`；L143-147 当 `_run >= 4` 且 `len(self.observed) >= 4`，取 `last4 = observed[-4:]`，若 4 张互异（`len(set)==4`）且都在 deck 内则 `_lock_from_tail(last4)`。
- 置信度: 已确认

#### M:CycleBayesFilter.hand_probs  [L151-165]
- 类型: method
- 签名: `def hand_probs(self) -> np.ndarray:`
- 作用: 每张卡在对手手牌（`cycle[:4]`）中的概率，按 deck 顺序返回。
- 参数: 无
- 返回: `np.ndarray(float32)`，长度 = `len(self.deck)`。
- 实现: L156 `probs = {c: 0.0 for c in self.deck}`；L157-159 锁定流：`_cycle[:4]` 每张置 1.0（精确 0/1）；L160-164 粒子相：`n = max(1, len(particles))`，对每个排列的前 4 张累加 `1.0/n`（频率近似）；L165 按 deck 顺序取值成数组。
- 置信度: 已确认

#### M:CycleBayesFilter.next_probs  [L167-176]
- 类型: method
- 签名: `def next_probs(self) -> np.ndarray:`
- 作用: 每张卡是"对手下一张牌"（`cycle[4]`）的概率，按 deck 顺序返回。
- 参数: 无
- 返回: `np.ndarray(float32)`，长度 = `len(self.deck)`。
- 实现: L169 `probs` 初始化；L170-171 锁定流：`probs[self._cycle[4]] = 1.0`；L172-175 粒子相：累加 `1.0/n`；L176 返回。**边界**：`next_probs` 依赖 `perm[4]`，若粒子长度不足 5 会 IndexError（代码未防护）。
- 置信度: 已确认

#### M:CycleBayesFilter.entropy  [L178-184]
- 类型: method
- 签名: `def entropy(self) -> float:`
- 作用: 手牌分布香农熵；锁定流恒 0。
- 参数: 无
- 返回: `float`（nats，自然对数）。
- 实现: L180-181 锁定流返回 0.0；L182 `p = self.hand_probs()`；L183 只留 `p > 0`；L184 返回 `-(p * log(p)).sum()`，`p.size == 0` 时返回 0.0。
- 置信度: 已确认

## F:src/clasher_new/rl/league.py
- 行数: 181（`wc -l` 实测）
- 模块作用: 联赛机制（规划文档 7）：对手池成员管理 + PFSP 采样 + Elo + checkpoint 注册/刷新 + Exploiter 入口 + 持久化。docstring（L1-9）记录三处修复：P1-9（`register_checkpoint` 注册新条目并保存权重副本，原 main 保持 `kind="main"`）、P1-10（`save_state`/`load_state` 持久化 Elo/PFSP/成员元信息）、P2（同 id 默认抛异常、`add_exploiter` 用递增计数器、补 `remove_agent`/`retire`）。
- 模块级 docstring 首行: `联赛机制（规划文档 7）：对手池 + PFSP + Elo + checkpoint + Exploiter 入口。`
- 主要导入: os, sys, dataclasses(dataclass, field), rl.pfsp(PFSP), rl.elo(Elo)
- 关键模块级常量: 无
- 顶层数据表/字典: 无

### S:LeagueAgent  [L24-29]
- 类型: class（`@dataclass`，L23）
- 签名: `class LeagueAgent:`
- 作用: 联赛成员的数据记录（id / 类别 / 策略对象 / checkpoint 路径 / Elo）。
- 参数: 见字段
- 返回: 无
- 实现: 5 个 dataclass 字段：`agent_id: str`（必填）、`kind: str = "main"`（注释枚举 main / historical / exploiter / baseline）、`policy: object = None`（callable(obs) -> ActionBundle | tuple）、`path: str = None`（checkpoint 路径）、`elo: float = 1500.0`。**类内无任何方法**（L24-29 全为字段声明）。注意 `elo` 字段实际未被 `League` 读写——`League` 的评分走 `self.elo`（`Elo` 实例）的 `ratings` 字典（L36、L50、L61），本字段在 `add_agent`（L49）里也未传值 ⇒ 恒为默认 1500.0。
- 调用: 被 `League.add_agent`（L49）构造；被 `League.load_state`（L178-180）构造（此时同样不传 `elo`）。
- 置信度: 已确认

### S:League  [L32-181]
- 类型: class
- 签名: `class League:`
- 作用: 联赛：成员表 + PFSP 采样器 + Elo 表 + 历史/统计持久化。
- 参数: 无（构造见 `__init__`）
- 返回: 无
- 实现: 方法：`__init__`、`add_agent`、`sample_opponent`、`record_match`、`register_checkpoint`、`refresh_snapshot`、`record_elo_history`、`record_round_stats`、`add_exploiter`、`remove_agent`（+ 别名 `retire = remove_agent`，L139）、`elo_table`、`save_state`、`load_state`。
- 置信度: 已确认

#### M:League.__init__  [L33-42]
- 类型: method
- 签名: `def __init__(self, pfsp_beta: float = 1.0, elo_k: float = 32.0, seed: int = 0):`
- 作用: 初始化成员表、PFSP、Elo 与各类记录容器。
- 参数:
  - `pfsp_beta` (1.0): 传给 `PFSP(beta=...)`。
  - `elo_k` (32.0): 传给 `Elo(k=...)`。
  - `seed` (0): 传给 `PFSP(seed=...)`（Elo 无随机）。
- 返回: 无
- 实现: L34 `self.agents = {}`；L35 `self.pfsp = PFSP(beta=pfsp_beta, seed=seed)`；L36 `self.elo = Elo(k=elo_k)`；L37 `self.history = []`（记录 `(agent_a, agent_b, score_a)` 元组）；L38 `_exploiter_counter = 0`；L39 `_ckpt_counter = {}`；L40 `elo_history = {}`（agent_id → [(step, elo)]，供训练网页 UI 画曲线）；L41 `round_stats = []`（每评估轮 `{step, est, games}`）；L42 `total_steps = 0`。
- 置信度: 已确认

#### M:League.add_agent  [L44-50]
- 类型: method
- 签名: `def add_agent(self, agent_id, kind="main", policy=None, path=None, replace=False):`
- 作用: 新增成员；同 id 且未 `replace` 时抛异常。
- 参数:
  - `agent_id`: 成员 id。
  - `kind` ("main"): 类别标签。
  - `policy` (None): 策略对象。
  - `path` (None): checkpoint 路径。
  - `replace` (False): True 则先 pop 旧条目（**重置其 Elo 吗？——不重置**，`self.elo.ratings` 未清理）。
- 返回: 无
- 实现: L45-48 已存在且不 replace → `raise ValueError`，replace 则 `pop`；L49 建 `LeagueAgent(...)`；L50 `self.elo.ensure(agent_id)`（不存在则初始化 1500.0）。
- 置信度: 已确认

#### M:League.sample_opponent  [L52-57]
- 类型: method
- 签名: `def sample_opponent(self, agent_id) -> LeagueAgent:`
- 作用: 为 `agent_id` 用 PFSP 采一个对手成员。
- 参数:
  - `agent_id`: 采样方。
- 返回: `LeagueAgent`（被选中的对手）。
- 实现: L53 候选 = 除自己外的所有 `self.agents` 键；L54-55 空则 `RuntimeError("联赛至少需要 2 个 agent")`；L56 `op_id = self.pfsp.sample(agent_id, opponents)`；L57 返回 `self.agents[op_id]`。PFSP 权重语义见下方"对手池与脚本对手采样逻辑"节。
- 置信度: 已确认

#### M:League.record_match  [L59-64]
- 类型: method
- 签名: `def record_match(self, agent_a, agent_b, score_a: float, n_games: int = 1):`
- 作用: 回填一场（或聚合多局）比赛：更新 Elo + 双向 PFSP 胜率 + 追加历史。
- 参数:
  - `agent_a` / `agent_b`: 对局双方 id。
  - `score_a` (float): a 的得分 ∈ {0, 0.5, 1}。
  - `n_games` (1): >1 时按局数缩放 Elo K（P1-11，docstring L60）。
- 返回: 无
- 实现: L61 `self.elo.update(agent_a, agent_b, score_a, n_games=n_games)`；L62-63 双向 `pfsp.update_winrate`（b 对 a 记 `1 - score_a`）；L64 `self.history.append((agent_a, agent_b, score_a))`。
- 置信度: 已确认

#### M:League.register_checkpoint  [L66-84]
- 类型: method
- 签名: `def register_checkpoint(self, agent_id, policy, path=None, metadata=None):`
- 作用: 把当前策略的**权重副本**注册成新的 historical 成员，返回新 id。
- 参数:
  - `agent_id`: 源策略 id（用作命名前缀与计数器键）。
  - `policy`: 源策略对象（须有 `hidden_dim`/`plan_dim`/`belief_dim` 属性与 `state_dict()`）。
  - `path` (None): 记录到新成员的 path 字段。
  - `metadata` (None): **函数体内未使用**（照实记录）。
- 返回: `new_id`（`f"{agent_id}_ckpt{n}"`，n 从 0 递增）。
- 实现: L71 函数内 `from rl.follower import FollowerPolicy`；L72-74 取/递增 `_ckpt_counter[agent_id]` 并拼 id；L75-78 用源策略的 `hidden_dim` / `plan_dim` / `belief_dim` 与 `value_bypass`、`value_independent`（`getattr` 兜底 False）构造 `FollowerPolicy` 快照；L79 `load_state_dict(policy.state_dict())`；L80 `snap.eval()`；L81-82 全部参数 `requires_grad_(False)`；L83 `add_agent(new_id, kind="historical", policy=snap, path=path)`；L84 返回 id。原 main 条目完全不动（P1-9）。
- 置信度: 已确认（实现）；`FollowerPolicy` 侧行为待确认（原因：未读 `rl/follower.py`）

#### M:League.refresh_snapshot  [L86-102]
- 类型: method
- 签名: `def refresh_snapshot(self, agent_id, policy, path=None):`
- 作用: 把 main 的权重副本刷到**固定槽位** `f"{agent_id}_ckpt"`（维持 5 模型稳定结构，替换不重置 Elo）。
- 参数:
  - `agent_id`: 源策略 id。
  - `policy`: 源策略对象。
  - `path` (None): 记录路径。
- 返回: `new_id`（固定槽位 id）。
- 实现: L91 `from rl.follower import FollowerPolicy`；L92 `new_id = f"{agent_id}_ckpt"`；L93-97 与 `register_checkpoint` 同构地建快照并 `load_state_dict`；L98-100 `eval()` + 冻结梯度；L101 `add_agent(new_id, kind="historical", policy=snap, path=path, replace=True)`（**replace=True** ⇒ 复用槽位，Elo 保留）；L102 返回 new_id。
- 置信度: 已确认

#### M:League.record_elo_history  [L104-109]
- 类型: method
- 签名: `def record_elo_history(self, step: int = None):`
- 作用: 记录当前各成员的 (step, elo) 快照。
- 参数:
  - `step` (None): 非 None 时把 `total_steps` 抬到 `max(total_steps, int(step))`。
- 返回: 无
- 实现: L106-107 单调抬升 `total_steps`；L108-109 遍历 `self.elo.ratings.items()`，对每个 id 追加 `(self.total_steps, float(rating))` 到 `elo_history`（`setdefault` 惰性建列表）。
- 置信度: 已确认

#### M:League.record_round_stats  [L111-123]
- 类型: method
- 签名: `def record_round_stats(self, step, stats):`
- 作用: 记录一轮评估的聚合测量（供曲线画噪声误差棒）。
- 参数:
  - `step`: 评估步数。
  - `stats`: `{"est": {aid: [round_est, se]}, "games": {aid: n}}`。
- 返回: 无
- 实现: docstring（L112-118）说明 `round_est` 是轮内聚合估计（BT-lite，SE≈347.5/√n），与运行中逐局 Elo（有限记忆、1σ≈±40 饱和）区分；L119-123 把 `step`/`est`/`games` 规整为 `{int, {str: [float, float]}, {str: int}}` 后 append 到 `round_stats`。**缺字段不报错**（`.get(..., {})`）。
- 置信度: 已确认

#### M:League.add_exploiter  [L125-129]
- 类型: method
- 签名: `def add_exploiter(self, policy, path=None):`
- 作用: 用递增计数器新增一个 exploiter 成员。
- 参数:
  - `policy`: 策略对象。
  - `path` (None): 记录路径。
- 返回: `aid`（`f"exploiter_{self._exploiter_counter}"`，计数器随后 +1）。
- 实现: L126-127 拼 id 并递增；L128 `add_agent(aid, kind="exploiter", policy=policy, path=path)`；L129 返回。
- 置信度: 已确认

#### M:League.remove_agent  [L131-137]
- 类型: method
- 签名: `def remove_agent(self, agent_id):`
- 作用: 移除成员并清理其 Elo 与 PFSP 胜率键。
- 参数:
  - `agent_id`: 目标 id。
- 返回: `bool`（不存在返回 False，成功 True）。
- 实现: L132-133 不存在直接 False；L134 `agents.pop`；L135 `elo.ratings.pop`；L136 过滤 `pfsp.winrates`，丢弃键中含该 id 的条目（键是 `(a, b)` 元组，故用 `agent_id not in k` 子串判断）；L137 返回 True。另有类级别名 `retire = remove_agent`（L139）。
- 置信度: 已确认

#### M:League.elo_table  [L141-142]
- 类型: method
- 签名: `def elo_table(self):`
- 作用: 返回按 Elo 降序的评分表。
- 参数: 无
- 返回: `dict`（来自 `Elo.table()`，`rl/elo.py` L44-45：`dict(sorted(ratings.items(), key=lambda kv: -kv[1]))`）。
- 实现: 单行 `return self.elo.table()`（L142）。
- 置信度: 已确认

#### M:League.save_state  [L146-160]
- 类型: method
- 签名: `def save_state(self, path):`
- 作用: 把 Elo / PFSP / 成员元信息 / 历史 / 计数器 / 曲线数据写成 JSON。
- 参数:
  - `path`: 输出 JSON 路径。
- 返回: 无
- 实现: L147 函数内 `import json`；L148-158 组 state 字典：`ratings`（Elo 拷贝）、`winrates`（`(a,b)` 元组键扁平化为 `"a|b"`）、`agents`（仅 `agent_id`/`kind`/`path`，**不含策略本体**）、`history`、`exploiter_counter`、`ckpt_counter`、`elo_history`（元组→list）、`round_stats`、`total_steps`；L159-160 `json.dump(state, f)`，`encoding="utf-8"`（未设 `ensure_ascii=False`）。
- 置信度: 已确认

#### M:League.load_state  [L162-181]
- 类型: method
- 签名: `def load_state(self, path, policies=None):`
- 作用: 恢复联赛状态；可选用 `policies` 字典重新挂载策略本体。
- 参数:
  - `path`: JSON 路径。
  - `policies` (None): `{agent_id: policy}`，用于给恢复的成员挂策略对象。
- 返回: `self`。
- 实现: L164 `import json`；L165-166 读盘；L167 `elo.ratings` 全量替换为 float；L168-169 `winrates` 键按 `"|"` 拆回元组；L170-175 恢复 history / exploiter_counter / ckpt_counter / elo_history（list→tuple）/ round_stats / total_steps（缺字段用默认值）；L176 `policies or {}`；L177-180 遍历 `state["agents"]` 重建 `LeagueAgent`（`policy` 从 policies 取，取不到为 None；**`elo` 字段仍为默认 1500.0**）；L181 返回 self。**注意**：恢复前不清理 `self.agents`（旧成员会与新载入的合并）——照实记录。
- 置信度: 已确认

## F:src/clasher_new/rl/observation.py
- 行数: 172（`wc -l` 实测）
- 模块作用: 玩家视角观测构建 + 特权隐藏状态标签。观测约定与 `CREnv` 一致：grid (32,18,15)、hand (5,)、elixir (1,)，另加 next_card / time 供 follower 与信念模块使用。文件前半（L25-84）是全卡池**实体名词表 v2**（前 13 位为 v1 冻结位序）与类型别名表；后半是两个函数。
- 模块级 docstring 首行: `玩家视角观测构建 + 特权隐藏状态标签（信念模块监督用）。`
- 主要导入: os, sys, numpy(np)
- 关键模块级常量:
  - `ENTITY_NAMES`（L25-73）— 实体/卡名词表。前 13 位为 v1 冻结位序：`None, Knight, MiniPekka, Arrows, Minions, Archer, Musketeer, Fireball, Giant, King_PrincessTowers, KingTower, ArrowsSpell, FireballSpell`；其后为 v2 尾部追加的全卡池实体名（部署/死亡刷出/法术包装器/弹射物，L31-65）与卡名（手牌/hand 编码用，L70-72：`BarbLog, Clone, GlobalClone, GoblinBarrel, GoblinPartyRocket, Graveyard, Lightning, Log, MergeMaiden, Mirror, Rocket, Snowball, Vines`）。注释说明前 13 位语义由 `follower.load_checkpoint` 的 `entity_emb` 行兼容分支保留（L21-24）。
  - `ENTITY_SET = frozenset(ENTITY_NAMES)`（L74）— 词表的集合形式。
  - `CARD_TYPES = ["troop","character","spell","building"]`（L75）— 位序冻结（grid 通道 3 + one_hot num_classes=4）。
  - `_TYPE_ALIAS = {"area_effect": "spell", "projectile": "spell", "bomb": "spell"}`（L81）— 把引擎附加 type 折叠进 spell（注释 L76-80：扩 one_hot 类别数 4→6 会把 CNN 输入通道 26→28，破坏旧卷积权重）。
  - `GRID_H, GRID_W = 32, 18`（L83）、`GRID_C = 15`（L84）— 网格形状常量。
- 顶层数据表/字典: `_TYPE_ALIAS`（引擎 type → 4 类归并）；`ENTITY_NAMES` 是列表但起词表字典的索引作用（`ENTITY_NAMES.index(name)` 即 id）。

### S:observe  [L87-144]
- 类型: function
- 签名: `def observe(battle, player_id: int = 0) -> dict:`
- 作用: 返回玩家 `player_id` 的可见观测字典（grid 张量 + 手牌 id + 圣水 + 下一张 + 时间）。
- 参数:
  - `battle`: 引擎对局对象，须有 `entities`（dict，值含 `is_alive`/`name`/`data`/`position`/`hp`/`player`）、`players`（可索引取玩家，含 `cycle`/`elixir`）、`time`。
  - `player_id` (0): 观察者（0 或 1）。
- 返回: `dict`，键：`"grid"`（(32,18,15) float32）、`"hand"`（(5,) int32，`p.cycle[:5]` 的词表 id）、`"elixir"`（(1,) float32）、`"next_card"`（(1,) int32，`p.cycle[4]` 的 id）、`"time"`（(1,) float32）。
- 实现: L89 建零 grid；L90-130 遍历 `battle.entities.values()`：L91-92 跳过死亡实体；L93-94 名字不在 `ENTITY_NAMES` 的跳过（不可见）；L95 `entity_id = ENTITY_NAMES.index(each.name)`；L96 类型经 `_TYPE_ALIAS` 折叠，L99 不在 `CARD_TYPES` 时兜底 `character`（应对数据行缺 `tidType`）；L100 `is_opponent = each.player != player_id`（己方统一 0）；L105-118 用 `getattr` 逐字段补零读取（注释 L102-104：垫片实体只有最小属性集）：`elixir`、`is_air`、`attack_ground`、`attack_air`、`speed`、`hp_left = log(hp)/10`（hp≠0 时）、`max_hp`、`hp_percentage = hp/max_hp`、`hit_speed`、`range/3`、`sight_range/3`、`damage/200`、`projectile_damage = projectile_data.damage/200`；L120-123 取整坐标，`player_id == 1` 时镜像 `x = 17 - x`、`y = 31 - y`；L124-130 只在 `0<=x<18 且 0<=y<32` 时把 15 维 `obs_arr` 写进 `obs[y][x]`。L132-137 组装 hand（`p.cycle[:5]` 逐张查词表，查不到记 0；L137 `next_card` 同理取 `p.cycle[4]`；L141 圣水、L143 `battle.time`。
- 调用: 未被本组其他文件调用。被 follower/信念模块链路使用待确认（原因：未读 `rl/follower.py`、`rl/belief.py` 等）。
- 置信度: 已确认（实现与返回结构）

### S:hidden_labels  [L147-172]
- 类型: function
- 签名: `def hidden_labels(battle, player_id: int = 0) -> dict:`
- 作用: 返回特权隐藏状态标签（对手真实手牌/牌序/圣水/王冠/塔血 + 我方圣水/手牌 + 时间），只允许训练期使用。
- 参数:
  - `battle`: 同上。
  - `player_id` (0): 观察者；对手 = `1 - player_id`。
- 返回: `dict`，键：`opp_hand`（(4,) int32 = `opp.cycle[:4]`）、`opp_next`（标量 int32 = `opp.cycle[4]`）、`opp_cycle`（(8,) int32）、`opp_elixir`（(1,) float32）、`opp_crown`（(1,) int32 = `opp.get_crown_count()`）、`opp_towers`（(3,) float32 = `[king_tower_hp, left_tower_hp, right_tower_hp]`）、`my_elixir`（(1,) float32）、`my_hand`（(4,) int32 = `me.cycle[:4]`）、`time`（(1,) float32）。
- 实现: L152 `opp_id = 1 - player_id`；L153-154 取双方玩家对象；L156-157 内部闭包 `_ids(cards)` 把卡名列表逐张映射为词表 id（查不到记 0）；L160 `opp_hand = opp.cycle[:4]`（注释 L159：循环前 4 张 = 可出牌）；L161-172 组装返回字典。docstring（L148-151）明确"绝不进跟随者观测"。
- 调用: 未被本组其他文件调用；训练/信念监督链路使用待确认（原因：未读 `rl/follower.py`、`rl/belief.py` 等）。
- 置信度: 已确认（实现与返回结构）

---

## 附：对手池与脚本对手的采样逻辑（本组的核心机制）

> 以下全部为源码可查证的引用；涉及 `rl/train_solo.py`、`rl/config.py`、`rl/pfsp.py` 的行号来自同仓被 import / 直接相邻模块，仅用于确认调用关系。

### 1. 训练期对手池（`_OpponentPool`，`rl/train_solo.py` L321-477）
- **配比**：`rl/config.py` L94 `DEFAULT_OPP_MIX = {"frozen": 0.1, "hist": 0.6, "defend": 0.2, "rand_anchor": 0.1}`；`TrainConfig.opp_mix` 默认由它 `default_factory` 生成（`config.py` L224-225）。`train_solo.py` L145 的 `_OPP_MIX` 是同值兜底常量（cfg 缺失才用），L352 `self.mix = dict(getattr(cfg, "opp_mix", None) or _OPP_MIX)`。
- **hist 池收集（`_collect_hist_ckpts`，L175-221）**：只在目录里认 `solo_main_<step>.pt` 命名（L192-196，step 非整数跳过）；按 step 升序（L197）；若文件数 ≤ 预算则全取（L200-201），否则 `idx = np.linspace(0, len(steps)-1, budget).astype(int)` 均匀抽（L202-203，**含端点 0**）；主目录优先（L205），不足 `max_n` 时按 `extra_dirs` 传入顺序逐目录补，用 `os.path.abspath` 去重（L206-221）；目录不存在/为空返回空列表 ⇒ 对手池退化为 frozen+defend（L186、L392-397 打印实际归一化配比）。
- **每局采样（`_sample_kind`，L448-477）**：`r = self.rng.random()`；有 hist 时按 **累积区间**判定：`r < hist → hist`（L453-457），否则 `r -= hist`，再 `r < defend → defend`（L458-461），再 `r < defend + rand_anchor → rand_anchor`（L462-465），否则 `frozen`（L466-467）。
- **无 hist 时的回退**：`denom = 1 - mix["hist"]`，各槽概率按 `mix/denom` 重新归一（L468-477），即把 hist 的概率空间按比例分给其余三类（E2 修复旧实现的误分配，docstring L439-442）。
- **hist 内部再用 PFSP 二次采样**：`opp_id = self._pfsp.sample("main", list(self.hist_paths))`（L454），id 是「父目录名 + 文件名」的稳定映射（`_reindex_hist`，L403-412），避免池增长后下标错位。
- **PFSP 权重（`rl/pfsp.py` L53-64）**：`w = (1 - winrate)^beta`；**未采样过的对手按 0 胜率**（乐观先验）⇒ 权重为 1（L57-59）；门禁：`seen and gate_hi < 1.0 and winrate > gate_hi` 时 `w *= gate_penalty`（L61-62）；`max(w, 1e-6)` 防零（L63）。`sample` 做 `p = w / w.sum()` 后 `rng.choices(..., k=1)`（L66-71）。训练侧显式传入 `alpha=0.20 / gate_hi=0.85 / gate_penalty=0.2`（`train_solo.py` L152-154 常量，L356-362 构造 `_PFSP`）；**`rl/pfsp.py` 自身的默认值仍是 `alpha=0.05 / gate_hi=1.0 / gate_penalty=1.0`**（`pfsp.py` L30-31）⇒ 默认等于旧行为、门禁关闭。
- **胜率回填（`record`，L479-485）**：只有 `_last_kind == "hist"` 且 `_last_hist_id` 非空时才 `_pfsp.update_winrate("main", hist_id, score)`，`score = {0: 1.0, 1: 0.0}.get(winner, 0.5)`（平局 0.5）；frozen/defend/rand_anchor 局**不更新 PFSP**。
- **刷新节奏**：`refresh_hist(step)`（L414-434）重扫磁盘把本 run 新快照纳入 hist 池，按副本同步间隔触发（打印文案 L400 称每 `solo_copy_every` 步；具体调用点在 `train_solo.py` 更后段，未读）。
- **rand_anchor**：`mix["rand_anchor"] > 0` 时构造固定随机权重锚点（L373-382，`belief_dim` 由 `BeliefInference(opp_deck=env.deck1, n_particles=128, seed=0).encode(None, None)` 的长度决定，`deterministic=True`），永不参与训练/同步/PFSP（L329-332）。

### 2. 脚本对手（本组 `opponents.py`）
- **`ScriptedPolicy`**：mode 白名单只有 `"random"` / `"heuristic"`，二者实为同一套掩码随机采样（docstring L5；实现 L105-116 不区分 mode）。卡组来源三分支：`deck_pool` 随机抽一副 > `pool` 每局重采样 8 张 > `None`（引擎默认）。**注意 `ScriptedPolicy(..., pool=[])` 会被 `__init__` 转成 None**（L90 用真值判断）。
- **`SelfDefenderPolicy`**：每帧以 `passive_prob`（默认 0.6）概率"停手"，才有机会走 `_random_action`；否则走 `_defend_action`（`play` L196-199 的短路顺序：先随机、后防守）。反制候选来自纯函数 `script_defender(sim, defender_id)`（`simulate_exchange.py` L80-89），落点由 `script_defender` 给**世界坐标**，本类做世界→本地网格逆变换（player 1：`(17-wx, 31-wy)`，与 `observation.py` L121-123 的镜像一致）；且必须满足 `p.can_play_card(card)` 与 `card in p.cycle[:4]`（L163-167）。
- **两者在训练对手池里的角色差别**：`_OpponentPool` 的 "defend" 槽是 `SelfDefenderPolicy`（`train_solo.py` L338、L346-347，`seed=cfg.seed + 7`，four 卡组时传 `deck_pool`）；`ScriptedPolicy` 在 `train_solo.py` 的对手池代码中**未出现**（L321-477 无引用）⇒ 它在本 run 的对手分布采样中不参与。**`ScriptedPolicy(mode="heuristic")` 不是会防守的对手**（实为掩码随机）——这一点由本文件 docstring（L5）与实现（L105-116）双重确认。
- **另一条独立的采样路径**：`League.sample_opponent`（`league.py` L52-57）用同一个 `PFSP.sample` 在**联赛成员表**（`self.agents`）上采样，与训练侧 `_OpponentPool` 的 hist 池是两套互不相干的机制（前者入参是 agent id 列表，后者入参是 checkpoint 文件路径列表）。

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| src/clasher_new/rl/opponents.py | 14 | 14 |
| src/clasher_new/rl/plan_space.py | 7 | 7 |
| src/clasher_new/rl/bayes_filter.py | 13 | 13 |
| src/clasher_new/rl/league.py | 15 | 15 |
| src/clasher_new/rl/observation.py | 2 | 2 |

COVERAGE_OK G043 files=5 symbols=51
