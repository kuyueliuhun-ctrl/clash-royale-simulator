# 组 G031 文件级分析

## F:scripts/probe_v3_mono_check.py
- 行数: 197
- 模块作用: 只读诊断脚本（自称"跑后追加，非预注册"），用于回答"v3 阶梯的 α 选择是否让层间比较失去意义"。它加载一个 npz（内含多层的 `X_<层名>` 特征矩阵、`y`、`ep`），做三件事：§1 逐位验证 `fused` 的前若干列是否就是 `grid_ln_out`（超集关系）；§2 打印每层在整条 α 网格上的 test/val `EV_within` 曲线；§3 把所有层放在同一批 α（`COMMON_ALPHAS`）上做可比阶梯，另加 §3b"每层自己的 val 最优 α"与 §4 复算判定（L1-15 模块 docstring）。
- 模块级 docstring 首行: `只读诊断（**跑后追加，非预注册**）：v3 阶梯的 \`α\` 选择是否让层间比较失去意义？`
- 主要导入: argparse, os, sys, numpy, pomdp_ceiling_probe(ev_within, standardize), probe_value_ln(ALPHAS_V2, GRID_FLAT)
- 关键模块级常量:
  - `COMMON_ALPHAS = (1e-2, 1e-1, 1e0, 1e1, 1e2, 1e3, 1e4, 1e5, 1e6)` — §3 共同 α 阶梯要打印的点（L37-38）
  - `LAYERS = ("raw_obs", "plan_f", "cnn_pre_ln", "grid_ln_out", "fused", "enc", "pre_ln")` — `--layers` 的默认层清单（L39）
  - `_HERE` / `_SRC`（L26-27）— 脚本目录与 `../src/clasher_new`，被插入 `sys.path`（L28-30）
- 顶层数据表/字典: 无（`X` 字典在 `main` 内动态构造）
- 备注（源码依据）: `GRID_FLAT` 在第 35 行被 import，但全文仅出现这一次（grep 计数 = 1）⇒ **导入后未使用**。`ALPHAS_V2` 定义在 `scripts/probe_value_ln.py:64`，是 14 个元素的元组 `(1e-6, 1e-4, 1e-3, 1e-2, 1e-1, 1e0, 1e1, 1e2, 1e3, 1e4, 1e5, 1e6, 1e7, 1e8)`（已读该文件确认）。`standardize(Xtr, Xs)`（`scripts/pomdp_ceiling_probe.py:337-341`）按训练集均值/标准差（`sd<1e-8` 置 1）返回 `[(x-mu)/sd for x in Xs]`；`ev_within(pred, y, ep)`（同文件 L202-213）做**逐局中心化**后返回 `1 - MSE/Var`，方差 ≤1e-12 时返回 `None`（已读该文件确认）。

### S:split_masks  [L42-53]
- 类型: function
- 签名: `def split_masks(ep, seed, test_frac=0.25, val_frac=0.15):`
- 作用: 以**局（episode）为单位**把样本切成 train/val/test 三个布尔掩码，避免逐帧随机切分。
- 参数:
  - `ep` (ndarray): 每帧所属局编号。
  - `seed` (int): 传给 `np.random.default_rng` 的随机种子。
  - `test_frac` (float, 默认 0.25): 测试局比例。
  - `val_frac` (float, 默认 0.15): 验证局比例。
- 返回: `(m_tr, m_va, m_te)` 三个与 `ep` 等长的布尔 ndarray。
- 实现: L43 建 `rng`；L44 `np.unique(ep)` 取全部局号；L45 原地 `rng.shuffle(eps)`；L46-47 用 `max(1, int(round(len(eps)*frac)))` 算测试/验证局数（下限 1，保证小数据也能切）；L48-49 取前 `n_test` 个为测试局、紧随其后的 `n_val` 个为验证局；L50-52 用列表推导生成布尔掩码，`m_tr = ~(m_te | m_va)`（训练集是剩余部分，非前段）。
- 调用: `main` 在 L106 调用 `split_masks(ep, a.seed)`；与 `scripts/pomdp_ceiling_probe.py` 同名工具同构（未在该文件内确认同名函数，只确认 `ev_within`/`standardize`）。它调用了 `numpy.random.default_rng`、`numpy.unique`。
- 置信度: 已确认

### S:alpha_curve  [L56-77]
- 类型: function
- 签名: `def alpha_curve(Xl, y, ep, m_tr, m_va, m_te, ep_va, alphas=ALPHAS_V2):`
- 作用: 对单层特征一次性解整条 Ridge α 路径，并返回每个 α 对应的 test 与 val 的局内中心化 EV。
- 参数:
  - `Xl` (ndarray): 该层特征矩阵（帧 × 维）。
  - `y` (ndarray): 已逐局中心化的回报目标（`main` L108-111 先做过中心化）。
  - `ep` (ndarray): 每帧局号（测试集的 EV 需要它）。
  - `m_tr` / `m_va` / `m_te` (bool ndarray): 三个切分掩码。
  - `ep_va` (ndarray): 验证集对应的局号序列（`ep[m_va]`）。
  - `alphas` (tuple, 默认 `ALPHAS_V2`): 待扫的 α 网格。
- 返回: `(out, dim)`；`out` 是 `{α: (test_EV_within, val_EV_within)}`（注意：函数 docstring L57 写的是 `{α: (test_EV_within, val_EV_within, dim)}`，与实际返回的二元组**不一致**，dim 是单独第二个返回值），`dim` 是保留的列数（int）。
- 实现: L58 转 float64；L59-60 用训练集标准差筛列（`sd_tr > 1e-8`）；L61-62 若无列存活则返回"全 0 曲线 + dim=0"；L63-64 用 `standardize` 以训练集统计量标准化 train/val/test 三块；L65-66 取训练目标的 `min`/`max`/`mean`（裁剪区间与中心）；L67-68 先算 `XtX`、`Xty`（目标已减均值）；L69 建单位阵；L71-76 对每个 α 解 `(XtX + αI)w = Xty`，把 val/test 预测 `clip` 回训练目标区间后各自调用 `ev_within` 求 EV。
- 调用: `main` 在 L137 对每层调用；它调用了 `standardize`、`ev_within`（外部模块，均已核对定义）与 `np.linalg.solve`。
- 置信度: 已确认

### S:main  [L80-193]
- 类型: function
- 签名: `def main():`
- 作用: 命令行入口：载入 npz → 切分 → §1 超集逐位校验 → §2 每层 α 曲线 → §3 共同 α 阶梯 → §3b 各层自选 α 与 ρ → §4 单行判定。
- 参数: 无（参数从 `argparse` 取）：`--npz`(必填), `--seed`(int, 默认 7), `--exclude`(默认 `"grid_x"`), `--layers`(nargs="*", 默认 `LAYERS`), `--raw-obs-layer`(默认 `"raw_obs"`，作为共同 α 的参考层)（L81-88）。
- 返回: 整数 `0`（L193）。
- 实现: L90 解析逗号分隔的 `--exclude`；L91 `np.load`；L92-100 遍历 `z.files`，只取以 `"X_"` 开头的键，去掉前缀作为层名（`k.split("_", 1)[1]` ⇒ `X_grid_ln_out` → `grid_ln_out`），跳过被排除的，统一转 float32；L101-102 读 `y`(float64) 与 `ep`；L103 打印帧数/局数/层名。L106 切分，L107 `ep_va`，L108-111 **逐局减去该局 `y` 均值**（局内中心化），L112-113 打印切分规模与 `Var(R)_within`。§1（L116-128）：若 `fused` 与 `grid_ln_out` 都在，取 `fused[:, :grid.shape[1]]` 与 `grid_ln_out` 做 `np.array_equal` 与 `max|Δ|`，并据此打印"超集关系成立／仪器有问题"；缺则打印 skip 并把 `eq` 置 `None`（`eq` 此后未再使用）。§2（L131-145）：对每个请求层调用 `alpha_curve`，按 **val** EV 取 argmax 作为"选中的 α"，再按 `ALPHAS_V2` 原序打印 test/val 两列（`None` 打印为 `0.0`）。§3（L148-166）：以参考层 `raw_obs` 的 val-最优 α 作为 `ref_alpha`，再按 `COMMON_ALPHAS` 逐行打印各层在该同一 α 下的 test EV（同列可比）。§3b（L168-181）：逐层打印各自 val 最优 α 的 test EV 与 `ρ = 该层 test / 参考层 test`（仅当参考层 test > 0 时给出，否则 `—`）。§4（L184-192）：在**同一** `ref_alpha` 下比较 `fused` 与 `grid_ln_out` 的 test EV，若差值 ≥ −1e-6 打印"超集不劣 ⇒ 层间落差是 α 选择造成的仪器假象"，否则打印"同一 α 下 fused 仍劣于其子块 ⇒ 非单纯 α 选择问题"。
- 调用: 由文件末尾 L196-197 的 `if __name__ == "__main__": main()` 调用；它调用了 `split_masks`、`alpha_curve`、`np.load`、`np.array_equal`、`np.linalg.solve`（经 `alpha_curve`）。被谁调用：待确认（脚本入口，未在本次允许范围内检索其它调用点）。
- 置信度: 已确认

## F:scripts/diag_gru_ablation.py
- 行数: 192
- 模块作用: GRU 饱和归因实验脚本。它载入一个 checkpoint，跑若干局镜像自对弈并逐帧收集 `enc`（编码器输出）与 GAE return，然后在三种输入变体（原始 enc / 去均值 enc / 标准化 enc）下各做一次"单步 GRU + 线性探针 + value_head"对照，用以判定"信息是被量级压死的"而非"输入里没有信息"（L1-15 模块 docstring）。
- 模块级 docstring 首行: `GRU 饱和归因实验（2026-09-11 §4 诊断第四步）。`
- 主要导入: argparse, json, os, sys, numpy, torch, rl.config(TrainConfig, reward_to_env), rl.belief(BeliefInference), rl.belief_planner(BeliefPlanner), rl.plan_space(PLAN_DIM), rl.follower(FollowerPolicy, load_checkpoint), rl.ppo(PPOTrainer), rl.train_follower(FollowerOpponent), rl.run_league(timeout_winner, overtime_open, _stall_probe, STALL_WINDOW), rl.train_solo(solo_env, _draw_penalty, resolve_deck_set)
- 关键模块级常量:
  - `_HERE` / `_SRC`（L22-23）— 脚本目录与 `../src/clasher_new`，仅 `_SRC` 被插入 `sys.path`（L24-25）
  - 本文件未自定义数值常量；用到的外部常量：`STALL_WINDOW = 10`（`rl/run_league.py:145`）、`PLAN_DIM = int(len(PlanToken().to_vector()))`（`rl/plan_space.py:187`）— 均为已读源码确认。
- 顶层数据表/字典: 无
- 备注（源码依据）: `W = pol.gru_cell.weight_hh...`（L164）赋值后**全文未再被引用**（grep 确认仅此一处）；L147 的 `adv` 解包后同样未被使用。
- 外部符号签名核对（均已读源确认）: `load_checkpoint(path, hidden_dim=None, plan_dim=None, belief_dim=None, ...)`（`rl/follower.py:67`）；`FollowerPolicy.__init__(self, hidden=256, plan_dim=None, belief_dim=None, num_entity=NUM_ENTITY, ...)`（`rl/follower.py:155`）；`_encode_parts(self, obs, belief_token, plan_token)`（L268）；`_value_from(self, enc, h, fused)`（L303）；`PPOTrainer.compute_gae(rewards, values, dones, gamma=0.99, lam=0.95, truncated=None, last_value=0.0)` 为 `@staticmethod`（`rl/ppo.py:167-168`）；`FollowerOpponent.__init__(self, policy, env, belief=None, planner=None, deterministic=True, use_plan_biases=False)`（`rl/train_follower.py:52,61`）；`BeliefInference.__init__(self, opp_deck, use_rule=True, use_stat=True, neural=None, n_particles=128, seed=0)`（`rl/belief.py:261`），`encode(self, obs=None, opp_played=None)`（L344），`reset(self, opp_deck=None)`（L274），`state(self)`（L318），`update(self, obs, opp_played, opp_x=None, opp_card_type=None, elixir_est=None)`（L300）；`BeliefPlanner.plan(self, battle, belief, obs=None)`（`rl/belief_planner.py:772`）；`solo_env(cfg, seed, deck0=None, deck1=None)`（`rl/train_solo.py:157`）；`resolve_deck_set(deck_set)`（L107）；`_draw_penalty(cfg)`（L164）；`timeout_winner(battle, hp_tiebreak=None)`（`rl/run_league.py:179`）；`_stall_probe(env, last_hp, stall_count)`（L252）；`overtime_open` 由 `rl.overtime` 导入（`rl/run_league.py:66`）。

### S:ev  [L41-46]
- 类型: function
- 签名: `def ev(v, r):`
- 作用: 计算**不分局**的线性 EV（解释方差）：`1 - MSE/Var(R)`。
- 参数:
  - `v`: 预测值序列（转 float64）。
  - `r`: 目标回报序列（转 float64）。
- 返回: float；当 `Var(r) <= 1e-12` 时返回 `float("nan")`（L45）。
- 实现: L42 转 float64；L43 取 `r.var()`；L44-45 方差过小直接返回 NaN（避免除零）；L46 返回 `1 - mean((v-r)^2)/var`。注意此函数**不做局内中心化**，与本文件另一处探针口径不同。
- 调用: `ridge_ev`（L68）与 `main` 打印表中 `vhead_EV`（L181）调用它；它不调用其它自定义函数。
- 置信度: 已确认

### S:ridge_ev  [L49-68]
- 类型: function
- 签名: `def ridge_ev(H, R, EP, lam=1e-2):`
- 作用: 按局 70/30 切分的线性（Ridge）探针，只在**测试局**上算 EV，测量隐藏状态 H 中"线性可读出"的回报信息。
- 参数:
  - `H` (ndarray): 特征（此处为 GRU 隐状态），帧 × 维。
  - `R` (ndarray): 每帧回报。
  - `EP` (ndarray): 每帧局号。
  - `lam` (float, 默认 1e-2): Ridge 正则强度。
- 返回: float，测试集上的 `ev(Xte @ w, R[m_te])`。
- 实现: L55-56 转数组；L56 `np.unique(EP)` 取局号；L57 用固定 `np.random.RandomState(0)`（保证可复算）；L58 原地打乱局号；L59 `cut = max(1, int(len(gs)*0.7))`；L60-62 生成 train/test 布尔掩码（`np.isin`）；L63 用**训练集**均值/标准差标准化（`+1e-6` 防零）；L64-65 追加全 1 列作为截距；L66-67 解 `(XᵀX + lam·I)w = XᵀR`；L68 返回测试集 EV。docstring（L50-54）声明这样做的理由是避免"训练=测试"过拟合假高分。
- 调用: `main` 在 L181 的表格里调用；它调用了 `ev`。
- 置信度: 已确认

### S:main  [L71-188]
- 类型: function
- 签名: `def main():`
- 作用: 命令行入口：加载 ckpt 与配置 → 跑 `--games` 局镜像自对弈收集 enc/value/reward → 构造 A/B/C 三种 enc 变体 → 打印变体对照表与"h 跨局标准差"表。
- 参数: 无（`argparse`）：`--ckpt`(必填), `--run-dir`(默认 `runs/prod_200k_valnorm_ev`), `--games`(int, 默认 8), `--seed`(int, 默认 999), `--device`(默认 `cuda`)（L72-78）。
- 返回: 无（函数体末尾没有 `return`，靠隐式 `None`）。
- 实现: L80-88 取 `TrainConfig.resolve("economy")` 后，若 `run_dir/config.json` 存在则逐键 `setattr`（仅当 `hasattr(cfg, k)`，异常被吞掉）；L89 设备回退（非 cpu 且无 CUDA 时降级 `"cpu"`）。L90-91 建 `env0` 仅为推算 belief 维度：`len(BeliefInference(opp_deck=env0.deck1, n_particles=128, seed=0).encode(None, None))`。L92-93 载入策略并搬到设备。L95-96 取镜像卡组并建镜像 `env`。L97-102 建 `BeliefPlanner`，并构造与 ckpt **同 value 架构**的 `FollowerPolicy` 作对手（显式读 `pol` 的 `value_bypass` / `value_independent`，注释 L98 说明否则键集不匹配），再 `load_state_dict(pol.state_dict())` 复制权重。L104-137 主循环：每局重设 `env.opponent` 为**确定性** `FollowerOpponent`，另建独立 `belief` 并 `reset`/`env.reset(seed=a.seed+2000+g)`；内层 `while not done and (steps < cfg.max_ep_steps or overtime_open(env.battle))`，按 `STALL_WINDOW` 调 `_stall_probe` 做僵局早停（仅当 `cfg.train_stall_stop`），随后 `bp.plan(...)` → `belief.encode(obs, None)` → `pol._encode_parts(...)` 得 `fused, enc` → 单步 `pol.gru_cell(enc, 零初始或 hidden.detach())` → 记录 enc 与 `pol._value_from(enc, h, fused)`（注释 L128 强调走真实 value 通路）→ `pol.act(..., deterministic=False)` 取动作 → `env.step` → 记录 reward/term/trunc → `belief.update(obs, info.get("opp_played"))`。L138-146 若对局未正常结束而循环退出且有 reward，用 `timeout_winner` 做到期裁决并把 `win_bonus`/`lose_penalty`/`_draw_penalty` 补到最后一步 reward。L147-149 用 `compute_gae` 算 adv/ret（`truncated=ep_trunc`、`last_value=0.0`），只把 `ret` 收进 `R_list`。L153-155 转数组并打印帧数/局数/`Var(R)`/`E[R]`。L157-163 构造三变体（A 原始、B 去均值、C 标准化）。L164 取出 `weight_hh`（未使用）。L168-181 对每个变体：**逐帧以零初始 hidden 单步**过 `gru_cell` 得 H（即不含跨帧递归），再算 `vh = H @ value_head.weight + bias`（注意此处**硬编码** `value_head` 线性头，与 L128 走 `_value_from` 的口径不同），打印 `||enc||`（取第 0 帧范数）、`h 跨帧 std`、`h_norm`、`ridge_ev(H,R,EP)`、`ev(vh,R)`。L184-188 第二次遍历 results，按局取均值后打印"跨局 per-dim std mean"与"局均差向量的 std"。
- 调用: 由 L191-192 `if __name__ == "__main__": main()` 调用；它调用了 `ev`、`ridge_ev`、`TrainConfig.resolve`、`reward_to_env`、`BeliefInference`、`BeliefPlanner`、`load_checkpoint`、`FollowerPolicy`、`resolve_deck_set`、`solo_env`、`FollowerOpponent`、`overtime_open`、`_stall_probe`、`timeout_winner`、`_draw_penalty`、`PPOTrainer.compute_gae`、`pol._encode_parts`、`pol._value_from`、`pol.act`、`pol.to_device`。被谁调用：待确认（脚本入口）。
- 置信度: 已确认（外部被调符号的签名已逐一读源核对）

## F:scripts/forensics_response.py
- 行数: 187
- 模块作用: 回放取证脚本 v2：读若干 `league_*.pkl` 回放，统计我方（P0）每次 deploy 是"过河敌军在场时的防守响应 / 对手 5s 内出牌后的响应 / 单边堆牌"，并给出威胁→首次响应延迟、落点到最近过河敌军的曼哈顿距离、拦截口径与接敌口径四项统计（L2-16 模块 docstring 说明世界坐标口径与分类语义）。
- 模块级 docstring 首行: `取证 v2：单边堆牌 vs 对牌响应率（2026-09-08，坐标系已修正）。`
- 主要导入: pickle, sys, io, glob, collections.Counter
- 关键模块级常量:
  - `RIVER = 16.0`（L22）— 河道 y 阈值；P1 部队 y < 16 视为"已过河进我方半场"
  - `RESP_WINDOW = 5.0`（L23）— "对手出牌后 5 秒内"的响应窗口（秒）
- 顶层数据表/字典: 无
- 备注（模块级副作用与死代码，源码依据）: L20 在 import 期就把 `sys.stdout` 换成 UTF-8 包装的 `io.TextIOWrapper`；L58-59 有一段 `foes_crossed = ... if False else ... if False else False` 的三元链，其结果恒为 `False`，且在第 65 行被立即重新赋值覆盖 ⇒ 是坐标系修正后残留的**死代码**。

### S:load_games  [L26-32]
- 类型: function
- 签名: `def load_games(pattern):`
- 作用: 按 glob 模式批量读取回放 pkl，并把每个文件里的 `rep["games"]` 列表拼接成一个局列表。
- 参数:
  - `pattern` (str): glob 模式（如 `runs/.../league_*.pkl`）。
- 返回: list，所有回放文件里 `games` 的并集（顺序按 `sorted(glob.glob(...))`）。
- 实现: L27 空列表；L28 `sorted(glob.glob(pattern))` 排序保证顺序确定；L29-30 二进制打开并 `pickle.load`；L31 `games.extend(rep["games"])`；L32 返回。
- 调用: L184 与 L186（`__main__` 块）调用它两次；它调用了 `glob.glob`、`pickle.load`。
- 置信度: 已确认（未读取任何 pkl 文件内容，故 `rep` 的键结构仅依据 L31 的取用方式）

### S:troop_list  [L35-36]
- 类型: function
- 签名: `def troop_list(fr):`
- 作用: 从一帧的 `entities` 里抽出所有"troop"实体的世界坐标 `(x, y)`。
- 参数:
  - `fr` (dict): 单帧记录；使用 `fr["entities"]`，每条实体索引 `e[1]`=x、`e[2]`=y、`e[5]`=类型字符串。
- 返回: `[(x, y), ...]`（float 元组列表，**不分玩家**）。
- 实现: 单行列表推导（L36）：过滤 `e[5] == "troop"`，把 `e[1]`/`e[2]` 转 float 成对返回。注意它不含玩家过滤，因此 `classify_games` 里的过河判定另起炉灶（L61-66）而没有使用本函数。
- 调用: 在 `classify_games` L57 被调用一次（结果 `tp` 之后未被用于判定）；它只调用内建 `float`。
- 置信度: 已确认

### S:classify_games  [L39-177]
- 类型: function
- 签名: `def classify_games(games, label):`
- 作用: 对一批回放逐帧扫描，统计我方与对手 deploy 的三类归属，并打印响应延迟/落点距离/拦截率/接敌率等取证指标。
- 参数:
  - `games` (list): 局记录列表，每局含 `frames`，每帧含 `t`、`entities`、`opp_played`、可选 `bundle`。
  - `label` (str): 打印用的批次名称。
- 返回: 无返回值（只打印）。
- 实现: L40-49 初始化计数器与统计列表：`my_cls`/`opp_cls` 三个键的 Counter、双方 deploy 总数、`def_frames_my`（敌军过河帧数）、`def_deploy_my`（其中我方有 deploy 的帧数）、`latencies`、`dist_near`、`intercepts`、`engagements`。L50-132 主循环（逐局 → 逐帧）：L56-57 取时间与 `troop_list`（`tp` 之后未用）；L58-59 死代码；L61-64 分别按 `int(e[4])` 抽 P1/P0 troop 世界坐标；L65-66 判 `foes_crossed`（P1 有 y<16）与 `my_crossed`（P0 有 y>16）。L67-76 对手出牌处理：先把 `last_opp_play_t` 置为当前帧时间 `t`，再对 `fr["opp_played"]` 里每次出牌计数并分类；由于 L68 刚把窗口起点刷成 `t`，L73 的 `t - last_opp_play_t <= RESP_WINDOW` 恒为真 ⇒ **按源码，对手侧只会落到 `response_defense` 或 `response_session`，`unilateral` 分支不可达**。L77-80 取我方 deploy 列表（`b[0] == "deploy"`）并累加总数；L81-124 分类：有敌军过河时记 `response_defense`，并对每次 deploy 依次 L84 把本地网格坐标换成世界坐标（`+0.5`）、L85-86 **跳过 `b[3] >= 20` 的落点**（注释称其为"幽灵动作：非法尝试，引擎未执行"，不进空间统计，但该 deploy 仍已被计入分类计数）、L87-120 在 `p1_troops` 非空时：L88-89 记录落点到最近 P1 troop 的曼哈顿距离；L92-104 接敌口径——向后扫最多 8 秒，若部署点曼哈顿 5 格内同时出现 P0 与 P1 troop 则记 1；L108-120 拦截口径——取最近过河敌军与近似塔前缘 `(8.5, 6.0)` 连成的线段，把落点投影到线段上，曼哈顿距离 ≤4 记 1；L121-124 无过河敌军时按 5s 窗口记 `response_session` 或 `unilateral`。L125-128 累加过河帧数与"其中有 deploy"的帧数；L129-132 维护 `in_threat`/`threat_start` 状态机（第一遍的这套状态**在 L134-135 被重置**，因此第一遍只用于威胁计数）。L133-147 第二遍逐局扫描计算响应延迟：重新判定过河开始/结束，在威胁区间内遇到含 deploy 的 `bundle` 就记 `fr["t"] - threat_start` 并立即把 `in_threat` 置 False（注释 L147：只记区间首个响应）。L149-177 打印：L149-153 我方三类计数与百分比（分母 `sum(my_cls.values()) or 1`）；L154-156 对手三类同法；L157-158 过河帧数与防守投入率；L159-163 延迟的中位数/p25/p75（用排序后下标 `n//2`、`n//4`、`3*n//4` 取）；L164-169 落点距离的中位数/分位与 `>8格` 占比；L170-173 拦截率；L174-177 接敌率。
- 调用: 由 `__main__` 块 L184-187 调用两次（一次当前 9j 回放、一次历史无闸门 100k 回放）。它调用了 `troop_list`、`Counter`、内建 `any`/`min`/`sorted`/`print`；不调用其它自定义模块。
- 置信度: 已确认（分类判定的全部依据均来自源码行；但 pkl 内部字段（`t`/`entities`/`opp_played`/`bundle` 的实际结构）未读取任何 pkl 文件，仅按代码取用方式描述）

## F:scripts/judge_critic_inertia.py
- 行数: 185
- 模块作用: critic 惰性检验的判读器：从**原始训练日志**正则抽取 `[solo advinert ...]` 诊断行，脚本复算各统计量的中位数（声明不信任 `run_state.json` 的汇总），按写死的预注册阈值 P1/P2/P3 输出判决，并额外给出（描述性的、不判决的）锚点序列与基线三跑对比。
- 模块级 docstring 首行: `critic 惰性检验判读器（预注册 docs/critic_inertia_prereg_2026-09-13.md）。`
- 主要导入: argparse, json, os, re, sys
- 关键模块级常量:
  - `P1_COS = 0.99`（L27）— P1 判据：`grad_cos` 中位 ≥ 0.99
  - `P1_RESID = 0.10`（L28）— P1 判据：`resid_frac_norm` 中位 ≤ 0.10
  - `P2_COS = 0.95`（L29）— P2 判据：`grad_cos` 中位 ≤ 0.95
  - `P2_RESID = 0.30`（L30）— P2 判据：`resid_frac_norm` 中位 ≥ 0.30
  - `LEVEL_NOTE = 0.10`（L31）— `level_shift_norm` 中位 > 0.10 则提示 Layer 2 归因受限
  - `_LINE`（L33-37）— 匹配 `[solo advinert <step>] corr= resid= resid_norm= lvl_shift= level_gap= grad_cos= gnorm_ratio=` 的具名分组正则
  - `_BASE_RUNS = ["d1_league_20k", "d1_league_20k_r2", "d1_league_20k_r3"]`（L40）— 次级对比所用的基线三跑
  - `_HERE` / `_SRC`（L23-24）— 用于 `--runs-root` 默认值
- 顶层数据表/字典: 无（`_BASE_RUNS` 为列表，见上）
- 备注（模块级副作用）: L18-21 尝试把 `sys.stdout` 重配为 UTF-8 且 `errors="replace"`，异常被吞掉。

### S:_median  [L43-48]
- 类型: function
- 签名: `def _median(v):`
- 作用: 过滤掉 `None` 后求中位数。
- 参数:
  - `v` (可迭代): 数值序列，允许含 `None`。
- 返回: float（偶数个取中间两个的平均）；空序列返回 `None`。
- 实现: L44 排序并剔除 `None`；L45-46 空则返回 `None`；L47-48 奇数取 `v[n//2]`，偶数取 `0.5*(v[n//2-1]+v[n//2])`。
- 调用: `main` 中多处调用（L129-138、L140、L153-155）；不调用其它自定义函数。
- 置信度: 已确认

### S:_fmt  [L51-52]
- 类型: function
- 签名: `def _fmt(x, nd=4):`
- 作用: 把数值按固定小数位格式化，`None` 直出字符串 `"None"`。
- 参数:
  - `x` (float 或 None): 待格式化值。
  - `nd` (int, 默认 4): 小数位数。
- 返回: str。
- 实现: 单行三元表达式（L52）：`None` → `"None"`，否则 f-string 定点格式。
- 调用: `main` 的汇总/判决打印处（L129-138、L149-155）。
- 置信度: 已确认

### S:parse_log  [L55-63]
- 类型: function
- 签名: `def parse_log(path):`
- 作用: 逐行扫日志，把所有匹配 `_LINE` 的诊断行解析成字典列表。
- 参数:
  - `path` (str): 日志文件路径。
- 返回: `list[dict]`；每项含 `step`(int) 与 `corr`/`resid`/`resid_norm`/`lvl`/`gap`/`cos`/`gr`(float)。
- 实现: L57-58 以 UTF-8、`errors="replace"` 打开并逐行；L59 用 `_LINE.search` 匹配；L60-62 只在命中时把 `groupdict()` 转为数值——`step` 用 `int`，其余一律 `float`；L63 返回。
- 调用: `main` 在 L102 调用；它调用了 `_LINE.search`。
- 置信度: 已确认

### S:anchor_series  [L66-74]
- 类型: function
- 签名: `def anchor_series(run_dir):`
- 作用: 从 run 目录的 `solo_state.json` 里读出 `vs == "baseline_rand"` 的锚点历史序列。
- 参数:
  - `run_dir` (str): run 目录（含 `solo_state.json`）。
- 返回: `[(step, winrate), ...]`（`step` 为 int，`winrate` 为 float）；文件不存在时返回 `[]`。
- 实现: L67-69 拼路径并做存在性判断（不存在直接返回空列表）；L70-71 读 JSON；L72-74 从 `st["_controls_history"]`（缺失时用 `{}` 兜底）里筛 `c["vs"] == "baseline_rand"` 的项并转成元组列表。
- 调用: `main` 在 L158（本 run）与 L169（基线三跑）调用。
- 置信度: 已确认（`solo_state.json` 的实际字段结构未打开文件验证，仅依据本函数取用方式）

### S:block_worst  [L77-92]
- 类型: function
- 签名: `def block_worst(series, n_blocks=5):`
- 作用: 把锚点序列按**序号**近等分成若干块，取每块最差（最小）胜率。
- 参数:
  - `series` (list): `(step, winrate)` 序列。
  - `n_blocks` (int, 默认 5): 期望块数。
- 返回: list[float]，每块的最小 winrate；空输入返回 `[]`。
- 实现: L80-81 空序列返回空；L82-83 `k = min(n_blocks, n)`；L84 `divmod(n, k)` 得基础块长与余数；L85-91 逐块切分，前 `rem` 块各多 1 个元素（`size = base + (1 if b < rem else 0)`），对非空块 `min(w for _, w in chunk)`；L92 返回。docstring L78-79 明确与 `scripts/judge_anchor_blocks.py` 同口径：**近等分块，块数 = min(n_blocks, 点数)，不是固定每块 9 点**。
- 调用: `main` 在 L166 打印本 run 的块 worst；它只调用内建 `min`。
- 置信度: 已确认

### S:main  [L95-181]
- 类型: function
- 签名: `def main():`
- 作用: 命令行入口：解析日志 → 打印逐点序列与汇总 → 按预注册阈值判决 → 打印次级（描述性）锚点对比。
- 参数: 无（`argparse`）：`--log`(必填), `--run`(必填，探针 run 目录，相对 `src/clasher_new`), `--runs-root`(默认 `os.path.join(_SRC, "runs")`)（L96-100）。
- 返回: int；无诊断行时返回 `2`（L107-109），否则返回 `0`（L181）。文件末尾用 `sys.exit(main())`（L185）。
- 实现: L102 解析日志；L103-106 打印表头与日志路径；L107-109 若一行都没解析到，打印"探针没生效或日志路径错"并 `return 2`。L111-116 抽出六条序列。L118 打印诊断更新数与覆盖 step 范围。L119-124 逐点打印七列表格。L126-127 取后半段（`rows[len//2:]`，为空则回退全体）。L128-138 汇总打印：`grad_cos` 中位/min/max 以及 `< P1_COS` 的点数、`resid_frac_norm` 中位/max、`corr` 中位/min、`level_shift_norm` 中位/max、`level_gap` 中位/max、`gnorm_ratio` 中位，以及后半段的两个中位数。L140-152 判决逻辑：中位数都非 None 时，`m_cos >= P1_COS 且 m_resid <= P1_RESID` → P1（critic 惰性）；否则 `m_cos <= P2_COS 或 m_resid >= P2_RESID` → P2（critic 在工作）；其余 → P3 灰区不判决；并打印两条阈值与实测值。L153-155 若 `level_shift_norm` 中位 > `LEVEL_NOTE`，追加一条分解项提示。L157-180 次级部分：取本 run 锚点序列，打印序列、min/mean/块 worst；再逐个读 `_BASE_RUNS` 的锚点，打印三跑的 min/末点/均值列表，并输出一段读法提示（`min <= 0.025` 或显著改善按预注册 §2 次级条款读）。L159 注释声明该部分"描述性，不判决（R5：n=1/臂 判不了零假设）"。
- 调用: 由 L184-185 `sys.exit(main())` 调用；它调用了 `parse_log`、`anchor_series`、`block_worst`、`_median`、`_fmt`。被谁调用：待确认（脚本入口）。
- 置信度: 已确认

## F:scripts/assassin_vs_sparky.py
- 行数: 181
- 模块作用: 可执行的搜索脚本（自带 `argparse` 与扫描循环），在引擎里复现"刺客完美解满蓄力电磁炮"：P1 在红桥头部署 Sparky（引擎卡名 `ZapMachine`），它过桥进入 P0 蓝左塔射程后被塔输出；脚本在 Sparky 即将开炮的时刻在若干候选落点部署刺客，用冲刺无敌躲炮、贴身输出，统计"蓝方三塔 0 掉血 + 刺客存活"的完美解配置（L2-18 模块 docstring）。
- 模块级 docstring 首行: `「刺客完美解满蓄力电磁炮」搜索脚本 v2（2026-09-10，含防御塔参与）。`
- 主要导入: io, sys, argparse, itertools, battle(as battle_mod), player(as player_mod), core(Position)
- 关键模块级常量:
  - `DECK = ['Knight', 'Arrows', 'Fireball', 'Musketeer', 'Giant', 'Minions', 'MiniPekka', 'Skeletons']`（L31-32）— 双方 8 卡卡组
  - `SPARKY_POS = (3.5, 18.0)`（L34）— P1 左桥头部署点（注释标注）
  - `HORIZON = 35.0`（L35）— `run_case` 默认模拟时长（秒）
- 顶层数据表/字典: 无
- 备注（模块级副作用，源码依据）: L24 在 import 期把 `sys.stdout` 换成 UTF-8 包装器；L25 用 `sys.path.insert(0, r"E:/clash-royale-simulator-main/src/clasher_new")` **硬编码 Windows 绝对路径**（不可移植）；L27-29 依赖同目录的 `battle` / `player` / `core` 模块。`run_case` 的形参 `label` 在函数体内从未被使用（grep 确认仅出现在 L38 签名）；`tower_damage_to_sparky`（L55）与 `last_tower_tgt`（L56）赋值后从未被引用。外部符号核对（已读源确认）：`battle.BattleState.__init__(self, player_0, player_1, card_level=None)`（`battle.py:2539`）、`BattleState.step(self, dt)`（L2652）、`deploy_card(self, player_id, card_name, position, _from_mirror=False)`（L2806）、`_spawn_entity(self, entity)`（L2603）、`Troop.__init__(self, id, position, player, card_name, battle_state=None, evolved=False)`（L760）、`player.PlayerState.__init__(self, player_id, cycle_queue, elixir, tower_hps=(4824, 3052, 3052))`（`player.py:6`，其中 `self.cycle = cycle_queue[:]` 可被外部覆写）、`core.Position` 为 dataclass（`x`,`y`）且带 `distance_to(other)`（用 `math.hypot`）（`core.py:5-9`）；`attack_cooldown`、`invincible`、`_dash_active` 均为 `Troop` 实例属性（`battle.py` 中 L~1148/1224 更新 `attack_cooldown`、L514 置 `invincible=True`、L108/L1153 读 `_dash_active`）。

### S:run_case  [L38-93]
- 类型: function
- 签名: `def run_case(label, assassin_pos, deploy_at, horizon=HORIZON, trace=False):`
- 作用: 跑单个配置：建 lv11 战场、强制 P1 先手出 Sparky 于 `SPARKY_POS`、在 `deploy_at` 秒生成刺客，按 60 fps 步进到 `horizon` 或 Sparky 死亡，返回塔损/刺客/Sparky 的结局与关键时刻。
- 参数:
  - `label` (str): 案例标签；**函数体内未被使用**（仅作调用方标记）。
  - `assassin_pos` (tuple): 刺客部署的世界坐标 `(x, y)`。
  - `deploy_at` (float): 相对模拟开始的部署时刻（秒）。
  - `horizon` (float, 默认 `HORIZON`=35.0): 最长模拟时长（秒）。
  - `trace` (bool, 默认 `False`): 是否打印逐帧 trace。
- 返回: dict：`{"tower_loss", "assassin_alive", "assassin_hp", "sparky_alive", "a_dash_t", "fires"}`（L91-93）；`a_dash_t` 为首次冲刺时刻或 `None`，`fires` 为探测到的 Sparky 开炮时刻列表。
- 实现: L40-42 用 `BattleState` + 两个 `PlayerState(deck, 10.0)`（card_level=11）建局；L43 先 `step(0.033)` 推进一帧；L44-45 把 P1 的 `cycle` 重排为 `ZapMachine` 打头；L46 `assert` 部署成功（失败即断言崩）；L47 从 `entities` 中按名取 Sparky；L48-49 抓三座塔（实体 id 3/4/6）并记录初始血量。L51-56 初始化刺客/开炮/塔伤相关局部变量。L58-84 主循环（`int(horizon*60)` 步）：L60-66 到点且 Sparky 存活时直接构造 `battle_mod.Troop(bs.next_entity_id, Position(*assassin_pos), 0, 'Assassin', bs)` 并 `bs._spawn_entity`（**绕过 `deploy_card` 的费用/合法性检查**，玩家写死为 0）；L67 `bs.step(1/60)`；L68-74 用启发式 `prev_cd < 0.2 且 cd > 3.5` 判定"刚刚开炮"并记录时刻（可 trace 打印刺客距离与无敌状态）；L75-78 检测刺客 `_dash_active` 首次为真并记时刻；L79-80 刺客死亡时 trace；L81-84 Sparky 死亡则 trace 并 `break`。L86-87 塔损 = 三塔初始血量与"存活则当前血量、死亡则 0"之差的和；L88-90 取刺客存活/hp 与 Sparky 存活；L91-93 返回 dict。
- 调用: `main` 在 L141（扫描循环）与 L177（trace 复跑）调用；它调用了 `battle_mod.BattleState`、`player_mod.PlayerState`、`bs.step`、`bs.deploy_card`、`bs._spawn_entity`、`battle_mod.Troop`、`Position`。被谁调用：待确认（脚本入口）。
- 置信度: 已确认（引擎侧属性存在性已在 `battle.py`/`player.py`/`core.py` 核对）

### S:main  [L96-177]
- 类型: function
- 签名: `def main():`
- 作用: 命令行入口：先单独跑一局定位 Sparky 的开炮时刻，再对候选落点 × 部署时机的笛卡尔积跑 `run_case`，分类打印"完美解 / 0 塔损但刺客阵亡 / 无 0 塔损的最近配置"，可选对首个完美解做逐帧 trace。
- 参数: 无（`argparse`）：`--timing`(store_true，启用更密的部署时机网格), `--trace`(store_true，打印逐帧)（L97-100）。
- 返回: 无（无 `return`）。
- 实现: L102-103 打印场景说明。L105-124 定位开炮时刻：与 `run_case` 同法建局、重排 cycle、部署 Sparky，然后空跑 `int(18*60)` 步，用与 `run_case` 相同的 `prev_cd/cd` 启发式收集 `fires`（Sparky 死亡即 break），打印开炮时刻列表与"刺客应在每次开炮前 ~0.8s 内突进"的提示（L125）。L128-129 定义扫描网格 `xs=[7.5, 8.5, 9.5, 10.5]`、`ys=[9.5..14.5]`（6 个值，步长 1.0）。L130 `f0 = fires[0] if fires else 7.1`（无开炮记录时退回 7.1 秒）。L131-136 按 `--timing` 选择时机网格：timing 模式 13 个点（`f0-2.2` 到 `f0+1.0`），否则 5 个点（`f0-2.0` 到 `f0-0.3`）。L138-142 用 `itertools.product` 遍历"落点 × 时机"并逐个调用 `run_case`，结果收集为 `((x,y), dt, r)`。L144-147 三个筛选：`perfect`（`tower_loss <= 0` 且刺客存活）、`alive_only`、`zero_only`（`tower_loss <= 0`）。L149-150 打印扫描总数与三档计数。L152-170 打印分支：有完美解则列前 15 个（含塔损/刺客 hp/Sparky 存活/突进时刻/开炮时刻）；否则若有 0 塔损配置列前 10 个；否则按塔损升序列最近 5 个。L172-177 若 `--trace`，取第一个完美解（否则取塔损最小者）再调一次 `run_case(..., trace=True)` 打逐帧。
- 调用: 由 L180-181 `if __name__ == "__main__": main()` 调用；它调用了 `run_case`、`itertools.product`、`battle_mod.BattleState`、`player_mod.PlayerState`、`bs.deploy_card`、`Position`。被谁调用：待确认（脚本入口）。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/probe_v3_mono_check.py | 3 | 3 |
| scripts/diag_gru_ablation.py | 3 | 3 |
| scripts/forensics_response.py | 3 | 3 |
| scripts/judge_critic_inertia.py | 6 | 6 |
| scripts/assassin_vs_sparky.py | 2 | 2 |

COVERAGE_OK G031 files=5 symbols=17
