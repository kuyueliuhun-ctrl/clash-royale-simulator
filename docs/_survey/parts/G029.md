# 组 G029 文件级分析

> 范围：`scripts/` 下 4 个脚本（共 956 行 / 17 个符号）。仅读本组文件与同仓被 import 的模块（`rl/*`、`battle.py`、`player.py`、`threat_calc.py`、`simulate_exchange.py`、`core.py`、`rl/action_mask.py`）。
> 本组 4 个脚本均为**只读取证 / 判读工具**：两个 `_probe_value_*.py` 在真实 rollout 帧上做 value 通路逐层方差解剖，`judge_anchor_blocks.py` 是锚点序列判据判读器，`question_bank_poc.py` 是题库预训练可行性 POC（临时脚本性质，含硬编码绝对路径）。

## F:scripts/_probe_value_collapse.py
- 行数: 257
- 模块作用: 只读取证的诊断脚本：对一个 FollowerPolicy checkpoint 跑真实 rollout，逐帧抓取 value 通路各中间张量（`fused`/`value_enc_fc` 前激活/`value_enc_ln` 后/MLP 隐层/`enc`/`h`/`value`），并同帧用 `PPOTrainer.compute_gae` 算回报 R，报告跨帧逐层 std、ReLU 死亡情况、SVD 主方向对齐度、以及池化 EV / 局内 EV / vstd/rstd 等指标。头部 docstring 自述回答「value 输出方差塌到 rstd 的 0.1% 发生在哪一层」。依据：L1-11 模块 docstring 与 L109-133 的逐层采集循环。
- 模块级 docstring 首行: `只读取证：value 通路逐层方差解剖（不写盘、不改训练）。`
- 主要导入: `argparse`, `json`, `os`, `sys`, `numpy`, `torch`, `rl.config`(TrainConfig), `rl.belief`(BeliefInference), `rl.belief_planner`(BeliefPlanner), `rl.plan_space`(PLAN_DIM), `rl.follower`(FollowerPolicy, load_checkpoint), `rl.train_follower`(FollowerOpponent), `rl.train_solo`(solo_env, resolve_deck_set), `rl.ppo`(PPOTrainer)
- 关键模块级常量: 无顶层 `NAME = 值` 形式的常量；只有两处路径变量 `_HERE = os.path.dirname(os.path.abspath(__file__))`（L23）、`_SRC = os.path.join(os.path.dirname(_HERE), "src", "clasher_new")`（L24），作用是把 `src/clasher_new` 插入 `sys.path`（L25-26）。另有 `layers` 字典字面量（L109-110）与空列表容器，均为 `main()` 内局部变量。
- 顶层数据表/字典: 无（L109-110 的 `layers` 是 `main()` 局部字典，键为层名、值为逐帧 numpy 数组列表；L112 的 `ep_obs/ep_bel/ep_plan/ep_rew/ep_val/ep_term/ep_trunc` 为局内缓冲列表）。

### S:measurement_line  [L42-56]
- 类型: function
- 签名: `measurement_line(pol, L)`
- 作用: 打印价值 MLP 第 0 层的 pre-activation 跨帧统计、LayerNorm 前统计与 ReLU 全零维度数。
- 参数:
  - `pol` (FollowerPolicy 实例): 提供 `value_head_mlp[0]` 的权重与偏置（L45-46）。
  - `L` (dict): 层名 → 逐帧数组，至少需含 `v_enc_post`（LN 后）、`v_enc_pre`（LN 前）、`v_hidden`（MLP0 后 ReLU 隐层）。
- 返回: 恒返回空字符串 `""`（L56，返回值未被调用方使用，调用点是 L183 的字符串拼接）。
- 实现: 从 `pol.value_head_mlp[0]` 取 `weight`/`bias` 转 numpy（L45-46）；用 `L["v_enc_post"] @ W.T + b` 在 python 侧离线重算 pre-activation `PRE`（L47）；打印 `PRE` 的逐维跨帧 std 均值、`|PRE|` 均值与 `PRE>0` 占比（L48-50）；再打印 `L['v_enc_pre']` 的逐维跨帧 std 均值与绝对值均值（L51-52）；用 `(L["v_hidden"] <= 0).all(axis=0).sum()` 统计「跨全部帧恒 ≤0」的维度数作为 ReLU 全零维数，并打印 `v_hidden` 的逐帧 L1 均值（L53-55）。
- 调用: 仅被本文件 `main()` 在 L183 调用；它调用 `pol.value_head_mlp`、`numpy`（函数内 `import numpy as _np`，L44）与 `python print`。无其它仓库内调用者（全仓 grep 未见于其它文件）。
- 置信度: 已确认

### S:_std  [L59-60]
- 类型: function
- 签名: `_std(x)`
- 作用: 把输入转成 float64 numpy 数组并返回其全局标准差（标量 float）。
- 参数:
  - `x` (任意可转 ndarray 的对象): 待求 std 的数据，转 `np.float64` 后取 `.std()`。
- 返回: `float`，`np.asarray(x, dtype=np.float64).std()` 的结果。
- 实现: 单行 return（L60），先 `np.asarray(x, dtype=np.float64)` 再 `.std()`，无 axis 参数（全局 std），外层 `float()` 转 python 标量。
- 调用: 本文件中**未被调用**（全文检索 `_std(` 只出现在 L59 定义处）；无其它模块 import 本文件（本文件无 import 者）。
- 置信度: 已确认

### S:main  [L63-253]
- 类型: function
- 签名: `main()`
- 作用: 命令行入口：解析参数、装载 ckpt 与配置、构造对手与信念链路、跑 `--frames` 帧确定性 rollout 并逐帧采集 value 通路各层，最后打印跨帧逐层统计、回报方差分解与多种 EV 口径。
- 参数: 无（参数经 `argparse` 从 `sys.argv` 取，见下）。
  - `--ckpt` (str, required): checkpoint 路径（L65）。
  - `--run-dir` (str, default=None): 含 `config.json` 的 run 目录；缺省用 ckpt 所在目录（L66、L73）。
  - `--frames` (int, default=900): rollout 帧数（L67）。
  - `--seed` (int, default=7): 环境/信念种子（L68）。
  - `--device` (str, default="cpu"): 设备；非 cpu 且 CUDA 不可用时回落 cpu（L69、L83）。
- 返回: 无（隐式返回 None；末尾只打印，L253 后无 return）。
- 实现: L72 用 `TrainConfig.resolve("economy")` 取配置；L73-82 读取 run 目录 `config.json`，把其中键逐个 `setattr` 到 cfg（仅当 `hasattr(cfg, k)`，异常静默吞掉）。L85 `solo_env(cfg, seed)` 建环境，L86 用 `BeliefInference(...).encode(None, None)` 的长度确定 belief 维度 `bdim`，L87-89 `load_checkpoint`（显式传 hidden_dim/plan_dim=PLAN_DIM/belief_dim=bdim）后 `to_device(device).eval()`，并打印 `value_independent`/`value_bypass`/`gamma`/`lam`（L90-92）。L94-102 另建一个同结构的 `FollowerPolicy` 对手（按主策略的 `value_bypass`/`value_independent` 旗标构造，L96-98），`load_state_dict(pol.state_dict())` 同步权重（L100），包成 `FollowerOpponent(..., deterministic=True)` 挂到 `env.opponent`（L101-102）。L104-107 建 belief、`env.reset(seed)`、`belief.reset(env.deck1)`、`hidden=None`。主循环 L113-161（`torch.no_grad()` 下，逐帧 t）：调 `bp.plan` 得 plan、`belief.encode` 得 tok、`plan.to_vector()` 得 pv（L115-117）；`pol._encode_parts` 取 `(fused, enc)`（L118）；`pol.gru_cell` 递推 hidden（首帧用零向量，L119-120）；`pol._value_from(enc, h, fused)` 取标量 value（L122）；随后**独立复算**逐层张量 `pre = value_enc_fc(fused)`、`post = value_enc_ln(relu(pre))`、`hid = relu(value_head_mlp[0](post))`（L124-126）并把 fused/pre/post/hid/value/enc/h 追加进 `layers`（L127-133）。L135-137 把 obs/tok/pv/value 存入局内缓冲并预置 rew=0/term=False/trunc=False。L139-141 确定性 `pol.act` 得 bundle 后 `env.step`；L142-144 回填该帧 reward 与 term/trunc；L145 `belief.update(obs, info.get("opp_played"))`。L146-161 在局结束时：若未终止（truncated）用 `pol.value` 算 bootstrap 末值 `last_val`，否则 0.0（L147-151）；调 `PPOTrainer.compute_gae(ep_rew, ep_val, ep_term, gamma, lam, truncated=ep_trunc, last_value=last_val)` 只取 `ret`（L152-154，`adv` 被丢弃）；把该局的 R/V 序列与 len/rew_last 存进 `per_ep`（L155-157）；`env.reset(seed=a.seed + t)` 重开一局并清 belief/hidden/缓冲（L158-161）。L163 把 `layers` 转成 ndarray 字典；L165-176 打印每层的 dim、`||x||` 均值/std、逐维跨帧 std 均值与最大值、相对变化（用 `dm.mean()/|X|.mean()`，分母有 1e-12 下限），以及 value 的 mean/std/min/max；L177 打印 value 唯一值个数与逐帧 `|Δ|` 最大值；L178-179 打印末层 bias 并据「唯一值数==1」判定 hidden 是否逐帧恒定。L181-182 是一个只做 `pass` 的空循环（无作用）。L183 调 `measurement_line`。L185-198 用末层权重 `value_head_mlp[2].weight`（展平成 (64,)）与去均值的隐层 `hv` 复算 `Σ w·(hidden−mean)` 的跨帧 std，并打印隐藏层恒定分量范数 vs 跨帧变化范数范数比、跨帧 std top5 维度（含对应权重）、以及 `Σ_k |std_k·w_k|` 上界。L199-203 对 `hv` 做 SVD，打印前 3 奇异值与 `|cos(w, PC1)|`；L204-205 打印 `|cos(w, hidden 均值方向)|`。L207-211 用**未启用的** `value_head(h)` 头做对照（把整个 `L['h']` 张量喂进去算 std 并打印其权重范数）；L212-213 对照打印 `enc`/`h` 的逐维跨帧 std 均值。L215-220 对 `v_enc_pre`/`v_enc_post` 分别拆「跨维均值（共同模）」与「去共同模残差」，打印各自的跨帧 std。L222-241 拼接所有局的 R 与 V 到同长度，打印全网 R 的 std/mean/min/max、局间 std（每局均值）与局内 std（R 减局均值），给出方差分解；计算 `mse=mean((V-R)^2)`、`var=R.var()`、以及「每局常数预测局均值」的 `mse_const`，打印 `EV(同帧池化)=1-mse/var`、`EV(局均值常数预测)`、`value std / 全网 R std`、`value std / 局内 R std`、`corr(V,R)`。L244-251 若 `per_ep` 非空，逐局算 `corr(V,R)`（仅取 len>3 且 R、V 的 std>1e-9 的局）并打印均值与抽样 6 局的 len/Rmean/Rstd/Vstd。L252-253 打印一条注记，指出 vstd/rstd 门槛的分母用池化 rstd、与局内 std 量纲不可比。
- 调用: 由文件末尾 `if __name__ == "__main__": main()`（L256-257）调用；本文件无被 import 记录（全仓检索 `_probe_value_collapse` 只出现在本文件与文档字符串）。它调用了：`TrainConfig.resolve`、`solo_env`、`resolve_deck_set`（L94 结果 `mirror_deck` 未被使用）、`BeliefInference`/`.encode`/`.reset`、`load_checkpoint`、`BeliefPlanner.plan`、`FollowerPolicy._encode_parts`/`gru_cell`/`_value_from`/`act`/`value`、`FollowerOpponent`、`PPOTrainer.compute_gae`、`env.reset`/`env.step`/`env.get_action_mask`。这些被调符号均在同仓 `rl/` 下存在（`rl/follower.py:67/268/303/433/704`、`rl/ppo.py:168`、`rl/train_solo.py:107/157`、`rl/belief_planner.py:341/772`）。
- 置信度: 已确认（唯一保留：L94 `mirror_deck` 与 L47-56/`measurement_line` 的返回值都未被使用，属代码事实，未推断作者意图）

## F:scripts/judge_anchor_blocks.py
- 行数: 240
- 模块作用: 锚点判据判读器：从 run 的 `solo_state.json`（或回退日志）读 `baseline_rand` 锚点序列，按锚点序号切块，算每块 worst/median，输出 P1 主判据与 Q2 描述性统计；`--groups` 模式则从磁盘复算 D1 组与无变化组的对照基线表并判断区间是否重叠。依据：L2-24 模块 docstring 与 L45-51 常量。
- 模块级 docstring 首行: `C1 判据判读器：锚点序列 → 分块 worst/median → 预注册判据（含基线组复算）。`
- 主要导入: `argparse`, `json`, `os`, `re`, `statistics`, `sys`（全部为标准库，无第三方/同仓导入）
- 关键模块级常量:
  - `_HERE = os.path.dirname(os.path.abspath(__file__))` — 本脚本目录（L41）。
  - `_ROOT = os.path.dirname(_HERE)` — 仓库根（L42）。
  - `RUNS = os.path.join(_ROOT, "src", "clasher_new", "runs")` — 默认 run 根目录（L43）。
  - `D1_GROUP = ["d1_league_20k", "d1_league_20k_r2", "d1_league_20k_r3"]` — D1 干预组三跑名字（L46）。
  - `NOCHANGE_GROUP = ["fprime_20k", "fprime_ev_20k", "gfix_20k"]` — 对照（无变化）组三跑名字（L47）。
  - `P1_MEAN_MIN = 0.10` — P1 判据 `mean(worst_b)` 下限（L49）。
  - `P1_MIN_MIN = 0.05` — P1 判据 `min(worst_b)` 下限（L50）。
  - `Q2_BAND = 0.10` — Q2 描述性带宽（末块中位数 − 首块中位数）（L51）。
- 顶层数据表/字典: 无字典/表字面量；`D1_GROUP`/`NOCHANGE_GROUP` 两个字符串列表即模块级配置数据（L46-47），其「数值一律由磁盘计算」的说明见 L45 注释。L35-39 的 `for _s in ("stdout","stderr")` 是 stdout/stderr UTF-8 reconfigure 兜底（非数据表）。

### S:load_anchors_from_state  [L54-67]
- 类型: function
- 签名: `load_anchors_from_state(run)`
- 作用: 从 `runs/<run>/solo_state.json` 的 `_controls_history` 中筛出 `vs == "baseline_rand"` 的锚点，返回 `[(step, winrate)]` 升序序列与整个 state 字典。
- 参数:
  - `run` (str): run 目录名，拼成 `RUNS/<run>/solo_state.json`（L56）。
- 返回: `(sorted(out), st)` 二元组：`out` 为 `(int(step), float(winrate))` 列表按 step 升序（L67）；`st` 为完整解析出的 JSON 字典（L58）。
- 实现: `os.path.join(RUNS, run, "solo_state.json")` 组路径后以 utf-8 打开 `json.load`（L56-58）；遍历 `st.get("_controls_history") or []`，跳过 `vs != "baseline_rand"` 的条目（L60-62），跳过 `winrate is None` 的条目（L63-65），其余追加 `(int(c["step"]), float(c["winrate"]))`（L66）；返回前整体 `sorted`（L67）。文件不存在或 JSON 非法会抛 `OSError`/`ValueError`，由 `main()` 的 `--groups` 分支捕获（L188）。
- 调用: 被 `main()` 的 `--groups` 分支（L187）与 `--run` 分支（L223）调用；无其它模块引用。实测 `src/clasher_new/runs/d1_long_100k/solo_state.json` 存在且 `_controls_history` 中 `baseline_rand` 条目为 41 条（step 0..100000），与 docstring 的「100k 的 2500 分辨率锚点序列天然可读」一致。
- 置信度: 已确认

### S:load_anchors_from_log  [L70-88]
- 类型: function
- 签名: `load_anchors_from_log(path)`
- 作用: 回退解析器：不读 state 而直接扫日志文本，抓 `eval@<step>:` / `anchor@<step>:` 后的 `vs baseline_rand: <数字>±` 值，返回 `(step, winrate)` 升序去重序列。
- 参数:
  - `path` (str): 日志文件路径（L75）。
- 返回: `sorted(set(out))` — 去重并升序的 `(step, winrate)` 列表（L88）。
- 实现: 预编译两个正则：`pat_step = r"(?:eval|anchor)@(\d+):"`（L73）、`pat_wr = r"vs baseline_rand: .*?([\d.]+)±"`（L74）。按行读文件（`errors="replace"`，L75-76）；若行内匹配到 step，则更新 `cur` 并**在同一行**再尝试匹配 winrate（新日志格式把锚点写在同一行），命中即追加后 `continue`（L77-84）；若当前行没有 step 但匹配到 winrate 且 `cur` 已有值，则把该值记到当前 step 上（旧日志的上下行格式，L85-87）。返回值经 `set` 去重，故同一 `(step,winrate)` 重复行只保留一条。
- 调用: 被 `main()` 的 `--log` 分支调用（L219）；无其它模块引用。
- 置信度: 已确认（正则为原样抄自 L73-74；对「旧日志格式」的具体版式未逐份日志验证，故只描述代码的分支行为）

### S:blocks_of  [L91-116]
- 类型: function
- 签名: `blocks_of(series, per_block)`
- 作用: 把锚点序列按**序号**连续切成每块 `per_block` 个点，返回每块的块号、点数、worst（块内最小 winrate）、median、首末 step 与 step 列表。
- 参数:
  - `series` (list[tuple[int,float]]): `(step, winrate)` 升序序列。
  - `per_block` (int): 每块点数（CLI 缺省 9）。
- 返回: `list[dict]`，每个 dict 含键 `block`（0 起的块号）、`n`（块内点数）、`worst`（`min(wrs)`）、`median`（`statistics.median(wrs)`）、`lo`/`hi`（首/末点的 step）、`steps`（块内全部 step 列表）（L107-115）。
- 实现: L102 `for b in range(0, len(series), per_block)` 按序号步进切片 `series[b:b+per_block]`（L103），空切片 `continue`（L104-105）；取块内 winrate 列表 `wrs`（L106）后组装 dict 并 append（L107-115）。**为什么按序号而非按 `step//block`**：L92-99 的 docstring 明确说明按 step 切会把边界点（如 step=20000 落在 `[20000,40000)` 块）单独切成一块，使 20k run 变成「8 点块 + 1 点块」，与 20k 判读口径（全程 9 点最差）不一致；按序号切时 9 点/块下 20k=1 块、100k 的 41 点=5 块（9,8,8,8,8）。docstring 还自述第一版按 step 切复算出 mean(worst) 0.400 与文档 0.192 对不上才暴露该错。
- 调用: 仅被本文件 `evaluate` 调用（L120）；`evaluate` 又被 `main()`（L194、L228）调用。
- 置信度: 已确认（docstring 中的历史数字（0.400/0.192）是注释自述，未独立复算，本条目只描述代码行为）

### S:evaluate  [L119-138]
- 类型: function
- 签名: `evaluate(series, per_block)`
- 作用: 在 `blocks_of` 结果上算 P1 判据布尔值、Q2 末块−首块中位数差，以及全局最小、块 worst 的 mean/min/max 与首末块中位数。
- 参数:
  - `series` (list[tuple[int,float]]): 锚点序列。
  - `per_block` (int): 每块点数，透传给 `blocks_of`（L120）。
- 返回: `res` 字典（L123-137），键：`rows`（`blocks_of` 输出）、`n_points`、`global_min`（全序列最小 winrate，空序列为 None）、`mean_worst`、`min_worst`、`max_worst`、`median_first`、`median_last`、`p1_pass`（bool）、`q2_delta`（float 或 None）。
- 实现: L120 取块；L121-122 抽出 `worsts` 与 `medians`（列表推导，空块列表时为空列表）；L123-132 组装字典，`global_min` 用 `min((w for _, w in series), default=None)`，各聚合量在空列表时取 None（L127-131）；L133-135 `p1_pass = mean_worst is not None and mean_worst >= P1_MEAN_MIN and min_worst >= P1_MIN_MIN`（短路保证非空）；L136-137 `q2_delta = median_last - median_first`（有 medians 时）否则 None。
- 调用: 被 `main()` 的 `--groups` 分支（L194）与最终判读路径（L228）调用；无其它模块引用。
- 置信度: 已确认

### S:print_run  [L141-162]
- 类型: function
- 签名: `print_run(run, per_block, series, res, note="", complete=True)`
- 作用: 把一个 run 的判读结果打印成 Markdown（标题 + 锚点范围 + 每块表格 + worsts/medians 汇总 + P1 结论行），并在数据不完整时附加警示。
- 参数:
  - `run` (str): 显示名（run 名或日志 basename）（L142）。
  - `per_block` (int): 每块点数；**函数体内未被使用**（仅在签名中出现）。
  - `series` (list[tuple[int,float]]): 锚点序列，用于打印起止 step（L143）。
  - `res` (dict): `evaluate` 的返回。
  - `note` (str, default=""): 附在标题后的备注（如 `(state total_steps=...)`）（L142）。
  - `complete` (bool, default=True): 数据是否完整；False 时在 P1 行后加「不得作为判决」警示（L161-162）。
- 返回: 无（只 print；`series` 为空时 L146-147 提前 return）。
- 实现: L142 打印 `### <run>`（有 note 时补两空格加 note）；L143-145 用条件表达式打印锚点点数/step 范围/全程最小——注意该三元表达式整体是 `print(... if series else f"### {run}: 无锚点数据")`，故 series 为空时打印的是「无锚点数据」标题；L146-147 再 `return`。L148-152 打印表头与逐块行（块号 `b{block+1}`、step 区间 `lo–hi`、点数、`**worst**`、median）；L153-155 打印 `worsts` 列表与 mean/min/max；L156-158 打印 `medians` 列表与 `末-首` 差值，并按 `Q2_BAND` 给「提示上移 / 提示下移 / 不可判」三分支；L159-162 打印 P1 判据（含阈值数值）与 PASS/FAIL，`complete=False` 时追加不完整警示。
- 调用: 仅被本文件 `main()` 在 L235 调用。
- 置信度: 已确认

### S:main  [L165-236]
- 类型: function
- 签名: `main()`
- 作用: CLI 入口，三条互斥路径：`--groups` 复算历史对照基线表并返回 0；`--log` 走日志回退解析；`--run` 走 state 解析；然后统一 `evaluate` + `print_run`。
- 参数: 无（argparse 参数如下）。
  - `--run` (str, default=None): `runs/<run>` 目录名（L167）。
  - `--log` (str, default=None): 回退日志路径（L168）。
  - `--per-block-points` (int, default=9, dest="per_block_points"): 每块锚点点数（L169-170）。
  - `--expect-total` (int, default=0, dest="expect_total"): 预期总步数，用于完整性标注（L171-172）。
  - `--groups` (store_true): 复算基线表（L173-174）。
- 返回: `0`（`--groups` 分支 L216 与正常分支 L236）；无 `--run/--log/--groups` 时 `ap.error(...)` 直接退出（L226）。
- 实现: L177-216 `--groups` 分支：打印标题（含每块点数，L178）；对 `("D1", D1_GROUP)` 与 `("NOCHANGE", NOCHANGE_GROUP)` 两组循环（L180），组内逐 run `load_anchors_from_state`（L187），捕获 `OSError/ValueError` 打「数据缺失」（L188-190），无锚点打「无锚点」（L191-193），否则 `evaluate` 后打印该 run 的一行表（点数/块数/worsts/mean/min/P1，L194-200）；同时把该 run 的 `min_worst` 与 `mean_worst` 收进 `worsts`/`means` 便于组级汇总（L196-197）。组内若有数据，打印该组单跑 min(worst) 列表、区间 `[min,max]` 与组均值（L201-209）。两组都有数据时（L210-215）用 `overlap = not (d.min > n.max or n.min > d.max)` 判区间是否重叠，并打印「是（判据力不足）/否（判据有判别力）」与两个区间。L218-226 非 groups 路径：`--log` 时用 `load_anchors_from_log`、`st={}`、name=basename（L218-221）；`--run` 时用 `load_anchors_from_state`、name=run（L222-224）；否则 `ap.error`。L228 `evaluate`；L229-234 完整性判定：仅当 `--run` 且 state 有 `total_steps` 时，`note` 记 `(state total_steps=<ts>)`，若 `ts < expect_total` **或** 末点 step `< ts` 则 `complete=False`（L230-234）；L235 `print_run(...)`；L236 return 0。
- 调用: 由文件末尾 `if __name__ == "__main__": sys.exit(main())`（L239-240）调用。它调用了本文件的 `load_anchors_from_state`/`load_anchors_from_log`/`evaluate`/`print_run`，以及 `argparse`/`print`。除本文件外，仓库内没有其它 Python 文件 import 或调用本脚本（全仓 grep `judge_anchor_blocks` 在本组 4 个脚本之外无命中）；它是给人手工执行的 CLI 工具。
- 置信度: 已确认

## F:scripts/question_bank_poc.py
- 行数: 239
- 模块作用: 题库预训练的可行性 POC（临时脚本性质）：用引擎构造「威胁牌已行进到某位置」的快照，枚举防守手牌 × 粗网格落点，调 `simulate_exchange(defender="none")` 逐候选结算并按训练奖励汇率打分，取 top-k 作为答案集；再用 3 道已知解题做对账，可选让 checkpoint 答题并报 EV gap。依据：L2-20 模块 docstring 与 L37-41 常量、L121-131 题表。
- 模块级 docstring 首行: `题库预训练可行性 POC（2026-09-09）：不写死答案，引擎判卷。`
- 主要导入: `copy`, `io`, `sys`, `time`, `battle`(as battle_mod), `player`(as player_mod), `card_utils`(Card), `core`(Position), `threat_calc`(estimate_tower_threat), `simulate_exchange`(simulate_exchange)；函数内延迟导入：`rl.action_mask`(legal_cells，L73)、`numpy`/`torch`/`rl.follower`(load_checkpoint)/`rl.belief`(BeliefInference, belief_token_dim)/`rl.belief_planner`(BeliefPlanner)/`rl.plan_space`(PLAN_DIM)/`rl.env_wrapper`(RLEnv)（L175-181）
- 关键模块级常量:
  - `K_TOWER_SAVE = 0.0012` — 塔伤节省的奖励汇率（少挨的塔伤 ⇒ 加分）（L38）。
  - `K_UNIT_DMG = 0.0005` — 敌军 HP 移除量的汇率（L39）。
  - `K_TOWER_DMG_OPP = 0.0010` — 对敌塔伤害的汇率（反打）（L40）。
  - `EDW = 0.5` — 我方圣水花销的惩罚系数（`elixir_diff_weight`）（L41）。
  - `HAND_N = 4` — 参战手牌张数（只枚举前 4 张）（L43）。
  - 另有两处**顶层副作用**（非常量）：L26 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")` 替换 stdout；L27 `sys.path.insert(0, r"E:/clash-royale-simulator-main/src/clasher_new")` 写死 Windows 绝对路径。
- 顶层数据表/字典:
  - `DECK_Q1`（L114-115）: 8 张卡名列表（MiniPekka/Knight/Musketeer/Arrows/Minions/Fireball/Skeletons/Giant），题 1 的双方卡组。
  - `DECK_Q2`（L116-117）: 8 张卡名列表（Cannon/Skeletons/Musketeer/Fireball/IceGolemite/Log/Knight/Arrows），题 2 的双方卡组。
  - `DECK_Q3`（L118-119）: 8 张卡名列表（Arrows/Musketeer/Minions/Knight/Fireball/Skeletons/Archers/Giant），题 3 的双方卡组。
  - `QUESTIONS`（L121-131）: 3 个 dict 组成的题库表。键含义：`qid` 题号；`defender_deck`/`attacker_deck` 双方卡组；`threat` 威胁卡名；`threat_pos` 部署坐标；`slice_y` 切片时的威胁 y；`horizon` 推演秒数；`expect` 已知解 `(卡名, 距离阈值)`。三条为 `giant_bridge`(Giant@(3.5,17.5), slice_y=14.0, horizon=20.0, expect=(MiniPekka,3.0))、`hog_bridge`(HogRider@(14.5,17.5), slice_y=15.0, horizon=12.0, expect=(Cannon,6.0))、`minions_air`(Minions@(3.5,17.5), slice_y=15.0, horizon=10.0, expect=(Arrows,99.0))。**注意 `build_questions` 会把 `battle`/`baseline`/`top` 三个运行时键写回这些 dict**（L145-147）。

### S:make_battle  [L46-68]
- 类型: function
- 签名: `make_battle(defender_deck, attacker_deck, threat_card, threat_pos, elixir=8.0)`
- 作用: 构造题目快照的 `BattleState`：防守方给定圣水（默认 8.0）、攻击方 5.0 圣水，把威胁卡排到攻击方手牌首位并部署，然后沿 y 方向直线下移到目标位置（近似「威胁已行进」）。
- 参数:
  - `defender_deck` (list[str]): 防守方卡组。
  - `attacker_deck` (list[str]): 攻击方卡组。
  - `threat_card` (str): 威胁卡名。
  - `threat_pos` (tuple[float,float]): 目标位置 `(x, y)`（最终切片位置）。
  - `elixir` (float, default=8.0): 防守方圣水。
- 返回: `bs`（`battle.BattleState` 实例，威胁实体已在其 `entities` 中且 y 已被设为 `threat_pos[1]`）（L68）。
- 实现: L48-50 用 `PlayerState(0, defender_deck, elixir)` 与 `PlayerState(1, attacker_deck, 5.0)` 建双方，再 `BattleState(..., card_level=11)`（`BattleState.__init__(player_0, player_1, card_level=None)` 见 `battle.py:2539`）。L51-53 注释说明威胁卡必须在前 4 张（`can_play_card` 校验），故把 `threat_card` 提到 `players[1].cycle` 首位。L54 直接 `deploy_card(1, threat_card, Position(*threat_pos))` 得 `ok`；L55-59 若失败按固定顺序重试三个备选落点 `(threat_pos[0],18.5)`、`(threat_pos[0],19.5)`、`(14.5,18.5)`；L60 `assert ok` 失败即抛。L61-63 从 `bs.entities.values()` 里取第一个「玩家 1 且存活且 id 不在 (1,2,5)」的实体作为威胁对象（id 1/2/5 是双塔与王塔，见 `battle.py:2553-2557` 的 `_spawn_entity`）。L64-67 直线下移近似：L65-66 `while threat.position.y > threat_pos[1] + 1e-6:` 每轮 `y -= max(speed, 0.5) * 0.25`（速度下限 0.5、步长 0.25 s），L67 最后对齐 `y = threat_pos[1]`。docstring（L47、L64）说明该近似不走引擎步进，以避免途中塔仇恨污染切片语义。
- 调用: 被 `build_questions` 在 L139-140 调用；无其它模块引用。它调用了 `battle_mod.BattleState`、`player_mod.PlayerState`、`Position`、`bs.deploy_card`（返回 bool，`battle.py:2806`）。
- 置信度: 已确认（「直线下移近似不会触发塔仇恨」是 docstring 的自述设计意图，未独立验证；本条目按源码描述行为）

### S:question_candidates  [L71-102]
- 类型: function
- 签名: `question_candidates(bs, horizon, coarse=2)`
- 作用: 枚举防守方手牌前 4 张 × 粗网格落点，逐候选调 `simulate_exchange(defender="none")` 结算并按本文件汇率打分，返回「不动手」基线与按分数降序的候选列表。
- 参数:
  - `bs` (BattleState): 题目快照（由 `make_battle` 产出）。
  - `horizon` (float): 推演秒数，传给 `estimate_tower_threat` 与 `simulate_exchange`。
  - `coarse` (int, default=2): 网格粗化步长（只取 `gx % coarse == 0` 且 `(gy-1) % coarse == 0` 的格子）。
- 返回: `(baseline, cands)`：`baseline` 为 `estimate_tower_threat(bs, 0, horizon=horizon)["total"]`（float，不动手时我方塔损）；`cands` 为 dict 列表（每项键 `card`/`pos`/`score`/`saved`/`killed`/`cost`），按 `score` 降序（L100）。
- 实现: L73 延迟导入 `rl.action_mask.legal_cells`；L74 取 `bs.players[0].cycle[:HAND_N]` 为防守手牌；L75 算不动手基线塔损。L77 起计时；L79-80 预生成 `xs`/`ys` 两个列表（`xs=[0.5+coarse*i for i in range(9)]`、`ys=[1.5+coarse*i for i in range(10)]`）——**这两个列表在后续循环中并未被使用**（循环直接遍历 `gy in range(32)`/`gx in range(18)`，L83-84）。L81-99 三重循环：对每张手牌先 `cells = legal_cells(bs, 0, cname)`（L82，返回 (32,18) bool），再遍历 `gy∈[0,32)`、`gx∈[0,18)`，跳过非法格与不满足粗化条件的格（L85），构 `pos = Position(gx+0.5, gy+0.5)`（L87）后调 `simulate_exchange(bs, 0, cname, pos, horizon=horizon, defender="none")`（L88-89）并计数 `n`（L90）；`legal=False` 的候选跳过（L91-92）；否则 `saved = baseline - res["my_towers"]["total"]`（L93），`score = K_TOWER_SAVE*saved + K_UNIT_DMG*res["opp_units"]["hp_removed"] + K_TOWER_DMG_OPP*res["opp_towers"]["total"] - EDW*res["my_cost"]`（L94-96），把 score 四舍五入到 4 位、saved 到 1 位存入候选（L97-99）。L100 按 `-score` 排序；L101 打印「候选结算数 + 耗时 + 不动手基线塔损」。
- 调用: 被 `build_questions` 在 L141 调用。它调用了 `rl.action_mask.legal_cells`（`rl/action_mask.py:441`）、`threat_calc.estimate_tower_threat`（`threat_calc.py:51`，返回 dict 含 `total`）、`simulate_exchange.simulate_exchange`（`simulate_exchange.py:163`，返回 dict 含 `legal`/`my_towers`/`opp_units`/`opp_towers`/`my_cost`）。**`xs`/`ys` 未被使用**为代码事实。
- 置信度: 已确认

### S:ev_of  [L105-111]
- 类型: function
- 签名: `ev_of(bs, cname, pos, horizon, baseline)`
- 作用: 对单个 `(卡, 落点)` 候选按同一汇率公式算分（用于给 checkpoint 的答案打分），非法部署返回 None。
- 参数:
  - `bs` (BattleState): 快照。
  - `cname` (str): 卡名。
  - `pos` (Position): 落点。
  - `horizon` (float): 推演秒数。
  - `baseline` (float): 不动手基线塔损（由 `question_candidates` 提供）。
- 返回: 4 位小数的 float 分数；`res["legal"]` 为假时返回 `None`（L107-108）。
- 实现: L106 调 `simulate_exchange(bs, 0, cname, pos, horizon=horizon, defender="none")`；L107-108 非法即返回 None；L109 算 `saved = baseline - res["my_towers"]["total"]`；L110-111 返回与 `question_candidates` 完全同式的加权和（四舍五入 4 位）。**注意它未做 `round(saved,1)`，与 `question_candidates` 里写入候选 dict 的 `saved` 字段存在取整差异，但打分公式本身同式。**
- 调用: 被 `checkpoint_quiz` 在 L206 调用。调用了 `simulate_exchange`。
- 置信度: 已确认

### S:build_questions  [L134-149]
- 类型: function
- 签名: `build_questions()`
- 作用: 遍历模块级 `QUESTIONS`，为每题构造快照、枚举候选、打印前 6 名，并把 `battle`/`baseline`/`top`（前 8 名）写回题目 dict。
- 参数: 无。
- 返回: `qs`（`QUESTIONS` 列表本身，元素已被就地补上 `battle`/`baseline`/`top` 三个键）（L148）。
- 实现: L135 建空 `qs`；L136 逐题循环；L137-138 打印题头（qid/threat/slice_y/防守手牌前 4 张/horizon）；L139-140 `make_battle(..., (threat_pos[0], slice_y))`——**注意传入的 y 是 `slice_y` 而不是 `threat_pos[1]`**；L141 `question_candidates(bs, horizon)`（未传 `coarse`，用缺省 2）；L142-144 打印前 6 名（名次、卡@格、score、少挨塔伤、击杀、费）；L145-147 把 `bs`/`baseline`/`cands[:8]` 分别写入 `q["battle"]`/`q["baseline"]`/`q["top"]`；L148 把 `q` 追加进 `qs`。
- 调用: 被文件末尾 `if __name__ == "__main__"` 块在 L230 调用；`qs` 随后传给 `reconcile`（L231）与可选 `checkpoint_quiz`（L235、L237）。
- 置信度: 已确认

### S:reconcile  [L152-171]
- 类型: function
- 签名: `reconcile(qs)`
- 作用: 已知解对账：检查每题期望卡是否在 top-8 内，并打印其名次与「落点是否贴近威胁实体」的几何判定，返回是否全部通过。
- 参数:
  - `qs` (list[dict]): `build_questions` 的返回（每题须含 `expect`/`top`/`battle`/`qid`）。
- 返回: `ok_all`（bool）：任一题期望卡不在 top-8 则为 False（L154、L161）。
- 实现: L153 打印小节标题；L154 `ok_all=True`；L155 逐题：取出 `expect=(exp_card, exp_dist)`（L156）；L157-158 用 `next(...)` 在 `q["top"]` 中找第一个卡名等于 `exp_card` 的名次（1 起），找不到为 None；L159-162 找不到时打印 `[FAIL] ... 不在 top-8`、置 `ok_all=False` 并 `continue`。L163 取该名次候选 `c`；L164 把网格坐标转世界坐标 `wx, wy = c["pos"][0]+0.5, c["pos"][1]+0.5`；L165-167 从 `q["battle"].entities` 重新取威胁实体（与 `make_battle` 同一条筛选条件：玩家 1、存活、id 不在 (1,2,5)）；L168 算欧氏距离 `dist`；L169 `geo = "贴身✓" if dist <= exp_dist else f"距离{dist:.1f}"`；L170 打印 `[PASS] qid: 卡 排名 #rank（geo，score ...）`。**`exp_dist` 同时充当「贴身」阈值**（如 Q3 的 `expect=("Arrows", 99.0)` 使该阈值实际不可能失败），这是源码的字面逻辑。
- 调用: 被文件末尾主块在 L231 调用；结果 `ok` 用于 L232 的打印。
- 置信度: 已确认

### S:checkpoint_quiz  [L174-225]
- 类型: function
- 签名: `checkpoint_quiz(qs, ckpt_path, ckpt_label)`
- 作用: 让指定 checkpoint 对每题出一次确定性动作，把其部署动作还原成 `(卡, 落点)` 并用 `ev_of` 打分，与 oracle 最高分比较给出 gap 与 top-8 命中名次。
- 参数:
  - `qs` (list[dict]): 已构造好的题库（含 `battle`/`baseline`/`top`/`defender_deck`/`attacker_deck`/`horizon`）。
  - `ckpt_path` (str): checkpoint 路径。
  - `ckpt_label` (str): 打印用标签。
- 返回: `rows`（list[dict]，每项键 `qid`/`ans`/`ev`/`best`/`gap`/`hit`）（L216-217、L225）。
- 实现: L175-181 函数内延迟导入 numpy/torch/`load_checkpoint`/`BeliefInference`,`belief_token_dim`/`BeliefPlanner`/`PLAN_DIM`/`RLEnv`；L184-185 `load_checkpoint(ckpt_path, plan_dim=PLAN_DIM, belief_dim=belief_token_dim(QUESTIONS[0]["attacker_deck"]))`——**belief_dim 用 `QUESTIONS[0]` 而不是当前题的卡组**（三题 attacker_deck 不同，故对 Q2/Q3 是按 Q1 卡组算出的维度）。L186 `RLEnv(opponent=None, seed=0)`（`opponent=None` 在 `_run_opponent` 中走 `self._random_opponent()`，见 `rl/env_wrapper.py:493-494`）。L188-220 逐题：L189 `env.battle = copy.deepcopy(q["battle"])` **直接替换 battle 属性、未调 `env.reset()`**；L190 同步 `env.deck0/deck1`；L191 `obs = env.observe(0)`；L192 新建 `BeliefInference(opp_deck=q["attacker_deck"], seed=0)`；L193-194 `BeliefPlanner().plan(...).to_vector()`；L195 `belief.encode(obs, None)`；L196-197 确定性 `policy.act(..., hidden=None, deterministic=True)`。L198-199 在 `bundle.sub_actions` 里找第一个 `kind=="deploy"` 且 `1<=slot<=4` 的子动作；L200-209 若无部署动作则答案记为 `WAIT(null)`、`ans_ev=0.0`；否则 L203 用 `q["defender_deck"][dep.slot-1]` 还原卡名、`ans_pos=(dep.x, dep.y)`，L205-207 把网格坐标转世界坐标后 `ev_of(env.battle, ans_card, Position(wx,wy), horizon, baseline)`，`ans_ev is None`（非法/被闸门拒）时按 0.0 处理（L208-209）。L210 `best = q["top"][0]["score"]`；L211-214 在 top 中找 `hit_rank`：卡名相同且 `|Δgx|<=1` 且 `|Δgy|<=1`；L215 `gap = best - ans_ev`；L216-217 组装行并打印（`ans`/`ev`/`oracle`/`gap`/`≈等效塔伤`/`top8命中`）；L221-224 打印平均 gap 与 top-8 命中率（`n_hit` 是「hit 为真」的比例）。
- 调用: 仅被文件末尾主块在 `--quiz` 分支调用两次（L235-238），分别传 `runs/archive/economy_100k_v1_ungated/solo_main.pt` 与 `src/clasher_new/main_ckpt_32000.pt`。**实测前者路径不存在**（`src/clasher_new/runs/` 下无 `archive/` 目录），故该次调用会在 `load_checkpoint` 处失败；后者 `main_ckpt_32000.pt` 实测存在（2,373,264 B）。它调用了 `load_checkpoint`、`RLEnv`/`observe`、`BeliefInference`/`encode`、`BeliefPlanner`/`plan`、`policy.act`、`copy.deepcopy`、`ev_of`。
- 置信度: 已确认（「第一个 archive 路径不存在 ⇒ 该次调用会失败」为对当前工作区的实测结论；`env.battle` 直接替换而未经 `reset` 的后果未进一步验证，不推断）

## F:scripts/_probe_value_path.py
- 行数: 220
- 模块作用: 与 `_probe_value_collapse.py` 同族的只读取证脚本：在真实 rollout 帧上逐帧抓 value 通路每层（`fused`、`value_enc_fc` 前/后、`value_enc_ln` 后、MLP0 前/后、`value`，以及 `enc`/`h`/GRU 候选 pre-activation），报告每层「恒定分量 vs 跨帧变化」、ReLU 存活率，并同帧算 GAE 回报做 EV 分解。依据：L1-11 模块 docstring 与 L107-149 采集段。
- 模块级 docstring 首行: `只读取证：独立价值通路逐层方差解剖（不写盘、不改训练）。`
- 主要导入: `argparse`, `json`, `os`, `sys`, `numpy`, `torch`, `rl.config`(TrainConfig), `rl.belief`(BeliefInference), `rl.belief_planner`(BeliefPlanner), `rl.plan_space`(PLAN_DIM), `rl.follower`(FollowerPolicy, load_checkpoint), `rl.train_follower`(FollowerOpponent), `rl.train_solo`(solo_env, resolve_deck_set), `rl.ppo`(PPOTrainer)
- 关键模块级常量: 无顶层 `NAME = 值` 常量；仅有 `_HERE`（L23）与 `_SRC`（L24）两处路径变量，作用是把 `src/clasher_new` 插入 `sys.path`（L25-26）。L107-108 的 `buf` 字典（11 个层名 → 逐帧数组列表）为 `main()` 局部变量。
- 顶层数据表/字典: 无（`buf`/`per_ep` 均为 `main()` 局部）。

### S:layer_stats  [L41-51]
- 类型: function
- 签名: `layer_stats(name, X)`
- 作用: 对一个 `(T, D)` 层的逐帧矩阵返回一行格式化描述：维度、绝对值均值、恒定分量范数、跨帧变化范数、相对变化、逐维 std 均值、零变化维占比。
- 参数:
  - `name` (str): 层名（格式化宽度 14 显示）。
  - `X` ((T,D) 类数组): 逐帧层张量。
- 返回: `str`，单行报告（L49-51）。
- 实现: L43 转 `np.float64` ndarray；L44 `dstd = X.std(axis=0)`（逐维跨帧 std）；L45 `fixed = ||X.mean(axis=0)||`（每维时间均值向量的范数 = 恒定分量大小）；L46 `varying = ||dstd||`（跨帧变化大小）；L47 `rel = varying / max(1e-12, fixed)`（相对变化，带分母下限）；L48 `frac_dead = (dstd < 1e-9).mean()`（逐维 std 近零的维占比）；L49-51 用 f-string 拼出 `name/D/|均值|/‖恒定‖/‖跨帧变化‖/相对变化/逐维std mean/零变化维占比`。
- 调用: 被 `main()` 在 L181 的循环中每层调用一次；无其它模块引用。
- 置信度: 已确认

### S:main  [L54-216]
- 类型: function
- 签名: `main()`
- 作用: 命令行入口：装载 ckpt 与配置、建对手/信念链路、跑 `--frames` 帧确定性 rollout 并逐帧采集 value 通路（含 GRU 候选 pre-activation）与 GAE 回报，最后打印逐层统计、ReLU 存活率与多种 EV/vstd 口径。
- 参数: 无（argparse 参数如下）。
  - `--ckpt` (str, required): checkpoint 路径（L56）。
  - `--run-dir` (str, default=None): 含 `config.json` 的目录，缺省取 ckpt 所在目录（L57、L65）。
  - `--frames` (int, default=900): rollout 帧数（L58）。
  - `--seed` (int, default=7): 种子（L59）。
  - `--device` (str, default="cpu"): 设备，CUDA 不可用时回落 cpu（L60、L75）。
  - `--tag` (str, default=""): 打印用的标签（L61）。
- 返回: 无（隐式 None）。
- 实现: L64-74 与 `_probe_value_collapse.main` 同构：`TrainConfig.resolve("economy")` → 读 `config.json` 逐键 `setattr`（`hasattr` 过滤、异常静默）→ 设备回落判定。L76-81 `solo_env`、按 `BeliefInference(...).encode(None,None)` 长度定 `bdim`、`load_checkpoint(hidden_dim/plan_dim/belief_dim)`、`to_device(device).eval()`；L82-84 打印标签与 `value_independent`/`value_bypass`/`gamma`/`lam`。L86-90 打印**参数表中名字含 `value` 的每个参数**：形状、float 范数，且 1 维参数额外打印均值。L92-100 同构地另建一个 `FollowerPolicy` 对手（沿用主策略的 `value_bypass`/`value_independent`，L95-96）、`load_state_dict` 同步（L98）、包成 `FollowerOpponent(deterministic=True)` 挂到 `env.opponent`；`resolve_deck_set` 的结果被丢弃（L92 `_, _ = ...`）。L102-105 建 belief、`env.reset(seed)`、`belief.reset(env.deck1)`、`hidden=None`。L107-108 初始化 `buf`（11 个层名）与 `per_ep`；L110 局内缓冲 `ep_rew/ep_val/ep_term/ep_trunc`。主循环 L111-176（`torch.no_grad()`）：L113-119 逐帧 `bp.plan` → `belief.encode` → `plan.to_vector()` → `_encode_parts` → `gru_cell` 递推（首帧零向量）。L120-126 **手工复算 GRU 候选 pre-activation**：`gi = enc @ weight_ih.T + bias_ih`、`gh = h @ weight_hh.T + bias_hh`、`Hd = hidden_size`、`n_pre = gi[:,2Hd:] + sigmoid(gi[:,:Hd] + gh[:,:Hd]) * gh[:,2Hd:]`（即 reset-gate 加权后的候选 tanh 输入，未取 tanh）。L128-139 逐层复算并采集：`pre_ln = value_enc_fc(fused)`、`relu_ln = relu(pre_ln)`、`post_ln = value_enc_ln(relu_ln)`；若 `pol.value_independent` 为真则用 `value_head_mlp[0]`→ReLU→`value_head_mlp[2]` 得 `val_t` 并把 MLP0 后张量存入 `buf["mlp2_pre"]`（L131-136），否则把 `mlp0_pre=mlp0_post=post_ln` 且 `val_t = pol._value_from(enc, h, fused)`（L137-139）——**非独立头的分支下 MLP0 前/后字段实际等于 `post_ln`，并非真正的 MLP0 张量**。L140-149 把 fused/pre_ln/relu_ln/post_ln/mlp0_pre/mlp0_post/value/enc/h/gru_pre 逐帧入 buf。L151-154 预置局内 rew/val/term/trunc；L155-157 确定性 `pol.act` 与 `env.step`；L158-160 回填；L161 `belief.update`。L162-176 局结束：非终止时用 `pol.value` 算 bootstrap `last_val`，否则 0.0（L163-167）；`PPOTrainer.compute_gae(ep_rew, ep_val, ep_term, gamma, lam, truncated=ep_trunc, last_value=last_val)` 只留 `ret`（L168-170，`_` 丢弃 adv）；存 `{"R","V"}` 到 `per_ep`（L171-172）；`env.reset(seed=a.seed + t)` 重开并清状态（L173-176）。L178-181 打印逐层行（对 buf 中除 value 外的 9 个键调 `layer_stats`）；L182-184 打印 value 的 mean/std/min/max 与唯一值数；L185-187 当 `value_independent` 为真时打印 `MLP2 bias` 并注明「value 与 bias 相等 ⇒ MLP0 全零」（该断言含义见 L186-187，脚本按字面打印）；L188-190 用 `(relu_ln>0).any(axis=0).mean()` 报 ReLU(value_enc) 存活维占比与逐帧存活率；L191-196 用 `(mlp0_post>0).mean()` 报 MLP0 ReLU 逐帧存活率、全零帧占比、以及 MLP0 pre 的跨帧逐维 std 均值与绝对值均值。L198-206 拼接所有局 R/V 为同长，计算逐局均值 `ep_means`、局内 `within`（R 减局均值）、`mse`、`var`、`mse_ep`（每局常数预测局均值的 MSE）。L207-216 打印 `R 全网 std/mean`、局间/局内 std、`MSE(v,R)`、`Var(R)`、`EV(池化)=1-mse/var`、`EV(局均值预测)=1-mse_ep/var`、`vstd/rstd(池化)`、`vstd/局内rstd`（L214 起仅在 `V_all.std() > 1e-12` 时打印 `corr(V,R)`）、以及「局内 MSE(v, R−局均值)」。
- 调用: 由文件末尾 `if __name__ == "__main__": main()`（L219-220）调用；本文件无被 import 记录（全仓 `_probe_value_path` 只出现在自身与文档）。它调用了 `TrainConfig.resolve`、`solo_env`、`resolve_deck_set`、`BeliefInference`/`encode`/`reset`/`state`、`load_checkpoint`、`BeliefPlanner.plan`、`FollowerPolicy._encode_parts`/`gru_cell`/`_value_from`/`act`/`value`/`named_parameters`/`state_dict`/`to_device`/`eval`、`FollowerOpponent`、`PPOTrainer.compute_gae`、`env.reset`/`step`/`get_action_mask`。被调符号均在 `rl/` 下存在（`rl/follower.py:67/268/303/433/704`、`rl/ppo.py:168`、`rl/train_solo.py:107/157`、`rl/belief_planner.py:772`）。
- 置信度: 已确认（`value_independent=False` 时 `mlp0_pre/mlp0_post` 等于 `post_ln`、`mlp2_pre` 缺项属代码事实；未推断作者意图）

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/_probe_value_collapse.py | 3 | 3 |
| scripts/judge_anchor_blocks.py | 6 | 6 |
| scripts/question_bank_poc.py | 6 | 6 |
| scripts/_probe_value_path.py | 2 | 2 |
| **合计** | **17** | **17** |

COVERAGE_OK G029 files=4 symbols=17
