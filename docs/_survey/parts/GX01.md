# 组 GX01 文件级分析

> 计数规则（本组统一口径，便于复算）：符号 = ①`function` 声明/函数表达式；②每一个箭头函数（含嵌套、含 `.catch(()=>{})`、含 `setTimeout` 回调这类单行箭头）；③对象字面量方法简写（`onEnter(args){}`）；④不可再拆的**顶层可执行段**（仅对 `l1_upgrade.ps1` / `run_probe_v3.sh` 使用）。
> `cdp_leaderboard.js` 的 `EXTRACT_JS` 是模板字符串，其内部的两处箭头（L12、L30、L35）按"文件内出现的箭头函数"一并计入，并在条目里注明是**注入到浏览器页面的代码**。
> CDP = Chrome DevTools Protocol：**已确认**（不是猜测）——这些脚本使用 CDP 的 HTTP 发现端点 `PUT /json/new`、`GET /json/close/<id>`，再连 `webSocketDebuggerUrl` 发送协议方法 `Page.enable` / `Page.navigate` / `Runtime.enable` / `Runtime.evaluate`（见各文件 L10-11、L26-31、L37、L43-46、L53）。

## F:src/clasher_new/hook_raw_capture.js
- 行数: 240
- 模块作用: Frida 注入用的 JS 脚本（不是 Node/Python，通过 `frida-gum` 全局 API 在目标进程内运行）。它按硬编码偏移调用 Android 游戏 `libg.so` 内的原生函数，读取战斗实体列表、手牌、圣水、塔血与玩家身份，并用 `send()` 以 100 ms 周期把 `runtime_snapshot` 事件推给宿主（Python 侧 `src/clasher_new/run_raw_capture.py` 把本文件读成字符串后 `create_script`）。依据：L1 的 `frida-gum` 引用注释、L4-L17 的偏移表、L116-L237 的 `setImmediate` 主体、L201-L222 的 `send({event:'runtime_snapshot',...})`；调用方见 `run_raw_capture.py:20`（`source = (current_dir / "hook_raw_capture.js").read_text()`）与 `run_raw_capture.py:60-62`（`session.create_script(source)` / `script.on("message", on_message)` / `script.load()`）。
- 模块级 docstring 首行: 无（本文件只有 `/// <reference types="frida-gum" />` 三斜线引用注释与 `'use strict';`）
- 主要导入: 无 `import`/`require`；依赖 Frida 注入的全局对象 `Process`、`NativeFunction`、`Interceptor`、`ptr`、`send`（L19、L87、L117、L120、L130、L137、L145）
- 关键模块级常量:
  - `SNAPSHOT_INTERVAL_MS = 100` — L4；快照定时器的周期（毫秒），L223 传给 `setInterval`
  - `OFFSETS = {tick:0x8d97a0, getManager:0xbb6508, getBattleObj:0xbb93e0, getHpState:0xbb05ac, getTowerHp:0xcf719c, getAccountContext:0xc85c28, getLocalAccountObject:0xc8a954, getCardByHandSlot:0xf37da0, getNextCard:0xf3e1a8, cardObjectToData:0xfef788, inboundDispatch:0x1386fc8}` — L5-L17；`libg.so` 内 11 个函数的静态偏移（基址在 L118 取得后相加）
  - `holder = ptr(0)` — L19；可变持有者指针（游戏 runtime holder 对象），由 L130-L135 的 `tick` 钩子每帧刷新，L158 判空后使用
  - L20-L21 声明的未初始化全局：`getManager, getBattleObj, getHpState, getTowerHp, getAccountContext, getLocalAccountObject`（L20）与 `getCardByHandSlot, getNextCard, cardObjectToData`（L21）——在 L120-L128 被赋值为 `NativeFunction` 包装
- 顶层数据表/字典: `OFFSETS`（见上，键=函数名、值=模块内偏移）；另有内联字典 `entity_layout`（L228-L235：`registry:0x08, collection:0x40, data:0x08, count:0x14, owner:0x18, hp_component:0x10`），在 `capture_ready` 事件里回传给宿主，声明实体读取链路各段偏移。
- 运行方式: 不作为独立程序运行；由 `src/clasher_new/run_raw_capture.py` 读成字符串经 Frida 注入 Android 目标进程（`run_raw_capture.py:20/60-62`；`adb forward tcp:27042`、`frida-server` 见同文件 L16、L23-L26）。

### S:p64  [L23-26]
- 类型: function
- 签名: `function p64(address, offset)`
- 作用: 读一个 8 字节指针：返回 `address+offset` 处的指针值，越界/不可读时返回空指针 `ptr(0)`。
- 参数:
  - `address` (无注解): 基地址（Frida `NativePointer`）
  - `offset` (无注解): 字节偏移（数字）
- 返回: `NativePointer`；异常路径返回 `ptr(0)`
- 实现: L25 在一个 `try/catch` 中调用 `address.add(offset).readPointer()`；任何读取异常被 `catch (_)` 吞掉并返回 `ptr(0)`（L25）。整个函数体只有这一个表达式，无分支逻辑（除异常分支）。该容错设计使调用方可以直接用 `.isNull()` 判空而不是层层 try（如 L53、L102）。
- 调用: 被 `readEntity`（L51-L52）、`readBattleEntities`（L80-L82）、`readPlayerIdentity`（L98）、快照回调（L159-L160、L172、L211）调用；它调用 `NativePointer.add` / `.readPointer` 与 `ptr`（Frida 全局）——已确认（同文件内调用点已逐一核对）。
- 置信度: 已确认

### S:i32  [L27-30]
- 类型: function
- 签名: `function i32(address, offset)`
- 作用: 读 32 位有符号整数（`address+offset`），读取失败返回 `null`。
- 参数:
  - `address` (无注解): 基地址
  - `offset` (无注解): 字节偏移
- 返回: `number | null`
- 实现: L29 在 `try/catch` 内执行 `address.add(offset).readS32()`，异常时 `catch (_)` 返回 `null`（L29）。是无副作用的纯读取封装。
- 调用: 被 `readEntity`（L41-L48、L54-L55、L70）、`readBattleEntities`（L83）、`readPlayerIdentity`（L101）、快照回调（L172-L173、L207、L210、L214）调用；内部调用 `NativePointer.readS32`——已确认。
- 置信度: 已确认

### S:u32  [L31-34]
- 类型: function
- 签名: `function u32(address, offset)`
- 作用: 读 32 位无符号整数（`address+offset`），读取失败返回 `null`。
- 参数:
  - `address` (无注解): 基地址
  - `offset` (无注解): 字节偏移
- 返回: `number | null`
- 实现: L33 在 `try/catch` 内执行 `address.add(offset).readU32()`，异常返回 `null`（L33）。与 `i32` 结构相同，仅读取方法不同；用于读玩家/账户 id 这类非负标识（L108、L110）。
- 调用: 被 `readPlayerIdentity` 在 L108（遍历 roster 读每个账户 id）和 L110（读本地账户 id）调用——已确认。
- 置信度: 已确认

### S:f32  [L35-38]
- 类型: function
- 签名: `function f32(address, offset)`
- 作用: 读 32 位浮点数（`address+offset`），读取失败返回 `null`。
- 参数:
  - `address` (无注解): 基地址
  - `offset` (无注解): 字节偏移
- 返回: `number | null`
- 实现: L37 在 `try/catch` 内执行 `address.add(offset).readFloat()`，异常返回 `null`（L37）。本文件内只用于读战斗时钟 `uiState+0x220`（L208）。
- 调用: 仅被快照回调 L208 调用（`battle_clock_220`）——已确认（全文仅此一处引用）。
- 置信度: 已确认

### S:readEntity  [L40-73]
- 类型: function
- 签名: `function readEntity(entity)`
- 作用: 从一个战斗实体对象按固定偏移抽出一组字段（种类/阵营/两组坐标/卡牌 id/等级/血量），组成供宿主消费的普通 JS 对象；HP 链路读不到时返回 `null`。
- 参数:
  - `entity` (无注解): 实体对象指针
- 返回: `object | null`（对象含 `event:'entity_observed'` 与 `kind_30`…`max_hp_18` 等字段）
- 实现: L41-L48 连续用 `i32` 读 `+0x30` 种类、`+0x78` 阵营、`+0x7c/+0x80` 与 `+0x84/+0x88` 两组坐标、`+0xac` 卡牌 id、`+0x120` 等级索引；L50-L55 走"被动 HP 路径"：`+0x18` 取 owner、owner`+0x10` 取 component，二者任一为空指针即 `return null`（L53），否则读 component`+0x10`/`+0x14` 得到 `hp`/`maxHp`；L57-L72 组装返回对象，额外用 `i32(component, 0x18)` 读 `max_hp_18` 并把指针 `.toString()` 成字符串；`source` 字段固定为 `'battle_entity_list'`（L71）。注意：返回对象带 `event` 字段，但本文件从不把它当顶层事件 `send`，而是塞进数组随 `runtime_snapshot` 一起发出（L220-L221）——已确认（全文检索 `send({` 仅 L145 与 L201）。
- 调用: 被 `readBattleEntities` L89 调用；它调用 `i32`（L41-L48、L54-L55、L70）与 `p64`（L51-L52）——已确认。
- 置信度: 已确认

### S:readBattleEntities  [L75-93]
- 类型: function
- 签名: `function readBattleEntities(hpState)`
- 作用: 从 `hpState` 出发沿"registry → collection → 指针数组"三级解引用，遍历实体集合，逐个调用 `readEntity` 收集观察结果。
- 参数:
  - `hpState` (无注解): 血量状态对象指针（由 `getHpState(manager)` 得到，见 L194）
- 返回: `{ valid: boolean, entities: Array }`；`hpState` 为空指针时返回 `{valid:false, entities:[]}`（L76）
- 实现: L76 先判 `hpState.isNull()` 短路返回；L80-L83 按注释说明的链路读 `hpState+0x08` → registry、`registry+0x40` → collection、`collection+0x08` → 指针数组起点、`collection+0x14` → 有符号元素个数；L85-L91 用 `for (index=0; index<count; index++)` 以 `index * Process.pointerSize` 为步长取指针，空指针 `continue` 跳过（L88），否则 `readEntity` 后非 `null` 才 `push`（L89-L90）；L92 返回 `{valid:true, entities}`。注意：`count` 未做上界校验，只依赖 `i32` 读取失败时的 `null`（`null < n` 为假，循环不进入）。
- 调用: 被快照回调 L200 调用（结果经 L220-L221 的 `entity_list_valid` / `entities` 发出）；它调用 `p64`、`i32`、`readEntity`——已确认。
- 置信度: 已确认

### S:readPlayerIdentity  [L95-114]
- 类型: function
- 签名: `function readPlayerIdentity(manager)`
- 作用: 取本局玩家名册并定位"本地账户 id"，返回 `[本地id, 名册id数组]`；任何一步失败返回 `null`。
- 参数:
  - `manager` (无注解): 由 `getManager()` 得到的管理器指针
- 返回: `[number, number[]] | null`
- 实现: L97 由 `getBattleObj(manager)` 取战斗对象、L98 读 `battle+0xa8` 得 roster；L99-L100 通过 `getAccountContext()` 与 `getLocalAccountObject(accountContext, 0, 0)` 取本地账户对象；L101 读 `roster+0x60` 作为玩家数；L102 做合法性闸门：`battle`/`roster`/`localAccount` 任一为空或 `count < 2 || count > 6` 即返回 `null`；L104 取本地 id 指针 `localAccount+0x20`；L106-L109 循环 `count` 次，每次读 `roster+0x30+index*8` 的账户指针并在偏移 `4` 处 `u32` 读 id，压入 `players`；L110 返回 `[u32(localId,4), players]`。整个函数体包在 `try/catch` 中，任何异常返回 `null`（L96、L111-L113）。
- 调用: 被快照回调 L217 调用（结果作为 `player_identity` 字段发出）；下游 `run_raw_capture.py:51-55` 读取该字段，若 `local_id in id_lst` 则用 `id_lst.index(local_id)` 设定本地玩家索引。它调用 `getBattleObj`、`getAccountContext`、`getLocalAccountObject`、`p64`、`i32`、`u32`——已确认。
- 置信度: 已确认

### S:setImmediate 回调（匿名）  [L116-237]
- 类型: function（`function ()` 表达式，作为 `setImmediate` 参数）
- 签名: `function ()`
- 作用: 注入初始化主体：解析 `libg.so` 基址、把 11 个偏移包装成 `NativeFunction`、安装两个 `Interceptor.attach` 钩子、启动 100 ms 快照定时器，并发出一次 `capture_ready` 广播。
- 参数: 无
- 返回: 无（返回值被 `setImmediate` 忽略）
- 实现: L117-L118 用 `Process.findModuleByName('libg.so')` 取模块并取其 `base`（未判空，模块不存在会在 L120 抛错）；L120-L128 依次 `new NativeFunction(base.add(OFFSETS.X), 返回类型, 参数类型数组)` 构造 9 个可调用包装（`getTowerHp` 返回 `'int'`，其余 `'pointer'`）；L130-L135 钩 `OFFSETS.tick`，`onEnter` 把 `args[0]` 存入全局 `holder`；L137-L155 钩 `OFFSETS.inboundDispatch`，在 `onEnter` 里过滤消息；L157-L223 注册 100 ms 快照回调；L225-L236 `send({event:'capture_ready', offsets: OFFSETS, entity_layout:{...}})` 把偏移表与实体布局回传宿主。注意：`const module` / `const base` 为块内常量，副作用仅在本次调用内生效并写全局变量 L20-L21。
- 调用: 被 Frida 注入后由 `setImmediate` 立即执行（L116）；它调用 `Process.findModuleByName`、`NativeFunction`、`Interceptor.attach`、`setInterval`、`send`，并被 `run_raw_capture.py` 经 `create_script(source)` + `script.load()`（`run_raw_capture.py:60-62`）触发——已确认。
- 置信度: 已确认

### S:setImmediate 回调.tick 钩子 onEnter  [L132-134]
- 类型: function（`Interceptor.attach` 选项对象的方法简写）
- 签名: `onEnter(args)`
- 作用: 在 `tick` 函数入口记录当前游戏 runtime holder 对象指针。
- 参数:
  - `args` (Frida `InvocationArguments`，无注解): 被钩函数的参数数组，`args[0]` 即 holder
- 返回: 无
- 实现: 只有 L133 一句 `holder = args[0];`，写入 L19 声明的模块级变量；无判空、无分支。该值随后被快照回调 L158 判 `isNull()` 后使用。注释 L131 说明 `holder` 即"current game runtime holder object"。
- 调用: 由 Frida 在每次 `libg.so+0x8d97a0` 被调用时回调；结果被快照回调 L159-L160 消费——已确认。
- 置信度: 已确认

### S:setImmediate 回调.inboundDispatch 钩子 onEnter  [L139-154]
- 类型: function（`Interceptor.attach` 选项对象的方法简写）
- 签名: `onEnter(args)`
- 作用: 在入站消息分发函数入口拦截消息号 23877，把其明文载荷以 `inbound_plaintext` 事件（含二进制 data）发给宿主。
- 参数:
  - `args` (Frida `InvocationArguments`，无注解): `args[1]`=消息号、`args[2]`=版本、`args[3]`=载荷指针容器、`args[4]`=载荷长度
- 返回: 无
- 实现: L140 若 `args[1].toInt32() !== 23877` 直接 `return`；L141 读长度，L142 长度 `<=0` 或 `>64*1024` 时丢弃；L144 从 `args[3].readPointer()` 起 `readByteArray(payloadLength)` 取载荷字节；L145-L151 `send(对象, payload)`——第二个参数是 Frida 的二进制 data 通道，对象只含元信息（`event`、`t_ms`、`message_id:23877`、`version`、`length`）；L152-L153 空的 `catch (_) {}` 静默吞掉读取/发送异常。注释 L138 说明只处理 23877。
- 调用: 由 Frida 在 `libg.so+0x1386fc8` 入口回调；`send` 的两段载荷由 `run_raw_capture.py:42-47` 的 `on_message` 接收（该处理器只读 `message["payload"]`，未消费 `data` 二进制通道——源码事实）——已确认。
- 置信度: 已确认

### S:setInterval 快照回调（匿名）  [L157-223]
- 类型: function（`function ()` 表达式，作为 `setInterval` 参数）
- 签名: `function ()`
- 作用: 每 100 ms 抓取一次运行态：手牌 4 槽、下一张牌、圣水、战斗时钟、双方国王塔血量、玩家身份与全部战斗实体，组装成 `runtime_snapshot` 事件发给宿主。
- 参数: 无
- 返回: 无
- 实现: L158 若 `holder` 为空直接返回；L159-L160 取 `holder+0xa8` 的 `uiState` 与 `holder+0x290` 的 `handModel`；L163-L178 对 4 个手牌槽循环：`getCardByHandSlot(handModel, slot)` → 非空则 `cardObjectToData(cardObject, 0)`，推入 `{slot, card_object, data_pointer, deck_index_220: i32(p64(handModel,0x220), slot*4), data_id_40: i32(data,0x40)}`，异常时推入全 null 的占位对象（L176）；L180-L186 try 内取 `nextCardObject = getNextCard(handModel)` 与其 data（异常被空 catch 吞掉，遗留空指针）；L188-L198 try 内取 `manager`、`hpState` 与 `upperKingHp/lowerKingHp = getTowerHp(hpState, 0/1)`；L200 调 `readBattleEntities(hpState)`；L201-L222 组装并 `send` 一个大对象，含 `own_elixir_1e0`、`battle_clock_220: f32(uiState,0x220)`、`hand`、`next_queue_count_23c`、`next_queue_deck_index_230`、`next_card_data_id_40`、`player_identity`、`entity_list_valid`、`entities`。返回类型由 `setInterval` 忽略。
- 调用: 由 `setInterval(..., SNAPSHOT_INTERVAL_MS)`（L223）每 100 ms 调用一次；它调用 `p64/i32/f32`、`getCardByHandSlot`、`cardObjectToData`、`getNextCard`、`getManager`、`getHpState`、`getTowerHp`、`readBattleEntities`、`readPlayerIdentity`、`send`。事件接收方为 `run_raw_capture.py:48-55`（`payload['event'] == 'runtime_snapshot'` 分支）——已确认。
- 置信度: 已确认

### S:keepAlive  [L240]
- 类型: function（具名函数表达式，作为 `setInterval` 参数）
- 签名: `function keepAlive() {}`
- 作用: 空函数，作为 1 秒周期的定时器占位，让注入脚本保持存活并继续接收事件。
- 参数: 无
- 返回: 无
- 实现: 函数体为空（L240），外层 `setInterval(function keepAlive() {}, 1000)` 每 1000 ms 调用一次。本行注释 L239 说明意图是"set a placeholder that keeps the script running and listens to activities"；未观察到它做任何清理或状态变更。
- 调用: 仅被 L240 的 `setInterval` 调用；无其它调用者——已确认。
- 置信度: 已确认

## F:scripts/cdp_evo.js
- 行数: 64
- 模块作用: Node 脚本，用 CDP 驱动一个已开启调试端口的 Chrome：为每个 URL 新开标签页、导航、等页面 `readyState` 到 `complete` 再缓冲 3 秒，然后取 `document.body.innerText` 落盘为文本文件。依据：L2-L5 头部注释与 L9-L57 的 `extractPage` 流程、L6 的 CDP 端点常量、L59-L64 的 CLI 入口循环。
- 模块级 docstring 首行: `/** CDP 页面文本抽取器 v2：Page.navigate 驱动（兼容 Chrome 152）`（L2）；其 L4 声明的输出路径 `/tmp/cdp_page_<n>.txt` 与代码实际写入的 `docs/_evo_<idx>.txt`（L54）**不一致，头部注释是复制后未更新的过期说明**（源码事实）。
- 主要导入: `fs`（L7，`require('fs')`）；全局 `fetch`、`WebSocket`、`console`、`process`（L10、L11、L60——均未 import，是否可用取决于 Node 运行时版本：无法确认（原因：仓库内未见 Node 版本声明或 WebSocket polyfill，本文件也不含 `require('ws')`））
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L6；CDP 调试端点（HTTP 发现 + WebSocket 基址）
- 顶层数据表/字典: 无（函数内的 `pending = new Map()`（L13）与 `waiters = []`（L14）是每次调用的局部状态，非模块级表）
- 运行方式: `node scripts/cdp_evo.js <url1> <url2> ...`（L60 `process.argv.slice(2)`）；须先有 Chrome 在 `172.28.144.1:9222` 监听 CDP（L6）。

### S:extractPage  [L9-57]
- 类型: async_function
- 签名: `async function extractPage(url, idx)`
- 作用: 用 CDP 打开一个 URL，等待加载与水合完成后抓取页面可见文本，写入 `docs/_evo_<idx>.txt`，并打印计数信息。
- 参数:
  - `url` (无默认值): 要导航到的地址，L30 传给 `Page.navigate`
  - `idx` (无默认值): 输出文件名与日志的序号
- 返回: `Promise<void>`（无显式 return；末尾 L56 `console.log` 后自然结束）
- 实现: L10 用 `PUT /json/new` 新建标签页并解析 JSON 取 `tab`；L11 用 `tab.webSocketDebuggerUrl` 建 `WebSocket`；L12-L19 定义自增 id 的 CDP 请求封装 `send`（把 promise 的 resolve 存进 `pending`，回包按 id 兑现）；L20-L24 的 `ws.onmessage` 优先按 id 兑现 pending，其次在 `Page.loadEventFired` 时清空并触发 `waiters`；L25 等 `ws.onopen`；L26-L27 依次 `Page.enable`、`Runtime.enable`；L29-L31 注册 `loaded` promise 后 `Page.navigate`，用 `Promise.race([loaded, 20s 超时])` 等待（结果存入 `nav`，**后续从未使用**）；L34-L40 最多 20 次、每次隔 1200 ms 轮询 `document.readyState`，读到 `'complete'` 即 break（异常注释为"跳转期忽略"）；L41 再固定等 3000 ms 给 SPA 水合；L43-L46 求页面 `href/title/textLen` 摘要（存入 `info`）；L47-L51 取 `document.body.innerText`，失败则把文本设为 `'EVAL_FAIL: ' + e.message`；L52-L53 关闭 WebSocket 并 `GET /json/close/<tab.id>` 关闭标签页（`.catch(()=>{})` 忽略失败）；L54-L56 以 utf-8 写 `/mnt/e/clash-royale-simulator-main/docs/_evo_${idx}.txt` 并打印 URL/路径/字符数/摘要。
- 调用: 被 L62 的 CLI 循环 `await extractPage(urls[i], i + 1)` 调用；它调用 CDP 方法 `Page.enable`、`Runtime.enable`、`Page.navigate`、`Runtime.evaluate` 与 HTTP 端点 `/json/new`、`/json/close/<id>`，以及 `fs.writeFileSync`——已确认。
- 置信度: 已确认

### S:extractPage.send（箭头函数）  [L15-19]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: CDP 请求封装：给请求分配自增 id、把 resolve 回调登记到 `pending`，然后把 `{id, method, params}` 经 WebSocket 发出。
- 参数:
  - `method` (无默认值): CDP 方法名，如 `Page.navigate`
  - `params` (默认 `{}`): 方法参数对象
- 返回: `Promise`（由 id 对应的回包 `m.result` 兑现）
- 实现: L16 `const i = ++id;` 取新 id；L17 `pending.set(i, res)` 登记 resolve；L18 `ws.send(JSON.stringify({id: i, method, params}))` 发出；L20-L23 的回包处理按 id 兑现并删除登记。注意 L15 的 `new Promise((res) => ...)` 执行器只接受 `res`，**没有 reject 通道**，因此协议错误或连接断开会让 promise 永久悬挂。
- 调用: 由 `extractPage` 内部在 L26-L27、L30、L37、L43-L46、L49 调用；消费方是 `ws.onmessage`（L20-L24）——已确认。
- 置信度: 已确认

### S:extractPage.send.executor（箭头函数）  [L15-19]
- 类型: function（箭头函数，`new Promise` 的执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: Promise 执行器，实际完成 id 分配、登记与发送三步。
- 参数:
  - `res` (无默认值): Promise 的 resolve 函数
- 返回: 无（执行器返回值被忽略）
- 实现: L16-L18 三句：自增 id、把 `res` 存入 `pending`、把 JSON 文本写入 WebSocket。它同步执行、不 await 任何东西，所以 `send` 返回的 promise 一定在消息发出后才挂起等待回包。该箭头与 L15 的 `send` 箭头的行区间重叠（同属 L15-L19 复合表达式），是嵌套关系。
- 调用: 由 `new Promise`（L15）在 `send` 被调用时立即执行——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage（箭头函数）  [L20-24]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 分发 CDP 回包：带 `id` 的回包兑现对应 promise；`Page.loadEventFired` 事件唤醒所有 `waiters`。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件，`ev.data` 为 JSON 文本
- 返回: 无
- 实现: L21 `JSON.parse(ev.data)`；L22 若 `m.id` 存在且 `pending` 中有该 id，则取出并调用 resolve（传 `m.result`，**不透传 error**）后 `pending.delete` 并 `return`；L23 若 `m.method === 'Page.loadEventFired'`，用 `waiters.splice(0)` 取走全部等待者并逐个调用（L23 内部还有 `forEach` 的 `(f) => f()` 箭头）。无 id 且非加载事件的其它 CDP 事件被静默忽略。
- 调用: 由 WebSocket 运行时在收到消息时回调；它调用 `JSON.parse`、`pending.get`、`waiters.splice`——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage.forEach 回调（箭头函数）  [L23]
- 类型: function（箭头函数，`Array.prototype.forEach` 回调）
- 签名: `(f) => f()`
- 作用: 逐个调用被 `waiters.splice(0)` 取出的 resolve 函数。
- 参数:
  - `f` (无默认值): 一个等待者的 resolve 回调
- 返回: `f()` 的返回值（forEach 忽略）
- 实现: 单表达式箭头；外层 `waiters.splice(0)` 保证取走并清空数组，因此一次 `Page.loadEventFired` 只唤醒当前批等待者（L23）。配合 L29 的 `loaded` promise，使首次加载事件能兑现 `Promise.race`。
- 调用: 由 L23 的 `forEach` 调用——已确认。
- 置信度: 已确认

### S:extractPage.ws.onopen executor（箭头函数）  [L25]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => { ws.onopen = r; }`
- 作用: 把 `ws.onopen` 直接设为 resolve，从而让该 promise 在 WebSocket 建连完成时兑现。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L25 单句赋值。这是"等待 onopen"的惯用写法；因为 `onmessage` 已在 L20 设过，两者互不覆盖。注意若建连失败该 promise 无 reject 路径。
- 调用: 由 `await new Promise(...)`（L25）执行——已确认。
- 置信度: 已确认

### S:extractPage.loaded executor（箭头函数）  [L29]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => waiters.push(r)`
- 作用: 把加载等待的 resolve 压入 `waiters`，供 `Page.loadEventFired` 触发。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: `waiters.push` 的返回值（新长度；被 Promise 构造忽略）
- 实现: L29 单表达式；由于执行器同步执行，`waiters` 在 L30 `Page.navigate` 发出前就已登记 resolve，避免事件先于注册到达的竞态。
- 调用: 由 L29 的 `new Promise` 执行；其 resolve 由 L23 的 forEach 调用——已确认。
- 置信度: 已确认

### S:extractPage 导航超时定时器（箭头函数）  [L31]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 20000)`
- 作用: 为 `Page.loadEventFired` 等待提供 20 秒兜底：超时后以 `undefined` 兑现 promise，使 `Promise.race` 结束。
- 参数:
  - `r` (无默认值): `Promise.race` 内层 promise 的 resolve
- 返回: `setTimeout` 的 timer 句柄（被 Promise 构造忽略）
- 实现: L31 单表达式；与 L29 的 `loaded` 用 `Promise.race([loaded, ...])` 竞争，先到者决定 `nav` 的值。超时情况下也不会抛错，只会继续走后续轮询（L34-L40）。
- 调用: 由 L31 的 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage readyState 轮询延时（箭头函数）  [L35]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 1200)`
- 作用: 在 readyState 轮询循环中每次等待 1200 ms。
- 参数:
  - `r` (无默认值): 延时 promise 的 resolve
- 返回: `setTimeout` 句柄（被忽略）
- 实现: L35 单表达式，作为 L34-L40 `for (let i = 0; i < 20; i++)` 循环体的第一步；最多 20 次 × 1200 ms ≈ 24 s 的上限。L38 读到 `'complete'` 时 `break`，L39 的 catch 注释"跳转期忽略"表明导航期间的 evaluate 失败被吞。
- 调用: 由 L35 的 `new Promise` 在每次循环迭代执行——已确认。
- 置信度: 已确认

### S:extractPage SPA 水合延时（箭头函数）  [L41]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 3000)`
- 作用: 在 readyState 到 complete 后再固定等待 3000 ms，给 SPA 客户端渲染留时间。
- 参数:
  - `r` (无默认值): 延时 promise 的 resolve
- 返回: `setTimeout` 句柄（被忽略）
- 实现: L41 单表达式，位于轮询循环之后、取摘要（L43）之前；这是固定等待、不随页面状态变化。
- 调用: 由 L41 的 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 关闭标签页 catch（箭头函数）  [L53]
- 类型: function（箭头函数，`Promise.prototype.catch` 回调）
- 签名: `() => {}`
- 作用: 吞掉 `fetch(/json/close/...)` 的失败，保证关闭标签页的异常不影响脚本结果。
- 参数: 无
- 返回: `undefined`
- 实现: L53 空函数体 `.catch(() => {})`；同一行的 `await fetch(\`${CDP}/json/close/${tab.id}\`)` 未检查响应状态码。
- 调用: 由 L53 的 `.catch` 在 fetch 失败时调用——已确认。
- 置信度: 已确认

### S:CLI 入口 IIFE（async 箭头函数）  [L59-64]
- 类型: async_function（`(async () => {...})()` 立即执行箭头函数，顶层可执行段）
- 签名: `(async () => { const urls = process.argv.slice(2); for (let i = 0; i < urls.length; i++) { try { await extractPage(urls[i], i + 1); } catch (e) { console.log(...) } } })()`
- 作用: 脚本主入口：按命令行参数顺序串行抽取每个 URL，单个失败只打印日志并继续。
- 参数: 无（参数取自 `process.argv`）
- 返回: `Promise<void>`
- 实现: L60 用 `process.argv.slice(2)` 取 URL 列表；L61-L63 `for` 循环串行 `await extractPage(urls[i], i + 1)`，`try/catch` 内失败打印 `[<n>] <url> 失败: <message>`（L62），不中断后续；无参数时循环体不执行、脚本静默结束。
- 调用: 模块加载时由自身 `()` 立即调用（L59/L64）；它调用 `extractPage`——已确认。
- 置信度: 已确认

## F:scripts/cdp_extract.js
- 行数: 64
- 模块作用: 与 `cdp_evo.js` 同源（逐行同构）的 CDP 页面文本抽取脚本：新开标签页 → `Page.navigate` → 轮询 readyState → 抓 `document.body.innerText` → 落盘。全文件唯一与 `cdp_evo.js` 不同的实质行是 L54 的输出路径前缀 `_page_`。依据：L9-L57 的 `extractPage` 流程、L54 的路径、L59-L64 的 CLI 入口。
- 模块级 docstring 首行: `/** CDP 页面文本抽取器 v2：Page.navigate 驱动（兼容 Chrome 152）`（L2）；L4 声明输出 `/tmp/cdp_page_<n>.txt`，代码实际写 `docs/_page_<idx>.txt`（L54），头部注释与实现不一致（过期说明，源码事实）。
- 主要导入: `fs`（L7）；全局 `fetch`、`WebSocket`、`console`、`process`（L10、L11、L60）均未 import——是否可用取决于 Node 运行时：无法确认（原因：本文件无 `require('ws')`，仓库内未见 Node 版本声明）
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L6；CDP 调试端点
- 顶层数据表/字典: 无（`pending`/`waiters` 为函数内局部状态，L13-L14）
- 运行方式: `node scripts/cdp_extract.js <url1> <url2> ...`（L60）；需目标 Chrome 在 `172.28.144.1:9222` 暴露 CDP（L6）。

### S:extractPage  [L9-57]
- 类型: async_function
- 签名: `async function extractPage(url, idx)`
- 作用: 用 CDP 打开指定 URL，等待加载/水合后抓取页面可见文本，写入 `docs/_page_<idx>.txt` 并打印信息。
- 参数:
  - `url` (无默认值): 导航目标，L30 传给 `Page.navigate`
  - `idx` (无默认值): 输出文件与日志的序号
- 返回: `Promise<void>`
- 实现: L10 `PUT /json/new` 建标签页；L11 连 `webSocketDebuggerUrl`；L12-L19 定义请求封装 `send`；L20-L24 回包分发（按 id 兑现 pending，`Page.loadEventFired` 唤醒 waiters）；L25 等 `ws.onopen`；L26-L27 `Page.enable` + `Runtime.enable`；L29-L31 注册 loaded 后导航，`Promise.race` 与 20 s 超时竞争（结果存 `nav`，未再使用）；L34-L40 最多 20 次 × 1200 ms 轮询 `document.readyState === 'complete'`；L41 固定再等 3000 ms；L43-L46 取 `{href,title,textLen}` 摘要到 `info`；L47-L51 取 `innerText`，失败置 `'EVAL_FAIL: ' + e.message`；L52-L53 关 WS、关标签页（catch 忽略）；L54-L56 写 `/mnt/e/clash-royale-simulator-main/docs/_page_${idx}.txt`（utf-8）并打印。
- 调用: 由 L62 的入口循环调用；它调用 CDP 方法与 `/json/new`、`/json/close/<id>`、`fs.writeFileSync`——已确认。
- 置信度: 已确认

### S:extractPage.send（箭头函数）  [L15-19]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: 把 CDP 请求编号后经 WebSocket 发出，并返回一个由对应回包兑现的 promise。
- 参数:
  - `method` (无默认值): CDP 方法名
  - `params` (默认 `{}`): 方法参数
- 返回: `Promise`（兑现值为回包 `m.result`）
- 实现: L16 自增 id；L17 把 resolve 存入 `pending`；L18 发送 `{id, method, params}` 的 JSON。无 reject 通道，连接异常会导致 promise 悬挂（L15 执行器只有一个参数）。
- 调用: 被 `extractPage` 在 L26-L27、L30、L37、L43-L46、L49 调用；回包由 L20-L24 处理——已确认。
- 置信度: 已确认

### S:extractPage.send.executor（箭头函数）  [L15-19]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: 执行器本体：分配 id、登记 resolve、发送请求三步同步完成。
- 参数:
  - `res` (无默认值): resolve 函数
- 返回: 无（被 Promise 构造忽略）
- 实现: L16-L18 三句同步语句；与 `send` 箭头共享 L15-L19 区间（嵌套关系）。无异步等待，所以 resolve 登记一定先于任何回包到达。
- 调用: 由 L15 `new Promise` 在 `send` 调用时立即执行——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage（箭头函数）  [L20-24]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 按 id 兑现 pending 请求；`Page.loadEventFired` 时清空并唤醒 `waiters`。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件
- 返回: 无
- 实现: L21 解析 JSON；L22 命中 id 时 resolve 并删除登记后 return（不透传 `m.error`）；L23 在加载事件分支里 `waiters.splice(0).forEach((f) => f())`；其余事件静默忽略。
- 调用: 由 WebSocket 运行时回调；调用 `JSON.parse`、`pending.get`、`waiters.splice`——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage.forEach 回调（箭头函数）  [L23]
- 类型: function（箭头函数，`forEach` 回调）
- 签名: `(f) => f()`
- 作用: 逐个执行等待者的 resolve，兑现加载等待。
- 参数:
  - `f` (无默认值): 等待者 resolve
- 返回: `f()` 的结果（forEach 忽略）
- 实现: 单表达式；配合 `splice(0)` 保证清空数组，只唤醒当批等待者（L23）。
- 调用: 由 L23 的 `forEach` 调用——已确认。
- 置信度: 已确认

### S:extractPage.ws.onopen executor（箭头函数）  [L25]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => { ws.onopen = r; }`
- 作用: 把 `ws.onopen` 设为 resolve，使建连完成即兑现等待。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L25 单句赋值，不覆盖 L20 已设的 `onmessage`；无 reject 路径。
- 调用: 由 L25 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage.loaded executor（箭头函数）  [L29]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => waiters.push(r)`
- 作用: 把加载等待的 resolve 压入 `waiters` 队列。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: `push` 返回的新长度（被忽略）
- 实现: L29 单表达式；在 L30 发出 `Page.navigate` 之前同步登记，消除事件先到的竞态。
- 调用: 由 L29 `new Promise` 执行；其 resolve 由 L23 调用——已确认。
- 置信度: 已确认

### S:extractPage 导航超时定时器（箭头函数）  [L31]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 20000)`
- 作用: 20 秒加载兜底，超时以 `undefined` 结束 `Promise.race`。
- 参数:
  - `r` (无默认值): 内层 promise 的 resolve
- 返回: timer 句柄（被忽略）
- 实现: L31 单表达式；与 L29 的 `loaded` 竞争，先到者决定 `nav` 的值（该值后续未被使用）。
- 调用: 由 L31 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage readyState 轮询延时（箭头函数）  [L35]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 1200)`
- 作用: 轮询循环每轮等待 1200 ms。
- 参数:
  - `r` (无默认值): 延时 promise 的 resolve
- 返回: timer 句柄（被忽略）
- 实现: L35 单表达式，位于 L34-L40 的 `for (i<20)` 循环首行；L38 命中 `'complete'` 时 break。
- 调用: 由 L35 `new Promise` 每轮执行——已确认。
- 置信度: 已确认

### S:extractPage SPA 水合延时（箭头函数）  [L41]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 3000)`
- 作用: 页面 complete 之后固定再等 3000 ms 供 SPA 渲染。
- 参数:
  - `r` (无默认值): 延时 promise 的 resolve
- 返回: timer 句柄（被忽略）
- 实现: L41 单表达式；与页面实际状态无关的固定等待。
- 调用: 由 L41 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 关闭标签页 catch（箭头函数）  [L53]
- 类型: function（箭头函数，`.catch` 回调）
- 签名: `() => {}`
- 作用: 静默忽略关闭标签页请求的失败。
- 参数: 无
- 返回: `undefined`
- 实现: L53 空函数体；`fetch` 的响应状态未被检查。
- 调用: 由 L53 的 `.catch` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口 IIFE（async 箭头函数）  [L59-64]
- 类型: async_function（顶层立即执行箭头函数）
- 签名: `(async () => { const urls = process.argv.slice(2); for (let i = 0; i < urls.length; i++) { try { await extractPage(urls[i], i + 1); } catch (e) { console.log(...) } } })()`
- 作用: 主入口：串行抽取 argv 中的每个 URL，逐个容错。
- 参数: 无（用 `process.argv`）
- 返回: `Promise<void>`
- 实现: L60 取参数；L61-L63 串行 await 并 `try/catch` 打印失败信息，不中断后续；无参数则空转结束。
- 调用: 模块加载即自调用；调用 `extractPage`——已确认。
- 置信度: 已确认

## F:scripts/cdp_hero.js
- 行数: 64
- 模块作用: 与 `cdp_evo.js`/`cdp_extract.js` 同构的 CDP 页面文本抽取脚本；唯一实质差异是 L54 的输出路径前缀为 `_hero_`（写 `docs/_hero_<idx>.txt`）。依据：L9-L57 流程、L54 路径、L59-L64 入口。
- 模块级 docstring 首行: `/** CDP 页面文本抽取器 v2：Page.navigate 驱动（兼容 Chrome 152）`（L2）；L4 声明的 `/tmp/cdp_page_<n>.txt` 与实际写出的 `docs/_hero_<idx>.txt`（L54）不一致（过期说明，源码事实）。
- 主要导入: `fs`（L7）；全局 `fetch`、`WebSocket`、`console`、`process`（L10、L11、L60）——可用性取决于 Node 运行时：无法确认（原因：本文件未 import WebSocket，仓库内未见 Node 版本声明）
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L6；CDP 调试端点
- 顶层数据表/字典: 无（`pending`/`waiters` 为 L13-L14 的局部状态）
- 运行方式: `node scripts/cdp_hero.js <url1> <url2> ...`（L60）；需目标 Chrome 在 `172.28.144.1:9222` 开 CDP（L6）。

### S:extractPage  [L9-57]
- 类型: async_function
- 签名: `async function extractPage(url, idx)`
- 作用: CDP 打开 URL、等待加载与水合后抓取页面可见文本，写入 `docs/_hero_<idx>.txt` 并打印摘要。
- 参数:
  - `url` (无默认值): 导航目标（L30）
  - `idx` (无默认值): 文件名/日志序号
- 返回: `Promise<void>`
- 实现: L10 `PUT /json/new` 取标签页；L11 建 WebSocket；L12-L19 请求封装 `send`；L20-L24 回包分发；L25 等 onopen；L26-L27 `Page.enable`、`Runtime.enable`；L29-L31 `Page.navigate` + `Promise.race`（20 s 超时，结果 `nav` 未使用）；L34-L40 最多 20 次 × 1200 ms 轮询 readyState；L41 再等 3000 ms；L43-L46 求 `{href,title,textLen}` 摘要；L47-L51 取 `innerText`（失败置 `EVAL_FAIL: ...`）；L52-L53 关 WS/标签页；L54-L56 以 utf-8 写 `/mnt/e/clash-royale-simulator-main/docs/_hero_${idx}.txt` 并打印。
- 调用: 由 L62 入口循环调用；调用 CDP 方法与 `/json/new`、`/json/close/<id>`、`fs.writeFileSync`——已确认。
- 置信度: 已确认

### S:extractPage.send（箭头函数）  [L15-19]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: 给 CDP 请求编号并发送，返回由对应回包兑现的 promise。
- 参数:
  - `method` (无默认值): CDP 方法名
  - `params` (默认 `{}`): 参数对象
- 返回: `Promise`（兑现值 `m.result`）
- 实现: L16 自增 id；L17 登记 resolve 到 `pending`；L18 发送 JSON。执行器无 reject，协议错误会悬挂。
- 调用: 被 `extractPage` 于 L26-L27、L30、L37、L43-L46、L49 调用——已确认。
- 置信度: 已确认

### S:extractPage.send.executor（箭头函数）  [L15-19]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: 执行 id 分配、resolve 登记与消息发送。
- 参数:
  - `res` (无默认值): resolve 函数
- 返回: 无（被忽略）
- 实现: L16-L18 三句同步语句，与 `send` 箭头构成 L15-L19 嵌套复合表达式。
- 调用: 由 L15 `new Promise` 立即执行——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage（箭头函数）  [L20-24]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 按 id 兑现 pending；`Page.loadEventFired` 时唤醒全部 waiters。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件
- 返回: 无
- 实现: L21 解析 JSON；L22 命中 id 则 resolve（传 `m.result`）、delete、return；L23 加载事件分支 `waiters.splice(0).forEach((f) => f())`；其它忽略。
- 调用: 由 WebSocket 运行时回调——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage.forEach 回调（箭头函数）  [L23]
- 类型: function（箭头函数，`forEach` 回调）
- 签名: `(f) => f()`
- 作用: 逐个兑现加载等待者的 promise。
- 参数:
  - `f` (无默认值): 等待者 resolve
- 返回: `f()` 的结果（被忽略）
- 实现: 单表达式，配合 `splice(0)` 的"取走即清空"语义（L23）。
- 调用: 由 L23 `forEach` 调用——已确认。
- 置信度: 已确认

### S:extractPage.ws.onopen executor（箭头函数）  [L25]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => { ws.onopen = r; }`
- 作用: 用 `ws.onopen` 作为 resolve，等待建连完成。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L25 单句赋值；无 reject 路径。
- 调用: 由 L25 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage.loaded executor（箭头函数）  [L29]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => waiters.push(r)`
- 作用: 把加载等待的 resolve 入队 `waiters`。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 新数组长度（被忽略）
- 实现: L29 单表达式，先于 L30 的导航发出，避免竞态。
- 调用: 由 L29 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 导航超时定时器（箭头函数）  [L31]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 20000)`
- 作用: 20 s 加载兜底，超时以 `undefined` 结束竞争。
- 参数:
  - `r` (无默认值): 内层 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L31 单表达式，参与 `Promise.race`。
- 调用: 由 L31 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage readyState 轮询延时（箭头函数）  [L35]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 1200)`
- 作用: 轮询循环每轮等待 1200 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L35 单表达式，位于 L34-L40 循环首行；最多 20 轮。
- 调用: 由 L35 `new Promise` 每轮执行——已确认。
- 置信度: 已确认

### S:extractPage SPA 水合延时（箭头函数）  [L41]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 3000)`
- 作用: complete 之后固定等待 3000 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L41 单表达式，固定等待不依赖页面状态。
- 调用: 由 L41 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 关闭标签页 catch（箭头函数）  [L53]
- 类型: function（箭头函数，`.catch` 回调）
- 签名: `() => {}`
- 作用: 静默忽略关闭标签页失败。
- 参数: 无
- 返回: `undefined`
- 实现: L53 空函数体；不检查响应状态。
- 调用: 由 L53 `.catch` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口 IIFE（async 箭头函数）  [L59-64]
- 类型: async_function（顶层立即执行箭头函数）
- 签名: `(async () => { const urls = process.argv.slice(2); for (let i = 0; i < urls.length; i++) { try { await extractPage(urls[i], i + 1); } catch (e) { console.log(...) } } })()`
- 作用: 主入口，串行处理 argv 中每个 URL，单个失败不中断。
- 参数: 无（`process.argv`）
- 返回: `Promise<void>`
- 实现: L60 取参数；L61-L63 串行 await + try/catch 打印失败；无参数时空转退出。
- 调用: 自调用（L59/L64）；调用 `extractPage`——已确认。
- 置信度: 已确认

## F:scripts/cdp_leaderboard.js
- 行数: 134
- 模块作用: Node 脚本：用 CDP 打开 `royaleapi.com/decks/leaderboard`，先把一段页面侧抽取代码（`EXTRACT_JS`）当字符串注入执行，从 DOM 中抓取第一页全部卡组（排名/玩家/部落/塔/卡牌列表/圣水/循环/奖杯/最近天梯战/copyDeck 链接），再写成 JSON + 人类可读文本两个文件。依据：L2-L4 头注释、L8-L10 的 URL 与输出常量、L12-L72 的注入代码、L110-L111 的 evaluate+解析、L114 与 L129 的两次 `fs.writeFileSync`。
- 模块级 docstring 首行: `/** 从用户调试 Chrome 抓取 royaleapi.com/decks/leaderboard 第一页全部卡组并保存。`（L2）
- 主要导入: `fs`（L7）；全局 `fetch`、`WebSocket`、`console`、`process`（L75、L76、L104、L134）——未 import，可用性取决于 Node 运行时：无法确认（原因：无 `require('ws')`，仓库内未见 Node 版本声明）
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L6；CDP 调试端点
  - `URL = 'https://royaleapi.com/decks/leaderboard'` — L8；被导航的目标页（L91）
  - `OUT_JSON = '/mnt/e/clash-royale-simulator-main/docs/leaderboard_decks.json'` — L9；JSON 输出路径（L114）
  - `OUT_TXT = '/mnt/e/clash-royale-simulator-main/docs/leaderboard_decks.txt'` — L10；文本输出路径（L129）
  - `EXTRACT_JS` — L12-L72；注入到页面执行的抽取代码（模板字符串），末尾返回 `JSON.stringify({count, url, decks})`
- 顶层数据表/字典: 无模块级字典；`EXTRACT_JS` 中被 push 的每条记录是固定 13 键对象：`rank, player, player_tag, clan, clan_tag, tower, cards, deck_data_name, copy_deck, elixir, cycle, trophies, last_battle`（L55-L69，字段来源见各符号实现）
- 运行方式: `node scripts/cdp_leaderboard.js`（无参数，L74 的 IIFE 直接跑）；需目标 Chrome 在 `172.28.144.1:9222` 开 CDP（L6），且能通过 Cloudflare 并渲染出 `.deck_lb_row`（L94-L107）。

### S:EXTRACT_JS 内页面侧 IIFE（箭头函数）  [L12-72]
- 类型: function（模板字符串内的箭头函数；**在浏览器页面上下文执行**，不是 Node 侧函数）
- 签名: `` `(() => { ... return JSON.stringify({ count: out.length, url: location.href, decks: out }); })()` ``
- 作用: 在页面里遍历 `.deck_lb_row` 行，为每行抽取排名、玩家、部落、塔、卡组卡牌、圣水/循环/奖杯数值、最近天梯战与 copyDeck 链接，返回 JSON 文本。
- 参数: 无（用 `document`、`location` 全局）
- 返回: `string`（`JSON.stringify({count, url, decks})`，L71）
- 实现: L13 `document.querySelectorAll('.deck_lb_row')` 转数组；L14-L70 对每行：L16-L19 取 `a.player_name`、`.clan_name a`、`.follow_button[data-name]`、`a.decklink`；L21-L27 从 `row.nextElementSibling` 往后扫描兄弟节点，找 `.deck_row`（卡组图）与 `.lb_deck_info`（数值块，命中即 break）；L28-L31 在 deckRow 内取 `a.deck_lb__deck_row` 并 `querySelectorAll('img.deck_card')`，用 `className.match(/deck_card_key_([\w-]+)/)` 抽卡名、`filter(Boolean)` 去空；L32-L33 在 info 内取塔图 `.tower.item img.deck_card` 并抽塔名；L34 取 `.rank.item` 的 `innerText`；L35-L43 定义内部箭头 `numOf`；L44-L46 用它取圣水/最短循环/奖杯；L47-L49 取 `.deck_lb__timestamp_container .i18n_duration_short` 文本作 `last_battle`；L50-L54 从 `decklink.href` 用 `match(/copyDeck\?deck=([^&]+)/)` 取 `copy_deck` 并 `decodeURIComponent`；L55-L69 push 记录；L71 返回 JSON。注意 `rank` 用 `parseInt(rank, 10)`，数值字段 `parseFloat` 后 `isNaN` 归 null。
- 调用: 由 Node 侧 L110 的 `Runtime.evaluate({expression: EXTRACT_JS, returnByValue: true})` 注入执行；其返回值在 L111 被 `JSON.parse`。它调用页面 DOM API（`querySelector`、`closest`、`getAttribute`、`innerText`）——已确认。
- 置信度: 已确认

### S:EXTRACT_JS 卡牌映射回调（箭头函数）  [L30-31]
- 类型: function（箭头函数，`Array.prototype.map` 回调，页面侧）
- 签名: `img => (img.className.match(/deck_card_key_([\w-]+)/) || [])[1]`
- 作用: 从卡牌 `<img>` 的 class 名中抽出卡牌 key（如 `deck_card_key_xxx` 的捕获组 1）。
- 参数:
  - `img` (无默认值): 一张卡图元素
- 返回: `string | undefined`（无匹配时为 `undefined`，随后被 `filter(Boolean)` 去掉）
- 实现: L30 单表达式：`className.match(...)` 不中时用 `|| []` 兜底，再取下标 `1`；L31 的 `filter(Boolean)` 过滤掉无卡名的元素。注意 L30 中的 `[\\w-]+` 是模板字符串转义写法，实际页面收到的是 `[\w-]+`。
- 调用: 由 `[...deckLink.querySelectorAll('img.deck_card')].map(...)`（L29-L31）调用——已确认。
- 置信度: 已确认

### S:EXTRACT_JS 的 numOf（箭头函数）  [L35-43]
- 类型: function（箭头函数，赋给 `const numOf`，页面侧）
- 签名: `const numOf = (iconSel) => {...}`
- 作用: 给定图标选择器，找到其最近的 `.item` 容器并把文本解析为浮点数（圣水/循环/奖杯三个数值共用）。
- 参数:
  - `iconSel` (无默认值): 图标 CSS 选择器（调用处传 `.elixir_icon`、`.shortest_cycle_icon`、`.rating_icon`）
- 返回: `number | null`（无 info 块、无图标、或解析出 NaN 时为 `null`）
- 实现: L36 若 `info` 为空直接返回 `null`；L37-L38 取图标元素，缺失返回 `null`；L39 `icon.closest('.item')`；L40 取 `.innerText.trim()`（item 为空则空串）；L41 `parseFloat`；L42 `isNaN` 则返回 `null`，否则返回数字。注意 `numOf` 在每次行迭代中重新定义（位于 L14-L70 的行循环体内）。
- 调用: 由同段代码 L44-L46 三次调用（`elixir`/`cycle`/`trophies`）——已确认。
- 置信度: 已确认

### S:CLI 入口 IIFE（async 箭头函数）  [L74-134]
- 类型: async_function（顶层立即执行箭头函数）
- 作用: 脚本主流程：建标签页 → 导航到排行榜 → 轮询等 Cloudflare 通过且卡组渲染 → 注入 `EXTRACT_JS` 抓数据 → 写 JSON 与文本 → 关标签页。
- 参数: 无
- 返回: `Promise<void>`（异常由 L134 的 `.catch` 兜底）
- 实现: L75 `PUT /json/new` 取标签页；L76 建 WebSocket；L77-L81 请求封装 `send`；L82-L86 回包分发；L87 等 onopen；L88 `Page.enable` + `Runtime.enable`；L90-L92 注册 loaded 后导航，20 s 超时竞争（结果未接收）；L95-L107 最多 40 次 × 1500 ms 轮询，每次 evaluate 取 `{title, ready, n}`（n = `.deck_lb_row` 数量）并打印，当 `ready==='complete'` 且标题不含 `Just a moment|请稍候` 且 `n>0` 时置 `ready=true` 结束；L108 再等 2000 ms；L110-L111 注入 `EXTRACT_JS` 并 `JSON.parse` 结果；L112 打印抓取数量与 url；L114 写 JSON（缩进 2）；L117-L129 生成文本报表（头部含 url/ISO 时间/卡组数/`'='.repeat(100)`，每副卡组 3~4 行：排名/玩家/部落、卡组卡名列表、塔/圣水/循环/奖杯/最近天梯战、copyDeck）；L130 打印保存路径；L132-L133 关 WS 与标签页。
- 调用: 模块加载即自调用；调用 `send`（CDP 方法）、`fs.writeFileSync`；末尾 `catch` 由 L134 的箭头处理——已确认。
- 置信度: 已确认

### S:CLI 入口.send（箭头函数）  [L78-81]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: CDP 请求封装：编号、登记 resolve、发送。
- 参数:
  - `method` (无默认值): CDP 方法名
  - `params` (默认 `{}`): 参数对象
- 返回: `Promise`（兑现值 `m.result`）
- 实现: L79 自增 id 并登记；L80 发送 JSON。无 reject 路径（执行器只接 `res`）。
- 调用: 被 L88-L92、L99-L102、L110 调用——已确认。
- 置信度: 已确认

### S:CLI 入口.send.executor（箭头函数）  [L78-81]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: 同步完成 id 分配、resolve 登记与发送。
- 参数:
  - `res` (无默认值): resolve 函数
- 返回: 无（被忽略）
- 实现: L79-L80 两句（L79 含 `const i = ++id; pending.set(i, res);`）；与 `send` 箭头同属 L78-L81 区间。
- 调用: 由 L78 `new Promise` 立即执行——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onmessage（箭头函数）  [L82-86]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 按 id 兑现 pending；加载事件时唤醒全部 waiters。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件
- 返回: 无
- 实现: L83 解析 JSON；L84 命中 id 则 resolve 后 delete 并 return；L85 加载事件分支 `waiters.splice(0).forEach(f => f())`；其它忽略。
- 调用: 由 WebSocket 运行时回调——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onmessage.forEach 回调（箭头函数）  [L85]
- 类型: function（箭头函数，`forEach` 回调）
- 签名: `f => f()`
- 作用: 逐个兑现加载等待。
- 参数:
  - `f` (无默认值): 等待者 resolve
- 返回: `f()` 的结果（被忽略）
- 实现: 单表达式（注意此处箭头无括号，语法上与其它文件写法略异）；配合 `splice(0)` 清空队列。
- 调用: 由 L85 `forEach` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onopen executor（箭头函数）  [L87]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `r => { ws.onopen = r; }`
- 作用: 用 `ws.onopen` 作为 resolve，等待建连完成。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L87 单句赋值；无 reject 路径。
- 调用: 由 L87 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口.loaded executor（箭头函数）  [L90]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `r => waiters.push(r)`
- 作用: 把加载等待 resolve 入队。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 新长度（被忽略）
- 实现: L90 单表达式，先于 L91 导航发出。
- 调用: 由 L90 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 导航超时定时器（箭头函数）  [L92]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 20000)`
- 作用: 20 s 加载兜底（此文件不接收 `Promise.race` 的值）。
- 参数:
  - `r` (无默认值): 内层 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L92 单表达式，位于 `await Promise.race([...])` 内。
- 调用: 由 L92 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 Cloudflare/渲染轮询延时（箭头函数）  [L97]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 1500)`
- 作用: 等待渲染轮询每轮间隔 1500 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L97 单表达式；位于 L96 `for (let i = 0; i < 40 && !ready; i++)` 循环体首行，最多 40 轮 ≈ 60 s。
- 调用: 由 L97 `new Promise` 每轮执行——已确认。
- 置信度: 已确认

### S:CLI 入口 抽取前缓冲延时（箭头函数）  [L108]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 2000)`
- 作用: 轮询结束到注入抽取代码之间固定等待 2000 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L108 单表达式；位于 L110 注入 `EXTRACT_JS` 之前。注意即使 40 轮都未满足条件（`ready` 仍 false）也会继续往下抽取，届时 `document.querySelectorAll` 可能查不到行（未做守卫）。
- 调用: 由 L108 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 关闭标签页 catch（箭头函数）  [L133]
- 类型: function（箭头函数，`.catch` 回调）
- 签名: `() => {}`
- 作用: 静默忽略关闭标签页失败。
- 参数: 无
- 返回: `undefined`
- 实现: L133 空函数体。
- 调用: 由 L133 `.catch` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口失败兜底 catch（箭头函数）  [L134]
- 类型: function（箭头函数，`.catch` 回调，挂在顶层 IIFE 之后）
- 签名: `e => { console.error('FAIL', e); process.exit(1); }`
- 作用: 顶层致命错误时打印 `FAIL` 与异常并以退出码 1 终止进程。
- 参数:
  - `e` (无默认值): 抛出的异常对象
- 返回: 无（内部 `process.exit(1)` 不会返回）
- 实现: L134 单表达式块：`console.error('FAIL', e)` 后 `process.exit(1)`；这是本文件唯一的非零退出路径（对比 `cdp_evo.js` 等只打印失败信息）。
- 调用: 由 L134 的 `.catch` 在 IIFE 抛错时调用——已确认。
- 置信度: 已确认

## F:scripts/cdp_leaderboard_probe.js
- 行数: 53
- 模块作用: 探针脚本（临时调试用）：用 CDP 打开一个排行榜页面（默认 `royaleapi.com/decks/leaderboard`），轮询等待页面就绪，然后把 `location.href`/`title`/`body.innerText`/`body.outerHTML` 全量落盘为 `.txt` 与 `.html`，用于人工检查 DOM 结构。依据：L2 头注释、L5 的默认 URL、L28-L40 轮询、L43-L50 落盘。
- 模块级 docstring 首行: `/** Probe royaleapi deck leaderboard via user's debug Chrome (CDP) */`（L2）
- 主要导入: `fs`（L4）；全局 `fetch`、`WebSocket`、`console`、`process`（L8、L9、L36、L48）——未 import，可用性取决于 Node 运行时：无法确认（原因：无 `require('ws')`，仓库内未见 Node 版本声明）
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L3；CDP 调试端点
  - `URL = process.argv[2] || 'https://royaleapi.com/decks/leaderboard'` — L5；目标页，可用第一个命令行参数覆盖
- 顶层数据表/字典: 无（`pending`/`waiters` 为 L10 的局部状态）
- 运行方式: `node scripts/cdp_leaderboard_probe.js [url]`（L5）；需目标 Chrome 在 `172.28.144.1:9222` 开 CDP（L3）。仓库内未检索到对本文件的引用（在 `scripts/` 与 `src/clasher_new/` 下检索文件名无命中），按独立探针使用。

### S:CLI 入口 IIFE（async 箭头函数）  [L7-53]
- 类型: async_function（顶层立即执行箭头函数）
- 作用: 建标签页、导航到目标 URL、等 Cloudflare 通过且正文足够长，然后把正文文本与整页 HTML 写盘。
- 参数: 无
- 返回: `Promise<void>`
- 实现: L8 `PUT /json/new` 取标签页；L9 建 WebSocket；L10-L14 请求封装 `send`；L15-L19 回包分发；L20 等 onopen；L21 `Page.enable` + `Runtime.enable`；L23-L25 注册 loaded 后 `Page.navigate`，与 20 s 超时竞争；L28-L40 最多 40 次 × 1500 ms 轮询，每次 evaluate 取 `{title, ready, href, textLen}` 并打印（字符串截断到 60/70 字符），当 `ready==='complete'`、标题不含 `Just a moment`、`textLen > 500` 时 break；L41 再等 3000 ms；L43-L46 一次 evaluate 取 `{href, title, text, html}`（`text` 为 `body.innerText`，`html` 为 `body.outerHTML`）；L47 解析结果；L48-L49 分别写 `docs/_leaderboard_probe.txt` 与 `docs/_leaderboard_probe.html`（utf-8）；L50 打印两者长度；L51-L52 关 WS 与标签页（`.catch(()=>{})`）。**注意：整个 IIFE 没有 `.catch` 兜底**，也未设置退出码，异常会成为未处理的 promise rejection。
- 调用: 模块加载即自调用；调用 CDP 方法与 `fs.writeFileSync`——已确认。
- 置信度: 已确认

### S:CLI 入口.send（箭头函数）  [L11-14]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: CDP 请求封装：分配 id、登记 resolve、经 WebSocket 发送。
- 参数:
  - `method` (无默认值): CDP 方法名
  - `params` (默认 `{}`): 参数对象
- 返回: `Promise`（兑现值 `m.result`）
- 实现: L12 自增 id 并登记；L13 发送 JSON 文本。无 reject 路径。
- 调用: 被 L21、L24、L31、L43 调用——已确认。
- 置信度: 已确认

### S:CLI 入口.send.executor（箭头函数）  [L11-14]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: 同步执行 id 分配、resolve 登记与发送。
- 参数:
  - `res` (无默认值): resolve 函数
- 返回: 无（被忽略）
- 实现: L12-L13 两句，与 `send` 箭头同属 L11-L14 区间。
- 调用: 由 L11 `new Promise` 立即执行——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onmessage（箭头函数）  [L15-19]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 按 id 兑现 pending 请求；加载事件时唤醒全部 waiters。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件
- 返回: 无
- 实现: L16 解析 JSON；L17 命中 id 则 resolve（传 `m.result`）后删除登记并 return；L18 加载事件分支 `waiters.splice(0).forEach(f => f())`；其它消息忽略。
- 调用: 由 WebSocket 运行时回调——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onmessage.forEach 回调（箭头函数）  [L18]
- 类型: function（箭头函数，`forEach` 回调）
- 签名: `f => f()`
- 作用: 逐个兑现加载等待。
- 参数:
  - `f` (无默认值): 等待者 resolve
- 返回: `f()` 的结果（被忽略）
- 实现: 单表达式；配合 `splice(0)` 清空队列（L18）。
- 调用: 由 L18 `forEach` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口.ws.onopen executor（箭头函数）  [L20]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `r => { ws.onopen = r; }`
- 作用: 用 `ws.onopen` 作为 resolve，等待建连完成。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L20 单句赋值；无 reject 路径。
- 调用: 由 L20 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口.loaded executor（箭头函数）  [L23]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `r => waiters.push(r)`
- 作用: 把加载等待 resolve 入队。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 新长度（被忽略）
- 实现: L23 单表达式；登记先于 L24 导航。
- 调用: 由 L23 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 导航超时定时器（箭头函数）  [L25]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 20000)`
- 作用: 20 s 加载兜底（`Promise.race` 的结果本文件不接收）。
- 参数:
  - `r` (无默认值): 内层 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L25 单表达式。
- 调用: 由 L25 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 就绪轮询延时（箭头函数）  [L29]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 1500)`
- 作用: 等待页面就绪的轮询间隔 1500 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L29 单表达式；位于 L28 `for (let i = 0; i < 40; i++)` 循环首行（无提前 break 条件以外的退出），最多约 60 s。
- 调用: 由 L29 `new Promise` 每轮执行——已确认。
- 置信度: 已确认

### S:CLI 入口 抽取前缓冲延时（箭头函数）  [L41]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `r => setTimeout(r, 3000)`
- 作用: 轮询之后、抽取之前固定等待 3000 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L41 单表达式，位于 L43 的抽取 evaluate 之前。
- 调用: 由 L41 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:CLI 入口 关闭标签页 catch（箭头函数）  [L52]
- 类型: function（箭头函数，`.catch` 回调）
- 签名: `() => {}`
- 作用: 静默忽略关闭标签页失败。
- 参数: 无
- 返回: `undefined`
- 实现: L52 空函数体；此行是 IIFE 的最后一条语句。
- 调用: 由 L52 `.catch` 调用——已确认。
- 置信度: 已确认

## F:scripts/cdp_spell.js
- 行数: 64
- 模块作用: 与 `cdp_evo.js` 同构的 CDP 页面文本抽取脚本；唯一实质差异是 L54 的输出路径前缀 `_spell_`（写 `docs/_spell_<idx>.txt`）。依据：L9-L57 流程、L54 路径、L59-L64 入口。
- 模块级 docstring 首行: `/** CDP 页面文本抽取器 v2：Page.navigate 驱动（兼容 Chrome 152）`（L2）；L4 声明的 `/tmp/cdp_page_<n>.txt` 与实际输出 `docs/_spell_<idx>.txt`（L54）不一致（过期说明，源码事实）。
- 主要导入: `fs`（L7）；全局 `fetch`、`WebSocket`、`console`、`process`（L10、L11、L60）——未 import，可用性取决于 Node 运行时：无法确认（原因：本文件无 `require('ws')`，仓库内未见 Node 版本声明）
- 关键模块级常量:
  - `CDP = 'http://172.28.144.1:9222'` — L6；CDP 调试端点
- 顶层数据表/字典: 无（`pending`/`waiters` 为 L13-L14 局部状态）
- 运行方式: `node scripts/cdp_spell.js <url1> <url2> ...`（L60）；需目标 Chrome 在 `172.28.144.1:9222` 开 CDP（L6）。

### S:extractPage  [L9-57]
- 类型: async_function
- 签名: `async function extractPage(url, idx)`
- 作用: CDP 打开 URL、等加载与水合后抓取页面可见文本，写入 `docs/_spell_<idx>.txt` 并打印摘要。
- 参数:
  - `url` (无默认值): 导航目标（L30）
  - `idx` (无默认值): 文件名/日志序号
- 返回: `Promise<void>`
- 实现: L10 `PUT /json/new` 取标签页；L11 建 WebSocket；L12-L19 请求封装 `send`；L20-L24 回包分发；L25 等 onopen；L26-L27 `Page.enable`、`Runtime.enable`；L29-L31 注册 loaded 后导航，20 s 超时竞争（结果 `nav` 未使用）；L34-L40 最多 20 次 × 1200 ms 轮询 `document.readyState`；L41 固定再等 3000 ms；L43-L46 取 `{href,title,textLen}` 摘要到 `info`；L47-L51 取 `innerText`（失败置 `EVAL_FAIL: ...`）；L52-L53 关 WS 与标签页；L54-L56 写 `/mnt/e/clash-royale-simulator-main/docs/_spell_${idx}.txt`（utf-8）并打印 URL/路径/字符数/摘要。
- 调用: 由 L62 入口循环调用；调用 CDP 方法与 `/json/new`、`/json/close/<id>`、`fs.writeFileSync`——已确认。
- 置信度: 已确认

### S:extractPage.send（箭头函数）  [L15-19]
- 类型: function（箭头函数，赋给 `const send`）
- 签名: `const send = (method, params = {}) => new Promise((res) => {...})`
- 作用: CDP 请求封装：编号、登记 resolve、发送。
- 参数:
  - `method` (无默认值): CDP 方法名
  - `params` (默认 `{}`): 参数对象
- 返回: `Promise`（兑现值 `m.result`）
- 实现: L16 自增 id；L17 登记 resolve；L18 发送 JSON。无 reject 路径。
- 调用: 被 `extractPage` 于 L26-L27、L30、L37、L43-L46、L49 调用——已确认。
- 置信度: 已确认

### S:extractPage.send.executor（箭头函数）  [L15-19]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(res) => { const i = ++id; pending.set(i, res); ws.send(JSON.stringify({ id: i, method, params })); }`
- 作用: 同步完成 id 分配、resolve 登记与发送。
- 参数:
  - `res` (无默认值): resolve 函数
- 返回: 无（被忽略）
- 实现: L16-L18 三句，与 `send` 箭头嵌套同区间（L15-L19）。
- 调用: 由 L15 `new Promise` 立即执行——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage（箭头函数）  [L20-24]
- 类型: function（箭头函数，赋给 `ws.onmessage`）
- 签名: `ws.onmessage = (ev) => {...}`
- 作用: 按 id 兑现 pending；`Page.loadEventFired` 时清空并唤醒 waiters。
- 参数:
  - `ev` (无默认值): WebSocket 消息事件
- 返回: 无
- 实现: L21 解析 JSON；L22 命中 id 则 resolve（传 `m.result`）、删除登记、return；L23 加载事件分支 `waiters.splice(0).forEach((f) => f())`；其它忽略。
- 调用: 由 WebSocket 运行时回调——已确认。
- 置信度: 已确认

### S:extractPage.ws.onmessage.forEach 回调（箭头函数）  [L23]
- 类型: function（箭头函数，`forEach` 回调）
- 签名: `(f) => f()`
- 作用: 逐个兑现加载等待。
- 参数:
  - `f` (无默认值): 等待者 resolve
- 返回: `f()` 的结果（被忽略）
- 实现: 单表达式；配合 `splice(0)` 清空队列（L23）。
- 调用: 由 L23 `forEach` 调用——已确认。
- 置信度: 已确认

### S:extractPage.ws.onopen executor（箭头函数）  [L25]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => { ws.onopen = r; }`
- 作用: 用 `ws.onopen` 作为 resolve，等待建连完成。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 无
- 实现: L25 单句赋值；不覆盖 L20 的 `onmessage`；无 reject 路径。
- 调用: 由 L25 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage.loaded executor（箭头函数）  [L29]
- 类型: function（箭头函数，`new Promise` 执行器）
- 签名: `(r) => waiters.push(r)`
- 作用: 把加载等待 resolve 入队 `waiters`。
- 参数:
  - `r` (无默认值): resolve 函数
- 返回: 新长度（被忽略）
- 实现: L29 单表达式，先于 L30 导航登记。
- 调用: 由 L29 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 导航超时定时器（箭头函数）  [L31]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 20000)`
- 作用: 20 s 加载兜底，超时以 `undefined` 结束竞争。
- 参数:
  - `r` (无默认值): 内层 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L31 单表达式，参与 `Promise.race`。
- 调用: 由 L31 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage readyState 轮询延时（箭头函数）  [L35]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 1200)`
- 作用: 轮询循环每轮等待 1200 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L35 单表达式，位于 L34-L40 循环首行（最多 20 轮）。
- 调用: 由 L35 `new Promise` 每轮执行——已确认。
- 置信度: 已确认

### S:extractPage SPA 水合延时（箭头函数）  [L41]
- 类型: function（箭头函数，`setTimeout` 回调）
- 签名: `(r) => setTimeout(r, 3000)`
- 作用: complete 之后固定等待 3000 ms。
- 参数:
  - `r` (无默认值): 延时 promise resolve
- 返回: timer 句柄（被忽略）
- 实现: L41 单表达式。
- 调用: 由 L41 `new Promise` 执行——已确认。
- 置信度: 已确认

### S:extractPage 关闭标签页 catch（箭头函数）  [L53]
- 类型: function（箭头函数，`.catch` 回调）
- 签名: `() => {}`
- 作用: 静默忽略关闭标签页失败。
- 参数: 无
- 返回: `undefined`
- 实现: L53 空函数体；不检查响应状态。
- 调用: 由 L53 `.catch` 调用——已确认。
- 置信度: 已确认

### S:CLI 入口 IIFE（async 箭头函数）  [L59-64]
- 类型: async_function（顶层立即执行箭头函数）
- 签名: `(async () => { const urls = process.argv.slice(2); for (let i = 0; i < urls.length; i++) { try { await extractPage(urls[i], i + 1); } catch (e) { console.log(...) } } })()`
- 作用: 主入口，串行抽取 argv 中每个 URL，逐个容错。
- 参数: 无（`process.argv`）
- 返回: `Promise<void>`
- 实现: L60 取参数；L61-L63 串行 await + try/catch 打印失败信息、不中断；无参数时空转结束。
- 调用: 自调用（L59/L64）；调用 `extractPage`——已确认。
- 置信度: 已确认

## F:scripts/l1_upgrade.ps1
- 行数: 30
- 模块作用: PowerShell 一次性运维/数据准备脚本：从 RoyaleAPI 的 `cr-api-data` 仓库（GitHub raw，`master/docs/json`）下载 7 个官方 JSON 到仓库的 `src\clasher_new\data_official\`，全部成功后再切到仓库根目录运行 `python scripts\coverage.py` 重算覆盖矩阵；任一文件失败只打印信息不继续。依据：L1 首行注释、L2-L11 变量与文件清单、L14-L22 下载循环、L24-L29 分支。
- 模块级 docstring 首行: 无 `docstring` 概念；首行为注释 `# ===== L1 官方数据升级 v3（路径已由诊断确认：docs/json/）=====`（L1，**文件以 UTF-8 BOM 开头**，`﻿` 在 L1 起始处）；该注释是脚本用途的唯一说明。
- 主要导入: 无 `Import-Module`/`using`；使用 .NET 类型 `[Net.ServicePointManager]`（L5）、cmdlet `New-Item`（L4）、`Join-Path`（L3）、`Invoke-WebRequest`（L16）、`Get-Item`（L17）、`Set-Location`（L26）
- 关键模块级常量:
  - `$repo = "E:\clash-royale-simulator-main"` — L2；仓库根（Windows 路径），供 L3 拼目标目录、L26 切目录
  - `$dst = Join-Path $repo "src\clasher_new\data_official"` — L3；下载落地目录
  - `$base = "https://raw.githubusercontent.com/RoyaleAPI/cr-api-data/master/docs/json"` — L7；远端目录前缀
  - `$files` — L9-L11；7 个文件名的字符串数组：`cards.json`, `cards_stats_characters.json`, `cards_stats_building.json`, `cards_stats_spell.json`, `cards_stats_projectile.json`, `cards_evo.json`, `cards_i18n.json`（L8 注释说明前者 5 个是 `coverage.py` 需要、后者 2 个是附带）
  - `$ok = $true` — L13；下载是否全成功的标志，决定 L24 分支
  - `[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12` — L5；把当前会话 TLS 协议设为 1.12（GitHub raw 需要），**这是环境副作用而非普通常量**
- 顶层数据表/字典: `$files` 数组是唯一的表结构（键=序号，值=远端/本地同名文件名）；脚本内无哈希表。
- 运行方式: 在 Windows PowerShell 中执行 `scripts\l1_upgrade.ps1`（需联网；脚本自身不检查管理员权限，也未设 `$ErrorActionPreference`）。运行前须存在 `E:\clash-royale-simulator-main`（L2 硬编码路径）。
- 符号计数说明: 本文件**无函数定义**（全文检索无 `function` 关键字）；按本组口径，符号 = 顶层可执行段，共 3 段（L2-L11 / L13-L22 / L24-L30）。

### S:顶层可执行段-1：路径/协议/文件清单初始化  [L2-11]
- 类型: 顶层可执行段（非函数；自脚本第 2 行起顺序执行）
- 签名: 不适用（PowerShell 顶层语句）
- 作用: 定义仓库路径与目标目录、创建目标目录、把 TLS 协议提升到 1.2、声明远端基址与待下载文件清单。
- 参数: 无
- 返回: 无（`New-Item` 输出被 `| Out-Null` 丢弃）
- 实现: L2 给 `$repo` 赋值（字符串，含反斜杠转义写法 `"E:\clash-royale-simulator-main"`）；L3 用 `Join-Path` 拼出 `$dst`；L4 `New-Item -ItemType Directory -Force -Path $dst | Out-Null` 幂等建目录（`-Force` 使已存在时不报错）并把成功输出丢弃；L5 把 `[Net.ServicePointManager]::SecurityProtocol` 设为 `Tls12`（影响整个会话的 .NET HTTP 栈）；L7 赋 `$base`；L9-L11 用逗号续行语法把 7 个文件名赋给 `$files`（PowerShell 数组）。L7-L11 之间的 L8 是注释，说明文件分组用途。
- 调用: 被脚本解释器顺序执行；它调用 `Join-Path`、`New-Item`；后续段（L16、L17、L27）读取这里定义的变量——已确认（同一脚本内变量流）。
- 置信度: 已确认

### S:顶层可执行段-2：下载循环  [L13-22]
- 类型: 顶层可执行段（`foreach` 循环，非函数）
- 签名: `foreach ($f in $files) { ... }`
- 作用: 逐文件从远端下载到 `$dst`，成功时打印格式化的大小，失败时打印错误信息并把 `$ok` 置 false。
- 参数:
  - `$f` (循环变量，值取自 `$files`，L9-L11): 当前文件名
- 返回: 无（循环；`$ok`/`$dst` 为副作用输出）
- 实现: L13 先把 `$ok` 置 `$true`；L14 遍历 `$files`；L16 `Invoke-WebRequest -Uri "$base/$f" -OutFile (Join-Path $dst $f) -UseBasicParsing -TimeoutSec 120` 下载，超时 120 秒、用基础解析（不依赖 IE 引擎）；L17 成功时用 `-f` 格式化输出 `文件名 → 本地文件字节数`（`Get-Item ... .Length`）；L18-L21 `catch` 中打印 `下载失败 $f ：$($_.Exception.Message)` 并把 `$ok` 置 `$false`（**不使用 `throw`，循环继续**，即单个失败不影响后续文件下载）。注意此处 `$_.Exception.Message` 的 `{e!r}` 式风险不适用（PowerShell 字符串插值）。
- 调用: 被解释器顺序执行；调用 `Invoke-WebRequest`、`Join-Path`、`Get-Item`；它写入 `$ok` 供 L24 判定——已确认。
- 置信度: 已确认

### S:顶层可执行段-3：收尾分支（跑覆盖矩阵 / 报错）  [L24-30]
- 类型: 顶层可执行段（`if/else`，非函数）
- 签名: `if ($ok) { ... } else { ... }`
- 作用: 全部下载成功时切到仓库根并运行 `python scripts\coverage.py` 重算覆盖矩阵；否则提示把失败输出发给操作者。
- 参数: 无
- 返回: 无
- 实现: L24 以 L13/L20 维护的 `$ok` 为条件；L25 打印 `全部下载成功，重跑覆盖矩阵...`；L26 `Set-Location $repo` 切换当前目录（脚本内后续 `python` 调用依赖此目录）；L27 `python scripts\coverage.py` 以裸调用方式执行（**依赖 PATH 中有 `python`，脚本不激活任何虚拟环境**；该目标文件已确认存在：`scripts/coverage.py`，19,621 字节，其文件头 docstring 声明它会优先使用 `src/clasher_new/data_official/`）；L28-L29 打印 `有文件下载失败，把上面输出发给我`。注意 `python` 的退出码未被检查，脚本也不会因覆盖矩阵失败而失败。
- 调用: 被解释器顺序执行；调用 `Set-Location`、外部命令 `python scripts\coverage.py`——已确认（`scripts/coverage.py` 在同仓库存在；其行为未逐行读完，本段只说明调用关系）。
- 置信度: 已确认

## F:scripts/run_probe_v3.sh
- 行数: 69
- 模块作用: Bash 驱动脚本：把价值通路探针 `scripts/probe_value_ln.py` 按"两阶段"跑法编排——阶段 1 用多个 seed 并行跑 rollout 并存 npz，阶段 2 串行做离线拟合出 json；用参数 `rollout|fit|all` 选择阶段，seed 列表默认 `7 11 13 17`。依据：L1-L10 头注释与用法、L11-L24 变量与参数解析、L26-L46 两个函数、L48-L69 的 `case` 分派。
- 模块级 docstring 首行: 无 `docstring` 概念；首块注释 `# v3 探针两阶段执行（预注册 docs/value_ln_probe3_prereg_2026-09-14.md）`（L2），随后 L3-L4 说明两阶段、L6-L8 是"路径纪律"警告、L10 是用法（该注释引用的预注册文档未按本组约束读取，此处只转述脚本自身注释）。
- 主要导入: 无 `source`/`.`；以外部命令方式使用 `.venv/Scripts/python.exe`（L14、L29、L40）与 `mkdir`（L19）；设置 `PYTHONIOENCODING=utf-8`（L29、L40）
- 关键模块级常量:
  - `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` — L12；由脚本自身位置推出仓库根（绝对路径）
  - `SRC="$ROOT/src/clasher_new"` — L13；Python 运行目录（L28、L39 `cd`）
  - `PY="$ROOT/.venv/Scripts/python.exe"` — L14；Windows venv 解释器
  - `PROBE="../../scripts/probe_value_ln.py"` — L15；探针脚本（**相对 `src/clasher_new`**，因注释 L6-L8 说明 Windows python 解析不了 `/mnt/e` 形式参数）
  - `LOGABS="$ROOT/runs/_probe_v3"` — L16；日志/输出目录（bash 侧绝对路径，L19 建目录、L33/L44 重定向）
  - `RELOUT="../../runs/_probe_v3"` — L17；传给 python 的相对输出目录（L32 `--save-npz`、L43 `--npz`/`--out`）
  - `CKPT="runs/critic_inert_probe_20k/solo_main_20000.pt"` — L18；被探针加载的 checkpoint（相对 `src/clasher_new`）
  - `set -u` — L11；未定义变量报错（**注意脚本未设 `-e`/`-o pipefail`，失败不自动中止**）
- 顶层数据表/字典: 无哈希表；`seeds` 是 Bash 数组变量（L23-L24），`phase` 是字符串变量（L21）
- 运行方式: `bash scripts/run_probe_v3.sh [rollout|fit|all] [seed...]`（L10、L67 的 usage）；缺省阶段 `all`、缺省 seed `7 11 13 17`（L21、L24）。
- 符号计数说明: 两个 Bash 函数 + 两个顶层可执行段 = 4。

### S:rollout  [L26-35]
- 类型: function（Bash 函数）
- 签名: `rollout() { local s="$1"; local extra="${2:-}"; ... }`
- 作用: 对单个 seed 在 `src/clasher_new` 下运行探针的 rollout 阶段，把 30000 帧的中间张量存成 npz；日志重定向到 `runs/_probe_v3/rollout_seed<s>.log`。
- 参数:
  - `$1` → `local s`: seed 值（作为字符串，直接拼进文件名与 `--tag`）
  - `$2` → `local extra`（默认空 `${2:-}`）: 附加参数串（调用方在 `s=7` 时传 `--cnn-input`，L51/L61）
- 返回: 无显式 return；打印一行 `[rollout] seed=... exit=... log=...`（L34）；真实退出码由调用方的 `&` + `wait` 处理
- 实现: L27 声明两个局部变量（`extra` 用 `:-` 兜底为空）；L28 `cd "$SRC" || exit 1` 切目录，失败即退出**整个脚本**（不是仅退出函数）；L29-L33 以 `PYTHONIOENCODING=utf-8 "$PY" "$PROBE"` 调探针，参数依次为 `--ckpt "$CKPT" --frames 30000 --seed "$s" --device cpu --tag "v3_seed$s" --ladder v3 --raw-obs $extra --save-npz "$RELOUT/seed$s.npz" --rollout-only`，stdout/stderr 全部重定向到 `"$LOGABS/rollout_seed$s.log" 2>&1`；L34 打印的 `$?` 是**重定向后整个命令**的退出码，但注意此处 `$?` 在 `echo` 的参数中展开于命令之后，读的是 python 的退出码。`$extra` 未加引号（有意为之，用于词分割成两个参数）。
- 调用: 被 L52（`rollout`）、L62（`all`）以 `rollout "$s" "$extra" &` 后台并行调用；它调用 `.venv/Scripts/python.exe` 与 `scripts/probe_value_ln.py` 的 CLI（`--ckpt/--frames/--seed/--device/--tag/--ladder/--raw-obs/--save-npz/--rollout-only` 为探针侧接口，本组未读该 py，参数语义按脚本自身给出的字面值记录）——调用关系已确认，探针参数语义待确认。
- 置信度: 已确认（对本脚本行为）；探针参数具体含义 待确认（原因：未读 `scripts/probe_value_ln.py`，本任务文件清单外）

### S:fit  [L37-46]
- 类型: function（Bash 函数）
- 签名: `fit() { local s="$1"; ... }`
- 作用: 对单个 seed 用上一步存下的 npz 做离线拟合（排除 `grid_x`），输出 json 到 `runs/_probe_v3/seed<s>.json`，日志写 `fit_seed<s>.log`。
- 参数:
  - `$1` → `local s`: seed 值（决定 `--seed`、`--npz`、`--out`、日志名）
- 返回: 无显式 return；打印 `[fit] seed=... exit=... json=$LOGABS/seed<s>.json`（L45）
- 实现: L38 声明局部；L39 `cd "$SRC" || exit 1`；L40-L44 调同一探针，参数为 `--ckpt "$CKPT" --seed "$s" --device cpu --tag "v3_seed$s" --ladder v3 --raw-obs --npz "$RELOUT/seed$s.npz" --exclude grid_x --out "$RELOUT/seed$s.json"`，输出重定向到 `"$LOGABS/fit_seed$s.log" 2>&1`；L45 打印退出码与 json 路径。注意 L45 里 `json=` 打的是 `$LOGABS/seed$s.json`，而 python 的 `--out` 是 `$RELOUT/seed$s.json`（`$LOGABS` 是绝对路径、`$RELOUT` 是相对 `src/clasher_new` 的相对路径）——两者**指向同一位置**，但打印的路径形式与命令实际参数形式不同。
- 调用: 被 L57（`fit`）与 L65（`all`）串行调用；它调用 `.venv/Scripts/python.exe` 与 `scripts/probe_value_ln.py` 的 CLI——已确认。
- 置信度: 已确认（对本脚本行为）；探针参数语义 待确认（原因：未读 `scripts/probe_value_ln.py`）

### S:顶层可执行段-1：变量与参数解析  [L11-24]
- 类型: 顶层可执行段（非函数）
- 签名: 不适用（Bash 顶层语句）
- 作用: 设置 shell 选项，推导仓库根/解释器/探针/日志路径，建日志目录，解析阶段参数与 seed 列表（含默认值）。
- 参数: 无（读取 `$1`、`$@`、`${BASH_SOURCE[0]}`）
- 返回: 无
- 实现: L11 `set -u`；L12 用子 shell `cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd` 求仓库绝对路径（支持从任意目录调用）；L13-L18 顺序赋值 `SRC`/`PY`/`PROBE`/`LOGABS`/`RELOUT`/`CKPT`；L19 `mkdir -p "$LOGABS"` 建输出目录；L21 `phase="${1:-all}"` 缺省 `all`；L22 `shift || true` 丢弃第一个位置参数（`|| true` 防止无参时 `set -u`/shift 报错影响）；L23 `seeds=("$@")` 收集剩余参数；L24 若 seed 个数为 0 则赋默认 `(7 11 13 17)`。注意 L15 的 `PROBE` 与 L17 的 `RELOUT` 是"相对 `src/clasher_new`"的路径，L6-L8 注释解释了原因（Windows python 解析不了 `/mnt/e/...`）。
- 调用: 被解释器顺序执行；它调用 `dirname`、`cd`、`pwd`、`mkdir`；产出的变量被 `rollout`/`fit` 与 L48-L69 使用——已确认。
- 置信度: 已确认

### S:顶层可执行段-2：阶段分派与收尾  [L48-69]
- 类型: 顶层可执行段（`case` 语句 + 收尾 `echo`）
- 签名: `case "$phase" in rollout) ... ;; fit) ... ;; all) ... ;; *) echo "usage: $0 [rollout|fit|all] [seeds...]"; exit 2;; esac`
- 作用: 按 `rollout|fit|all` 分派：rollout 后台并行跑全部 seed 后 `wait`；fit 串行拟合全部 seed；all 先并行 rollout 再串行 fit；未知阶段打印用法并以码 2 退出；最后打印完成行。
- 参数: 无（读取 `$phase`、`${seeds[@]}`）
- 返回: 退出码 0（正常）或 2（未知阶段，L67）；最后一行打印 `[done] phase=... seeds=...`（L69）
- 实现: L49-L55 `rollout` 分支：`for s in "${seeds[@]}"` 循环，`extra=""; [ "$s" = "7" ] && extra="--cnn-input"` 给 seed 7 传特殊参数（L51），`rollout "$s" "$extra" &` 放后台，循环结束 `wait` 等全部结束；L56-L58 `fit` 分支：串行 `fit "$s"`；L59-L66 `all` 分支：先与 rollout 分支相同地并行 rollout 并 `wait`（L60-L64），再串行 fit（L65）——**注意 fit 阶段不重传 seed 7 的 `--cnn-input`**（`fit` 只传 `--raw-obs`，见 L42），与 rollout 阶段的参数不对称；L67 `*)` 分支打印用法并 `exit 2`；L68 `esac`；L69 打印完成行（`${seeds[*]}` 展开为空格分隔的 seed 列表）。
- 调用: 被解释器顺序执行；它调用 `rollout`、`fit`（后台或串行）、内建 `wait`/`exit`/`echo`——已确认。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| src/clasher_new/hook_raw_capture.js | 12 | 12 |
| scripts/cdp_evo.js | 12 | 12 |
| scripts/cdp_extract.js | 12 | 12 |
| scripts/cdp_hero.js | 12 | 12 |
| scripts/cdp_leaderboard.js | 15 | 15 |
| scripts/cdp_leaderboard_probe.js | 11 | 11 |
| scripts/cdp_spell.js | 12 | 12 |
| scripts/l1_upgrade.ps1 | 3 | 3 |
| scripts/run_probe_v3.sh | 4 | 4 |

COVERAGE_OK GX01 files=9 symbols=93
