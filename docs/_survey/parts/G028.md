# 组 G028 文件级分析

> 说明：本组 3 个文件全部读完（311 + 297 + 294 = 902 行）。为确认调用关系额外查看了同仓
> `src/clasher_new/rl/env_wrapper.py`、`src/clasher_new/battle.py`、`src/clasher_new/player.py`、
> `src/clasher_new/card_mechanics.py`、`src/clasher_new/card_utils.py`、`src/clasher_new/arena.py`、
> `src/clasher_new/evo_2025_data.py`、`src/clasher_new/rl/train_solo.py`、`src/clasher_new/rl/follower.py` 的
> 相关片段（仅用于确认签名/字段/钩子归属）。

## F:scripts/probe_reward_composition.py
- 行数: 311
- 模块作用: 只读探针脚本：在**内存里**把 `rl.env_wrapper.compute_reward` 包一层记录器（不改磁盘代码），用加载的 ckpt 跑 rollout，对每次奖励计算额外求出 4 个"只把某一项权重置 0"的变体，用差值精确拆出逐帧奖励的 5 个分量（edw / crown / tower / terminal / unit）。随后按 `Σ|分量|` 归一打印占比、按 `t=120s` 分期打印占比、打印 Φ（手牌圣水+场上部署费）轨迹，最后按写死的 4 条判据给出 verdict 并可落盘 JSON。依据：文件头 docstring L1-15 与 L47-75 的包装器、L218-284 的统计与判据段。
- 模块级 docstring 首行: `只读：100k 策略的**逐帧奖励分量分解**（引擎真值，不做重建）。`
- 主要导入: argparse, json, os, sys, time, numpy(as np), torch, rl.env_wrapper(as EW), rl.config(TrainConfig), rl.belief(BeliefInference), rl.belief_planner(BeliefPlanner), rl.plan_space(PLAN_DIM), rl.follower(FollowerPolicy, load_checkpoint), rl.train_follower(FollowerOpponent), rl.train_solo(solo_env, resolve_deck_set), rl.ppo(PPOTrainer)
  - 备注：`PPOTrainer`（L44）在本文件内**未被使用**（全文仅此一处出现）；`import rl.env_wrapper as EW` 仅用于取/替换 `EW.compute_reward`（L48、L75）。
- 关键模块级常量:
  - `_HERE = os.path.dirname(os.path.abspath(__file__))` — 本脚本所在目录（L28）
  - `_SRC = os.path.join(os.path.dirname(_HERE), "src", "clasher_new")` — 仓库源码根，L30-31 在缺失时插到 `sys.path[0]`，使 `rl.*` 与 `battle` 可 import
  - `REC = {}` — "最近一次 `compute_reward` 调用的分量"，由 `_install_recorder` 写入、由 `main` 逐帧读取（L46-47、L64-73、L173）
  - `_ORIG = EW.compute_reward` — 保存原始函数引用，包装器以内层 `_ORIG` 计算真值与变体（L48、L53、L58）
- 顶层数据表/字典: 无静态数据表；`REC` 是运行时被覆写的字典（键 `full/edw/crown/tower/terminal/edw_coef/tower_opp_coef`，L65-73）。

### S:_install_recorder  [L51-75]
- 类型: function
- 签名: `def _install_recorder():`
- 作用: 用闭包 `wrapped` 替换模块级 `EW.compute_reward`，使每次奖励计算同时算出 `full` 与 4 个"去某项"变体的差值并写入全局 `REC`，然后原样返回 `full`。
- 参数: 无
- 返回: 无返回值（副作用：改写 `EW.compute_reward` 与 `REC`）
- 实现: L52 定义 `wrapped(rw, **kw)`：先 `full = _ORIG(rw, **kw)`（L53）；L55-58 定义内层 `variant(**over)`，做法是 `dict(rw)` 拷贝后 `update(over)` 再调 `_ORIG`；L60-63 依次构造 `no_edw`（`elixir_diff_weight=0.0`）、`no_crown`（`crown_weight=0.0, crown_lose_weight=0.0`）、`no_tower`（`tower_dmg_opp=0.0, tower_dmg_self=0.0`）、`no_term`（`win_bonus=0.0, lose_penalty=0.0, draw_penalty=0.0`）；L64-73 `REC.clear()` 后写入 5 个差值（`full`、`full - no_edw` 等）以及从 `rw` 里读出的 `edw_coef`、`tower_opp_coef`；L74 返回 `full`；L75 完成替换。差值即"精确项"的前提是各项对权重线性——已在 `rl/env_wrapper.py:240-253`（`reward = rw["crown_weight"]*(...) + rw["tower_dmg_opp"]*red_dmg ...`）与 `L243-248`（`reward += edw * ((me_a-op_a)-(me_b-op_b))`）确认。
- 调用: 被 `main` 在 L103 调用一次；`wrapped` 由 `rl.env_wrapper.RLEnv.step` 内的全局名 `compute_reward(rw, ...)`（`rl/env_wrapper.py:678`，2026-09-14 更正：原文 684 有 6 行偏差）在每次 step 时调用。已知前提由 `main` L175-176 在 `REC` 为空时 `raise SystemExit` 自检。
- 置信度: 已确认
  - 附加事实（依据 `rl/env_wrapper.py`）：`RLEnv.step` 在调用前已把"两段价格"解析结果写回 `rw["tower_dmg_opp"] / rw["tower_dmg_self"] / rw["elixir_diff_weight"]`（`env_wrapper.py:678-683` 调 `_phase_weights`，L169-178 定义 120s 切档），而 `compute_reward` 内部不再重新解析（`env_wrapper.py:218-249` 直接读 `rw`），因此"把这三个键置 0"在前后两段都能真正关掉对应项；`compute_reward` 开头 `rw = dict(_DEFAULT_REWARD, **(rw or {}))`（`env_wrapper.py:209`）保证显式 0 值覆盖默认值生效。

### S:_stat  [L78-88]
- 类型: function
- 签名: `def _stat(x):`
- 作用: 把一维数值序列压成一份固定键的统计字典（个数/均值/中位数/和/绝对值和/10 与 90 分位/最大绝对值），用于打印与 JSON 落盘。
- 参数:
  - `x`: 任意可转 `np.asarray(..., dtype=np.float64)` 的序列（调用处传入 `rows[k]` 这类 Python list）
- 返回: `dict`；空输入返回 `{"n": 0}`（L80-81），否则键为 `n/mean/median/sum/sum_abs/p10/p90/max_abs`，数值统一 `round(..., 6)`（`sum`/`sum_abs` 为 4 位小数）
- 实现: L79 `np.asarray(x, dtype=np.float64)`；L80-81 空数组提前返回 `{"n": 0}`；L82-88 用 `mean/median/sum/abs().sum()/quantile(0.10)/quantile(0.90)/abs().max()` 组装返回字典，全部经 `round` 截断。
- 调用: 被 `main` 在 L229（§1 逐分量）与 L294（JSON 的 `stats`）调用。
- 置信度: 已确认

### S:main  [L91-307]
- 类型: function
- 签名: `def main():`
- 作用: 探针主流程：解析参数 → 装记录器 → 建环境/加载 ckpt/装镜像对手 → 跑 rollout 逐帧分解奖励 → 打印占比/分期/Φ → 按预注册判据给 verdict → 可选写 JSON。
- 参数: 无（参数由 `argparse` 从命令行取：`--ckpt`(必填) / `--run-dir` / `--mode`(stoch|det，default stoch) / `--frames`(int，default 4000) / `--seed`(int，default 7) / `--device`(default cpu) / `--out` / `--verbose`，L92-101）
- 返回: `dict`（`res`，L286-302）；同时把同一 dict 以 `json.dump(..., ensure_ascii=False, indent=1)` 写到 `--out`（L303-306）。`__main__` 调用（L310-311）忽略该返回值。
- 实现: L103 先装记录器；L105-115 用 `TrainConfig.resolve("economy")` 建配置，再从 `run_dir/config.json` 逐键覆盖 `cfg`（仅 `hasattr` 命中的键，异常吞掉）；L117-123 决定 device、`solo_env(cfg, seed)` 建镜像环境、用 `BeliefInference(...).encode(None, None)` 的长度当 `belief_dim`，`load_checkpoint(ckpt, hidden_dim, plan_dim=PLAN_DIM, belief_dim)` 载入策略并 `to_device(device).eval()`；L128-139 打印 `env.reward_weights` 中的奖励权重白名单（为空则告警"权重取自默认表"）；L141-150 另建一个 `FollowerPolicy` 作为对手、`load_state_dict` 复制同一权重、包进 `FollowerOpponent`（`deterministic=(mode=="det")`），并丢弃 `resolve_deck_set(...)` 的返回值；L152-156 建 `BeliefPlanner`/`BeliefInference`，`env.reset(seed)` 并把 `hidden` 置 None；L165-210 主循环：每帧先 `bp.plan(...)`、`belief.encode(obs, None)`、`plan.to_vector()`，再 `pol.act(obs, tok, pv, env.get_action_mask, hidden, deterministic)` 得 bundle，**在 `env.step` 前** `REC.clear()`（L173），step 后断言 `REC` 非空，否则 `SystemExit`，然后把 `full`、`unit = r - full`（L182，单位项在 `compute_reward` 之外由 `env_wrapper.py:699-701` 加）、`edw/crown/tower/terminal` 以及 t/圣水/field_v/Φ/部署数写入本局缓冲；`term or trunc` 时把本局缓冲批量并入全局 `rows`/`extra`、记录局长、`env.reset(seed + n_frame)`、清 hidden（L195-210）；L216-217 若没有任何完整一局则 `SystemExit`；L218-226 用 5 项 `Σ|·|` 求占比 `share`；L227-235 打印 §1；L237-249 用 `extra["t"]` 按 `t<120` / `t>=120` 分两组打印各组占比与平均 `edw_coef`；L251-267 打印 §3（`elixir0/field_v0/Φ_me/field_v1/Φ_opp/ΔΦ`，以及"有部署帧 vs 空帧"的 edw 均值对比，用 `dep >= 1` 分组）；L270-284 判据：`share["edw"]>=0.50 且 share["tower"]>=0.50 → "W1+W2"`、仅 edw → `W1_REWARD_DOMINATED_BY_RESOURCE_ACCOUNT`、仅 tower → `W2_REWARD_DOMINATED_BY_TOWER_DAMAGE`、都不满足 → `W4_NO_DOMINANT_TERM`，并额外算布尔 `w3 = share["terminal"] < 0.10`；L286-306 组装并（可选）落盘 JSON。
- 调用: 由本文件 `__main__`（L310-311）调用；仓库内无其他 Python 文件引用本脚本（全仓 grep `probe_reward_composition` 仅命中本文件自身）。它调用了 `_install_recorder`、`_stat`、`TrainConfig.resolve`、`solo_env`、`BeliefInference`、`load_checkpoint`、`FollowerPolicy`、`FollowerOpponent`、`resolve_deck_set`、`BeliefPlanner`、`env.reset/step`、`pol.act`、`plan.to_vector`、`belief.update`；`EW.compute_reward` 的**实际**调用点在 `rl/env_wrapper.py:684`。
- 置信度: 已确认
  - 一处需注意的口径细节（依据 `rl/env_wrapper.py:40-58`）：5 个分量里没有单独的"`elixir_bonus` 项"；`_DEFAULT_REWARD["elixir_bonus"] = 0.0` 故默认下不影响读数，但若传入非 0 的 `elixir_bonus`，该项会留在 `full` 内而不被任何变体扣除。同时 `unit` 分量由 `r - full` 反推（L182），其含义是"`step_reward` 中 `compute_reward` 之外加的那部分"（默认即 `unit_dmg_k` 项）。

## F:scripts/test_m4_evo7.py
- 行数: 297
- 模块作用: 一个**独立可运行的验收脚本**（非 pytest）：把 `src/clasher_new` 插入 `sys.path` 并 `chdir` 到该目录，构造 `BattleState` 直接调用引擎钩子，对 M6 批"2025 新觉醒 7 张卡"逐张断言（Princess 减速箭/死亡领域、MinionHorde 首击面纱、RoyalHogs 起飞落地、Ghost 显形召唤、SkeletonArmy 的 General Gerry 与亡影、BabyDragon 气流、Furnace 热生成），外加一条"觉醒周期触发路径"断言；末尾打印通过/失败并按 `FAIL` 决定退出码。依据：docstring L2-12、L13-20 的路径/导入、L286-297 的 runner。
- 模块级 docstring 首行: `M6 — 2025 新觉醒 7 张验收（快照无 evolvedSpellsData, 数据层 evo_2025_data.py + 引擎钩子）。`
- 主要导入: sys, os, battle, battle(BattleState, Troop, Building, Position, Entity, EvoEffectZone, interpret_action_group), player(PlayerState)；函数内 `from card_utils import Card`（L235）与 `traceback`（L294）
  - 备注：`import battle`（L17）、`Entity`、`EvoEffectZone`、`interpret_action_group`（L18-19）在本文件内**均未被使用**（全文无 `battle.` 形式的调用）。
- 关键模块级常量: `PASS = FAIL = 0`（L22）— 断言通过/失败计数器，由 `check` 用 `global` 累加（L24-26）。
- 顶层数据表/字典: 无。两副 8 卡卡组是 `make_battle` 内的字面量（L29-30），断言文案与期望数值散在各 test 函数内。

### S:check  [L23-26]
- 类型: function
- 签名: `def check(name, cond, extra=''):`
- 作用: 单调计数器式断言：`cond` 为真自增 `PASS` 并打印 `OK <name>`，否则自增 `FAIL` 并打印 `FAIL <name>`；`extra` 非空时方括号附在后面。
- 参数:
  - `name`: 断言名（打印用）
  - `cond`: 任意真值表达式（不抛异常、不记录，仅判真假）
  - `extra=''`: 附加信息字符串（如实测值）
- 返回: 无
- 实现: L24 `global PASS, FAIL`；L25 真分支 `PASS += 1` 并 `print(f'  OK {name}' + (f'  [{extra}]' if extra else ''))`；L26 假分支同理累加 `FAIL` 打印 `FAIL`。不中断执行。
- 调用: 被本文件所有 8 个 test 函数调用；`PASS/FAIL` 在 L296 汇总、L297 `sys.exit(1 if FAIL else 0)`。
- 置信度: 已确认

### S:make_battle  [L28-34]
- 类型: function
- 签名: `def make_battle(elixir=10):`
- 作用: 用两副写死的 8 卡卡组构造一个 `BattleState`，并把双方圣水都顶到 `elixir`（构造后再次显式赋值）。
- 参数:
  - `elixir=10`: 双方初始圣水
- 返回: `BattleState` 实例
- 实现: L29-30 定义 `d0`（Knight/Archers/Fireball/Giant/Musketeer/Arrows/Minions/Cannon）与 `d1`（Knight/MiniPekka/Arrows/Minions/Musketeer/Fireball/Giant/Archer）；L31 用 `PlayerState(0, d0, elixir)` 与 `PlayerState(1, d1, elixir)` 构造 `BattleState`；L32-33 再把 `bs.players[0].elixir`、`bs.players[1].elixir` 显式设为 `elixir`（说明构造后可能被重置/校验）；L34 返回。依据 `player.py:6-12`，`PlayerState(player_id, cycle_queue, elixir, tower_hps=(4824,3052,3052))`。
- 调用: 被 `test_princess_slow_shot`(L59)、`test_minionhorde_first_hit_veil`(L83)、`test_royalhogs_flying_landing`(L101)、`test_ghost_souldier_summon`(L126)、`test_skeletonarmy_general_gerry`(L166)、`test_babydragon_gust`(L209)、`test_furnace_hot_spawn`(L243)、`test_cycle_trigger_path`(L270) 调用。
- 置信度: 已确认

### S:kill_blue_towers  [L36-38]
- 类型: function
- 签名: `def kill_blue_towers(bs):`
- 作用: 把实体 id 3、4、6 的 `is_alive` 直接置 False（= 蓝方两座公主塔与蓝方王塔），用于排除塔的攻击干扰。
- 参数:
  - `bs`: `BattleState`
- 返回: 无
- 实现: L37-38 遍历 `(3, 4, 6)`，对 `bs.entities[eid].is_alive = False`。依据 `battle.py:2556-2561`，id 3/4 = `BLUE_LEFT_TOWER`/`BLUE_RIGHT_TOWER`（player 0），id 6 = `BLUE_KING_TOWER`（player 0）；蓝方是 player 0（`arena.py:11-12`：BLUE 塔在 y=3.0/6.5 一侧）。
- 调用: 被本文件 7 个 test 函数调用（L60、L102、L117、L127、L167、L210、L244）。注：直接改 `is_alive` **不改 hp**，而 `BattleState.update_player_hp` 只同步 `.hp`（`battle.py:2642-2651`）、`PlayerState.get_crown_count` 也只判 `hp<=0`（`player.py:54-58`），因此本函数不会因"杀王塔"而触发 `step()` 的 `game_over` 判定（`battle.py:2653-2659`）。
- 置信度: 已确认

### S:spawn_troop  [L40-43]
- 类型: function
- 签名: `def spawn_troop(bs, card, x, y, player, evolved=False):`
- 作用: 用给定坐标/归属/觉醒标志构造一个 `Troop` 并注册进战场，返回该实体（部署延迟由调用方自行清零）。
- 参数:
  - `bs`: `BattleState`（提供 `next_entity_id` 与 `_spawn_entity`）
  - `card`: 卡名字符串
  - `x`, `y`: 世界坐标
  - `player`: 归属玩家号
  - `evolved=False`: 是否觉醒形态
- 返回: 新建的 `Troop`
- 实现: L41 `Troop(bs.next_entity_id, Position(x, y), player, card, bs, evolved=evolved)`；L42 `bs._spawn_entity(t)`；L43 返回 `t`。`_spawn_entity` 会做走位校正并递增 `next_entity_id`（`battle.py:2603-2608`）；`Troop.__init__(id, position, player, card_name, battle_state=None, evolved=False)`（`battle.py:760`）。
- 调用: 被本文件全部 8 个 test 函数调用（如 L61-63、L84、L103、L128、L168-169、L211、L216-217、L245 为 `spawn_building`）。
- 置信度: 已确认

### S:spawn_building  [L45-48]
- 类型: function
- 签名: `def spawn_building(bs, card, x, y, player, evolved=False):`
- 作用: 构造一个 `Building` 并注册进战场，返回该实体。
- 参数:
  - `bs`: `BattleState`
  - `card`: 卡名
  - `x`, `y`: 世界坐标
  - `player`: 归属玩家号
  - `evolved=False`: 是否觉醒形态
- 返回: 新建的 `Building`
- 实现: L46 `Building(bs.next_entity_id, Position(x, y), player, card, False, evolved=evolved)`——第 5 个位置参数是 `persistent=False`；L47 `bs._spawn_entity(b)`；L48 返回。`Building.__init__(id, position, player, card_name, persistent=False, evolved=False)`（`battle.py:1234`），注意该构造函数**不接收** `battle_state`（由 `_spawn_entity` 注入，`battle.py:2604`）。
- 调用: 仅被 `test_furnace_hot_spawn` 调用（L245，`FirespiritHut` 觉醒建筑）。
- 置信度: 已确认

### S:step_for  [L50-52]
- 类型: function
- 签名: `def step_for(bs, seconds):`
- 作用: 以 1/60 s 固定步长推进战斗 `int(seconds*60)` 次。
- 参数:
  - `bs`: `BattleState`
  - `seconds`: 秒数（内部按 `int(seconds * 60)` 取整，故非 1/60 整数倍的尾数被丢弃）
- 返回: 无
- 实现: L51-52 `for _ in range(int(seconds * 60)): bs.step(1 / 60)`。
- 调用: 被 6 个 test 函数调用（L67、L73、L91、L118、L140、L144、L155、L157、L192、L198、L219、L223、L227、L229、L250-252 处另有内联 `b.step(1/60)` 循环）。
- 置信度: 已确认

### S:test_princess_slow_shot  [L56-76]
- 类型: function
- 签名: `def test_princess_slow_shot():`
- 作用: 断言觉醒公主的"首发射击附加 30% 减速（×0.70，约 5.5s）"与"死亡后留下半径 3.0 的减速领域"两条机制。
- 参数: 无
- 返回: 无（结果经 `check` 计数）
- 实现: L57 docstring 写明口径"首发减速（半径 3.0 / 30% / 5.5s, 每 2 发循环, 4/8/2026 平衡口径）"；L59-63 建战场并关蓝塔，在 (9,14) 放 player 0 的觉醒 `Princess`，敌方放 `near` Knight(9,12)（"距目标点 0 → 减速圈内"）与 `far` Knight(3,3)（"圈外"）；L64 三方 `deploy_delay_remaining = 0`；L65-66 手动把 `pr.target_id` 设为 near 并直接调 `pr.entity_holder.on_attack(near)`（不等待自然接敌）；L67 `step_for(b, 0.1)`；L68-70 断言 `near.speed_debuff ≈ 0.70`、`near.debuff_time_remaining ∈ [5.0,5.5]`、`far.speed_debuff ≈ 1.0`（圈外不受影响）；L71 断言 `pr._evo_attack_count == 1`（第 2 发不重复触发，字段见 `battle.py:963-965`）；L72 `pr.take_damage(pr.hp + pr.shield_health + 1)` 击杀；L73 `step_for(b, 0.2)`；L74-76 收集存活且 `name == 'Princess_EV1_DeathZone'` 的实体，断言恰好 1 个、`radius ≈ 3.0`、`lifetime > 5.0`（该 label 见 `battle.py:1044`）。
- 调用: 由 `__main__` runner（L287-290）调用；调用 `make_battle`/`kill_blue_towers`/`spawn_troop`/`step_for`/`check`；触发引擎侧 `card_mechanics` 的 Princess 觉醒钩子与 `battle.py:1044` 的死亡领域生成。减速数值落点见 `battle.py:129-134`（`speed_buff`/`speed_debuff`）。
- 置信度: 已确认

### S:test_minionhorde_first_hit_veil  [L80-94]
- 类型: function
- 签名: `def test_minionhorde_first_hit_veil():`
- 作用: 断言觉醒亡灵大军"每成员首次受击被闪避 + 进入 0.67s 无敌窗口，窗口结束后正常受伤"。
- 参数: 无
- 返回: 无
- 实现: L81 docstring 注明 0.67s 为"假设值"；L83-85 在 (9,12) 放 player 0 的觉醒 `MinionHorde` 并把 `deploy_delay_remaining` 清零（注释：面纱计时在 `update` 中走表）；L86 断言 `m._evo_veil_time == 0.67`（该值在装配时写入，见 `battle.py:821-824` 读 `evo2025Hooks.firstHitVeil`，数据值见 `evo_2025_data.py:67`）；L87-90 记录 `hp0`，`m.take_damage(50)` 后断言 HP 不变且 `m.invincible is True`（引擎侧 `battle.py:511-515`：首次受击置 `_evo_veil_timer` + `invincible=True` 后 `return`）；L91-92 `step_for(b, 0.8)` 后断言 `invincible is False`（倒计时在 `battle.py:350-352`）；L93-94 再次 `take_damage(50)` 断言 HP 恰好减 50（`_evo_veil_used` 已置位，走正常伤害路径）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`spawn_troop`/`step_for`/`check`；被断言的行为在 `battle.py:350-352`、`511-515`、`821-824`。
- 置信度: 已确认

### S:test_royalhogs_flying_landing  [L98-119]
- 类型: function
- 签名: `def test_royalhogs_flying_landing():`
- 作用: 断言觉醒皇家野猪"部署即飞行（地面单位选不中）→ 受击或攻击时落地并造成一次落地 AoE（lv11≈43）"。
- 参数: 无
- 返回: 无
- 实现: L99 docstring 写明"lv11=43, 平衡后口径"；L101-103 关蓝塔，在 (9,10) 放 player 0 的觉醒 `RoyalHogs`；L104 断言 `hog.data.is_air_unit is True`（装配侧 `battle.py:826-833` 起飞并算落地伤害）；L105-106 断言 `hog._evo_landing['damage'] == 43`；L107-108 在 (9,11) 放敌方 Knight 并把其部署延迟清零（注释：距离 1.0 < 落地半径 1.5）；L109-111 记录 `khp0`，用 `hog.take_damage(1, source=k)` 制造"受击"并断言 `hog.data.is_air_unit is False`（引擎侧 `battle.py:529-535`：落地 + `deal_area_damage(..., radius, damage, True, True)`）；L112 断言 `abs(k.data.hp - k.hp - 43) <= 1`（`deal_area_damage` 为即时结算，`battle.py:3250-3265`）；L114-119 第二只 `hog2`(9,18) 与敌方 Giant(9,19.5)，清零延迟后再次关蓝塔并 `step_for(b, 3.0)`，断言 `hog2.data.is_air_unit is False`（"攻击目标即落地"）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_blue_towers`/`spawn_troop`/`step_for`/`check`；相关引擎分支 `battle.py:529-535`、`826-833`、`3250-3265`。
- 置信度: 已确认

### S:test_ghost_souldier_summon  [L123-158]
- 类型: function
- 签名: `def test_ghost_souldier_summon():`
- 作用: 断言觉醒皇家幽灵"部署即隐身（不可选取）→ 首次攻击显形并召唤 2 名 Souldier（召唤伤害 81@lv11）→ Souldier 不自然消失 → 脱战 2s 后重新隐身"。
- 参数: 无
- 返回: 无
- 实现: L124 docstring 给出上述口径；L126-129 关蓝塔、在 (9,12) 放觉醒 `Ghost` 并断言 `gh.targetable is False`；L130-135 在 (9,12.3) 放敌方 Knight、清零延迟、手动设 `target_id` 并调 `entity_holder.on_attack(k)`，断言 `gh.targetable is True`（显形）；L136-139 统计存活且 `name == 'Souldier'` 的实体，断言数量为 2 且 `souls[0].hp ≈ 81`（注释标 [Fandom]）；L140-142 `step_for(b, 0.3)` 后用 `khp0 - k.hp` 断言落在 `[380, 470]`（"幽灵攻击+2×召唤伤害 ≈423"）；L144-146 再 `step_for(b, 5.0)` 后断言 Souldier 仍为 2（Fandom 策略节"do not despawn naturally"）；L147-152 再放 `k2`(9.3,12) 并手动攻击，断言"再次显形"；L153-154 用 `take_damage(99999)` 清掉 k 与 k2（注释：首个骑士仍活着会刷新显形计时）；L155-156 `step_for(1.5)` 断言 `targetable is True`；L157-158 再 `step_for(0.7)` 断言 `targetable is False`（脱战 2s 后再隐身，觉醒延迟 2.0s）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_blue_towers`/`spawn_troop`/`step_for`/`check`；Souldier 派生角色在 `evo_2025_data.py:145` 注册（L12 注释：注册 3 个派生角色）。
- 置信度: 已确认

### S:test_skeletonarmy_general_gerry  [L162-202]
- 类型: function
- 签名: `def test_skeletonarmy_general_gerry():`
- 作用: 断言觉醒骷髅军团"同批部署只生成 1 个 General Gerry（HP=护盾=32、排在己方后排）"、"Gerry 存活时阵亡骷髅转成无敌且不可选取的亡影（仅法术可穿透）"、"Gerry 阵亡后亡影全灭且骷髅不再转化"。
- 参数: 无
- 返回: 无
- 实现: L163-164 docstring 给出上述口径（含"内存: HP/护盾/伤害=32"）；L166-169 关蓝塔后在 (8,12) 与 (10,12) 连放两只 player 0 的觉醒 `SkeletonArmy`（构成"同批"）；L170-171 过滤 `_evo2025_is_gerry` 且存活的实体，断言数量为 1（引擎侧分组逻辑 `battle.py:856-864`）；L173-176 断言 `hp == 32 and shield_health == 32`、`position.y < 12.0`（后排）；L177-179 击杀 `s1` 后过滤 `_evo2025_is_shadow` 断言恰好 1 个亡影；L182-187 断言亡影 `invincible is True and targetable is False`，`take_damage(500)` 不掉血，而 `take_damage(500, pierce_invincible=True)` 掉血（`battle.py:507-516` 的 `pierce_invincible` 分支）；L189-195 对 Gerry 连打两次 `take_damage(999)`（先破 32 护盾再破 32 HP）后 `step_for(0.2)`，断言存活的亡影数为 0；L196-200 记录 Gerry 阵亡前 `_evo2025_is_shadow` 实体总数 `n_before`，击杀 `s2` 并步进后断言 `n_after == n_before`（不再转化）；L201-202 最后一条 `check('存活骷髅不受 Gerry 阵亡影响', ... or True)`——其表达式 `not any(... for e in []) or True` 恒为真（空列表 `any` 为 False ⇒ `not False` 为 True，再 `or True`），**该断言不具区分力**，属恒真占位。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_blue_towers`/`spawn_troop`/`step_for`/`check`；相关引擎分支 `battle.py:856-864`、`507-516`、`3158`（`_evo2025_is_gerry` 分支）。
- 置信度: 已确认（含 L201-202 恒真断言这一事实的确认）

### S:test_babydragon_gust  [L206-230]
- 类型: function
- 签名: `def test_babydragon_gust():`
- 作用: 断言觉醒幼龙"攻击期间友军 +30% / 敌军 -30%（半径 4.0 假设）"、"觉醒弹道伤害为基础弹道伤害 ×1.04"、"死亡后气流残留约 2s 且残留期内减速仍生效、到期解除"。
- 参数: 无
- 返回: 无
- 实现: L207 docstring 注明半径 4.0 为假设、8×9 格口径；L209-211 关蓝塔后在 (9,12) 放觉醒 `BabyDragon`；L212-215 断言 `abs(dr.data.projectile_data.damage - round(Card_base_damage()*1.04)) <= 1`（注释：基础幼龙伤害在弹道 `data.damage=0`）；L216-218 放敌方 `enemy`(9,13.2) 与友方 `ally`(9,10.2)，三方延迟清零；L219-221 `step_for(1.0)` 后断言 `ally.speed_buff ≈ 1.30`、`enemy.speed_debuff ≈ 0.70`；L222-226 `dr.take_damage(dr.hp + 1)` 击杀后 `step_for(0.3)`，断言存活且 `name == 'BabyDragon_EV1_Gust'` 的领域恰好 1 个且 `1.5 <= lifetime <= 2.0`（label 见 `battle.py:1055`）；L227-228 再 `step_for(1.0)` 断言 `enemy.speed_debuff ≈ 0.70`（残留期仍减速）；L229-230 再 `step_for(2.5)` 断言恢复 `≈1.0`。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_blue_towers`/`spawn_troop`/`step_for`/`check` 与同文件 `Card_base_damage`（L214-215）。
- 置信度: 已确认

### S:Card_base_damage  [L233-236]
- 类型: function
- 签名: `def Card_base_damage():`
- 作用: 取"基础幼龙"（未觉醒 `BabyDragon`）的弹道伤害作为对照基准值。
- 参数: 无
- 返回: `Card('BabyDragon').projectile_data.damage`（数值）
- 实现: L234 docstring 说明"基础幼龙伤害载体 = 弹道（data.damage=0, 伤害在 projectileData）"；L235 函数内 `from card_utils import Card`；L236 返回 `Card('BabyDragon').projectile_data.damage`。`Card.__init__(card_name, level=None)`（`card_utils.py:222`），`projectile_data` 在 `card_utils.py:253` 构造，其 `damage` 可被等级表覆写（`card_utils.py:356-359`）。
- 调用: 仅被 `test_babydragon_gust` 调用（L214-215，两次）。
- 置信度: 已确认

### S:test_furnace_hot_spawn  [L240-259]
- 类型: function
- 签名: `def test_furnace_hot_spawn():`
- 作用: 断言觉醒火焰熔炉"攻击期间以 2.4s 间隔热生成火灵，且左右侧交替生成"。
- 参数: 无
- 返回: 无
- 实现: L241 docstring 写"热生成 2.4s/灵（数值同原版），左侧→右侧交替生成"；L243-245 关蓝塔后在 (4,6) 放 player 0 的觉醒建筑 `FirespiritHut`；L246-247 断言 `(fu.evo or {}).get('evo2025Hooks', {}).get('hotSpawn', {}).get('interval') == 2.4`（数据侧 `evo_2025_data.py:128`，消费侧 `battle.py:1401`）；L248-256 手写 12s（`int(12*60)` 次）`b.step(1/60)` 循环，用 `seen` 记录每个新出现的 `FireSpirits` 实体 id 相对熔炉 x 的符号（`e.position.x > fu.position.x` 记 +1，否则 -1）并按出现顺序追加到 `order`；L257 断言 12s 内至少出现 3 个（≥3）；L258 断言 `order[0] == -1`（首个从左侧）；L259 断言 `len(order) >= 2 and order[1] == 1`（第二个在右侧 = 左右交替）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_blue_towers`/`spawn_building`/`check`，并直接内联 `b.step`（L250-252）。
- 置信度: 已确认

### S:test_cycle_trigger_path  [L263-283]
- 类型: function
- 签名: `def test_cycle_trigger_path():`
- 作用: 对 7 张卡逐一验证"觉醒周期触发路径畅通"：把该卡放进觉醒槽并置入循环队首后连续出牌 `times` 次，断言每次部署都成功且最后一次部署出的是带 `evo` 属性的觉醒实体。
- 参数: 无
- 返回: 无
- 实现: L264-265 docstring 说明这 7 张此前无 `evo_raw`，本测试确认 `Card.evo_raw` 注入 + `evolution_state` 周期触发（`cycle=2` → 第 3 次出牌为觉醒形态；建筑同样适用）；L267-269 遍历 `(('Princess',3), ('MinionHorde',2), ('RoyalHogs',3), ('Ghost',3), ('SkeletonArmy',3), ('BabyDragon',3), ('FirespiritHut',3))`；L270-273 每张新建战场、取 player 0、`set_evolution_slots([card])`（`player.py:17-19`，最多 2 张）、并把该卡挪到 `p.cycle` 首位；L276-281 循环 `times` 次：每次重置 `p.elixir = 10` 与队首，`ok = ok and b.deploy_card(0, card, Position(9.0, 12.0))`，并把 `last_evolved` 更新为"场上存在该卡且 player 0 且带真值 `evo` 属性的实体"；L282-283 断言 `ok and last_evolved`。该函数**不刷新** `last_card`/`last_card_form` 之外的状态，也不清场，因此同一战场内多次部署的实体共存；断言只看"最后一次部署后场上是否存在该卡的觉醒实体"。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`check` 与 `PlayerState.set_evolution_slots`（`player.py:17`）、`BattleState.deploy_card`（`battle.py:2806`）。
- 置信度: 已确认

## F:scripts/test_m5_data.py
- 行数: 294
- 模块作用: 与 `test_m4_evo7.py` 同形的独立验收脚本（非 pytest）：把 `src/clasher_new` 插入 `sys.path` 并 `chdir` 进去，直接构造 `BattleState` 对 M7 批"三张非觉醒的数据接入卡"做机制断言——Ronin 被动格挡反击、Vines 藤蔓束缚（法术领域）、Spirit Empress 双费用形态（部署规则）；末尾按 `FAIL` 决定退出码。依据：docstring L2-15、L16-22、L283-294。
- 模块级 docstring 首行: `M7 — 数据接入三卡引擎机制验收（Ronin 格挡反击 / Vines 藤蔓束缚 / Spirit Empress 双费用形态）。`
- 主要导入: sys, os, battle(BattleState, Troop, Position), card_utils(Card), player(PlayerState)；`traceback` 在 runner 内导入（L291）
- 关键模块级常量: `PASS = FAIL = 0`（L24）— 断言计数器，由 `check`（L25-28）用 `global` 累加。
- 顶层数据表/字典: 无。卡组是 `make_battle` 内的字面量（L31-33）与 `_mm_battle` 内改写（L201）。

### S:check  [L25-28]
- 类型: function
- 签名: `def check(name, cond, extra=''):`
- 作用: 与 `test_m4_evo7.py` 的同名函数逐字同形：真则 `PASS += 1` 打印 `OK`，假则 `FAIL += 1` 打印 `FAIL`，`extra` 非空时方括号附加。
- 参数:
  - `name`: 断言名
  - `cond`: 真值表达式
  - `extra=''`: 附加信息
- 返回: 无
- 实现: L26 `global PASS, FAIL`；L27 真分支自增并打印；L28 假分支自增并打印；不中断执行。
- 调用: 被本文件 9 个 test 函数调用；`PASS/FAIL` 在 L293 汇总、L294 `sys.exit(1 if FAIL else 0)`。
- 置信度: 已确认

### S:make_battle  [L30-34]
- 类型: function
- 签名: `def make_battle():`
- 作用: 构造一副固定卡组（含被测三卡）对 `['Knight']*8` 的 `BattleState`，双方圣水均为 10。
- 参数: 无
- 返回: `BattleState`
- 实现: L31-33 `BattleState(PlayerState(0, ['Ronin','MiniPekka','Vines','MergeMaiden','Musketeer','Arrows','Minions','Giant'], 10), PlayerState(1, ['Knight']*8, 10))`，无额外赋值；L34 返回。注意：与 `test_m4_evo7.make_battle` 不同，本函数**不**接收 `elixir` 参数、也不二次赋圣水（圣水由各测试自己改，如 L148、L236）。
- 调用: 被 `test_ronin_data`(L57)、`test_ronin_parry_reflect`(L69)、`test_ronin_cooldown_and_ranged`(L85)、`test_vines_data_and_zone`(L115)、`test_vines_two_hits_and_tower`(L135)、`test_vines_ground_snare_top3`(L161 与 L180)、`_mm_battle`(L199) 调用。
- 置信度: 已确认

### S:kill_princess_towers  [L36-39]
- 类型: function
- 签名: `def kill_princess_towers(bs):`
- 作用: 把实体 id 1、2、3、4（双方四座公主塔）的 `is_alive` 置 False，并附注"King 塔未激活不攻击"，以排除塔防干扰。
- 参数:
  - `bs`: `BattleState`
- 返回: 无
- 实现: L37 为 docstring；L38-39 遍历 `(1, 2, 3, 4)` 置 `bs.entities[eid].is_alive = False`。依据 `battle.py:2556-2559`：id 1/2 = RED 左/右公主塔（player 1），id 3/4 = BLUE 左/右公主塔（player 0）。
- 调用: 被本文件 8 处调用（L70、L86、L116、L162、L181、L203，以及 `test_vines_two_hits_and_tower` 只用 (3,4) 的等价写法 L137-138）。
- 置信度: 已确认

### S:spawn_troop  [L41-45]
- 类型: function
- 签名: `def spawn_troop(bs, card, x, y, player):`
- 作用: 构造 `Troop`、注册进战场，并**在函数内**把 `deploy_delay_remaining` 清零后返回（与 m4 版本不同点）。
- 参数:
  - `bs`: `BattleState`
  - `card`: 卡名
  - `x`, `y`: 世界坐标
  - `player`: 归属玩家号
- 返回: 新建的 `Troop`（已就绪、无部署延迟）
- 实现: L42 `Troop(bs.next_entity_id, Position(x, y), player, card, bs)`（未传 `evolved`，即非觉醒）；L43 `bs._spawn_entity(t)`；L44 `t.deploy_delay_remaining = 0.0`；L45 返回 `t`。
- 调用: 被 `test_ronin_*`、`test_vines_*` 各测试调用（如 L58、L71-72、L87-89、L99、L117、L139、L163-164、L182-185）。
- 置信度: 已确认

### S:step_for  [L47-49]
- 类型: function
- 签名: `def step_for(bs, seconds):`
- 作用: 以 1/60 s 步长推进 `int(seconds*60)` 次。
- 参数:
  - `bs`: `BattleState`
  - `seconds`: 秒数（按 `int` 截断）
- 返回: 无
- 实现: L48-49 `for _ in range(int(seconds * 60)): bs.step(1 / 60)`。
- 调用: 被 `test_ronin_parry_reflect`(L75)、`test_ronin_cooldown_and_ranged`(L94、L96)、`test_vines_data_and_zone`(L119)、`test_vines_two_hits_and_tower`(L142、L152)、`test_vines_ground_snare_top3`(L166、L173、L176、L187)、`test_mergemaiden_forms`(L242) 调用。
- 置信度: 已确认

### S:test_ronin_data  [L54-63]
- 类型: function
- 签名: `def test_ronin_data():`
- 作用: 断言 Ronin 的格挡参数已由 gamedata 字段经 `card_mechanics.Ronin` 挂载到 `entity_holder` 上（反射 200%、冷却 3.5s、仅近战、部署即就绪）。
- 参数: 无
- 返回: 无
- 实现: L56 打印段名；L57-59 建战场并在 (9,20) 放 player 0 的 `Ronin`，取 `h = r.entity_holder`；L60 断言 `abs(h.parry_percent - 2.0) < 1e-9`（`card_mechanics.py:685`：`(parryReflectPercent or 200)/100.0`）；L61 断言 `abs(h.parry_cooldown - 3.5) < 1e-9`（`card_mechanics.py:686`：`(parryCooldownMs or 3500)/1000.0`）；L62 断言 `h.parry_melee_only is True`（`card_mechanics.py:687`）；L63 断言 `h.parry_timer == 0.0`（`card_mechanics.py:688` 初始 0；模块 docstring L13 已把"部署即就绪"标为 [假设]）。
- 调用: 由 `__main__` runner 调用（L284-292 列表首项）；调用 `make_battle`/`spawn_troop`/`check`。
- 置信度: 已确认

### S:test_ronin_parry_reflect  [L66-79]
- 类型: function
- 签名: `def test_ronin_parry_reflect():`
- 作用: 断言近战首击被格挡（本体不掉血）+ 攻击者受到 200% 反弹 + 进入约 3.5s 冷却。
- 参数: 无
- 返回: 无
- 实现: L68 打印段名；L70-72 关四座公主塔，在 (9,20) 放 Ronin，在 (16,12) 放敌方 `MiniPekka`（注释：远离战场，只作手动受击靶）；L73 记录 `rhp0, mhp0`；L74 `r.take_damage(100, source=m)` 手动制造一次受击；L75 `step_for(b, 0.1)`（6 tick，用于结算延迟反弹伤害）；L76 断言 `r.hp == rhp0`（格挡短路，`card_mechanics.py:709-711` 返回 True）；L77 断言 `abs(m.hp - (mhp0 - 200)) < 0.01`（反弹走 `source.take_damage(amount*parry_percent, delayed=True, source=e)`，`card_mechanics.py:709`）；L78-79 断言 `abs(r.entity_holder.parry_timer - 3.4) < 0.1`（3.5 冷却经 0.1s tick 递减）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_princess_towers`/`spawn_troop`/`step_for`/`check`；被断言行为在 `card_mechanics.py:696-711` 与 `battle.py:520-525`（`on_take_damage` 钩子短路点）。
- 置信度: 已确认

### S:test_ronin_cooldown_and_ranged  [L82-102]
- 类型: function
- 签名: `def test_ronin_cooldown_and_ranged():`
- 作用: 断言"冷却期间正常受伤、冷却结束后再次格挡、远程攻击不触发格挡"三条。
- 参数: 无
- 返回: 无
- 实现: L84 打印段名；L86-89 关塔后放 Ronin(9,20)、敌方 MiniPekka(16,12)、敌方 `Musketeer`(16,14)；L90-93 记录 `rhp0` 后连续两次 `r.take_damage(100, source=m)`，断言 `abs(r.hp - (rhp0 - 100)) < 0.01`（第 1 击格挡、第 2 击因 `parry_timer > 0` 走正常伤害，`card_mechanics.py:698-699`）；L94-98 `step_for(b, 3.6)`（> 3.5s 冷却）后再吃一击并 `step_for(0.1)`，断言 HP 仍为 `rhp0 - 100`（再次格挡）且 `parry_timer > 3.0`；L99-102 另放 `r2`(9,24)，用 `source=mus` 一击，断言 `abs(r2.hp - (r2hp0 - 100)) < 0.01`（Musketeer 射程 6 > `MELEE_RANGE_THRESHOLD = 1.5`，`card_mechanics.py:677-705`）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_princess_towers`/`spawn_troop`/`step_for`/`check`。
- 置信度: 已确认

### S:test_vines_data_and_zone  [L107-128]
- 类型: function
- 签名: `def test_vines_data_and_zone():`
- 作用: 断言 Vines 数值贯通（弹道伤害 lv11=153、弹速已注入）且出牌后落地生成 `Vines_AeO` 领域（半径 2.5、剩余寿命 ≤2s、锁定 ≤3 个高血目标）。
- 参数: 无
- 返回: 无
- 实现: L109 打印段名；L110-114 直接 `Card('Vines')` 断言 `abs(c.projectile_data.damage - 153) < 0.01` 与 `c.projectile_data.speed > 0`（弹速为 [假设] 600，模块 docstring L13）；L115-118 建战场、关四座公主塔、在 (9,13) 放敌方 Giant，`b.deploy_card(0, 'Vines', Position(9.0, 13.0))` 返回给 `ok`；L119 `step_for(b, 1.5)`（略去弹道飞行 ~1.0s + 落地）；L120-121 收集存活且 `name == 'Vines_AeO'` 的实体，断言 `ok and len(zones) == 1`；L122-128 在有领域时断言 `abs(z.radius - 2.5) < 0.01`、`1.0 < z.lifetime <= 2.01`（属性表口径）、`z.locked is not None and len(z.locked) <= 3`（锁定在首个 update tick 完成，`battle.py:2160-2164`）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_princess_towers`/`spawn_troop`/`step_for`/`check` 与 `Card`（L110）；引擎侧领域类 `battle.py:2103-2170`、弹道钩子 `battle.py:1449`。
- 置信度: 已确认

### S:test_vines_two_hits_and_tower  [L131-154]
- 类型: function
- 签名: `def test_vines_two_hits_and_tower():`
- 作用: 断言 Vines 对单位两跳共 306（153×2）伤害、领域 2s 后消失、对王塔类按 25% 计价（2×153×0.25=76.5）。
- 参数: 无
- 返回: 无
- 实现: L132-133 docstring 说明布景意图；L135-139 只关 **蓝方**（id 3、4）两座公主塔（注释：Giant 在蓝塔射程边界内会挨塔击），并在 (9,13) 放敌方 Giant、记录 `ghp0`；L141-143 出 Vines 后 `step_for(b, 3.2)`，断言 `abs(ghp0 - g.hp - 306) < 2.0`（两跳分别在落地与落地 +1s，跳间隔 = 领域时长/跳数，`battle.py:2124`）；L144-145 断言所有 `Vines_AeO` 均已 `is_alive == False`；L147-148 把 `'Vines'` 从 `p.cycle` 移除并插到首位、`p.elixir = 10`（手牌校验要求在前 4 位）；L149-154 取 `b.entities[5]`（注释：红方王塔 player 1）记录 `khp0`，在 (9,29) 出 Vines 后 `step_for(b, 5.4)`（横跨全场飞行 ~2.6s + 领域 2s），断言 `abs(khp0 - king.hp - 76.5) < 2.0`（对王塔降伤分支在 `battle.py:2154-2159`：`'King' in t.name` 时乘 `crown_pct`）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`spawn_troop`/`step_for`/`check` 与 `b.deploy_card`。
- 置信度: 已确认

### S:test_vines_ground_snare_top3  [L157-193]
- 类型: function
- 签名: `def test_vines_ground_snare_top3():`
- 作用: 断言 Vines 的拽落（空中单位临时落地、可被地面单位选取）、束缚（不移动/不攻击、时长 >2s、结束后复飞）与 3 目标上限。
- 参数: 无
- 返回: 无
- 实现: L158-159 docstring（拽落靶选血厚的 `MegaMinion`）；L161-165 关四座公主塔，放敌方 `MegaMinion`(9,13) 与己方 `Knight`(10,13)，出 Vines；L166 `step_for(1.5)`；L167 断言 `mn.data.is_air_unit is False`（拽落分支 `battle.py:2144-2151`）；L168-170 调 `kn.update_current_target()` 后断言 `kn.target_id == mn.id`（落地后可被地面单位锁定）；L171 断言 `mn.freeze_timer > 2.0`（`apply_buff(stun=snare_duration)`，`battle.py:2139-2141`）；L172-175 快照位置后 `step_for(0.5)`，断言 x/y 位移均 <0.05（束缚期间不自主移动）；L176-178 再 `step_for(1.8)`（snare 2.5s 自落地 ~t=1.0 起），断言 `mn.is_alive and mn.data.is_air_unit is True`（复飞，`battle.py:355-361` 用绝对时刻判定）；L180-193 第二个战场 `b2`：关塔后放 4 个敌人（Giant(9,13)、Knight(9.5,13)、`Minions` m1(8.5,13)、`Minions` m2(9.2,12.8)），出 Vines 并 `step_for(1.5)`，取唯一的存活 `Vines_AeO`，断言 `len(z.locked) == 3 and (m1_hurt != m2_hurt)`（只锁 3 个，且两只 Minions 的受损状态互斥——即第 4 个未受损；锁定按 `hp + shield_health` 降序取前 N，`battle.py:2132-2137`）。
- 调用: 由 `__main__` runner 调用；调用 `make_battle`/`kill_princess_towers`/`spawn_troop`/`step_for`/`check`、`Troop.update_current_target`。
- 置信度: 已确认

### S:_mm_battle  [L198-204]
- 类型: function
- 签名: `def _mm_battle(elixir):`
- 作用: 为 Spirit Empress 测试准备战场：把 player 0 的手牌循环改成 `['MergeMaiden'] + ['Knight']*7`、圣水设为给定值、关掉四座公主塔，返回 `(battle, player0)`。
- 参数:
  - `elixir`: player 0 的圣水值
- 返回: `(b, p)` 二元组（`BattleState`, `PlayerState`）
- 实现: L199 `b = make_battle()`；L200 `p = b.players[0]`；L201 `p.cycle = ['MergeMaiden'] + ['Knight'] * 7`；L202 `p.elixir = elixir`；L203 `kill_princess_towers(b)`；L204 返回二元组。
- 调用: 被 `test_mergemaiden_forms`(L231) 与 `test_mergemaiden_mirror`(L269) 调用。
- 置信度: 已确认

### S:_mm_forms  [L206-208]
- 类型: function
- 签名: `def _mm_forms(b):`
- 作用: 过滤出战场上"塔之外的、存活的、卡名以 `MergeMaiden` 开头"的实体（即两种形态的部署产物）。
- 参数:
  - `b`: `BattleState`
- 返回: 实体列表（list）
- 实现: L207-208 列表推导：`e.id > 6`（排除 id≤6 的初始塔，`battle.py:2556-2561`）、`e.is_alive`、`e.card_name.startswith('MergeMaiden')`。
- 调用: 被 `test_mergemaiden_forms`(L233、L246、L254、L260) 与 `test_mergemaiden_mirror`(L275) 调用。
- 置信度: 已确认

### S:test_mergemaiden_data  [L211-225]
- 类型: function
- 签名: `def test_mergemaiden_data():`
- 作用: 断言 Spirit Empress 两个形态条目的数值贯通：飞行形态 lv11 hp 1798 / dmg 309 / 攻速 1.6 / 射程 5 / 对空对地 / 飞行；地面形态攻速 1.2 / 近战射程 1.2 / 仅对地 / 地面；两形态共享 hp 1798 与 dmg 309。
- 参数: 无
- 返回: 无
- 实现: L213 打印段名；L214-216 `m = Card('MergeMaiden_Mounted')`，断言 `round(m.hp) == 1798 and round(m.damage) == 309`（注释：Legendary lv9 轴）；L217-219 断言 `abs(m.hit_speed - 1.6) < 1e-9`、`abs(m.range - 5.0) < 1e-9`、`m.attack_air and m.attack_ground and m.is_air_unit`；L220-223 `n = Card('MergeMaiden_Normal')`，断言 `abs(n.hit_speed - 1.2) < 1e-9`、`abs(n.range - 1.2) < 1e-9`、`(not n.attack_air) and n.attack_ground and (not n.is_air_unit)`；L224-225 断言 `round(n.hp) == 1798 and round(n.damage) == 309`。属性来源：`card_utils.py:240-247`（`hit_speed`/`is_air_unit`/`attack_air`/`range` 均由 `summonCharacterData` 派生）。
- 调用: 由 `__main__` runner 调用；调用 `check` 与 `Card`；数值表来源据 docstring L11 为官方数值表行 + M7 补建 Normal 行。
- 置信度: 已确认

### S:test_mergemaiden_forms  [L228-263]
- 类型: function
- 签名: `def test_mergemaiden_forms():`
- 作用: 断言双费用形态的部署规则：圣水 ≥6 出 6 费飞行远程（Mounted）、<6 出 3 费地面（Normal）、恰好 6 出空中形态、圣水不足实际费用时拒绝且不扣费，并核对扣费、手牌循环、`last_card`、`last_card_cost` 一致性。
- 参数: 无
- 返回: 无
- 实现: L229 docstring 列出上述口径；L231-236 `_mm_battle(8)` 后 `deploy_card(0,'MergeMaiden',Position(9,12))`，断言恰好 1 个形态且 `card_name == 'MergeMaiden_Mounted'`、`abs(p.elixir - 2.0) < 1e-9`（8−6）；L237-240 断言手牌循环按 `MergeMaiden` 推进（`'MergeMaiden' in p.cycle and p.cycle[-1] == 'MergeMaiden'`）、`p.last_card == 'MergeMaiden'`、`p.last_card_cost == 6`（镜像费用锚点）；L242-249 `step_for(1.0)` 后把圣水置 5、把 `MergeMaiden` 挪回队首，再部署并断言 `MergeMaiden_Normal` 恰好 1 个且 `data.is_air_unit is False`、圣水剩 2（5−3）；L251-256 圣水置 6.0 后再部署，断言 `MergeMaiden_Mounted` 累计 2 个（含之前那只）且 `p.elixir == 0.0`（6−6，边界"恰好 6 → 空中"）；L258-263 圣水置 2.0、记录形态数快照后出牌，断言 `(not ok)`、形态数不变、圣水仍为 2.0（拒绝且不扣费）。引擎侧对应分支：`battle.py:2826-2850`（`p.elixir >= 6` 选 Mounted / 否则 Normal，手牌校验用实际费用，`p.last_card_cost`、`p.last_card_form` 记录，`_hand_card` 保证手牌循环按 `MergeMaiden` 推进）。
- 调用: 由 `__main__` runner 调用；调用 `_mm_battle`/`_mm_forms`/`step_for`/`check` 与 `BattleState.deploy_card`。
- 置信度: 已确认

### S:test_mergemaiden_mirror  [L266-280]
- 类型: function
- 签名: `def test_mergemaiden_mirror():`
- 作用: 断言镜像法术复制 Spirit Empress 的**上一形态**（此处为空中 Mounted），且按其"实际费用 + 1"扣费（6+1=7）。
- 参数: 无
- 返回: 无
- 实现: L268 打印段名；L269-271 `_mm_battle(10)` 后把手牌改为 `['MergeMaiden','Mirror'] + ['Knight']*6`，出 `MergeMaiden`（圣水 10 ≥6 → 空中，扣 6 剩 4）；L272 `mirror_cost = p.last_card_cost`（此处应为 6，作为期望基准）；L273 `p.elixir = 10` 补足镜像费用（注释：镜像需实际费用+1=7）；L274 出 `Mirror`；L275-277 用 `_mm_forms` 断言 `MergeMaiden_Mounted` 数量为 2（第二只来自镜像）；L278-280 断言 `abs(p.elixir - (10 - (mirror_cost + 1))) < 1e-9`。引擎侧：`battle.py:2806-2823` 的 Mirror 分支（`last == 'MergeMaiden'` 时 `_mirror_base = p.last_card_cost or 3`，要求圣水 ≥ base+1，随后递归 `deploy_card(..., _from_mirror=True)` 并由该分支统一扣 `_mirror_base + 1`；`_from_mirror` 时形态取 `p.last_card_form`，`battle.py:2833-2838`）。
- 调用: 由 `__main__` runner 调用；调用 `_mm_battle`/`_mm_forms`/`check` 与 `BattleState.deploy_card`。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/probe_reward_composition.py | 3 | 3 |
| scripts/test_m4_evo7.py | 15 | 15 |
| scripts/test_m5_data.py | 16 | 16 |

COVERAGE_OK G028 files=3 symbols=34
