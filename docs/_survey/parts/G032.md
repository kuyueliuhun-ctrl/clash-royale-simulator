# 组 G032 文件级分析

> 说明：本组 7 个文件全部位于 `scripts/`，都是**离线分析 / 判读 / 基准 / 摸底工具脚本**（不参与训练主循环）。
> 除 `_survey_groups.py` 与 `bench_train_speed.py` 外，其余 5 个脚本都显式把 `src/clasher_new` 插入 `sys.path` 后 import RL 侧模块，属"只读消费训练产物"的工具。
> 读取范围遵守硬约束：仅本组 7 个文件 + 为确认调用关系而查看的同仓被 import 模块（`rl/train_solo.py`、`rl/follower.py`、`rl/config.py`、`rl/plan_space.py` 的函数签名）。

## F:scripts/summarize_probe_v3_mono.py
- 行数: 168
- 模块作用: 把 v3 价值阶梯探针的 `mono_check` 文本日志**用正则解析**成结构化 JSON 汇总（超集逐位验证结果、共同 α 阶梯表、每层自身 α* 读数、§4 同 α 差值），并把若干派生量（同 α 配对差、`pre_ln > enc` 的 α 计数）现场复算后打印 + 落盘。设计意图写在 docstring 第 1 行：判据数值必须**脚本复算**而非手抄（依据：L1-2 "【红线 R4】禁止手抄"）。脚本本身不做任何模型计算，只做文本 → 结构化的转换与算术。
- 模块级 docstring 首行: `只读：把 v3 的 `mono_check` 日志解析成**脚本复算**的汇总（【红线 R4】禁止手抄）。`
- 主要导入: argparse, json, os, re, sys
- 关键模块级常量:
  - `ALPHAS = (1e-2, 1e-1, 1e0, 1e1, 1e2, 1e3, 1e4, 1e5, 1e6)` — 允许被 `parse` 接受的 α 白名单；只有"整数 α 或落在该元组里的 α"才会进入共同 α 表（L21，用在 L54）
  - `RE_SUP = re.compile(r"X_fused\[:, :(\d+)\] == X_grid_ln_out \? (\w+)")` — 匹配超集逐位验证行，捕获列数与布尔字面量（L22）
  - `RE_HDR = re.compile(r"^\s*([\d.eE+-]+)\s+((?:[+-][\d.]+|—)(?:\s+(?:[+-][\d.]+|—))*)\s*$")` — 匹配共同 α 表的**数据行**：行首一个数字（α）+ 若干"带符号小数 或 破折号（缺失）"列（L23）
  - `RE_OWN = re.compile(r"^\s*(\S+)\s+α\*=\s*([\d.eE+-]+)\s+test=([+-][\d.]+)\s+ρ=(\S+)\s*$")` — 匹配每层"自身 α*"行：层名 / α* / test 值 / ρ（L24）
  - `os` 被 import（L11）但在文件内**未出现任何使用点**（L11-168 全文检索无 `os.` 调用）——照实记录：多余导入
- 顶层数据表/字典: 无模块级数据表；`main()` 内默认列名表 `["raw_obs", "plan_f", "cnn_pre_ln", "grid_ln_out", "fused", "enc", "pre_ln"]`（L85-86），代表共同 α 表中各列的固定顺序（列 0 起计数），§B/§C/§F 的分支逻辑依赖这个位序（如 `vals[2]`=cnn_pre_ln、`vals[3]`=grid_ln_out、`vals[5]`=enc、`vals[6]`=pre_ln）。

### S:parse  [L27-75]
- 类型: function
- 签名: `parse(path)`
- 作用: 读入单个 `mono_check` 日志文件，抽取"超集逐位验证 / 共同 α 表 / 每层自身 α* / 同 α 差值"四类信息，返回一个 dict。
- 参数:
  - `path` (str): 日志文件路径；其**文件名必须含 `mono_seed<数字>`**，否则本函数第一步即抛 `AttributeError`（L29）
- 返回: `dict`，键固定为 `seed`(int)、`path`(str)，条件性含 `superset_cols`(int)、`superset_bitwise_equal`(bool)、`common_alpha_table`(str→list)、`own_alpha`(str→dict)、`same_alpha`(dict)。
- 实现: L28 一次性读全文（未指定 errors，非 UTF-8 会抛错）；L29 用 `re.search(r"mono_seed(\d+)", path)` 从**路径字符串**（不是文件内容）取 seed。L31-34 用 `RE_SUP` 搜超集验证行，命中则填 `superset_cols` 与 `superset_bitwise_equal`（字面比较 `"True"`，因此 `"1"`/`"true"` 都会被判 False）。L36-59 解析共同 α 表：以状态机 `in3` 开关界定"§3 共同 α 阶梯" 到 "§3b" 之间的区间（L39-45），只在该区间内用 `RE_HDR` 匹配数据行；L50-53 把首列转 float，L54 的过滤条件是"α 非整数 且 不在 `ALPHAS` 白名单"⇒ 跳过；L56 把 `—` 映射为 `None`，其余转 float；L58-59 以 float α 为键存表，最后 L59 转 str 键并按数值排序输出。L61-68 用 `RE_OWN` 逐行抓"自身 α*"，键为层名，值为 `{alpha, test, rho}`（ρ 为 `—` 时取 `None`）。L70-74 用一条跨行多段正则抓 §4 的 "同一 α 下：fused=X vs grid_ln_out=Y ⇒ 差 Z" 行，命中则填 `same_alpha`。**注意**：`common_alpha_table` 的 list 长度即列数，调用方按位序索引（见本文件 L104/L116/L139）。
- 调用: 被同文件 `main()` 在 L87 对每个日志路径调用一次；未被其它文件 import（本组内 grep 无外部引用）。它自己不调用任何项目内函数。
- 置信度: 已确认

### S:main  [L78-164]
- 类型: function
- 签名: `main()`
- 作用: 解析全部日志、跨 seed 归约为 6 个小节（§A/§B/§C/§D/§F/§E）的文本报告，并可把同一份汇总写 JSON。
- 参数: 无（参数经 argparse：`--logs`（必填，nargs="+"）、`--labels`（nargs="*"，默认 None）、`--out`（默认 None），L79-83）
- 返回: `summary`（dict，键 `records`/`labels`/`grid_ln_neutrality`/`pre_vs_enc`，L158-159）；同时经 `--out` 落盘 JSON（L160-162）。注意返回值只在被 import 调用时有意义，`__main__` 路径下被丢弃（L167-168）。
- 实现: L84 解析参数；L85-86 未给 `--labels` 时用写死的 7 列默认顺序。L87 逐日志 `parse`，L88 按 seed 升序排序。L90-92 打印标题。§A（L93-96）逐 seed 打印超集列数与逐位相等布尔。§B（L98-109）对每个 seed 遍历共同 α 表，只取"列数 ≥4 且第 2、3 列都非 None"的 α，算 `cnn_pre_ln − grid_ln_out` 的差序列，记 `n_alpha`/`max_abs_diff`/`signed_diffs` 进 `stats`（grid_ln 中性性证据），并打印 max 绝对差（空序列打印 `nan`）。§C（L111-118）按 α 数值升序打印 grid_ln_out/fused/enc/pre_ln 四列原始读数（要求列数 ≥7）。§D（L120-130）对每 seed 比较"各自 α* 读法"下 fused 与 grid_ln_out 的差，以及 §4 的"同 α 读法"下差值；此处**假定** `own_alpha` 必有 `fused` 与 `grid_ln_out` 两个键，缺失会在 L127 抛 `KeyError`（L124-125 的 `.get` 只保护到倒数第二层）。§F（L132-146）统计每个 seed 上"`pre_ln > enc` 的 α 数"与"< 的 α 数"，按多数记入 `f_cnt`（三态计数 `pre>enc`/`pre<enc`/`tie`），并打印逐 seed 计数。§E（L148-156）按 `labels` 顺序打印各层自身 α* 下的 test 值表格（`—` 表示该层无记录）。L158-162 组装 summary 并可选写盘（`ensure_ascii=False, indent=1`）。
- 调用: 由 `if __name__ == "__main__":` 调用（L167-168）；命令行调用方式见 docstring L5-7。它调用 `parse`（L87）。
- 置信度: 已确认

## F:scripts/diag_value_head.py
- 行数: 164
- 模块作用: critic 诊断脚本（docstring L1-9）：加载一个 ckpt，先按模块打印参数量级、单列 `value_head.weight/bias`，再跑一段 rollout 收集编码 `enc`、GRU 隐状态 `h` 与价值输出，报各自跨帧 std 与范数，最后抽样 3 帧手工重算 GRU 三个门，用于区分"价值头死"与"输入 h 本身近乎恒定"。脚本把仓库 `src/clasher_new` 插入 `sys.path`（L16-19）后 import RL 模块。
- 模块级 docstring 首行: `价值头解剖（2026-09-11 §4 诊断第二步）。`
- 主要导入: argparse, os, sys, numpy（as np）, torch, rl.config(TrainConfig), rl.belief(BeliefInference), rl.belief_planner(BeliefPlanner), rl.plan_space(PLAN_DIM), rl.follower(FollowerPolicy, load_checkpoint), rl.train_follower(FollowerOpponent), rl.train_solo(solo_env, resolve_deck_set)
- 关键模块级常量:
  - `_HERE = os.path.dirname(os.path.abspath(__file__))` — 本脚本目录（L16）
  - `_SRC = os.path.join(os.path.dirname(_HERE), "src", "clasher_new")` — 仓库源码根（L17），L18-19 条件插入 `sys.path`
  - 无其它模块级常量（阈值/表均写死在函数体内，例如 `n_particles=128`、`seed=7`）
- 顶层数据表/字典: 无

### S:main  [L33-160]
- 类型: function
- 签名: `main()`
- 作用: 对一个 ckpt 做"参数范数 + rollout 隐状态/价值统计 + GRU 门抽样"三段落诊断并打印结论性提示。
- 参数: 无（argparse：`--ckpt` 必填、`--run-dir` 默认 `runs/prod_200k_valnorm_ev`、`--frames` 默认 200、`--device` 默认 `cuda`，L34-39）
- 返回: 无（末行只 print 判读提示，L159-160）
- 实现:
  - **配置**（L41-51）：以 `TrainConfig.resolve("economy")` 为底，读 `--run-dir/config.json`，对其中每个"cfg 已有同名属性"的键 `setattr` 覆盖；单个 setattr 失败被静默吞掉（L50-51）。文件不存在则跳过。
  - **设备**（L53）：请求 cuda 但 `torch.cuda.is_available()` 为假时退回 cpu。
  - **模型装配**（L54-58）：`solo_env(cfg, 0)` 造环境；用 `BeliefInference(opp_deck=env.deck1, n_particles=128, seed=0).encode(None, None)` 的长度推 belief 维度 `bdim`；`load_checkpoint(ckpt, hidden_dim, plan_dim=PLAN_DIM, belief_dim=bdim)` 加载策略（`load_checkpoint` 签名已核对：`rl/follower.py:67`）；`pol.to_device(device)`。
  - **第一部分：参数范数**（L60-70）：按参数名首段（`name.split(".")[0]`）分组累加平方和（L66）、`numel`（L67）、`max|W|`（L68）；按 `numel` 降序打印 `||W||`/`RMS`/`max|W|`。注释未提但代码事实：这是**全模型**参数，不只是价值头。
  - **value_head 细节**（L72-77）：直接取 `pol.value_head.weight/bias` 的 numpy 副本打印范数与 bias 值。**风险（源码事实，非推断）**：docstring 未提，但 L83-85 的构造表明该头是否参与前向取决于 ckpt 携带的 `value_bypass`/`value_independent` 标志；`AGENTS` 级结论不在本任务允许阅读范围内，此处仅记录：本脚本对这两个标志**不做任何提示**，直接打印。
  - **对手与 rollout 准备**（L80-93）：`resolve_deck_set(getattr(cfg,"deck_set",None) or "default")` 取镜像牌组；新建镜像 `FollowerPolicy` 并用 `load_state_dict(pol.state_dict())` 对齐权重，注释说明"镜像对手必须用与 ckpt 相同的 value 架构，否则键集不匹配"（L82-86）；包成 `FollowerOpponent(..., deterministic=True)` 设为 `env.opponent`（L87-89）；独立建 belief（seed=7）并 `env.reset(seed=7)`。
  - **采集循环**（L95-114）：`torch.no_grad()` 下逐帧：`bp.plan(battle, belief.state(), obs)` 得计划 → `belief.encode(obs, None)` 得 token → `pol._encode_parts(obs, tok, plan.to_vector())` 取 `(fused, enc)` → 手工跑 `pol.gru_cell(enc, ...)`（首帧用零隐状态，L100-102）→ `pol._value_from(enc, h, fused)` 取标量价值（注释注明这是 B′/E′ 后的统一入口，L103-104）→ 收集 `h`/`val`/`enc`（L105-107）→ `pol.act(..., deterministic=False)` 出动作并 `env.step`（L108-110）→ `belief.update(obs, info.get("opp_played"))`（L111）；局末（`term or trunc`）以 `seed=7+t` 重新 `env.reset` 并重置 belief 与隐状态（L112-114）。**注意**：循环开头直接用 `env.battle`，说明 `solo_env` 返回的环境对象带 `battle` 属性（依据 L97；未进一步进入 `train_solo.py` 验证该属性定义位置 ⇒ 该属性名来自本文件用法）。
  - **统计输出**（L115-134）：转 numpy 后打印 `enc` 每维跨帧 std 的 mean/median/max、`enc` 每帧 L2 范数区间、`h` 同上、`||h||` 区间；价值标签按 `value_independent` / `value_bypass` / 默认三分支选择文案（L127-130）；额外打印 `h·w`（去掉 bias 的价值分量）的 std（L133-134）。
  - **GRU 门诊断**（L136-157）：取 `weight_ih/weight_hh/bias_ih/bias_hh`，抽第 0、中间、末三帧的 `enc`/`h`，按 PyTorch GRU 公式手工算 `r`、`z`、`n`（L145-150），打印三门均值与 `|z−1|`（→1 表示 h 冻结，L152-155）。**L137-139 定义了内部函数 `_g(which, x)`（`return getattr(gc, which)`）但在文件内从未被调用**（照实记录：死代码）。L156-157 捕获任何异常并打印 `{e!r}`——与仓库"catch-all 里不要打印异常对象"的约定相反，此处照实记录其存在。
- 调用: 由 `if __name__ == "__main__":` 调用（L163-164）。它调用：`TrainConfig.resolve`、`solo_env`、`resolve_deck_set`、`BeliefInference`、`load_checkpoint`、`FollowerPolicy`、`FollowerOpponent`、`BeliefPlanner`、`pol.to_device/_encode_parts/_value_from/act/gru_cell`、`env.reset/step/get_action_mask`（`get_action_mask` 以函数对象形式传入 `pol.act`，L108）。未被其它文件 import。
- 置信度: 已确认（`env.battle` 属性的定义位置未回溯到 `train_solo.py` 内部，但用法在 L97 明确）

## F:scripts/judge_probe_v4.py
- 行数: 140
- 模块作用: v4 探针的跨 seed 归约器（docstring L1-5）：读取 `runs/_probe_v4/seed<N>.json`，逐 seed 打印闸门、判据命中（分支票数）、共同 α 阶梯曲线、两个关键比值（`z_enc/z_rand`、`post_ln/relu_ln`），并按预注册规则给出"全命中 / 全不中 / 不稳定"的三态裁决，最后落盘 summary JSON。判据数值全部现场复算（R4）。
- 模块级 docstring 首行: `只读：v4（真·同一张量的投影/归一化对账）跨 seed 归约 —— 判据全部脚本复算（R4）。`
- 主要导入: argparse, json, os, sys
- 关键模块级常量:
  - `COMMON_ALPHAS = (1e-2, 1e-1, 1e0, 1e1, 1e2, 1e3, 1e4, 1e5, 1e6)` — 逐 seed 打印曲线时的固定 α 序列（L18，用在 L81/L96）
  - `SHOW = ("raw_obs", "grid_ln_out", "fused", "z_enc", "r_enc", "enc", "z_rand", "pre_ln", "relu_ln", "post_ln")` — §3 阶梯表要展示的层名顺序（L19-20）
  - `BRANCHES = ("P-PROJ", "P-LN", "P-BOTH", "P-RANDOM", "P-VLN", "P-VLN-NEUTRAL")` — 六个判据分支名（L21）；含义在本文件内**未定义**（只在 §4 里用自然语言解释了 (a)(b) 两个比值）⇒ 分支判定语义来源是各 seed.json 的生产脚本，见"无法确认"
- 顶层数据表/字典: 无

### S:S  [L24-29]
- 类型: function
- 签名: `S(rec, layer, al)`
- 作用: 从单 seed 记录的 `curves[layer]` 曲线里按 α 取值，取该 α 对应条目的**第 0 个元素**（test 值），缺失返回 `None`。
- 参数:
  - `rec` (dict): 单 seed JSON（含 `curves` 键）
  - `layer` (str): 层名，如 `"z_enc"`
  - `al` (float): α 值
- 返回: float 或 `None`
- 实现: L25 取 `rec["curves"][layer]`（无该层或 `curves` 缺失 ⇒ `None`）；L27-29 先按 `str(float(al))` 查键（覆盖 json 序列化出的浮点键形式），再退化用 `f"{al:g}"` 查键（覆盖 `0.01` → `"0.01"` vs `1e-2` 的字符串差异；`f"{1e-2:g}"` = `"0.01"`，与前者实际同形）；键不存在返回 `None`；命中则取 `v[0]`（列表首元素）。
- 调用: 被同文件 `main()` 在 L80/L84/L97-98/L109-111 大量调用。它不调用其它项目函数。
- 置信度: 已确认

### S:main  [L32-136]
- 类型: function
- 签名: `main()`
- 作用: 归约多 seed 的 v4 探针 JSON，打印 5 个小节（闸门 / 分支命中票数 / 同 α 阶梯 / 关键比值 / 三态裁决）并可写 summary。
- 参数: 无（argparse：`--dir` 默认 `runs/_probe_v4`、`--seeds` nargs="*" type=int 默认 `[7, 11, 13]`、`--out` 默认 None，L33-37）
- 返回: `out`（dict：`seeds`/`votes`/`per_seed`/`prereg`，L122-131）；`--out` 给定则落盘（L132-134）。
- 实现:
  - **载入**（L39-48）：逐个 seed 读 `{dir}/seed{s}.json`，缺失打 warn 并跳过；一个都没读到则 `raise SystemExit("[abort] 没有可归约的 json")`（L46-47）；`seeds` 升序。
  - **§1 闸门**（L54-61）：逐 seed 打印 `gates_ok` 与每个 gate 的三态（`N/A`/`P`/`F`），附 `offline_max_abs_diff`（离线重算与实抓编码的最大绝对差；离线重算机制本身在本文件内不可见 ⇒ 见"无法确认"）。
  - **§2 分支命中票数**（L63-74）：按 `BRANCHES` 顺序读取每个 seed 的 `branches[b]` 布尔，累计票数并打印 `HIT`/`-` 矩阵与末行 `票数 n/N`。
  - **§3 同 α 阶梯**（L76-88）：逐 seed 打印表头（只包含"在 α=1e-2 有值"的层，L80）与 `COMMON_ALPHAS` 每行值；**实现瑕疵（源码事实）**：L82-88 逐层过滤 `None` 后 `append` 到 `cells`，因此某层缺值时该行**列会错位**（表头列数与数据列数不再对应），文件内无对齐保护。
  - **§4 两个关键比值**（L90-111）：对每个 α 取 `z_enc/z_rand`（L99-100）与 `post_ln/relu_ln`（L101-102），分母为 `None`、0 或 `|分母|<1e-9` 时记 `None`（打印 `—`）；随后若 `z_rand2`/`z_rand3` 两条对照曲线在每个共同 α 都存在，则整条打印（L108-111），用于"3 个随机投影是否同量级"的描述性判断（注释 L107）。
  - **§5 汇总裁决**（L113-120）：对每个分支：票数 = seed 数 ⇒ `✅ 全部命中 ⇒ 可写结论`；票数 = 0 ⇒ `❌ 本轮不支持`；否则 `⚠️ 不稳定，只报票数不合并`（注释指明规则出自"预注册 §6"）。
  - **落盘**（L122-134）：`per_seed` 只保留 gates/gates_ok/branches/hits/verdict/offline_max_abs_diff；`prereg` 字段写死文档路径字符串 `docs/value_ln_probe4_prereg_2026-09-14.md`（L131，仅字符串引用，未打开该文件）。
- 调用: 由 `if __name__ == "__main__":` 调用（L139-140）；命令行用法见 docstring L4-5。它调用本文件 `S`（L80/L84/L97-98/L109-111）。
- 置信度: 已确认（`BRANCHES` 各分支的判定条件与 `curves` 的生成方在本文件外 ⇒ 归约逻辑本身已确认，语义待确认）

## F:scripts/bench_train_speed.py
- 行数: 130
- 模块作用: 训练吞吐基准脚本（docstring L2-19）：以子进程方式在 `src/clasher_new` 目录启动一个短 `rl/run_league.py --mode solo` run，用后台线程给每行 stdout 实时打上"相对启动的墙钟秒数"，跑完后解析日志得到评估点墙钟、启动耗时分解、训练循环耗时与混合吞吐（含评估），并检测评估降级/worker 失败。docstring 明确记录了口径来源与历史数值（`fix_gru_ln_norm_20k` 是 20000/2709s ≈ 7.4 步/s，L10-12）。
- 模块级 docstring 首行: `训练速度基准（2026-09-12）：跑一个短 solo run，实时给日志行打时间戳，`
- 主要导入: argparse, os, re, subprocess, sys, threading, time
- 关键模块级常量:
  - `REPO_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` — 仓库根（L29）
  - `WORKDIR = os.path.join(REPO_ROOT, "src", "clasher_new")` — 训练子进程 cwd（L30）
  - `PY = os.path.join(REPO_ROOT, ".venv", "Scripts", "python.exe")` — 固定解释器路径（Windows venv；注释说 WSL2 里也是这个解释器，L31-32）
- 顶层数据表/字典: 无

### S:main  [L35-126]
- 类型: function
- 签名: `main()`
- 作用: 组装并运行一次受控短训练，采集带时间戳的日志，解析并打印吞吐/评估成本/降级提示，返回子进程退出码。
- 参数: 无（argparse：`--steps` 3000、`--per-eval` 1000、`--eval-games` 2、`--eval-workers` 12、`--device` cuda、`--config` economy、`--config-name` bench_speed、`--hist-dirs` nargs="*" []，L38-46）
- 返回: `rc`（int，子进程退出码，L126）；两条前置检查失败时返回 `1`（L59/L62）
- 实现:
  - **命令组装**（L49-56）：`run_league.py --mode solo --config <config> --config-name <name> --fresh --total-steps --steps-per-eval --n-eval-games --eval-workers --device --value-norm running --adv-norm scale --diagnose-every 10`，再对 `--hist-dirs` 每项追加 `--hist-seed-dir`。**注意**：基准命令写死 `--value-norm running --adv-norm scale`，与 docstring 的"20k 标准协议"一致，但与"训练语义兼容红线"相关的其它 PPO 超参（如 `--ppo-epochs` 等）**未**传入 ⇒ 走 CLI 默认值（本文件内不体现默认值内容）。
  - **前置检查**（L57-62）：解释器文件不存在或工作目录不存在 ⇒ 打 stderr 并 `return 1`。
  - **子进程 + 读线程**（L64-83）：打印完整 cmd 与 cwd；`t0 = time.monotonic()`；`subprocess.Popen(..., stdout=PIPE, stderr=STDOUT, text=True, encoding="utf-8", errors="replace")`（L68-70）；用一个 `threading.Lock` 保护的 daemon 线程逐行 append `(elapsed, line.rstrip("\n"))`（L71-78）；主线程 `proc.wait()` 后 `th.join()`，计算 `wall`，加锁拷贝成 `lines`（L79-83）。
  - **解析**（L85-100）：遍历时间戳行——`\[solo\] eval@(\d+):` 收评估点（步号, 墙钟）（L91-93）；含"启动耗时分解"记录 `startup`（L94-95）；`训练循环耗时 ([\d.]+)s` 取 `loop_time`（L96-98）；行内含"降级"/"worker 失败"/"降级串行"任一即 `degraded += 1`（L99-100）。`loop_time` 为空时兜底用 `wall`（L102-103，注释说明这是 run 异常结束时的兜底）。
  - **输出**（L105-125）：打印退出码、进程墙钟、本次配置参数；若 `degraded>0` 则打印"数字不可比、按全局约定先归因外部再判读"的告警（L109-111）；打印启动分解行；打印训练循环耗时与 `混合吞吐 = steps/loop_time 步/s`（分母下限 `0.001` 防除零，L115-116）；若有评估点则逐点打印相对启动的墙钟，并在 ≥2 点时计算相邻间隔并打印（注释说明间隔 = `per-eval` 步训练 + 一次评估，L117-125）。
  - `degraded` 的检测是**纯文本匹配**，可能被正文里偶然出现"降级"的日志行误触发（源码事实，L99）。
- 调用: 由 `if __name__ == "__main__": sys.exit(main())` 调用（L129-130）；被调用方为外部进程 `rl/run_league.py`（仓库内文件，本任务未读取，仅确认路径存在性由 L57/L60 的检查保证）。本文件不 import 任何项目内模块。
- 置信度: 已确认（`rl/run_league.py` 的具体行为、以及日志行格式是否恒含上述模式，未回溯验证 ⇒ 属"待确认（原因：未读该脚本，仅按正则假定日志格式）"）

## F:scripts/_survey_groups.py
- 行数: 129
- 模块作用: 本次摸底的**分组工具**（docstring L2-14）：读 `inventory.json`（文件清单 + 行数 + 符号），把"单文件 ≥400 行的大文件"各自独立成组，其余按**同目录**聚合、贪心装箱到 ~1000 行 / 最多 12 个文件一组，输出 `groups.json`；并在写出前用断言保证"组覆盖的文件集合 == 清单全集"（零遗漏/零重复）。
- 模块级 docstring 首行: `把 inventory.json 的文件切成「子代理任务组」，保证**全量覆盖、无文件遗漏**。`
- 主要导入: argparse, io, json, os, sys, collections.OrderedDict, typing(Any, Dict, List)
- 关键模块级常量:
  - `BIG_LINES = 400` — 超过该行数的文件独立成组（L28，用在 L52-53）
  - `CAP_LINES = 1000` — 非大文件组的行数装箱上限（L29，用在 L77）
  - `MAX_FILES = 12` — 非大文件组的文件数上限（L30，用在 L77）
  - `from __future__ import annotations`（L15）— 使 `List[Dict[str, Any]]` 等注解在运行期不求值
  - `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`（L25-26）— 条件式强制 UTF-8 输出（本仓库防 GBK 崩的通用姿势）
- 顶层数据表/字典: 无（组结构在 `main()` 内动态构造：每组含 `id`/`kind`/`files`/`lines`/`symbols`/`slug`（+ 组模式含 `dir`），L56-103）

### S:slug  [L33-37]
- 类型: function
- 签名: `slug(path: str) -> str`
- 作用: 把仓库相对路径压成扁平的小写短名，用作组名后缀（如 `scripts-summarize-probe-v3-mono`）。
- 参数:
  - `path` (str): 仓库相对路径，如 `scripts/xxx.py` 或 `src/clasher_new/rl/xxx.py`
- 返回: `str`
- 实现: L34 先去掉前缀 `src/clasher_new/`，再把 `scripts/` 换成 `scripts-`（**顺序敏感**：`src/clasher_new/` 必须先剥，否则会留下层级）；L35 去 `.py` 后缀（仅当末尾是 `.py`）；L36 依次把 `/`/`_`/`.` 换成 `-`；L37 `strip("-")` 后 `lower()`。**注意**：路径中段若含 `.pt` 之类的点也会被替换成 `-`（源码事实，L36）。
- 调用: 被同文件 `main()` 在 L63（单文件组）、L86 与 L101（目录组：取 `cur[0]["path"]` 的 slug 再加 `-x{文件数}`）调用。它不调用其它项目函数。
- 置信度: 已确认

### S:main  [L40-125]
- 类型: function
- 签名: `main() -> int`
- 作用: 执行分组算法、断言全量覆盖、写 `groups.json` 并打印每组摘要。
- 参数: 无（argparse：`--inventory` 必填、`--out` 必填，L41-44）
- 返回: `0`（L125）
- 实现:
  - **读清单**（L46-49）：`io.open(inventory, encoding="utf-8")` 载入 JSON，取 `inv["files"]` 列表（每项至少含 `path`/`lines`/`symbols`）。
  - **大文件独立成组**（L52-65）：按 `lines >= BIG_LINES` 切出 `big`，**按行数降序**遍历（`sorted(..., key=lambda r: -r["lines"])`），各生成一个 `kind="single"` 的组；组号用 `f"G{len(groups)+1:03d}"` 递增 ⇒ 大文件组占据前若干连续编号。
  - **小文件按目录聚合**（L67-103）：`bydir` 是 `OrderedDict`，键为 `os.path.dirname(path)`；填充顺序为**小文件路径的字典序**（L69-70），因此目录顺序 = 该目录下最小路径的字典序。目录内再按行数降序（L73）。贪心装箱：若已有累计内容且"再加本文件会超 `CAP_LINES`"或"已有文件数 ≥ `MAX_FILES`"，则先封箱（L77-89，封箱时 `slug` = 首文件 slug + `-x{个数}`，行的 `symbols` = 成员符号数之和），再开新箱；目录循环结束后若 `cur` 非空再封最后一箱（L92-103）。
  - **覆盖断言**（L105-109）：`covered`（所有组成员路径排序）必须**逐个等于** `allfiles`（清单全部路径排序），否则 `assert` 抛错并给出缺失/多余集合。
  - **写出**（L111-121）：payload 记录三个阈值常量、`n_files`、`n_groups`、`groups`；`os.makedirs(dirname(abspath(out)), exist_ok=True)` 后 `json.dump(..., ensure_ascii=False, indent=1)`。
  - **打印**（L122-124）：总文件数→组数、`coverage exact = True`，随后逐组打印 `id / 行数 / 符号数 / 文件数 / slug`。**注意**：L122 的 "coverage exact = True" 是**只读断言通过**的必然结果（断言在 L107 已把关），不是独立校验。
  - 全局副作用：仅创建输出目录与写输出文件；`groups.json` 的父目录默认即 `docs/_survey`。
- 调用: 由 `if __name__ == "__main__": raise SystemExit(main())` 调用（L128-129）；用法见 docstring L11-13。它调用本文件 `slug`（L63/L86/L101）。本文件无项目内 import。
- 置信度: 已确认

## F:scripts/_forensics_cycling.py
- 行数: 128
- 模块作用: cycling（策略非传递循环）取证脚本（docstring L1-16）：**只读**训练产物，把 `main@N` 分别对阵"训练起点随机策略 `solo_main_0.pt`"、"另一随机种子的全新随机策略"，再做"起点 vs 全新随机"的公平性 sanity，另解析 run 自带 `replays/league_<total_steps>.pkl`（main vs 冻结副本）作参考；对每场报胜率、疑似早停占比与帧数统计，用于区分"绝对变弱 / 僵局早停假象 / 真非传递"。
- 模块级 docstring 首行: `cycling 取证（2026-09-12，计划 v3 §3.8.3 A′）。`
- 主要导入: argparse, os, pickle, statistics, sys, tempfile, numpy（as np）, rl.train_solo, rl.config(TrainConfig), rl.follower(FollowerPolicy, load_checkpoint), rl.plan_space(PLAN_DIM), rl.belief(BeliefInference)
- 关键模块级常量:
  - `_HERE` / `_SRC`（L29-30）— 脚本目录与 `src/clasher_new`，L31-32 条件插入 `sys.path`
  - 无其它常量（`step = 90001` 是 `_run_matchup` 内的局部量，见下）
- 顶层数据表/字典: 无

### S:_analyze_pkl  [L43-66]
- 类型: function
- 签名: `_analyze_pkl(path)`
- 作用: 解析一个回放 pkl（`{"games": [...]}`），算出胜负平、胜率、疑似早停局数与帧数统计。
- 参数:
  - `path` (str): pkl 路径
- 返回: dict：`n`/`w0`/`w1`/`draw`/`winrate0`/`early`/`early_frac`/`f_mean`/`f_min`/`f_max`
- 实现: L44-45 `pickle.load` 后取 `["games"]` 列表。L46-49 统计 `winner==0` / `winner==1` 的局数，其余计平局（含 winner 缺失或为 None 的情况，因为既不等于 0 也不等于 1）。L47 的 w0 是 **main 侧（player 0）** 胜场，与 `winrate0` 的命名一致。L50 取每局帧数列表。L51 从首局的 `meta["max_steps"]` 取上限（`or 360` 兜底；`games` 为空时也用 360）。L52-59 判定"疑似早停"：帧数 < max_steps **且**末帧 `crown0`/`crown1` 任一缺失 **或** 双方皇冠都 <3（即没有一方达成 3 冠却提前结束）；两个条件同时满足才算。L60-66 组装返回值，`winrate0 = (w0 + 0.5*draw)/n`（平局算半胜），`f_mean` 用 `statistics.mean` 保留 1 位小数，`f_min`/`f_max` 取自 `lens`。**边界**：`n == 0` 时 L62/L63 会除零 —— 本函数不做空集保护（源码事实）。
- 调用: 被同文件 `_run_matchup`（L76）与 `main`（L111）调用。它不调用项目内函数。
- 置信度: 已确认（`games[i]` 的字段名 `winner`/`frames`/`meta.max_steps`/`frames[-1].crown0/1` 均按本文件用法认定；这些字段的**生产方**未回溯 ⇒ 字段语义待确认）

### S:_run_matchup  [L69-81]
- 类型: function
- 签名: `_run_matchup(cfg, main, opp, label, games, workers, tmpdir, seed)`
- 作用: 用一次并行评估跑指定的 main-vs-opp 对阵，写出回放到临时目录，解析该回放并打印一行结果。
- 参数:
  - `cfg` (TrainConfig): 训练配置（提供 `max_ep_steps`）
  - `main` (FollowerPolicy): 被测主策略（回放中的 player 0）
  - `opp` (FollowerPolicy): 对手策略
  - `label` (str): 打印用标签
  - `games` (int): 局数
  - `workers` (int): 并行 worker 数
  - `tmpdir` (str): 回放写出目录
  - `seed` (int): 评估种子
- 返回: `rep`（`_analyze_pkl` 的 dict）
- 实现: L70 `train_solo.solo_env(cfg, seed)` 建环境；L71 固定 `step = 90001`，注释说明是"独立 step，避免与 run 自带 `league_<step>.pkl` 撞名"。L72-75 调 `train_solo.eval_solo_parallel(env, main, opp, games, int(cfg.max_ep_steps), seed, cfg, n_workers=workers, record_replays=True, replays_dir=tmpdir, step=step, frozen_step=None, save_replays=True)` —— 已核对被调函数签名（`rl/train_solo.py:941-943`）：位置参数与关键字参数完全对齐，`frozen_step=None` 表示"不按冻结副本命名"。L76 从 `tmpdir/league_90001.pkl` 读回放并分析。L77-80 打印 `[标签] 胜率(main侧)`（含 W/L/D 与 n）、疑似早停数与占比（`:.0%`）、帧数 mean/min/max，`flush=True`。
- 调用: 被同文件 `main()` 在 L118-123 调用三次（三种对阵）。它调用 `train_solo.solo_env`、`train_solo.eval_solo_parallel`、本文件 `_analyze_pkl`。
- 置信度: 已确认（被调两个 train_solo 函数的存在与签名已核对；`eval_solo_parallel` 的并行/降级内部行为未展开读）

### S:main  [L84-124]
- 类型: function
- 签名: `main()`
- 作用: 装配 main / 起点 / 全新随机三个策略，先解析 run 自带主评估回放，再依次跑三场对阵取证。
- 参数: 无（argparse：`--run-dir` 默认 `runs/fix_gru_ln_norm_20k`、`--games` 100、`--workers` 16、`--device` cuda，L85-90）
- 返回: 无（末尾只 print `[done]`，L124）
- 实现:
  - L92 `TrainConfig.load(run_dir/config.json)` 载入配置（已核对 `rl/config.py:311` 的 `load` 存在），L93 用 CLI 的 `--workers` 覆盖 `cfg.eval_workers`。
  - L94-96 `load_checkpoint(run_dir/solo_main.pt, hidden_dim=cfg.hidden_dim)` 载 `main` 并 `to_device`；L97-99 同法载 `solo_main_0.pt` 作 `baseline0`（**注意**：两次 `load_checkpoint` 都**未传** `plan_dim`/`belief_dim`，与 `diag_value_head.py` 的传法不同 —— 是否触发旧 ckpt 维度回退由 `load_checkpoint` 内部元数据决定，本文件不体现）。
  - L100-103 用 `BeliefInference(opp_deck=list(train_solo.DEFAULT_SOLO_DECK), n_particles=128, seed=0).encode(None, None)` 的**长度**推 belief 维 `bd`，再新建一个**未加载任何权重**的 `FollowerPolicy(hidden=cfg.hidden_dim, plan_dim=PLAN_DIM, belief_dim=bd)` 作为"全新随机"对手（默认初始化 ⇒ 随机策略），并 `to_device`。
  - L105-106 打印 run / games / workers / `max_ep_steps`。
  - L108-114 参考场：路径 `replays/league_{cfg.total_steps}.pkl` 存在则直接 `_analyze_pkl` 并打印"main vs 冻结副本(自带回放)"的胜率/早停/平均帧数（不带帧数极值）。**该 pkl 不是本次跑的，而是 run 时代留下的产物**（源码事实，L109）。
  - L116-123 `with tempfile.TemporaryDirectory(prefix="fc_") as td:` 内：`seed = int(cfg.seed) + 70000`，依次跑三场（各自 seed 偏移 +0/+1000/+2000）：`main@N vs baseline0(起点随机)`、`main@N vs 全新随机`、`baseline0 vs 全新随机 (sanity)`；第三场的期望是 ≈0.5（docstring L13）。退出 `with` 后临时目录被自动清理。
  - 硬编码量：`+70000` 的 seed 偏移与 `step=90001` 都无 CLI 出口（源码事实）。
- 调用: 由 `if __name__ == "__main__":` 调用（L127-128）；用法见 docstring L18-19。它调用 `TrainConfig.load`、`load_checkpoint`、`BeliefInference`、`FollowerPolicy`、`train_solo.DEFAULT_SOLO_DECK`、`_analyze_pkl`、`_run_matchup`。
- 置信度: 已确认

## F:scripts/value_displacement_scan.py
- 行数: 126
- 模块作用: 参数位移指纹扫描（docstring L1-13）：对每个 run 目录按步数排序取 `solo_main_<step>.pt`（排除 `solo_main.pt`/`solo_opt.pt`），对相邻窗口逐张量算相对位移 `‖ΔW‖/‖W‖`，统计"位移恰为 0"的窗口数；原理是 Adam 对 loss 常数缩放不变 ⇒ 位移恰为零等价于该张量梯度精确为零（用于区分"梯度小"与"梯度恒零/吸收态"）。脚本只读 ckpt，不改任何东西。
- 模块级 docstring 首行: `只读：参数位移指纹扫描（R14）。`
- 主要导入: argparse, os, re, sys, numpy（as np）, torch
- 关键模块级常量:
  - `_HERE` / `_SRC`（L25-26）— 脚本目录与 `src/clasher_new`；L27-28 条件插入 `sys.path`。**注意**：本文件**没有 import 任何 rl.* 模块**，插 `sys.path` 只是为了 `torch.load` 能解析 ckpt 里可能被 pickle 引用的类路径（源码未注释说明意图 ⇒ 该解释属推断，见"无法确认"）
  - `sys.stdout.reconfigure(encoding="utf-8")`（L20-23）— 包在 try/except 里
- 顶层数据表/字典: 无

### S:load_ckpts  [L34-42]
- 类型: function
- 签名: `load_ckpts(run_dir)`
- 作用: 列出 run 目录下所有 `solo_main_<数字>.pt`，按步数升序返回。
- 参数:
  - `run_dir` (str): run 目录
- 返回: `[(step:int, path:str), ...]` 升序
- 实现: L37 `os.listdir` 遍历；L38 `re.fullmatch(r"solo_main_(\d+)\.pt", fn)` 精确匹配（`fullmatch` ⇒ 天然排除 `solo_main.pt`、`solo_opt.pt`、`solo_main_0.pt.bak`）；L40 用 `os.path.join` 拼路径；L41 `out.sort()` 按 (step, path) 元组排序 —— 因 step 唯一，等价于按步数升序。docstring（L35）明确说忽略 `solo_main.pt` / `solo_opt.pt`，实现由正则保证。
- 调用: 被同文件 `scan`（L53）调用。不调用项目内函数。
- 置信度: 已确认

### S:state_dict_of  [L45-49]
- 类型: function
- 签名: `state_dict_of(path)`
- 作用: 加载 ckpt 并把其中的张量转成 float32 的 numpy 数组字典。
- 参数:
  - `path` (str): ckpt 路径
- 返回: `{参数名: np.ndarray(float32)}`
- 实现: L46 `torch.load(path, map_location="cpu", weights_only=False)` —— 显式 `weights_only=False`（允许反序列化任意对象，因为 ckpt 可能是含元数据的 dict）。L47-48 若结果是含 `"state_dict"` 键的 dict 则取内层（兼容两种 ckpt 格式）。L49 用字典推导：只保留有 `.numpy()` 方法的条目（即 tensor），`.float().numpy()` 转 float32 numpy；非张量条目（如 int/str/list 元数据）被静默丢弃。
- 调用: 被同文件 `scan`（L57）调用。不调用项目内函数。
- 置信度: 已确认

### S:scan  [L52-98]
- 类型: function
- 签名: `scan(run_dir, filt, topk)`
- 作用: 对一个 run 的全部相邻 ckpt 窗口，按 `filt` 子串筛选张量，统计每张量的"恰零位移窗口数"与中位相对位移，并给出策略侧（不含 filt）张量的整体冻结占比。
- 参数:
  - `run_dir` (str): run 目录
  - `filt` (str): 参数名子串过滤（默认 `"value"`，来自 main 的 `--filter`）
  - `topk` (int): 打印前 k 行
- 返回: dict `{"run": run_dir, "windows": nw, "rows": rows, "policy_frozen_frac": float|None}`；ctkpt 少于 2 个时返回 `None`（L55-56）
- 实现:
  - L53 取 ckpt 列表；L54-56 少于 2 个直接打印跳过并 `return None`。
  - L57 对每个 ckpt 调 `state_dict_of`，得到 `(step, sd)` 列表（`sds`）。
  - L58 `names` = 第一个 ckpt 里"名字含 `filt`" 且"名字不含 `num_batches`"的键（`num_batches` 是 BN 统计计数器，排除以免噪声参与）。
  - L59-73 对每个名字遍历窗口 `i = 0..len(sds)-2`：L64-66 若该键在两侧缺失或 shape 变化则 `continue`（**注意**：跳过的窗口不计数，因此 `len(rels)` 即该张量可用窗口数）；L67-69 `diff = b - a`，`not np.any(diff)` ⇒ `zeros += 1`（**逐元素全零**判定）；L70-71 分母为 `max(1e-12, ‖a‖)` 防除零，记 `‖diff‖/‖a‖`。L72-73 只把 `rels` 非空的张量收进 `rows`（`(键, 窗口数, 恰零数, 中位相对位移)`）。
  - L74-85 **对照口径**：`pol_names` = 名字**不含** `filt` 且不含 `num_batches` 的键（即"策略侧"），累计 `pol_tot` 与 `pol_zero`（同样用 `not np.any(b-a)`）。**注意**：这里的"策略侧"只是"名字不含 filter 子串"，与价值支路的真实归属无关（源码事实，L75 注释自称"策略侧"）。
  - L86-91 打印 run 名、窗口数、各 ckpt 步数、匹配张量数，以及对照冻结占比（浮点 3 位）。
  - L92-96 排序：按"恰零率降序"，恰零率相同再按键名字典序；打印前 `topk` 行，附标记 `全窗冻结`（`z == nwv`）或 `多数窗冻结`（`z*2 > nwv`），及中位相对位移（科学计数 `:.3e`）。
  - L97-98 返回 dict。
- 调用: 被同文件 `main`（L113）调用；调用 `load_ckpts`（L53）、`state_dict_of`（L57）。
- 置信度: 已确认

### S:main  [L101-122]
- 类型: function
- 签名: `main()`
- 作用: 对命令行给出的多个 run 目录逐个扫描，最后汇总打印每个 run 中"全窗冻结"的 value 路径张量名。
- 参数: 无（argparse：`runs` nargs="+"、`--filter` 默认 `"value"`、`--topk` type=int 默认 12，L102-106）
- 返回: 无（无 return 语句）
- 实现: L108-109 打印标题与判定规则（"位移恰为 0 ⟺ 梯度精确为 0"），`flush=True`。L110-117 遍历 `a.runs`：`os.path.isdir` 通过则调 `scan`，返回真值才收进 `allr`；否则打印 `[缺失]`。L118-122 汇总：每个 run 里 `z == nwv` 的键名集合（全窗冻结），打印数量与名单（`{frozen if frozen else ''}`；空列表打印空串）。
- 调用: 由 `if __name__ == "__main__":` 调用（L125-126）；用法见 docstring L10-12（在 `src/clasher_new` 下用相对路径调）。它调用本文件 `scan`。函数内不 import 项目模块。
- 置信度: 已确认

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/summarize_probe_v3_mono.py | 2 | 2 |
| scripts/diag_value_head.py | 1 | 1 |
| scripts/judge_probe_v4.py | 2 | 2 |
| scripts/bench_train_speed.py | 1 | 1 |
| scripts/_survey_groups.py | 2 | 2 |
| scripts/_forensics_cycling.py | 3 | 3 |
| scripts/value_displacement_scan.py | 4 | 4 |

COVERAGE_OK G032 files=7 symbols=15
