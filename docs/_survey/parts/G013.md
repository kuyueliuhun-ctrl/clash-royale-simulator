# 组 G013 文件级分析

## F:scripts/duel_search.py
- 行数: 636（已用 read offset/limit 分 4 段读完全文：1-160 / 161-320 / 321-480 / 481-636）
- 模块作用: 一个**离线对抗搜索脚本**（非库）：把敌方单卡/双卡按固定时间步预演成时间轴检查点，对我方整格部署位（224 个）与部署时机做笛卡尔积展开，逐组合跑完整引擎推演（`battle.BattleState` + `battle.Troop`），用加权损失排序输出「最优部署配置清单 + 部署点热图」，支持 1v1 / 2v1 / 2v2 三种场景，并用 `multiprocessing.Pool` 分批并行。依据：模块 docstring L2-23 与顶层结构（常量 + 推演函数 + search_* + main）。
- 模块级 docstring 首行: `泛化对抗搜索器：敌方行进 0.1 格评估 × 我方整格部署，求最小损失。`
- 主要导入: `io`, `sys`, `os`, `time`, `argparse`, `itertools`, `json`, `collections.defaultdict`；`battle as battle_mod`, `player as player_mod`, `core.Position`, `arena.TileGrid`（L24-40）；函数内局部导入 `copy`（L154，未使用）、`random`（L337，未使用）、`multiprocessing as mp`（L468）
- 关键模块级常量:
  - `DECK = ['Knight', 'Arrows', 'Fireball', 'Musketeer', 'Giant', 'Minions', 'MiniPekka', 'Skeletons']`（L42-43）— 所有推演里双方 `PlayerState` 共用的占位卡组，只影响玩家状态构造，不参与对抗（脚本另用 `Troop` 直接注入单位）。
  - `OWN_CELLS = _own_deploy_cells()`（L55）— 我方（player 0）合法部署整格列表；**实测长度 224**（复算：对 15×18=270 个候选格调用 `TileGrid().can_deploy_at(Position(x,y),0)`，y∈{0.5…14.5}、x∈{0.5…17.5}，得 224 个合法格）。
  - `ENEMY_START = (3.5, 18.0)`（L56）— 敌左桥头（P1 侧）固定出生点，注释写「敌左桥头（P1）」。
  - `HORIZON = 35.0`（L57）— 推演时长上限（秒），被 `enemy_timeline` / `run_duel*` 作为默认参数。
  - 另：L35 `sys.path.insert(0, r"E:/clash-royale-simulator-main/src/clasher_new")` — 硬编码 Windows 路径导入本仓库引擎模块。
- 顶层数据表/字典: 无顶层数据表；`DECK`（列表）与 `OWN_CELLS`（坐标元组列表）是仅有的两个模块级可变数据；其余「表」均为函数内局部（如 `e2_positions` 六格桥头附近部署位 L326-327/L409-410）与 `search_1v1` 的 `timing_meta` 字典（L508）。

### S:_own_deploy_cells  [L46-53]
- 类型: function
- 签名: `def _own_deploy_cells():`
- 作用: 枚举 player 0 的全部合法整格部署位（格子中心坐标）。
- 参数: 无
- 返回: `list[tuple[float, float]]` — 通过 `TileGrid.can_deploy_at` 检验的 (x, y) 列表。
- 实现: L47 新建 `TileGrid()`（未传 battle_state）；L49 外层循环 `y = i + 0.5`（i 取 0..14）共 15 行、L50 内层 `x = i + 0.5`（i 取 0..17）共 18 列，即 270 个候选格子中心；L51 用 `tg.can_deploy_at(Position(x, y), 0)` 判定（`arena.py:173` 的签名 `can_deploy_at(self, pos, player_id, battle_state=None, is_spell=False, spell_obj=None, is_building=False)`，此处只传前两参 ⇒ battle_state=None、非法术、非建筑，走 `get_deploy_zones(0, None)` 的分区判定 + 边界/阻挡格/塔格排除）；L52 通过者以普通元组 `(x, y)` 追加；L53 返回。实测返回 224 项。
- 调用: 仅在 L55 被模块级 `OWN_CELLS = _own_deploy_cells()` 调用一次；它调用 `arena.TileGrid` / `arena.TileGrid.can_deploy_at` / `core.Position`。
- 置信度: 已确认

### S:enemy_timeline  [L61-115]
- 类型: function
- 签名: `def enemy_timeline(enemy_card, start=None, horizon=HORIZON, dt=0.1):`
- 作用: 在一个只有该敌方单位的空场里预演该卡，按固定时间步 `dt` 记录 `(t, x, y, phase)` 时刻表。
- 参数:
  - `enemy_card`（无默认）: 敌方卡名（传给 `battle.Troop` 的 `card_name`）。
  - `start`（默认 `None`）: 出生坐标元组；为假值时用 `ENEMY_START`（L77 `start or ENEMY_START`）。
  - `horizon`（默认 `HORIZON`=35.0）: 预演时长（秒）。
  - `dt`（默认 0.1）: 记录间隔（秒）。
- 返回: `list[tuple[float, float, float, str]]` — 每项 `(t, x, y, phase)`；`phase` ∈ `jump_<holder._mk_jump['phase']>` / `river_jump` / `dead` / `walk`（L86-94）；坐标与 t 用 `round(...,3)` 截断（L95-96）。
- 实现: L73-75 构造 `BattleState(PlayerState(0, list(DECK), 10.0), PlayerState(1, list(DECK), 10.0), card_level=11)`；L76 先空推 `bs.step(0.033)`（让塔等初始实体就位）；L77-78 造 `Troop(bs.next_entity_id, Position(*(start or ENEMY_START)), 1, enemy_card, bs)`（player=1 = 敌方）并 `bs._spawn_entity(e)`（`battle.py:2603` 会重设 `entity.id = next_entity_id` 并自增，故传入 `bs.next_entity_id` 无冲突）。L82 `steps = int(horizon / dt)`（默认 350）；L83-96 每轮先把**当前**位置/阶段记录进 `timeline`（阶段判定优先级：`holder._mk_jump` 存在 ⇒ `jump_<phase>`；否则 `e.jumping_across_river` ⇒ `river_jump`；否则 `not e.is_alive` ⇒ `dead`；否则 `walk`），再用 L98-102 的内层 while 以 `min(remaining, 1/60)` 为子步把时间精确推进 `dt`（60Hz 子步，浮点余量 >1e-9 兜底），子步仅在 `e.is_alive` 时执行。L103-108 若单位已死，则用当前（不变）坐标把 `i+1..steps-1` 全部补录为 `dead` 并 break；L109-114 若 `e.position.y < 5.0`（注释：已推到塔后），则把剩余时刻以**末尾 phase** 补录到 horizon 并 break。docstring L62-72 说明为何用时间驱动而非位置采样：超骑起跳预备期位置不动但时间在走，0.1 格位置采样会漏掉 prep/air 窗口。
- 调用: 被 `enemy_path_checkpoints`(L120)、`search_2v2`(L317)、`search_2v1`(L397)、`search_1v1`(L487) 调用；它调用 `battle.BattleState` / `battle.BattleState.step` / `battle.Troop` / `BattleState._spawn_entity` / `player.PlayerState` / `core.Position`，并读取 `Troop` 的 `position.x/y`、`is_alive`、`jumping_across_river`、`entity_holder._mk_jump`。
- 置信度: 已确认

### S:enemy_path_checkpoints  [L118-126]
- 类型: function
- 签名: `def enemy_path_checkpoints(enemy_card, start=None, horizon=HORIZON, step=0.1):`
- 作用: 旧接口兼容层 —— 把时间轴变成「每个 0.1 格 y 首次出现时刻」的位置检查点。
- 参数:
  - `enemy_card`（无默认）: 敌方卡名，透传 `enemy_timeline`。
  - `start`（默认 `None`）: 出生坐标，透传。
  - `horizon`（默认 `HORIZON`）: 预演时长，透传。
  - `step`（默认 0.1）: **函数体内未被引用**（L120-126 无 `step` 出现）⇒ 该参数当前无作用，仅保留签名兼容。
- 返回: `list[tuple[float, float]]` — `(y_量化值, 首次到达该 y 的 t)` 按 y 升序。
- 实现: L120 调 `enemy_timeline(enemy_card, start, horizon)`（注意未透传 `dt`，用默认 0.1）；L121 `cps = {}`；L122-125 逐项把 `y` 量化为 `yt = round(y * 10) / 10`，用 `if yt not in cps` 只保留**首次**出现的 t（依赖时间轴按时间升序 ⇒ 首次即最早）；L126 `sorted(cps.items())` 按 y 排序返回。
- 调用: 全仓库（除本文件外）未检出调用方（`grep -rn "enemy_path_checkpoints"` 仅命中定义行 L118）；`enemy_timeline` 是其唯一被调用者入口。属遗留兼容函数。
- 置信度: 已确认（「无外部调用方」这一结论限于本仓库非 docs 文件范围）

### S:_loss_metrics  [L129-149]
- 类型: function
- 签名: `def _loss_metrics(bs, my_card, enemy_card, towers0, my_ent, enemy_ent):`
- 作用: 从**调用当下的** `bs.entities` 计算一次对抗的塔损/存活指标（未完成的辅助函数）。
- 参数:
  - `bs`: `battle.BattleState`，从 `bs.entities[3]/[4]/[6]` 取蓝方（player 0）塔、`[1]/[2]/[5]` 取红方（player 1）塔。
  - `my_card`（无默认）: **函数体内未被引用**。
  - `enemy_card`（无默认）: **函数体内未被引用**。
  - `towers0`: 映射 `tower_id -> 初始 hp`，供算我方塔损。
  - `my_ent`: 我方单位对象（可为 None），只读 `is_alive`。
  - `enemy_ent`: 敌方单位对象（可为 None），只读 `is_alive`。
- 返回: `dict`，键为 `my_tower_loss` / `enemy_tower_loss` / `my_unit_alive` / `enemy_unit_alive`（L144-149）。
- 实现: L131 取蓝方三塔（`bs.entities[3], [4], [6]`）；L132-133 `my_tower_loss = Σ(towers0[t.id] − (t.hp if t.is_alive else 0))`。L135 取红方三塔；L136 **当场**用当前 hp 构建 `et0 = {t.id: t.hp for t in etowers}`；L137 注释自述「etowers 的 hp 在克隆时已变，需传初始」，但 L138-141 的 `base = et0.get(t.id, t.hp)` 与 `enemy_tower_loss += base − (t.hp if t.is_alive else 0)` 用的就是同一份当前 hp ⇒ 塔存活时该项恒为 0，只有塔在**调用瞬间已死**（`is_alive` 为假 ⇒ 减 0）时才等于该塔当时的 hp；即 `enemy_tower_loss` 实际不是「本次对抗造成的敌塔损」。L142-143 统计双方单位 `is_alive`。L144-149 组 dict 返回。**注意**：其键名 `my_unit_alive`/`enemy_unit_alive` 与 `run_duel*` 的 `my_alive`/`enemy_alive` 不一致，而 `total_loss` 读的是 `r["my_alive"]` ⇒ 把本函数结果喂给 `total_loss` 会 `KeyError`。
- 调用: 全仓库（除本文件外）未检出调用方（`grep -rn "_loss_metrics"` 仅命中定义行 L129）⇒ 目前是死代码/未接线辅助函数。
- 置信度: 已确认（无调用方结论限于本仓库非 docs 文件范围；`enemy_tower_loss` 恒零的推导基于 L136-141 的自洽读法）

### S:run_duel  [L152-185]
- 类型: function
- 签名: `def run_duel(enemy_card, my_card, my_pos, deploy_at, horizon=HORIZON):`
- 作用: 敌单卡从固定桥头推进、我方在 `deploy_at` 秒于 `my_pos` 部署单卡的完整 1v1 推演。
- 参数:
  - `enemy_card`（无默认）: 敌方卡名。
  - `my_card`（无默认）: 我方卡名。
  - `my_pos`（无默认）: 我方部署坐标 `(x, y)`，`Position(*my_pos)` 展开。
  - `deploy_at`（无默认）: 我方部署时刻（秒），条件为 `t >= deploy_at`。
  - `horizon`（默认 `HORIZON`）: 推演上限（秒）。
- 返回: `dict`，键 `my_tower_loss` / `enemy_tower_loss` / `my_alive` / `enemy_alive`（L180-185）。
- 实现: L154 `import copy`（局部导入后**在函数体内未被使用**）；L155-157 构造 `BattleState`（双方 10.0 圣水、`card_level=11`）；L158 空推 0.033s；L159-161 在 `ENEMY_START` 生成 player=1 的 `Troop` 并 `_spawn_entity`；L162-163 分别快照我方三塔（3/4/6）与敌方三塔（1/2/5）的**初始 hp`；L164 `m = None`；L165-173 以 60Hz 循环 `int(horizon*60)` 次：`t = i/60`，当 `m is None and t >= deploy_at and e.is_alive` 时在 `my_pos` 生成 player=0 的我方单位（L167-170），然后 `bs.step(1/60)`；`e` 死且（`m is None` 或 `m` 死）时 break。L174-179 按初始快照算双方塔损（存活取 `t.hp`，死亡取 0）。L183-184 存活判定为 `m is not None and m.is_alive` / `e.is_alive`。
- 调用: 被 `_run_1v1_worker`(L459) 调用（即 `search_1v1` 的并行单元）；`_run_1v1_worker` 是本仓库内唯一检出调用方。它调用 `battle.BattleState` / `step` / `Troop` / `_spawn_entity` / `player.PlayerState` / `core.Position`。
- 置信度: 已确认

### S:run_duel_2v1  [L188-232]
- 类型: function
- 签名: `def run_duel_2v1(enemy1, enemy2, enemy2_pos, enemy2_at, my_card, my_pos, my_at, horizon=HORIZON):`
- 作用: 敌方双卡**异步**（第二张在指定时刻/位置补出）对我方单卡的推演。
- 参数:
  - `enemy1`（无默认）: 敌方首卡，固定在 `ENEMY_START` 出生。
  - `enemy2`（无默认）: 敌方第二卡。
  - `enemy2_pos`（无默认）: 第二张敌卡的部署坐标。
  - `enemy2_at`（无默认）: 第二张敌卡的部署时刻（秒）。
  - `my_card` / `my_pos` / `my_at`（无默认）: 我方卡名 / 部署坐标 / 部署时刻。
  - `horizon`（默认 `HORIZON`）: 推演上限。
- 返回: `dict`，键 `my_tower_loss` / `enemy_tower_loss` / `my_alive` / `enemy_alive`（L227-232）。
- 实现: L195-198 构造 `BattleState` 并空推 0.033s；L199-201 在 `ENEMY_START` 生成 `e1`（player=1）；L202-205 初始化 `e2 = None`、`m = None` 并快照双方塔初始 hp；L206-220 以 60Hz 循环：`e2` 未出且 `t >= enemy2_at` 且 `e1.is_alive` 时在 `enemy2_pos` 生成 player=1 的 `enemy2`（L208-211，note 第二张的出生**条件依赖 e1 仍存活**）；`m` 未出且 `t >= my_at` 且（`e1` 活或 `e2` 已出且活）时在 `my_pos` 生成 player=0 的我方卡（L212-215）；随后 `bs.step(1/60)`；L218-220 在 `e1` 与 `e2` 皆死且（`m` 未出或 `m` 死）时 break。L221-226 用初始快照算双塔损。L231 `enemy_alive` = `e1.is_alive or (e2 is not None and e2.is_alive)`。
- 调用: 被 `_run_2v1_worker`(L237) 调用（`search_2v1` 的并行单元），`_run_2v1_worker` 是仓库内唯一检出调用方；它调用引擎构造/步进 API（同上）。
- 置信度: 已确认

### S:_run_2v1_worker  [L235-238]
- 类型: function
- 签名: `def _run_2v1_worker(args):`
- 作用: `multiprocessing` 任务包装器：解包 7 元组、跑 `run_duel_2v1`、回带搜索坐标。
- 参数:
  - `args`（无默认）: 7 元组 `(enemy1, enemy2, e2_pos, e2_at, my_card, my_pos, my_at)`（L236 解包）。
- 返回: `tuple` `(my_pos, my_at, e2_pos, e2_at, r)`（L238），其中 `r` 是 `run_duel_2v1` 的 dict。
- 实现: L236 一元解包；L237 调 `run_duel_2v1(...)`（**不传 horizon** ⇒ 用默认 `HORIZON`）；L238 把输入坐标与结果拼成 5 元组返回，以便父进程在不共享状态的情况下重挂元数据。
- 调用: 被 `search_2v1` 以函数引用形式传给 `_pmap`（L431）或串行 `[_run_2v1_worker(t) for t in tasks]`（L433）。
- 置信度: 已确认

### S:run_duel_2v2  [L241-288]
- 类型: function
- 签名: `def run_duel_2v2(enemy1, enemy2, e2_pos, e2_at, my1, my2, my1_pos, my1_at, my2_pos, my2_at, horizon=HORIZON):`
- 作用: 敌方双卡异步 vs 我方双卡异步的推演（我方两张牌各自有独立时刻/位置）。
- 参数:
  - `enemy1`（无默认）: 敌方首卡，`ENEMY_START` 出生。
  - `enemy2` / `e2_pos` / `e2_at`（无默认）: 敌方第二卡名 / 坐标 / 时刻。
  - `my1` / `my1_pos` / `my1_at`（无默认）: 我方第一张卡名 / 坐标 / 时刻。
  - `my2` / `my2_pos` / `my2_at`（无默认）: 我方第二张卡名 / 坐标 / 时刻。
  - `horizon`（默认 `HORIZON`）: 推演上限。
- 返回: `dict`，键 `my_tower_loss` / `enemy_tower_loss` / `my_alive` / `enemy_alive`（L283-288），其中 `my_alive` 为两张我方卡任一存活。
- 实现: L244-250 构造 `BattleState`、空推、在 `ENEMY_START` 生成 `e1`；L251-255 初始化 `e2/m1/m2 = None` 并快照我方/敌方三塔初始 hp；L256-275 以 60Hz 循环：`e2` 在 `t >= e2_at` 且 `e1.is_alive` 时生成（L258-261）；`m1` 在 `t >= my1_at` 时生成（L262-265，**无存活前置条件**）；`m2` 在 `t >= my2_at` 时生成（L266-269，**同样无前置条件** ⇒ 即使敌方已全灭仍会按时刻刷出）；随后 `bs.step(1/60)`；L274-275 四方全死才 break。L276-281 用初始快照算塔损；L282 `my_alive = (m1 活) or (m2 活)`。
- 调用: 被 `_run_2v2_worker`(L293) 调用（`search_2v2` 的并行单元），为仓库内唯一检出调用方。
- 置信度: 已确认

### S:_run_2v2_worker  [L291-294]
- 类型: function
- 签名: `def _run_2v2_worker(args):`
- 作用: `multiprocessing` 任务包装器：解包 10 元组、跑 `run_duel_2v2`、回带 6 个搜索坐标。
- 参数:
  - `args`（无默认）: 10 元组 `(e1, e2, e2p, e2at, m1, m2, m1p, m1at, m2p, m2at)`（L292 一元解包）。
- 返回: `tuple` `(m1p, m1at, m2p, m2at, e2p, e2at, r)`（L294），`r` 为 `run_duel_2v2` 的 dict。
- 实现: L292 解包；L293 调 `run_duel_2v2`（不传 horizon ⇒ 默认 `HORIZON`）；L294 把 6 项坐标元数据与结果拼成 7 元组返回。
- 调用: 被 `search_2v2` 传给 `_pmap`（L353）或串行列表推导（L355）。
- 置信度: 已确认

### S:search_2v2  [L297-367]
- 类型: function
- 签名: `def search_2v2(enemy1, enemy2, my1, my2, workers=None, max_e2=None, max_my=None, guide_topk=None):`
- 作用: 2v2 组合搜索：用 1v1 热图挑 TopK 部署位，再把「双卡位置×双卡时机」的笛卡尔积展开成任务批量并行推演。
- 参数:
  - `enemy1` / `enemy2`（无默认）: 敌方两张卡名。
  - `my1` / `my2`（无默认）: 我方两张卡名（同点不同时也允许）。
  - `workers`（默认 `None`）: 进程数；`>1` 走 `_pmap`，否则串行。
  - `max_e2`（默认 `None`）: 敌方第二卡时机的抽稀上限，None ⇒ 4（L319-320）。
  - `max_my`（默认 `None`）: 我方时机抽稀上限，None ⇒ 4（L329-330）。
  - `guide_topk`（默认 `None`）: 热图引导的部署位个数，None ⇒ 16（L303-304）；为假值时不做热图引导、直接用 `OWN_CELLS`。
- 返回: `tuple` `(results, len(results), dt_elapsed)`（L367）；`results` 每项为含 `my1_pos/my1_at/my2_pos/my2_at/e2_pos/e2_at/my_tower_loss/enemy_tower_loss/my_alive/enemy_alive/loss` 的 dict（L359-366）。
- 实现: L303-316 热图引导：`try` 内跑 `search_1v1(enemy1, my1, max_timings=6, workers=workers)`、`heatmap(r1)`、按 loss 升序取前 `guide_topk` 个位置为 `top_pos`；异常时 `print("[warn] 热图引导失败，回退全位: ...")` 并 `top_pos = OWN_CELLS`（`_hm`、`n1` 取到但未再使用）。L317 取 `enemy1` 时间轴；L321-325 用非 `dead` 且 `t <= 8.0` 的时刻作为敌2时机，超过 `max_e2` 时按 `step = len/max_e2` 索引抽稀（取 `int(i*step)`）。L326-327 固定 6 个敌2部署位（桥头两侧，y=17.5/18.5 × x=3.5/8.5/14.5）。L337 `import random`（**未使用**）。L338-343 `my_combos` = `top_pos × top_pos × my_times × my_times` 四重笛卡尔积（同一位置同一时刻、以及 (p1,t1) 与 (p2,t2) 互换的重复组合都会保留 ⇒ 组合数含大量对称冗余）。L345-350 任务 = `e2_positions × e2_times × my_combos`，每项 10 元组。L351-356 计时并执行（并行 `_pmap` / 串行列表推导）。L357-366 把 worker 返回的 7 元组重挂为结果 dict，并按 `total_loss(r)` 填 `loss`。
- 调用: 被 `main()` 的 2v2 分支调用（L612-617，`max_e2=min(max_timings or 3, 3)`、`max_my=min(max_timings or 2, 2)`、`guide_topk=4`）。它调用 `search_1v1`、`heatmap`、`enemy_timeline`、`total_loss`、`_pmap`、`_run_2v2_worker`。
- 置信度: 已确认

### S:search_2v1  [L370-445]
- 类型: function
- 签名: `def search_2v1(enemy1, enemy2, my_card, my_at_grid=None, e2_timings=None, workers=None, max_e2=None, max_my=None, guide_topk=None):`
- 作用: 2v1 搜索：热图挑 TopK 我方部署位后，展开「敌2时机 × 敌2位置 × 我方(时机,位置)」任务并推演。
- 参数:
  - `enemy1` / `enemy2`（无默认）: 敌方两张卡名。
  - `my_card`（无默认）: 我方单卡卡名。
  - `my_at_grid`（默认 `None`）: 显式的我方 `(时刻, 位置)` 候选列表；None ⇒ 用「敌1时间轴早期时刻 × 热图 TopK 位置」自动生成（L412-420）。
  - `e2_timings`（默认 `None`）: 显式的敌2 `(t, (x,y))` 候选；None ⇒ 从敌1时间轴早期抽取（L398-405）。
  - `workers`（默认 `None`）: 进程数，`>1` 走 `_pmap`。
  - `max_e2`（默认 `None`）: 敌2时机抽稀上限（为假值则不做抽稀与截断，L403）。
  - `max_my`（默认 `None`）: 我方时刻抽稀上限（同上，L417）。
  - `guide_topk`（默认 `None`）: 热图引导部署位数，None ⇒ 32（L382-383）；为假值 ⇒ 用全部 `OWN_CELLS`。
- 返回: `tuple` `(results, len(results), dt_elapsed)`（L445）；`results` 每项含 `my_pos/my_at/e2_pos/e2_at/my_tower_loss/enemy_tower_loss/my_alive/enemy_alive/loss`（L437-444）。
- 实现: L382-395 与 `search_2v2` 同构的热图引导（`search_1v1(enemy1, my_card, max_timings=6, workers=workers)` → `heatmap` → TopK；异常回退 `OWN_CELLS` 并打 warn）。L397 取敌1时间轴。L398-405：`e2_timings` 为 None 时取非 `dead` 且 `t <= 8.0` 的 `(t, (x,y))`，**为空则回退到全部非 dead 时刻**（L401-402）；`max_e2` 为真值时按索引步长抽稀。L406-407 否则直接用调用方给的 `e2_timings`。L409-410 固定 6 个敌2部署位（与 2v2 相同坐标）。L412-420：`my_at_grid` 为 None 时取敌1时间轴非 dead 且 `t <= 8.0` 的时刻（为空回退全部非 dead），按 `max_my` 抽稀，再与 `top_pos` 做笛卡尔积得 `my_cands = [(t, pos)]`。L424-428 任务 = `e2_cands × e2_positions × my_cands`，每项 7 元组 `(enemy1, enemy2, e2p, e2_at, my_card, myp, my_at)`。L429-434 计时并执行（`_pmap` / 串行）。L435-444 重挂元数据 + `total_loss`。docstring L379 自述「Windows spawn 实测：>512 任务批量并行会 WinError，分小批（_pmap ≤256）」，但实际 `_pmap` 的 `BATCH = 256`（L470）⇒ 注释里的 512 与代码不一致（以代码 256 为准）。
- 调用: 被 `main()` 的 2v1 分支调用（L589-592，`max_e2=args.max_timings`、`max_my=args.max_timings`，未传 `guide_topk` ⇒ 默认 32）。它调用 `search_1v1`、`heatmap`、`enemy_timeline`、`total_loss`、`_pmap`、`_run_2v1_worker`。
- 置信度: 已确认

### S:total_loss  [L448-453]
- 类型: function
- 签名: `def total_loss(r, w_tower=1.0, w_unit=0.5, w_enemy_tower=1.5, w_elixir=0.0):`
- 作用: 把一次推演结果折算成待最小化的加权标量损失。
- 参数:
  - `r`（无默认）: `run_duel*` 返回的 dict，需含键 `my_alive` / `my_tower_loss` / `enemy_tower_loss`。
  - `w_tower`（默认 1.0）: 我方塔损权重。
  - `w_unit`（默认 0.5）: 我方单位阵亡惩罚权重。
  - `w_enemy_tower`（默认 1.5）: 敌塔损权重（作为**收益**从损失中减去）。
  - `w_elixir`（默认 0.0）: **函数体内未被引用**（L450-453 无 `w_elixir`）⇒ 圣水交换项当前未实现，尽管模块 docstring L9 宣称损失含「圣水交换×w_elixir」。
- 返回: `float` — `w_tower*my_tower_loss + w_unit*my_unit_pen − w_enemy_tower*enemy_tower_loss`。
- 实现: L450 `my_unit_pen = 0.0 if r["my_alive"] else 0.5`（注意此处**硬编码 0.5**，与参数 `w_unit` 无关，`w_unit` 在 L452 再乘一次 ⇒ 实际阵亡惩罚 = `w_unit * 0.5`）；L451-453 返回三项加权组合，敌塔损取负号（进攻越多损失越低）。
- 调用: 被 `search_2v1`(L443)、`search_2v2`(L365)、`search_1v1`(L518) 调用，全部用默认权重。
- 置信度: 已确认

### S:_run_1v1_worker  [L456-460]
- 类型: function
- 签名: `def _run_1v1_worker(args):`
- 作用: `multiprocessing` 任务包装器：解包 `(敌卡, 我卡, 位置, 时刻)` 并跑 `run_duel`。
- 参数:
  - `args`（无默认）: 4 元组 `(enemy_card, my_card, pos, dt)`（L458 解包，`dt` 在此语义为**部署时刻**）。
- 返回: `tuple` `(pos, dt, r)`（L460）。
- 实现: L458 解包；L459 调 `run_duel`（不传 horizon ⇒ 默认 `HORIZON`）；L460 返回坐标+结果。
- 调用: 被 `search_1v1` 传给 `_pmap`（L503）或串行执行（L505）。
- 置信度: 已确认

### S:_pmap  [L463-475]
- 类型: function
- 签名: `def _pmap(fn, tasks, workers):`
- 作用: 分批流式并行 map：把大任务列表切成小批喂给 `mp.Pool.map`，规避 Windows spawn 的管道/句柄限制。
- 参数:
  - `fn`（无默认）: 三处 `_run_*_worker` 之一（可 pickle 的顶层函数）。
  - `tasks`（无默认）: 任务元组列表。
  - `workers`（无默认）: 进程池大小。
- 返回: `list` — 按批顺序 `extend` 的结果列表（顺序与串行一致，因为逐批 map 保序）。
- 实现: L468 局部 `import multiprocessing as mp`；L469 `out = []`；L470 `BATCH = 256`；L471 `with mp.Pool(workers) as pool:`（进程池上下文管理器，退出即 terminate/join）；L472-474 以 256 步长切片，每批 `pool.map(fn, batch, chunksize=max(1, len(batch) // workers // 2))` 并把结果 extend 进 `out`；L475 返回。docstring L464-467 自述「单批 5376 任务 BrokenPipe/PermissionError，分小批（≤512）稳定」，与代码 `BATCH=256` 不一致（以代码为准）。
- 调用: 被 `search_1v1`(L503)、`search_2v1`(L431)、`search_2v2`(L353) 在 `workers > 1` 时调用。
- 置信度: 已确认

### S:search_1v1  [L478-520]
- 类型: function
- 签名: `def search_1v1(enemy_card, my_card, time_every=0.1, phases=None, max_timings=None, workers=None):`
- 作用: 1v1 全扫：敌1时间轴检查点 × 我方 224 个整格部署位的完整笛卡尔积推演。
- 参数:
  - `enemy_card` / `my_card`（无默认）: 敌我卡名。
  - `time_every`（默认 0.1）: 敌方时间轴采样间隔（秒），透传给 `enemy_timeline(dt=...)`。
  - `phases`（默认 `None`）: 阶段白名单前缀列表，如 `['jump_prep']`；None ⇒ 不过滤。
  - `max_timings`（默认 `None`）: 时间点数量上限；None 或假值 ⇒ 全部保留。
  - `workers`（默认 `None`）: 进程数；`>1` 走 `_pmap`，否则串行（docstring L485 写「None=CPU核数-2」，但函数体**不做该回退**，只判断真值 ⇒ None 即串行；核数回退只在 `main()` L554 等处做）。
- 返回: `tuple` `(results, len(results), dt_elapsed)`（L520）；`results` 每项含 `deploy_pos/deploy_at/enemy_pos/enemy_phase/my_tower_loss/enemy_tower_loss/my_alive/enemy_alive/loss`（L512-519）。
- 实现: L487 `tl = enemy_timeline(enemy_card, dt=time_every)`；L488-494 逐项过滤：`phases` 非空时要求 `any(phase.startswith(p) for p in phases)`，并**始终丢弃 `dead`** 阶段，保留 `(t, x, y, phase)`；L495-497 超过 `max_timings` 时按 `step = len/max_timings` 索引抽稀。L499-500 任务 = `itertools.product(timings, OWN_CELLS)` 展开为 4 元组 `(enemy_card, my_card, pos, dt)`（此处 `dt` 复用为**部署时刻 t**，命名与 `time_every` 无关）。L501-506 计时 + 并行/串行执行 + 计时。L508 `timing_meta = {dt: (ex, ey, phase)}` 把时刻映射回敌方位置与阶段（时刻为 0.1 的整数倍，`round(t,3)` 后无碰撞）。L509-519 逐条重挂 `deploy_pos/deploy_at/enemy_pos/enemy_phase` 并加 `loss=total_loss(r)`；L520 返回三元组。注意 L510 的循环变量名 `dt` 在解包 `(pos, dt, r)` 中即部署时刻。
- 调用: 被 `main()` 的 1v1 分支调用（L555-559）；并被 `search_2v1`(L387) 与 `search_2v2`(L308) 以 `max_timings=6` 调用作为热图引导来源。它调用 `enemy_timeline`、`_pmap`、`_run_1v1_worker`、`total_loss`。
- 置信度: 已确认

### S:heatmap  [L523-529]
- 类型: function
- 签名: `def heatmap(results):`
- 作用: 按部署位对结果损失取算术平均，得到部署点热图。
- 参数:
  - `results`（无默认）: `search_1v1` 风格结果列表，每项需含 `deploy_pos` 与 `loss`（L526-527）。
- 返回: `dict[tuple[float,float], float]` — 位置 → 该位置所有时机的平均 loss（L528-529）。
- 实现: L525 `defaultdict(list)`；L526-527 遍历把 `r["loss"]` 追加到 `hm[r["deploy_pos"]]`；L528-529 对每桶 `sum(v)/len(v)` 转成普通 dict 返回。无空桶风险（桶由键创建）。
- 调用: 被 `search_2v1`(L388)、`search_2v2`(L309) 用于挑 TopK 引导位；被 `main()` 的 1v1 分支调用打印最优/最差部署位（L571）。注意它只对 `search_1v1` 的结果可用（`search_2v1/2v2` 的结果键是 `my_pos`/`my1_pos`，直接喂会 `KeyError`）。
- 置信度: 已确认

### S:main  [L532-632]
- 类型: function
- 签名: `def main():`
- 作用: CLI 入口：解析参数、按 `--mode` 分派到 1v1/2v1/2v2 搜索，打印损失升序前 10 与热图，可选写 JSON。
- 参数: 无（全部经 `argparse`）
- 返回: 无显式返回值（`2v1` 缺 `--enemy2`、`2v2` 缺 `--enemy2/--mine2` 时提前 `return`，L587/L609）。
- 实现: L533 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")`（在 `main` 内重包装 stdout，注释 L33-34 说明是为避免 import 时污染调用方）；L534-545 定义参数：`--enemy`(默认 MegaKnight)、`--mine`(默认 Assassin)、`--mode`(choices 1v1/2v1/2v2，默认 1v1)、`--enemy2`/`--mine2`(默认 None)、`--time-every`(float, 0.1)、`--max-timings`(int, 30)、`--phases`(默认 None，逗号分隔)、`--out`、`--workers`(int, None)。L547-550 打印标题与 `len(OWN_CELLS)`、时间间隔/阶段过滤/上限的说明。L552-583 **1v1 分支**：`phases = args.phases.split(",")` 或 None；`workers = args.workers or (os.cpu_count() - 2 if os.cpu_count() else 4)`；调 `search_1v1`；打印 `扫 {n} 配置，耗时 {el:.1f}s（{n/el:.0f} 局/s）`（L560，`el` 为 0 时该式会 `ZeroDivisionError`）；按 `loss` 升序排序打印前 10（含部署位/时刻/敌方阶段/敌位/塔损/存活）；打印热图最优 10 与最差 5；`--out` 时把 `{"results":..., "heatmap": {"x,y": loss}}` 写 JSON（`ensure_ascii=False, indent=1`，L580-582）。L584-605 **2v1 分支**：缺 `--enemy2` 则提示并返回；调 `search_2v1(..., max_e2=args.max_timings, max_my=args.max_timings)`（未传 `guide_topk` ⇒ 32）；打印前 10（我方位置/时刻 + 敌2位置/时刻）；`--out` 写 `{"results": ...}`。L606-632 **2v2 分支**：缺 `--enemy2`/`--mine2` 则提示并返回；调 `search_2v2(..., max_e2=min(args.max_timings or 3, 3), max_my=min(args.max_timings or 2, 2), guide_topk=4)`；打印前 10（我1/我2/敌2 的位置与时刻）；`--out` 写 JSON。注意 `--mine2` 只在 2v2 分支被使用，1v1/2v1 分支忽略它。
- 调用: 仅在 `if __name__ == "__main__": main()`（L635-636）被调用；它调用 `search_1v1` / `search_2v1` / `search_2v2` / `heatmap` 与 `os.cpu_count`、`argparse`、`json`。
- 置信度: 已确认（`n/el` 在 `el==0` 时除零为按代码的直接推断）

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/duel_search.py | 17 | 17 |

COVERAGE_OK G013 files=1 symbols=17
