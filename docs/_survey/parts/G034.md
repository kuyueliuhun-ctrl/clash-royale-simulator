# 组 G034 文件级分析

> 说明：本组 11 个文件全部是**仓库根目录的薄包装脚本（wrapper）**，位于 `scripts/rl/` 下，职责统一为
> 「切到 `src/clasher_new` 目录 → 把该目录加进 `sys.path` → 用 `runpy.run_path(..., run_name="__main__")`
> 执行 `src/clasher_new/rl/<同名>.py`」。真实逻辑全部在 `src/clasher_new/rl/<同名>.py` 里。
>
> 行数口径：本文件写的是**用 read 工具实际读到的行数**（`run_league.py` = 17 行，其余 10 个 = 7 行）。
> 任务简报写的 18 行 / 8 行与实测不符（实测每份文件均比简报少 1 行），此处以源码实测为准。
>
> 已确认被调用关系（grep `scripts/rl`，全仓命中）：
> - `start_rl.bat:207` → `scripts/rl/selftest.py`；`start_rl.bat:276` 与 `:292` → `scripts/rl/run_league.py`；`start_rl.bat:284` 与 `:294` → `scripts/rl/dashboard.py`
> - `start_training.bat:163` → `scripts/rl/selftest.py`；`start_training.bat:195` → `scripts/rl/run_league.py`；`start_training.bat:203` → `scripts/rl/dashboard.py`
> - `src/clasher_new/rl/README.md:229` 文字说明「仓库根目录另有 `scripts/rl/*` 包装脚本，用法一致」
> - `src/clasher_new/rl/dashboard.py:22`、`src/clasher_new/rl/selftest.py:20` 的 docstring 提到本包装层
> - 未发现任何 `.py` 文件 import `scripts/rl/*`
>
> 全部 10 个被包装目标的存在性与行数已用 `wc -l` 逐个核实（`src/clasher_new/rl/` 下）：
> `run_league.py` 1375、`dashboard.py` 2365、`evaluate.py` 557、`export_replay.py` 66、`selftest.py` 4983、
> `train_baseline.py` 102、`train_bc.py` 131、`train_belief.py` 260、`train_exploiter.py` 137、
> `train_follower.py` 283、`train_prophet.py` 158。且 11 个目标各有 `if __name__ == "__main__":` 守卫
> （行号：1374 / 2364 / 526 / 65 / 4982 / 101 / 119 / 247 / 136 / 265 / 157，grep `__main__` 实测），
> 因此 `run_name="__main__"` 能正确触发其入口逻辑。

## F:scripts/rl/run_league.py
- 行数: 17
- 模块作用: `src/clasher_new/rl/run_league.py`（联赛/训练主入口）的仓库根目录包装脚本；把工作目录切到 `src/clasher_new` 并注入 `sys.path` 后，用 `runpy` 以 `__main__` 身份执行真实入口。文件头 docstring 显式警告必须用 `if __name__ == "__main__"` 保护，否则跨进程并行（spawn）的子进程会重新导入本文件并再次 runpy 执行，造成无限递归（L4-5）。
- 模块级 docstring 首行: `run_league 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: 无（`_SRC` 是 `main()` 内的局部变量，定义在 L10-11，不是模块级常量）
- 顶层数据表/字典: 无

### S:scripts/rl/run_league.py::main  [L9-14]
- 类型: function
- 签名: `def main():`
- 作用: 定位仓库内 `src/clasher_new` 目录，切换 cwd 与 `sys.path`，再以 `__main__` 身份执行其中真正的 `rl/run_league.py`。
- 参数: 无
- 返回: 无返回值（函数体内没有 `return`，靠 `runpy.run_path` 执行目标脚本；目标脚本是否退出进程由目标脚本自身决定）
- 实现: L10-11 用 `__file__` 的绝对路径取目录名，向上两级拼出 `../../src/clasher_new`，再经 `os.path.realpath` 规范化为 `_SRC`；L12 `os.chdir(_SRC)` 切到该目录（源码注释说明原因：`card_utils` 等模块以相对路径读 `gamedata.json`）；L13 `sys.path.insert(0, _SRC)` 把该目录置为最高优先级的导入根；L14 调用 `runpy.run_path(os.path.join(_SRC, "rl", "run_league.py"), run_name="__main__")`，即以 `__main__` 名字执行真实入口脚本。无循环、无异常捕获、无边界分支。
- 调用: 被 `start_rl.bat:276`（`start "CR-RL Training [%MODE%]" ... "%ROOT%scripts\rl\run_league.py" %TRAIN_ARGS%`）、`start_rl.bat:292`（回显的命令）与 `start_training.bat:195` 以 `python <本文件> <ARGS>` 形式启动；也被用户/文档直接命令行调用（`src/clasher_new/rl/README.md:229` 说明用法一致）。它自身调用 `os.path.realpath` / `os.path.join` / `os.path.dirname` / `os.path.abspath` / `os.chdir` / `sys.path.insert` / `runpy.run_path`。
- 置信度: 已确认

## F:scripts/rl/dashboard.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/dashboard.py`（RL 训练仪表盘/曲线服务）的仓库根目录包装脚本；在模块顶层（不属于任何函数）完成路径解析、切目录、注入 `sys.path`、执行真实入口。没有 `main()`、也没有 `if __name__ == "__main__"` 守卫——**该文件被 import 也会立即执行**（实现上等价于一次性脚本）。
- 模块级 docstring 首行: `dashboard 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(os.path.join(os.path.dirname(os.path.abspath(__file__)), "../../src/clasher_new"))`（L4）— 由本文件位置向上两级推出的 `src/clasher_new` 绝对真实路径，是后续 `chdir`/`sys.path`/`runpy` 的目标根
- 顶层数据表/字典: 无
- 符号说明: 本文件无函数/类定义（与简报一致），只有 L4-L7 四条模块级语句：L4 计算 `_SRC`；L5 `os.chdir(_SRC)`（无行内注释，但语义与 `evaluate.py:5` 相同）；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "dashboard.py"), run_name="__main__")`。因此「模块导入即执行」是本文件的实际行为。
- 调用: 被 `start_rl.bat:284`（`... "%ROOT%scripts\rl\dashboard.py" %DASH_ARGS% --port %PORT%`）、`start_rl.bat:294`（回显）与 `start_training.bat:203`（`... "%ROOT%scripts\rl\dashboard.py" --state=%STATE_JSON% --port %PORT%`）启动；`src/clasher_new/rl/dashboard.py:22` 的 docstring 反向提及本包装脚本。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/dashboard.py`（2365 行，含 `__main__` 守卫于 L2364）。
- 置信度: 已确认

## F:scripts/rl/evaluate.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/evaluate.py`（离线评估）的仓库根目录包装脚本；在模块顶层完成路径解析、切目录、注入 `sys.path`、执行真实入口。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `evaluate 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— 同上一文件的路径解析结果，`src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。模块级语句：L4 计算 `_SRC`；L5 `os.chdir(_SRC)`，行内注释写明原因是「`card_utils` 等以相对路径读 `gamedata.json`」；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "evaluate.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中任何调用本文件的 `.bat`/`.py`/`.md`（`start_rl.bat` / `start_training.bat` 只调用 selftest/run_league/dashboard 三者）⇒ 推断为供用户手工命令行使用（`src/clasher_new/rl/README.md:229` 的「`scripts/rl/*` 包装脚本」为泛化描述，未逐一点名）。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/evaluate.py`（557 行，`__main__` 守卫 L526）。「具体由哪个外部脚本调用」无法确认（原因：全仓无引用点，仅能确认无自动化调用方）。
- 置信度: 已确认（仅"谁调用它"为无引用点，已在上面写明）

## F:scripts/rl/export_replay.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/export_replay.py`（回放导出）的仓库根目录包装脚本；结构与其他 wrapper 完全一致，顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `export_replay 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 并注明 `card_utils` 相对路径读 `gamedata.json` 的原因；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "export_replay.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用（同 `evaluate.py`）⇒ 无已知自动化调用方，推断为手工使用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/export_replay.py`（66 行，`__main__` 守卫 L65）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/selftest.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/selftest.py`（97 项 RL 自检）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `selftest 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)`（**注意本文件 L5 无行内注释**，与 evaluate/export_replay/train_* 版本的注释不同）；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "selftest.py"), run_name="__main__")`。
- 调用: 被 `start_rl.bat:207`（`"%PY%" "%ROOT%scripts\rl\selftest.py"`，非 `start`，即前台阻塞执行）与 `start_training.bat:163`（同形式）调用；另 `src/clasher_new/rl/selftest.py:20` 的 docstring 写明「需在 `src/clasher_new` 下，或由 `scripts/rl/selftest.py` 包装」。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/selftest.py`（4983 行，`__main__` 守卫 L4982）。
- 置信度: 已确认

## F:scripts/rl/train_baseline.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_baseline.py`（基线行为克隆/监督训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_baseline 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_baseline.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用（`start_rl.bat` / `start_training.bat` 只走 `run_league.py`）⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_baseline.py`（102 行，`__main__` 守卫 L101）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/train_bc.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_bc.py`（行为克隆训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_bc 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_bc.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用 ⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_bc.py`（131 行，`__main__` 守卫 L119）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/train_belief.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_belief.py`（信念网络/belief 训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_belief 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_belief.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用 ⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_belief.py`（260 行，`__main__` 守卫 L247）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/train_exploiter.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_exploiter.py`（利用者/exploiter 训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_exploiter 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_exploiter.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用 ⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_exploiter.py`（137 行，`__main__` 守卫 L136）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/train_follower.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_follower.py`（跟随者/follower 训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_follower 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_follower.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用 ⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_follower.py`（283 行，`__main__` 守卫 L265）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## F:scripts/rl/train_prophet.py
- 行数: 7
- 模块作用: `src/clasher_new/rl/train_prophet.py`（先知/prophet 训练）的仓库根目录包装脚本；顶层执行。无函数/类定义，无 `__main__` 守卫。
- 模块级 docstring 首行: `train_prophet 入口（wrapper）。`
- 主要导入: os, sys, runpy
- 关键模块级常量: `_SRC = os.path.realpath(...)`（L4）— `src/clasher_new` 的绝对路径
- 顶层数据表/字典: 无
- 符号说明: 无函数/类定义（与简报一致）。L4 计算 `_SRC`；L5 `os.chdir(_SRC)` 带 `gamedata.json` 原因注释；L6 `sys.path.insert(0, _SRC)`；L7 `runpy.run_path(os.path.join(_SRC, "rl", "train_prophet.py"), run_name="__main__")`。
- 调用: 全仓 grep `scripts/rl` 未命中对本文件的引用 ⇒ 无已知自动化调用方，推断为手工调用。它调用 `runpy.run_path` 执行 `src/clasher_new/rl/train_prophet.py`（158 行，`__main__` 守卫 L157）。调用方无法确认（原因：全仓无引用点）。
- 置信度: 已确认（仅"谁调用它"为无引用点）

## 覆盖清单
| 文件 | 符号总数 | 已文档化 |
|---|---|---|
| scripts/rl/run_league.py | 1 | 1 |
| scripts/rl/dashboard.py | 0 | 0 |
| scripts/rl/evaluate.py | 0 | 0 |
| scripts/rl/export_replay.py | 0 | 0 |
| scripts/rl/selftest.py | 0 | 0 |
| scripts/rl/train_baseline.py | 0 | 0 |
| scripts/rl/train_bc.py | 0 | 0 |
| scripts/rl/train_belief.py | 0 | 0 |
| scripts/rl/train_exploiter.py | 0 | 0 |
| scripts/rl/train_follower.py | 0 | 0 |
| scripts/rl/train_prophet.py | 0 | 0 |

COVERAGE_OK G034 files=11 symbols=1
